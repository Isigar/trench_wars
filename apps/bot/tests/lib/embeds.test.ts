// Source: .planning/phases/05-discord-bot-v1/05-01-PLAN.md task 2 (Wave 0 RED stub).
// SC-1 / SC-3 — canonical EmbedBuilder factories: matchCard(), clanCard(),
// profileCard(). Snapshot test on the resulting embed.toJSON() shape to guard
// against accidental breakage of the embed contract.
// Replaced by plan 05-10 (bot components + embed builders wave).

import { describe, it } from 'vitest';

describe('embed builders', () => {
    it.todo('matchCard emits expected EmbedBuilder shape — plan 05-10');
    it.todo('clanCard emits expected EmbedBuilder shape — plan 05-10');
    it.todo('profileCard emits expected EmbedBuilder shape — plan 05-10');
});
