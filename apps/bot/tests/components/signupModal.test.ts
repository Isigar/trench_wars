// Source: .planning/phases/05-discord-bot-v1/05-01-PLAN.md task 2 (Wave 0 RED stub).
// SC-2 — /match signup modal: build modal with role picker dropdown from match's
// role list; submit handler POSTs to BotApiMatchSignupController::store; tag-restricted
// and capacity errors surface via bot.errors.* keys.
// Replaced by plan 05-10 (bot components wave).

import { describe, it } from 'vitest';

describe('/match signup modal', () => {
    it.todo('builds modal with role-picker dropdown from match roles — plan 05-10');
    it.todo('submit handler creates signup via BotApi — plan 05-10');
});
