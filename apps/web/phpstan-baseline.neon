# PHPStan baseline — empty until Filament/Laravel-internal level-8 findings surface.
# Regenerate ONLY with explicit user request via:
#   docker compose exec web ./vendor/bin/phpstan analyse --generate-baseline --memory-limit=2G
parameters:
    ignoreErrors: []
