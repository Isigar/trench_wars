<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — replaced by plan 07-11 (Inertia v2 SSR enable for public
| routes + Node ssr sidecar in docker-compose. Asserts bootstrap/ssr/ssr.mjs
| exists after vite build:ssr, and INERTIA_SSR_ENABLED config honoured).
| Source: .planning/phases/07-cms/07-01-PLAN.md task 2.
*/

it('placeholder for Inertia SSR bundle presence + config — replace via plan 07-11', function (): void {
    expect(true)->toBe(false);
});
