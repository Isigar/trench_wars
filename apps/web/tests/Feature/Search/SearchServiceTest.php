<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — replaced by plan 07-08 (Postgres FTS service —
| UNION'd articles + clans + players via tsvector @@ websearch_to_tsquery,
| ts_rank ordering, D-018 PlayerPrivacyGate applied to player rows).
| Source: .planning/phases/07-cms/07-01-PLAN.md task 2.
*/

it('placeholder for SearchService UNION + ranking — replace via plan 07-08', function (): void {
    expect(true)->toBe(false);
});
