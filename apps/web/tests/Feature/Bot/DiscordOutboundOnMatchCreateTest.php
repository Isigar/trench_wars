<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — flipped GREEN by plan 05-05 (MatchObserver outbound writer wave).
| Source: 05-01-PLAN.md task 2 + 05-VALIDATION.md Per-Plan Coverage Map.
| SC-3 — MatchObserver inserts a pending discord_outbound_messages row of type
| match_announce on GameMatch create (or status open transition) with the host
| clan's discord_announce_channel_id + payload containing the match embed shape
| (title, status, scheduled, RSVP buttons).
*/

it('placeholder — replace in plan 05-05', function (): void {
    $this->markTestIncomplete('Wave 0 RED stub — implementation in plan 05-05.');
});
