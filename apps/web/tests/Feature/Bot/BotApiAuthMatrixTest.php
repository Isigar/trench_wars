<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — flipped GREEN by plan 05-03 (ResolveBotActsAsUser + Sanctum abilities).
| Source: 05-01-PLAN.md task 2 + 05-VALIDATION.md Per-Plan Coverage Map.
| SC-5 — auth matrix: missing Authorization → 401, wrong ability → 403, no token →
| 401, token without bot:read for GET → 403; full coverage for /api/bot/* gate.
*/

it('placeholder — replace in plan 05-03', function (): void {
    $this->markTestIncomplete('Wave 0 RED stub — implementation in plan 05-03.');
});
