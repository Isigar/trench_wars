<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — replaced by plan 07-12 (D-012 — Article uses LogsActivity
| trait; admin audit log surfaces created/updated/status_changed events with
| causer = author user).
| Source: .planning/phases/07-cms/07-01-PLAN.md task 2.
*/

it('placeholder for Article audit log entries — replace via plan 07-12', function (): void {
    expect(true)->toBe(false);
});
