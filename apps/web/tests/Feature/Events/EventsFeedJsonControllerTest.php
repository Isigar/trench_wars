<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — replaced by plan 07-09 (EventsFeedController — JSON
| endpoint backing the FullCalendar fetch, UNION'd match + tournament +
| article events with start/end ISO timestamps).
| Source: .planning/phases/07-cms/07-01-PLAN.md task 2.
*/

it('placeholder for EventsFeedController JSON shape — replace via plan 07-09', function (): void {
    expect(true)->toBe(false);
});
