<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — replaced by plan 09-10 (static Vue template scan asserts
| every <input>/<select>/<textarea> in apps/web/resources/js has an associated
| <label for> or aria-label). Asserts intent of SC-5 (a11y).
|
| Source: .planning/phases/09-polish/09-01-PLAN.md task 1.
| Validation Architecture row (09-RESEARCH.md L1358): "All form inputs have associated labels (Vue template static scan)".
*/

test('Wave 0 stub: every Vue form input has an associated label or aria-label (static scan)', function (): void {
    expect(false)->toBeTrue('Wave 0 stub — implement in plan 09-10');
})->skip('Wave 0 stub — turned GREEN in plan 09-10');
