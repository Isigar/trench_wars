<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — replaced by plan 09-10 (root Blade `app.blade.php` emits
| `<html lang="{app.getLocale()}">` so every public Inertia page advertises its
| locale to screen readers). Asserts intent of SC-5 (a11y) AND D-013 (i18n).
|
| Source: .planning/phases/09-polish/09-01-PLAN.md task 1.
| Validation Architecture row (09-RESEARCH.md L1357): "All public pages render <html lang> correctly".
*/

test('Wave 0 stub: all public pages render <html lang="{locale}"> correctly', function (): void {
    expect(false)->toBeTrue('Wave 0 stub — implement in plan 09-10');
})->skip('Wave 0 stub — turned GREEN in plan 09-10');
