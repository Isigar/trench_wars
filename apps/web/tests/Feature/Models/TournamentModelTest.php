<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — replaced by plan 06-03 (Wave 2, Models).
| Source: .planning/phases/06-tournaments-brackets/06-01-PLAN.md task 2.
|
| Bare Pest functional convention (Phase 5 D-05-01-C canonical): no namespace,
| no per-file uses() call — Pest.php autowires TestCase + RefreshDatabase via
| uses(...)->in('Feature').
|
| Threat-mitigation T-03-01-01 carry-over: literal "placeholder" supports
| phase-close grep audit for un-replaced stubs.
*/

it('placeholder for Tournament model — replace via plan 06-03', function (): void {
    expect(true)->toBe(false);
});
