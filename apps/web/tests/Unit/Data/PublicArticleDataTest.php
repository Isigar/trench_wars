<?php

declare(strict_types=1);

/*
| Wave 0 RED stub — replaced by plan 07-03/07-05 (PublicArticleData DTO —
| spatie/laravel-data shape exported to packages/shared-types via the
| typescript-transformer; resolves translatable title/excerpt/body to the
| active locale, exposes hero_url + author_username with D-018 privacy gate).
| Source: .planning/phases/07-cms/07-01-PLAN.md task 2.
*/

it('placeholder for PublicArticleData DTO shape + TS export — replace via plan 07-03/07-05', function (): void {
    expect(true)->toBe(false);
});
