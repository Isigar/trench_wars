---
phase: 05-discord-bot-v1
plan: 13
type: execute
wave: 11
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12]
files_modified:
  - .planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REQ-goal-discord-ux
must_haves:
  truths:
    - "All 5 quality gates GREEN: make pest (web full suite), docker compose exec bot pnpm test (bot full suite), make pint --test, make phpstan, docker compose exec bot pnpm run typecheck"
    - "shared-types pipeline regressionless: pnpm --filter @trenchwars/shared-types typecheck via the appropriate host-side or container-side path"
    - "05-PHASE-VERIFICATION.md authored mapping SC-1..SC-5 + REQ-goal-discord-ux + 12 RESEARCH pitfalls + 5 open questions to concrete test files / commits + observable behaviours"
    - "ROADMAP.md Phase 5 entry updated: Plans 13/13 Complete + Completed date + plan list flips all 13 to [x]"
    - "REQUIREMENTS.md REQ-goal-discord-ux flipped from Pending -> Complete in the v1 traceability table"
    - "STATE.md updated: phase 4 -> 5 closed; total_plans + completed_plans bumped; performance metrics + accumulated decisions appended; session continuity field reflects phase 5 close"
    - "ROADMAP.md placeholder Phase 5 plan list (which incorrectly carried Phase 2 plan filenames per orchestrator note) is REPLACED with the real 13 plan filenames produced in this phase"
    - "Status flag PENDING_MANUAL_SMOKE for the live Discord guild interaction + outbound delivery + role sync + guildMemberUpdate manual checks per VALIDATION.md"
  artifacts:
    - path: ".planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md"
      provides: "Phase 5 close artifact with SC mapping + quality gate evidence"
      contains: "SC-1"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 marked Complete; real plan filenames replacing placeholder Phase 2 list"
      contains: "13/13 Complete"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-goal-discord-ux flipped to Complete in traceability table"
      contains: "REQ-goal-discord-ux | Phase 5 | Complete"
  key_links:
    - from: ".planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md"
      to: "ROADMAP.md SC-1..SC-5 + REQUIREMENTS REQ-goal-discord-ux"
      via: "traceability table at top of the artifact"
      pattern: "REQ-goal-discord-ux"
---

<objective>
Wave 11 (BLOCKING): the phase close artifact. Run all 5 quality gates, write 05-PHASE-VERIFICATION.md mapping every SC + every RESEARCH pitfall + every RESEARCH open question to concrete evidence (test file, commit hash, source file ref), update ROADMAP.md + REQUIREMENTS.md + STATE.md to reflect phase 5 completion, and emit the PENDING_MANUAL_SMOKE status for the live Discord guild verifications operator must perform out-of-band.

This plan is the canonical Phase 5 close — Phase 4 plan 04-13 is the precedent.

Quality gates (all MUST be GREEN before this plan ships):
1. `make pest` — full web Pest suite (Phase 1-4 regressionless + all Phase 5 GREEN)
2. `docker compose exec bot pnpm test` — full bot Vitest suite (all 9 Phase 5 stubs flipped GREEN)
3. `make pint ARGS="--test"` — Pint style check
4. `make phpstan` — Larastan L8
5. `docker compose exec bot pnpm run typecheck` — strict tsc

Plus the cross-package shared-types typecheck (Phase 4 plan 04-13 precedent — typically run host-side via corepack pnpm).

Output: 1 verification artifact + ROADMAP + REQUIREMENTS + STATE amendments. NO new test code; NO new source code. This is the documentation + accounting plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-discord-bot-v1/05-RESEARCH.md
@.planning/phases/05-discord-bot-v1/05-VALIDATION.md
@.planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md
@.planning/phases/04-matches-manual/04-13-PLAN.md
@.planning/phases/03-games-match-types/03-10-PLAN.md

<interfaces>
05-PHASE-VERIFICATION.md structure (Phase 4 plan 04-13 + Phase 3 plan 03-10 precedent):

```markdown
---
phase: 05-discord-bot-v1
status: Complete (PENDING_MANUAL_SMOKE)
completed: YYYY-MM-DD
plans_complete: 13
plans_total: 13
test_count: <number from pest output>
assertion_count: <number from pest output>
bot_test_count: <number from vitest output>
gates_passed:
  pest: true
  vitest: true
  pint: true
  phpstan: true
  tsc: true
  shared_types: true
requirements:
  - REQ-goal-discord-ux
manual_smoke_required:
  - Live Discord guild slash command roundtrip (SC-1)
  - Outbound delivery end-to-end (SC-3)
  - Role sync on clan join (SC-4)
  - guildMemberUpdate reconciliation (SC-4)
  - Sanctum bot:* token misuse rejection (SC-5)
---

# Phase 5: Discord bot v1 — Verification

## Overview

Phase 5 delivered: Sanctum-scoped bot↔web auth, durable outbox table for Discord side-effects, ResolveBotActsAsUser middleware for human causer attribution, Filament admin retry surface, plus the complete bot-side stack (discord.js v14.26 + slash commands + RSVP buttons + signup modal + outbound worker + guildMemberUpdate reconciler). REQ-goal-discord-ux: Complete.

## Quality Gates

| Gate | Command | Result |
|------|---------|--------|
| Pest (web) | `make pest` | GREEN (N tests / M assertions) |
| Vitest (bot) | `docker compose exec bot pnpm test` | GREEN (N tests) |
| Pint | `make pint ARGS="--test"` | clean |
| PHPStan L8 | `make phpstan` | clean |
| tsc strict (bot) | `docker compose exec bot pnpm run typecheck` | clean |
| shared-types typecheck | (host-side per plan 01-16 / 04-13 idiom) | clean |

## Success Criteria Traceability

| SC | Description | Evidence (test file + plan) |
|----|-------------|------------------------------|
| SC-1 | Slash commands /clan, /match, /profile, /me — privacy-aware within 3s (deferReply for slow paths) | apps/bot/tests/commands/{match,clan,profile}.test.ts (plan 05-09) + apps/web/tests/Feature/Bot/BotApiUserMeTest.php (plan 05-04) + manual Discord guild smoke (VALIDATION.md) |
| SC-2 | /match signup modal -> match_signups row visible on website; clan-role rules enforced | apps/bot/tests/components/signupModal.test.ts (plan 05-10) + apps/web/tests/Feature/Bot/BotApiMatchSignupTest.php (plan 05-04 — service reuse verified) |
| SC-3 | Match creation -> announce-channel embed + RSVP buttons + discord_outbound_messages durability | apps/web/tests/Feature/Bot/DiscordOutboundOnMatchCreateTest.php (plan 05-05) + apps/web/tests/Feature/Bot/BotApiOutboundClaimTest.php (plan 05-04 atomic claim) + apps/web/tests/Feature/Bot/BotApiOutboundAckTest.php (plan 05-04 sent/failed) + apps/bot/tests/services/outbound.test.ts (plan 05-11) + manual delivery smoke |
| SC-4 | Clan join/leave -> Horizon role sync + guildMemberUpdate reconcile | apps/web/tests/Feature/Bot/SyncDiscordRolesJobTest.php + SyncDiscordRolesJobDispatchTest.php (plan 05-06) + apps/bot/tests/events/guildMemberUpdate.test.ts (plan 05-11) + apps/web/tests/Feature/Bot/DiscordEventRoleChangeEchoSuppressionTest.php (plan 05-04 Pitfall 10) + manual role sync smoke |
| SC-5 | Sanctum bot:* + X-Bot-Acts-As-User -> human causer attribution | apps/web/tests/Feature/Bot/ResolveBotActsAsUserMiddlewareTest.php (plan 05-03) + apps/web/tests/Feature/Bot/BotApiAuthMatrixTest.php (plan 05-03) + apps/web/tests/Feature/Bot/MatchSignupViaBotCauserAttributionTest.php (plan 05-12 capstone) |

## RESEARCH Pitfall Coverage

| Pitfall | Description | Mitigation (file + plan) |
|---------|-------------|--------------------------|
| 1 | 3-second interaction window | apps/bot/src/commands/* all deferReply first (plan 05-09) + interactionCreate.ts modal-aware branching (plan 05-10) |
| 2 | Discord global rate limit 50 req/s | apps/web/app/Http/Controllers/BotApi/BotApiOutboundController.php limit=20 clamp (plan 05-04) |
| 3 | Sanctum token leakage | apps/bot/src/services/api.ts token scrub on error (plan 05-08) + Issue/RevokeBotTokenCommand rotation (plan 05-07) + .env.example empty WEB_API_TOKEN (plan 05-01) |
| 4 | Sanctum stateful guard bleed-through | plan 05-01 Pitfall 4 verification step + plan 05-03 config:show check |
| 5 | customId 100-char cap | apps/bot/src/lib/customIds.ts short prefix scheme + apps/bot/tests/lib/customIds.test.ts length assertion (plan 05-08) |
| 6 | discord.js Intents misconfig | apps/bot/src/client.ts GuildMembers intent (plan 05-08) + VALIDATION.md manual smoke check |
| 7 | ResolveBotActsAsUser swallowing system writes | plan 05-04 route grouping — bot.acts-as only on write-on-behalf endpoints |
| 8 | Horizon worker missing | docker-compose.yml worker service (plan 05-01) |
| 9 | PHP match reserved keyword | D-04-03-A LOCKED — all Phase 5 controllers use App\Models\GameMatch direct import (plan 05-04) |
| 10 | Echo loop via own guildMemberUpdate | apps/web/app/Http/Controllers/BotApi/BotApiDiscordEventController.php 60s window (plan 05-04) + apps/web/tests/Feature/Bot/DiscordEventRoleChangeEchoSuppressionTest.php |
| 11 | Modal submit 3s window | apps/bot/src/events/interactionCreate.ts isModalSubmit defer (plan 05-09 + 05-10) |
| 12 | Bot WEB_API_URL hostname | docker-compose.yml uses http://web-nginx (Phase 1 + plan 05-01) |

## RESEARCH Open Question Resolutions

| # | Question | Resolution | Plan |
|---|----------|-----------|------|
| Q1 | Channel resolution — who populates discord_announce_channel_id? | New column on clans table (plan 05-02 migration); admin sets via ClanResource Filament field (plan 05-07); MatchObserver reads on match create (plan 05-05) |
| Q2 | Acts-as-user when Discord user has never logged in | Fail with 422 + friendly message via __('bot.errors.acts_as_unknown'); user must log in at website first (plan 05-03) |
| Q3 | Filament retry action shape | Flip status=failed -> pending + zero attempts + clear backoff_until + clear last_error (plan 05-07) |
| Q4 | Bot service account User | Dedicated User row with discord_id='SYSTEM_BOT' username='Trenchwars Bot' email='bot@trenchwars.local'; provisioned via BotServiceUserSeeder + trenchwars:bot:issue-token Artisan (plan 05-07) |
| Q5 | /me vs /profile boundary | /me uses own-profile bypass; /profile v1 ships redirect-to-web stub (plan 05-09); future polish (Phase 9) adds viewer-aware /users/by-discord/{id} endpoint |

## Manual Smoke Checklist (PENDING_MANUAL_SMOKE)

Operator must verify out-of-band against a live Discord guild + production Railway environment:

A. Discord slash commands appear after first deploy + bot login (Pitfall 6 + Pattern 3)
   - In Discord, type `/` in the league guild; verify /clan, /match, /profile, /me appear
   - Invoke /match list; verify ephemeral reply within 3s

B. /match signup modal flow end-to-end
   - Invoke /match signup with a real match id
   - Verify modal opens; submit a real role UUID
   - Verify match_signups row appears in the website's /admin/matches/{id}/edit Slots tab

C. Match creation outbound delivery
   - Create a public match via Filament with the clan's announce channel set
   - Within ~10s, verify embed appears in the configured Discord channel
   - Verify discord_outbound_messages row exists with status=sent + sent_message_id populated

D. Role sync on clan join
   - Player joins clan via My Clan invite flow on website
   - Within ~30s, verify Discord role assigned in the league guild

E. guildMemberUpdate reconciliation
   - Admin manually removes a clan role from a user in Discord
   - Verify ClanMembership ended on website (left_at populated)

F. Sanctum token misuse rejected
   - curl /api/bot/matches/X/signups with the wrong abilities token (or no token) -> 403/401
   - curl with valid token but unknown X-Bot-Acts-As-User -> 422

Operator sign-off updates the verification status from PENDING_MANUAL_SMOKE -> Complete + writes the sign-off date.

## Performance Metrics (per STATE.md velocity table)

(Plan execution times to be appended after each plan ships.)

## Open Items Carrying Forward to Phase 6+

- /profile viewer-aware endpoint (RESEARCH Q5; deferred per plan 05-09)
- StringSelectMenu replacement for signup modal text input (plan 05-10 deferred to Phase 9 polish)
- /clan apply real implementation — currently redirect-to-web stub (plan 05-09 + RESEARCH Q2)
- Multi-replica bot deployment (Assumption A6; currently single-instance OK because web's lockForUpdate handles claim safety)
- i18n for bot responses — currently English only at v1 (CONTEXT.md deferred to Phase 7/9)
- Token rotation playbook — Artisan commands ship in plan 05-07; operator procedure documented; automation deferred to Phase 9

## Files Created / Modified Summary

(Output of `git log --name-status --since="<phase-5-start-date>"` filtered to the 13 commits)
```

ROADMAP.md amendments (3 surgical edits in the Phase 5 section):

1. The opening section ` - [ ] **Phase 5: Discord bot v1**` flips to `- [x]` with completion date inline
2. The "Plans: 14 plans" line stays accurate (this plan ships 13 — UPDATE the placeholder "14 plans" to "13 plans")
3. The placeholder plan list (currently carrying Phase 2 plan filenames per orchestrator note) is REPLACED with the real 13 filenames:
   - [x] 05-01-PLAN.md — Wave 0 scaffolding (Sanctum + Horizon + worker + bot deps + 20 stubs + i18n)
   - [x] 05-02-PLAN.md — Migrations (discord_outbound_messages + clans.discord_announce_channel_id)
   - [x] 05-03-PLAN.md — ResolveBotActsAsUser middleware + Sanctum abilities matrix
   - [x] 05-04-PLAN.md — BotApi controllers (clan/match/outbound/discord-events/users) + 4 FormRequests
   - [x] 05-05-PLAN.md — MatchObserver outbound writer + DiscordOutboundPayloadBuilder
   - [x] 05-06-PLAN.md — SyncDiscordRolesJob + ClanMembershipObserver (Horizon retry)
   - [x] 05-07-PLAN.md — DiscordOutboundMessageResource (Filament read-only) + retry action + Artisan token-issue/revoke + BotServiceUserSeeder + ClanResource amendment
   - [x] 05-08-PLAN.md — Bot core (env + client + api + customIds + colors + apiContracts)
   - [x] 05-09-PLAN.md — Slash commands (clan/match/profile/me) + registerCommands + interactionCreate dispatcher
   - [x] 05-10-PLAN.md — Embeds + buttons + rsvpButton + signupModal + match.ts embed upgrade
   - [x] 05-11-PLAN.md — Outbound worker + render + guildMemberUpdate reconciler
   - [x] 05-12-PLAN.md — i18n key coverage + audit log integration + SC-5 capstone test
   - [x] 05-13-PLAN.md — [BLOCKING] phase verification + ROADMAP/REQUIREMENTS/STATE updates

4. The Phase 5 row in the Progress table: `| 5. Discord bot v1 | 13/13 | Complete | YYYY-MM-DD |`

REQUIREMENTS.md amendment (single line in the Traceability table):

  | REQ-goal-discord-ux | Phase 5 | Pending |  ->  | REQ-goal-discord-ux | Phase 5 | Complete |

Plus update the Coverage section if the wording references "Complete" counts:
  - Old: "Mapped to phases: 15 of 15 mappable" — stays
  - "Last updated: 2026-05-13 ..." -> add new line: "Last updated: YYYY-MM-DD — Phase 5 close: REQ-goal-discord-ux flipped to Complete"

Plus update the active-requirements bullet for `REQ-goal-discord-ux`:
  - Old: `- [ ] **REQ-goal-discord-ux**: ...` -> `- [x] **REQ-goal-discord-ux**: ...`

STATE.md amendments:

  status: planning -> implementing (or appropriate state)
  stopped_at: "Phase 5 COMPLETE — 05-13 plan executed, 05-PHASE-VERIFICATION.md written, ROADMAP marked 13/13 Complete YYYY-MM-DD; ready for Phase 6 (Tournaments & brackets)."
  progress.completed_phases: 4 -> 5
  progress.total_plans: 55 -> 68 (55 + 13 plans this phase)
  progress.completed_plans: 55 -> 68
  progress.percent: 44 -> recompute (5/9 = 56)

Plus append:
  - Decisions table: Phase 5 plan-level decisions (D-05-01-A through D-05-13-A as applicable per each plan's SUMMARY)
  - Performance metrics: Phase 05 plan timings as captured from each plan's runtime
  - Session continuity: bump last_session timestamp + stopped_at reflects phase 5 close
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification artifact -> source of truth | Cross-references SCs to test files; if a test file is renamed/deleted later, the verification artifact is the audit trail |
| ROADMAP / REQUIREMENTS / STATE -> next-phase planner | Phase 6 planner reads these to understand "what's done" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13-01 | Repudiation | Verification artifact claims SC-5 done but no test actually covers it | mitigate | Direct cross-reference to MatchSignupViaBotCauserAttributionTest from plan 05-12; plan ships only if that test is GREEN |
| T-05-13-02 | Tampering | ROADMAP.md flip happens before tests pass | mitigate | All 5 quality gates verified BEFORE the ROADMAP edit; this plan's verify step gates on all 5 |
| T-05-13-03 | Repudiation | PENDING_MANUAL_SMOKE never resolved -> live Discord behaviour undefined | accept | Operator-owned out-of-band procedure; documented in verification artifact + VALIDATION.md |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run all 5 quality gates + collect counts</name>
  <files />
  <read_first>
    - .planning/phases/04-matches-manual/04-13-PLAN.md (Phase 4 close pattern — verbatim quality gate command list)
  </read_first>
  <acceptance_criteria>
    - make pest -> GREEN; captures total test count + assertion count + Phase 5 delta
    - docker compose exec bot pnpm test -> GREEN; captures total bot test count
    - make pint ARGS="--test" -> clean
    - make phpstan -> clean
    - docker compose exec bot pnpm run typecheck -> clean
    - host-side pnpm --filter @trenchwars/shared-types typecheck (or in-container equivalent per plan 01-16 idiom) -> clean
    - If ANY gate fails, return failure to orchestrator with the exact diagnostic; do NOT proceed to task 2
  </acceptance_criteria>
  <action>
    Run each command in order. Capture stdout. Each command MUST exit 0.

    Command sequence:
    1. make pest -> note the "Tests: N passed, M assertions" line
    2. docker compose exec bot pnpm test -> note the "Test Files N passed" + "Tests N passed" lines
    3. make pint ARGS="--test" -> ensure exit 0, no diff
    4. make phpstan -> ensure exit 0, no errors
    5. docker compose exec bot pnpm run typecheck -> ensure exit 0
    6. (host-side) pnpm --filter @trenchwars/shared-types typecheck (or alternate per plan 01-16 idiom) -> ensure exit 0

    If any command fails, ABORT this plan and surface the failure. No ROADMAP / REQUIREMENTS / STATE edits happen before gates are green.

    Record the test count + assertion count for task 2's verification artifact frontmatter.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --no-coverage 2>&amp;1 | tail -3 &amp;&amp; docker compose exec bot pnpm test 2>&amp;1 | tail -3 &amp;&amp; docker compose exec web ./vendor/bin/pint --test 2>&amp;1 | tail -3 &amp;&amp; docker compose exec web ./vendor/bin/phpstan analyse --no-progress 2>&amp;1 | tail -3 &amp;&amp; docker compose exec bot pnpm run typecheck 2>&amp;1 | tail -3</automated>
  </verify>
  <done>All 5 quality gates GREEN; counts captured for verification artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Write 05-PHASE-VERIFICATION.md + update ROADMAP.md + REQUIREMENTS.md + STATE.md</name>
  <files>
    .planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md (Phase 4 close artifact — verbatim structure)
    - .planning/ROADMAP.md (current state — particularly the Phase 5 section + its placeholder Phase 2 plan list per orchestrator note)
    - .planning/REQUIREMENTS.md (Traceability table + active-requirements bullet for REQ-goal-discord-ux)
    - .planning/STATE.md (progress + decisions + performance metrics + session continuity)
    - All 12 prior 05-NN-SUMMARY.md files (for decision-log entries to roll up into STATE)
  </read_first>
  <acceptance_criteria>
    - .planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md exists per `<interfaces>` structure:
      - Frontmatter with status=Complete (PENDING_MANUAL_SMOKE) + counts + gates_passed table
      - SC-1..SC-5 traceability table with evidence (test files + plan numbers)
      - 12 pitfalls coverage table
      - 5 open question resolutions
      - 6-item manual smoke checklist (A through F per VALIDATION.md analog)
      - Open items carrying forward
    - .planning/ROADMAP.md amended:
      - [ ] **Phase 5: Discord bot v1** -> [x] **Phase 5: Discord bot v1**
      - "Plans: 14 plans" -> "Plans: 13 plans"
      - Placeholder Phase 2 plan filenames REPLACED with the real 13 Phase 5 filenames per `<interfaces>`
      - All 13 plan entries marked [x]
      - Progress table row for Phase 5: 13/13 Complete + completion date
    - .planning/REQUIREMENTS.md amended:
      - Active-requirements bullet `- [ ] **REQ-goal-discord-ux**` -> `- [x] **REQ-goal-discord-ux**`
      - Traceability table row | REQ-goal-discord-ux | Phase 5 | Pending | -> Complete |
      - Footer last-updated date appended with Phase 5 close note
    - .planning/STATE.md amended:
      - progress: completed_phases 4 -> 5; total_plans + completed_plans bumped; percent recomputed
      - stopped_at: "Phase 5 COMPLETE — 05-13 plan executed ..." per `<interfaces>`
      - Decisions section appended with Phase 5 plan-level decisions (D-05-NN-* refs as documented in each 05-NN-SUMMARY)
      - Performance metrics appended with Phase 5 plan timings
      - Session continuity timestamp bumped
    - Verification: docker compose exec web cat .planning/ROADMAP.md | grep '5. Discord bot v1' | grep -c 'Complete' >= 1
  </acceptance_criteria>
  <action>
    Step 1 — Write the verification artifact verbatim from `<interfaces>` template. Fill in:
    - Date (today's date in YYYY-MM-DD format)
    - Test count + assertion count from task 1's captured output
    - Bot test count from task 1
    - File list from `git log --name-status --since="<phase-5-start>" --format=oneline` filtered to non-merge commits in this phase
    - Plan-level decision references from each 05-NN-SUMMARY.md

    Step 2 — Amend ROADMAP.md with the 4 surgical edits per `<interfaces>`. The placeholder Phase 2 plan filename issue (per orchestrator note) is the most critical correction — the current ROADMAP.md Phase 5 section incorrectly carries 02-01..02-14 filenames. Replace verbatim with the real 13 filenames.

    Step 3 — Amend REQUIREMENTS.md. Two surgical edits:
    - Active-requirements bullet [ ] -> [x] for REQ-goal-discord-ux
    - Traceability table cell Pending -> Complete
    - Optional: footer "Last updated" date stamp

    Step 4 — Amend STATE.md. Surgical edits to:
    - progress.completed_phases + total_plans + completed_plans + percent
    - stopped_at
    - status field if it tracks phase-level state
    - Appended decision entries from each 05-NN-SUMMARY (the executor will roll these up; planner here just notes the rollup expectation)
    - Appended performance metrics

    Run docker compose exec web cat .planning/ROADMAP.md | grep -A2 'Phase 5' to confirm the edit.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md &amp;&amp; grep -c 'Complete' .planning/ROADMAP.md | grep -v '^0$' &amp;&amp; grep -c 'REQ-goal-discord-ux | Phase 5 | Complete' .planning/REQUIREMENTS.md | grep -v '^0$' &amp;&amp; echo OK</automated>
  </verify>
  <done>05-PHASE-VERIFICATION.md authored; ROADMAP + REQUIREMENTS + STATE updated; phase 5 marked Complete (PENDING_MANUAL_SMOKE); all 13 plans flagged [x].</done>
</task>

</tasks>

<verification>
- All 5 quality gates GREEN (pest + vitest + pint + phpstan + tsc)
- .planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md exists + has the SC-1..SC-5 traceability table
- .planning/ROADMAP.md shows Phase 5: 13/13 Complete
- .planning/REQUIREMENTS.md REQ-goal-discord-ux status=Complete in traceability + [x] in active bullet
- .planning/STATE.md progress.completed_phases=5 + completed_plans=68 (or current correct value)
- git status clean except for the 4 modified planning files (no orphan code edits)
</verification>

<success_criteria>
Phase 5 closed. SC-1..SC-5 + REQ-goal-discord-ux + 12 RESEARCH pitfalls all traceable to concrete test/source evidence. PENDING_MANUAL_SMOKE flag set for operator's out-of-band live Discord verification. Phase 6 (Tournaments & brackets) is the next phase to plan.
</success_criteria>

<output>
After completion, create .planning/phases/05-discord-bot-v1/05-13-SUMMARY.md documenting:
- 5 quality gate results (counts captured)
- 13 plan close (all [x] in ROADMAP)
- REQ-goal-discord-ux flipped to Complete in REQUIREMENTS.md
- STATE.md progress + decisions + metrics updates
- 6-item manual smoke checklist for operator
- Forward-looking items carrying to Phase 6+
</output>
</content>
