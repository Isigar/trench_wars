---
phase: 11-tournament-depth
plan: 02
type: tdd
wave: 2
depends_on: ["11-01"]
files_modified:
  - apps/web/app/Services/EloRatingService.php
  - apps/web/app/Services/Standings/SwissStandingsCalculator.php
  - apps/web/tests/Feature/Services/EloRatingServiceTest.php
  - apps/web/tests/Feature/Services/SwissMedianBuchholzTest.php
autonomous: true
requirements: [TOUR-02, TOUR-03]

must_haves:
  truths:
    - "EloRatingService.applyResult moves both clans' ratings by the standard K=32 Elo delta, inside a DB transaction that row-locks both clan rows"
    - "A draw between equal-rated clans leaves both ratings unchanged; both clans' elo_matches_count increments on every applied result"
    - "Swiss standings carry median Buchholz (Buchholz Cut 1): opponents' scores minus the single highest and single lowest; for <3 opponents median == plain Buchholz"
    - "Swiss standings sort breaks ties points → Buchholz → median Buchholz → seed"
  artifacts:
    - path: "apps/web/app/Services/EloRatingService.php"
      provides: "stateless Clan Elo calculator"
      min_lines: 40
      contains: "applyResult"
    - path: "apps/web/app/Services/Standings/SwissStandingsCalculator.php"
      provides: "median Buchholz pass + third tiebreak + median_buchholz column write"
      contains: "median_buchholz"
  key_links:
    - from: "apps/web/app/Services/EloRatingService.php"
      to: "clans.elo_rating"
      via: "DB::transaction lockForUpdate on both clan rows"
      pattern: "lockForUpdate"
    - from: "apps/web/app/Services/Standings/SwissStandingsCalculator.php"
      to: "tournament_standings.median_buchholz"
      via: "TournamentStanding::create median_buchholz field"
      pattern: "median_buchholz"
---

<objective>
The pure-logic core of Phase 11. Two stateless, deterministic transforms with
known input→output, built test-first (RED scaffolds shipped by 11-01 turn GREEN
here):

1. EloRatingService (NEW, TOUR-02) — standard Elo, K=32, base 1500. Computes
   expected scores and applies the rating delta to both clans atomically.
2. Median Buchholz in SwissStandingsCalculator (TOUR-03) — Buchholz Cut 1, stored
   on the median_buchholz column, added as the third Swiss tiebreaker.

Neither touches BracketAdvancementService, seeding, or the materialiser — those
hooks land in 11-03/11-04. This plan keeps the math isolated so the GREEN cycle
is fast and the idempotency/integration concerns stay out of the unit surface.

Purpose: TOUR-02 needs a correct Elo engine before anything calls it; TOUR-03
needs the median value computed and persisted before the DTO/Vue can show it.
Output: 1 new service, 1 extended calculator, 2 GREEN test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-tournament-depth/11-CONTEXT.md
@.planning/phases/11-tournament-depth/11-PATTERNS.md

# Stateless-service analog (final class, declare(strict_types=1), no ctor injection)
@apps/web/app/Services/ClanSlugGenerator.php

# The calculator being extended (Buchholz pass at lines 150-159; sort at 173-184; create at 189-201)
@apps/web/app/Services/Standings/SwissStandingsCalculator.php

# Activity-log + DB::transaction idioms to reuse
@apps/web/app/Services/TournamentSeedingService.php
@apps/web/app/Models/Clan.php

<interfaces>
<!-- Contracts the executor implements + downstream callers (11-03) consume. -->

EloRatingService (NEW) — final class, App\Services namespace, no constructor:
  public function applyResult(\App\Models\Clan $winner, \App\Models\Clan $loser, bool $draw = false): void
  Constants: K = 32, BASE = 1500 (base only matters for new-clan default, set in migration).
  Formula: Ea = 1 / (1 + 10 ** (($Rb - $Ra) / 400)); winner score Sa = 1.0 (or 0.5 on draw),
           loser  score Sb = 0.0 (or 0.5 on draw); newRa = round($Ra + K*(Sa - Ea)).
  Must round to int and write via ->update(['elo_rating'=>..., 'elo_matches_count'=>...+1]).
  Wrap in DB::transaction; first statements re-fetch BOTH clans with lockForUpdate()->firstOrFail()
  (re-read inside the lock — do NOT trust the passed-in stale models for the math).
  This plan does NOT add the rated_at idempotency guard — that lives in the CALLER
  (BracketAdvancementService, plan 11-03). applyResult itself is unconditional.

SwissStandingsCalculator::compute(Tournament): void — existing signature, unchanged.
  Today it computes $buchholzByParticipant and inserts tiebreak_score = buchholz.
  Add a parallel $medianBuchholzByParticipant pass and a third usort tiebreaker.
</interfaces>
</context>

<feature>
  <name>EloRatingService — standard Clan Elo (K=32, base 1500)</name>
  <files>apps/web/app/Services/EloRatingService.php, apps/web/tests/Feature/Services/EloRatingServiceTest.php</files>
  <behavior>
    Cases (equal-rating 1500 vs 1500):
      - win:  winner 1500 → 1516, loser 1500 → 1484 (K*(1 - 0.5) = 16).
      - draw: winner 1500 → 1500, loser 1500 → 1500 (K*(0.5 - 0.5) = 0).
    Lopsided (favourite 1800 vs underdog 1200):
      - favourite wins → gains < 16 (small Ea≈0.97 → +~1); underdog loses → loses the same small amount.
      - underdog wins (passed as $winner) → gains > 16 (large delta).
    Side effects:
      - both clans' elo_matches_count += 1 on every applyResult (win OR draw).
      - the write happens under a DB::transaction with lockForUpdate on both rows.
    Determinism: integer rounding via round(); no float drift persisted.
  </behavior>
  <implementation>
    RED → GREEN → REFACTOR.

    RED: EloRatingServiceTest.php already exists from 11-01 (failing — service absent).
    Run it; confirm it fails because App\Services\EloRatingService is missing. If any
    asserted value in the scaffold is wrong, FIX THE TEST to the correct Elo math
    (1516/1484 for the equal win) — the test is the spec; commit the corrected RED.

    GREEN: create app/Services/EloRatingService.php following ClanSlugGenerator's shape
    (final class, declare(strict_types=1), namespace App\Services, no constructor).
    Implement applyResult per the <interfaces> formula. Inside DB::transaction:
      $w = Clan::query()->whereKey($winner->id)->lockForUpdate()->firstOrFail();
      $l = Clan::query()->whereKey($loser->id)->lockForUpdate()->firstOrFail();
    compute expected scores from the LOCKED ratings, derive Sa/Sb from $draw,
    update both rows (elo_rating rounded int, elo_matches_count incremented).
    Add an activity()->performedOn()->withProperties([... rating deltas ...])->log('Clan Elo updated')
    line mirroring TournamentSeedingService's activity idiom (causer = auth()->user() — may be null in
    the bracket-advance path; that is acceptable, system-attributed).

    REFACTOR: extract the expected-score helper if it improves readability; keep K and
    BASE as private const. Re-run the test green.

    Commit cadence: test(11-02) corrected RED → feat(11-02) EloRatingService GREEN.
  </implementation>
</feature>

<feature>
  <name>Median Buchholz (Cut 1) in SwissStandingsCalculator</name>
  <files>apps/web/app/Services/Standings/SwissStandingsCalculator.php, apps/web/tests/Feature/Services/SwissMedianBuchholzTest.php</files>
  <behavior>
    - With ≥3 opponents: median_buchholz = sum of opponent scores after dropping the single
      highest and single lowest (sort, array_shift, array_pop, array_sum).
    - With <3 opponents: median_buchholz == plain Buchholz (tiebreak_score) — nothing to drop.
    - The standings row persists median_buchholz; the existing tiebreak_score (plain Buchholz)
      is unchanged.
    - Sort order becomes points DESC → buchholz DESC → median_buchholz DESC → seed ASC; two
      participants identical on points+buchholz but differing on median order by median.
    - Non-swiss calculators are untouched (their TournamentStanding::create omits median_buchholz;
      the column default 0 covers them — no error).
  </behavior>
  <implementation>
    RED: SwissMedianBuchholzTest.php exists from 11-01 (failing — median_buchholz written as
    column-default 0). Run; confirm RED. Correct any scaffolded expected values to the true
    Cut-1 result for the fixture; commit corrected RED.

    GREEN: in SwissStandingsCalculator::compute(), AFTER the existing plain-Buchholz pass
    (the `$buchholzByParticipant` loop at lines ~150-159), add the parallel median pass per
    11-PATTERNS.md "Median Buchholz to add": for each participant collect opponent scores from
    $opponentsByParticipant + $pointsByParticipant; if count ≥ 3 sort + drop ends; median =
    array_sum. Add 'median_buchholz' => $medianBuchholzByParticipant[$p->id] to the $rows[] map
    and to the TournamentStanding::create() call. Add the third tiebreaker to the usort closure
    between the buchholz and seed comparisons (median_buchholz DESC).

    Use the participant id as the array key consistently (the existing code keys by $p->id).

    REFACTOR: none expected beyond keeping the two passes adjacent and commented.

    Commit cadence: test(11-02) corrected RED → feat(11-02) median Buchholz GREEN.
  </implementation>
</feature>

<verification>
- `make pest ARGS="tests/Feature/Services/EloRatingServiceTest.php"` — GREEN.
- `make pest ARGS="tests/Feature/Services/SwissMedianBuchholzTest.php"` — GREEN.
- `make pest ARGS="tests/Feature/Services/StandingsCalculatorServiceTest.php"` — existing Swiss standings test STILL GREEN (no plain-Buchholz / ranking regression).
- `make phpstan` — level 8 clean on both files.
- `make pint ARGS="--test"` — style gate green.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → EloRatingService | ratings are computed server-side from clan rows only; no client value is trusted |
| concurrent advances → clan rows | two simultaneous bracket results could race the same clan's rating |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering | clan elo_rating source | mitigate | rating delta is derived only from the two locked clan rows + the win/draw flag passed by the trusted bracket path; no request-supplied rating ever reaches applyResult |
| T-11-02-02 | Integrity | concurrent applyResult on the same clan | mitigate | DB::transaction + lockForUpdate re-reads both clan rows inside the lock; the math uses locked values, serialising concurrent updates |
| T-11-02-03 | Repudiation | silent rating change | mitigate | activity()->log('Clan Elo updated') records the delta + causer for the audit trail |
| T-11-02-04 | Integrity | median Buchholz miscount for small N | mitigate | <3-opponent branch returns plain Buchholz unchanged; covered by an explicit small-N test case |
| T-11-02-SC | Tampering | package installs | accept | no installs — pure PHP service + calculator edit |
</threat_model>

<success_criteria>
- EloRatingService produces the exact K=32 deltas (1516/1484 equal win; 0 on equal draw); both elo_matches_count increment; writes are transaction-locked.
- median_buchholz computed (Cut 1), persisted, and used as the third Swiss tiebreaker; <3-opponent edge equals plain Buchholz.
- The two 11-01 RED scaffolds (Elo + median) are GREEN; the existing StandingsCalculatorServiceTest stays GREEN.
- PHPStan L8 + Pint green.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-depth/11-02-SUMMARY.md` when done.
</output>
