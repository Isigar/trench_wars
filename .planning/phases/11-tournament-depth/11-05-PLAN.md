---
phase: 11-tournament-depth
plan: 05
type: execute
wave: 4
depends_on: ["11-02", "11-03", "11-04"]
files_modified:
  - apps/web/resources/js/components/tournaments/StandingsTable.vue
  - apps/web/lang/en/tournaments.php
  - packages/shared-types/src/index.ts
  - .planning/phases/11-tournament-depth/11-PHASE-VERIFICATION.md
autonomous: true
requirements: [TOUR-01, TOUR-02, TOUR-03, TOUR-04]

must_haves:
  truths:
    - "The public Swiss standings table shows a Median Buchholz column alongside plain Buchholz"
    - "The Median Buchholz column appears only for Swiss tournaments (hidden for other formats)"
    - "Generated TS types include TournamentStandingData.median_buchholz and vue-tsc passes"
    - "The full web + bot quality-gate suite is green and all four TOUR requirements are traced to a passing test"
  artifacts:
    - path: "apps/web/resources/js/components/tournaments/StandingsTable.vue"
      provides: "median Buchholz column (swiss-only)"
      contains: "median_buchholz"
    - path: "apps/web/lang/en/tournaments.php"
      provides: "standings.tiebreak_median_buchholz label"
      contains: "tiebreak_median_buchholz"
    - path: ".planning/phases/11-tournament-depth/11-PHASE-VERIFICATION.md"
      provides: "SC + requirement traceability for TOUR-01..04"
      contains: "TOUR-01"
  key_links:
    - from: "apps/web/resources/js/components/tournaments/StandingsTable.vue"
      to: "TournamentStandingData.median_buchholz"
      via: "row.median_buchholz cell rendered when format === swiss"
      pattern: "median_buchholz"
---

<objective>
Surface median Buchholz on the public standings view, regenerate the shared TS
types so the DTO field reaches the frontend type system, and run the full
phase-close gate suite with TOUR-01..04 traceability.

This is the last wave: it can only run after the median value exists (11-02), the
auto-advance/Elo hooks land (11-03), and the materialiser/Filament override ships
(11-04), because the phase-close suite must exercise all of them green.

Purpose: TOUR-03's SC explicitly requires the tiebreaker "visible on the public
bracket view"; this plan delivers that pixel and closes the phase. Output: 1 Vue
edit, 1 lang key, regenerated shared-types, 1 verification doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-tournament-depth/11-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# The component being extended (tiebreakLabel at 24-28; Buchholz th at 64; td at 79)
@apps/web/resources/js/components/tournaments/StandingsTable.vue

# Standings lang keys (tiebreak_buchholz at line 208)
@apps/web/lang/en/tournaments.php

<interfaces>
<!-- The DTO field already exists after 11-02; the Vue reads it; shared-types must regen. -->

StandingsTable.vue props: { standings: TournamentStandingData[] | null; format: string }.
Today it renders a single tiebreak column (tiebreakLabel = Buchholz for swiss).
TournamentStandingData (post-11-02) carries median_buchholz: number.
Add a swiss-only second tiebreak column.

shared-types regen command (canonical, runs in the web container per D-021):
  php artisan typescript:transform   # regenerates resources/js/types/api.d.ts (App.Data.*)
Then sync to packages/shared-types per the project's sync mechanism (plan 01-15:
in-container artisan command + bind mount, or packages/shared-types/scripts/sync-types.sh).
vue-tsc gate: (cd apps/web && node_modules/.bin/vue-tsc --noEmit).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Median Buchholz column on StandingsTable.vue + lang key + shared-types regen</name>
  <read_first>
    - apps/web/resources/js/components/tournaments/StandingsTable.vue (full file — tiebreakLabel computed, thead/tbody structure)
    - apps/web/lang/en/tournaments.php lines 200-211 (standings keys)
    - .planning/STATE.md decisions on shared-types sync (plan 01-15: artisan typescript:transform + sync to packages/shared-types)
  </read_first>
  <files>
    apps/web/resources/js/components/tournaments/StandingsTable.vue,
    apps/web/lang/en/tournaments.php,
    packages/shared-types/src/index.ts
  </files>
  <action>
    StandingsTable.vue: add a swiss-only Median Buchholz column next to the existing Buchholz column.
      - Add `const showMedianBuchholz = computed<boolean>(() => props.format === 'swiss');` alongside tiebreakLabel.
      - In <thead>, after the existing tiebreak <th> (line 64), add
        `<th v-if="showMedianBuchholz" class="text-right px-3 py-2 font-semibold">{{ t('tournaments.standings.tiebreak_median_buchholz') }}</th>`.
      - In the <tbody> row, after the tiebreak_score <td> (line 79), add
        `<td v-if="showMedianBuchholz" class="px-3 py-2 text-right">{{ row.median_buchholz }}</td>`.
      Use the typed field row.median_buchholz directly (it exists on TournamentStandingData after
      shared-types regen — do NOT use an `as any` cast; the field is typed number). Keep the existing
      plain Buchholz column rendering tiebreak_score unchanged.

    lang/en/tournaments.php: add to the 'standings' array (after tiebreak_buchholz):
      'tiebreak_median_buchholz' => 'Median Buchholz',
    (keep all existing keys).

    Regenerate shared types so TournamentStandingData.median_buchholz reaches the TS surface:
      run `php artisan typescript:transform` inside the web container, then sync to
      packages/shared-types via the project mechanism (plan 01-15). Confirm
      packages/shared-types / resources/js/types/api.d.ts now declares median_buchholz on
      App.Data.TournamentStandingData. If packages/shared-types/src/index.ts needs no edit
      (it re-exports via ambient namespace alias), leave it; otherwise ensure the alias still resolves.
  </action>
  <verify>
    <automated>cd apps/web && docker compose -f ../../docker-compose.yml exec -T web php artisan typescript:transform 2>&1 | tail -3 && grep -rn "median_buchholz" resources/js/types/ 2>/dev/null | head -3 && node_modules/.bin/vue-tsc --noEmit 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - [ ] generated TS types declare median_buchholz on TournamentStandingData (grep finds it in resources/js/types)
    - [ ] StandingsTable.vue renders the median column only when format === 'swiss' (v-if guard)
    - [ ] row.median_buchholz used without `as any` (typed access)
    - [ ] tournaments.standings.tiebreak_median_buchholz label present
    - [ ] vue-tsc --noEmit passes (no type errors)
  </acceptance_criteria>
  <done>Public Swiss standings show Median Buchholz (swiss-only); TS types regenerated; vue-tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-close full gate suite + TOUR-01..04 traceability</name>
  <read_first>
    - .planning/REQUIREMENTS.md (TOUR-01..04 + Traceability table)
    - .planning/ROADMAP.md (Phase 11 entry + 4 success criteria)
    - .planning/phases/10-clan-applications/10-07-* (prior phase-close SUMMARY/verification doc as the format template, if present)
  </read_first>
  <files>
    .planning/phases/11-tournament-depth/11-PHASE-VERIFICATION.md
  </files>
  <action>
    Run the FULL gate suite (Docker available) and record results in 11-PHASE-VERIFICATION.md:
      - make pest                 (full web suite — Phase 11 + all prior tests green, no regression)
      - make pint ARGS="--test"   (style gate)
      - make phpstan              (level 8)
      - make artisan ARGS="migrate:fresh --seed"   (schema + seed green from scratch)
      - (cd apps/web && node_modules/.bin/vue-tsc --noEmit)   (frontend types)
      - bot suite regressionless: (cd apps/bot && pnpm test) — expect green, NO bot changes this phase.
        If the bot test command differs, use the project's bot test script; the assertion is "bot suite unchanged + green".

    Write 11-PHASE-VERIFICATION.md mapping each ROADMAP SC + requirement to the proving test:
      - SC-1 / TOUR-01 (Swiss auto-advance) → SwissAutoAdvanceTest (11-03)
      - SC-2 / TOUR-02 (Elo by_rank seeding + Elo applied) → EloRatingServiceTest (11-02), EloAdvancementHookTest + TournamentSeedingServiceTest by_rank (11-03)
      - SC-3 / TOUR-03 (median Buchholz visible) → SwissMedianBuchholzTest (11-02) + StandingsTable median column (11-05)
      - SC-4 / TOUR-04 (stage match-type override) → StageMatchTypeOverrideTest + StagesRelationManagerOverrideTest (11-04)
    Include the idempotency evidence (double-fire Elo + double-fire round both asserted) and the
    all-1500 no-regression evidence (D-11-03-A) as explicit traceability rows. Record the final test
    counts + which gates passed. Status PENDING_MANUAL_SMOKE for any operator UI walkthrough (the
    Filament override Select + the public median column render) since automated coverage proves logic
    but not pixels.

    Update REQUIREMENTS.md Traceability: flip TOUR-01..04 from Pending → Complete. Update ROADMAP.md
    Phase 11 row: Plans 5/5, status Complete, completion date. (These doc edits land here as part of
    phase close; the orchestrator's update_roadmap step also runs but make the requirement-status flip
    explicit.)
  </action>
  <verify>
    <automated>cd /home/rtx/projects/trench-wars && docker compose exec -T web ./vendor/bin/pest 2>&1 | tail -8 && docker compose exec -T web ./vendor/bin/pint --test 2>&1 | tail -3 && docker compose exec -T web ./vendor/bin/phpstan analyse 2>&1 | tail -3 && test -f .planning/phases/11-tournament-depth/11-PHASE-VERIFICATION.md && grep -c "TOUR-0" .planning/phases/11-tournament-depth/11-PHASE-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] make pest full suite GREEN (Phase 11 + all prior; no regression)
    - [ ] make pint --test, make phpstan, vue-tsc --noEmit all GREEN
    - [ ] migrate:fresh --seed GREEN from scratch
    - [ ] bot suite GREEN and unchanged (no bot files modified this phase)
    - [ ] 11-PHASE-VERIFICATION.md maps each of TOUR-01..04 (and the idempotency + no-regression evidence) to a passing test
    - [ ] REQUIREMENTS.md TOUR-01..04 flipped to Complete; ROADMAP Phase 11 row updated to 5/5 Complete
  </acceptance_criteria>
  <done>Full web+bot gate suite green; every TOUR requirement traced to a passing test; phase docs updated to Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DTO → TS surface | regenerated types must match the runtime DTO or the public view breaks silently |
| public standings render | median Buchholz is public per D-018 — no privacy concern, but must not crash on null |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Information Disclosure | standings data on public view | accept | standings (incl. median Buchholz) are public per D-018; no PII exposed |
| T-11-05-02 | Integrity | TS types drift from runtime DTO | mitigate | typescript:transform regen + vue-tsc gate ensures the field exists and is typed before render |
| T-11-05-03 | Denial of Service | null median crashes the Vue render | mitigate | column gated by format==='swiss' (where the value is always written); numeric field defaults 0 at the DB layer so it is never null for swiss rows |
| T-11-05-SC | Tampering | package installs | accept | no installs — Vue + lang + type regen only |
</threat_model>

<verification>
- `make pest` full suite, `make pint ARGS="--test"`, `make phpstan`, `make artisan ARGS="migrate:fresh --seed"`, `vue-tsc --noEmit`, bot suite — all GREEN.
- grep confirms median_buchholz in generated TS types.
- 11-PHASE-VERIFICATION.md traces TOUR-01..04 to passing tests.
</verification>

<success_criteria>
- Public Swiss standings show Median Buchholz; types regenerated; vue-tsc green.
- Full web + bot gate suite green; no regression.
- TOUR-01..04 traced and flipped to Complete; ROADMAP/REQUIREMENTS updated.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-depth/11-05-SUMMARY.md` when done.
</output>
