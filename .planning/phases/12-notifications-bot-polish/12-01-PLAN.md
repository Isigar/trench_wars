---
phase: 12-notifications-bot-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/resources/js/components/UserMenu.vue
  - apps/web/lang/en/common.php
  - apps/web/tests/Feature/NotificationPreferencesHonorTest.php
autonomous: true
requirements: [NOTF-01]
must_haves:
  truths:
    - "A logged-in user can reach the notification-preferences page from the visible account/user menu (no URL typing)"
    - "Posting a preference change that disables a channel removes that channel from User::enabledNotificationChannels for that event type"
    - "The matching Notification's via() reflects the disabled channel (does not include it)"
  artifacts:
    - path: "apps/web/resources/js/components/UserMenu.vue"
      provides: "Notification-preferences nav link in the user dropdown with an i18n label"
      contains: "account/notification-preferences"
    - path: "apps/web/tests/Feature/NotificationPreferencesHonorTest.php"
      provides: "End-to-end honor test: POST pref change -> enabledNotificationChannels + Notification via() reflect it"
      contains: "enabledNotificationChannels"
  key_links:
    - from: "apps/web/resources/js/components/UserMenu.vue"
      to: "/account/notification-preferences"
      via: "anchor href in DropdownMenuItem"
      pattern: "account/notification-preferences"
---

<objective>
Make the already-built notification-preferences page discoverable and prove the full preference-honor loop end-to-end. NOTF-01's controller, routes, Vue matrix, and dispatcher wiring shipped in Phase 9 (09-06) and MUST NOT be rebuilt. This plan only (1) adds a visible nav link if one is missing, and (2) adds a Feature test proving a posted preference change is honored by `User::enabledNotificationChannels()` and the Notification `via()`.

Purpose: Closes NOTF-01's SC-1 — a user can find the page and the dispatcher honors changes "from that point forward".
Output: One nav link (UserMenu), one i18n key, one Feature test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notifications-bot-polish/12-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Existing, DO NOT rebuild. Extracted from codebase. -->

Routes (apps/web/routes/web.php:167-170, inside the auth group):
  account.notification-preferences.edit  -> GET  /account/notification-preferences
  account.notification-preferences.update -> POST /account/notification-preferences

apps/web/app/Models/User.php:251
  public function enabledNotificationChannels(string $eventType): array
  // Returns the channels (e.g. ['mail','discord','database']) enabled for $eventType,
  // honoring UserNotificationPreference rows (updateOrCreate keyed on user+event+channel)
  // with default-policy fallback. The 5 Notification classes' via() call this.

UserMenu.vue dropdown pattern (existing items "My Clan", "My Profile"):
  <DropdownMenuItem as-child>
    <a href="/my-clan" class="...">{{ t('common.nav.my_clan') }}</a>
  </DropdownMenuItem>

lang/en/common.php has a 'nav' array (keys: my_clan, my_profile, ...).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add notification-preferences link to the user dropdown</name>
  <read_first>
    - apps/web/resources/js/components/UserMenu.vue (existing "My Clan"/"My Profile" DropdownMenuItem anchors — copy that pattern exactly)
    - apps/web/lang/en/common.php (the 'nav' array — add the new key alongside my_clan/my_profile)
  </read_first>
  <files>apps/web/resources/js/components/UserMenu.vue, apps/web/lang/en/common.php</files>
  <action>
    First grep UserMenu.vue and any layout for an existing link to `account/notification-preferences` or route name `account.notification-preferences.edit`. If a visible link ALREADY exists, make NO Vue change and note "link already present" in the SUMMARY — only ensure the i18n key exists.
    If absent: add a new `DropdownMenuItem as-child` anchor in UserMenu.vue, placed above the divider/logout, mirroring the existing "My Clan" item exactly (same Tailwind classes, `as-child` wrapper, `<a href>`). Set `href="/account/notification-preferences"` and label `{{ t('common.nav.notification_preferences') }}`.
    Add `'notification_preferences' => 'Notification preferences'` to the `'nav'` array in lang/en/common.php. Per D-013 every UI string flows through t()/__(); no hardcoded label.
  </action>
  <verify>
    <automated>cd /home/rtx/projects/trench-wars/apps/web && grep -q "account/notification-preferences" resources/js/components/UserMenu.vue && grep -q "notification_preferences" lang/en/common.php && node_modules/.bin/vue-tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - UserMenu.vue contains an anchor to /account/notification-preferences using t('common.nav.notification_preferences')
    - common.php 'nav' array has the notification_preferences key
    - vue-tsc --noEmit passes
  </acceptance_criteria>
  <done>The notification-preferences page is reachable from the visible user dropdown with an i18n label.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: End-to-end notification-preference honor test</name>
  <read_first>
    - apps/web/app/Models/User.php (enabledNotificationChannels signature + the UserNotificationPreference relationship/keying)
    - apps/web/routes/web.php:167-170 (the update route name + payload shape the controller expects)
    - apps/web/app/Http/Controllers/Account/NotificationPreferencesController.php (the exact update() request keys: event types and channels, e.g. match_starting_soon × discord)
    - apps/web/tests/Pest.php (RefreshDatabase + auth helpers already wired)
    - one of the 5 Notification classes whose via() calls enabledNotificationChannels (pick the one for match_starting_soon)
  </read_first>
  <files>apps/web/tests/Feature/NotificationPreferencesHonorTest.php</files>
  <behavior>
    - Test 1 (honor via model): authenticate a user; assert enabledNotificationChannels('match_starting_soon') includes 'discord' by default; POST to account.notification-preferences.update disabling discord for match_starting_soon; re-resolve the user and assert enabledNotificationChannels('match_starting_soon') no longer includes 'discord'.
    - Test 2 (honor via Notification): with discord disabled for that event, instantiate the matching Notification and assert its via($user) does NOT include the discord/Discord channel, while a still-enabled channel remains.
  </behavior>
  <action>
    Write a Pest Feature test (NOT PHPUnit syntax — `it(...)`, `expect(...)`) in apps/web/tests/Feature/. Use the real update route via `$this->actingAs($user)->post(route('account.notification-preferences.update'), [...])` with the exact payload shape the controller's update() reads (match the request keys from the controller — do not invent a shape). Assert against `$user->fresh()->enabledNotificationChannels(...)` to defeat in-memory caching. For Test 2, construct the actual Notification class used for match_starting_soon and assert on `via($user->fresh())`.
    Do NOT modify the controller, routes, model, or Vue — this is verification only. If an equivalent integration assertion already exists in UserNotificationPreferencesTest, still add this dedicated end-to-end test that drives the HTTP route AND checks via() (the CONTEXT requires nailing the SC end-to-end).
  </action>
  <verify>
    <automated>cd /home/rtx/projects/trench-wars && docker compose exec -T web ./vendor/bin/pest --filter=NotificationPreferencesHonorTest</automated>
  </verify>
  <acceptance_criteria>
    - NotificationPreferencesHonorTest.php exists with two it() cases
    - make pest --filter=NotificationPreferencesHonorTest passes
    - Test drives the HTTP update route and asserts on fresh() enabledNotificationChannels and Notification via()
  </acceptance_criteria>
  <done>Posting a pref change that disables a channel is provably honored by both enabledNotificationChannels() and the Notification via().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> POST /account/notification-preferences | authenticated user submits preference changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-E | Elevation | NotificationPreferencesController::update | accept | Existing updateOrCreate is keyed on auth()->id() (Phase 9). This plan adds NO write path and MUST preserve that keying — a user can only edit their OWN prefs. No change to authorization. |
| T-12-01-T | Tampering | preference payload | accept | Controller already validates event-type/channel keys against the known matrix (Phase 9). No new input surface introduced. |
</threat_model>

<verification>
- vue-tsc --noEmit passes (Task 1)
- make pest --filter=NotificationPreferencesHonorTest passes (Task 2)
- make pint ARGS="--test" and make phpstan stay green (covered by phase-close plan 12-05)
</verification>

<success_criteria>
- Notification-preferences page reachable from the visible user dropdown with an i18n label
- End-to-end test proves a posted pref change is honored by enabledNotificationChannels() and Notification via()
- No rebuild of the existing controller/routes/Vue matrix
</success_criteria>

<output>
Create `.planning/phases/12-notifications-bot-polish/12-01-SUMMARY.md` when done
</output>
