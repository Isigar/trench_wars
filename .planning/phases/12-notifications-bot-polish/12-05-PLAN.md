---
phase: 12-notifications-bot-polish
plan: 05
type: execute
wave: 4
depends_on: [12-01, 12-02, 12-03, 12-04]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [NOTF-01, BOT-01]
must_haves:
  truths:
    - "The full web gate suite (pest, pint, phpstan, vue-tsc) passes"
    - "The full bot gate suite (vitest, tsc --noEmit, eslint) passes"
    - "NOTF-01 and BOT-01 are traced to their delivering plans and marked Met in REQUIREMENTS.md"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "NOTF-01 + BOT-01 marked Met with plan traceability"
      contains: "NOTF-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "12-01 / 12-02 / 12-03 / 12-04 SUMMARYs"
      via: "requirement-to-plan trace"
      pattern: "BOT-01"
---

<objective>
Phase 12 close-out: run the full web and bot gate suites green across all of this phase's changes, then trace NOTF-01 and BOT-01 to their delivering plans and mark them Met. This is the single phase-level verification gate (individual plans only run filtered/targeted checks).

Purpose: Confirms both SCs hold under the complete suite and records requirement traceability for the milestone.
Output: Green gates + updated REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-notifications-bot-polish/12-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full web + bot gate suites</name>
  <read_first>
    - CLAUDE.md §3 (Pint/PHPStan), §4 (Pest, container-only), §1 (Makefile aliases)
    - .planning/phases/12-notifications-bot-polish/12-01..04-SUMMARY.md (what each plan changed; note any deviations to re-verify)
  </read_first>
  <files>(verification only — no source edits unless a gate fails)</files>
  <action>
    Run the complete gate suites:
    - Web (container, per D-021): `make pest`, `make pint ARGS="--test"`, `make phpstan`, and `(cd apps/web && node_modules/.bin/vue-tsc --noEmit)`.
    - Bot (host): `cd apps/bot && node_modules/.bin/vitest run && node_modules/.bin/tsc --noEmit && node_modules/.bin/eslint .`.
    If any gate fails, fix the offending code (smallest change that satisfies the gate without weakening behavior) and re-run until all green. Do NOT regenerate the phpstan baseline (CLAUDE.md §3 — only on explicit user request). Do NOT touch the existing NOTF-01 controller/Vue matrix.
  </action>
  <verify>
    <automated>cd /home/rtx/projects/trench-wars && make pest && make pint ARGS="--test" && make phpstan && (cd apps/web && node_modules/.bin/vue-tsc --noEmit) && (cd apps/bot && node_modules/.bin/vitest run && node_modules/.bin/tsc --noEmit && node_modules/.bin/eslint .)</automated>
  </verify>
  <acceptance_criteria>
    - make pest passes (all suites, including NotificationPreferencesHonorTest)
    - make pint ARGS="--test" passes
    - make phpstan passes (no new violations, baseline untouched)
    - vue-tsc --noEmit passes
    - bot vitest + tsc --noEmit + eslint all pass
  </acceptance_criteria>
  <done>Full web and bot gate suites are green across all Phase 12 changes.</done>
</task>

<task type="auto">
  <name>Task 2: Trace NOTF-01 + BOT-01 and mark Met</name>
  <read_first>
    - .planning/REQUIREMENTS.md (the NOTF-01 / BOT-01 rows + the trace table at lines ~86-87; follow the existing Met/Pending format used by Phases 10-11)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    Update REQUIREMENTS.md: check off the NOTF-01 and BOT-01 checkboxes and update their status from Pending to Met in the trace table, citing the delivering plans:
    - NOTF-01 -> 12-01 (nav discoverability + end-to-end honor test; matrix/controller/dispatcher pre-built in 09-06).
    - BOT-01 -> 12-02 (list_page customId + Prev/Next factory), 12-03 (command render), 12-04 (handler in-message update).
    Match the exact column/format Phases 10 and 11 used for their Met rows. Do not invent new columns.
  </action>
  <verify>
    <automated>cd /home/rtx/projects/trench-wars && grep -Eiq "NOTF-01.*(Met|12-01)" .planning/REQUIREMENTS.md && grep -Eiq "BOT-01.*(Met|12-0[234])" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - NOTF-01 row marked Met and traced to 12-01
    - BOT-01 row marked Met and traced to 12-02/03/04
    - Format matches the existing Phase 10/11 Met rows
  </acceptance_criteria>
  <done>Both phase requirements are traced to their plans and marked Met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Verification + documentation only; no runtime trust boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05-N | n/a | gate run + REQUIREMENTS.md edit | accept | No code behavior changes; gates are the safety net. Threats for actual behavior covered in 12-01..04 registers. |
</threat_model>

<verification>
- All web gates green: pest, pint --test, phpstan, vue-tsc --noEmit
- All bot gates green: vitest run, tsc --noEmit, eslint
- NOTF-01 + BOT-01 marked Met with plan traceability
</verification>

<success_criteria>
- Full web + bot suites pass across the complete phase
- Both SCs hold (preferences honored end-to-end; in-message pagination works)
- REQUIREMENTS.md reflects NOTF-01 + BOT-01 as Met
</success_criteria>

<output>
Create `.planning/phases/12-notifications-bot-polish/12-05-SUMMARY.md` when done
</output>
