---
phase: 12-notifications-bot-polish
plan: 04
type: tdd
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - apps/bot/src/components/rsvpButton.ts
  - apps/bot/src/events/interactionCreate.ts
  - apps/bot/tests/components/rsvpButton.test.ts
autonomous: true
requirements: [BOT-01]
must_haves:
  truths:
    - "Clicking Prev/Next re-fetches the requested page and updates the SAME message (interaction.update), not a new reply"
    - "list_page buttons are NOT pre-deferred by the dispatcher (update() is the initial response, like m:o:)"
    - "The updated message shows the new page's items + refreshed Prev/Next disabled-bounds + new 'Page X of Y'"
  artifacts:
    - path: "apps/bot/src/components/rsvpButton.ts"
      provides: "list_page button branch that re-fetches and interaction.update()s the same message"
      contains: "list_page"
    - path: "apps/bot/src/events/interactionCreate.ts"
      provides: "pg: prefix routed without pre-deferReply"
      contains: "pg:"
  key_links:
    - from: "apps/bot/src/components/rsvpButton.ts"
      to: "apps/bot/src/commands (renderMatchListPage / renderClanListPage)"
      via: "shared page-render builder reused by the handler"
      pattern: "render(Match|Clan)ListPage"
    - from: "apps/bot/src/events/interactionCreate.ts"
      to: "handleButton"
      via: "pg: branch invoked before the generic deferReply"
      pattern: "startsWith\\('pg:'\\)"
---

<objective>
Complete BOT-01: handle Prev/Next clicks by re-fetching the requested page and calling `interaction.update()` to mutate the SAME message in place (new items, refreshed bound-aware buttons, new page indicator). Route `pg:` buttons through the dispatcher WITHOUT the generic ephemeral pre-defer, because `update()` is itself the initial interaction response.

Purpose: Delivers BOT-01 SC-2 — "navigate to subsequent pages using interactive controls in the same message".
Output: list_page branch in the button handler, pg: routing in interactionCreate, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-notifications-bot-polish/12-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contracts from 12-02 + 12-03 + existing dispatch. -->

apps/bot/src/lib/customIds.ts (from 12-02):
  decodeButtonId('pg:m:3') -> { kind: 'list_page', listType: 'match', page: 3 }

apps/bot/src/commands/*.ts (from 12-03):
  a shared page-render builder per list, e.g. renderMatchListPage(page) / renderClanListPage(page)
  returning the editable message payload { embeds?, content?, components } — REUSE these in the handler so
  click-render and command-render are identical.

apps/bot/src/components/rsvpButton.ts (existing handler shape):
  export async function handle(interaction: ButtonInteraction): Promise<void> {
    const decoded = decodeButtonId(interaction.customId);
    if (!decoded) { /* "Unknown button." */ return; }
    if (decoded.kind === 'match_open_signup_modal') { ... showModal ... return; }
    if (decoded.kind === 'match_signup') { ... editReply ... return; }   // deferred by dispatcher
    if (decoded.kind === 'match_leave') { ... }
    if (decoded.kind === 'clan_apply') { ... }
  }

apps/bot/src/events/interactionCreate.ts (existing button routing):
  if (interaction.isButton()) {
    if (interaction.customId.startsWith('m:o:')) {   // modal buttons: NO pre-defer
      await handleButton(interaction); return;
    }
    await interaction.deferReply({ flags: MessageFlags.Ephemeral });   // everything else pre-deferred
    await handleButton(interaction); return;
  }
  // KEY: list_page must join the m:o: branch (no pre-defer) because interaction.update() is the response.
</interfaces>
</context>

<feature>
  <name>list_page button handler + non-deferred pg: routing</name>
  <files>apps/bot/src/events/interactionCreate.ts, apps/bot/src/components/rsvpButton.ts, apps/bot/tests/components/rsvpButton.test.ts</files>
  <behavior>
    interactionCreate routing (assert via existing dispatch test or add coverage in rsvpButton.test.ts as appropriate):
    - a button whose customId starts with 'pg:' is routed to handleButton WITHOUT a prior interaction.deferReply (mirrors the m:o: branch)
    - non-pg buttons still get the ephemeral deferReply (regression guard)

    rsvpButton.handle list_page branch (tests/components/rsvpButton.test.ts):
    - given customId 'pg:m:2', handle() re-fetches page 2 for matches and calls interaction.update() (NOT interaction.reply / editReply) with the rebuilt payload (embeds + components for page 2)
    - given customId 'pg:c:3', handle() re-fetches clan page 3 and interaction.update()s with clan page 3 payload
    - the update payload's components reflect the NEW page's bounds (e.g. page 2 of 2 -> Next disabled)
    - malformed pg ids already return null from decode -> existing "Unknown button" path (no crash)
  </behavior>
  <implementation>
    RED first: extend tests/components/rsvpButton.test.ts — mock decodeButtonId path via real customId 'pg:m:2', mock the shared renderMatchListPage / api.get to return a known page-2 payload, and assert interaction.update() was called with that payload (and that reply/editReply were NOT). Add a routing assertion for the 'pg:' no-defer branch. Run vitest, confirm failing. Commit `test(12-04): list_page handler updates same message + pg routing`.

    GREEN:
    - interactionCreate.ts: in the isButton() block, extend the no-pre-defer condition to also match 'pg:' — i.e. `if (interaction.customId.startsWith('m:o:') || interaction.customId.startsWith('pg:')) { await handleButton(interaction); return; }`. Leave the ephemeral deferReply path for all other buttons unchanged.
    - rsvpButton.ts: add a branch `if (decoded.kind === 'list_page') { ... }` BEFORE the deferred-only branches. Build the new page payload by calling the shared renderer from 12-03 (renderMatchListPage(decoded.page) when listType==='match', renderClanListPage(decoded.page) when 'clan'); forward actsAsDiscordId = interaction.user.id on the API fetch (per-user attribution, same as other branches). Then `await interaction.update(payload)` to mutate the SAME message in place — do NOT reply or editReply (those create/replace ephemeral responses; update() edits the original component message). Wrap the fetch in try/catch using the existing translateError pattern; on error, update() the message with the error text (or follow the existing handler's error convention) without leaving the interaction unacknowledged.
    Run vitest green; tsc --noEmit; eslint. Commit `feat(12-04): list_page handler re-fetches page and updates the same message`.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord Prev/Next click -> bot handler -> web GET ?page=N | a user-clicked button carries a page integer that drives an API fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-04-T | Tampering | crafted pg: customId | mitigate | decodeButtonId (12-02) strictly validates arity + listType + positive-integer page; a malformed/crafted pg id decodes to null and hits the existing "Unknown button" path. customId carries only list-type + page — no user id, no secret. |
| T-12-04-D | DoS | page integer in fetch | mitigate | Even a crafted in-range integer only re-runs a public, paginate-bounded list query (web clamps limit; out-of-range page -> empty data). No write, no amplification. actsAsDiscordId is the clicker's own id. |
| T-12-04-S | Spoofing | actsAsDiscordId | accept | Forwarded as interaction.user.id from Discord's verified interaction, same trust model as existing rsvp branches. |
</threat_model>

<verification>
- cd apps/bot && node_modules/.bin/vitest run (rsvpButton + dispatch specs green)
- cd apps/bot && node_modules/.bin/tsc --noEmit
- cd apps/bot && node_modules/.bin/eslint .
</verification>

<success_criteria>
- Prev/Next re-fetches the requested page and interaction.update()s the SAME message (not a new reply)
- pg: buttons bypass the ephemeral pre-defer; non-pg buttons unaffected
- Updated message shows new page items + refreshed bounds + new page indicator; full bot suite green
</success_criteria>

<output>
Create `.planning/phases/12-notifications-bot-polish/12-04-SUMMARY.md` when done
</output>
