---
phase: 01-foundations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - Makefile
  - docker/web/Dockerfile
  - docker/web/php.ini
  - docker/web/nginx.conf
  - docker/web/entrypoint.sh
  - docker/bot/Dockerfile
  - docker/rcon-worker/Dockerfile
autonomous: true
requirements:
  - REQ-constraint-railway-deploy
  - REQ-constraint-en-launch-i18n-ready
must_haves:
  truths:
    - "`docker compose up -d` brings up web (php-fpm 8.4 + nginx), bot, rcon-worker, postgres 16, redis 7 with healthchecks reaching ready state."
    - "`docker compose exec web php -v` reports PHP 8.4.x with intl, pdo_pgsql, redis, gd, bcmath, zip extensions present."
    - "Postgres container persists data via named volume `pg_data`; redis via `redis_data`."
    - ".env.example documents every env var the compose file substitutes (DB_*, REDIS_*, DISCORD_*, APP_*) without containing real secrets."
    - "Makefile exposes `up`, `down`, `logs`, `artisan`, `composer`, `pnpm`, `pest`, `pint`, `phpstan`, `shell` short aliases."
  artifacts:
    - path: "docker-compose.yml"
      provides: "Five-service local dev stack at repo root (D-021 LOCKED)"
      contains: "services:"
    - path: ".env.example"
      provides: "Documents env shape; never holds real secrets"
      contains: "DISCORD_CLIENT_ID="
    - path: "Makefile"
      provides: "Short aliases for `docker compose exec` commands"
      contains: ".PHONY"
    - path: "docker/web/Dockerfile"
      provides: "PHP 8.4-fpm image with Laravel-required extensions"
      contains: "FROM php:8.4-fpm"
    - path: "docker/web/nginx.conf"
      provides: "nginx reverse proxy for php-fpm"
      contains: "fastcgi_pass"
    - path: "docker/bot/Dockerfile"
      provides: "Node 22 alpine image for bot"
      contains: "FROM node:22-alpine"
    - path: "docker/rcon-worker/Dockerfile"
      provides: "Node 22 alpine image for rcon-worker"
      contains: "FROM node:22-alpine"
  key_links:
    - from: "docker-compose.yml"
      to: "docker/web/Dockerfile"
      via: "build.context + build.dockerfile"
      pattern: "dockerfile:.*docker/web/Dockerfile"
    - from: "docker-compose.yml"
      to: "postgres healthcheck"
      via: "healthcheck.test"
      pattern: "pg_isready"
    - from: "docker-compose.yml"
      to: "redis healthcheck"
      via: "healthcheck.test"
      pattern: "redis-cli.*ping"
    - from: "Makefile"
      to: "docker compose exec web"
      via: "shell command"
      pattern: "docker compose exec"
---

<objective>
Author the repo-root `docker-compose.yml` (D-021 LOCKED) plus per-service Dockerfiles for `web` (php:8.4-fpm + nginx sidecar), `bot` (node:22-alpine), and `rcon-worker` (node:22-alpine). Add `.env.example` documenting every env var substituted into compose, plus a `Makefile` with the short aliases CONTEXT.md calls for.

Purpose: D-021 mandates ALL local dev runs through containers; host installs of PHP/Postgres/Redis are NOT used. This plan ships the parity-with-Railway compose stack so plan 04 can run `composer create-project` *inside* the web container. STATE.md notes Docker Desktop WSL integration must be enabled before this plan executes.

Output: One compose file + four Dockerfile/config files + .env.example + Makefile. After this plan: `docker compose up -d` brings the stack up; `apps/web/` is empty but mounted (Laravel install lands in plan 04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-foundations/01-CONTEXT.md
@.planning/phases/01-foundations/01-RESEARCH.md
@.docs/02-architecture.md

<interfaces>
<!-- D-021 (LOCKED): all five Railway services containerized; PHP/Postgres/Redis NEVER on host. -->
<!-- D-014: Railway env groups (database, redis, discord, app) — local compose mirrors that shape. -->
<!-- D-016: Postgres 16, Redis 7 (NOT 7-alpine vs 7 — research used redis:7-alpine which is fine). -->
<!-- PHP extensions Laravel 12 + Filament v3 require: intl, pdo_pgsql, redis, gd, bcmath, zip, mbstring, pcntl, exif, opcache. -->
<!-- nginx fronts php-fpm so /admin (Filament/Livewire) and / (Inertia) both work; we expose port 8000 (matches DISCORD_REDIRECT_URI=http://localhost:8000/auth/discord/callback). -->
<!-- apps/web/ is bind-mounted; vendor/ and node_modules use named volumes to dodge Windows-bind-mount perf (per CONTEXT.md Specifics, even though we're already on the WSL filesystem). -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author docker/web/Dockerfile + nginx.conf + php.ini + entrypoint.sh</name>
  <files>docker/web/Dockerfile, docker/web/nginx.conf, docker/web/php.ini, docker/web/entrypoint.sh, docker/bot/Dockerfile, docker/rcon-worker/Dockerfile</files>
  <read_first>
    - .planning/phases/01-foundations/01-RESEARCH.md (Standard Stack: PHP 8.4 + Filament v3 + Inertia v2; Common Pitfalls 5: Postgres extensions; Common Pitfalls 9: PHP 8.4 deprecation noise)
    - .docs/02-architecture.md (Services table: web=Laravel 12 + PHP 8.4)
  </read_first>
  <behavior>
    - web Dockerfile installs PHP 8.4-fpm with extensions: intl, pdo_pgsql, pgsql, redis (via pecl), gd, bcmath, zip, mbstring, pcntl, exif, opcache
    - web Dockerfile installs Composer (multi-stage from official composer image) and pnpm globally via corepack
    - web container ships nginx as a sidecar in its own image (separate Dockerfile) — using the standard pattern of two services (`web` = php-fpm, `web-nginx` = nginx fastcgi proxy) per RESEARCH Validation Strategy
    - Both bot and rcon-worker Dockerfiles use node:22-alpine, install pnpm via corepack, set workdir, COPY package.json + tsconfig + src, default CMD `node dist/index.js`
    - php.ini overrides for dev: memory_limit=512M, upload_max_filesize=20M, post_max_size=20M, opcache.validate_timestamps=1
    - entrypoint.sh: ensures storage/ + bootstrap/cache exist + writable, then `exec php-fpm`
  </behavior>
  <action>
Create `docker/web/Dockerfile`:
```dockerfile
# Source: research 01-RESEARCH.md Common Pitfalls 5 (postgres extensions) + Standard Stack
# PHP 8.4-fpm with extensions Laravel 12 + Filament v3 require.

FROM composer:2 AS composer-bin

FROM php:8.4-fpm-bookworm

# System deps for PHP extensions
RUN apt-get update && apt-get install -y --no-install-recommends \
    git curl unzip ca-certificates \
    libicu-dev libpq-dev libzip-dev libonig-dev \
    libpng-dev libjpeg-dev libwebp-dev libfreetype6-dev \
    libxml2-dev \
  && rm -rf /var/lib/apt/lists/*

# Configure & install PHP extensions
RUN docker-php-ext-configure intl \
  && docker-php-ext-configure gd --with-freetype --with-jpeg --with-webp \
  && docker-php-ext-install -j$(nproc) \
    intl pdo_pgsql pgsql gd bcmath zip mbstring pcntl exif opcache

# Redis extension via pecl
RUN pecl install redis-6.1.0 \
  && docker-php-ext-enable redis

# Composer (copy from composer:2 stage)
COPY --from=composer-bin /usr/bin/composer /usr/bin/composer

# Node 22 + pnpm (for `php artisan typescript:transform` + Vite builds invoked from web container)
RUN curl -fsSL https://deb.nodesource.com/setup_22.x | bash - \
  && apt-get install -y --no-install-recommends nodejs \
  && rm -rf /var/lib/apt/lists/* \
  && corepack enable \
  && corepack prepare pnpm@9.15.0 --activate

# Custom php.ini overrides
COPY docker/web/php.ini /usr/local/etc/php/conf.d/zz-trenchwars.ini

# Entrypoint ensures Laravel runtime dirs exist + writable
COPY docker/web/entrypoint.sh /usr/local/bin/entrypoint.sh
RUN chmod +x /usr/local/bin/entrypoint.sh

WORKDIR /app
EXPOSE 9000

ENTRYPOINT ["/usr/local/bin/entrypoint.sh"]
CMD ["php-fpm", "-F"]
```

Create `docker/web/nginx.conf`:
```nginx
# Source: research RESEARCH.md System Architecture Diagram (web = php-fpm + nginx sidecar)
worker_processes auto;
events { worker_connections 1024; }

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    sendfile on;
    keepalive_timeout 65;
    client_max_body_size 20M;

    server {
        listen 80;
        server_name _;
        root /app/public;
        index index.php index.html;

        add_header X-Frame-Options "SAMEORIGIN";
        add_header X-Content-Type-Options "nosniff";

        location / {
            try_files $uri $uri/ /index.php?$query_string;
        }

        location ~ \.php$ {
            fastcgi_pass web:9000;
            fastcgi_index index.php;
            fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
            include fastcgi_params;
            fastcgi_read_timeout 60s;
        }

        location ~ /\.(?!well-known).* {
            deny all;
        }
    }
}
```

Create `docker/web/php.ini`:
```ini
; Source: 01-RESEARCH.md Common Pitfalls 9 + dev defaults
memory_limit = 512M
upload_max_filesize = 20M
post_max_size = 20M
max_execution_time = 60
date.timezone = UTC

; Opcache — dev settings (production overrides via env later)
opcache.enable = 1
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0

; Suppress deprecation noise from Filament v3 internals (Pitfall 9)
log_errors = On
error_reporting = E_ALL & ~E_DEPRECATED & ~E_STRICT
```

Create `docker/web/entrypoint.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail

# Source: 01-RESEARCH.md (Laravel runtime dirs must exist + be writable inside container).
# This entrypoint is idempotent — safe to run on every container start.

if [ -d "/app" ]; then
  mkdir -p \
    /app/storage/app/public \
    /app/storage/framework/cache \
    /app/storage/framework/sessions \
    /app/storage/framework/views \
    /app/storage/framework/testing \
    /app/storage/logs \
    /app/bootstrap/cache 2>/dev/null || true

  chmod -R 0775 /app/storage /app/bootstrap/cache 2>/dev/null || true
fi

exec "$@"
```

Create `docker/bot/Dockerfile`:
```dockerfile
# Source: 01-RESEARCH.md Recommended Project Structure — apps/bot Dockerfile node:22-alpine
FROM node:22-alpine

RUN corepack enable && corepack prepare pnpm@9.15.0 --activate

WORKDIR /app

# Copy workspace files for `pnpm install --filter` to work
COPY pnpm-workspace.yaml package.json tsconfig.base.json ./
COPY packages/shared-types ./packages/shared-types
COPY apps/bot ./apps/bot

RUN pnpm install --frozen-lockfile=false --filter @trenchwars/bot... \
 && cd apps/bot && pnpm build

WORKDIR /app/apps/bot
CMD ["node", "dist/index.js"]
```

Create `docker/rcon-worker/Dockerfile`:
```dockerfile
# Source: 01-RESEARCH.md Recommended Project Structure — apps/rcon-worker Dockerfile node:22-alpine
FROM node:22-alpine

RUN corepack enable && corepack prepare pnpm@9.15.0 --activate

WORKDIR /app

COPY pnpm-workspace.yaml package.json tsconfig.base.json ./
COPY packages/shared-types ./packages/shared-types
COPY apps/rcon-worker ./apps/rcon-worker

RUN pnpm install --frozen-lockfile=false --filter @trenchwars/rcon-worker... \
 && cd apps/rcon-worker && pnpm build

WORKDIR /app/apps/rcon-worker
CMD ["node", "dist/index.js"]
```
  </action>
  <verify>
    <automated>test -f docker/web/Dockerfile && test -f docker/web/nginx.conf && test -f docker/web/php.ini && test -f docker/web/entrypoint.sh && test -f docker/bot/Dockerfile && test -f docker/rcon-worker/Dockerfile && grep -q 'FROM php:8.4-fpm' docker/web/Dockerfile && grep -q 'pdo_pgsql' docker/web/Dockerfile && grep -q 'pecl install redis' docker/web/Dockerfile && grep -q 'fastcgi_pass web:9000' docker/web/nginx.conf && grep -q 'FROM node:22-alpine' docker/bot/Dockerfile && grep -q 'FROM node:22-alpine' docker/rcon-worker/Dockerfile</automated>
  </verify>
  <acceptance_criteria>
    - All six files exist
    - web/Dockerfile installs intl + pdo_pgsql + redis + gd + bcmath + zip + mbstring + pcntl + exif + opcache
    - web/Dockerfile installs Composer + Node 22 + pnpm@9.15.0 (so artisan + vite + typescript:transform run inside the container)
    - nginx.conf fastcgi_passes to `web:9000` (compose service name)
    - bot/rcon-worker Dockerfiles use node:22-alpine + corepack pnpm
  </acceptance_criteria>
  <done>Image build files ready; `docker compose build` (run in task 2) will succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author docker-compose.yml + .env.example + Makefile</name>
  <files>docker-compose.yml, .env.example, Makefile</files>
  <read_first>
    - docker/web/Dockerfile (just created — referenced by build context)
    - docker/web/nginx.conf (just created)
    - .planning/phases/01-foundations/01-RESEARCH.md (Pattern 1: Discord OAuth env vars; Common Pitfalls 5: postgres extensions enabled by migration NOT image)
    - .docs/02-architecture.md (Services table; Deployment env groups)
    - .planning/phases/01-foundations/01-CONTEXT.md (Specific Ideas: Postgres dev creds, named volumes)
  </read_first>
  <behavior>
    - Compose declares 6 services: web (php-fpm), web-nginx (nginx fronting web), bot, rcon-worker, postgres, redis. Network `trenchwars` shared.
    - Healthchecks: web uses `php-fpm -t`, web-nginx uses `nginx -t`, postgres uses `pg_isready`, redis uses `redis-cli ping`. Bot/rcon-worker have simple `pgrep node` healthchecks.
    - `apps/web` bind-mounted to `/app` inside web + web-nginx. `vendor/` and `node_modules` use named volumes (perf + avoid host pollution).
    - postgres uses named volume `pg_data`; redis uses `redis_data`.
    - .env.example documents EVERY env var compose substitutes — zero real secrets, only shape and links to source (Discord developer portal etc.). Use placeholder values like `__SET_ME__`.
    - Makefile: `make up`, `make down`, `make logs`, `make shell`, `make artisan ARGS="..."`, `make composer ARGS="..."`, `make pnpm ARGS="..."`, `make pest`, `make pint`, `make phpstan`, `make migrate`, `make build`. All wrap `docker compose exec web ...` (or `run --rm` for one-shots before web is up).
  </behavior>
  <action>
Create `docker-compose.yml` at repo root:
```yaml
# Source: D-021 (LOCKED) — all five Railway services + Postgres + Redis containerized for local dev.
# Per CONTEXT.md Specific Ideas: bind-mount apps/web; vendor/ + node_modules in named volumes; named pg_data + redis_data.
# Per .docs/02-architecture.md Services table.
name: trenchwars

services:
  web:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    container_name: trenchwars-web
    working_dir: /app
    volumes:
      - ./apps/web:/app
      - web_vendor:/app/vendor
      - web_node_modules:/app/node_modules
    environment:
      APP_ENV: ${APP_ENV:-local}
      APP_DEBUG: ${APP_DEBUG:-true}
      APP_URL: ${APP_URL:-http://localhost:8000}
      APP_KEY: ${APP_KEY:-}
      DB_CONNECTION: pgsql
      DB_HOST: postgres
      DB_PORT: 5432
      DB_DATABASE: ${DB_DATABASE:-trenchwars}
      DB_USERNAME: ${DB_USERNAME:-trenchwars}
      DB_PASSWORD: ${DB_PASSWORD:-trenchwars}
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-null}
      SESSION_DRIVER: ${SESSION_DRIVER:-redis}
      CACHE_STORE: ${CACHE_STORE:-redis}
      QUEUE_CONNECTION: ${QUEUE_CONNECTION:-redis}
      DISCORD_CLIENT_ID: ${DISCORD_CLIENT_ID:-}
      DISCORD_CLIENT_SECRET: ${DISCORD_CLIENT_SECRET:-}
      DISCORD_REDIRECT_URI: ${DISCORD_REDIRECT_URI:-http://localhost:8000/auth/discord/callback}
      LOG_DEPRECATIONS_CHANNEL: ${LOG_DEPRECATIONS_CHANNEL:-null}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "php-fpm -t || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    networks: [trenchwars]

  web-nginx:
    image: nginx:1.27-alpine
    container_name: trenchwars-nginx
    ports:
      - "8000:80"
    volumes:
      - ./apps/web:/app:ro
      - ./docker/web/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      web:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nginx -t || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
    networks: [trenchwars]

  bot:
    build:
      context: .
      dockerfile: docker/bot/Dockerfile
    container_name: trenchwars-bot
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      DISCORD_BOT_TOKEN: ${DISCORD_BOT_TOKEN:-}
      DISCORD_APPLICATION_ID: ${DISCORD_APPLICATION_ID:-}
      WEB_API_URL: ${WEB_API_URL:-http://web-nginx}
      WEB_API_TOKEN: ${WEB_API_TOKEN:-}
      REDIS_URL: redis://redis:6379
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "pgrep node > /dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks: [trenchwars]

  rcon-worker:
    build:
      context: .
      dockerfile: docker/rcon-worker/Dockerfile
    container_name: trenchwars-rcon-worker
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      WEB_HMAC_SECRET: ${WEB_HMAC_SECRET:-}
      WEB_INTERNAL_URL: ${WEB_INTERNAL_URL:-http://web-nginx}
      REDIS_URL: redis://redis:6379
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "pgrep node > /dev/null || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks: [trenchwars]

  postgres:
    image: postgres:16-alpine
    container_name: trenchwars-postgres
    environment:
      POSTGRES_DB: ${DB_DATABASE:-trenchwars}
      POSTGRES_USER: ${DB_USERNAME:-trenchwars}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-trenchwars}
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "${DB_PORT_HOST:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-trenchwars} -d ${DB_DATABASE:-trenchwars}"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks: [trenchwars]

  redis:
    image: redis:7-alpine
    container_name: trenchwars-redis
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis_data:/data
    ports:
      - "${REDIS_PORT_HOST:-6379}:6379"
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping | grep -q PONG"]
      interval: 5s
      timeout: 5s
      retries: 10
    networks: [trenchwars]

volumes:
  pg_data:
  redis_data:
  web_vendor:
  web_node_modules:

networks:
  trenchwars:
    driver: bridge
```

Create `.env.example` at repo root (NEVER holds real secrets):
```bash
# Trenchwars — local-dev environment shape.
# Copy to `.env` and fill in real values. NEVER commit .env to git.
# Production secrets live in Railway env groups (D-014).

# ─── App ────────────────────────────────────────────────────────────────
APP_NAME=Trenchwars
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8000
APP_KEY=
# Generate with: docker compose exec web php artisan key:generate

LOG_DEPRECATIONS_CHANNEL=null
# Set to "stack" if you want to surface PHP deprecation warnings (Filament v3 emits some on PHP 8.4)

# ─── Database (Postgres 16) ─────────────────────────────────────────────
DB_CONNECTION=pgsql
DB_HOST=postgres
DB_PORT=5432
DB_DATABASE=trenchwars
DB_USERNAME=trenchwars
DB_PASSWORD=trenchwars
DB_PORT_HOST=5432
# Host-side port (so `psql -h localhost -p 5432` works from outside the container).

# ─── Redis ──────────────────────────────────────────────────────────────
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=null
REDIS_PORT_HOST=6379

# ─── Sessions / Cache / Queue (D-001 + research) ────────────────────────
SESSION_DRIVER=redis
SESSION_LIFETIME=43200
# 30 days = 43200 minutes (CONTEXT.md "Remember-me 30 days")
SESSION_SECURE_COOKIE=false
# Set to true ONLY in production; false on http://localhost
SESSION_SAME_SITE=lax
# Lax — works on http://localhost OAuth (research Pitfall 3)

CACHE_STORE=redis
QUEUE_CONNECTION=redis

# ─── Discord OAuth (D-002) ──────────────────────────────────────────────
# Create a Discord application at https://discord.com/developers/applications
# OAuth2 → Redirects: register `http://localhost:8000/auth/discord/callback` (LOCAL, exact match).
# Copy CLIENT_ID and CLIENT_SECRET below.
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_REDIRECT_URI=http://localhost:8000/auth/discord/callback
# Production redirect URI is set in Railway env group; do NOT add a trailing slash anywhere.

# ─── Discord bot (Phase 5; declared now for env-shape parity) ───────────
DISCORD_BOT_TOKEN=
DISCORD_APPLICATION_ID=

# ─── Bot ↔ web Sanctum token (Phase 5) ──────────────────────────────────
WEB_API_URL=http://web-nginx
WEB_API_TOKEN=

# ─── RCON worker ↔ web HMAC (Phase 8) ───────────────────────────────────
WEB_HMAC_SECRET=
WEB_INTERNAL_URL=http://web-nginx
```

Create `Makefile` at repo root:
```makefile
# Trenchwars dev shortcuts. All commands run inside containers (D-021).
# Use: make up | make down | make logs | make shell | make artisan ARGS="migrate"

.PHONY: up down restart logs ps shell build pull \
        composer artisan pnpm npm node \
        pest pint phpstan typescript-transform \
        migrate fresh seed

up:
	docker compose up -d

down:
	docker compose down

restart: down up

logs:
	docker compose logs -f --tail=200

ps:
	docker compose ps

build:
	docker compose build

pull:
	docker compose pull

# ─── Web container (Laravel) ────────────────────────────────────────────
shell:
	docker compose exec web bash

composer:
	docker compose exec web composer $(ARGS)

artisan:
	docker compose exec web php artisan $(ARGS)

pnpm:
	docker compose exec web pnpm $(ARGS)

npm:
	docker compose exec web npm $(ARGS)

node:
	docker compose exec web node $(ARGS)

# ─── Quality gates ──────────────────────────────────────────────────────
pest:
	docker compose exec web ./vendor/bin/pest $(ARGS)

pint:
	docker compose exec web ./vendor/bin/pint $(ARGS)

phpstan:
	docker compose exec web ./vendor/bin/phpstan analyse $(ARGS)

# ─── DB shortcuts ───────────────────────────────────────────────────────
migrate:
	docker compose exec web php artisan migrate

fresh:
	docker compose exec web php artisan migrate:fresh --seed

seed:
	docker compose exec web php artisan db:seed

typescript-transform:
	docker compose exec web php artisan typescript:transform
```
  </action>
  <verify>
    <automated>test -f docker-compose.yml && test -f .env.example && test -f Makefile && grep -q 'image: postgres:16-alpine' docker-compose.yml && grep -q 'image: redis:7-alpine' docker-compose.yml && grep -q 'pg_isready' docker-compose.yml && grep -q 'redis-cli ping' docker-compose.yml && grep -q 'condition: service_healthy' docker-compose.yml && grep -q 'pg_data:' docker-compose.yml && grep -q 'redis_data:' docker-compose.yml && grep -q 'DISCORD_CLIENT_ID=' .env.example && grep -q 'DISCORD_REDIRECT_URI=' .env.example && grep -v '^#' .env.example | grep -c 'DISCORD_CLIENT_SECRET=$' | grep -q '^1$' && grep -q '.PHONY' Makefile && grep -q 'docker compose exec web' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml declares 6 services: web, web-nginx, bot, rcon-worker, postgres, redis
    - All five non-nginx services + nginx have healthchecks
    - postgres + redis use named volumes for persistence
    - apps/web bind-mounted into web + web-nginx; vendor/ + node_modules in named volumes
    - .env.example contains DISCORD_CLIENT_ID, DISCORD_CLIENT_SECRET (empty), DISCORD_REDIRECT_URI (with default), DB_*, REDIS_*, plus comments pointing to https://discord.com/developers/applications
    - Real secrets are NEVER in .env.example (CLIENT_SECRET line is empty)
    - Makefile exposes up/down/logs/shell/artisan/composer/pnpm/pest/pint/phpstan/migrate
  </acceptance_criteria>
  <done>`docker compose up -d` brings the stack up after this plan; web container ready for plan 04 to run `composer create-project`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → containers | Real env values from `.env` (gitignored) flow into container env. `.env.example` is shape-only. |
| browser → web-nginx (port 8000) | Untrusted client traffic; nginx adds X-Frame-Options + X-Content-Type-Options headers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-12-a | Information Disclosure | `.env.example` | mitigate | File contains zero real values — all secret-like fields are empty (`DISCORD_CLIENT_SECRET=`); `.env` is gitignored (plan 01) |
| T-1-12-b | Information Disclosure | postgres dev credentials in compose | accept | dev-only creds (`trenchwars/trenchwars`) bound to localhost only; production uses Railway plugin-injected DATABASE_URL |
| T-1-13 | Tampering | apps/web bind mount | accept | dev-only — host filesystem is trusted; production uses image with COPY rather than mount |
| T-1-14 | DoS | nginx client_max_body_size | mitigate | Capped at 20M to bound multipart abuse |
</threat_model>

<verification>
- `docker compose config` validates the YAML
- `docker compose build` (manual after plan 04 once apps/web has package.json) succeeds for all services
- All required env vars surface in `.env.example`
- Makefile targets resolve
</verification>

<success_criteria>
M9: `docker compose up -d` brings up web, bot, rcon-worker, postgres, redis with healthchecks green (smoke verified manually after plan 04 lands; full success in plan 18 [BLOCKING] verification).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-foundations-02-SUMMARY.md`.
</output>
