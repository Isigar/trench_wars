---
phase: 03-games-match-types
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - apps/web/database/seeders/GameSeeder.php
  - apps/web/database/seeders/DatabaseSeeder.php
  - apps/web/tests/Feature/Database/GameSeederTest.php
autonomous: true
requirements:
  - REQ-platform-vision
must_haves:
  truths:
    - "GameSeeder seeds 1 Game (HLL), 15 GameRoles, 5 GameMatchTypes, plus capacity rows for Scrim 50v50 and Skirmish 6v6 (Friendly/Tournament/Clan War have NO capacity rows — admin fills via Filament)"
    - "Re-running GameSeeder leaves the row counts unchanged (idempotent via firstOrCreate; Pitfall 5)"
    - "Re-running GameSeeder does NOT clobber admin edits to translatable name or capacity (firstOrCreate's second-arg [other_attrs] only applies on create, never on second-run lookup)"
    - "DatabaseSeeder calls GameSeeder AFTER ClanTagSeeder (Pitfall 9 — migration ordering)"
    - "GameSeederTest GREEN with idempotency, count, and admin-edit-preservation assertions (replaces Wave 0 stub from plan 03-01)"
  artifacts:
    - path: "apps/web/database/seeders/GameSeeder.php"
      provides: "HLL preset: 1 Game + 15 Roles + 5 MatchTypes + capacity matrix (Scrim 50v50 + Skirmish 6v6)"
      contains: "firstOrCreate"
    - path: "apps/web/database/seeders/DatabaseSeeder.php"
      provides: "DatabaseSeeder call order extended with GameSeeder"
      contains: "GameSeeder::class"
    - path: "apps/web/tests/Feature/Database/GameSeederTest.php"
      provides: "Idempotency + count + admin-edit-preservation tests"
      contains: "firstOrCreate"
  key_links:
    - from: "GameSeeder"
      to: "Game/GameRole/GameMatchType/GameMatchTypeRoleLimit models"
      via: "Model::firstOrCreate([lookup_keys], [other_attrs])"
      pattern: "firstOrCreate"
    - from: "DatabaseSeeder->run()"
      to: "GameSeeder::class"
      via: "$this->call([..., GameSeeder::class])"
      pattern: "GameSeeder::class"
---

<objective>
Land the HLL preset seeder per CONTEXT.md success criterion #3: idempotently seed 1 Game (key='hll', name=['en' => 'Hell Let Loose']), 15 GameRoles (the canonical roster per RESEARCH Assumption A1: Commander, Officer, Squad Leader, Rifleman, Assault, Automatic Rifleman, Medic, Engineer, Support, Machine Gunner, Anti-Tank, Sniper, Spotter, Tank Commander, Crewman), 5 GameMatchTypes (Scrim 50v50, Skirmish 6v6, Friendly, Tournament, Clan War), and capacity rows for Scrim 50v50 + Skirmish 6v6 only (Friendly/Tournament/Clan War left blank per RESEARCH Recommendation Q2 — admin fills via Filament post-seed). Wire GameSeeder into DatabaseSeeder.run() AFTER ClanTagSeeder (Pitfall 9). Replace the Wave 0 GameSeederTest stub with a GREEN test asserting idempotency + counts + admin-edit preservation.

Purpose: D-007 + CONTEXT.md SC-3 require HLL exist as a seeded preset, not hardcoded. The `firstOrCreate([lookup_keys], [other_attrs])` pattern (Pattern 5) is the foundation of D-007's "zero code changes to add a new game" promise — admin edits to translatable display labels or capacity numbers survive `db:seed` re-runs because the `[other_attrs]` argument only fires on create, never on second-run lookup.

Output: GameSeeder.php; DatabaseSeeder.php amended; GameSeederTest.php GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-games-match-types/03-RESEARCH.md
@.planning/phases/03-games-match-types/03-CONTEXT.md
@CLAUDE.md
@apps/web/database/seeders/DatabaseSeeder.php
@apps/web/database/seeders/DiscordGuildSeeder.php
@apps/web/database/seeders/ClanTagSeeder.php
@apps/web/database/seeders/PermissionSeeder.php
@apps/web/tests/Feature/Clans/DiscordGuildSeederTest.php

<interfaces>
Canonical Phase 2 seeder pattern (verified `apps/web/database/seeders/ClanTagSeeder.php`):
- `<?php\ndeclare(strict_types=1); namespace Database\Seeders;`
- `class XSeeder extends Seeder { public function run(): void { ... } }`
- For each row: `Model::firstOrCreate([lookup_keys], [other_attrs])` — the `[lookup_keys]` MUST match a UNIQUE index column (or composite) exactly; second-arg `[other_attrs]` applies on create only

Existing DatabaseSeeder (verified codebase):
- Currently calls: PermissionSeeder, DiscordGuildSeeder, ClanTagSeeder
- Has a comment `// Phase 3+ adds GameSeeder etc.` reserving the slot
- Insert GameSeeder::class AFTER ClanTagSeeder::class per Pitfall 9

From RESEARCH.md Pattern 5 + Code Examples § GameSeeder (verbatim spec):
- HLL Game: key='hll', name=['en' => 'Hell Let Loose'], is_active=true
- 15 roles with sort_order in 10-step increments (10, 20, 30, ..., 150)
- 5 match types: scrim_50v50, skirmish_6v6, friendly, tournament, clan_war
- Capacity matrix for Scrim 50v50 (50 total slots distributed across 15 roles per RESEARCH starter distribution)
- Capacity matrix for Skirmish 6v6 (6 infantry-only slots: squad_leader=1, rifleman=2, assault=1, medic=1, support=1)
- Friendly/Tournament/Clan War: NO capacity rows seeded — admin sets via Filament after seed (RESEARCH Q2)

Idempotency keys (matching the UNIQUE indexes from plan 03-02):
- Game: ['key' => 'hll']
- GameRole: ['game_id' => $hll->id, 'key' => $r['key']]
- GameMatchType: ['game_id' => $hll->id, 'key' => $m['key']]
- GameMatchTypeRoleLimit: ['game_match_type_id' => $matchTypeId, 'game_role_id' => $roleId]

Same-game invariant (Pitfall 10): when seeding RoleLimits, ALL roles MUST belong to $hll and ALL matchTypes MUST belong to $hll. The seeder achieves this naturally by fetching roles via `$hll->roles()->get()->keyBy('key')` and matchTypes via the local $matchTypeIds map keyed by match-type key — but the saving() listener (plan 03-03) is the safety net for typos.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Seeder runtime to DB | Seeders run inside the migration container; trusted operation but MUST be idempotent (Pitfall 5) |
| Repeated seeder invocation | CI/dev re-runs `db:seed` regularly; second run MUST NOT create duplicate rows OR clobber admin edits |
| Seeder to translatable JSONB | The `name` and `display_name` arrays are wire-format `['en' => string]`; HasTranslations handles the column write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | Seeder duplicates rows on re-run (Pitfall 5) | mitigate | All inserts use `firstOrCreate([lookup_keys], [other_attrs])` where lookup_keys match the DB UNIQUE index; test asserts counts unchanged after second seed call |
| T-03-05-02 | Tampering | Seeder clobbers admin edits to translatable name | mitigate | firstOrCreate's second-arg only applies on create; admin edits survive; test asserts edited row persists after re-seed |
| T-03-05-03 | Repudiation | Seeder writes have no causer | accept | Seeders run under console kernel; LogsActivity records `event=created` with `causer_id=null` which is correct for system seeds |
| T-03-05-04 | Tampering | Cross-game role injection via seeder bug | mitigate | All seeder roles fetched via $hll->roles()->get() — guaranteed same game; saving() listener (plan 03-03) is the model-layer safety net |
| T-03-05-05 | Repudiation | Wrong role count breaks SC-3 | mitigate | Test asserts exactly 15 GameRoles + 5 GameMatchTypes exist after seed — CONTEXT.md SC-3 contract |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: GameSeeder.php (HLL preset: 1 Game + 15 Roles + 5 MatchTypes + capacity matrix for 2 MatchTypes) + DatabaseSeeder wiring</name>
  <files>
    apps/web/database/seeders/GameSeeder.php,
    apps/web/database/seeders/DatabaseSeeder.php
  </files>
  <read_first>
    - apps/web/database/seeders/ClanTagSeeder.php (Phase 2 analog — firstOrCreate with UNIQUE slug key; multi-row loop pattern)
    - apps/web/database/seeders/DiscordGuildSeeder.php (singleton firstOrCreate pattern)
    - apps/web/database/seeders/DatabaseSeeder.php (current state — Phase 3 reserved comment + call order)
    - .planning/phases/03-games-match-types/03-RESEARCH.md (Pattern 5 verbatim seeder code — 15 roles list with keys/display names/sort orders + 5 match types + capacity matrices for Scrim 50v50 and Skirmish 6v6)
    - .planning/phases/03-games-match-types/03-CONTEXT.md (Success criterion #3 — the canonical roster list)
  </read_first>
  <acceptance_criteria>
    - `GameSeeder.php`:
      - `<?php\ndeclare(strict_types=1); namespace Database\Seeders;`
      - `use App\Models\Game; use App\Models\GameRole; use App\Models\GameMatchType; use App\Models\GameMatchTypeRoleLimit; use Illuminate\Database\Seeder;`
      - `class GameSeeder extends Seeder { public function run(): void { ... } }`
      - Inside run():
        - Step 1: Game::firstOrCreate(['key' => 'hll'], ['name' => ['en' => 'Hell Let Loose'], 'is_active' => true]) — capture $hll
        - Step 2: 15-element array literal `$roles` with shape `['key' => '<snake_case>', 'display' => '<English>', 'sort' => <int>]`; for each entry, GameRole::firstOrCreate(['game_id' => $hll->id, 'key' => $r['key']], ['display_name' => ['en' => $r['display']], 'sort_order' => $r['sort'], 'is_active' => true])
        - The 15 roles MUST be exactly: commander/Commander/10, officer/Officer/20, squad_leader/Squad Leader/30, rifleman/Rifleman/40, assault/Assault/50, automatic_rifleman/Automatic Rifleman/60, medic/Medic/70, engineer/Engineer/80, support/Support/90, machine_gunner/Machine Gunner/100, anti_tank/Anti-Tank/110, sniper/Sniper/120, spotter/Spotter/130, tank_commander/Tank Commander/140, crewman/Crewman/150
        - Step 3: 5-element array literal `$matchTypes` with shape `['key', 'name', 'description']`; for each, GameMatchType::firstOrCreate(['game_id' => $hll->id, 'key' => $m['key']], ['name' => ['en' => $m['name']], 'description' => ['en' => $m['description']], 'is_active' => true]); collect IDs in $matchTypeIds map keyed by match-type key
        - The 5 match types MUST be: scrim_50v50/Scrim 50v50/<desc>, skirmish_6v6/Skirmish 6v6/<desc>, friendly/Friendly/<desc>, tournament/Tournament/<desc>, clan_war/Clan War/<desc>
        - Step 4: private method `seedRoleLimits(Game $hll, array $matchTypeIds): void` — fetches `$roles = $hll->roles()->get()->keyBy('key');` then for the 'scrim_50v50' key: loop the RESEARCH-recommended capacity distribution (commander=1, officer=4, squad_leader=4, rifleman=14, assault=4, automatic_rifleman=4, medic=4, engineer=4, support=4, machine_gunner=2, anti_tank=2, sniper=1, spotter=1, tank_commander=1, crewman=0 → 50 total) using GameMatchTypeRoleLimit::firstOrCreate(['game_match_type_id' => $matchTypeIds['scrim_50v50'], 'game_role_id' => $roles[$key]->id], ['capacity' => $cap, 'sort_order' => $roles[$key]->sort_order])
        - For Skirmish 6v6: 5 entries (squad_leader=1, rifleman=2, assault=1, medic=1, support=1) — 6 total
        - For Friendly, Tournament, Clan War: NO capacity rows seeded (RESEARCH Q2 recommendation)
        - All 4 steps use `firstOrCreate` with the lookup-key matching the DB UNIQUE index (Pattern 5 + Pitfall 5)
    - `DatabaseSeeder.php` extended: add `GameSeeder::class` to the existing `$this->call([...])` array, AFTER `ClanTagSeeder::class` (Pitfall 9 — Phase 3 migrations land before this call, so the games table exists; but Phase 2 seeders depend only on Phase 2 tables, so ordering must be Phase 1 → Phase 2 → Phase 3)
    - `make artisan ARGS="migrate:fresh --seed"` succeeds end-to-end
    - psql `SELECT COUNT(*) FROM games` returns 1; `FROM game_roles WHERE game_id = (SELECT id FROM games WHERE key='hll')` returns 15; `FROM game_match_types WHERE game_id = ...` returns 5; `FROM game_match_type_role_limits` returns 15 + 5 = 20 (15 Scrim + 5 Skirmish)
    - `make phpstan` reports no new errors
    - `make pint --test database/seeders` clean
  </acceptance_criteria>
  <action>
    Build GameSeeder.php verbatim from RESEARCH.md Pattern 5 § seeder code. The structure:
    1. Class extends Illuminate\Database\Seeder; run(): void method.
    2. Game::firstOrCreate creates or returns the HLL singleton.
    3. Array literal of 15 role specs; foreach + firstOrCreate keyed by (game_id, key).
    4. Array literal of 5 match-type specs; foreach + firstOrCreate keyed by (game_id, key); capture $matchTypeIds keyed by match-type key.
    5. private function seedRoleLimits($hll, $matchTypeIds): void — fetches roles via $hll->roles()->get()->keyBy('key'), then loops the Scrim 50v50 capacity distribution + the Skirmish 6v6 capacity distribution; each row via firstOrCreate keyed by (game_match_type_id, game_role_id).

    Annotate each array literal with `/** @var list<array{key: string, display: string, sort: int}> $roles */` and `/** @var list<array{key: string, name: string, description: string}> $matchTypes */` for PHPStan L8 list-shape requirements.

    For DatabaseSeeder.php: read the current file, locate the `$this->call([...])` array, append `GameSeeder::class,` AFTER `ClanTagSeeder::class`. Phase 1 plan 01-11 wired PermissionSeeder; Phase 2 plan 02-04 wired DiscordGuildSeeder + ClanTagSeeder. The reservation comment `// Phase 3+ adds GameSeeder etc.` is the intended insertion point — replace the comment with the actual class entry.

    Run `make artisan ARGS="migrate:fresh --seed"` to verify clean execution. Inspect counts via psql. Run `make pint` and `make phpstan`.
  </action>
  <verify>
    <automated>docker compose exec web php artisan migrate:fresh --seed --no-interaction 2>&1 | grep -E '(Seeding|GameSeeder|DONE)' | head -5 && docker compose exec postgres psql -U postgres trenchwars -t -c "SELECT (SELECT COUNT(*) FROM games)::text || '|' || (SELECT COUNT(*) FROM game_roles)::text || '|' || (SELECT COUNT(*) FROM game_match_types)::text || '|' || (SELECT COUNT(*) FROM game_match_type_role_limits)::text" 2>&1 | tr -d ' '</automated>
  </verify>
  <done>GameSeeder lands; counts after fresh+seed are 1|15|5|20; DatabaseSeeder wired with GameSeeder AFTER ClanTagSeeder.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Wave 0 GameSeederTest stub with GREEN idempotency + count + admin-edit-preservation assertions</name>
  <files>apps/web/tests/Feature/Database/GameSeederTest.php</files>
  <read_first>
    - apps/web/tests/Feature/Clans/DiscordGuildSeederTest.php (Phase 2 analog — idempotency assertion pattern via double seed call)
    - .planning/phases/03-games-match-types/03-RESEARCH.md (Validation Architecture § Phase Requirements → Test Map — the 3 SC-3 assertions enumerated; Pitfall 5 — duplicate-creation QueryException must NOT happen if firstOrCreate is used correctly)
    - .planning/phases/03-games-match-types/03-CONTEXT.md (SC-3 — 15 roles + 5 match types, fully editable post-seed)
  </read_first>
  <acceptance_criteria>
    - `GameSeederTest.php` replaces the Wave 0 stub. 4+ `it()` blocks:
      - `it('seeds exactly 1 Game (HLL), 15 GameRoles, 5 GameMatchTypes')` — runs `$this->seed(GameSeeder::class)`; asserts the four counts (Game::count()===1, GameRole::where('game_id', $hll->id)->count()===15, GameMatchType::where('game_id', $hll->id)->count()===5, GameMatchTypeRoleLimit::count()===20)
      - `it('is idempotent — running the seeder twice does not duplicate any row')` — seed twice; assert counts unchanged
      - `it('preserves admin edits to translatable name on re-seed (firstOrCreate other-attrs do not fire on second-run lookup)')` — seed once; manually update $hll->setTranslation('name', 'en', 'My Custom HLL Name')->save(); seed again; assert $hll->fresh()->getTranslations('name')['en'] === 'My Custom HLL Name' (NOT 'Hell Let Loose')
      - `it('preserves admin capacity edits on RoleLimit rows after re-seed')` — seed once; manually update a Scrim 50v50 RoleLimit row to capacity=99; seed again; assert the row's capacity is still 99
      - `it('seeds capacity rows only for Scrim 50v50 and Skirmish 6v6 (other match types are admin-fillable blanks)')` — seed; assert GameMatchTypeRoleLimit::where('game_match_type_id', $friendlyMatchTypeId)->count() === 0 (and same for tournament + clan_war)
      - `it('all seeded roles are scoped to HLL (no cross-game role injection)')` — seed; assert every GameRole row has game_id === $hll->id (defensive check that the cross-game saving() listener is not silently rejecting rows)
    - `make pest ARGS="--filter=GameSeederTest"` GREEN
    - The Wave 0 `placeholder` it() block is REMOVED
  </acceptance_criteria>
  <action>
    Replace the file entirely. Pattern from DiscordGuildSeederTest.php: top-level `use App\Models\Game;`, `use App\Models\GameRole;`, etc., `use Database\Seeders\GameSeeder;`. RefreshDatabase auto-applied via tests/Pest.php.

    For each `it()` block: minimal fixture setup (the seeder itself creates the data; tests verify counts or row state post-seed).

    For the admin-edit-preservation test: call `$this->seed(GameSeeder::class);` once; fetch $hll via `Game::where('key', 'hll')->first();` then `$hll->setTranslation('name', 'en', 'My Custom HLL Name');` then `$hll->save();` then seed again via `$this->seed(GameSeeder::class);`; then fresh-fetch and assert the translation is the EDITED value, NOT the seeded default. This is the Pattern 5 mechanism's critical contract.

    For the capacity-edit preservation: similar approach — find a Scrim 50v50 RoleLimit row, update capacity to 99, re-seed, fresh-fetch, assert capacity===99.

    For the "no cross-game" defensive check: `GameRole::whereHas('game', fn ($q) => $q->where('key', '!=', 'hll'))->count() === 0` — proves the seeder didn't accidentally create off-game roles.

    Run `make pest ARGS="--filter=GameSeederTest"`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Database/GameSeederTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:|placeholder)' | head -8</automated>
  </verify>
  <done>GameSeederTest GREEN with 4+ it() blocks covering count + idempotency + admin-edit preservation + scope.</done>
</task>

</tasks>

<verification>
- `make artisan ARGS="migrate:fresh --seed"` -> green; counts: 1 game | 15 roles | 5 match_types | 20 role_limits
- `make pest ARGS="--filter=GameSeederTest"` -> GREEN
- `make phpstan` -> no new errors
- `make pint ARGS="--test database/seeders tests/Feature/Database"` -> green
</verification>

<success_criteria>
HLL preset lives in GameSeeder per CONTEXT.md SC-3. Idempotency + admin-edit preservation proven by Pest — this is the D-007 "zero code changes to add a new game" contract at the runtime layer. Plans 03-06 and 03-07's Filament resources can rely on the seeded data being present for admin presence tests in plan 03-08.
</success_criteria>

<output>
After completion, create `.planning/phases/03-games-match-types/03-05-SUMMARY.md` documenting:
- GameSeeder row counts on first run and second run
- Capacity matrix decisions (Scrim 50v50 distribution + Skirmish 6v6 distribution; Friendly/Tournament/Clan War left blank per RESEARCH Q2)
- DatabaseSeeder call order
- Test scenarios covered
</output>
