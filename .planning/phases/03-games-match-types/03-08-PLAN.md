---
phase: 03-games-match-types
plan: 08
type: execute
wave: 6
depends_on: [01, 03, 05, 06, 07]
files_modified:
  - apps/web/tests/Feature/Admin/GameResourcesPresentTest.php
autonomous: true
requirements:
  - REQ-platform-vision
must_haves:
  truths:
    - "/admin/games returns 200 for admin user (SC-1 reachability)"
    - "/admin/game-match-types returns 200 for admin user (SC-2 reachability)"
    - "/admin/games/{record}/edit page renders with Profile + Audit Tabs visible AND RolesRelationManager + MatchTypesRelationManager tabs render (Pitfall 3 — relationship typo detection)"
    - "/admin/game-match-types/{record}/edit page renders with Profile + Audit Tabs AND RoleLimitsRelationManager tab renders"
    - "Non-admin users get 403 on /admin/games/create (admin-access gate verification)"
    - "GameResourcesPresentTest replaces the Wave 0 stub from plan 03-01 and is GREEN"
  artifacts:
    - path: "apps/web/tests/Feature/Admin/GameResourcesPresentTest.php"
      provides: "Filament admin reachability + RelationManager render assertions (Pitfall 3 catch)"
      contains: "assertStatus(200)"
  key_links:
    - from: "GameResourcesPresentTest"
      to: "Phase 1 PermissionSeeder + Phase 2 ClanResourcesPresentTest analog"
      via: "beforeEach seeds permissions + actingAs admin"
      pattern: "givePermissionTo\\('admin-access'\\)"
---

<objective>
Land the Filament admin reachability + RelationManager render tests for GameResource + GameMatchTypeResource. This is the security-critical net for Pitfall 3 (RelationManager `$relationship` typo silently mounts a blank table — the only thing that catches the typo is a test that GETs the edit page and asserts the relation tab content renders).

Purpose: SC-1 + SC-2 reachability is verified at the integration layer (HTTP test kernel), not just by the unit-level model + DTO tests in plans 03-03 and 03-04. The admin-access gate inheritance from Phase 1 is also verified via a non-admin 403 assertion. This test is the Phase 3 analog of Phase 2's ClanResourcesPresentTest.

Output: 1 GREEN test file replacing the Wave 0 stub from plan 03-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-games-match-types/03-RESEARCH.md
@.planning/phases/03-games-match-types/03-VALIDATION.md
@CLAUDE.md
@apps/web/tests/Feature/Admin/ClanResourcesPresentTest.php
@apps/web/tests/Feature/Admin/FilamentResourcesPresentTest.php
@apps/web/database/seeders/PermissionSeeder.php

<interfaces>
Canonical Phase 2 admin presence test pattern (verified `apps/web/tests/Feature/Admin/ClanResourcesPresentTest.php`):
- `<?php\ndeclare(strict_types=1);`
- `use App\Models\Clan; use App\Models\User; use Database\Seeders\PermissionSeeder;`
- `beforeEach(function (): void { $this->seed(PermissionSeeder::class); $this->admin = User::factory()->create(); $this->admin->givePermissionTo('admin-access'); $this->actingAs($this->admin); });`
- `it('registers ClanResource at /admin/clans', function (): void { $this->get('/admin/clans')->assertStatus(200); });`
- Pattern works because Phase 1 plan 01-12 wires AdminPanelProvider + admin-access permission gate

From Phase 2 plan 02-12 verbatim Pitfall 3 mitigation:
- `it('Filament edit page for Clan renders KeyValue description field', ...)` — load /admin/clans/{record}/edit; assertStatus(200) + assertSee('description', false) + assertSee('en', false)
- The HTML inspection assertSee is the workable HTTP-test fallback; full Livewire-based RelationManager rendering tests would require panel middleware context that's not set up in HTTP kernel

From plan 03-05 GameSeeder: seeded data exists if `$this->seed([PermissionSeeder::class, DiscordGuildSeeder::class, ClanTagSeeder::class, GameSeeder::class])` is called in beforeEach — but the test should construct its own fixtures (Game::factory()) rather than rely on the seeder so it works against RefreshDatabase

From Phase 2 plan 02-13 (verified codebase): standalone resources for child entities (ClanMembershipResource etc.) have similar reachability tests — analogous for GameMatchTypeResource

Filament v3 record route binding (Pitfall 3 + ClanResource verified):
- Game model has no getRouteKeyName() override (unlike Clan which uses 'slug') — Game uses default UUID
- So /admin/games/{uuid}/edit is the route
- GameMatchType same — UUID route binding
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP test client to Filament panel | Tests act as authenticated admin; assertions exercise the panel's middleware + page rendering pipeline |
| RelationManager $relationship to model HasMany | A typo here is silent (Pitfall 3); this test is the only programmatic detector |
| Non-admin user to admin panel | Phase 1 admin-access gate enforced; test asserts 403 for non-admin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | Tampering | RelationManager $relationship typo (Pitfall 3) | mitigate | This test GETs the edit page and asserts the relation tab content renders — typo would cause a blank table that the test catches |
| T-03-08-02 | Elevation of Privilege | Non-admin reaches /admin/games/* | mitigate | Phase 1 admin-access gate; test asserts 403 for non-admin |
| T-03-08-03 | Information Disclosure | Test fixtures leak in CI | accept | RefreshDatabase + Pest in-container DB; no real data |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace Wave 0 GameResourcesPresentTest stub with GREEN reachability + RelationManager render assertions</name>
  <files>apps/web/tests/Feature/Admin/GameResourcesPresentTest.php</files>
  <read_first>
    - apps/web/tests/Feature/Admin/ClanResourcesPresentTest.php (canonical Phase 2 analog — full structure)
    - apps/web/tests/Feature/Admin/FilamentResourcesPresentTest.php (Phase 1 multi-resource reachability test)
    - apps/web/app/Filament/Resources/GameResource.php (plan 03-06 — confirm route bindings and getPages keys)
    - apps/web/app/Filament/Resources/GameMatchTypeResource.php (plan 03-07 — same)
    - .planning/phases/03-games-match-types/03-RESEARCH.md (Pitfall 3 — "Add a Pest test that GETs /admin/games/{record}/edit, asserts 200, and checks the RelationManager tab content renders")
  </read_first>
  <acceptance_criteria>
    - `GameResourcesPresentTest.php` (replaces Wave 0 stub):
      - `<?php\ndeclare(strict_types=1);`
      - Source comment block citing this plan + the Phase 2 ClanResourcesPresentTest analog
      - Imports: `use App\Models\Game; use App\Models\GameMatchType; use App\Models\GameRole; use App\Models\User; use Database\Seeders\PermissionSeeder;`
      - `beforeEach(function (): void { $this->seed(PermissionSeeder::class); $this->admin = User::factory()->create(); $this->admin->givePermissionTo('admin-access'); $this->actingAs($this->admin); });`
      - 8+ `it()` blocks:
        - `it('registers GameResource at /admin/games', function () { $this->get('/admin/games')->assertStatus(200); })`
        - `it('registers GameMatchTypeResource at /admin/game-match-types', ...)` — same shape
        - `it('Filament create page for Game renders form with key + name fields', ...)` — assertStatus(200) on /admin/games/create + assertSee('key', false) + assertSee('name', false)
        - `it('Filament edit page for Game renders KeyValue name field with en locale key', ...)` — create a Game::factory() then GET /admin/games/{id}/edit + assertStatus(200) + assertSee('name', false) + assertSee('en', false) (Pitfall 2 verification)
        - `it('Filament edit page for Game renders the Roles RelationManager tab (Pitfall 3 typo guard)', ...)` — create a Game with one GameRole::factory()->for($game)->create() + GET edit page + assertSee('admin.game.tab.roles' label OR the role's key 'rifleman_xyz') — proves the relationship='roles' is wired correctly
        - `it('Filament edit page for Game renders the MatchTypes RelationManager tab', ...)` — same pattern for matchTypes
        - `it('Filament create page for GameMatchType requires game_id Select', ...)` — assertSee('game_id' OR 'game' label, false) on /admin/game-match-types/create
        - `it('Filament edit page for GameMatchType renders the RoleLimits RelationManager tab', ...)` — create Game + GameMatchType + GET edit + assertSee role_limits or capacity heading
        - `it('non-admin user gets 403 on /admin/games/create', function () { $nonAdmin = User::factory()->create(); $this->actingAs($nonAdmin); $this->get('/admin/games/create')->assertStatus(403); })`
        - `it('non-admin user gets 403 on /admin/game-match-types/create', ...)` — same shape for the second resource
    - `make pest ARGS="--filter=GameResourcesPresent"` GREEN
    - Wave 0 `placeholder` it() block REMOVED
  </acceptance_criteria>
  <action>
    Replace the file completely using ClanResourcesPresentTest.php as the structural analog. The beforeEach + imports + actingAs pattern are verbatim.

    For each assertion: the HTML-inspection assertSee approach is the Phase 2 workable fallback (Livewire's panel middleware context is not set up in HTTP test kernel per plan 02-12 decision). The strings to assertSee are deliberately fuzzy — `'name'` matches the form field's label/attribute; `'en'` matches the KeyValue's default sub-key; `'rifleman_xyz'` (factory-generated unique key) matches if the RelationManager rendered its child rows.

    For Pitfall 3 verification (the most security-critical assertion): create a Game::factory() then create a GameRole::factory()->for($game)->create() with a deterministic key (e.g. `'pitfall3_test_role'`). Then GET /admin/games/{game->id}/edit and `assertSee('pitfall3_test_role')`. If the relationship='roles' is correct, the RolesRelationManager renders its child rows and the assertion passes. If the relationship name is typo'd, the table is blank and the assertion fails — exactly the catch we want.

    For the non-admin 403 assertions: create a User::factory() WITHOUT givePermissionTo, actingAs that user, GET the protected route, assertStatus(403).

    Run `make pest ARGS="--filter=GameResourcesPresent"`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Admin/GameResourcesPresentTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:|placeholder)' | head -8</automated>
  </verify>
  <done>GameResourcesPresentTest GREEN; Pitfall 3 typo guard active; non-admin 403 assertions pass; Wave 0 stub removed.</done>
</task>

</tasks>

<verification>
- `make pest ARGS="--filter=GameResourcesPresent"` -> GREEN (replaces Wave 0 RED stub)
- `make phpstan` -> no new errors
- `make pint ARGS="--test tests/Feature/Admin"` -> green
</verification>

<success_criteria>
SC-1 + SC-2 admin reachability verified at the integration layer. Pitfall 3 RelationManager typo guard active. Phase 1 admin-access gate inheritance proven via non-admin 403 assertion. The test is the same security net Phase 2 ClanResourcesPresentTest provided.
</success_criteria>

<output>
After completion, create `.planning/phases/03-games-match-types/03-08-SUMMARY.md` documenting:
- 8+ it() block names + their assertion targets
- The Pitfall 3 RelationManager render-check approach
- Any HTML-inspection fallback decisions (Livewire panel context unavailability)
</output>
