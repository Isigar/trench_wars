---
phase: 10-clan-applications
plan: 07
type: execute
wave: 5
depends_on: ["10-01", "10-02", "10-03", "10-04", "10-05", "10-06"]
files_modified:
  - .planning/phases/10-clan-applications/10-PHASE-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CLAN-01, CLAN-02, CLAN-03, CLAN-04]
must_haves:
  truths:
    - "The full web + bot quality-gate suite passes on a fresh schema"
    - "All four ROADMAP success criteria are traced to passing tests"
    - "The slug-vs-UUID button discrepancy from plan 10-05 is verified end-to-end or documented as a tracked follow-up"
    - "REQUIREMENTS.md and ROADMAP.md reflect CLAN-01..04 as complete"
  artifacts:
    - path: ".planning/phases/10-clan-applications/10-PHASE-VERIFICATION.md"
      provides: "SC-1..SC-4 traceability + gate results + open items"
      contains: "SC-1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "CLAN-01..04 marked complete"
      contains: "CLAN-04"
  key_links:
    - from: ".planning/phases/10-clan-applications/10-PHASE-VERIFICATION.md"
      to: "ROADMAP Phase 10 success criteria"
      via: "SC → test mapping"
      pattern: "SC-[1-4]"
---

<objective>
Close Phase 10: run every quality gate on a fresh schema, trace all four ROADMAP success criteria to passing tests, resolve or formally track the slug-vs-UUID button discrepancy (plan 10-05), and update REQUIREMENTS.md + ROADMAP.md.

Purpose: prove the phase is shippable and leave a durable verification record (the same PHASE-VERIFICATION artifact every prior phase produced).
Output: 10-PHASE-VERIFICATION.md + requirement/roadmap status updates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-clan-applications/10-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Gate commands (all container-only per D-021). -->
<!-- Web: make pest ; make pint ARGS="--test" ; make phpstan ; make artisan ARGS="migrate:fresh --seed" -->
<!-- Bot:  cd apps/bot && node_modules/.bin/vitest run && node_modules/.bin/tsc --noEmit && node_modules/.bin/eslint . -->
<!-- Web TS: cd apps/web && node_modules/.bin/vue-tsc --noEmit -->

<!-- ROADMAP Phase 10 success criteria (the four SCs to trace). -->
From .planning/ROADMAP.md Phase 10:
  SC-1: logged-in non-member can submit from a clan's public page; appears in Filament admin view.
  SC-2: /clan apply <slug> in Discord submits via the API (not a stub).
  SC-3: duplicate pending OR already-in-clan → clear localized error on BOTH web and Discord; nothing persisted.
  SC-4: leader/officer toggles "accepting applications"; attempts to a closed clan rejected with a localized reason on BOTH surfaces.

<!-- Prior PHASE-VERIFICATION format precedent. -->
.planning/milestones/v1.0-phases/09-polish/09-PHASE-VERIFICATION.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate suite on a fresh schema; verify the button discrepancy</name>
  <read_first>
    - .planning/phases/10-clan-applications/10-05-SUMMARY.md (the slug-vs-UUID button discrepancy resolution note)
    - apps/web/routes/api.php (confirm the bot applications route binding)
    - apps/web/routes/web.php (confirm clans.apply binding)
  </read_first>
  <files>(no source files — this task only runs gates and records results into the verification artifact in Task 2)</files>
  <action>
    Run, in order, and capture pass/fail + counts for each:
    1. `make artisan ARGS="migrate:fresh --seed"` — schema durability with all Phase 10 migrations.
    2. `make pest` — full web suite (NOT filtered; proves no Phase 10 change broke a prior test).
    3. `make pint ARGS="--test"` — style gate.
    4. `make phpstan` — Larastan L8.
    5. `cd apps/web && node_modules/.bin/vue-tsc --noEmit` — web TS typecheck.
    6. `cd apps/bot && node_modules/.bin/vitest run && node_modules/.bin/tsc --noEmit && node_modules/.bin/eslint .` — full bot suite + typecheck + lint.

    Then resolve the plan-10-05 button discrepancy END-TO-END: the bot `clan_apply` button posts `decoded.clanId` (a UUID) but the web route is `{clan:slug}`. Determine the actual behavior: read 10-05-SUMMARY's chosen option, then confirm against `routes/api.php`. If the route only resolves by slug, a UUID POST from the button would 404. Decide and record ONE outcome:
      (a) If the slash-command path (`/clan apply <slug>`) is the only live CLAN-02 surface (no production code creates a clan_apply button — confirm via `grep -rn "kind: 'clan_apply'" apps/bot/src | grep -v test`), then the button mismatch is NOT on any shipping flow → document it as a tracked follow-up (note in the verification artifact + leave the button code as-is, since fixing it cleanly requires either a route change or a button-encoder change owned by a future plan).
      (b) If a button IS created somewhere in production, this is a SHIPPING defect → record it as a BLOCKING open item and recommend a follow-up gap plan (do NOT silently pass the phase).

    If any gate fails, STOP and record the failure as a blocking open item (do not mark the phase complete). Re-running the relevant earlier plan's fixes is out of scope for this plan — surface the failure.
  </action>
  <verify>
    <automated>make artisan ARGS="migrate:fresh --seed" && make pest && make pint ARGS="--test" && make phpstan && (cd apps/bot && node_modules/.bin/vitest run && node_modules/.bin/tsc --noEmit && node_modules/.bin/eslint .)</automated>
  </verify>
  <acceptance_criteria>
    - `make artisan ARGS="migrate:fresh --seed"` exits 0.
    - `make pest` is fully green (no regressions in the prior suite).
    - `make pint ARGS="--test"`, `make phpstan`, `vue-tsc --noEmit` all exit 0.
    - The bot suite (`vitest run` + `tsc --noEmit` + `eslint .`) is fully green.
    - The button slug/UUID discrepancy outcome is determined: either (a) confirmed not-on-shipping-flow + tracked, or (b) flagged blocking. `grep -rn "kind: 'clan_apply'" apps/bot/src | grep -v test` evidence is recorded.
  </acceptance_criteria>
  <done>All web + bot gates green on a fresh schema; the button discrepancy is resolved or formally tracked with grep evidence; any failure recorded as a blocking open item.</done>
</task>

<task type="auto">
  <name>Task 2: Write 10-PHASE-VERIFICATION.md + update REQUIREMENTS.md + ROADMAP.md</name>
  <read_first>
    - .planning/milestones/v1.0-phases/09-polish/09-PHASE-VERIFICATION.md (format precedent — SC traceability table, gate results, open items)
    - .planning/REQUIREMENTS.md (CLAN-01..04 checkboxes + Traceability table lines 78-81)
    - .planning/ROADMAP.md (Phase 10 entry + Progress table row)
    - Task 1 gate results
  </read_first>
  <files>.planning/phases/10-clan-applications/10-PHASE-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    Write `10-PHASE-VERIFICATION.md` following the prior-phase format. Include:
    - A header (phase, date, status: COMPLETE or PENDING per Task 1 results).
    - An SC → evidence table mapping each of the four ROADMAP success criteria to the specific passing test(s):
      - SC-1 → ClanApplyWebTest (web submit happy path) + ClanShowApplyTest (Apply block visibility) + note that the application appears in the existing Filament ClanApplicationResource admin view (Phase 2 review side).
      - SC-2 → clan.test.ts apply describe (api.post called with slug path) + BotApiClanApplicationTest happy 201.
      - SC-3 → ClanApplyServiceTest (3 guards) + BotApiClanApplicationTest (3 × 422) + ClanApplyWebTest guard case + rsvpButton.test.ts/clan.test.ts translated-error cases + the partial-unique index (10-01).
      - SC-4 → ClanAcceptsApplicationsToggleTest (leader toggles, non-member 403) + the clan_not_recruiting guard on both surfaces (service test + BotApiClanApplicationTest + ClanApplyWebTest).
    - A gate-results section (pest count, pint/phpstan/vue-tsc/bot results from Task 1).
    - An open-items section recording the button slug/UUID discrepancy outcome and any deferred ideas (Discord cover-message modal; leader "new application" notification — both deferred per CONTEXT.md).
    - A traceability line mapping CLAN-01..04 → covered.

    In `REQUIREMENTS.md`: tick the four CLAN checkboxes (`- [ ]` → `- [x]`) in the "Clan applications (CLAN)" section, and update the Traceability table rows for CLAN-01..04 from `Pending` to `Complete` (or `Verified`, matching the convention prior phases used — check the table header).

    In `ROADMAP.md`: update the Phase 10 list item (mark it done `- [x]`) and the Progress-table row for Phase 10 (Plans Complete `7/7`, Status `Complete`, Completed date). If the phase has a blocking open item from Task 1, mark status `PENDING_MANUAL_SMASH`/`Blocked` instead and do NOT tick the requirements — surface the blocker.
  </action>
  <verify>
    <automated>test -f .planning/phases/10-clan-applications/10-PHASE-VERIFICATION.md && grep -q "SC-1" .planning/phases/10-clan-applications/10-PHASE-VERIFICATION.md && grep -q "CLAN-04" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `10-PHASE-VERIFICATION.md` exists with an SC-1..SC-4 traceability table, each SC mapped to ≥ 1 named passing test.
    - `grep -c "\[x\] \*\*CLAN-0" .planning/REQUIREMENTS.md` returns 4 (all four ticked) — UNLESS a blocking item exists, in which case the unticked items + blocker are documented.
    - REQUIREMENTS.md Traceability rows for CLAN-01..04 no longer say `Pending`.
    - ROADMAP.md Phase 10 row shows `7/7` plans + a terminal status.
    - The button discrepancy outcome and the two deferred ideas are recorded in the open-items section.
  </acceptance_criteria>
  <done>10-PHASE-VERIFICATION.md traces all four SCs to passing tests; REQUIREMENTS.md + ROADMAP.md reflect CLAN-01..04 completion (or a documented blocker); deferred ideas + button discrepancy recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan runs gates and writes planning docs; it introduces no new runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07-01 | Repudiation | claiming the phase passed without evidence | mitigate | Every SC is mapped to a named, runnable test; gate results are captured from actual command runs, not asserted |
| T-10-07-02 | Tampering | merging a shipping defect (button mismatch) silently | mitigate | Task 1 explicitly determines whether the clan_apply button is on a live flow and blocks the phase if so (no silent pass) |
| T-10-07-SC | Tampering | npm/composer installs | accept | No package installs — gate execution + doc authoring only |
</threat_model>

<verification>
- All Task 1 gates exit 0 on a fresh schema.
- 10-PHASE-VERIFICATION.md exists with SC-1..SC-4 traceability.
- REQUIREMENTS.md + ROADMAP.md updated.
</verification>

<success_criteria>
- Full web + bot gate suite green on migrate:fresh --seed.
- Four SCs each traced to a passing test.
- Button discrepancy resolved or tracked (no silent pass on a shipping defect).
- CLAN-01..04 marked complete in REQUIREMENTS.md + ROADMAP.md.
</success_criteria>

<output>
Create `.planning/phases/10-clan-applications/10-07-SUMMARY.md` when done.
</output>
