---
phase: 10-clan-applications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/database/migrations/2026_06_04_100000_add_accepts_applications_to_clans.php
  - apps/web/app/Models/Clan.php
  - apps/web/app/Exceptions/ClanNotRecruitingException.php
  - apps/web/app/Exceptions/AlreadyInClanException.php
  - apps/web/app/Exceptions/DuplicateApplicationException.php
autonomous: true
requirements: [CLAN-03, CLAN-04]
must_haves:
  truths:
    - "clans table has a boolean accepts_applications column defaulting to true"
    - "A second pending application to the same clan by the same user is rejected at the DB layer"
    - "Clan model exposes accepts_applications as a boolean-cast, mass-assignable attribute"
    - "Three typed domain exceptions exist for the apply() guard failures"
  artifacts:
    - path: "apps/web/database/migrations/2026_06_04_100000_add_accepts_applications_to_clans.php"
      provides: "accepts_applications column + clan_applications_one_pending_per_clan partial unique index"
      contains: "CREATE UNIQUE INDEX clan_applications_one_pending_per_clan"
    - path: "apps/web/app/Models/Clan.php"
      provides: "accepts_applications fillable + boolean cast"
      contains: "accepts_applications"
    - path: "apps/web/app/Exceptions/ClanNotRecruitingException.php"
      provides: "ClanNotRecruitingException typed exception"
      contains: "extends \\DomainException"
    - path: "apps/web/app/Exceptions/AlreadyInClanException.php"
      provides: "AlreadyInClanException typed exception"
      contains: "extends \\DomainException"
    - path: "apps/web/app/Exceptions/DuplicateApplicationException.php"
      provides: "DuplicateApplicationException typed exception"
      contains: "extends \\DomainException"
  key_links:
    - from: "apps/web/app/Models/Clan.php"
      to: "clans.accepts_applications column"
      via: "fillable + casts()"
      pattern: "accepts_applications.*=>.*'boolean'"
---

<objective>
Lay the schema + model + exception foundation for the clan-application submission flow.

Adds the `clans.accepts_applications` boolean column (CLAN-04 recruiting toggle, default `true` so existing clans accept applications without operator action) and the `clan_applications_one_pending_per_clan` partial-unique index (CLAN-03 duplicate-pending last-line defence, mirroring the D-009 partial-unique idiom). Makes `accepts_applications` mass-assignable + boolean-cast on the `Clan` model. Ships the three typed domain exceptions the service layer (plan 10-02) and bot controller (plan 10-03) map to distinct error codes.

Purpose: every downstream plan depends on these primitives existing — the column, the index, the cast, and the three exception classes. Building them first prevents a scavenger hunt.
Output: 1 migration, 1 model edit, 3 exception classes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-clan-applications/10-CONTEXT.md
@.planning/phases/10-clan-applications/10-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Partial-unique index idiom (D-009 precedent). Schema::unique() CANNOT express WHERE; raw DB::statement is required. -->
From apps/web/database/migrations/2026_05_12_100400_create_clan_memberships_table.php:
```php
DB::statement('CREATE UNIQUE INDEX clan_memberships_one_active ON clan_memberships (user_id) WHERE left_at IS NULL;');
// down():
DB::statement('DROP INDEX IF EXISTS clan_memberships_one_active;');
```

<!-- Clan model current $fillable (no casts() method yet — must be ADDED). -->
From apps/web/app/Models/Clan.php (lines 51-62):
```php
protected $fillable = [
    'slug', 'tag', 'name', 'description', 'country_code',
    'owner_user_id', 'status', 'discord_role_id', 'discord_announce_channel_id',
];
```

<!-- clan_applications table columns (existing). status values: pending|accepted|declined|cancelled. -->
From apps/web/app/Models/ClanApplication.php $fillable:
```php
['clan_id', 'applicant_user_id', 'status', 'message', 'decided_at', 'decided_by']
```

<!-- Typed exception pattern (entire file is one line of body). -->
From apps/web/app/Exceptions/MatchNotOpenException.php:
```php
final class MatchNotOpenException extends \DomainException {}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — accepts_applications column + pending-per-clan partial unique index</name>
  <read_first>
    - apps/web/database/migrations/2026_05_12_100400_create_clan_memberships_table.php (partial-unique idiom + down() drop order)
    - apps/web/database/migrations/2026_05_14_100100_create_match_slots_table.php (second partial-unique analog)
  </read_first>
  <files>apps/web/database/migrations/2026_06_04_100000_add_accepts_applications_to_clans.php</files>
  <action>
    Create the migration class (anonymous `return new class extends Migration`). In `up()`, issue two raw `DB::statement` calls (do NOT use Blueprint for either — the boolean column can use Blueprint, but follow the raw-SQL precedent for the index since Schema::unique() cannot express WHERE):
    1. Add the column: `Schema::table('clans', fn (Blueprint $t) => $t->boolean('accepts_applications')->default(true)->after('status'));`
    2. The partial-unique index: `DB::statement('CREATE UNIQUE INDEX clan_applications_one_pending_per_clan ON clan_applications (applicant_user_id, clan_id) WHERE status = \'pending\';');`

    In `down()`, drop in REVERSE order — index first, then column: `DB::statement('DROP INDEX IF EXISTS clan_applications_one_pending_per_clan;');` then `Schema::table('clans', fn (Blueprint $t) => $t->dropColumn('accepts_applications'));`.

    Default is `true` per CONTEXT.md decision 1 (existing clans accept applications; leaders opt OUT). The index is the CLAN-03 last-line defence behind the service guard (plan 10-02). `import App\..` not needed — use `Illuminate\Database\Migrations\Migration`, `Illuminate\Database\Schema\Blueprint`, `Illuminate\Support\Facades\Schema`, `Illuminate\Support\Facades\DB`. Use the dated filename `2026_06_04_100000_add_accepts_applications_to_clans.php`.
  </action>
  <verify>
    <automated>make artisan ARGS="migrate" && make artisan ARGS="migrate:fresh --seed"</automated>
  </verify>
  <acceptance_criteria>
    - `make artisan ARGS="migrate"` applies the migration with no error.
    - `make artisan ARGS="migrate:fresh --seed"` rebuilds the full schema including this migration cleanly (durability check — proves up() runs after seeders and the index name fits Postgres' 63-byte identifier limit).
    - `grep -c "CREATE UNIQUE INDEX clan_applications_one_pending_per_clan" apps/web/database/migrations/2026_06_04_100000_add_accepts_applications_to_clans.php` returns 1.
    - `down()` drops the index BEFORE the column (source order assertion).
  </acceptance_criteria>
  <done>accepts_applications column exists on clans (default true); clan_applications_one_pending_per_clan partial unique index exists; migrate:fresh --seed is green.</done>
</task>

<task type="auto">
  <name>Task 2: Clan model — fillable + boolean cast; three typed exceptions</name>
  <read_first>
    - apps/web/app/Models/Clan.php (lines 51-62 $fillable; note NO casts() method currently exists)
    - apps/web/app/Exceptions/MatchNotOpenException.php (the entire one-line-body pattern to copy verbatim)
  </read_first>
  <files>apps/web/app/Models/Clan.php, apps/web/app/Exceptions/ClanNotRecruitingException.php, apps/web/app/Exceptions/AlreadyInClanException.php, apps/web/app/Exceptions/DuplicateApplicationException.php</files>
  <action>
    In `Clan.php`: add `'accepts_applications'` to the `$fillable` array. Then ADD a `casts()` method (it does not exist yet — the model currently has no casts) that returns `['accepts_applications' => 'boolean']`. Use the protected-method form: `protected function casts(): array { return ['accepts_applications' => 'boolean']; }`. Do NOT touch the `$translatable`, media-conversion, or sitemap blocks.

    Create three exception files in `app/Exceptions/`, each a verbatim copy of the `MatchNotOpenException` one-line-body pattern (`declare(strict_types=1)`, `namespace App\Exceptions`, `final class X extends \DomainException {}`), with a short doc-comment naming the failure mode and the bot error code each maps to:
    - `ClanNotRecruitingException` → mapped to error code `clan_not_recruiting` (clan has `accepts_applications = false`).
    - `AlreadyInClanException` → mapped to `already_in_clan` (applicant already has an active ClanMembership; reuses D-009 one-active invariant).
    - `DuplicateApplicationException` → mapped to `duplicate_application` (applicant already has a pending application to the same clan).

    These are the typed-exception → 422 mapping primitives consumed by the bot controller in plan 10-03 (mirrors the Phase 4/5 MatchNotOpenException → match_not_open pattern).
  </action>
  <verify>
    <automated>make phpstan && make pint ARGS="--test"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "accepts_applications" apps/web/app/Models/Clan.php` returns ≥ 2 (fillable entry + cast entry).
    - `apps/web/app/Models/Clan.php` defines a `casts()` method containing `'accepts_applications' => 'boolean'`.
    - Each of the three exception files exists and contains `extends \DomainException`.
    - `make phpstan` (PHPStan L8) passes — no new errors.
    - `make pint ARGS="--test"` passes — no style violations.
  </acceptance_criteria>
  <done>Clan::$fillable includes accepts_applications; casts() casts it to boolean; ClanNotRecruitingException, AlreadyInClanException, DuplicateApplicationException all exist extending \DomainException; phpstan + pint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB write path → clan_applications | Untrusted submission eventually inserts a pending row; the partial-unique index is the last-line integrity gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01-01 | Tampering | clan_applications duplicate-pending insert (race) | mitigate | `clan_applications_one_pending_per_clan` partial unique index — DB rejects a second concurrent pending row even if the service guard (plan 10-02) is bypassed or races |
| T-10-01-02 | Elevation of Privilege | Clan mass-assignment of accepts_applications | mitigate | Column is fillable here, but write access is gated by `UpdateClanProfileRequest::authorize()` (ClanPolicy::update — leader/officer) in plan 10-04; the model fillable alone grants no HTTP surface |
| T-10-01-SC | Tampering | npm/composer installs | accept | No package installs in this plan — pure migration + PHP class authoring |
</threat_model>

<verification>
- `make artisan ARGS="migrate:fresh --seed"` rebuilds the full schema (durability).
- `make phpstan` + `make pint ARGS="--test"` green.
- Index and column exist (confirmed via the migration source and a fresh migrate).
</verification>

<success_criteria>
- clans.accepts_applications boolean column exists, default true.
- clan_applications_one_pending_per_clan partial unique index exists (status='pending').
- Clan model: accepts_applications fillable + boolean cast.
- ClanNotRecruitingException, AlreadyInClanException, DuplicateApplicationException all exist.
- phpstan L8 + pint green; migrate:fresh --seed green.
</success_criteria>

<output>
Create `.planning/phases/10-clan-applications/10-01-SUMMARY.md` when done.
</output>
