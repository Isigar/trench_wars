---
phase: 02-clans-tags
plan: 13
type: execute
wave: 5
depends_on: [03, 06, 12]
files_modified:
  - apps/web/app/Filament/Resources/ClanMembershipResource.php
  - apps/web/app/Filament/Resources/ClanMembershipResource/Pages/ListClanMemberships.php
  - apps/web/app/Filament/Resources/ClanMembershipResource/Pages/ViewClanMembership.php
  - apps/web/app/Filament/Resources/ClanInviteResource.php
  - apps/web/app/Filament/Resources/ClanInviteResource/Pages/ListClanInvites.php
  - apps/web/app/Filament/Resources/ClanInviteResource/Pages/ViewClanInvite.php
  - apps/web/app/Filament/Resources/ClanApplicationResource.php
  - apps/web/app/Filament/Resources/ClanApplicationResource/Pages/ListClanApplications.php
  - apps/web/app/Filament/Resources/ClanApplicationResource/Pages/ViewClanApplication.php
  - apps/web/app/Filament/Resources/DiscordGuildResource.php
  - apps/web/app/Filament/Resources/DiscordGuildResource/Pages/ListDiscordGuilds.php
  - apps/web/app/Filament/Resources/DiscordGuildResource/Pages/EditDiscordGuild.php
  - apps/web/tests/Feature/Admin/ClanFilamentResourceTest.php
autonomous: true
requirements:
  - REQ-tenancy-single-guild
  - REQ-constraint-single-guild
  - REQ-tenancy-multi-clan
must_haves:
  truths:
    - "ClanMembershipResource registered as read-only list+view; no Create/Edit/Delete (lifecycle owned by My Clan)"
    - "ClanInviteResource registered as read-only list+view"
    - "ClanApplicationResource registered as read-only list+view"
    - "DiscordGuildResource registered with NO Create page (D-003); only Edit page accessible"
    - "All 4 navigationSort values placed per UI-SPEC (5, 6, 7, 8)"
    - "ClanFilamentResourceTest GREEN covering presence + no-Create restriction"
  artifacts:
    - path: "apps/web/app/Filament/Resources/ClanMembershipResource.php"
      provides: "Read-only membership audit listing"
      contains: "class ClanMembershipResource extends Resource"
    - path: "apps/web/app/Filament/Resources/DiscordGuildResource.php"
      provides: "Single-row guild editor (D-003)"
      contains: "Pages\\ListDiscordGuilds::route"
  key_links:
    - from: "DiscordGuildResource::getPages"
      to: "no 'create' route — only index + edit"
      via: "Filament page registration"
      pattern: "(?!.*'create')"
    - from: "ClanMembershipResource::table actions"
      to: "ViewAction only"
      via: "no Create/Edit/Delete"
      pattern: "ViewAction"
---

<objective>
Ship the remaining 4 Filament resources to satisfy D-012 completeness. All three Clan{Membership,Invite,Application}Resources are read-only list+view — the lifecycle is owned by `/my-clan` (plans 02-09, 02-10, 02-11). DiscordGuildResource explicitly omits the Create route per D-003 (Pitfall 8 — Filament shows Create by default; we drop it from getPages()).

Purpose: D-012 — every domain entity is in Filament. Audit visibility is the primary purpose; admin lifecycle changes for invites/applications/memberships happen via the My Clan UI flow that already audits. This plan finalises the admin surface.

Output: 4 resources + 8 pages (2 per resource — list + view/edit), ClanFilamentResourceTest GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-clans-tags/02-PATTERNS.md
@.planning/phases/02-clans-tags/02-RESEARCH.md
@.planning/phases/02-clans-tags/02-UI-SPEC.md
@apps/web/app/Filament/Resources/PermissionResource.php
@apps/web/app/Filament/Resources/ClanResource.php
@apps/web/lang/en/admin.php

<interfaces>
From plans 02-03, 02-06, 02-12 outputs:
- All 4 target models exist with $fillable + relations
- `lang/en/admin.php` carries label/plural_label + fields for each resource (added in plan 02-06)
- ClanResource pattern established in plan 02-12 (tabs + audit + i18n)

PermissionResource.php (Phase 1) is the structural analog for ALL 4 resources in this plan because each is "list-mostly" with restricted actions:
- ClanMembershipResource: List + View only
- ClanInviteResource: List + View only
- ClanApplicationResource: List + View only
- DiscordGuildResource: List + Edit only (no Create per D-003)

Pitfall 8 — no-Create restriction (RESEARCH.md):
- In `getPages()`, omit the 'create' route. Filament's default UI hides the Create button when no route is registered.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin to read-only resources | Read = audit visibility; no mutations from these resources |
| Admin to DiscordGuildResource | Edit only; create blocked at Filament level + verified by test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10-01 | Tampering | Direct ClanMembership.delete from admin | mitigate | ClanMembershipResource has no DeleteAction; admin must use the My Clan UI which sets left_at instead of hard-deleting |
| T-02-10-02 | Tampering | Multiple discord_guild rows created via admin | mitigate | getPages() omits 'create' route; visiting /admin/discord-guilds/create returns 404 |
| T-02-10-03 | Information Disclosure | Application/invite messages displayed to wrong admin | accept | Admin role assumes full visibility; this is by design |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 3 read-only resources (ClanMembershipResource, ClanInviteResource, ClanApplicationResource) + 6 pages</name>
  <files>
    apps/web/app/Filament/Resources/ClanMembershipResource.php,
    apps/web/app/Filament/Resources/ClanMembershipResource/Pages/ListClanMemberships.php,
    apps/web/app/Filament/Resources/ClanMembershipResource/Pages/ViewClanMembership.php,
    apps/web/app/Filament/Resources/ClanInviteResource.php,
    apps/web/app/Filament/Resources/ClanInviteResource/Pages/ListClanInvites.php,
    apps/web/app/Filament/Resources/ClanInviteResource/Pages/ViewClanInvite.php,
    apps/web/app/Filament/Resources/ClanApplicationResource.php,
    apps/web/app/Filament/Resources/ClanApplicationResource/Pages/ListClanApplications.php,
    apps/web/app/Filament/Resources/ClanApplicationResource/Pages/ViewClanApplication.php
  </files>
  <read_first>
    - apps/web/app/Filament/Resources/PermissionResource.php (read-only resource analog)
    - .planning/phases/02-clans-tags/02-UI-SPEC.md § Filament Admin Resources (lines for ClanMembershipResource, ClanInviteResource, ClanApplicationResource — exact list columns + actions specified)
    - apps/web/lang/en/admin.php (verify keys added by plan 02-06: admin.clan_membership.*, admin.clan_invite.*, admin.clan_application.*)
  </read_first>
  <acceptance_criteria>
    - `ClanMembershipResource`:
      - model = ClanMembership::class; navigationIcon = 'heroicon-o-identification'; navigationSort = 5
      - getModelLabel + getPluralModelLabel via admin.clan_membership.*
      - form(): used only by the View page — placeholder fields (since no Create/Edit). Filament v3 requires `form()` to exist; populate with View-only renderings via Placeholder components or `->disabled()`.
      - table: columns user.username, clan.name, role (badge), joined_at (sortable), left_at (sortable, success-color when null = active); filters: active only (whereNull left_at), clan SelectFilter, role SelectFilter
      - actions: ViewAction only
      - getPages(): index, view (NO create, NO edit)
    - `ClanInviteResource`:
      - model = ClanInvite::class; navigationIcon = 'heroicon-o-envelope'; navigationSort = 6
      - getModelLabel + getPluralModelLabel via admin.clan_invite.*
      - table: clan.name, invitee.username (relationship 'invitee.username'), inviter.username, status (badge), message (truncated 60 chars), decided_at; filters: status SelectFilter, clan SelectFilter
      - actions: ViewAction only
      - getPages(): index, view
    - `ClanApplicationResource`:
      - model = ClanApplication::class; navigationIcon = 'heroicon-o-inbox-arrow-down'; navigationSort = 7
      - getModelLabel + getPluralModelLabel via admin.clan_application.*
      - table: clan.name, applicant.username, status (badge), message (truncated 60), decided_at, decidedBy.username (nullable); filters: status SelectFilter, clan SelectFilter
      - actions: ViewAction only
      - getPages(): index, view
    - All 9 files declare `declare(strict_types=1)` and use proper imports.
    - `make phpstan` clean.
  </acceptance_criteria>
  <action>
    Mirror PermissionResource.php for the form/table/getPages structure. Substitute models, relationships, and column lists. Each ListXxx page extends `ListRecords` and sets `$resource = …Resource::class`. Each ViewXxx page extends `ViewRecord` and sets `$resource = …Resource::class`.

    For status badge colors: use Filament v3's `Tables\Columns\TextColumn::make('status')->badge()->color(fn ($state) => match ($state) { 'pending' => 'warning', 'accepted' => 'success', 'declined'|'revoked'|'cancelled' => 'danger', 'expired' => 'gray', default => 'gray' })`.

    Note that the form() method MUST exist on the Resource even when not used by Create/Edit — Filament's v3 ViewRecord page uses the same form schema with `->disabled()`. Build a minimal form() that lists the fields with `->disabled()->dehydrated(false)` so the View page renders them without enabling edits.

    Run phpstan + pint.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/phpstan analyse app/Filament/Resources/ClanMembershipResource.php app/Filament/Resources/ClanInviteResource.php app/Filament/Resources/ClanApplicationResource.php --no-progress --error-format=raw 2>&1 | tail -5</automated>
  </verify>
  <done>3 resources + 6 pages committed; phpstan clean.</done>
</task>

<task type="auto">
  <name>Task 2: DiscordGuildResource with no-Create restriction (D-003)</name>
  <files>
    apps/web/app/Filament/Resources/DiscordGuildResource.php,
    apps/web/app/Filament/Resources/DiscordGuildResource/Pages/ListDiscordGuilds.php,
    apps/web/app/Filament/Resources/DiscordGuildResource/Pages/EditDiscordGuild.php
  </files>
  <read_first>
    - apps/web/app/Filament/Resources/PermissionResource.php (exact analog for no-Create pattern; lines 81-88 omit 'create' route)
    - .planning/phases/02-clans-tags/02-RESEARCH.md (Pattern 4 + Pitfall 8 — no-Create restriction)
    - .planning/phases/02-clans-tags/02-PATTERNS.md § "DiscordGuildResource" (no-Create getPages snippet)
  </read_first>
  <acceptance_criteria>
    - `DiscordGuildResource`:
      - model = DiscordGuild::class; navigationIcon = 'heroicon-o-server-stack'; navigationSort = 8
      - getModelLabel = __('admin.discord_guild.label'); getPluralModelLabel = __('admin.discord_guild.plural_label') — note: these keys must exist in lang/en/admin.php. Plan 02-06 added admin.clan/.clan_tag/.clan_membership/.clan_invite/.clan_application but may have missed admin.discord_guild — if absent, add the keys in this task (Rule 2 amendment): `'discord_guild' => ['label' => 'Discord guild', 'plural_label' => 'Discord guild', 'fields' => ['guild_id' => 'Discord guild ID', 'name' => 'Guild name', 'icon_url' => 'Icon URL']]`
      - form(): TextInput guild_id (mono, required, max 32, regex `^[0-9]+$` since Discord snowflakes are numeric strings), TextInput name (max 255), TextInput icon_url (URL, nullable)
      - table: guild_id (mono), name, icon_url (URL link), created_at; no filters; only EditAction
      - getPages(): index, edit ONLY — comment: `// INTENTIONALLY no 'create' route — discord_guild holds exactly one row (D-003).`
      - No DeleteAction anywhere (preserve the singleton row)
    - 2 pages files committed.
    - `make phpstan` clean.
  </acceptance_criteria>
  <action>
    Copy PermissionResource.php's structure verbatim, substitute model + columns. The getPages() omission is the critical bit:
    ```
    /** @return array<string, PageRegistration> */
    public static function getPages(): array
    {
        return [
            'index' => Pages\ListDiscordGuilds::route('/'),
            // INTENTIONALLY no 'create' route — discord_guild holds exactly one row (D-003).
            // Seeder populates the row (DiscordGuildSeeder, plan 02-04); admin fills fields via edit only.
            'edit'  => Pages\EditDiscordGuild::route('/{record}/edit'),
        ];
    }
    ```

    If admin.discord_guild keys are missing from lang/en/admin.php (verify by reading the file), add them inline in this task as a Rule 2 amendment to plan 02-06.

    Run phpstan + pint.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/phpstan analyse app/Filament/Resources/DiscordGuildResource.php app/Filament/Resources/DiscordGuildResource/Pages --no-progress --error-format=raw 2>&1 | tail -5</automated>
  </verify>
  <done>DiscordGuildResource + 2 pages committed; no-Create restriction in place; phpstan clean.</done>
</task>

<task type="auto">
  <name>Task 3: Replace ClanFilamentResourceTest Wave 0 stub with comprehensive admin-side feature tests</name>
  <files>apps/web/tests/Feature/Admin/ClanFilamentResourceTest.php</files>
  <read_first>
    - apps/web/tests/Feature/Admin/FilamentResourcesPresentTest.php (Phase-1 analog incl. no-Create assertion for User and Player)
    - apps/web/tests/Feature/Admin/ClanResourcesPresentTest.php (from plan 02-12 — extend, don't duplicate)
  </read_first>
  <acceptance_criteria>
    - 10+ `it()` blocks:
      - it('registers ClanMembershipResource at /admin/clan-memberships')
      - it('registers ClanInviteResource at /admin/clan-invites')
      - it('registers ClanApplicationResource at /admin/clan-applications')
      - it('registers DiscordGuildResource at /admin/discord-guilds')
      - it('does NOT register Create for ClanMembershipResource (lifecycle in My Clan)') — /admin/clan-memberships/create -> 404
      - it('does NOT register Create for ClanInviteResource') — 404
      - it('does NOT register Create for ClanApplicationResource') — 404
      - it('does NOT register Create for DiscordGuildResource (D-003 single-row)') — 404
      - it('does NOT register Delete on activity_log rows') — verifies that activity_log Filament resource (Phase 1) has no Delete action; included here as a cross-resource audit-integrity check per CLAUDE.md §6
      - it('admin can view a Clan record and the activity_log tab populates after a mutation') — create a Clan, edit it via factory `->update()`, visit the View page, assert HTML contains the audit-tab partial output
    - Reuse the admin beforeEach pattern from FilamentResourcesPresentTest.
    - `make pest ARGS="--filter=ClanFilamentResource"` GREEN.
  </acceptance_criteria>
  <action>
    Pattern from FilamentResourcesPresentTest.php. The "does not register Create" tests are 404 assertions on /admin/{resource}/create URLs.

    For the activity_log integrity assertion (it #9): visit /admin/audit (the global page from Phase 1 plan 14), look for any Delete action HTML — should be absent. Use the same Livewire helper as plan 02-12 if available.

    For the audit-tab populated assertion (it #10): create a clan, then `$clan->update(['name' => 'new'])` to trigger a LogsActivity row. Visit /admin/clans/{record}, assert the page renders the audit tab and contains the recent activity. Pragmatic: assert via Inertia/Livewire that the audit-tab partial is rendered with `subject->id === $clan->id`.

    Run pest.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Admin/ClanFilamentResourceTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:.*passed)' | tail -5</automated>
  </verify>
  <done>10+ admin-resource tests GREEN; D-003 no-Create restriction verified.</done>
</task>

</tasks>

<verification>
- `make pest ARGS="--filter='Admin/(ClanResourcesPresent|ClanFilamentResource)'"` -> GREEN
- `make phpstan` -> no new errors
- `make pint --test` -> green
- All 5 Phase-2 Filament resources visible in the /admin sidebar with proper sort order (3-8)
</verification>

<success_criteria>
D-012 complete for Phase 2 — every domain entity has a Filament resource with appropriate actions. D-003 no-Create restriction proven by test. Wave 5 closes; Wave 6 is phase verification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-clans-tags/02-13-SUMMARY.md` documenting:
- 4 resource sort orders + nav icons
- DiscordGuildResource no-Create wiring
- Any admin.discord_guild lang key amendment to plan 02-06
- Test coverage summary
</output>
