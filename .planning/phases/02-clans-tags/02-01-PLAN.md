---
phase: 02-clans-tags
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/composer.json
  - apps/web/composer.lock
  - apps/web/tests/Feature/Clans/.gitkeep
  - apps/web/tests/Feature/Models/ClanMembershipModelTest.php
  - apps/web/tests/Feature/Models/ClanModelTest.php
  - apps/web/tests/Feature/Clans/ClanDirectoryTest.php
  - apps/web/tests/Feature/Clans/ClanShowTest.php
  - apps/web/tests/Feature/Clans/PlayerProfilePrivacyTest.php
  - apps/web/tests/Feature/Clans/PublicClanRoutesTest.php
  - apps/web/tests/Feature/Clans/DiscordGuildSeederTest.php
  - apps/web/tests/Feature/Clans/DiscordGuildSingleRowTest.php
  - apps/web/tests/Feature/Clans/MyClanManagementTest.php
  - apps/web/tests/Feature/Clans/ClanInviteTest.php
  - apps/web/tests/Feature/Clans/ClanApplicationTest.php
  - apps/web/tests/Feature/Admin/ClanResourcesPresentTest.php
  - apps/web/tests/Feature/Admin/ClanFilamentResourceTest.php
  - apps/web/tests/Unit/Data/PlayerProfileDataTest.php
  - apps/web/tests/Unit/Services/PlayerPrivacyGateTest.php
  - apps/web/database/factories/ClanFactory.php
  - apps/web/database/factories/ClanTagFactory.php
  - apps/web/database/factories/ClanMembershipFactory.php
  - apps/web/database/factories/ClanInviteFactory.php
  - apps/web/database/factories/ClanApplicationFactory.php
  - apps/web/database/factories/DiscordGuildFactory.php
autonomous: true
requirements:
  - REQ-tenancy-single-guild
  - REQ-constraint-single-guild
  - REQ-tenancy-multi-clan
  - REQ-goal-public-profiles
must_haves:
  truths:
    - "spatie/laravel-translatable ^6.14 is present in composer.lock"
    - "All 12 Phase-2 test stub files exist and are discoverable by Pest"
    - "Wave 0 stubs fail intentionally (red), proving infra is wired and remaining waves have a target"
    - "Six new factory stubs exist (Clan, ClanTag, ClanMembership, ClanInvite, ClanApplication, DiscordGuild)"
  artifacts:
    - path: "apps/web/composer.lock"
      provides: "spatie/laravel-translatable installation evidence"
      pattern: "spatie/laravel-translatable"
    - path: "apps/web/tests/Feature/Clans/ClanDirectoryTest.php"
      provides: "Wave 0 stub for /clans coverage"
      contains: "it("
    - path: "apps/web/tests/Unit/Services/PlayerPrivacyGateTest.php"
      provides: "Wave 0 stub for privacy gate"
      contains: "it("
    - path: "apps/web/database/factories/ClanFactory.php"
      provides: "Clan factory stub"
      contains: "class ClanFactory"
  key_links:
    - from: "apps/web/composer.json"
      to: "vendor/spatie/laravel-translatable"
      via: "composer require"
      pattern: "spatie/laravel-translatable"
    - from: "tests/Feature/Clans/*.php"
      to: "Pest discovery"
      via: "PHPUnit XML directory glob"
      pattern: "tests/Feature/Clans"
---

<objective>
Wave 0 scaffold: install `spatie/laravel-translatable ^6.14`, create the test directory + 14 failing test stubs (12 from VALIDATION + 2 model tests), and create 6 factory stubs. Wave 0 must be RED — every stub `it()` asserts `expect(true)->toBeFalse()` so subsequent waves have a concrete target. Factory stubs throw `RuntimeException` so they cannot accidentally execute before Wave 1 lands.

Purpose: Per 02-VALIDATION.md Wave 0 Requirements, infrastructure must be wired and visible to Pest before Wave 1 begins (Nyquist sampling rule). Per CLAUDE.md §1, composer runs inside the container.

Output: composer.lock with `spatie/laravel-translatable`, 14 Pest test stubs, 6 factory stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-clans-tags/02-RESEARCH.md
@.planning/phases/02-clans-tags/02-VALIDATION.md
@.planning/phases/02-clans-tags/02-PATTERNS.md
@CLAUDE.md
@apps/web/tests/Feature/Auth/FirstLoginProvisioningTest.php
@apps/web/tests/Feature/Admin/FilamentResourcesPresentTest.php
@apps/web/database/factories/PlayerFactory.php
@apps/web/database/factories/PlayerPrivacyFactory.php

<interfaces>
From apps/web/database/factories/PlayerFactory.php:
- `final class PlayerFactory extends Factory` with `protected $model = Player::class` and `public function definition(): array`
- `@extends Factory<Player>` PHPDoc; `declare(strict_types=1)`; namespace `Database\Factories`

From apps/web/tests/Feature/Auth/FirstLoginProvisioningTest.php:
- Pest functional style (`beforeEach()`, `it(...)`) — NO `class … extends TestCase`
- `declare(strict_types=1);` at top
- `use App\Models\X;` global imports
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer registry to vendor/ | New package source; vendor lock pinned in composer.lock |
| Pest test runner to app code | Test stubs MUST NOT touch production data; intentionally RED |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-00-01 | Tampering | composer install | mitigate | Pin `^6.14`; commit composer.lock; CI re-installs from lock |
| T-02-00-02 | Information Disclosure | Test stubs | accept | Stubs assert `expect(true)->toBeFalse()` — no PII, no DB writes |
| T-02-00-03 | Denial of Service | Factory stubs | mitigate | Factory `definition()` throws `RuntimeException` until Wave 1 lands real models |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install spatie/laravel-translatable inside the web container</name>
  <files>apps/web/composer.json, apps/web/composer.lock</files>
  <read_first>
    - .planning/phases/02-clans-tags/02-RESEARCH.md (Standard Stack — New; Environment Availability)
    - CLAUDE.md §1 (container-only commands), §2 (locked stack)
    - apps/web/composer.json (current require block)
  </read_first>
  <acceptance_criteria>
    - `composer.json` `require` contains `"spatie/laravel-translatable": "^6.14"`
    - `composer.lock` resolves cleanly with no other dep upgrades
    - `make composer ARGS="show spatie/laravel-translatable"` reports `6.14.*`
  </acceptance_criteria>
  <action>
    Run `make composer ARGS="require spatie/laravel-translatable:^6.14"` from the repo root. This invokes `docker compose exec web composer require ...` per CLAUDE.md §1 + D-021. Do NOT run composer on the host. Verify post-install with `make composer ARGS="show spatie/laravel-translatable"`. The `HasTranslations` trait is applied in Wave 1 plan 02-03; do not publish or wire any config in this task. If composer wants to upgrade unrelated packages, abort and report — Wave 0 is install-only.
  </action>
  <verify>
    <automated>docker compose exec web composer show spatie/laravel-translatable 2>&1 | grep -E '^versions\s*:\s*\*\s*6\.14'</automated>
  </verify>
  <done>spatie/laravel-translatable ^6.14 installed and locked; no other deps changed.</done>
</task>

<task type="auto">
  <name>Task 2: Create 14 failing Pest test stubs</name>
  <files>
    apps/web/tests/Feature/Clans/.gitkeep,
    apps/web/tests/Feature/Models/ClanModelTest.php,
    apps/web/tests/Feature/Models/ClanMembershipModelTest.php,
    apps/web/tests/Feature/Clans/ClanDirectoryTest.php,
    apps/web/tests/Feature/Clans/ClanShowTest.php,
    apps/web/tests/Feature/Clans/PlayerProfilePrivacyTest.php,
    apps/web/tests/Feature/Clans/PublicClanRoutesTest.php,
    apps/web/tests/Feature/Clans/DiscordGuildSeederTest.php,
    apps/web/tests/Feature/Clans/DiscordGuildSingleRowTest.php,
    apps/web/tests/Feature/Clans/MyClanManagementTest.php,
    apps/web/tests/Feature/Clans/ClanInviteTest.php,
    apps/web/tests/Feature/Clans/ClanApplicationTest.php,
    apps/web/tests/Feature/Admin/ClanResourcesPresentTest.php,
    apps/web/tests/Feature/Admin/ClanFilamentResourceTest.php,
    apps/web/tests/Unit/Data/PlayerProfileDataTest.php,
    apps/web/tests/Unit/Services/PlayerPrivacyGateTest.php
  </files>
  <read_first>
    - .planning/phases/02-clans-tags/02-VALIDATION.md (Per-Task Verification Map: each row's "Automated Command" specifies the file name; Wave 0 Requirements list)
    - apps/web/tests/Feature/Auth/FirstLoginProvisioningTest.php (Pest functional style header)
    - apps/web/tests/Feature/Admin/FilamentResourcesPresentTest.php (beforeEach + actingAs admin pattern — reused in Wave 5)
    - apps/web/phpunit.xml (Feature + Unit testsuites already glob subdirs)
  </read_first>
  <acceptance_criteria>
    - 16 stub files exist (14 listed under "Wave 0 stubs" + the `.gitkeep` + 2 Models tests inside the existing Models dir)
    - Each contains `declare(strict_types=1);`
    - Each contains one `it('placeholder Wave 0 stub - replace in Wave N', fn () => expect(true)->toBeFalse());`
    - `make pest ARGS="--filter='placeholder Wave 0 stub'"` discovers all 14 (each prints FAIL, not parse error)
  </acceptance_criteria>
  <action>
    Per VALIDATION.md per-task map, each Wave 0 stub maps to a future plan. Stub contents are identical structurally, only the comment changes. Example for `ClanDirectoryTest.php`:
    ```
    <?php
    declare(strict_types=1);

    /*
    | Wave 0 stub — replaced by plan 02-07 (Public routes Wave 3).
    | Covers REQ-tenancy-multi-clan public /clans access (T-02-04).
    | See .planning/phases/02-clans-tags/02-VALIDATION.md Per-Task Verification Map.
    */

    it('placeholder Wave 0 stub - replace in Wave 3', function (): void {
        expect(true)->toBeFalse();
    });
    ```
    Wave mapping:
    - `ClanModelTest`, `ClanMembershipModelTest`, `DiscordGuildSeederTest`, `DiscordGuildSingleRowTest` => Wave 1 (plan 02-03 + 02-04)
    - `PlayerProfilePrivacyTest`, `PublicClanRoutesTest`, `Unit/Data/PlayerProfileDataTest`, `Unit/Services/PlayerPrivacyGateTest`, `ClanDirectoryTest`, `ClanShowTest` => Waves 2-3 (plans 02-05 .. 02-08)
    - `MyClanManagementTest`, `ClanInviteTest`, `ClanApplicationTest` => Wave 4 (plans 02-09, 02-10, 02-11)
    - `ClanResourcesPresentTest`, `ClanFilamentResourceTest` => Wave 5 (plan 02-12, 02-13)
    Add zero `use App\Models\…` imports (those models do not exist yet). Use ONLY Pest's global `it()`. Do NOT modify `phpunit.xml`. Touch `tests/Feature/Clans/.gitkeep` to commit the empty-ish directory (it will host the 9 test files immediately). Per CLAUDE.md §4 use `it()` only — no PHPUnit class syntax. Run pint on each new file after writing.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter='placeholder Wave 0 stub' --no-coverage 2>&1 | grep -E 'Tests:.*failed'</automated>
  </verify>
  <done>14 stubs exist and are RED; Pest discovers them; no parse errors in suite.</done>
</task>

<task type="auto">
  <name>Task 3: Create 6 factory stubs (Clan, ClanTag, ClanMembership, ClanInvite, ClanApplication, DiscordGuild)</name>
  <files>
    apps/web/database/factories/ClanFactory.php,
    apps/web/database/factories/ClanTagFactory.php,
    apps/web/database/factories/ClanMembershipFactory.php,
    apps/web/database/factories/ClanInviteFactory.php,
    apps/web/database/factories/ClanApplicationFactory.php,
    apps/web/database/factories/DiscordGuildFactory.php
  </files>
  <read_first>
    - apps/web/database/factories/PlayerFactory.php (exact analog for ClanFactory)
    - apps/web/database/factories/PlayerPrivacyFactory.php (analog for association factories — parent factory FK pattern)
    - .planning/phases/02-clans-tags/02-PATTERNS.md (ClanFactory + ClanMembershipFactory sections — Wave 1 replaces these stubs)
  </read_first>
  <acceptance_criteria>
    - All 6 files exist with `declare(strict_types=1);` and namespace `Database\Factories`
    - Each `definition()` throws `\RuntimeException('Wave 0 stub - plan 02-03 replaces this.')`
    - Each `protected $model` uses the string form `'App\\Models\\X'` (NOT `X::class`) so the not-yet-existing class is not eager-loaded
    - `make pint ARGS="--test database/factories"` passes
    - `make phpstan` does not regress (no new errors)
  </acceptance_criteria>
  <action>
    For each factory write the stub form. Example for ClanFactory:
    ```
    <?php
    declare(strict_types=1);

    namespace Database\Factories;

    use Illuminate\Database\Eloquent\Factories\Factory;

    /**
     * Wave 0 stub — plan 02-03 (Wave 1, Models) replaces this with the real definition.
     * Source analog: apps/web/database/factories/PlayerFactory.php.
     *
     * @extends Factory<\App\Models\Clan>
     */
    final class ClanFactory extends Factory
    {
        /** @var class-string */
        protected $model = 'App\\Models\\Clan';

        /** @return array<string, mixed> */
        public function definition(): array
        {
            throw new \RuntimeException('Wave 0 stub - plan 02-03 (Models) replaces this with real definitions.');
        }
    }
    ```
    Use the STRING form of `$model` (`'App\\Models\\Clan'`) so PHP does not eager-load the not-yet-existing class. Wave 1 plan 02-03 changes `$model` to `Clan::class` plus a `use App\Models\Clan;` import. Apply the same pattern verbatim to the 5 other factories. Run `make pint database/factories` to auto-format afterwards.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pint --test database/factories/ClanFactory.php database/factories/ClanTagFactory.php database/factories/ClanMembershipFactory.php database/factories/ClanInviteFactory.php database/factories/ClanApplicationFactory.php database/factories/DiscordGuildFactory.php 2>&1 | tail -5</automated>
  </verify>
  <done>6 factory stubs exist; pint clean; phpstan does not regress.</done>
</task>

</tasks>

<verification>
- `docker compose exec web composer show spatie/laravel-translatable` -> version `6.14.*`
- `make pest ARGS="--filter='placeholder Wave 0 stub'"` -> all 14 stubs reported failed (intentional RED)
- `make pint ARGS="--test"` -> green
- `make phpstan` -> no NEW errors (existing baseline acceptable)
</verification>

<success_criteria>
Wave 0 RED gate proven: spatie/laravel-translatable installed, 14 RED Pest stubs visible to runner, 6 factory stubs guard against premature execution. Wave 1 plans (02-02 migrations, 02-03 models, 02-04 seeders) have concrete targets to convert RED to GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/02-clans-tags/02-01-SUMMARY.md` documenting:
- composer.lock diff (one line)
- test stub paths created
- factory stub strategy (string `$model` for forward-compat)
- Any deviations
</output>
