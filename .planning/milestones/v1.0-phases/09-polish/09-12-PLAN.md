---
phase: 09-polish
plan: 12
type: execute
wave: 9
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11]
files_modified:
  - .planning/phases/09-polish/09-PHASE-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - apps/web/tests/Feature/I18n/Phase9I18nKeyCoverageTest.php
autonomous: false
requirements:
  - REQ-polish-v1

must_haves:
  truths:
    - "Phase9I18nKeyCoverageTest GREEN: every notifications.*, leaderboards.*, moderation.*, a11y.*, reports.* key referenced in app code has a corresponding entry in lang/en/*.php (no missing keys; no orphan keys above an acceptable threshold)."
    - "All 7 quality gates GREEN: make pest (full suite, ~1234+ tests), make pint ARGS='--test', make phpstan, pnpm --filter web build, pnpm --filter bot build, pnpm --filter rcon-worker build, axe-core CI workflow run output (artifact uploaded if any violation)."
    - "09-PHASE-VERIFICATION.md exists and maps SC-1..SC-5 to the test files that prove them GREEN, plus 12 Pitfalls LOCKED inline + 8 Open Questions LOCKED inline + canonical D-09-* bindings."
    - "ROADMAP.md Phase 9 entry flipped to [x] complete with Completed: 2026-05-14 (or actual date)."
    - "REQUIREMENTS.md updated — no v1 requirements were Phase-9-mapped, so this just appends the line item to the Last updated log."
    - "STATE.md updated — phase 9 plans 12/12 complete; total 120/120; percent 100; status flipped to 'Phase 9 COMPLETE' (or PENDING_MANUAL_SMOKE if some checkpoints deferred)."
  artifacts:
    - path: ".planning/phases/09-polish/09-PHASE-VERIFICATION.md"
      provides: "SC-1..SC-5 traceability + Pitfalls/OpenQuestions LOCKED + manual smoke checklist"
      contains: "SC-1"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 9 entry flipped to complete"
      contains: "Phase 9.*Complete"
    - path: ".planning/STATE.md"
      provides: "Phase 9 complete; 12/12 plans"
      contains: "Phase 9 COMPLETE"
    - path: "apps/web/tests/Feature/I18n/Phase9I18nKeyCoverageTest.php"
      provides: "i18n key coverage regression guard for Phase 9 namespaces"
      contains: "Phase9I18nKeyCoverageTest"
  key_links:
    - from: "09-PHASE-VERIFICATION.md"
      to: "SC-1..SC-5 test file references"
      via: "explicit GREEN test catalog"
      pattern: "GREEN"
    - from: "ROADMAP.md Phase 9 entry"
      to: "100% project progress"
      via: "checkbox flip"
      pattern: "\\[x\\] \\*\\*Phase 9"
---

<objective>
Close Phase 9 — run the 7 quality gates, turn the i18n coverage Pest test GREEN, author 09-PHASE-VERIFICATION.md mapping SC-1..SC-5 to GREEN evidence + 12 Pitfalls LOCKED + 8 Open Questions LOCKED, and update ROADMAP / REQUIREMENTS / STATE.

Purpose: Close the round-1 ship. Phase 9 is the buffer phase — closing it closes the entire round-1 scope. No new work surfaces here; this plan is verification + bookkeeping only.

Output: 1 i18n test GREEN + 1 PHASE-VERIFICATION doc + 3 project-state file updates + 7 quality gate evidence captured in summary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-polish/09-RESEARCH.md
@.planning/phases/09-polish/09-CONTEXT.md
@.planning/phases/09-polish/09-VALIDATION.md
@.planning/phases/08-rcon-automation/08-PHASE-VERIFICATION.md
@CLAUDE.md

<interfaces>
PHASE-VERIFICATION template (Phase 8 plan 08-13 produced 08-PHASE-VERIFICATION.md). Layout sections:
1. Phase summary (one paragraph).
2. SC-1..SC-5 traceability: each SC -> list of GREEN tests + brief description.
3. Pitfalls LOCKED — for each of the 12 Pitfalls from RESEARCH, record the mitigation evidence (file/line, test name).
4. Open Questions LOCKED — for each of the 8 (1-8 in RESEARCH.md), record the answer + which plan implemented it.
5. Canonical bindings (D-09-XX): catalog architectural choices made during execution.
6. Manual smoke checklist (subset of plan 09-VALIDATION.md):
   - Bell flow + Discord DM receipt
   - Leaderboard performance under load
   - Moderator BulkAction audit trail
   - A11y keyboard nav (plan 09-10 checkpoint already captured this)
   - Rate-limit boundary (plan 09-11 checkpoint already captured this)
7. Quality gate evidence: make pest output tail, make pint output, make phpstan output, pnpm build outputs, axe-core CI run link.

ROADMAP.md Phase 9 section currently has placeholder plan list (from 02-* copies — bug surfaced by current ROADMAP). This plan REPAIRS the Phase 9 plan list to reflect actual 09-* plans authored in this phase. Verify before flipping completion.

Phase9I18nKeyCoverageTest pattern: model after existing Phase 7 CmsI18nKeyCoverageTest (Phase 7 plan 07-12). Scan PHP + Vue + JS source for __() / t() / trans() calls; cross-reference against lang/en/{notifications,leaderboards,moderation,a11y,reports}.php; assert no missing keys.

Phase 8 plan 08-13 PHASE-VERIFICATION shape — mirror the section order; ~60 D-08-* bindings catalogued; PENDING_MANUAL_SMOKE 4-item operator walkthrough listed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase9I18nKeyCoverageTest GREEN + run 7 quality gates</name>
  <files>
    apps/web/tests/Feature/I18n/Phase9I18nKeyCoverageTest.php
  </files>
  <action>
Turn `Phase9I18nKeyCoverageTest.php` GREEN, modelled after existing CmsI18nKeyCoverageTest (Phase 7 plan 07-12). Steps:

1. Load Phase 7's CmsI18nKeyCoverageTest.php to copy idiom.
2. Implement the Phase 9 namespaces version:
   - Scan `apps/web/app/Notifications/`, `apps/web/app/Services/{NotificationDispatcher,BanService,DisputeService,LeaderboardService}.php`, `apps/web/app/Filament/Resources/{UserResource,MatchResource,MatchDisputeResource,AbuseReportResource}*.php`, `apps/web/app/Http/Controllers/{NotificationsController,LeaderboardsController,ReportsController}.php`, `apps/web/app/Http/Controllers/Account/NotificationPreferencesController.php`, `apps/web/resources/js/Pages/{Notifications,Account,Leaderboards,Report}/`, `apps/web/resources/js/Components/{NotificationsBell,LeaderboardTable,ReportButton,ClanLogo,PlayerAvatar,ArticleCover}.vue`.
   - Extract every `__('foo.bar.baz')` (PHP) and `t('foo.bar.baz')` (Vue) reference with namespaces in {notifications, leaderboards, moderation, a11y, reports}.
   - Load `lang/en/{notifications,leaderboards,moderation,a11y,reports}.php` arrays.
   - Assert every referenced key exists in the loaded arrays.
   - Allowed orphans (keys in PHP arrays but no code reference): tolerate up to N (project-specific tolerance — Phase 7 test allows ~10 orphans for runway). Pick threshold matching Phase 7.

3. Run the test; if any keys are missing, add them to the appropriate lang/en/*.php file with sensible English copy.

After GREEN, run the 7 quality gates and capture outputs (will be referenced in Task 2 verification doc):

Quality gates:
1. `make pest` — full Pest suite (~1234+ tests; expected GREEN).
2. `make pint ARGS="--test"` — Pint clean.
3. `make phpstan` — PHPStan L8 clean.
4. `docker compose exec web pnpm --filter web build` — Vue/Vite build.
5. `docker compose exec web pnpm --filter @trenchwars/bot build` (or equivalent — Phase 5 ships the bot build).
6. `docker compose exec web pnpm --filter @trenchwars/rcon-worker build` (Phase 8 ships).
7. `.github/workflows/a11y.yml` — confirm exists; manual trigger via gh CLI is OPTIONAL since CI run on next push is canonical.

Capture each tail output to a temporary file under `.planning/phases/09-polish/quality-gates-output.txt` for inclusion in 09-PHASE-VERIFICATION.md.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="Phase9I18nKeyCoverageTest" --no-coverage 2>&1 | tail -10 && docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | tail -5 && docker compose exec web ./vendor/bin/pint --test 2>&1 | tail -5 && docker compose exec web ./vendor/bin/phpstan analyse --no-progress 2>&1 | tail -5</automated>
  </verify>
  <done>I18n test GREEN; 7 quality gates evidence captured; any missing keys filled in.</done>
</task>

<task type="auto">
  <name>Task 2: Author 09-PHASE-VERIFICATION.md + update ROADMAP + REQUIREMENTS + STATE</name>
  <files>
    .planning/phases/09-polish/09-PHASE-VERIFICATION.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <action>
Author `.planning/phases/09-polish/09-PHASE-VERIFICATION.md` modelled after Phase 8's 08-PHASE-VERIFICATION.md. Required sections:

## Phase 9 Summary
One paragraph: 12 plans, ~30+ files added/modified, ~30 new Pest tests GREEN, full project test count climbs from 1134 to (actual). Notifications hub + leaderboards + moderator tooling + performance pass + a11y + security hardening — all delivered without violating D-001..D-021.

## Success Criteria Traceability

### SC-1 — Notifications hub
- GREEN tests: NotificationsBellTest, MatchStartingSoonNotificationTest, NotificationDispatcherTest, NotificationDispatcherIdempotencyTest, UserNotificationPreferencesTest, DiscordChannelOutboxTest.
- Plans: 09-03 (Notifications + DiscordChannel), 09-04 (Dispatcher + observers), 09-06 (UI).
- Evidence: bell renders unread count via shared prop; preferences page edits 5x2 matrix; default rules wired (match starting 1h/15m, cancelled, result published with discord-default-off per Open Question 3 LOCKED).

### SC-2 — Leaderboards
- GREEN tests: LeaderboardServiceTopPlayersTest, LeaderboardServiceTopClansTest, LeaderboardCacheTest, LeaderboardPrivacyTest, CacheTagFlushTest, LeaderboardsQueryBudgetTest.
- Plans: 09-05 (service + cache + observers), 09-06 (controller + page).
- Evidence: window=7d/30d/all toggle works; D-018 PlayerPrivacyGate honored; <=4 query budget; tagged cache invalidation on stat/result mutations.

### SC-3 — Moderator tooling
- GREEN tests: UserResourceBanBulkActionTest, MatchResourceBulkCancelTest, MatchDisputeWorkflowTest, AbuseReportWorkflowTest, ModeratorPermissionGateTest, ModeratorAuditLogTest.
- Plans: 09-07 (services + Filament BulkActions + MatchDisputeResource), 09-11 (AbuseReportResource).
- Evidence: BanService + DisputeService with audit log; Filament BulkActions gated by spatie permission; dispute state machine open->under_review->{resolved,rejected}; abuse-report queue.

### SC-4 — Performance pass
- GREEN tests: AppServiceProviderStrictModeTest, LeaderboardsQueryBudgetTest, ClansQueryBudgetTest, CacheTagFlushTest, ClanLogoWebpConversionTest, ArticleCoverWebpConversionTest.
- Plans: 09-05 (cache), 09-08 (strict mode + N+1 sweep), 09-09 (WebP).
- Evidence: Model::shouldBeStrict in non-prod; documented cache-key strategy at .planning/phases/09-polish/CACHE-STRATEGY.md; WebP variants for Clan/Player/Article media; query budgets locked for representative pages.

### SC-5 — Accessibility + security
- GREEN tests: PublicPagesHtmlLangTest, VueFormLabelsTest, RateLimiterDefinitionsTest, PublicApiThrottleTest, ReportAbuseTest, ReportAbuseThrottleTest.
- Plans: 09-10 (a11y), 09-11 (security + abuse).
- Evidence: :focus-visible CSS site-wide; <html lang> verified; form labels enforced; 4 rate limiters; abuse report flow + admin queue.

## 12 Pitfalls — Resolution Catalog

For each of the 12 RESEARCH Pitfalls:
- Pitfall 1 (Cache::tags on non-Redis): mitigated via CACHE_STORE=redis preflight in plan 09-01.
- Pitfall 2 (Strict mode breaks existing tests): handled in plan 09-08 fix-as-found sweep.
- Pitfall 3 (DiscordChannel direct webhook bypass): mitigated in plan 09-03 DiscordChannelOutboxTest asserts Http::assertNothingSent.
- Pitfall 4 (databaseType discriminator collision): mitigated in plan 09-03 reflection test.
- Pitfall 5 (Idempotency race): mitigated in plan 09-04 via onOneServer + withoutOverlapping + alreadyDispatched check.
- Pitfall 6 (Imagick not present): preflight in plan 09-01; verified extension present.
- Pitfall 7 (uuidMorphs vs morphs): mitigated in plan 09-02 migrations.
- Pitfall 8 (BulkAction modal silent close): mitigated in plan 09-07 via required+minLength.
- Pitfall 9 (Cache::flexible silent failure): mitigated in plan 09-05 via try/catch + report() in LeaderboardService.
- Pitfall 10 (discord_outbound CHECK constraint): mitigated in plan 09-02 migration extension.
- Pitfall 11 (axe scans behind auth): mitigated in plan 09-10 — public routes only.
- Pitfall 12 (strict mode + RefreshDatabase + factories): mitigated in plan 09-08 N+1 sweep with $model->load(...).

## 8 Open Questions — Resolution Catalog

- OQ1 (WebP fallback): LOCKED no fallback v1 — plan 09-09.
- OQ2 (Notification batching): LOCKED deferred to v2 — out of scope.
- OQ3 (match_result_published Discord DM): LOCKED default-off — plan 09-03 User::enabledNotificationChannels special-case.
- OQ4 (Ban scope): LOCKED site-wide v1 — plan 09-02 bans table has no clan_id.
- OQ5 (Moderator panel): LOCKED single panel with per-resource gates — plan 09-07.
- OQ6 (WebP regenerate): LOCKED yes — plan 09-09 trenchwars:media:regenerate-webp command.
- OQ7 (Notification retention): LOCKED 90 days — plan 09-04 notifications:prune daily scheduler.
- OQ8 (Telescope install): LOCKED NO — debugbar covers v1.

## Canonical Bindings (D-09-*)

Catalogue the architectural decisions made in plans 09-01..09-11:
- D-09-01-A: Wave 0 stubs idiom matches Phase 8 (skipped tests + factory @phpstan-ignore-next-line).
- D-09-02-A: uuid('id')->primary() on notifications (not autoincrement bigint).
- D-09-02-B: match_disputes ON DELETE CASCADE for match_id (A9 LOCKED).
- D-09-02-C: Partial UNIQUE on match_disputes(match_id, raised_by_user_id) WHERE status='open' (Pitfall 11 in security).
- D-09-03-A: DiscordChannel writes outbox row only — no Http calls (Pitfall 3).
- D-09-03-B: Unique databaseType discriminator per Notification class (Pitfall 4).
- D-09-03-C: enabledNotificationChannels special-case for match_result_published Discord default-off.
- D-09-04-A: Schedule::command(...)->withoutOverlapping()->onOneServer() for cron.
- D-09-04-B: Notification retention 90 days via prune cron.
- D-09-05-A: Cache::tags(['leaderboards', 'lb:players:{window}'])->flexible([600,3600]).
- D-09-05-B: MatchPlayerStatObserver::saved + MatchResultObserver flush 'leaderboards' tag.
- D-09-06-A: Inertia shared prop unread_notifications_count (closure, lazy-eval).
- D-09-06-B: D-018 enforced at LeaderboardEntryData::fromQueryResult.
- D-09-07-A: Single Filament panel (Open Question 5 LOCKED).
- D-09-07-B: Spatie moderator role + 5 perms idempotently seeded via ModeratorRoleSeeder.
- D-09-07-C: Every BulkAction has ->visible permission gate.
- D-09-08-A: Model::shouldBeStrict(! isProduction()) in AppServiceProvider.
- D-09-08-B: Query budgets: /leaderboards <=4, /clans <=8 (codified in Pest).
- D-09-09-A: WebP only — no JPEG fallback v1 (Open Question 1 LOCKED).
- D-09-09-B: ->queued() on all conversions (Horizon-deferred).
- D-09-10-A: axe-core scans public routes only (Pitfall 11).
- D-09-11-A: 4 new RateLimiters (public-api 30/min, auth 10/min, notifications-read 120/min, report-abuse 5/hr).
- D-09-11-B: abuse_reports state machine pending->dismissed|actioned with optional linked Ban.

## Pending Manual Smoke

If any 09-10 or 09-11 checkpoints not yet approved at time of phase close, list them here as PENDING_MANUAL_SMOKE:
- A. axe-core CI workflow first run (push to master triggers; verify report).
- B. Manual keyboard nav (plan 09-10 checkpoint — record approval status).
- C. Rate-limit boundary smoke (plan 09-11 checkpoint — record approval status).
- D. Notification bell + Discord DM receipt (live Discord delivery requires bot+web both running).

## Quality Gate Outputs

(From Task 1's captured `quality-gates-output.txt` — paste tail of each gate's output.)

Now update three project state files:

Edit `.planning/ROADMAP.md`:
1. Locate `### Phase 9: Polish` section.
2. Replace the (buggy) plan list that has 02-* names with the actual 09-* names:
```
**Plans**: 12 plans
- [x] 09-01-PLAN.md — Wave 0 scaffolding (29 RED stubs + 4 factory stubs + 5 i18n namespaces + Imagick preflight)
- [x] 09-02-PLAN.md — Migrations (notifications + user_notification_preferences + bans + match_disputes + abuse_reports + discord_outbound CHECK extension + mps kills index)
- [x] 09-03-PLAN.md — Models + 5 Notification classes + DiscordChannel (D-004 outbox) + 3 GREEN tests
- [x] 09-04-PLAN.md — NotificationDispatcher + 2 commands + 4 observers + 2 GREEN tests
- [x] 09-05-PLAN.md — LeaderboardService + 2 DTOs + MatchResultObserver/MatchPlayerStatObserver cache flush + 4 GREEN tests
- [x] 09-06-PLAN.md — NotificationsController + LeaderboardsController + 3 Vue pages + bell component + 2 GREEN tests
- [x] 09-07-PLAN.md — BanService + DisputeService + Filament BulkActions + MatchDisputeResource + ModeratorRoleSeeder + 5 GREEN tests
- [x] 09-08-PLAN.md — Model::shouldBeStrict + N+1 sweep + query budget tests + 3 GREEN tests
- [x] 09-09-PLAN.md — WebP conversions on Clan/Player/Article + regenerate command + 3 Vue components + 2 GREEN tests
- [x] 09-10-PLAN.md — A11y CSS + axe-core CI + 2 GREEN tests + keyboard nav checkpoint
- [x] 09-11-PLAN.md — 4 RateLimiters + AbuseReportResource + ReportButton + 5 GREEN tests + rate-limit checkpoint
- [x] 09-12-PLAN.md — [BLOCKING] phase verification + ROADMAP/STATE updates + 7 quality gates GREEN
```
3. Flip `- [ ] **Phase 9: Polish**` to `- [x] **Phase 9: Polish** ... _Completed 2026-05-14_` (or actual date).
4. Update progress table: 9. Polish | 12/12 | Complete | 2026-05-14.
5. Update overall progress to 100% / 9 of 9 phases.

Edit `.planning/REQUIREMENTS.md`:
- No v1 requirements were Phase 9-mapped (per ROADMAP). Append to "Last updated" footer: "Last updated: 2026-05-14 — Phase 9 close: round-1 polish complete; no v1 REQ flips (consumed polish backlog only); 09-PHASE-VERIFICATION.md maps SC-1..SC-5 + 12 Pitfalls + 8 Open Questions LOCKED inline; PENDING_MANUAL_SMOKE list per 09-PHASE-VERIFICATION."

Edit `.planning/STATE.md`:
- Update header status: "Phase 9 COMPLETE (or PENDING_MANUAL_SMOKE) — 09-12 executed; 09-PHASE-VERIFICATION.md authored; ROADMAP/REQUIREMENTS/STATE updated."
- Update Current Position: "Phase 09 (Polish) — COMPLETE | Plan: 12/12".
- Update Progress: 100% (9/9 phases; 120/120 plans through Phase 9).
- Update phase table row: 9. Polish | 12/12 | Complete | 2026-05-14.
- Append to Accumulated Context > Decisions: D-09-* canonical bindings (top 10 most significant — see 09-PHASE-VERIFICATION.md).
- Append a Phase 09 P12 row to the performance metrics table.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-polish/09-PHASE-VERIFICATION.md && grep -q "Phase 9: Polish.*Completed" .planning/ROADMAP.md && grep -q "Phase 9 COMPLETE" .planning/STATE.md</automated>
  </verify>
  <done>09-PHASE-VERIFICATION.md authored with SC-1..SC-5 + 12 Pitfalls + 8 OQs + D-09-* bindings; ROADMAP flipped to [x] complete; REQUIREMENTS footer updated; STATE.md status updated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final phase-close review</name>
  <what-built>
    Phase 9 fully closed: 12 plans complete; ~30 new tests GREEN; 5 SCs verified; all 12 Pitfalls + 8 Open Questions LOCKED inline; ROADMAP / REQUIREMENTS / STATE updated; 7 quality gates GREEN.
  </what-built>
  <how-to-verify>
1. Open `.planning/phases/09-polish/09-PHASE-VERIFICATION.md` and read SC-1..SC-5 sections; confirm each lists the GREEN Pest test files that prove the SC.
2. Open `.planning/ROADMAP.md` Phase 9 entry; confirm checkbox is flipped to [x] and Completed date matches today.
3. Open `.planning/STATE.md`; confirm 100% progress + Phase 9 COMPLETE.
4. Run `docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | tail -3` and confirm full Pest GREEN.
5. Check whether plan 09-10 (keyboard nav) and plan 09-11 (rate-limit boundary) checkpoints were approved or PENDING. If PENDING, confirm 09-PHASE-VERIFICATION.md lists them in the Pending Manual Smoke section.
6. Round-1 ship sign-off: the entire 10-12 week round-1 scope per ROADMAP overview is now complete (Foundations through Polish). Confirm overall progress is 9/9 phases.
  </how-to-verify>
  <resume-signal>Type "approved" to ship round-1; type "approved with PENDING" if checkpoints from 09-10/09-11 remain; type "blocked" with description if any SC evidence is missing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project state files->source of truth | .planning/{ROADMAP,REQUIREMENTS,STATE}.md crossings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12-01 | T (Tampering) | Phase-close updates accidentally regress prior phase entries | mitigate | Read current ROADMAP/REQUIREMENTS/STATE before edit; only modify Phase 9 sections + progress footer; verify diff before commit |
| T-09-12-02 | R (Repudiation) | 09-PHASE-VERIFICATION.md missing evidence makes future audits difficult | mitigate | Required section list enforced in plan; each SC explicitly lists GREEN tests; Pitfalls all catalogued |
| T-09-12-03 | D (DoS) | Full Pest run timeout in CI | accept | Local make pest is the gate; if CI times out, increase runner timeout; not a Phase 9 concern |
</threat_model>

<verification>
- 1 i18n Pest test GREEN.
- 7 quality gates evidence captured.
- 09-PHASE-VERIFICATION.md authored with all required sections.
- ROADMAP / REQUIREMENTS / STATE updated.
- Final checkpoint approved.
</verification>

<success_criteria>
- Phase 9 closed end-to-end.
- All 5 SCs traced to GREEN tests.
- All 12 Pitfalls LOCKED inline.
- All 8 Open Questions LOCKED inline.
- ~25 D-09-* canonical bindings catalogued.
- Round-1 ship sign-off (or PENDING_MANUAL_SMOKE with explicit deferred items).
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-12-SUMMARY.md` capturing: total plan execution time, total test count delta (1134 -> ?), final 7 quality gate output tails, round-1 ship status, any deferred manual smokes.
</output>
