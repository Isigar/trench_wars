---
phase: 09-polish
plan: 08
type: execute
wave: 7
depends_on: [05, 06]
files_modified:
  - apps/web/app/Providers/AppServiceProvider.php
  - apps/web/app/Http/Controllers/ClansController.php
  - apps/web/app/Http/Controllers/PlayersController.php
  - apps/web/app/Http/Controllers/MatchesController.php
  - apps/web/app/Http/Controllers/TournamentsController.php
  - apps/web/app/Http/Controllers/BlogController.php
  - apps/web/app/Http/Controllers/HomeController.php
  - apps/web/app/Http/Controllers/LeaderboardsController.php
  - apps/web/tests/Unit/AppServiceProviderStrictModeTest.php
  - apps/web/tests/Feature/Performance/LeaderboardsQueryBudgetTest.php
  - apps/web/tests/Feature/Performance/ClansQueryBudgetTest.php
autonomous: true
requirements:
  - REQ-polish-v1

must_haves:
  truths:
    - "AppServiceProvider::boot() calls Model::shouldBeStrict(! $this->app->isProduction()) so dev/test environments fail loud on lazy loads (SC-4)."
    - "All existing Phase 1-8 tests stay GREEN after the strict-mode flip (Pitfall 2 mitigation — N+1s in feed code paths fixed in this plan)."
    - "/leaderboards runs <= 4 queries (LeaderboardsQueryBudgetTest GREEN)."
    - "/clans runs <= 8 queries (ClansQueryBudgetTest GREEN)."
    - "Phase 1-8 controllers that lazy-load have ->with([relations]) additions documented + applied."
    - "AppServiceProviderStrictModeTest GREEN."
  artifacts:
    - path: "apps/web/app/Providers/AppServiceProvider.php"
      provides: "Model::shouldBeStrict + Vite::prefetch + (rate limiter from plan 09-11)"
      contains: "Model::shouldBeStrict"
    - path: "apps/web/tests/Feature/Performance/LeaderboardsQueryBudgetTest.php"
      provides: "Query budget regression guard for /leaderboards (<= 4 queries)"
      contains: "DB::enableQueryLog"
    - path: "apps/web/tests/Feature/Performance/ClansQueryBudgetTest.php"
      provides: "Query budget regression guard for /clans (<= 8 queries)"
      contains: "DB::enableQueryLog"
  key_links:
    - from: "AppServiceProvider::boot"
      to: "Eloquent ORM strict-mode flag"
      via: "Model::shouldBeStrict(! production)"
      pattern: "shouldBeStrict"
    - from: "controllers"
      to: "Eloquent queries"
      via: "->with([relations]) eager-loads"
      pattern: "->with\\("
---

<objective>
Flip on `Model::shouldBeStrict()` in non-production environments to make N+1s test-suite-RED, then sweep through Phase 1-8 controllers fixing any LazyLoadingViolation exceptions surfaced. Lock query budgets via Pest tests for two representative public pages (/leaderboards and /clans). SC-4 backend correctness.

Purpose: N+1s are the dominant performance regression risk on a stack that ships SSR with Inertia (every page render is SSR-blocking — a 30-query page kills first-paint). Strict mode + query-budget tests make the regression impossible to merge once landed. Pitfall 2 is non-trivial: every previously-tolerated lazy load will surface as test RED; this plan budget covers fixes for all of them.

Output: 1 provider extension + N controller edits (fix-as-found) + 3 Pest tests GREEN + cache-strategy doc captured in SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-polish/09-RESEARCH.md
@CLAUDE.md

<interfaces>
AppServiceProvider.php existing structure (Phase 1+):
- boot() method with existing config (Vite, etc.).
- Append `Model::shouldBeStrict(! $this->app->isProduction());` per RESEARCH Pattern 6 + State of the Art table.

Strict mode behaviour: lazy loading + accessing-non-existent-attribute + missing-attribute all throw LazyLoadingViolationException / MissingAttributeException.

Pitfall 2 mitigation strategy:
1. Run `make pest` BEFORE adding strict mode to baseline GREEN count.
2. Add Model::shouldBeStrict(true) gated to env('APP_ENV') !== 'production'.
3. Re-run `make pest` — RED count surfaces every lazy load. RESEARCH estimates 20-40 fixes.
4. For each RED test, find the controller/page resolver, add `->with([relations])` to the Eloquent query.
5. Reverify by re-running the same filter; must turn GREEN.
6. Once full suite is GREEN, commit.

Pitfall 12: strict mode + RefreshDatabase + factories. Tests that assert against unloaded relations should `$model->load('relation')` first. Don't disable strict mode for tests — the test bug is real, fix it.

Query budget pattern (RESEARCH "Performance & N+1"):
```php
it('renders /leaderboards under 4 queries', function () {
    \DB::enableQueryLog();
    $response = $this->get('/leaderboards');
    $response->assertStatus(200);
    expect(count(\DB::getQueryLog()))->toBeLessThanOrEqual(4);
});
```

Cache layer (plan 09-05) absorbs leaderboards reads on cache hit; first-run query count is the budget. Tests run on a cold cache (RefreshDatabase + cache flush in beforeEach).

Per RESEARCH "Target query budgets per public page":
| Page | Max queries |
| / (home) | 12 |
| /clans (directory) | 8 |
| /clans/{slug} | 15 |
| /players/{slug} | 10 |
| /matches | 6 |
| /matches/{id} | 12 |
| /tournaments/{slug} | 15 |
| /leaderboards | 4 |
| /articles (blog index) | 6 |
| /articles/{slug} | 8 |

This plan codifies budgets for /leaderboards (lowest, tightest) and /clans (medium, representative of complex JOIN). Other budgets can be added in later polish if regressions emerge — not required for plan 09 close.

debugbar (Phase 1) — APP_DEBUG=true triggers Queries panel; useful for manual sweep but not used in automated tests.

Cache layer interaction: /leaderboards FIRST request budget = 4 (uncached); subsequent within fresh TTL = 0 cache + 0 query. The 4-query budget assumes cold cache + Cache::tags flush in beforeEach.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable strict mode + fix all surfaced N+1s + AppServiceProviderStrictModeTest GREEN</name>
  <files>
    apps/web/app/Providers/AppServiceProvider.php,
    apps/web/app/Http/Controllers/ClansController.php,
    apps/web/app/Http/Controllers/PlayersController.php,
    apps/web/app/Http/Controllers/MatchesController.php,
    apps/web/app/Http/Controllers/TournamentsController.php,
    apps/web/app/Http/Controllers/BlogController.php,
    apps/web/app/Http/Controllers/HomeController.php,
    apps/web/app/Http/Controllers/LeaderboardsController.php,
    apps/web/tests/Unit/AppServiceProviderStrictModeTest.php
  </files>
  <action>
Step 1: Read current `apps/web/app/Providers/AppServiceProvider.php`. Identify the existing boot() method.

Step 2: Append to boot() (after any existing setup, before any return):
```php
use Illuminate\Database\Eloquent\Model;
// ...
Model::shouldBeStrict(! $this->app->isProduction());
```
Place the use statement at the top of the file.

Step 3: Run the FULL Pest suite via `make pest` to surface every LazyLoadingViolationException / MissingAttributeException. Capture the RED test list.

Step 4: For each RED test, identify the controller/page resolver responsible and add `->with([relations])` to the offending query. Expected hotspots per RESEARCH (Phase 7-8 article-feed code paths, Phase 6 tournament bracket, Phase 8 match-results pages):

Probable fixes (verify with actual RED output):
- `ClansController::index` — add `->with(['tags', 'memberships' => fn ($q) => $q->where('active', true), 'memberships.player'])` to clans listing query.
- `ClansController::show` — eager-load tags, memberships, recent matches.
- `PlayersController::show` — eager-load activeClanMembership.clan, privacy, recent stats.
- `MatchesController::index` — eager-load hostClan, awayClan, signups (with limit), gameMatchType.game.
- `MatchesController::show` — eager-load signups.user.player, result, slots.gameRole, mvps, hostClan, awayClan.
- `TournamentsController::show` — eager-load participants.clan, stages.brackets.match, standings.
- `BlogController::index` — eager-load articles.category, articles.author, articles.media.
- `BlogController::show` — eager-load category, author, media, related articles.
- `HomeController::index` — eager-load featured articles, featured matches, top clans preview.
- `LeaderboardsController::index` — verify plan 09-05's ->with(['player.user.privacy','player.activeClanMembership.clan']) covers all access patterns.

For each fix:
1. Add the explicit `->with([relations])` call.
2. If a Phase 1-8 test asserts against a relation that strict mode caught as unloaded in test setup, edit the test setup to `->load('relation')` after factory creation (Pitfall 12).
3. Run filter for that specific test to confirm GREEN.

Step 5: Author `apps/web/tests/Unit/AppServiceProviderStrictModeTest.php` (turn Wave 0 stub GREEN):
- Test 1: it('enables Model::shouldBeStrict in testing environment') — assert `Model::preventsLazyLoading()` returns true (or equivalent introspection method).
- Test 2: it('does NOT enable strict mode in production-simulated boot') — temporarily set env to production, re-boot provider, assert preventsLazyLoading() === false. (May require some test gymnastics — alternative: assert the conditional logic via reflection on AppServiceProvider source.)
- Test 3: it('lazy load on User->bans triggers LazyLoadingViolationException in test env') — create user without loading bans, access $user->bans, assert exception thrown. Confirms strict mode is active.

Commit boundary: Strict mode flip + ALL N+1 fixes + AppServiceProviderStrictModeTest must land in a single coherent commit so the test suite never goes RED at HEAD.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | tail -10 && docker compose exec web ./vendor/bin/pest --filter="AppServiceProviderStrictModeTest" --no-coverage</automated>
  </verify>
  <done>Strict mode active in non-production; all RED tests fixed; full Pest suite GREEN (1134 + Phase 9 turn-GREEN count); AppServiceProviderStrictModeTest GREEN; PHPStan + Pint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Query budget tests for /leaderboards (<=4) + /clans (<=8) + cache-strategy doc</name>
  <files>
    apps/web/tests/Feature/Performance/LeaderboardsQueryBudgetTest.php,
    apps/web/tests/Feature/Performance/ClansQueryBudgetTest.php
  </files>
  <action>
Turn `LeaderboardsQueryBudgetTest.php` GREEN (SC-4):
- beforeEach: flush leaderboards cache via `Cache::tags(['leaderboards'])->flush()` (RefreshDatabase already gives clean schema).
- Test 1: it('renders /leaderboards under 4 queries on cold cache for default window') — seed minimal data (3 players + stats), `DB::enableQueryLog()`, GET /leaderboards, assert status 200 AND `count(DB::getQueryLog()) <= 4`.
- Test 2: it('renders /leaderboards under 1 query on warm cache') — call once (warms cache), enable log, call again, assert <= 1 (or 0 — verify locally first, lock the actual observed minimum).
- Test 3: it('renders /leaderboards under 4 queries with window=30d filter').
- Test 4: it('renders /leaderboards under 4 queries with game filter').
- Test 5: it('renders /leaderboards under 4 queries with empty database (no players)') — empty-state path.

If actual measured query count exceeds budget, EITHER:
- Fix the N+1 (preferred — add eager loads).
- OR raise the budget in the test with a code-comment justification (documented deviation).

Turn `ClansQueryBudgetTest.php` GREEN:
- beforeEach: flush clan-directory cache (if plan 09-05 doesn't add one yet, /clans is uncached — still measure cold query).
- Test 1: it('renders /clans under 8 queries on cold load') — seed 10 clans with tags and memberships, GET /clans, assert <= 8 queries.
- Test 2: it('renders /clans under 8 queries with tag filter applied').
- Test 3: it('renders /clans under 8 queries when paginated to page 2').
- Test 4: it('renders /clans under 8 queries with empty database').

Author `.planning/phases/09-polish/CACHE-STRATEGY.md` (project-level doc, NOT in apps/web — lives alongside the phase summaries) at `.planning/phases/09-polish/CACHE-STRATEGY.md`. Content per RESEARCH "Cache Strategy" section verbatim:
- Tagged cache key registry table (lb:players:*, lb:clans:*, clan:directory:*, cms:articles:*, home:hero:articles, player:profile:*).
- TTL guidelines (fresh, stale).
- Invalidation observer map.
- Cache key conventions.

This file is informational and read by future plans / docs consumers; not committed alongside apps/web code per project convention.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="LeaderboardsQueryBudgetTest|ClansQueryBudgetTest" --no-coverage</automated>
  </verify>
  <done>2 query budget Pest tests GREEN; CACHE-STRATEGY.md authored; full test suite stays GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ORM->controllers | Strict-mode flag enforces eager-load discipline |
| controller->Eloquent query | Query budget caps query count |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08-01 | D (DoS) | N+1 query explosion under load | mitigate | Model::shouldBeStrict in non-prod makes regressions test-RED; query-budget Pest tests cap representative pages |
| T-09-08-02 | D (DoS) | Pitfall 2 — strict mode breaks Phase 1-8 tests | mitigate | This plan's Task 1 includes the fix-as-found sweep; commit boundary keeps HEAD GREEN |
| T-09-08-03 | T (Tampering) | Production strict mode causes runtime exception in user-facing page | accept | Flag is `! isProduction()` — production stays relaxed; only dev/test surfaces RED |
| T-09-08-04 | D (DoS) | Cache::tags flush invalidates leaderboards mid-request causing fallback to cold compute | accept | Cold compute is <= 4 queries (verified by query budget test); SWR semantics absorb the cold-recompute spike |
</threat_model>

<verification>
- AppServiceProvider modified.
- All Phase 1-8 + Phase 9 (so far) tests GREEN after strict mode flip.
- 3 Pest tests GREEN: AppServiceProviderStrictModeTest, LeaderboardsQueryBudgetTest, ClansQueryBudgetTest.
- CACHE-STRATEGY.md exists.
- PHPStan L8 + Pint clean.
</verification>

<success_criteria>
- SC-4 N+1 elimination: every lazy load surfaces as test-RED in dev/test.
- SC-4 cache-key strategy documented.
- /leaderboards <= 4 queries, /clans <= 8 queries (budget locked).
- No regression to Phase 1-8 test suite.
- Pitfall 2 (strict mode breaks existing tests) handled inline.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-08-SUMMARY.md` capturing: list of N+1 fixes applied (controller + relations), final query budget table (verified counts), Pitfall 2+12 evidence.
</output>
