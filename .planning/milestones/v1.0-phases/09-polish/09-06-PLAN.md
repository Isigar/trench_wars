---
phase: 09-polish
plan: 06
type: execute
wave: 6
depends_on: [03, 05]
files_modified:
  - apps/web/app/Http/Controllers/NotificationsController.php
  - apps/web/app/Http/Controllers/Account/NotificationPreferencesController.php
  - apps/web/app/Http/Controllers/LeaderboardsController.php
  - apps/web/app/Http/Middleware/HandleInertiaRequests.php
  - apps/web/routes/web.php
  - apps/web/resources/js/Pages/Notifications/Index.vue
  - apps/web/resources/js/Pages/Account/NotificationPreferences.vue
  - apps/web/resources/js/Pages/Leaderboards/Index.vue
  - apps/web/resources/js/Components/NotificationsBell.vue
  - apps/web/resources/js/Components/LeaderboardTable.vue
  - apps/web/resources/js/Layouts/PublicLayout.vue
  - apps/web/tests/Feature/Notifications/NotificationsBellTest.php
  - apps/web/tests/Feature/Leaderboards/LeaderboardPrivacyTest.php
autonomous: true
requirements:
  - REQ-polish-v1

must_haves:
  truths:
    - "Authenticated user can GET /notifications and see their unread notifications list."
    - "Authenticated user can POST /notifications/{id}/read to mark a single notification read."
    - "Authenticated user can POST /notifications/read-all to mark all read."
    - "Authenticated user can GET /account/notification-preferences and toggle 5x2 (event_type x channel) matrix; POST saves the diff."
    - "Public visitor can GET /leaderboards and see top players + top clans with window toggle (7d/30d/all) and game filter."
    - "Inertia shared.user includes unread_notifications_count so the bell badge renders without an extra request (no real-time polling)."
    - "Players with show_stats=false render anonymously on /leaderboards (D-018 + LeaderboardPrivacyTest GREEN)."
    - "NotificationsBellTest GREEN; LeaderboardPrivacyTest GREEN."
  artifacts:
    - path: "apps/web/app/Http/Controllers/NotificationsController.php"
      provides: "Inertia + JSON endpoints for notification list + mark-read"
      exports: ["index", "markAsRead", "markAllAsRead"]
    - path: "apps/web/app/Http/Controllers/Account/NotificationPreferencesController.php"
      provides: "Inertia GET + POST for 5x2 preference matrix"
      exports: ["edit", "update"]
    - path: "apps/web/app/Http/Controllers/LeaderboardsController.php"
      provides: "Public Inertia controller for /leaderboards"
      exports: ["index"]
    - path: "apps/web/app/Http/Middleware/HandleInertiaRequests.php"
      provides: "Extended shared.user with unread_notifications_count"
      contains: "unread_notifications_count"
    - path: "apps/web/resources/js/Pages/Leaderboards/Index.vue"
      provides: "Public leaderboards page (top players + top clans + window/game filters)"
      contains: "LeaderboardTable"
    - path: "apps/web/resources/js/Components/NotificationsBell.vue"
      provides: "Bell button + drawer with mark-read CTAs"
      contains: "unread_notifications_count"
  key_links:
    - from: "PublicLayout.vue (existing)"
      to: "NotificationsBell.vue"
      via: "auth-conditional render of bell"
      pattern: "NotificationsBell"
    - from: "LeaderboardsController::index"
      to: "LeaderboardService::topPlayers + topClans"
      via: "service call with window/gameId/limit from request"
      pattern: "LeaderboardService"
    - from: "HandleInertiaRequests::share"
      to: "User unreadNotifications count"
      via: "shared.user.unread_notifications_count"
      pattern: "unreadNotifications"
---

<objective>
Ship the public-facing UI surface for SC-1 (notification bell + preferences) and SC-2 (public leaderboards) with D-018 PlayerPrivacyGate enforcement at the page level.

Purpose: Bell badge re-renders on every Inertia navigation via shared props (no WebSocket, no real-time polling). Leaderboards renders cached aggregates with privacy gate at row serialization.

Output: 3 controllers + 3 Vue pages + 2 components + Inertia shared-prop extension + 5 routes + 2 GREEN tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-polish/09-RESEARCH.md
@.planning/phases/09-polish/09-03-PLAN.md
@.planning/phases/09-polish/09-05-PLAN.md
@CLAUDE.md

<interfaces>
HandleInertiaRequests.php (Phase 1) shares auth.user + translations + flash. This plan appends a `unread_notifications_count` closure.

useT() composable (Phase 1 plan 01-08) — flat dot-keyed lookup against shared translations.

PublicLayout.vue (Phase 1 plan 01-07) — has nav slot for auth-only widgets.

User model (extended plan 09-03) — has Notifiable trait, unreadNotifications() relation, markAsRead() on individual notification.

LeaderboardService (plan 09-05) — topPlayers(window, gameId, limit), topClans(window, gameId, limit).

PlayerPrivacyGate (Phase 1) — for(?User $viewer, Player $player): array.

LeaderboardEntryData / LeaderboardClanEntryData (plan 09-05 DTOs) — have is_anonymous flag set per D-018.

Tailwind v4 semantic tokens (Phase 1 plan 01-07) — use var(--color-bg) / var(--color-fg) / var(--color-accent). No raw hex.

Reka UI primitives (Phase 1) — Modal/Drawer/Switch components available; bell drawer uses Reka Drawer.

Throttle middleware: bell mark-read endpoints attach throttle:notifications-read (Phase 9 plan 09-11 defines).

routes/web.php existing structure (Phase 7) — public routes grouped, auth routes under auth middleware.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NotificationsController + PreferencesController + shared-prop extension + Vue pages + NotificationsBellTest GREEN</name>
  <files>
    apps/web/app/Http/Controllers/NotificationsController.php,
    apps/web/app/Http/Controllers/Account/NotificationPreferencesController.php,
    apps/web/app/Http/Middleware/HandleInertiaRequests.php,
    apps/web/routes/web.php,
    apps/web/resources/js/Pages/Notifications/Index.vue,
    apps/web/resources/js/Pages/Account/NotificationPreferences.vue,
    apps/web/resources/js/Components/NotificationsBell.vue,
    apps/web/resources/js/Layouts/PublicLayout.vue,
    apps/web/tests/Feature/Notifications/NotificationsBellTest.php
  </files>
  <action>
Author `app/Http/Controllers/NotificationsController.php`:
- `index(Request $request): Response` — auth required; load `auth()->user()->notifications()->latest()->paginate(20)`; return `Inertia::render('Notifications/Index', ['notifications' => $paginator])`.
- `markAsRead(string $id): RedirectResponse` — `auth()->user()->notifications()->find($id)?->markAsRead(); return back();`. Return 404 if not found (use abort_if).
- `markAllAsRead(): RedirectResponse` — `auth()->user()->unreadNotifications->markAsRead(); return back();`.

Author `app/Http/Controllers/Account/NotificationPreferencesController.php`:
- `edit(): Response` — load `$prefs = auth()->user()->notificationPreferences()->get()`. Build 5x2 matrix as `[event_type => [channel => enabled]]` using User::enabledNotificationChannels default logic. Return `Inertia::render('Account/NotificationPreferences', ['preferences' => $matrix, 'event_types' => [...5 keys], 'channels' => ['database','discord']])`.
- `update(Request $request): RedirectResponse` — validate input shape (array of {event_type, channel, enabled}); for each tuple, `UserNotificationPreference::updateOrCreate(['user_id'=>auth()->id(),'event_type'=>$et,'channel'=>$ch], ['enabled'=>$en])`. Return redirect back with flash.

Extend `app/Http/Middleware/HandleInertiaRequests.php` `share()` method:
- First read existing share() shape via Read to find the right insertion point.
- Append `'unread_notifications_count' => fn () => auth()->user()?->unreadNotifications()->count() ?? 0`. Closure (lazy eval) — Inertia evaluates per-request.

Edit `routes/web.php` appending under auth middleware group:
```php
Route::middleware(['auth'])->group(function () {
    Route::get('/notifications', [NotificationsController::class, 'index'])->name('notifications.index');
    Route::post('/notifications/{id}/read', [NotificationsController::class, 'markAsRead'])
        ->middleware('throttle:notifications-read')->name('notifications.markRead');
    Route::post('/notifications/read-all', [NotificationsController::class, 'markAllAsRead'])
        ->middleware('throttle:notifications-read')->name('notifications.markAllRead');
    Route::get('/account/notification-preferences', [NotificationPreferencesController::class, 'edit'])->name('account.notification-preferences.edit');
    Route::post('/account/notification-preferences', [NotificationPreferencesController::class, 'update'])->name('account.notification-preferences.update');
});
```

Author `resources/js/Components/NotificationsBell.vue`:
- Props: `count: number`.
- Renders bell icon (Lucide BellRing) with `aria-label` from `t('a11y.notifications.bell_label', { count: props.count })`.
- Badge with count when count > 0; uses Tailwind v4 semantic tokens (var(--color-accent) for badge bg).
- Click opens Reka Drawer; drawer fetches /notifications via Inertia router.visit when opened.
- Drawer lists notifications with mark-read CTAs (`router.post(route('notifications.markRead', n.id))`).
- Empty state when count === 0.
- All UI strings via useT().

Author `resources/js/Pages/Notifications/Index.vue`:
- Props: `notifications` (Laravel paginator shape).
- Per-row renderer keyed off `notification.type` discriminator ('match.starting_soon' etc.). For unknown types, fallback to `useT(notification.data.i18n_key)`.
- mark-read + mark-all-read buttons via router.post.

Author `resources/js/Pages/Account/NotificationPreferences.vue`:
- Props: preferences matrix + event_types + channels.
- 5x2 grid of Reka Switch components (one per event_type x channel).
- onSubmit: build payload array, router.post route('account.notification-preferences.update').

Edit `resources/js/Layouts/PublicLayout.vue` — in nav slot, mount `<NotificationsBell :count="$page.props.unread_notifications_count" v-if="$page.props.auth?.user" />`.

Turn `NotificationsBellTest.php` GREEN:
- Test 1: it renders unread count in shared props (3 unread, GET /, assert props contain unread_notifications_count: 3).
- Test 2: GET /notifications returns Inertia page with user notifications.
- Test 3: POST /notifications/{id}/read marks that notification read.
- Test 4: POST /notifications/read-all marks every unread notification read.
- Test 5: returns 404 when marking another user's notification.
- Test 6: unauthenticated requests redirect to login on /notifications and /account/notification-preferences.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="NotificationsBellTest" --no-coverage</automated>
  </verify>
  <done>3 controllers + 3 Vue pages + 1 component + 1 layout edit; routes registered; Pest test GREEN; PHPStan + Pint clean.</done>
</task>

<task type="auto">
  <name>Task 2: LeaderboardsController + Vue page + LeaderboardTable component + LeaderboardPrivacyTest GREEN</name>
  <files>
    apps/web/app/Http/Controllers/LeaderboardsController.php,
    apps/web/routes/web.php,
    apps/web/resources/js/Pages/Leaderboards/Index.vue,
    apps/web/resources/js/Components/LeaderboardTable.vue,
    apps/web/tests/Feature/Leaderboards/LeaderboardPrivacyTest.php
  </files>
  <action>
Author `app/Http/Controllers/LeaderboardsController.php`:
- `index(Request $request, LeaderboardService $service): Response`:
  - Validate query: `window` in ['7d','30d','all'] (default '7d'), `game` integer nullable, `limit` integer 1..100 (default 25).
  - Call `$topPlayers = $service->topPlayers($window, $gameId, $limit);` and `$topClans = $service->topClans($window, $gameId, $limit);`.
  - Map each row through `LeaderboardEntryData::fromQueryResult($row, auth()->user())` and `LeaderboardClanEntryData::fromQueryResult($row)`.
  - Return `Inertia::render('Leaderboards/Index', ['players' => $playerEntries, 'clans' => $clanEntries, 'filters' => ['window'=>$window,'game'=>$gameId,'limit'=>$limit], 'games' => Game::orderBy('name')->get(['id','name','slug'])])`.

Edit `routes/web.php` appending in public group (REQ-tenancy-multi-clan: public surface, no auth required):
```php
Route::get('/leaderboards', [LeaderboardsController::class, 'index'])
    ->middleware('throttle:public-api')
    ->name('leaderboards.index');
```

Author `resources/js/Pages/Leaderboards/Index.vue`:
- Props: `players: LeaderboardEntryData[]`, `clans: LeaderboardClanEntryData[]`, `filters`, `games`.
- Reka Tabs: Players | Clans.
- Window toggle (7d / 30d / all-time) using semantic-token-styled buttons; onChange router.reload with new query params.
- Game filter dropdown (Reka Select).
- LeaderboardTable component renders the active tab's data.

Author `resources/js/Components/LeaderboardTable.vue`:
- Props: `rows: (LeaderboardEntryData|LeaderboardClanEntryData)[]`, `mode: 'players'|'clans'`.
- For mode='players': columns Player | Clan | Kills | Deaths | KDR | Matches.
- For mode='clans': columns Clan | Kills | Wins | Matches.
- D-018 enforcement: when row.is_anonymous === true, render player_name as `t('leaderboards.anonymous_player')` and do NOT wrap in Inertia <Link>; otherwise <Link :href="route('players.show', row.player_id)">.
- All headers via useT().
- Empty state: t('leaderboards.empty_state').
- KDR formatting: null = "-", Infinity = "infinity glyph", else .toFixed(2).

Turn `LeaderboardPrivacyTest.php` GREEN:
- Test 1: shows player name when show_stats=true.
- Test 2: renders anonymously when show_stats=false (D-018 critical).
- Test 3: respects viewer tier for show_to='clan' players (same clan visible, other clan anonymous).
- Test 4: public visitor sees only show_to=public players with show_stats=true.
- Test 5: returns 200 for unauthenticated visitor (route is public).
- Test 6: applies window filter from query string.
- Test 7: applies game filter from query string.
- Test 8: route uses throttle:public-api middleware (assert via route::getRoutes()).
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="LeaderboardPrivacyTest" --no-coverage</automated>
  </verify>
  <done>1 controller + 1 Vue page + 1 component + 1 route added; 1 Pest test GREEN; PHPStan + Pint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous visitor->LeaderboardsController | Public surface; query params crossed |
| auth user->NotificationsController | Auth required; user-scoped data only |
| auth user->PreferencesController | Auth required; user updates their own prefs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | I (Information Disclosure) | Bypass PlayerPrivacyGate via /leaderboards | mitigate | LeaderboardEntryData::fromQueryResult runs PlayerPrivacyGate; LeaderboardTable.vue renders is_anonymous rows without name or link; LeaderboardPrivacyTest GREEN asserts |
| T-09-06-02 | E (Elevation of Privilege) | User A marking User B's notification read | mitigate | Controller scopes via `auth()->user()->notifications()->find($id)` — find returns null for other users; abort_if 404 |
| T-09-06-03 | T (Tampering) | User updating another user's preferences | mitigate | updateOrCreate keyed by auth()->id(); only the auth user's rows touched |
| T-09-06-04 | D (DoS) | Mass mark-read calls | mitigate | throttle:notifications-read (120/min per user, plan 09-11) on POST endpoints |
| T-09-06-05 | D (DoS) | Anonymous bot scraping /leaderboards | mitigate | throttle:public-api (30/min by IP, plan 09-11) + Cache::tags layer absorbs repeated reads |
| T-09-06-06 | I (Information Disclosure) | Unread_notifications_count leaks user existence via timing | accept | Count is per-user via auth; no cross-user enumeration |
</threat_model>

<verification>
- 3 controllers + 3 Vue pages + 2 components + 1 layout edit + 1 middleware extension.
- 5 routes registered.
- 2 Pest tests GREEN.
- Existing 1134 Pest tests stay GREEN.
- PHPStan L8 + Pint clean.
</verification>

<success_criteria>
- SC-1 web bell renders unread count without extra request (shared prop).
- SC-1 mark-read endpoints work + user-scoped.
- SC-1 preferences page allows 5x2 matrix edit.
- SC-2 leaderboards page renders top players + top clans with window/game filters.
- D-018 PlayerPrivacyGate respected — show_stats=false players render anonymously.
- D-013 every UI string via useT() / __().
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-06-SUMMARY.md` capturing: route table for /notifications + /account + /leaderboards, Inertia shared-prop diff, D-018 enforcement points, bell UX flow diagram.
</output>
