---
phase: 09-polish
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - apps/web/app/Models/Notification.php
  - apps/web/app/Models/UserNotificationPreference.php
  - apps/web/app/Models/Ban.php
  - apps/web/app/Models/MatchDispute.php
  - apps/web/app/Models/AbuseReport.php
  - apps/web/app/Models/User.php
  - apps/web/app/Notifications/MatchStartingSoon.php
  - apps/web/app/Notifications/MatchCancelled.php
  - apps/web/app/Notifications/MatchResultPublished.php
  - apps/web/app/Notifications/ClanApplicationDecided.php
  - apps/web/app/Notifications/ClanInviteReceived.php
  - apps/web/app/Notifications/Channels/DiscordChannel.php
  - apps/web/database/factories/AbuseReportFactory.php
  - apps/web/database/factories/BanFactory.php
  - apps/web/database/factories/MatchDisputeFactory.php
  - apps/web/database/factories/UserNotificationPreferenceFactory.php
  - apps/web/tests/Unit/Notifications/MatchStartingSoonNotificationTest.php
  - apps/web/tests/Unit/UserNotificationPreferencesTest.php
  - apps/web/tests/Feature/Notifications/DiscordChannelOutboxTest.php
autonomous: true
requirements:
  - REQ-polish-v1

must_haves:
  truths:
    - "5 Eloquent models (Ban, MatchDispute, AbuseReport, UserNotificationPreference, Notification override) exist with HasFactory + correct fillable + casts."
    - "5 Notification classes (MatchStartingSoon, MatchCancelled, MatchResultPublished, ClanApplicationDecided, ClanInviteReceived) exist, each implements ShouldQueue, defines via()/toArray()/toDiscord()/databaseType()."
    - "DiscordChannel class writes exactly one discord_outbound_messages row per send() invocation and makes ZERO outbound HTTP calls (Pitfall 3 LOCKED)."
    - "Each Notification class returns a UNIQUE databaseType() discriminator (Pitfall 4 LOCKED) — no two classes share a value."
    - "User model gains enabledNotificationChannels(string $eventType): array method honouring user_notification_preferences with default-policy fallback (Pattern 7)."
    - "MatchStartingSoonNotificationTest GREEN (channels chosen via preferences; toArray + toDiscord shape verified)."
    - "UserNotificationPreferencesTest GREEN (default-on web, default-off discord without discord_id, opt-out honoured)."
    - "DiscordChannelOutboxTest GREEN (one outbox row inserted; Http::fake() asserts NO outbound request)."
  artifacts:
    - path: "apps/web/app/Notifications/Channels/DiscordChannel.php"
      provides: "Channel that inserts discord_outbound_messages rows (D-004 compliant) — never calls discord.com directly"
      exports: ["send"]
    - path: "apps/web/app/Notifications/MatchStartingSoon.php"
      provides: "T-60min/T-15min match-starting notification class"
      contains: "App\\Models\\GameMatch"
    - path: "apps/web/app/Notifications/MatchCancelled.php"
      provides: "Match cancelled notification class"
      contains: "match.cancelled"
    - path: "apps/web/app/Notifications/MatchResultPublished.php"
      provides: "Match result published notification class (discord DEFAULT-OFF per Open Question 3 LOCKED)"
      contains: "match.result_published"
    - path: "apps/web/app/Notifications/ClanApplicationDecided.php"
      provides: "Clan application approved/rejected notification class"
      contains: "clan.application_decided"
    - path: "apps/web/app/Notifications/ClanInviteReceived.php"
      provides: "Clan invite received notification class"
      contains: "clan.invite_received"
    - path: "apps/web/app/Models/User.php"
      provides: "Extended with enabledNotificationChannels(string): array + notificationPreferences() HasMany"
      contains: "enabledNotificationChannels"
  key_links:
    - from: "DiscordChannel::send"
      to: "discord_outbound_messages table"
      via: "DB::table('discord_outbound_messages')->insert"
      pattern: "discord_outbound_messages.*insert"
    - from: "MatchStartingSoon::via"
      to: "User::enabledNotificationChannels('match_starting_soon')"
      via: "via() delegates to user prefs"
      pattern: "enabledNotificationChannels"
    - from: "Notification::databaseType"
      to: "Vue frontend renderer (plan 09-06)"
      via: "stable discriminator string"
      pattern: "databaseType"
---

<objective>
Author 5 Eloquent models (Notification override + 4 new) + 5 Notification classes + custom DiscordChannel that writes to the Phase 5 outbox (NOT direct webhook — D-004 LOCKED), real factory implementations, and turn 3 Wave-0 stubs GREEN.

Purpose: Lock the notification class surface and the outbox-write contract before plan 09-04 builds the dispatcher cron or plan 09-06 builds the bell UI. The DiscordChannel design is the single highest-risk architectural choice in Phase 9 — Pitfall 3 (D-004 violation by writing direct webhooks) is the dominant failure mode.

Output: 5 models + 5 Notification classes + DiscordChannel + 4 real factories; 3 Wave-0 tests turned GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-polish/09-RESEARCH.md
@.planning/phases/09-polish/09-02-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Existing Phase 5 outbox contract — the canonical write target for DiscordChannel -->

discord_outbound_messages table columns (from Phase 5 migration 05-02 + extensions):
- id (bigint PK)
- message_type (varchar, CHECK-constrained — after plan 09-02 includes 'user_dm')
- recipient_clan_id (uuid nullable) | recipient_user_discord_id (text nullable) — depending on type
- payload (jsonb) — embed + buttons + CTAs
- status (varchar: pending|sent|failed)
- attempts (smallint)
- last_error (text nullable)
- created_at, updated_at

For message_type='user_dm':
- recipient_user_discord_id: set to the User's discord_id
- recipient_clan_id: NULL
- payload: { embed_title, embed_description, cta_url, color_token }

DiscordOutboundPayloadBuilder (apps/web/app/Services/DiscordOutboundPayloadBuilder.php — Phase 5+):
- existing methods: buildMatchAnnounce, buildMatchResultAnnounce, buildArticleAnnounce, buildTournamentAnnounce
- Phase 9 does NOT add a new builder method; DiscordChannel inlines the payload shape per-notification via toDiscord() return.

GameMatch FQN (D-04-03-A LOCKED):
- `use App\Models\GameMatch;` — NO `use ... as MatchModel` alias-on-import.

User model existing structure (Phase 1):
- UUID PK, citext email, has Notifiable trait already? — VERIFY in implementation. If not present, add `use Notifiable;` from Illuminate\Notifications.

PlayerPrivacyGate (Phase 1):
- `apps/web/app/Services/PlayerPrivacyGate.php` — exposes `for(User $viewer, Player $player): array` returning per-section gate flags. Not directly used in Phase 9 plan 03 but referenced for stat-bearing notifications.

Pest stub idiom + factory idiom — see plan 09-01 interfaces.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 5 Eloquent models + extend User + real factories</name>
  <files>
    apps/web/app/Models/Notification.php,
    apps/web/app/Models/UserNotificationPreference.php,
    apps/web/app/Models/Ban.php,
    apps/web/app/Models/MatchDispute.php,
    apps/web/app/Models/AbuseReport.php,
    apps/web/app/Models/User.php,
    apps/web/database/factories/AbuseReportFactory.php,
    apps/web/database/factories/BanFactory.php,
    apps/web/database/factories/MatchDisputeFactory.php,
    apps/web/database/factories/UserNotificationPreferenceFactory.php
  </files>
  <action>
Author `app/Models/Notification.php` — extends `Illuminate\Notifications\DatabaseNotification` (NOT the abstract base) so we inherit polymorphic morph defaults; override `$table = 'notifications'` (Laravel's default already, explicit for clarity); declare `$keyType = 'string'` and `$incrementing = false` because of UUID PK; cast `data => 'array'`, `read_at => 'datetime'`. Use HasFactory ONLY if we ship a NotificationFactory (skip for now — notifications are created via `->notify()`).

Author `app/Models/UserNotificationPreference.php` — `final class` with HasFactory generic typed `<UserNotificationPreferenceFactory>`. `$fillable = ['user_id', 'event_type', 'channel', 'enabled']`. `casts: ['enabled' => 'bool']`. `belongsTo` User.

Author `app/Models/Ban.php` — `final class` with HasFactory. `$fillable = ['user_id', 'ban_type', 'reason', 'expires_at', 'issued_by_user_id', 'lifted_at', 'lifted_by_user_id', 'lift_reason']`. `casts: ['expires_at' => 'datetime', 'lifted_at' => 'datetime']`. BelongsTo user (FK user_id), BelongsTo issuedBy (FK issued_by_user_id, User), BelongsTo liftedBy (nullable). Scope `active()` returning where (lifted_at IS NULL) AND (expires_at IS NULL OR expires_at > NOW()). Do NOT use LogsActivity trait per Pitfall in Anti-Patterns ("DON'T add LogsActivity to bans/disputes/abuse_reports — services call activity()->log() explicitly").

Author `app/Models/MatchDispute.php` — `final class` with HasFactory. `$fillable = ['match_id', 'raised_by_user_id', 'body', 'status', 'resolution', 'resolution_notes', 'resolved_by_user_id', 'resolved_at']`. `casts: ['resolved_at' => 'datetime']`. BelongsTo match (D-04-03-A: `BelongsTo<GameMatch, $this>` with explicit `match_id` FK arg). BelongsTo raisedBy, resolvedBy (User). No LogsActivity.

Author `app/Models/AbuseReport.php` — `final class` with HasFactory. `$fillable = ['reporter_user_id', 'target_type', 'target_id', 'reason_code', 'body', 'status', 'reviewed_by_user_id', 'reviewed_at', 'review_notes']`. `casts: ['reviewed_at' => 'datetime']`. BelongsTo reporter (FK reporter_user_id, User). morphTo target via target_type/target_id (NOTE: target_id is varchar — Laravel morphTo handles string IDs fine). No LogsActivity.

Extend `app/Models/User.php`:
- Add `use Notifiable;` from Illuminate\Notifications if not present (likely IS present — verify via grep before adding).
- Add `notificationPreferences(): HasMany` returning `$this->hasMany(UserNotificationPreference::class)`.
- Add `bans(): HasMany` returning `$this->hasMany(Ban::class, 'user_id')`.
- Add `activeBan(): ?Ban` helper returning `$this->bans()->active()->first()` (for plan 09-11 ban-check middleware).
- Add `enabledNotificationChannels(string $eventType): array` per Pattern 7:
  - Load `$prefs = $this->notificationPreferences->where('event_type', $eventType)->keyBy('channel');`
  - Default-ON web bell unless explicitly disabled: if `($prefs['database']?->enabled ?? true) === true`, append `'database'`.
  - Special-case `match_result_published` event_type: Discord DM defaults to FALSE per Open Question 3 LOCKED. Other event types default to TRUE for Discord IF user has discord_id.
  - Return the resulting array (possibly empty).

Author 4 real factories replacing the Wave 0 stubs (apps/web/database/factories/{Ban,MatchDispute,AbuseReport,UserNotificationPreference}Factory.php):
- BanFactory: `definition()` returns user_id => User::factory(), ban_type => 'temporary', reason => fake()->sentence(8), expires_at => now()->addDays(7), issued_by_user_id => User::factory(). State `permanent()` sets ban_type='permanent', expires_at=null. State `lifted()` sets lifted_at, lifted_by_user_id, lift_reason.
- MatchDisputeFactory: definition returns match_id => GameMatch::factory() (D-04-03-A), raised_by_user_id => User::factory(), body => fake()->paragraph(2), status => 'open'. State `resolved()` sets status='resolved', resolution='no_action', resolved_by_user_id, resolved_at.
- AbuseReportFactory: definition returns reporter_user_id => User::factory(), target_type => 'App\\Models\\Player', target_id => (string) Player::factory()->create()->id, reason_code => fake()->randomElement(['harassment','spam','cheating','inappropriate_content','other']), body => fake()->sentence, status => 'pending'.
- UserNotificationPreferenceFactory: definition returns user_id => User::factory(), event_type => 'match_starting_soon', channel => 'database', enabled => true.

All factories: `final class` + `/** @extends Factory<Model> */` PHPDoc to satisfy PHPStan L8 generics check.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="UserNotificationPreferencesTest" --no-coverage 2>&1 | tail -20 && docker compose exec web ./vendor/bin/phpstan analyse app/Models app/Notifications database/factories --no-progress 2>&1 | tail -10</automated>
  </verify>
  <done>5 models + 4 real factories exist; User extended; UserNotificationPreferencesTest GREEN; PHPStan L8 clean.</done>
</task>

<task type="auto">
  <name>Task 2: Author 5 Notification classes + DiscordChannel + turn 2 stubs GREEN</name>
  <files>
    apps/web/app/Notifications/MatchStartingSoon.php,
    apps/web/app/Notifications/MatchCancelled.php,
    apps/web/app/Notifications/MatchResultPublished.php,
    apps/web/app/Notifications/ClanApplicationDecided.php,
    apps/web/app/Notifications/ClanInviteReceived.php,
    apps/web/app/Notifications/Channels/DiscordChannel.php,
    apps/web/tests/Unit/Notifications/MatchStartingSoonNotificationTest.php,
    apps/web/tests/Feature/Notifications/DiscordChannelOutboxTest.php
  </files>
  <action>
Author `app/Notifications/Channels/DiscordChannel.php` (Pitfall 3 LOCKED — D-004 compliance is the prime directive):
```
final class DiscordChannel
{
    public function send(mixed $notifiable, Notification $notification): void
    { ... }
}
```
Implementation:
1. Assert `$notification` has `toDiscord(): array` method; if not, throw RuntimeException.
2. Call `$payload = $notification->toDiscord($notifiable);` to get the outbox payload.
3. INSERT one row into `discord_outbound_messages` with: message_type = $payload['message_type'] (e.g., 'user_dm'), recipient_user_discord_id = $payload['recipient_id'], payload = json of $payload['payload'], status = 'pending', attempts = 0, timestamps.
4. NO `Http::*` calls; NO `discord.com` references. Pest test asserts via `Http::fake(); Http::assertNothingSent();` post-send.

Author 5 Notification classes — each is `final class`, extends `Illuminate\Notifications\Notification`, `implements ShouldQueue`, uses `Queueable` trait. Each class:

a) `MatchStartingSoon` (per RESEARCH Pattern 1 verbatim):
- Constructor: `public readonly GameMatch $match, public readonly int $minutesUntilStart`.
- `via(User $notifiable): array` returns `$notifiable->enabledNotificationChannels('match_starting_soon')` mapped to channel keys: 'database' stays 'database'; 'discord' maps to `DiscordChannel::class`.
- `toArray(User $notifiable): array` returns `['match_id'=>$this->match->id,'match_slug'=>$this->match->slug,'minutes'=>$this->minutesUntilStart,'i18n_key'=>'notifications.match_starting_soon.title']`.
- `databaseType(User $notifiable): string` returns `'match.starting_soon'`.
- `toDiscord(User $notifiable): array` returns ['message_type'=>'user_dm','recipient_id'=>$notifiable->discord_id,'payload'=>['embed_title'=>__('notifications.match_starting_soon.title', ['min'=>$this->minutesUntilStart]),'embed_description'=>__('notifications.match_starting_soon.body', ['match'=>$this->match->title]),'cta_url'=>route('matches.show', $this->match)]].

b) `MatchCancelled`: constructor `public readonly GameMatch $match`, databaseType `match.cancelled`, via uses event_type 'match_cancelled', toDiscord title from notifications.match_cancelled.title.

c) `MatchResultPublished`: constructor `public readonly GameMatch $match, public readonly ?Clan $winnerClan`, databaseType `match.result_published`, via uses event_type 'match_result_published' (Open Question 3 LOCKED — Discord DM default-off, the User::enabledNotificationChannels special-case handles this).

d) `ClanApplicationDecided`: constructor `public readonly ClanApplication $application`, databaseType `clan.application_decided`, via uses event_type 'clan_application_decided'.

e) `ClanInviteReceived`: constructor `public readonly ClanInvite $invite`, databaseType `clan.invite_received`, via uses event_type 'clan_invite_received'.

All 5 use D-04-03-A canonical FQN `App\Models\GameMatch` — no aliases.

All 5 use `__()` for any user-visible string (D-013).

Turn `MatchStartingSoonNotificationTest.php` GREEN:
- Test 1: `it('routes to database channel only when user has only web bell enabled')` — create User with discord_id=null, assert via() returns `['database']`.
- Test 2: `it('routes to both channels when user has discord_id and default prefs')` — assert via() returns `['database', DiscordChannel::class]`.
- Test 3: `it('toArray includes match_id, minutes, i18n_key')` — assert array keys/values.
- Test 4: `it('databaseType is match.starting_soon')` — discriminator stable.
- Test 5: `it('every notification class has a unique databaseType discriminator')` per Pitfall 4 — reflect on all 5 classes via Glob + ReflectionClass, assert no duplicates.

Turn `DiscordChannelOutboxTest.php` GREEN (Pitfall 3 critical test):
- Test 1: `it('writes a discord_outbound_messages row on send')` — instantiate notification, attach to user with discord_id, send via DiscordChannel, assert exactly 1 row inserted with message_type='user_dm'.
- Test 2: `it('makes NO outbound HTTP calls')` — `Http::fake();` before send, `Http::assertNothingSent();` after. This is the Pitfall 3 LOCKED test.
- Test 3: `it('throws RuntimeException when notification lacks toDiscord method')`.

Update `apps/web/tests/Unit/UserNotificationPreferencesTest.php` (already GREEN from Task 1, but extend to test the match_result_published special-case — Discord DM default-off):
- Test: `it('defaults match_result_published discord channel to OFF')` — user with discord_id, no preference rows, assert enabledNotificationChannels('match_result_published') === ['database'] (no discord).
- Test: `it('defaults match_starting_soon discord channel to ON when user has discord_id')` — assert array contains both.
- Test: `it('honours explicit opt-out')` — create row {channel:'database', enabled:false}, assert excluded.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="MatchStartingSoonNotificationTest|DiscordChannelOutboxTest|UserNotificationPreferencesTest" --no-coverage 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 notification classes + DiscordChannel exist; 3 Wave-0 tests turned GREEN; Pitfall 3 (no direct HTTP) and Pitfall 4 (unique databaseType) asserted; PHPStan + Pint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| notification queue→DiscordChannel | Queued jobs trigger DiscordChannel::send — input is a Notification class instance |
| DiscordChannel→discord_outbound_messages | Web tier writes outbox row; bot picks up (Phase 5) |
| notification dispatch→user prefs | enabledNotificationChannels reads user-owned preference rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | E (Elevation of Privilege) | DiscordChannel could bypass D-004 by direct HTTP | mitigate | Pitfall 3 LOCKED — DiscordChannelOutboxTest asserts Http::assertNothingSent; only DB::table('discord_outbound_messages')->insert path allowed |
| T-09-03-02 | T (Tampering) | Notification toDiscord payload could be forged to send unwanted DMs | mitigate | Notification classes are server-side authored only; no user input controls payload directly; route() helper validates match exists |
| T-09-03-03 | I (Information Disclosure) | Notification toArray data leaks private match info | mitigate | data->match_id is a UUID (not enumerable); Vue renderer (plan 09-06) gates access via auth — bell only shown to recipient |
| T-09-03-04 | S (Spoofing) | enabledNotificationChannels could be flipped by attacker writing to user_notification_preferences | mitigate | Plan 09-06 controller for /account/notification-preferences scopes write to auth()->user(); Inertia CSRF enforces |
| T-09-03-05 | T (Tampering) | databaseType discriminator collision routes to wrong renderer | mitigate | Pitfall 4 LOCKED — Pest reflection test asserts all classes have unique databaseType |
| T-09-03-06 | D (DoS) | Mass notification flood via dispatcher bug | mitigate | NotificationDispatcher idempotency check (plan 09-04) + Horizon throttle on the notifications queue |
</threat_model>

<verification>
- 5 models exist; User extended.
- 5 Notification classes exist + DiscordChannel.
- 4 real factories replace Wave 0 stubs.
- 3 Pest tests turn GREEN: MatchStartingSoonNotificationTest, DiscordChannelOutboxTest, UserNotificationPreferencesTest.
- `make phpstan` L8 clean.
- `make pint ARGS="--test"` clean.
- Existing 1134 Pest tests stay GREEN.
</verification>

<success_criteria>
- All artefacts exist; all 3 Wave 0 stubs GREEN.
- DiscordChannel writes outbox row, makes ZERO outbound HTTP (Pitfall 3).
- All 5 Notification classes have unique databaseType (Pitfall 4).
- D-04-03-A respected — `App\Models\GameMatch` used directly, no aliases.
- D-004 respected — no Discord webhook called from web tier.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-03-SUMMARY.md` capturing: D-09-03-* canonical bindings, Pitfall 3+4 verification output, factory state methods catalogue.
</output>
