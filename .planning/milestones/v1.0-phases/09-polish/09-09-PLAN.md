---
phase: 09-polish
plan: 09
type: execute
wave: 7
depends_on: [02]
files_modified:
  - apps/web/app/Models/Clan.php
  - apps/web/app/Models/Player.php
  - apps/web/app/Models/Article.php
  - apps/web/app/Console/Commands/MediaRegenerateWebpCommand.php
  - apps/web/resources/js/Components/ClanLogo.vue
  - apps/web/resources/js/Components/PlayerAvatar.vue
  - apps/web/resources/js/Components/ArticleCover.vue
  - apps/web/tests/Feature/Media/ClanLogoWebpConversionTest.php
  - apps/web/tests/Feature/Media/ArticleCoverWebpConversionTest.php
autonomous: true
requirements:
  - REQ-polish-v1

must_haves:
  truths:
    - "Clan model registers WebP conversions (avatar-thumb 48x48, avatar-card 200x200, avatar-hero 800x800) via spatie/laravel-medialibrary."
    - "Player model registers WebP avatar conversions matching Clan shape."
    - "Article model registers WebP cover conversions (cover-thumb 200x120, cover-card 600x400, cover-hero 1200x630)."
    - "All conversions ->format('webp') + ->queued() per Pattern 5; spatie/image-optimizer auto-runs (no ->nonOptimized())."
    - "MediaRegenerateWebpCommand triggers php artisan media-library:regenerate for one-time backfill (Open Question 6 LOCKED: yes, backfill)."
    - "ClanLogo / PlayerAvatar / ArticleCover Vue components render WebP only (Open Question 1 LOCKED: no JPEG fallback v1)."
    - "ClanLogoWebpConversionTest GREEN + ArticleCoverWebpConversionTest GREEN."
    - "Imagick verified present (preflight done in plan 09-01); conversions actually emit .webp files."
  artifacts:
    - path: "apps/web/app/Models/Clan.php"
      provides: "Extended registerMediaConversions with 3 WebP variants"
      contains: "->format('webp')"
    - path: "apps/web/app/Models/Player.php"
      provides: "Extended registerMediaConversions with avatar WebP variants"
      contains: "->format('webp')"
    - path: "apps/web/app/Models/Article.php"
      provides: "Extended registerMediaConversions with cover WebP variants"
      contains: "->format('webp')"
    - path: "apps/web/app/Console/Commands/MediaRegenerateWebpCommand.php"
      provides: "One-time backfill command wrapping media-library:regenerate"
      contains: "media-library:regenerate"
    - path: "apps/web/resources/js/Components/ClanLogo.vue"
      provides: "WebP-only logo renderer with width/height/loading=lazy/decoding=async"
      contains: "conversions"
  key_links:
    - from: "Clan model registerMediaConversions"
      to: "spatie/laravel-medialibrary conversion engine"
      via: "->addMediaConversion('name')->width(N)->height(N)->format('webp')->queued()"
      pattern: "addMediaConversion"
    - from: "ClanLogo.vue"
      to: "Clan media conversions"
      via: "clan.media[0].conversions['avatar-card']"
      pattern: "conversions\\['avatar"
---

<objective>
Add WebP variants to all three image-bearing models (Clan logos, Player avatars, Article covers) using spatie/laravel-medialibrary's `->format('webp')` chain. Author a one-time backfill command for existing media (Open Question 6 LOCKED: yes — regenerate). Update Vue renderers to consume WebP only (Open Question 1 LOCKED: no JPEG fallback v1).

Purpose: SC-4 image perf pass. WebP cuts image weight 25-35% vs equivalent JPEG with no visible quality loss; combined with `loading=lazy` + `decoding=async` it removes images from the critical render path.

Output: 3 model extensions + 1 artisan command + 3 Vue component updates + 2 GREEN tests + one-time backfill execution evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-polish/09-RESEARCH.md
@CLAUDE.md

<interfaces>
spatie/laravel-medialibrary v11.22 (installed via Phase 7). Conversion pattern (RESEARCH Pattern 5):
```php
public function registerMediaConversions(?Media $media = null): void
{
    $this->addMediaConversion('avatar-thumb')
        ->width(48)->height(48)
        ->format('webp')
        ->queued();
}
```
Notes:
- `->queued()` defers conversion to Horizon (Phase 5 queue); upload returns immediately.
- `->nonOptimized()` would opt OUT of spatie/image-optimizer (we do NOT call this — keep optimization on, per RESEARCH).
- Imagick driver required per Pitfall 6; verified in plan 09-01 preflight.

Vue render pattern (RESEARCH Pattern 5):
```vue
<img
  :src="clan.media[0]?.conversions['avatar-card']"
  :alt="t('clans.logo_alt', { name: clan.name })"
  width="200" height="200"
  loading="lazy"
  decoding="async"
/>
```

Media accessor — Spatie medialibrary exposes `$model->media` collection; each item has `getUrl('conversion-name')` or `conversions['conversion-name']` (depending on serializer). Vue components should use whichever the project's existing serializer/DTO exposes.

php artisan media-library:regenerate command — built-in to spatie; regenerates all conversions for all media. Project wraps it with our own command for Open Question 6 LOCKED documentation.

Existing models:
- Clan (Phase 2 plan 02-03) — already uses InteractsWithMedia trait + HasTranslations. May already have a logo conversion (verify before editing).
- Player (Phase 2 plan 02-03) — uses InteractsWithMedia; avatar collection.
- Article (Phase 7 plan 07-03) — uses InteractsWithMedia; cover collection.

If existing conversions exist (e.g., 'logo-thumb' as JPEG/PNG), we REPLACE them with WebP versions matching the same names (do NOT add parallel old-named conversions; existing Vue references update to point at the same names).

Open Question 1 LOCKED: WebP only, no <picture> + JPEG fallback in v1. Browser support is >99% per RESEARCH. If monitoring shows >0.5% failure rate post-launch, add JPEG fallback as a v2 enhancement.

Open Question 6 LOCKED: yes — run media-library:regenerate as a deployment-time step for existing Phase 7 article media. Conversions are queued so the regenerate doesn't block deploy; Horizon processes async.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend 3 models with WebP conversions + MediaRegenerateWebpCommand + 2 GREEN tests</name>
  <files>
    apps/web/app/Models/Clan.php,
    apps/web/app/Models/Player.php,
    apps/web/app/Models/Article.php,
    apps/web/app/Console/Commands/MediaRegenerateWebpCommand.php,
    apps/web/tests/Feature/Media/ClanLogoWebpConversionTest.php,
    apps/web/tests/Feature/Media/ArticleCoverWebpConversionTest.php
  </files>
  <action>
Read existing Clan.php, Player.php, Article.php to confirm:
- They use InteractsWithMedia trait.
- If a `registerMediaConversions()` method already exists, REPLACE its body with the WebP versions.
- If absent, ADD the method.

Edit `apps/web/app/Models/Clan.php`:
- Ensure `use Spatie\MediaLibrary\HasMedia;` + `use Spatie\MediaLibrary\InteractsWithMedia;` declared (likely already).
- Class implements HasMedia + `use InteractsWithMedia;`.
- Add/replace:
```php
public function registerMediaConversions(?Media $media = null): void
{
    $this->addMediaConversion('avatar-thumb')->width(48)->height(48)->format('webp')->queued();
    $this->addMediaConversion('avatar-card')->width(200)->height(200)->format('webp')->queued();
    $this->addMediaConversion('avatar-hero')->width(800)->height(800)->format('webp')->queued();
}
```
- Add `use Spatie\MediaLibrary\MediaCollections\Models\Media;` to imports.

Edit `apps/web/app/Models/Player.php` — same shape: `avatar-thumb` 48x48, `avatar-card` 200x200, `avatar-hero` 800x800 (matching Clan for visual consistency).

Edit `apps/web/app/Models/Article.php` — different shape (article covers are wider/banner-style):
- `cover-thumb` 200x120, `cover-card` 600x400, `cover-hero` 1200x630 (1200x630 hits OpenGraph optimal size). All ->format('webp')->queued().

Author `apps/web/app/Console/Commands/MediaRegenerateWebpCommand.php`:
- `$signature = 'trenchwars:media:regenerate-webp'`.
- `$description = 'Backfill WebP conversions for all existing media (Phase 9 one-time deployment step).'`.
- `handle(): int { $this->info('Dispatching media:regenerate (queued via Horizon)...'); $this->call('media-library:regenerate'); $this->info('Done. Watch Horizon dashboard for completion.'); return self::SUCCESS; }`.
- This command is NOT scheduled — operator runs once post-deploy.

Turn `ClanLogoWebpConversionTest.php` GREEN:
- Test 1: it('generates avatar-thumb.webp on logo upload') — create Clan, addMedia from a fixture PNG/JPEG, run queue worker sync, assert `getFirstMedia()->getPath('avatar-thumb')` ends in .webp AND file exists on disk.
- Test 2: it('generates avatar-card.webp and avatar-hero.webp variants').
- Test 3: it('spatie/image-optimizer ran (file size smaller than input)') — best-effort assertion comparing file sizes; if optimizer didn't run, fail with explanation.
- Test 4: it('conversion is queued by default').
- Use a fixture PNG/JPEG in `apps/web/tests/Fixtures/` (create if needed — e.g., `apps/web/tests/Fixtures/sample-logo.png`).

Turn `ArticleCoverWebpConversionTest.php` GREEN:
- Test 1: it('generates cover-thumb.webp on article cover upload').
- Test 2: it('generates cover-card.webp and cover-hero.webp variants').
- Test 3: it('cover-hero dimensions are 1200x630 (OpenGraph optimal)').

After writing all tests, run them. If a test fails because Imagick is missing despite plan 09-01 preflight (regression), flag as deviation and apply Dockerfile patch per Pitfall 6.

Per RESEARCH: spatie/image-optimizer is bundled with medialibrary and runs by default — don't add `->nonOptimized()`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="ClanLogoWebpConversionTest|ArticleCoverWebpConversionTest" --no-coverage 2>&1 | tail -20 && docker compose exec web php artisan list trenchwars 2>&1 | grep "media:regenerate-webp"</automated>
  </verify>
  <done>3 models extended; 1 command exists; 2 Pest tests GREEN; Imagick still verified; PHPStan + Pint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Vue components for WebP rendering (ClanLogo + PlayerAvatar + ArticleCover)</name>
  <files>
    apps/web/resources/js/Components/ClanLogo.vue,
    apps/web/resources/js/Components/PlayerAvatar.vue,
    apps/web/resources/js/Components/ArticleCover.vue
  </files>
  <action>
Author `apps/web/resources/js/Components/ClanLogo.vue`:
- Props: `clan: Clan` (from shared-types), `variant: 'thumb' | 'card' | 'hero'` (default 'card').
- Renders `<img>` element with:
  - `src` resolved from `clan.media?.[0]?.conversions['avatar-' + variant]` (chain-safe access).
  - `alt` = `t('clans.logo_alt', { name: clan.name })` via useT().
  - `width` + `height` matching variant dimensions (thumb 48, card 200, hero 800).
  - `loading="lazy"` + `decoding="async"`.
- Fallback when clan has no media: render a placeholder with initials (existing project pattern? grep for placeholder shape; if absent, use first char of clan.name in a styled div).

Author `apps/web/resources/js/Components/PlayerAvatar.vue` — same shape with `player` prop + 'avatar' Open Question 1 LOCKED (no JPEG fallback v1). Variant dimensions match Clan.

Author `apps/web/resources/js/Components/ArticleCover.vue` — different variant set: 'thumb' (200x120), 'card' (600x400), 'hero' (1200x630). Render in article index, article cards, and article show banner respectively.

For all 3 components:
- `<script setup lang="ts">` with `defineProps<{...}>()`.
- All strings via useT() (D-013).
- TS types from `@trenchwars/shared-types`.

Find existing usages of clan/player/article images in the codebase via grep:
- `<img.*clan.*logo`
- `<img.*player.*avatar`
- `<img.*article.*cover`
Replace each with the new component usage:
- `<ClanLogo :clan="clan" variant="card" />` etc.

Run typescript:generate to ensure shared-types are current.
  </action>
  <verify>
    <automated>docker compose exec web pnpm --filter web build 2>&1 | tail -10 && docker compose exec web ./vendor/bin/pest --filter="NoHardcodedStringsTest" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>3 Vue components exist; build succeeds; D-013 i18n compliance verified (NoHardcodedStringsTest stays GREEN); no regressions in component snapshot tests if any.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user upload->medialibrary | File upload crosses; MIME-type validated by Filament/Spatie |
| medialibrary->Imagick | Native code processes user-uploaded image |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-09-01 | E (Code Execution) | Malicious image triggering Imagick CVE | mitigate | Spatie medialibrary validates MIME before passing to Imagick; container isolation (D-021) limits blast radius; Imagick is kept up to date via apt-get install in Dockerfile |
| T-09-09-02 | D (DoS) | Large image upload causing OOM in conversion | mitigate | Filament/medialibrary file size limits at upload; queued conversion (Horizon) isolates failures from web request |
| T-09-09-03 | I (Information Disclosure) | Conversion failure leaks original filename in error response | accept | Conversions queued via Horizon; errors logged server-side, not surfaced to users; original filename is set by uploader anyway |
| T-09-09-04 | T (Tampering) | WebP file overwrites JPEG conversion via media:regenerate | accept | Open Question 6 LOCKED — operator-triggered; conversion names are namespaced (avatar-* vs cover-*); no name collision possible |
</threat_model>

<verification>
- 3 model extensions + 1 artisan command + 3 Vue components.
- 2 Pest tests GREEN.
- pnpm build succeeds.
- D-013 i18n compliance preserved (no hardcoded strings).
- Imagick still verified.
</verification>

<success_criteria>
- WebP variants emitted for Clan logos, Player avatars, Article covers.
- Open Question 1 LOCKED: no JPEG fallback v1.
- Open Question 6 LOCKED: regeneration command shipped for one-time backfill.
- Image-optimizer optimization preserved (not disabled).
- All Vue renderers use loading=lazy + decoding=async.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-09-SUMMARY.md` capturing: conversion name registry per model, file size reduction comparison (sample fixture), Open Question 1+6 LOCKED notes, Pitfall 6 re-verification.
</output>
