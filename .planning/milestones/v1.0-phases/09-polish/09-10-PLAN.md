---
phase: 09-polish
plan: 10
type: execute
wave: 8
depends_on: [06]
files_modified:
  - apps/web/resources/css/app.css
  - apps/web/resources/js/Layouts/PublicLayout.vue
  - .github/workflows/a11y.yml
  - apps/web/tests/Feature/A11y/PublicPagesHtmlLangTest.php
  - apps/web/tests/Feature/A11y/VueFormLabelsTest.php
autonomous: false
requirements:
  - REQ-polish-v1

must_haves:
  truths:
    - "Global :focus-visible CSS rule emits 2px outline + 2px offset using var(--color-focus) on every focusable element."
    - "Enhanced focus shadow for buttons + links via 4px color-mix outer ring."
    - "<html lang> reflects active locale on every public page (PublicPagesHtmlLangTest GREEN)."
    - "Every Vue form input in apps/web/resources/js/ has an associated <label> or aria-label (VueFormLabelsTest GREEN, static-scan)."
    - "SkipToContent link present in PublicLayout (already from Phase 1; verify still wired)."
    - "axe-core CI workflow .github/workflows/a11y.yml exists and runs against public routes only (Pitfall 11 — no admin routes)."
    - "Checkpoint: manual keyboard nav smoke confirmed across every public page (RESEARCH 'Manual keyboard test checklist')."
  artifacts:
    - path: "apps/web/resources/css/app.css"
      provides: "Extended with :focus-visible global + interactive enhancement"
      contains: ":focus-visible"
    - path: ".github/workflows/a11y.yml"
      provides: "GitHub Actions axe-core workflow scanning public routes only"
      contains: "@axe-core/cli"
    - path: "apps/web/tests/Feature/A11y/PublicPagesHtmlLangTest.php"
      provides: "Regression guard for <html lang>"
      contains: "html lang"
    - path: "apps/web/tests/Feature/A11y/VueFormLabelsTest.php"
      provides: "Static scan: every <input> has label or aria-label"
      contains: "VueFormLabelsTest"
  key_links:
    - from: "app.css :focus-visible"
      to: "var(--color-focus)"
      via: "Tailwind v4 @theme semantic token"
      pattern: ":focus-visible"
    - from: ".github/workflows/a11y.yml"
      to: "@axe-core/cli scan target URL"
      via: "axe https://preview.../"
      pattern: "@axe-core/cli"
---

<objective>
Ship SC-5 accessibility deliverables: global :focus-visible rules using Tailwind v4 semantic tokens, axe-core CI workflow scanning public routes (Pitfall 11 — public only), Pest static-scan tests for <html lang> + Vue form labels, and a manual keyboard-nav smoke checkpoint.

Purpose: WCAG 2.1 AA round-1 compliance. axe-core catches contrast + ARIA defects automatically; manual keyboard nav catches the things axe can't (focus traps, logical tab order). Pitfall 11 (axe scanning behind auth) is sidestepped by limiting scope to public surface.

Output: 1 CSS edit + 1 GitHub Actions workflow + 2 Pest tests GREEN + 1 manual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-polish/09-RESEARCH.md
@CLAUDE.md

<interfaces>
Tailwind v4 semantic tokens (Phase 1 plan 01-07):
- `--color-focus` declared in @theme block in apps/web/resources/css/app.css.
- Existing :focus rules likely present from Phase 1; verify before editing — append only the :focus-visible global if not already there.

axe-core/cli v4.11.3 (RESEARCH "Standard Stack"):
- Installed via `npm install -g @axe-core/cli@^4.11.3` in CI.
- Scans a URL: `axe URL --tags wcag2aa,wcag21aa --exit --reporter json > axe-report.json`.

CI workflow pattern (RESEARCH "Accessibility (WCAG 2.1 AA)"):
```yaml
name: Accessibility (axe-core)
on:
  push: { branches: [master] }
  pull_request: { branches: [master] }
jobs:
  axe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 22 }
      - run: npm install -g @axe-core/cli@^4.11.3
      - name: Start staging or local stack
        # PROJECT-SPECIFIC: spin up docker compose stack on the runner
      - name: Run axe scan
        run: axe http://localhost:8080/ --tags wcag2aa,wcag21aa --exit --reporter json > axe-report.json
      - uses: actions/upload-artifact@v4
        if: failure()
        with: { name: axe-report, path: axe-report.json }
```

Pitfall 11: scan ONLY public routes:
- / (homepage)
- /clans
- /players (representative — use a seeded public player slug)
- /matches
- /tournaments
- /articles
- /events
- /leaderboards

Do NOT scan /admin/*, /login, /auth/*, /notifications, /account/* — these are gated/auth-required and axe will scan the login redirect instead.

CLAUDE.md s7 D-013: every UI string via __() / t(). Existing NoHardcodedStringsTest (Phase 1) already enforces this — no new test needed here. But verify when adding :focus-visible CSS doesn't introduce inline text.

PublicLayout.vue (Phase 1) already has a Skip-to-content link with t('a11y.skip_to_content'). Verify it's still present + correctly wired to <main> via skip-link href anchor.

Checkpoint task pattern (per checkpoint section of planner prompt):
- type="checkpoint:human-verify"
- Specific URLs + tab sequence + expected outcomes
- resume-signal "approved" or describe issues
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS focus-visible + axe-core CI workflow + PublicPagesHtmlLangTest + VueFormLabelsTest GREEN</name>
  <files>
    apps/web/resources/css/app.css,
    apps/web/resources/js/Layouts/PublicLayout.vue,
    .github/workflows/a11y.yml,
    apps/web/tests/Feature/A11y/PublicPagesHtmlLangTest.php,
    apps/web/tests/Feature/A11y/VueFormLabelsTest.php
  </files>
  <action>
Edit `apps/web/resources/css/app.css` — append (after existing @theme block):
```css
/* SC-5 a11y focus-visible — Phase 9 plan 09-10 */
*:focus-visible {
  outline: 2px solid var(--color-focus);
  outline-offset: 2px;
  border-radius: 2px;
}

button:focus-visible,
a:focus-visible,
[role="button"]:focus-visible {
  box-shadow: 0 0 0 4px color-mix(in srgb, var(--color-focus) 30%, transparent);
}
```

Verify `--color-focus` is declared in @theme. If not, add a sensible value (matching --color-accent or a higher-contrast variant for both light + dark themes).

Verify `apps/web/resources/js/Layouts/PublicLayout.vue` has SkipToContent link:
```html
<a href="#main" class="sr-only focus:not-sr-only ...">{{ t('a11y.skip_to_content') }}</a>
<main id="main">...</main>
```
If absent or broken (e.g., #main anchor missing on inner page wrappers), fix to ensure consistent skip-link behaviour.

Author `.github/workflows/a11y.yml` (NEW workflow):
- Triggers: push to master + pull_request to master + workflow_dispatch.
- Job 'axe' on ubuntu-latest:
  - Checkout code.
  - Setup Node 22.
  - Install @axe-core/cli globally.
  - Spin up docker compose stack: `docker compose up -d --build` (use repo root compose).
  - Wait for the web service to be reachable: `until curl -sf http://localhost/health >/dev/null; do sleep 2; done` (or whatever the healthcheck endpoint is — verify via Phase 1 routes).
  - Run scans against each public route in a matrix or sequential script:
    ```bash
    for url in / /clans /matches /tournaments /articles /events /leaderboards; do
      echo "Scanning ${url}..."
      axe "http://localhost${url}" --tags wcag2aa,wcag21aa --exit --reporter json > "axe-${url//\//_}.json" || EXIT=$?
    done
    exit ${EXIT:-0}
    ```
  - Upload all axe-*.json artifacts on failure.
- Pitfall 11 compliance: comment explicitly that admin routes are NOT scanned in v1.

Turn `PublicPagesHtmlLangTest.php` GREEN:
- For each public route in the test matrix (/, /clans, /matches, /tournaments, /articles, /events, /leaderboards), GET the response and assert the HTML body contains `<html lang="en"` (or app()->getLocale() value).
- Test 1: it('renders <html lang="en"> on / (homepage)').
- Test 2-8: same for the other public routes.

Turn `VueFormLabelsTest.php` GREEN (static-scan Pest test — pattern from Phase 1 NoHardcodedStringsTest):
- For each `.vue` file under `apps/web/resources/js/`, parse the <template> block.
- Find every `<input>`, `<textarea>`, `<select>` element.
- Assert each has at minimum ONE of:
  - An adjacent or wrapping `<label for="X">` with id matching the input.
  - An `aria-label="..."` attribute.
  - An `aria-labelledby="..."` attribute.
- Output: list of violations with file + line number; expect 0.
- Implementation: use Symfony DomCrawler or a regex-based check (the project already has a similar pattern in NoHardcodedStringsTest — match that style).
- Allowed exceptions: type="hidden" inputs, type="submit" buttons.
- Pre-existing violations: this is a SC-5 deliverable — every existing Vue form input must be label-compliant before this test passes. Fix as found (likely small number, mostly already compliant from D-013 i18n discipline).
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="PublicPagesHtmlLangTest|VueFormLabelsTest" --no-coverage 2>&1 | tail -20 && test -f /home/rtx/projects/trench-wars/.github/workflows/a11y.yml</automated>
  </verify>
  <done>CSS + layout edits + workflow + 2 Pest tests GREEN; any pre-existing form label violations fixed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual keyboard navigation smoke (RESEARCH 'Manual keyboard test checklist')</name>
  <what-built>
    Tab/Shift+Tab/Enter/Esc behaviour across the public surface:
    - Top nav links
    - Theme toggle (Space activates)
    - Mobile menu (open/close, focus return)
    - Login button
    - User menu (when authed)
    - Notifications bell (open drawer, arrow keys cycle, Esc closes)
    - Notification preferences (Space toggles switches)
    - Match signup modal (Tab through slots, Enter selects, Esc cancels)
    - Article reader (heading/link order)
    - Filament admin BulkActions (focus trap inside modal)
  </what-built>
  <how-to-verify>
1. Start the local stack via `make up`.
2. Open http://localhost in a browser with mouse hidden / Tab-only.
3. Walk the checklist in RESEARCH 'Manual keyboard test checklist' verbatim:
   - Tab through top nav: every link receives focus ring; Enter activates.
   - Tab to theme toggle: Space toggles light/dark; aria-pressed updates.
   - On mobile width (resize to <768px): Tab into hamburger; menu opens; Tab cycles inside; Esc closes; focus returns to hamburger button.
   - Tab to LoginButton: Enter redirects to Discord OAuth.
   - After login: Tab to user menu; Enter opens; Esc closes.
   - Tab to notifications bell: Enter opens drawer; arrow keys cycle through items; Enter on item marks read; Esc closes.
   - Navigate to /account/notification-preferences: Tab through 5x2 switch matrix; Space toggles each.
   - Navigate to /matches/{id} with open signups: Tab opens signup modal; Tab cycles slots; Enter selects; Esc cancels.
   - Navigate to /articles/{any}: Tab through headings + links in DOM order.
   - Open /admin (as admin user): select 2 users with checkboxes (Space); open BulkAction menu (Enter); Tab cycles inside modal; Esc closes; focus returns to BulkAction trigger.
4. Verify visible focus rings appear on every focusable element (2px outline + 4px shadow on buttons/links).
5. Note any anomalies in the resume signal.
  </how-to-verify>
  <resume-signal>Type "approved" if every step passes, or describe failures with surface name + Tab step number.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner->local docker stack | axe scans served HTML; no auth crossing |
| browser keyboard input->Vue components | Native focus + keydown handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-10-01 | I (Information Disclosure) | axe-core CI artifact leaks DOM contents | accept | artifacts upload on FAILURE only; report contains rule violations + selectors, not sensitive data |
| T-09-10-02 | D (DoS) | axe scan retries hang CI runner | mitigate | --exit flag returns non-zero immediately on violation; per-URL timeout via CI step timeout default |
| T-09-10-03 | T (Tampering) | Manual keyboard checkpoint skipped or rubber-stamped | accept | Reviewer-discipline issue, not technical; checkpoint sets blocking gate on the plan completion |
| T-09-10-04 | I (Information Disclosure) | Pitfall 11 — axe scanning admin routes | mitigate | Workflow explicitly enumerates public routes only; comment in YAML documents the constraint |
</threat_model>

<verification>
- :focus-visible CSS active site-wide (verify manually via DevTools inspector).
- axe-core workflow file exists.
- 2 Pest tests GREEN.
- Manual keyboard checkpoint approved.
- PublicPagesHtmlLangTest covers every public route.
- NoHardcodedStringsTest stays GREEN.
</verification>

<success_criteria>
- SC-5 keyboard navigation works across public surface.
- SC-5 visible focus rings on every focusable element.
- SC-5 <html lang> correct on every public page.
- Vue form inputs have associated labels.
- axe-core CI workflow gates merges on WCAG 2.1 AA compliance.
- Pitfall 11 (admin scan) avoided.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-10-SUMMARY.md` capturing: axe-core workflow URL list, focus-visible CSS diff, form-label fixes catalog, manual keyboard smoke verification log.
</output>
