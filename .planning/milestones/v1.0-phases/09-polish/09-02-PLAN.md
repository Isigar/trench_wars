---
phase: 09-polish
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apps/web/database/migrations/2026_05_18_100000_create_notifications_table.php
  - apps/web/database/migrations/2026_05_18_100100_create_user_notification_preferences_table.php
  - apps/web/database/migrations/2026_05_18_100200_create_bans_table.php
  - apps/web/database/migrations/2026_05_18_100300_create_match_disputes_table.php
  - apps/web/database/migrations/2026_05_18_100400_create_abuse_reports_table.php
  - apps/web/database/migrations/2026_05_18_100500_extend_discord_outbound_message_types_for_user_dm.php
  - apps/web/database/migrations/2026_05_18_100600_add_match_player_stats_kills_index.php
autonomous: true
requirements:
  - REQ-polish-v1

must_haves:
  truths:
    - "`notifications` table exists with UUID PK, `uuidMorphs('notifiable')`, `jsonb data`, `read_at`, composite indexes on (notifiable_type, notifiable_id, read_at) and (notifiable_type, notifiable_id, created_at)."
    - "`user_notification_preferences` table exists with composite UNIQUE (user_id, event_type, channel) and supporting index on user_id."
    - "`bans` table exists with FKs to users (issued_by, lifted_by); `(user_id, expires_at)` index; `ban_type` constrained to {temporary, permanent}."
    - "`match_disputes` table exists with FK to matches (CASCADE per A9 RESEARCH assumption), FKs to users (raised_by, resolved_by); status CHECK + resolution CHECK; (status, created_at) index."
    - "`abuse_reports` table exists with morph-style target_type/target_id, status CHECK, supporting indexes per RESEARCH section."
    - "`discord_outbound_messages` CHECK constraint extended to include `user_dm` (Pitfall 10 mitigation)."
    - "Composite index `(player_id, kills DESC)` added to `match_player_stats` for top-N performance (RESEARCH A5)."
    - "Migrations are reversible — down() drops everything cleanly."
  artifacts:
    - path: "apps/web/database/migrations/2026_05_18_100000_create_notifications_table.php"
      provides: "Laravel polymorphic notifications table — UUID PK, uuidMorphs, jsonb data, read_at"
      contains: "Schema::create('notifications'"
    - path: "apps/web/database/migrations/2026_05_18_100100_create_user_notification_preferences_table.php"
      provides: "Per-user × event_type × channel toggle matrix"
      contains: "unp_unique"
    - path: "apps/web/database/migrations/2026_05_18_100200_create_bans_table.php"
      provides: "Site-wide bans table (Open Question 4 LOCKED: site-wide v1)"
      contains: "ban_type"
    - path: "apps/web/database/migrations/2026_05_18_100300_create_match_disputes_table.php"
      provides: "Match dispute workflow table — status state machine"
      contains: "match_disputes"
    - path: "apps/web/database/migrations/2026_05_18_100400_create_abuse_reports_table.php"
      provides: "Report-abuse review queue table"
      contains: "abuse_reports"
    - path: "apps/web/database/migrations/2026_05_18_100500_extend_discord_outbound_message_types_for_user_dm.php"
      provides: "CHECK constraint extension adding user_dm (mirrors Phase 5/6/7/8 pattern)"
      contains: "user_dm"
    - path: "apps/web/database/migrations/2026_05_18_100600_add_match_player_stats_kills_index.php"
      provides: "Index (player_id, kills DESC) for top-N leaderboard aggregation"
      contains: "match_player_stats"
  key_links:
    - from: "notifications.notifiable_type/id"
      to: "users.id (UUID)"
      via: "uuidMorphs polymorphic"
      pattern: "uuidMorphs"
    - from: "user_notification_preferences.user_id"
      to: "users.id"
      via: "foreignUuid with cascadeOnDelete"
      pattern: "foreignUuid.*constrained.*cascadeOnDelete"
    - from: "bans.user_id"
      to: "users.id"
      via: "foreignUuid with cascadeOnDelete"
      pattern: "foreignUuid"
    - from: "match_disputes.match_id"
      to: "matches.id"
      via: "foreignUuid with cascadeOnDelete (A9 LOCKED)"
      pattern: "foreignUuid.*matches"
---

<objective>
Author 7 migrations creating the Phase 9 schema: `notifications`, `user_notification_preferences`, `bans`, `match_disputes`, `abuse_reports` tables; extend `discord_outbound_messages` CHECK to allow `user_dm`; add `(player_id, kills DESC)` composite index to `match_player_stats`.

Purpose: Lay the entire Phase 9 schema in a single deterministic step. Mirrors the Phase 4/6/7/8 "all migrations land in one plan" idiom so that subsequent model/service plans never need to touch schema.

Output: 7 migration files, applied cleanly to a fresh DB; `make artisan ARGS="migrate:fresh"` returns clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-polish/09-RESEARCH.md
@.planning/phases/09-polish/09-01-PLAN.md
@CLAUDE.md

<interfaces>
<!-- Existing Phase 5/8 migration patterns the executor inherits -->

UUID FK pattern (from existing migrations):
```php
$table->foreignUuid('user_id')->constrained()->cascadeOnDelete();
$table->foreignUuid('issued_by_user_id')->constrained('users');
$table->foreignUuid('lifted_by_user_id')->nullable()->constrained('users');
```

CHECK constraint extension pattern (from 2026_05_15_100500_extend_discord_outbound_message_types_for_phase_6.php and Phase 7/8 successors):
```php
DB::statement('ALTER TABLE discord_outbound_messages DROP CONSTRAINT IF EXISTS discord_outbound_messages_message_type_check');
DB::statement("ALTER TABLE discord_outbound_messages ADD CONSTRAINT discord_outbound_messages_message_type_check CHECK (message_type IN ('match_signup_open','match_result_announce',/* full existing list */ ,'user_dm'))");
```
The executor MUST first read the most recent extend_discord_outbound_message_types_for_* migration to copy the current canonical IN-list, then append `user_dm`.

uuidMorphs pattern (from Pitfall 7):
```php
$table->uuidMorphs('notifiable'); // NOT morphs() — users PK is UUID
```

jsonb column type (Postgres-specific):
```php
$table->jsonb('data');
```

Composite index with explicit name:
```php
$table->index(['notifiable_type', 'notifiable_id', 'read_at'], 'notifications_unread_idx');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create notifications + user_notification_preferences + bans tables</name>
  <files>
    apps/web/database/migrations/2026_05_18_100000_create_notifications_table.php,
    apps/web/database/migrations/2026_05_18_100100_create_user_notification_preferences_table.php,
    apps/web/database/migrations/2026_05_18_100200_create_bans_table.php
  </files>
  <action>
Author migration `create_notifications_table` per Pitfall 7 (LOCKED):
- `$table->uuid('id')->primary();`
- `$table->uuidMorphs('notifiable');` (creates notifiable_type VARCHAR + notifiable_id UUID + composite index)
- `$table->string('type');` (the `databaseType()` discriminator; index it for Pitfall 4 dedupe queries)
- `$table->jsonb('data');` (NOT text — Postgres jsonb for indexable querying)
- `$table->timestamp('read_at')->nullable();`
- `$table->timestamps();`
- Composite indexes per RESEARCH "Database indexes added in Phase 9": `(notifiable_type, notifiable_id, read_at)` and `(notifiable_type, notifiable_id, created_at)`.
- down() drops the table.

Author migration `create_user_notification_preferences_table`:
- `$table->id();` (bigint PK — preferences are not user-visible, no need for UUID)
- `$table->foreignUuid('user_id')->constrained()->cascadeOnDelete();`
- `$table->string('event_type');` (per RESEARCH: match_starting_soon | match_cancelled | match_result_published | clan_application_decided | clan_invite_received)
- `$table->string('channel');` (database | discord)
- `$table->boolean('enabled')->default(true);`
- `$table->timestamps();`
- `$table->unique(['user_id', 'event_type', 'channel'], 'unp_unique');`
- `$table->index('user_id');`

Author migration `create_bans_table` (per RESEARCH Moderator Tooling schema + Open Question 4 LOCKED: site-wide v1, no clan_id column):
- `$table->id();`
- `$table->foreignUuid('user_id')->constrained()->cascadeOnDelete();`
- `$table->string('ban_type');` (temporary | permanent — enforce via Pest test in plan 09-03, not via DB CHECK to stay portable)
- `$table->text('reason');`
- `$table->timestampTz('expires_at')->nullable();` (NULL for permanent)
- `$table->foreignUuid('issued_by_user_id')->constrained('users');`
- `$table->timestampTz('lifted_at')->nullable();`
- `$table->foreignUuid('lifted_by_user_id')->nullable()->constrained('users');`
- `$table->text('lift_reason')->nullable();`
- `$table->timestamps();`
- `$table->index(['user_id', 'expires_at']);`
- `$table->index('issued_by_user_id');`

All three migrations: idiomatic Laravel return-anonymous-class pattern with `up()` and `down()`; PHP 8.4 readonly typed properties N/A here (migrations are stateful).
  </action>
  <verify>
    <automated>docker compose exec web php artisan migrate:fresh --force --pretend 2>&1 | grep -E "create_notifications_table|create_user_notification_preferences_table|create_bans_table"</automated>
  </verify>
  <done>Three migration files exist, `migrate:fresh --pretend` lists all three in the plan, and `make phpstan` passes (migration anonymous classes are excluded by default).</done>
</task>

<task type="auto">
  <name>Task 2: Create match_disputes + abuse_reports tables + extend discord_outbound CHECK + add kills index</name>
  <files>
    apps/web/database/migrations/2026_05_18_100300_create_match_disputes_table.php,
    apps/web/database/migrations/2026_05_18_100400_create_abuse_reports_table.php,
    apps/web/database/migrations/2026_05_18_100500_extend_discord_outbound_message_types_for_user_dm.php,
    apps/web/database/migrations/2026_05_18_100600_add_match_player_stats_kills_index.php
  </files>
  <action>
Author migration `create_match_disputes_table`:
- `$table->id();`
- `$table->foreignUuid('match_id')->constrained('matches')->cascadeOnDelete();` (A9 LOCKED — CASCADE per RESEARCH assumption)
- `$table->foreignUuid('raised_by_user_id')->constrained('users');`
- `$table->text('body');`
- `$table->string('status')->default('open');` (state machine validated in plan 09-07 service layer)
- `$table->string('resolution')->nullable();` (result_amended | result_voided | no_action | sanction_issued)
- `$table->text('resolution_notes')->nullable();`
- `$table->foreignUuid('resolved_by_user_id')->nullable()->constrained('users');`
- `$table->timestampTz('resolved_at')->nullable();`
- `$table->timestamps();`
- `$table->index(['status', 'created_at']);`
- `$table->index('match_id');`
- Pitfall 11 partial unique index (one open dispute per match per user): `$table->unique(['match_id', 'raised_by_user_id'], 'one_open_dispute_per_user_per_match');` — but only when status='open'. Implement via raw SQL since Laravel Blueprint does not expose partial unique:
  `DB::statement("CREATE UNIQUE INDEX one_open_dispute_per_user_per_match ON match_disputes (match_id, raised_by_user_id) WHERE status = 'open'");`

Author migration `create_abuse_reports_table` (per RESEARCH "Report-abuse flow" schema):
- `$table->id();`
- `$table->foreignUuid('reporter_user_id')->constrained('users')->cascadeOnDelete();`
- `$table->string('target_type');` (e.g., 'App\Models\Clan', 'App\Models\Player', 'App\Models\Article', 'App\Models\GameMatch')
- `$table->string('target_id');` (varchar — accepts both UUID and bigint targets)
- `$table->string('reason_code');` (harassment | spam | cheating | inappropriate_content | other)
- `$table->text('body')->nullable();`
- `$table->string('status')->default('pending');` (pending | dismissed | actioned)
- `$table->foreignUuid('reviewed_by_user_id')->nullable()->constrained('users');`
- `$table->timestampTz('reviewed_at')->nullable();`
- `$table->text('review_notes')->nullable();`
- `$table->timestamps();`
- `$table->index(['status', 'created_at']);`
- `$table->index(['target_type', 'target_id']);`
- `$table->index('reporter_user_id');`

Author migration `extend_discord_outbound_message_types_for_user_dm` per Pitfall 10:
1. Read the latest existing extend_discord_outbound_message_types_for_* migration in apps/web/database/migrations (Phase 8 path: 2026_05_16_100500_extend_discord_outbound_message_types_for_match_result_announce.php).
2. Copy the current canonical IN-list verbatim.
3. Append `'user_dm'` to the list.
4. Drop then recreate CHECK constraint: `DB::statement("ALTER TABLE discord_outbound_messages DROP CONSTRAINT IF EXISTS discord_outbound_messages_message_type_check"); DB::statement("ALTER TABLE discord_outbound_messages ADD CONSTRAINT discord_outbound_messages_message_type_check CHECK (message_type IN (<full list including user_dm>))");`
5. down() recreates the previous CHECK without user_dm.

Author migration `add_match_player_stats_kills_index` per RESEARCH A5:
- `Schema::table('match_player_stats', fn (Blueprint $t) => $t->index(['player_id', 'kills'], 'mps_player_kills_idx'));` — Postgres B-tree handles DESC ordering on a plain ASC index at query time; explicit DESC requires raw SQL only if seq-scan profiling proves it. Use plain index first.
- down() drops the index.
  </action>
  <verify>
    <automated>docker compose exec web php artisan migrate:fresh --force 2>&1 | tail -20 && docker compose exec web php artisan migrate:rollback --step=7 --force 2>&1 | tail -5 && docker compose exec web php artisan migrate --force 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 migrations apply cleanly on `migrate:fresh`; rollback succeeds; full Pest suite (existing 1134 tests) still GREEN after re-migrating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner→Postgres | Schema DDL crosses; runs as configured DB user with DDL privileges |
| application code→new tables | Eloquent models in plan 09-03 query/insert under app DB user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | T (Tampering) | notifications.data jsonb column | mitigate | Writes restricted to Notification class path; admin Filament UI in plan 09-07 reads-only (no edit/delete) per CLAUDE.md §6 (activity_log style) |
| T-09-02-02 | I (Information Disclosure) | bans.reason free-text field | accept | Reason is moderator-authored; visible only to moderators/admins via Filament; not exposed on public surface |
| T-09-02-03 | D (DoS) | abuse_reports table fillable from anonymous reporter | mitigate | `report-abuse` rate limiter (5/hour by user_id) added in plan 09-11; auth required (no anonymous reports v1) |
| T-09-02-04 | E (Elevation of Privilege) | match_disputes.resolution column | mitigate | Plan 09-07 DisputeService enforces moderator permission on transition; activity_log row written for every change |
| T-09-02-05 | T (Tampering) | discord_outbound CHECK constraint | mitigate | CHECK enforced at DB layer; new message_type values must be added via migration (auditable change) |
| T-09-02-06 | I (Information Disclosure) | notifications table polymorphic morphs | accept | notifiable_id is UUID; not enumerable; notifications are user-scoped via auth middleware in plan 09-06 controller |
</threat_model>

<verification>
- `docker compose exec web php artisan migrate:fresh` succeeds without errors.
- All 7 migrations show up in `migrations` table.
- Rollback + re-migrate cycle clean.
- Existing 1134 Pest tests stay GREEN under fresh schema.
- `make phpstan` passes.
</verification>

<success_criteria>
- 7 migration files exist at the canonical 2026_05_18_* timestamps.
- `migrate:fresh` applies all 7 cleanly.
- `migrate:rollback --step=7` reverses cleanly.
- Existing test suite stays GREEN.
- Pint+PHPStan gates pass.
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-02-SUMMARY.md` capturing: migration list with row counts, CHECK constraint final IN-list, rollback verification output, A5/A9 LOCKED notes.
</output>
