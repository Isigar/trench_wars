---
phase: 09-polish
plan: 11
type: execute
wave: 8
depends_on: [02, 03, 07, 08]
files_modified:
  - apps/web/app/Providers/AppServiceProvider.php
  - apps/web/app/Http/Controllers/ReportsController.php
  - apps/web/app/Http/Requests/StoreAbuseReportRequest.php
  - apps/web/app/Filament/Resources/AbuseReportResource.php
  - apps/web/app/Filament/Resources/AbuseReportResource/Pages/ListAbuseReports.php
  - apps/web/app/Filament/Resources/AbuseReportResource/Pages/ViewAbuseReport.php
  - apps/web/routes/web.php
  - apps/web/resources/js/Pages/Report/Create.vue
  - apps/web/resources/js/Components/ReportButton.vue
  - apps/web/tests/Unit/RateLimiterDefinitionsTest.php
  - apps/web/tests/Feature/Security/PublicApiThrottleTest.php
  - apps/web/tests/Feature/Reports/ReportAbuseTest.php
  - apps/web/tests/Feature/Reports/ReportAbuseThrottleTest.php
  - apps/web/tests/Feature/Admin/AbuseReportWorkflowTest.php
autonomous: false
requirements:
  - REQ-polish-v1

must_haves:
  truths:
    - "AppServiceProvider::boot() defines 4 new RateLimiters: public-api (30/min by IP), auth (10/min by IP), notifications-read (120/min by user), report-abuse (5/hour by user)."
    - "Existing /clans.json /players.json /events/feed.json /search routes attach throttle:public-api middleware."
    - "POST /reports endpoint accepts {target_type, target_id, reason_code, body} and creates abuse_reports row + activity_log entry."
    - "AbuseReportResource exists in Filament with list/view pages + Dismiss + Action transitions; gated by view-reports + manage-reports permissions."
    - "Reports throttle: 6th report in 1 hour returns 429."
    - "ReportButton.vue component renders on Clan/Player/Article/Match detail pages for authenticated users."
    - "5 Pest tests GREEN."
    - "Checkpoint: manual rate-limit boundary smoke confirms 31st request to /clans.json returns 429."
  artifacts:
    - path: "apps/web/app/Providers/AppServiceProvider.php"
      provides: "Extended boot() with 4 RateLimiter definitions"
      contains: "RateLimiter::for"
    - path: "apps/web/app/Http/Controllers/ReportsController.php"
      provides: "create + store endpoints for abuse reports"
      exports: ["create", "store"]
    - path: "apps/web/app/Http/Requests/StoreAbuseReportRequest.php"
      provides: "Validation rules for abuse_reports payload"
      contains: "rules"
    - path: "apps/web/app/Filament/Resources/AbuseReportResource.php"
      provides: "Filament resource for abuse_reports review queue"
      contains: "AbuseReport"
    - path: "apps/web/resources/js/Components/ReportButton.vue"
      provides: "Inline Report CTA on Clan/Player/Article/Match pages"
      contains: "router.visit"
  key_links:
    - from: "AppServiceProvider::boot"
      to: "Laravel rate limiter registry"
      via: "RateLimiter::for"
      pattern: "RateLimiter::for"
    - from: "public JSON endpoints"
      to: "throttle:public-api middleware"
      via: "->middleware"
      pattern: "throttle:public-api"
---

<objective>
Ship SC-5 security hardening: 4 RateLimiter definitions, throttle middleware on public JSON + auth + notifications + report-abuse endpoints, AbuseReportResource Filament queue with state machine, public POST /reports endpoint, and ReportButton inline component for users to flag bad actors.

Purpose: Layered defenses against DoS, harassment, and privilege escalation. Layered with moderator tooling from plan 09-07, this completes the abuse-response loop.

Output: 1 provider extension + 1 controller + 1 FormRequest + 1 Filament resource + 1 Vue page + 1 component + 5 GREEN tests + manual checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-polish/09-RESEARCH.md
@.planning/phases/09-polish/09-08-PLAN.md
@.planning/phases/09-polish/09-07-PLAN.md
@CLAUDE.md

<interfaces>
RateLimiter::for pattern (Laravel 12 — AppServiceProvider since RouteServiceProvider removed):
```php
use Illuminate\Cache\RateLimiting\Limit;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\RateLimiter;

RateLimiter::for('public-api', fn (Request $r) => Limit::perMinute(30)->by($r->ip()));
RateLimiter::for('auth', fn (Request $r) => Limit::perMinute(10)->by($r->ip()));
RateLimiter::for('notifications-read', fn (Request $r) => Limit::perMinute(120)->by((string) $r->user()?->id));
RateLimiter::for('report-abuse', fn (Request $r) => Limit::perHour(5)->by((string) $r->user()?->id));
```

Existing limiters (Phase 1-8): api (60/min), discord-bot (600/min), rcon-internal (600/min). DO NOT MODIFY.

Middleware attachment: `->middleware('throttle:public-api')` on route or group.

Existing Phase 7 plan 07-09 throttle:60,1 inline on /events/feed.json and /search — REPLACE with the named throttle:public-api (30/min by IP) for consistency.

AbuseReport model (plan 09-03): reporter_user_id, target_type, target_id, reason_code, body, status (pending|dismissed|actioned), reviewed_by_user_id, reviewed_at, review_notes.

Spatie permission gates (plan 09-07 seeder):
- view-reports — see queue.
- manage-reports — transition status.

BanService::issue from plan 09-07 — moderator action_with_ban delegates here when transitioning report->actioned with a linked ban.

Filament Resource template — use plan 09-07 MatchDisputeResource shape.

Phase 1+ /auth/discord/callback route may already have a throttle; check and apply throttle:auth (10/min) if not.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 4 RateLimiter definitions + throttle wiring + RateLimiterDefinitionsTest + PublicApiThrottleTest GREEN</name>
  <files>
    apps/web/app/Providers/AppServiceProvider.php,
    apps/web/routes/web.php,
    apps/web/tests/Unit/RateLimiterDefinitionsTest.php,
    apps/web/tests/Feature/Security/PublicApiThrottleTest.php
  </files>
  <action>
Extend `apps/web/app/Providers/AppServiceProvider::boot()` (joins Model::shouldBeStrict from plan 09-08 — append new RateLimiter::for calls). Add the 4 limiters per the verbatim block in interfaces. Add necessary use statements.

Edit `apps/web/routes/web.php`:
- Locate existing JSON endpoint routes via grep. Update or add middleware to `throttle:public-api`:
  - /clans.json (or /clans?format=json)
  - /players.json
  - /events/feed.json — REPLACE existing throttle:60,1 with throttle:public-api
  - /search — REPLACE existing throttle:60,1 with throttle:public-api
- Locate /auth/discord/callback (Phase 1 plan 01-09) — add throttle:auth if not already present.
- Locate /login (if exists) — add throttle:auth.
- /leaderboards already has throttle:public-api from plan 09-06.

Turn `RateLimiterDefinitionsTest.php` GREEN:
- Test 1: public-api limiter defined at 30/min by IP — invoke RateLimiter::limiter('public-api') closure with fake Request, assert Limit shape.
- Test 2: auth limiter at 10/min by IP.
- Test 3: notifications-read at 120/min by user.
- Test 4: report-abuse at 5/hour by user.
- Test 5: existing api, discord-bot, rcon-internal limiters still defined (regression guard).

Turn `PublicApiThrottleTest.php` GREEN:
- beforeEach: `RateLimiter::clear` for the relevant key.
- Test 1: 30 requests to /clans.json allowed within 1 minute.
- Test 2: 31st returns 429.
- Test 3: different IPs do not share the limit (use withServerVariables(['REMOTE_ADDR' => 'X.X.X.X'])).
- Test 4: /players.json, /events/feed.json, /search all carry throttle:public-api (assert route middleware list).
- Test 5: /admin routes do NOT carry public-api throttle (they use admin guard instead).
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="RateLimiterDefinitionsTest|PublicApiThrottleTest" --no-coverage</automated>
  </verify>
  <done>4 limiters defined; middleware attached; 2 Pest tests GREEN; existing test suite still GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: ReportsController + AbuseReportResource + ReportButton + 3 GREEN tests</name>
  <files>
    apps/web/app/Http/Controllers/ReportsController.php,
    apps/web/app/Http/Requests/StoreAbuseReportRequest.php,
    apps/web/app/Filament/Resources/AbuseReportResource.php,
    apps/web/app/Filament/Resources/AbuseReportResource/Pages/ListAbuseReports.php,
    apps/web/app/Filament/Resources/AbuseReportResource/Pages/ViewAbuseReport.php,
    apps/web/routes/web.php,
    apps/web/resources/js/Pages/Report/Create.vue,
    apps/web/resources/js/Components/ReportButton.vue,
    apps/web/tests/Feature/Reports/ReportAbuseTest.php,
    apps/web/tests/Feature/Reports/ReportAbuseThrottleTest.php,
    apps/web/tests/Feature/Admin/AbuseReportWorkflowTest.php
  </files>
  <action>
Author `app/Http/Requests/StoreAbuseReportRequest.php`:
- authorize: return auth()->check().
- rules: target_type required|string|in:App\Models\Clan,App\Models\Player,App\Models\Article,App\Models\GameMatch; target_id required|string|max:64; reason_code required|in:harassment,spam,cheating,inappropriate_content,other; body nullable|string|max:2000.

Author `app/Http/Controllers/ReportsController.php`:
- create(Request request): Response — Inertia render Report/Create with target_type + target_id + target_name prefill from query string.
- store(StoreAbuseReportRequest request): RedirectResponse — validate target exists (find on the morph class; abort 404 if not); create AbuseReport with reporter_user_id=auth()->id(); activity()->causedBy(auth()->user())->performedOn($target)->withProperties(['reason_code'=>$rc,'body_preview'=>Str::limit($body,200)])->log('abuse.reported'); redirect back with flash 'reports.flash.submitted'.

Edit `apps/web/routes/web.php` appending under auth middleware:
```php
Route::middleware(['auth', 'throttle:report-abuse'])->group(function () {
    Route::get('/reports/create', [ReportsController::class, 'create'])->name('reports.create');
    Route::post('/reports', [ReportsController::class, 'store'])->name('reports.store');
});
```

Author `app/Filament/Resources/AbuseReportResource.php`:
- Model = AbuseReport::class, navigation 'Moderation' group, icon heroicon-o-flag.
- canViewAny gated `auth()->user()?->can('view-reports')`.
- Table: columns reporter (user name link), target_type short label, target_id (linked if resolvable), reason_code badge, status badge, created_at.
- Filters: status, reason_code, date range.
- Actions: 
  - Action::make('dismiss') with form Textarea(review_notes required), action: update status=dismissed, reviewed_by, reviewed_at, review_notes; activity log; visible(user can manage-reports).
  - Action::make('action_with_ban') with form Textarea(review_notes), Select(ban_type), Textarea(ban_reason min 10), DateTimePicker(expires_at conditional on temporary); action: $report->update(status=actioned ...); if target_type=Player resolve target user via player; BanService::issue(...); activity log; visible(user can manage-reports AND moderate-users).
- Each action writes activity_log row with causer=moderator, subject=AbuseReport, log='abuse.report_transitioned'.

Author Pages: ListAbuseReports + ViewAbuseReport — standard Filament v3 page stubs.

Author `resources/js/Pages/Report/Create.vue`:
- Props: target_type, target_id, target_name.
- Form fields: read-only target display, Select reason_code, Textarea body.
- onSubmit: router.post route('reports.store').
- All strings via useT().

Author `resources/js/Components/ReportButton.vue`:
- Props: targetType, targetId, targetName.
- v-if auth user; renders Flag icon button with t('reports.cta.submit') label.
- onClick: router.visit(route('reports.create', { target_type, target_id })).

Find existing public detail pages (Clan/Show.vue, Player/Show.vue, Article/Show.vue, Match/Show.vue) and embed `<ReportButton ... />` in each.

Turn `ReportAbuseTest.php` GREEN:
- Test 1: POST /reports creates abuse_reports row with reporter=auth user.
- Test 2: writes activity_log row with causer=reporter, subject=target.
- Test 3: 404 when target_id does not exist.
- Test 4: validates reason_code enum.
- Test 5: validates body max 2000.
- Test 6: anonymous POST redirects to login.

Turn `ReportAbuseThrottleTest.php` GREEN:
- Test 1: 5 reports in 1 hour allowed.
- Test 2: 6th returns 429.
- Test 3: different users have separate limits.

Turn `AbuseReportWorkflowTest.php` GREEN:
- Test 1: moderator can dismiss pending report — status flips, activity log row written.
- Test 2: moderator can action a report and link a ban — both AbuseReport.status=actioned AND new Ban row exists.
- Test 3: non-moderator gets 403 on /admin/abuse-reports.
- Test 4: non-moderator cannot dismiss/action.
- Test 5: view-reports without manage-reports allows list+view but blocks dismiss/action.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter="ReportAbuseTest|ReportAbuseThrottleTest|AbuseReportWorkflowTest" --no-coverage</automated>
  </verify>
  <done>Controller + FormRequest + Filament resource + Vue page + component shipped; 3 Pest tests GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual rate-limit + abuse-flow boundary smoke</name>
  <what-built>
    4 rate limiters live + abuse-report flow end-to-end:
    - public-api 30/min by IP
    - auth 10/min by IP
    - notifications-read 120/min by user
    - report-abuse 5/hour by user
    - POST /reports creates AbuseReport + activity_log
    - Filament AbuseReportResource (Moderation group) with dismiss + action_with_ban transitions
  </what-built>
  <how-to-verify>
1. Start the stack via `make up`.
2. Public-API throttle: from a single host:
   - `for i in $(seq 1 31); do curl -s -o /dev/null -w "%{http_code}\n" http://localhost/clans.json; done`
   - First 30 should be 200; 31st should be 429.
3. Auth throttle: hammer /auth/discord/callback 11 times in 1 minute — 11th request returns 429 (without actually completing OAuth, since 429 fires before redirect).
4. Report-abuse throttle: auth as a test user; POST /reports 5 times via Inertia form within an hour; 6th returns 429.
5. Filament:
   - Auth as admin (or moderator with view-reports + manage-reports).
   - Open /admin/abuse-reports.
   - Verify list shows pending reports from step 4.
   - Open one report, click Dismiss action, fill review_notes, submit. Verify status flips and activity_log shows the action.
   - Open another report, click action_with_ban — verify ban form appears, fill, submit. Verify both AbuseReport.status='actioned' AND new Ban row in DB.
6. Verify non-moderator user cannot access /admin/abuse-reports (403 or redirect).
  </how-to-verify>
  <resume-signal>Type "approved" if every step passes, or describe failures with the relevant step number and observed behaviour.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous->public JSON | IP-keyed throttle is the only gate; absorbed by Cache + Redis-backed limiter |
| auth user->/reports POST | auth + per-user throttle + FormRequest validation |
| moderator->Filament dismiss/action | permission gate + audit log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11-01 | D (DoS) | Mass scraping of /clans.json /players.json | mitigate | public-api throttle (30/min by IP); cache absorbs repeat reads |
| T-09-11-02 | S (Spoofing) | X-Forwarded-For injection bypassing IP-keyed throttle | mitigate | Laravel TrustProxies middleware (existing Phase 1) sets request IP from trusted upstream; Railway terminates TLS |
| T-09-11-03 | T (Tampering) | Report-abuse storm targeting one user | mitigate | report-abuse throttle (5/hour by reporter user_id); body kept for moderator review |
| T-09-11-04 | E (Elevation of Privilege) | Non-moderator hitting /admin/abuse-reports | mitigate | Spatie permission gate + Filament canViewAny + action ->visible per-permission |
| T-09-11-05 | I (Information Disclosure) | Report body contains victim's PII | accept | Reports visible only to moderators (gated by permission); v1 trusts moderator handling — future GDPR work in OPS-V2-01 |
| T-09-11-06 | R (Repudiation) | Moderator denies dismissing a report | mitigate | activity_log row per dismissal (causer + subject + properties); CLAUDE.md s6 append-only |
| T-09-11-07 | D (DoS) | Auth throttle blocks legitimate user after typo | accept | 10/min by IP is generous; user can wait or try different IP |
</threat_model>

<verification>
- 4 RateLimiters defined; existing 3 preserved.
- Throttle middleware attached to all relevant routes.
- 5 Pest tests GREEN.
- Manual checkpoint approved.
- Existing tests stay GREEN.
- PHPStan L8 + Pint clean.
</verification>

<success_criteria>
- SC-5 rate limiting matrix complete + tested.
- SC-5 abuse-report flow end-to-end functional + audited.
- Permission gates enforced on every moderator action.
- Pitfall 11 not regressed (admin still gated; no axe scan crossing).
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish/09-11-SUMMARY.md` capturing: limiter registry table with exact values, route middleware diff, abuse-report state-machine notes, manual boundary smoke log.
</output>
