---
phase: 02-clans-tags
plan: 14
type: execute
wave: 6
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13]
files_modified:
  - apps/web/lang/en/clans.php
  - apps/web/lang/en/players.php
  - apps/web/lang/en/admin.php
  - apps/web/lang/en/common.php
  - apps/web/resources/js/types/api.d.ts
  - apps/web/packages/shared-types/src/index.ts
  - apps/web/tests/Feature/Clans/ClanMembershipUniqueTest.php
  - .planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md
autonomous: true
requirements:
  - REQ-tenancy-single-guild
  - REQ-constraint-single-guild
  - REQ-tenancy-multi-clan
  - REQ-goal-public-profiles
must_haves:
  truths:
    - "Final i18n keys audit: every t()/__() reference in plans 02-07 through 02-13 has a matching key in lang/en/*.php"
    - "TypeScript types regenerated and synced to packages/shared-types"
    - "ClanMembershipUniqueTest (final Wave-0 stub) replaced with full D-009 partial-index coverage"
    - "Full suite GREEN: pest, phpstan, pint, NoHardcodedStringsTest, NoHardcoded i18n keys"
    - "02-PHASE-VERIFICATION.md committed mapping each ROADMAP SC and each REQ to the test that proves it"
    - "Phase 2 ROADMAP entry's plan list and goal are populated correctly"
  artifacts:
    - path: ".planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md"
      provides: "Phase close artefact with M1..M5 traceability"
      contains: "SC-1"
    - path: "apps/web/resources/js/types/api.d.ts"
      provides: "Phase-2 DTOs in TypeScript"
      contains: "ClanData"
  key_links:
    - from: "PHASE-VERIFICATION SC-1"
      to: "tests/Feature/Clans/PublicClanRoutesTest"
      via: "test name reference"
      pattern: "PublicClanRoutesTest"
    - from: "ROADMAP.md Phase 2 Plans list"
      to: "14 plan files in 02-clans-tags/"
      via: "ROADMAP plan checklist"
      pattern: "02-01-PLAN.md"
---

<objective>
Phase-close work: catch loose ends (final test stub, ts type regen, lang keys), run the full quality gate sweep, write the phase verification report mapping every ROADMAP SC + REQ to a passing test, and update ROADMAP.md to reflect Phase 2 completion.

Purpose: Mirror Phase 1's `01-18-PLAN.md` closure pattern. This is the human-readable proof that Phase 2 shipped.

Output: 1 new test, regenerated TS types, lang key audit, PHASE-VERIFICATION.md, ROADMAP.md update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-clans-tags/02-RESEARCH.md
@.planning/phases/02-clans-tags/02-VALIDATION.md
@.planning/phases/01-foundations/01-18-PLAN.md

<interfaces>
The "final stub" mapping per 02-VALIDATION.md:
- ClanMembershipUniqueTest is the LAST Wave-0 stub not yet replaced (plan 02-03 replaced ClanModelTest + ClanMembershipModelTest but the VALIDATION map specifically calls out a DEDICATED "Active membership" test row in addition; if plan 02-03 already covered all D-009 assertions, this becomes a leaner targeted test verifying the SAME constraint at the integration boundary — i.e. it tests the constraint via a controller route rather than the model factory).

ROADMAP.md update procedure (per planner step `update_roadmap`):
- Find "### Phase 2: Clans & tags"
- Replace "**Plans**: TBD" with "**Plans**: 14 plans"
- Add the 14 plan checklist `- [ ] 02-NN-PLAN.md — short description` entries
- Goal line is already populated; verify it reads "Stand up clans as first-class entities…"

The phase-close PHASE-VERIFICATION.md follows the Phase-1 template at `.planning/phases/01-foundations/PHASE-VERIFICATION.md` (if exists) or a reasonable approximation:
- Header with phase metadata
- ROADMAP success criteria mapping (SC-1..SC-5 -> test files)
- Requirements mapping (REQ-* -> test files)
- Pest summary (counts)
- Pint + PHPStan status
- Manual smokes (deferred to operator) list
- Sign-off line
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase verification report to ROADMAP source-of-truth | Verification claims must be backed by passing tests; no false claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11-01 | Repudiation | PHASE-VERIFICATION claims unsupported | mitigate | Each SC + REQ row in the verification table cites the exact test file + command; reviewer can rerun |
| T-02-11-02 | Information Disclosure | api.d.ts leaking unstable types | accept | Generated artefact; committed for cross-package consumption per D-020 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace final Wave-0 stub ClanMembershipUniqueTest + regenerate TypeScript types</name>
  <files>
    apps/web/tests/Feature/Clans/ClanMembershipUniqueTest.php,
    apps/web/resources/js/types/api.d.ts,
    apps/web/packages/shared-types/src/index.ts
  </files>
  <read_first>
    - apps/web/tests/Feature/Models/ClanMembershipModelTest.php (from plan 02-03 — overlap check; this test must cover DIFFERENT facets, not duplicate)
    - apps/web/tests/Feature/Clans/MyClanManagementTest.php (from plan 02-09 — integration-level membership tests)
    - .planning/phases/02-clans-tags/02-VALIDATION.md (table row "Active membership")
    - apps/web/packages/shared-types/scripts/sync-types.sh (if exists — Phase 1 plan 15 wired this)
  </read_first>
  <acceptance_criteria>
    - `ClanMembershipUniqueTest.php` covers D-009 from the INTEGRATION layer (not just model-level which plan 02-03 covered):
      - it('rejects concurrent dual-membership-creation race via DB partial unique index')
        - Setup: User, two clans, one ClanInvite for each clan, both pending
        - Action: accept both invites within a single DB::transaction wrapper (mocking concurrent dispatch)
        - Assertion: only one ClanMembership exists; second insert raises QueryException
      - it('membership history persists when player leaves a clan and joins another')
        - Setup: User joins clan A; leaves (left_at=now()); joins clan B
        - Assertion: ClanMembership::count() === 2, exactly 1 has whereNull('left_at')
      - it('partial unique index survives migrate:fresh cycle')
        - Run artisan migrate:fresh inside the test; reapply the assertion; ensures the migration is durable
    - `make artisan ARGS="typescript:transform"` regenerates `resources/js/types/api.d.ts` with all Phase-2 DTOs present (ClanData, ClanTagData, ClanMembershipData, ClanInviteData, ClanApplicationData, PublicPlayerData)
    - `apps/web/packages/shared-types/src/index.ts` updated via Phase 1's sync-types script (or manual export aliases per Phase 1's `01-15-PLAN.md` summary; "uses '/// <reference path>' + type aliases ('export type UserData = App.Data.UserData')")
    - `make pest ARGS="--filter=ClanMembershipUnique"` GREEN
    - `make pnpm ARGS="-F shared-types check"` GREEN (Phase-1 CI gate)
  </acceptance_criteria>
  <action>
    Build the Pest test — focus on integration scenarios that plan 02-03's unit-level test didn't cover. The "concurrent dual-membership-creation race" via single DB::transaction is the most valuable assertion.

    For TS type regen:
    ```
    make artisan ARGS="typescript:transform"
    ```
    Then ensure shared-types/src/index.ts has the new type aliases — read the Phase-1 plan 15 summary's pattern (`/// <reference path>` + `export type X = App.Data.X`). Add aliases for ClanData, ClanTagData, ClanMembershipData, ClanInviteData, ClanApplicationData, PublicPlayerData.

    Run shared-types check.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Clans/ClanMembershipUniqueTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:.*passed)' && grep -E '(ClanData|ClanTagData|ClanMembershipData|PublicPlayerData)' apps/web/resources/js/types/api.d.ts | wc -l</automated>
  </verify>
  <done>Last Wave 0 stub replaced; TS types synced; shared-types check green.</done>
</task>

<task type="auto">
  <name>Task 2: i18n key audit + missing-key sweep across lang/en/{clans,players,common,admin}.php</name>
  <files>
    apps/web/lang/en/clans.php,
    apps/web/lang/en/players.php,
    apps/web/lang/en/admin.php,
    apps/web/lang/en/common.php
  </files>
  <read_first>
    - apps/web/lang/en/clans.php (from plan 02-06; possibly amended by later plans)
    - apps/web/lang/en/players.php (from plan 02-06)
    - apps/web/lang/en/admin.php (from plan 02-06 + plan 02-13 if Rule 2 amendment landed)
    - apps/web/lang/en/common.php (from plan 02-06 + plan 02-08 if any key added for the disabled tooltip)
    - apps/web/resources/js/pages/Clans/Index.vue + Show.vue + Players/Show.vue + MyClan/Index.vue (audit every t('...') call against lang files)
    - apps/web/resources/js/components/clans/* and components/UserMenu.vue (audit components)
    - apps/web/app/Filament/Resources/*.php (audit __('...') calls)
  </read_first>
  <acceptance_criteria>
    - Run `grep -rohE "t\('([a-z._]+)'" apps/web/resources/js | sort -u` and audit each key exists in lang/en/*.php
    - Run `grep -rohE "__\('([a-z._]+)'" apps/web/app | sort -u` and audit similarly
    - Any missing keys added with sensible English copy
    - NoHardcodedStringsTest GREEN
    - `make pest ARGS="--filter='I18n'"` GREEN
  </acceptance_criteria>
  <action>
    Two-step audit:
    1. Extract all t() and __() keys used in Vue/PHP under apps/web (from Wave 3 + Wave 4 + Wave 5 plans).
    2. Diff against `array_keys(array_merge_recursive(...))` of the 4 lang files (flat dot-keyed).

    For any key in the code that is missing in lang: add the EN copy. Use UI-SPEC § Copywriting Contract as the canonical source for visible strings; for technical messages (e.g. error keys like `clans.invites.error.not_pending`) write a concise English sentence.

    The `clans.create.success` flash key used in plan 02-09 needs to exist — verify in clans.php; add if missing: `'create' => ['success' => 'Your clan has been created.', 'cta' => 'Create your clan']`.

    Run NoHardcodedStringsTest and the rest of I18n test suite.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter='I18n' --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:.*passed)'</automated>
  </verify>
  <done>All keys present; I18n test suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Run full quality gate suite + write 02-PHASE-VERIFICATION.md + update ROADMAP.md</name>
  <files>
    .planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    - .planning/phases/01-foundations/01-18-PLAN.md (Phase 1 close pattern)
    - .planning/phases/01-foundations/PHASE-VERIFICATION.md (if exists)
    - .planning/ROADMAP.md (current Phase 2 entry — find "### Phase 2: Clans & tags")
    - .planning/REQUIREMENTS.md (REQ-* IDs and descriptions)
  </read_first>
  <acceptance_criteria>
    - Full quality gate sweep:
      - `make pest` -> all GREEN (Phase 1 + Phase 2 tests)
      - `make pint ARGS="--test"` -> GREEN
      - `make phpstan` -> no errors
      - `make pnpm ARGS="-F web run typecheck"` -> clean
      - `make pnpm ARGS="-F shared-types check"` -> clean
    - `02-PHASE-VERIFICATION.md` written with sections:
      - Phase metadata (number, name, completed date, plans count)
      - ROADMAP Success Criteria mapping table:
        | SC | Description | Test file | Pest filter |
        | SC-1 | Public clan directory + detail without auth | tests/Feature/Clans/PublicClanRoutesTest.php + ClanDirectoryTest.php + ClanShowTest.php | --filter='Clans/(PublicClanRoutes\|ClanDirectory\|ClanShow)' |
        | SC-2 | Player profile with privacy enforcement | tests/Feature/Clans/PlayerProfilePrivacyTest.php + tests/Unit/Services/PlayerPrivacyGateTest.php + tests/Unit/Data/PlayerProfileDataTest.php | --filter='(PlayerProfilePrivacy\|PlayerPrivacyGate\|PlayerProfileData)' |
        | SC-3 | Leader/Officer manage clan + audit | tests/Feature/Clans/MyClanManagementTest.php + ClanInviteTest.php + ClanApplicationTest.php | --filter='(MyClanManagement\|ClanInvite\|ClanApplication)' |
        | SC-4 | discord_guild single row + role-id-only on clans | tests/Feature/Clans/DiscordGuildSingleRowTest.php + DiscordGuildSeederTest.php + ClanFilamentResourceTest.php | --filter='DiscordGuild' |
        | SC-5 | One active ClanMembership; history preserved | tests/Feature/Models/ClanMembershipModelTest.php + tests/Feature/Clans/ClanMembershipUniqueTest.php | --filter='ClanMembership' |
      - REQ-* mapping (4 rows: REQ-tenancy-single-guild, REQ-constraint-single-guild, REQ-tenancy-multi-clan, REQ-goal-public-profiles)
      - Pest results summary (count tests pass/total)
      - Pint + PHPStan + NoHardcodedStringsTest status
      - Manual smokes deferred to operator (visit /clans logged-out, /clans/{slug}, /players/{slug}, log in, /my-clan, create a clan, invite a member, etc.)
      - Sign-off line: "Phase 2 verified complete — pending operator manual smoke; ROADMAP.md updated"
    - `ROADMAP.md` updated:
      - Phase 2 entry: `**Plans**: 14 plans`
      - 14-item plan checklist under Phase 2 listing each `- [ ] 02-NN-PLAN.md — short description` (the [ ] checkboxes will be ticked off as plans execute; planner leaves them unchecked for executors to tick)
      - Goal line preserved verbatim (no need to change)
      - Progress table update at bottom: Phase 2 row updated to `2. Clans & tags | 0/14 | Not started | -` (executor updates as plans complete)
  </acceptance_criteria>
  <action>
    Run `make pest`, `make pint --test`, `make phpstan`, `make pnpm -F web run typecheck`, `make pnpm -F shared-types check`. If anything is red, surface the failure and route back to the responsible plan for fix-up.

    Write 02-PHASE-VERIFICATION.md per the structure in acceptance criteria. Each SC row's "Test file" and "Pest filter" columns must accurately point at extant files (verified by `ls`).

    Update ROADMAP.md: open the file, find the Phase 2 section, replace `**Plans**: TBD` with `**Plans**: 14 plans` followed by the 14 plan list:
    ```
    - [ ] 02-01-PLAN.md — Wave 0 scaffolding (composer install + test stubs + factory stubs)
    - [ ] 02-02-PLAN.md — Migrations (7 tables incl. partial unique index)
    - [ ] 02-03-PLAN.md — Models (6 new + Player HasTranslations + factories) + 2 model tests
    - [ ] 02-04-PLAN.md — Seeders (DiscordGuild + ClanTag) + 2 single-row tests
    - [ ] 02-05-PLAN.md — Privacy gate service + DTOs + 2 unit tests
    - [ ] 02-06-PLAN.md — Slug generator + i18n key files + 8 Vue components
    - [ ] 02-07-PLAN.md — Public controllers + routes + 4 feature tests
    - [ ] 02-08-PLAN.md — Vue pages (Clans Index/Show + Players Show) + UserMenu + PublicLayout nav
    - [ ] 02-09-PLAN.md — My Clan controllers + Policies + MyClanManagementTest
    - [ ] 02-10-PLAN.md — ClanInviteService + controller + ClanInviteTest
    - [ ] 02-11-PLAN.md — ClanApplicationService + MyClan/Index.vue + ClanApplicationTest
    - [ ] 02-12-PLAN.md — ClanResource + ClanTagResource (Filament) + ClanResourcesPresentTest
    - [ ] 02-13-PLAN.md — Remaining Filament resources (Membership/Invite/Application/DiscordGuild) + ClanFilamentResourceTest
    - [ ] 02-14-PLAN.md — [BLOCKING] phase verification + ROADMAP update + final quality gates
    ```
    Update the progress table row at the bottom.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | grep -E 'Tests:.*passed' | tail -1 && docker compose exec web ./vendor/bin/pint --test 2>&1 | grep -E '(PASS|FAIL)' | tail -3 && docker compose exec web ./vendor/bin/phpstan analyse --no-progress 2>&1 | tail -3</automated>
  </verify>
  <done>All quality gates GREEN; PHASE-VERIFICATION.md written; ROADMAP.md updated.</done>
</task>

</tasks>

<verification>
- `make pest` -> ALL GREEN (Phase 1 + Phase 2)
- `make pint ARGS="--test"` -> GREEN
- `make phpstan` -> no errors
- `make pnpm ARGS="-F web run typecheck"` -> clean
- `make pnpm ARGS="-F shared-types check"` -> clean
- ROADMAP.md Phase 2 entry shows 14 plans
- 02-PHASE-VERIFICATION.md exists and maps every SC + REQ to a passing test
</verification>

<success_criteria>
Phase 2 closed cleanly. The next phase (Phase 3 — Games & match types) has a clean slate to build on. Operator should run the manual smokes documented in 02-PHASE-VERIFICATION.md before declaring Phase 2 truly shipped.
</success_criteria>

<output>
After completion, create `.planning/phases/02-clans-tags/02-14-SUMMARY.md` documenting:
- Final test count (Phase 1 + Phase 2 combined)
- Quality gate snapshot
- Manual smoke list
- ROADMAP delta
</output>
