---
phase: 08-rcon-automation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - apps/web/database/migrations/2026_05_16_100000_create_match_servers_table.php
  - apps/web/database/migrations/2026_05_16_100100_create_match_server_bookings_table.php
  - apps/web/database/migrations/2026_05_16_100200_create_match_player_stats_table.php
  - apps/web/database/migrations/2026_05_16_100300_create_match_events_table.php
  - apps/web/database/migrations/2026_05_16_100400_add_source_and_manual_entry_required_to_matches_and_results.php
  - apps/web/database/migrations/2026_05_16_100500_extend_discord_outbound_message_types_for_match_result_announce.php
autonomous: true
requirements:
  - REQ-goal-rcon-history
  - REQ-constraint-league-owns-servers
  - REQ-success-end-to-end-scrim

must_haves:
  truths:
    - "`match_servers` table exists with `credentials_encrypted` jsonb column (encrypted:array casts succeed roundtrip in plan 08-03)."
    - "`match_server_bookings` table exists with `btree_gist` extension enabled and `EXCLUDE USING gist (server_id WITH =, tstzrange(reserved_from, reserved_to, '[)') WITH &&) WHERE (status = 'active')` constraint preventing overlapping active bookings on the same server."
    - "`match_events` table exists with unique `(match_id, crcon_stream_id)` for idempotent re-ingestion + CHECK on canonical event_type enum."
    - "`match_player_stats` table exists with unique `(match_id, player_id)` for idempotent aggregator upsert + non-negative CHECK constraints."
    - "`match_results` has new column `source text DEFAULT 'manual'` with CHECK in ('manual','rcon')."
    - "`matches` has new column `manual_entry_required boolean DEFAULT false` — flipped true on RCON failure paths (D-019)."
    - "`discord_outbound_messages.message_type` CHECK constraint accepts `match_result_announce`."
  artifacts:
    - path: "apps/web/database/migrations/2026_05_16_100100_create_match_server_bookings_table.php"
      provides: "match_server_bookings table with EXCLUDE USING gist on tstzrange — Pitfall 7 btree_gist extension MUST be enabled inside this migration"
      contains: "CREATE EXTENSION IF NOT EXISTS btree_gist"
    - path: "apps/web/database/migrations/2026_05_16_100300_create_match_events_table.php"
      provides: "match_events with idempotent (match_id, crcon_stream_id) UNIQUE + 10-value event_type CHECK"
      contains: "match_events_type_check"
    - path: "apps/web/database/migrations/2026_05_16_100400_add_source_and_manual_entry_required_to_matches_and_results.php"
      provides: "match_results.source enum + matches.manual_entry_required flag (RESEARCH Gap A1 — Phase 4 did NOT ship the source column despite CONTEXT.md claim)"
      contains: "match_results_source_check"
  key_links:
    - from: "match_server_bookings.server_id"
      to: "match_servers.id"
      via: "restrictOnDelete"
      pattern: "restrictOnDelete"
    - from: "match_events.match_id"
      to: "matches.id"
      via: "cascadeOnDelete (RESEARCH cascade matrix — events follow match lifecycle)"
      pattern: "cascadeOnDelete"
    - from: "match_player_stats.player_id"
      to: "players.id"
      via: "restrictOnDelete"
      pattern: "restrictOnDelete"
---

<objective>
Add 6 migrations laying down the Phase 8 schema floor: match_servers, match_server_bookings (with btree_gist EXCLUDE), match_events, match_player_stats, the missing match_results.source column + matches.manual_entry_required (RESEARCH Gap A1), and the discord_outbound message_type CHECK extension for `match_result_announce`.

Purpose: All Phase 8 services run against this schema. Without the EXCLUDE constraint the booking overlap test cannot turn GREEN; without the source enum the manual-override-wins invariant cannot be enforced; without the idempotency UNIQUEs the worker reconnect path silently double-writes.

Output: Six migration files plus passing schema-level Pest tests for EXCLUDE and CHECK constraints. No models / factories yet — those land in 08-03 / 08-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-rcon-automation/08-CONTEXT.md
@.planning/phases/08-rcon-automation/08-RESEARCH.md
@.planning/phases/08-rcon-automation/08-VALIDATION.md
@apps/web/database/migrations/2026_05_14_100300_create_match_results_table.php
@apps/web/database/migrations/2026_05_14_100000_create_matches_table.php

<interfaces>
<!-- Canonical idioms reused from prior phases — do NOT invent new patterns. -->

Postgres extension-in-migration idiom (Phase 1 plan 01-04 / Pitfall 5):
```php
DB::statement('CREATE EXTENSION IF NOT EXISTS btree_gist;');
```

Partial-unique idiom (Phase 2 D-009):
```php
DB::statement('CREATE UNIQUE INDEX ... ON ... (...) WHERE ...');
```
Schema builder cannot express WHERE clauses — use raw DB::statement.

Composite UNIQUE for idempotency (Phase 3 03-02 — gmtrl_match_type_role_unique pattern):
```php
$t->unique(['match_id', 'crcon_stream_id'], 'match_events_match_stream_unique');
```
Short index name to fit Postgres 63-byte limit (Pitfall 1 from Phase 3).

UUID PK + default (Phase 1 standard):
```php
$t->uuid('id')->primary();
DB::statement('ALTER TABLE <name> ALTER COLUMN id SET DEFAULT gen_random_uuid();');
```

ALTER TABLE for source enum on existing match_results:
```php
Schema::table('match_results', function (Blueprint $t) {
    $t->text('source')->default('manual')->after('axis_score');
});
DB::statement("ALTER TABLE match_results ADD CONSTRAINT match_results_source_check
               CHECK (source IN ('manual','rcon'));");
```

discord_outbound CHECK extension idiom (Phase 5 / 6 / 7 precedent):
```php
DB::statement('ALTER TABLE discord_outbound_messages DROP CONSTRAINT discord_outbound_messages_message_type_check;');
DB::statement("ALTER TABLE discord_outbound_messages ADD CONSTRAINT discord_outbound_messages_message_type_check
               CHECK (message_type IN ('clan_announce','match_announce','signup_open','signup_reminder',
                                       'tournament_announce','article_announce','match_result_announce'));");
```

down() must reverse the EXCLUDE constraint AND drop the btree_gist extension only if no other consumer (do NOT drop the extension — leave for safety).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create match_servers + match_server_bookings + match_events + match_player_stats migrations</name>
  <files>apps/web/database/migrations/2026_05_16_100000_create_match_servers_table.php, apps/web/database/migrations/2026_05_16_100100_create_match_server_bookings_table.php, apps/web/database/migrations/2026_05_16_100200_create_match_player_stats_table.php, apps/web/database/migrations/2026_05_16_100300_create_match_events_table.php</files>
  <behavior>
    - `match_servers`: uuid PK + gen_random_uuid default, `name text NOT NULL`, `host text NOT NULL`, `port_rcon integer NOT NULL`, `region text NULL`, `credentials_encrypted jsonb NOT NULL` (encrypted at app layer in plan 08-03 model cast), `is_active boolean DEFAULT true`, `last_test_at timestamptz NULL`, `last_test_status text NULL` with CHECK in ('ok','error'), `last_test_error text NULL`, `timestamps()`.
    - `match_server_bookings`: uuid PK + gen_random_uuid default, `match_id uuid` FK matches.id cascadeOnDelete, `server_id uuid` FK match_servers.id restrictOnDelete, `reserved_from timestamptz NOT NULL`, `reserved_to timestamptz NOT NULL`, `status text DEFAULT 'active'` with CHECK in ('active','cancelled','completed'), `timestamps()`. **`CREATE EXTENSION IF NOT EXISTS btree_gist;` MUST run BEFORE the EXCLUDE statement (Pitfall 7).** EXCLUDE: `EXCLUDE USING gist (server_id WITH =, tstzrange(reserved_from, reserved_to, '[)') WITH &&) WHERE (status = 'active')`. Half-open interval — back-to-back bookings sharing an endpoint do NOT conflict.
    - `match_events`: uuid PK + gen_random_uuid default, `match_id uuid` FK matches.id cascadeOnDelete, `event_type text NOT NULL` with CHECK in ('game_start','round_start','player_kill','player_team_kill','player_connect','player_disconnect','team_switch','round_end','match_end','manual_error'), `crcon_action text NULL`, `crcon_stream_id text NULL`, `payload jsonb NOT NULL`, `occurred_at timestamptz NOT NULL`, `ingested_at timestamptz DEFAULT now()`, composite UNIQUE `(match_id, crcon_stream_id)` named `match_events_match_stream_unique`, index `(match_id, event_type, occurred_at)` for aggregator query.
    - `match_player_stats`: uuid PK + gen_random_uuid default, `match_id uuid` FK matches.id cascadeOnDelete, `player_id uuid` FK players.id restrictOnDelete, `kills/deaths/team_kills/score integer DEFAULT 0`, `role_played text NULL`, `weapons_used jsonb NULL`, `timestamps()`, composite UNIQUE `(match_id, player_id)` for idempotent aggregator upsert (Pitfall 4 — aggregator runs ONCE on match_end, NOT per event), CHECK `kills>=0 AND deaths>=0 AND team_kills>=0 AND score>=0`.
  </behavior>
  <action>Author all four migrations following the prior-phase idiom verbatim (UUID PK default via DB::statement, FK matrix per RESEARCH Pattern 1 / Phase 4 cascade matrix, EXCLUDE constraint with btree_gist created in the same migration as Phase 1 Pitfall 5 pattern). Run `make artisan ARGS="migrate:fresh --seed"` to verify the chain applies clean on a fresh DB. Then run a one-off SQL probe to assert the EXCLUDE constraint exists: `docker compose exec postgres psql -U trenchwars -d trenchwars -c "\\d match_server_bookings" | grep -i exclude` returns the constraint.</action>
  <verify>
    <automated>make artisan ARGS="migrate:fresh --seed" 2>&amp;1 | grep -qE 'Migrated|migrated' &amp;&amp; docker compose exec -T postgres psql -U trenchwars -d trenchwars -tA -c "SELECT 1 FROM pg_extension WHERE extname='btree_gist';" | grep -q 1 &amp;&amp; docker compose exec -T postgres psql -U trenchwars -d trenchwars -tA -c "SELECT count(*) FROM information_schema.tables WHERE table_name IN ('match_servers','match_server_bookings','match_events','match_player_stats');" | grep -q '^4$'</automated>
  </verify>
  <done>Four tables exist, btree_gist extension installed by THIS migration (not the Docker image), EXCLUDE constraint visible in `\d match_server_bookings`, both composite UNIQUEs visible, every CHECK constraint accepted by Postgres.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add source enum + manual_entry_required + extend discord_outbound CHECK</name>
  <files>apps/web/database/migrations/2026_05_16_100400_add_source_and_manual_entry_required_to_matches_and_results.php, apps/web/database/migrations/2026_05_16_100500_extend_discord_outbound_message_types_for_match_result_announce.php</files>
  <behavior>
    - `match_results.source text DEFAULT 'manual'` ADDED via ALTER TABLE (RESEARCH Gap A1 — Phase 4 migration verified by file inspection does NOT ship this column; CONTEXT.md claim was wrong); CHECK in ('manual','rcon') named `match_results_source_check`.
    - `matches.manual_entry_required boolean DEFAULT false` ADDED via ALTER TABLE; index on `(manual_entry_required) WHERE manual_entry_required = true` (partial index — only flagged rows queried).
    - discord_outbound CHECK constraint dropped + recreated with `match_result_announce` appended to the existing 6-value enum (current: clan_announce, match_announce, signup_open, signup_reminder, tournament_announce, article_announce).
    - down() reverses cleanly (drop CHECK, drop column, restore old CHECK).
  </behavior>
  <action>Author the two ALTER migrations following the canonical Phase 5/6/7 extend-CHECK idiom (DROP CONSTRAINT then ADD CONSTRAINT with full new IN-list, never CONCAT). Verify by running `make artisan ARGS="migrate:fresh --seed"` and probing the constraints with psql `\d match_results` (expect `source` column + check), `\d matches` (expect `manual_entry_required` column + partial index), `\d discord_outbound_messages` (expect new CHECK list).</action>
  <verify>
    <automated>make artisan ARGS="migrate:fresh --seed" &amp;&amp; docker compose exec -T postgres psql -U trenchwars -d trenchwars -tA -c "SELECT column_default FROM information_schema.columns WHERE table_name='match_results' AND column_name='source';" | grep -q manual &amp;&amp; docker compose exec -T postgres psql -U trenchwars -d trenchwars -tA -c "SELECT column_default FROM information_schema.columns WHERE table_name='matches' AND column_name='manual_entry_required';" | grep -qE 'false|0' &amp;&amp; docker compose exec -T postgres psql -U trenchwars -d trenchwars -tA -c "SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='discord_outbound_messages_message_type_check';" | grep -q match_result_announce</automated>
  </verify>
  <done>match_results.source column visible with default 'manual'; matches.manual_entry_required column visible with default false; discord_outbound CHECK includes match_result_announce; all Phase 1-7 tests still pass (`make pest ARGS="--exclude=Phase8"` green; RefreshDatabase rebuilds schema).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application→Postgres | Constraints are the last line of defence; the EXCLUDE constraint is the integrity gate for booking conflicts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | match_server_bookings overlap | mitigate | Postgres EXCLUDE USING gist with `WHERE status='active'` predicate — DB-tier guarantee per RESEARCH Pattern (Phase 2 D-009 partial UNIQUE idiom mirror). |
| T-08-02-02 | Tampering | RCON late delivery overwriting manual result | mitigate | `match_results.source` enum + CHECK in ('manual','rcon'); plan 08-08 service guard refuses to overwrite source='manual'. |
| T-08-02-03 | Tampering | match_events double-ingest on worker reconnect | mitigate | UNIQUE (match_id, crcon_stream_id) — second INSERT collides; ingest service treats UNIQUE violation as idempotent no-op (plan 08-07). |
| T-08-02-04 | Information disclosure | credentials_encrypted column dumped in plaintext | mitigate | Column is jsonb but written exclusively via Laravel `encrypted:array` cast (plan 08-03 model); raw value is ciphertext envelope under APP_KEY. |
</threat_model>

<verification>
- `make artisan ARGS="migrate:fresh --seed"` — chain applies clean.
- All Phase 1-7 tests still pass under fresh schema (RefreshDatabase regression check).
- `docker compose exec postgres psql -U trenchwars -d trenchwars -c "\d match_server_bookings"` shows the EXCLUDE constraint.
- pg_extension shows btree_gist installed.
</verification>

<success_criteria>
- 6 migrations exist and apply cleanly in order.
- EXCLUDE constraint visible on match_server_bookings; btree_gist installed by THIS migration not the image.
- match_results.source column present with default 'manual' (Gap A1 closed).
- matches.manual_entry_required column present with default false.
- discord_outbound_messages CHECK includes match_result_announce.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rcon-automation/08-02-SUMMARY.md`
</output>
