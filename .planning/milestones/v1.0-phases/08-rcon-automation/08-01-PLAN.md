---
phase: 08-rcon-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/tests/Feature/Phase8/RconMatchResultIngestionTest.php
  - apps/web/tests/Feature/Phase8/MatchPlayerStatAggregatorTest.php
  - apps/web/tests/Feature/Phase8/MatchEventIdempotencyTest.php
  - apps/web/tests/Feature/Phase8/MatchServerCredentialEncryptionTest.php
  - apps/web/tests/Feature/Phase8/MatchServerBookingOverlapTest.php
  - apps/web/tests/Feature/Phase8/ScrimE2EHappyPathTest.php
  - apps/web/tests/Feature/Phase8/VerifyRconSignatureTest.php
  - apps/web/tests/Feature/Phase8/ManualOverrideWinsTest.php
  - apps/web/tests/Feature/Phase8/RconUnreachableFlagsManualTest.php
  - apps/web/tests/Feature/Phase8/MatchEventNormaliserContractTest.php
  - apps/web/database/factories/MatchServerFactory.php
  - apps/web/database/factories/MatchServerBookingFactory.php
  - apps/web/database/factories/MatchEventFactory.php
  - apps/web/database/factories/MatchPlayerStatFactory.php
  - apps/web/lang/en/rcon.php
  - apps/web/lang/en/admin.php
  - apps/rcon-worker/package.json
  - apps/rcon-worker/src/index.ts
  - apps/rcon-worker/src/config.ts
  - apps/rcon-worker/src/logging/logger.ts
  - apps/rcon-worker/src/ingest/HmacSigner.ts
  - apps/rcon-worker/src/crcon/CrconEventNormaliser.ts
  - apps/rcon-worker/tests/unit/HmacSigner.test.ts
  - apps/rcon-worker/tests/unit/CrconEventNormaliser.test.ts
  - apps/rcon-worker/tests/integration/CrconClient.integration.test.ts
  - pnpm-lock.yaml
autonomous: true
requirements:
  - REQ-goal-rcon-history
  - REQ-constraint-league-owns-servers
  - REQ-success-end-to-end-scrim

must_haves:
  truths:
    - "Pest RED stubs exist for all 9 Phase 8 web tests and assert intent of REQ-goal-rcon-history, REQ-constraint-league-owns-servers, REQ-success-end-to-end-scrim."
    - "Vitest RED stubs exist for HmacSigner, CrconEventNormaliser, CrconClient.integration in apps/rcon-worker/tests."
    - "apps/rcon-worker has runtime deps installed: ws ^8, undici ^7 (NOT ^8 — Pitfall undici-v7 Node-22 fetch compat), ioredis ^5, pino ^9, zod ^4."
    - "apps/web/lang/en/rcon.php exists with the namespaced i18n keys this phase uses; admin.php is extended with match_servers.* keys."
    - "Factory stubs for MatchServer, MatchServerBooking, MatchEvent, MatchPlayerStat exist with @phpstan-ignore-next-line generics (D-04-01 idiom)."
  artifacts:
    - path: "apps/rcon-worker/package.json"
      provides: "Phase 8 worker runtime deps (ws/undici-v7/ioredis/pino/zod) + @types/ws devDep."
      contains: "ws"
    - path: "apps/rcon-worker/src/config.ts"
      provides: "zod env schema for WEB_HMAC_SECRET, WEB_INTERNAL_URL, NODE_ENV, POLL_INTERVAL_MS"
      exports: ["loadConfig"]
    - path: "apps/rcon-worker/src/ingest/HmacSigner.ts"
      provides: "node:crypto-based HMAC-SHA256 signer + timing-safe verify helper (STUB returning a typed shape)"
      exports: ["sign", "verify"]
    - path: "apps/rcon-worker/src/crcon/CrconEventNormaliser.ts"
      provides: "CRCON action → canonical match_event_type stub"
      exports: ["normalise"]
    - path: "apps/web/tests/Feature/Phase8/ScrimE2EHappyPathTest.php"
      provides: "SC-5 capstone RED stub"
      contains: "test('two clans complete full round-1 happy path"
    - path: "apps/web/lang/en/rcon.php"
      provides: "rcon.* i18n keys (admin, audit, errors)"
      contains: "return"
  key_links:
    - from: "apps/rcon-worker/src/ingest/HmacSigner.ts"
      to: "apps/web verification middleware (plan 08-05)"
      via: "raw-body HMAC SHA-256 over (timestamp + body)"
      pattern: "createHmac"
    - from: "apps/web/tests/Feature/Phase8/*Test.php"
      to: "plans 08-02..08-12 implementations"
      via: "RED → GREEN handover"
      pattern: "expect|->assert"
---

<objective>
Wave 0 scaffolding for Phase 8: install rcon-worker runtime deps (pnpm-workspaces, container per D-021), author all RED test stubs (Pest + Vitest) that subsequent plans turn GREEN, create factory stubs and i18n key files.

Purpose: Lock the test surface and external-dependency surface before any implementation. Aligns with Phase 4 / Phase 5 / Phase 6 Wave 0 idiom (8435020 commit) — bare Pest stubs with no `uses(...)` (autowired via tests/Pest.php), `@phpstan-ignore-next-line` per missingType.generics on factories.

Output:
- 10 Pest RED stubs under `apps/web/tests/Feature/Phase8/`
- 3 Vitest RED stubs under `apps/rcon-worker/tests/`
- 4 factory stubs under `apps/web/database/factories/`
- `apps/web/lang/en/rcon.php` (new namespace) + extended `admin.php`
- `apps/rcon-worker` deps installed (ws@^8, undici@^7, ioredis@^5, pino@^9, zod@^4, @types/ws devDep)
- `apps/rcon-worker/src/{index,config,logging/logger,ingest/HmacSigner,crcon/CrconEventNormaliser}.ts` skeletons (typed empty exports)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-rcon-automation/08-CONTEXT.md
@.planning/phases/08-rcon-automation/08-RESEARCH.md
@.planning/phases/08-rcon-automation/08-VALIDATION.md
@CLAUDE.md

<interfaces>
<!-- Carry-over from prior phases the stubs reference (no codebase exploration needed). -->

Pest 4 Wave 0 idiom (canonical Phase 5 D-05-01-C):
```
// apps/web/tests/Feature/Phase8/RconMatchResultIngestionTest.php
<?php
test('match_end CRCON event auto-populates MatchResult with source=rcon', function () {
    expect(true)->toBeFalse(); // RED — implemented by plan 08-08
});
```
No `namespace`, no `uses(TestCase::class, RefreshDatabase::class)` — both autowired via apps/web/tests/Pest.php.

Factory stub idiom (Phase 4 04-01 commit 8435020):
```
<?php
namespace Database\Factories;
use App\Models\MatchServer;
use Illuminate\Database\Eloquent\Factories\Factory;
/** @phpstan-ignore-next-line missingType.generics */
final class MatchServerFactory extends Factory
{
    /** @phpstan-ignore-next-line property.defaultValue */
    protected $model = 'App\\Models\\MatchServer';
    public function definition(): array { return []; } // plan 08-03 fleshes out
}
```

Vitest stub idiom (Phase 5 bot precedent):
```
import { describe, expect, it } from 'vitest';
describe('HmacSigner', () => {
  it('produces a stable HMAC-SHA256 over (timestamp + body)', () => {
    expect(true).toBe(false); // RED — implemented by plan 08-10
  });
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install rcon-worker runtime deps + author worker skeleton files</name>
  <files>apps/rcon-worker/package.json, apps/rcon-worker/src/index.ts, apps/rcon-worker/src/config.ts, apps/rcon-worker/src/logging/logger.ts, apps/rcon-worker/src/ingest/HmacSigner.ts, apps/rcon-worker/src/crcon/CrconEventNormaliser.ts, pnpm-lock.yaml</files>
  <behavior>
    - `docker compose exec rcon-worker pnpm add ws@^8 undici@^7 ioredis@^5 pino@^9 zod@^4` adds the runtime block (per D-021 — container, never host).
    - `docker compose exec rcon-worker pnpm add -D @types/ws` adds dev dep.
    - undici is pinned `^7` NOT `^8` (Node 22 built-in fetch compatibility — RESEARCH Standard Stack table + cited undici#3901).
    - `apps/rcon-worker/src/config.ts` exports `loadConfig()` returning a zod-validated env shape: `WEB_HMAC_SECRET` (min 32), `WEB_INTERNAL_URL` (url), `NODE_ENV`, `POLL_INTERVAL_MS` (default 30000), `REDIS_URL` (optional).
    - `apps/rcon-worker/src/ingest/HmacSigner.ts` exports `sign(secret, body, timestamp): string` and `verify(secret, body, timestamp, signature): boolean` — both stubs (throw `new Error('not implemented')`); contracts only.
    - `apps/rcon-worker/src/crcon/CrconEventNormaliser.ts` exports `normalise(crconLog: unknown): { event_type: string; payload: unknown; crcon_stream_id: string; occurred_at: string; crcon_action: string } | null` — stub (throw).
    - `apps/rcon-worker/src/logging/logger.ts` exports a `pino` instance with redact `['steam_id_64', 'player', 'victim', 'killer']` (Pitfall 9 mitigation prep).
    - `apps/rcon-worker/src/index.ts` is a minimal `console.log('rcon-worker booted')` placeholder — replaced in plan 08-11.
  </behavior>
  <action>Inside the rcon-worker container, install the five runtime deps with the exact version ranges above, install the @types/ws devDep, and author the five skeleton TypeScript files. Do NOT implement signing/normalisation/connection logic — those are owned by plans 08-10 + 08-11. Run `pnpm typecheck` to verify the files compile and `pnpm lint` to verify eslint passes. Commit `pnpm-lock.yaml` at repo root (workspace-rooted per D-015).</action>
  <verify>
    <automated>docker compose exec rcon-worker pnpm typecheck &amp;&amp; docker compose exec rcon-worker pnpm lint &amp;&amp; node -e "const p=require('./apps/rcon-worker/package.json'); for (const k of ['ws','undici','ioredis','pino','zod']) if (!p.dependencies?.[k]) { console.error('missing '+k); process.exit(1); } console.log('deps ok');"</automated>
  </verify>
  <done>Five skeleton TS files compile + lint clean; package.json shows ws/undici/ioredis/pino/zod in dependencies and @types/ws in devDependencies; pnpm-lock.yaml updated at repo root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author Pest + Vitest RED stubs + 4 factory stubs + i18n key files</name>
  <files>apps/web/tests/Feature/Phase8/RconMatchResultIngestionTest.php, apps/web/tests/Feature/Phase8/MatchPlayerStatAggregatorTest.php, apps/web/tests/Feature/Phase8/MatchEventIdempotencyTest.php, apps/web/tests/Feature/Phase8/MatchServerCredentialEncryptionTest.php, apps/web/tests/Feature/Phase8/MatchServerBookingOverlapTest.php, apps/web/tests/Feature/Phase8/ScrimE2EHappyPathTest.php, apps/web/tests/Feature/Phase8/VerifyRconSignatureTest.php, apps/web/tests/Feature/Phase8/ManualOverrideWinsTest.php, apps/web/tests/Feature/Phase8/RconUnreachableFlagsManualTest.php, apps/web/tests/Feature/Phase8/MatchEventNormaliserContractTest.php, apps/web/database/factories/MatchServerFactory.php, apps/web/database/factories/MatchServerBookingFactory.php, apps/web/database/factories/MatchEventFactory.php, apps/web/database/factories/MatchPlayerStatFactory.php, apps/web/lang/en/rcon.php, apps/web/lang/en/admin.php, apps/rcon-worker/tests/unit/HmacSigner.test.ts, apps/rcon-worker/tests/unit/CrconEventNormaliser.test.ts, apps/rcon-worker/tests/integration/CrconClient.integration.test.ts</files>
  <behavior>
    - 10 Pest RED stubs (each ends `expect(true)->toBeFalse();`), bare-form (no namespace, no uses) per D-05-01-C canonical idiom.
    - 3 Vitest RED stubs under apps/rcon-worker/tests using `expect(true).toBe(false);` per file.
    - 4 PHP factory stubs use string-FQN $model + per-file `@phpstan-ignore-next-line missingType.generics` + `@phpstan-ignore-next-line property.defaultValue` (Phase 4 D-04-01 idiom).
    - `apps/web/lang/en/rcon.php` is a new namespace returning an array tree with keys: `events.types.{game_start,round_start,player_kill,player_team_kill,player_connect,player_disconnect,team_switch,round_end,match_end,manual_error}`, `errors.{unreachable,auth_failed,permission_denied,replayed_nonce,stale_signature,bad_signature}`, `audit.{manual_override_wins,rcon_arrived_locked,test_connection_queued,test_connection_ok,test_connection_error}`.
    - `apps/web/lang/en/admin.php` is extended (NOT replaced) with `match_servers.{label,plural_label,fields.*,actions.test,fields.last_test_status.{ok,error,none}}` + `match_server_bookings.{label,plural_label,fields.*}` + `audit.match_servers.*` (extend the existing return array).
  </behavior>
  <action>Author the 17 PHP test/factory/lang files and 3 TS Vitest files exactly per the behaviour list. RED stubs must FAIL (not pass) so plan 08-13 can prove every test was author-introduced and later turned GREEN. Run `make pest ARGS="--filter=Phase8" 2>&amp;1 | grep -E 'failures|errors|Tests'` to confirm the suite runs and produces RED. Run `make phpstan` to confirm factory stubs don't break L8. Run `docker compose exec rcon-worker pnpm vitest run` to confirm Vitest stubs are RED.</action>
  <verify>
    <automated>make pest ARGS="--filter=Phase8" 2>&amp;1 | grep -qE 'FAIL|Failed|failure' &amp;&amp; make phpstan 2>&amp;1 | grep -qE 'No errors|\[OK\]' &amp;&amp; docker compose exec rcon-worker pnpm vitest run --reporter=verbose 2>&amp;1 | grep -qE 'FAIL|failed'</automated>
  </verify>
  <done>10 Pest stubs RED, 3 Vitest stubs RED, 4 factory stubs PHPStan-clean, `rcon.php` namespace loads (`make artisan ARGS="lang:list"` shows en/rcon), `admin.php` regression-free (other Phase 1-7 admin tests still pass: `make pest ARGS="--filter=Admin"` green).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host→containers | Operator runs `pnpm add` inside container (D-021); no host PHP/Node touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Tampering | `pnpm-lock.yaml` (Wave 0 lockfile commit) | mitigate | Commit lockfile change with the explicit five-dep diff visible in PR; CI verifies `pnpm install --no-frozen-lockfile` reproduces it (plan 01-16 workflow). |
| T-08-01-02 | Information disclosure | Pino logger config | mitigate | Initial redact list (`steam_id_64`, `player`, `victim`, `killer`) baked into logger.ts so plan 08-10's first commit cannot accidentally log raw payloads (Pitfall 9). |
| T-08-01-03 | Spoofing | undici version drift to v8 (Node 22 fetch incompat) | mitigate | Pin `undici@^7` literal in package.json; PR review gate is the version string. |
</threat_model>

<verification>
- `make pest ARGS="--filter=Phase8"` — 10 RED stubs present and failing.
- `docker compose exec rcon-worker pnpm test` — 3 RED Vitest stubs present and failing.
- `make phpstan` — passes (Phase 1-7 baseline preserved; new factory stubs use per-line ignores).
- `make pint ARGS="--test"` — passes.
- `docker compose exec rcon-worker pnpm typecheck` + `pnpm lint` — pass.
</verification>

<success_criteria>
- All Phase 8 test files exist as RED stubs (per VALIDATION map Wave 0 Gaps section).
- apps/rcon-worker has runtime deps installed; the four Phase 8 factories exist as stubs.
- `apps/web/lang/en/rcon.php` is a new file; `admin.php` extended without regressing Phase 1-7 admin tests.
- All quality gates remain green (Phase 1-7 baseline preserved).
</success_criteria>

<output>
After completion, create `.planning/phases/08-rcon-automation/08-01-SUMMARY.md`
</output>
