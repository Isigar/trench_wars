---
phase: 08-rcon-automation
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/web/app/Models/MatchServer.php
  - apps/web/app/Models/MatchServerBooking.php
  - apps/web/database/factories/MatchServerFactory.php
  - apps/web/database/factories/MatchServerBookingFactory.php
  - apps/web/tests/Feature/Phase8/MatchServerCredentialEncryptionTest.php
  - apps/web/tests/Feature/Phase8/MatchServerBookingOverlapTest.php
  - apps/web/app/Models/MatchResult.php
  - apps/web/app/Models/GameMatch.php
autonomous: true
requirements:
  - REQ-constraint-league-owns-servers
  - REQ-success-end-to-end-scrim

must_haves:
  truths:
    - "MatchServer model casts credentials_encrypted as `encrypted:array` — round-trip stores ciphertext, reads back the plaintext array."
    - "MatchServerBooking model with active/cancelled/completed status — overlap test fails on EXCLUDE violation (PDOException -> domain exception)."
    - "MatchResult model adds `source` to fillable + casts; defaults to 'manual' on Filament path; refuses overwrite when source='manual' (service guard is plan 08-08)."
    - "GameMatch model adds `manual_entry_required` to fillable + boolean cast; observer wires unchanged."
    - "Factories for MatchServer + MatchServerBooking produce valid persistable instances (token in encrypted form via factory state)."
  artifacts:
    - path: "apps/web/app/Models/MatchServer.php"
      provides: "MatchServer Eloquent model with encrypted:array cast"
      exports: ["MatchServer"]
      contains: "encrypted:array"
    - path: "apps/web/app/Models/MatchServerBooking.php"
      provides: "MatchServerBooking model + match() + server() relations + scopeActive()"
      exports: ["MatchServerBooking"]
    - path: "apps/web/database/factories/MatchServerFactory.php"
      provides: "Factory yielding {api_token: 'fake-bearer-...'} encrypted via cast"
      exports: ["MatchServerFactory"]
    - path: "apps/web/database/factories/MatchServerBookingFactory.php"
      provides: "Factory + ->forMatch() + ->onServer() + ->overlapping() helpers"
      exports: ["MatchServerBookingFactory"]
  key_links:
    - from: "MatchServer.credentials_encrypted"
      to: "Laravel encrypter (APP_KEY-derived)"
      via: "encrypted:array cast"
      pattern: "encrypted:array"
    - from: "MatchServerBooking::scopeActive"
      to: "match_server_bookings.status='active'"
      via: "where('status', 'active')"
      pattern: "scopeActive"
    - from: "MatchResult.source"
      to: "MatchResultService::upsertFromRcon"
      via: "source guard (plan 08-08)"
      pattern: "source"
---

<objective>
Author the two new Phase 8 Eloquent models that the entire RCON ingest path depends on, plus their factories. Extend MatchResult + GameMatch with new fillable / cast entries for source + manual_entry_required (RESEARCH Gap A1 followup).

Purpose: Plan 08-02 added the columns; this plan teaches Eloquent how to read/write them safely (encrypted:array cast for credentials; source enum surfaces as plain string in $fillable; manual_entry_required as a boolean cast).

Output:
- Two new models (MatchServer, MatchServerBooking) with factories
- Two RED Pest tests turned GREEN: MatchServerCredentialEncryptionTest (cast roundtrip) and MatchServerBookingOverlapTest (EXCLUDE rejects).
- MatchResult + GameMatch fillable/casts amended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/app/Models/MatchResult.php
@apps/web/app/Models/GameMatch.php
@apps/web/app/Models/Clan.php
@apps/web/database/factories/UserFactory.php

<interfaces>
<!-- Reuse Phase 4 / Phase 6 model idiom verbatim. -->

MatchResult current shape (relevant excerpt):
```php
protected $fillable = [
    'match_id',
    'winner_clan_id',
    'allies_score',
    'axis_score',
    'notes',
    'recorded_by_user_id',
    'recorded_at',
];
```
Phase 8 amendment: add 'source' to $fillable. Add 'source' => 'string' cast? NO — text column, default mutator is fine; just $fillable add.

GameMatch current shape (relevant excerpt):
```php
protected $fillable = [
    'host_clan_id', 'game_match_type_id', 'status', 'scheduled_start', 'scheduled_end',
    ...
];
protected function casts(): array { return ['status' => 'string', ...]; }
```
Phase 8 amendment: add 'manual_entry_required' to $fillable + 'manual_entry_required' => 'boolean' cast.

LogsActivity pattern on existing models (Phase 1 D-012):
```php
public function getActivitylogOptions(): LogOptions {
    return LogOptions::defaults()->logFillable()->logOnlyDirty()->dontLogIfAttributesChangedOnly([...]);
}
```
MatchServer + MatchServerBooking MUST use LogsActivity (D-012).

Encrypted cast (Laravel 12 docs verified in RESEARCH):
```php
protected function casts(): array {
    return [
        'credentials_encrypted' => 'encrypted:array',
        'last_test_at' => 'datetime',
        'is_active' => 'boolean',
    ];
}
```

EXCLUDE violation surface: Postgres throws SQLSTATE 23P01 (`exclusion_violation`); Laravel wraps as `Illuminate\Database\QueryException` with a vendor-specific code. Test asserts the exception class + message contains "match_server_bookings_no_overlap".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MatchServer + MatchServerBooking models + factories</name>
  <files>apps/web/app/Models/MatchServer.php, apps/web/app/Models/MatchServerBooking.php, apps/web/database/factories/MatchServerFactory.php, apps/web/database/factories/MatchServerBookingFactory.php</files>
  <behavior>
    - `MatchServer` extends Eloquent Model, uses HasFactory + LogsActivity (D-012). HasUuids trait (Phase 1 idiom). `$fillable = ['name','host','port_rcon','region','credentials_encrypted','is_active','last_test_at','last_test_status','last_test_error']`. Casts: `credentials_encrypted => encrypted:array`, `is_active => boolean`, `last_test_at => datetime`. Relations: `bookings()` HasMany. Scope `active()` filters `is_active=true`.
    - `MatchServerBooking` extends Eloquent Model, uses HasFactory + LogsActivity. HasUuids. `$fillable = ['match_id','server_id','reserved_from','reserved_to','status']`. Casts: `reserved_from`, `reserved_to` => datetime. Relations: `match()` BelongsTo GameMatch (explicit FK 'match_id' per D-04-03-B), `server()` BelongsTo MatchServer. Scope `active()` filters `status='active'`. Scope `dueWithin($from, $to)` returns active bookings whose `reserved_from <= $to AND reserved_to >= $from` (used by plan 08-11 BookingScheduler).
    - Factories: MatchServerFactory generates `name='Server '.faker->numerify('##')`, `host='crcon-'.faker->word()`, `port_rcon=8010+offset`, `credentials_encrypted=['api_token'=>'fake-bearer-'.Str::random(40)]` (cast handles encryption), `is_active=true`. State `inactive()` flips `is_active=false`.
    - MatchServerBookingFactory generates `reserved_from=now()->addHour()`, `reserved_to=now()->addHours(3)`, `status='active'`, FKs via `->forMatch(GameMatch $m)`, `->onServer(MatchServer $s)`. State `overlapping(MatchServerBooking $other)` mirrors `reserved_from/to` from `$other` to trigger EXCLUDE in tests.
  </behavior>
  <action>Author both models + factories following the canonical Phase 2/4 idiom. ALL new BelongsTo to GameMatch pass `match_id` as an explicit FK arg (D-04-03-B). Use direct `use App\Models\GameMatch;` import — no Match alias (D-04-03-A / D-04-04-C). Add `/** @use HasFactory<XFactory> */` PHPDoc tags so PHPStan L8 doesn't flag HasFactory (D-01-10 idiom). Replace the plan 08-01 stubs in factory files with real definitions; remove the `@phpstan-ignore-next-line` lines (D-04-01 Wave-0-to-real transition).</action>
  <verify>
    <automated>make pest ARGS="--filter=MatchServerCredentialEncryptionTest" 2>&amp;1 | grep -qE 'PASS|passed' &amp;&amp; make phpstan</automated>
  </verify>
  <done>MatchServer factory creates a row whose `credentials_encrypted` decrypts to `['api_token'=>...]` via the cast; MatchServerBooking factory creates rows with valid FKs; both models LogsActivity-enabled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN MatchServerBookingOverlapTest + extend MatchResult/GameMatch</name>
  <files>apps/web/tests/Feature/Phase8/MatchServerCredentialEncryptionTest.php, apps/web/tests/Feature/Phase8/MatchServerBookingOverlapTest.php, apps/web/app/Models/MatchResult.php, apps/web/app/Models/GameMatch.php</files>
  <behavior>
    - `MatchServerCredentialEncryptionTest` cases:
      1. Create MatchServer with `credentials_encrypted = ['api_token'=>'test-token-123']`; reload from DB; assert `$server->credentials_encrypted['api_token'] === 'test-token-123'`.
      2. Query the raw `credentials_encrypted` column via `DB::table('match_servers')->value('credentials_encrypted')`; assert it is NOT plaintext (starts with `eyJ` Laravel-encrypter JSON prefix or contains base64 noise).
    - `MatchServerBookingOverlapTest` cases:
      1. Create server, create booking A `[10:00, 12:00]`; expect insert succeeds.
      2. Create booking B `[11:00, 13:00]` same server; expect `Illuminate\Database\QueryException` with message containing `match_server_bookings_no_overlap`.
      3. Create booking C `[12:00, 14:00]` same server (back-to-back, sharing endpoint with A); expect insert SUCCEEDS (half-open interval).
      4. Cancel booking A (`->update(['status'=>'cancelled'])`), then create booking D `[10:30, 11:30]` same server; expect SUCCESS (cancelled bookings don't block).
      5. Booking E on a DIFFERENT server overlapping A's window; expect SUCCESS.
    - MatchResult amendments: add `'source'` to `$fillable` BEFORE 'recorded_at'; no cast (text default 'manual' is fine). Add `isManual()` and `isRcon()` accessors.
    - GameMatch amendments: add `'manual_entry_required'` to `$fillable`; add `'manual_entry_required' => 'boolean'` to casts.
  </behavior>
  <action>Replace the RED stubs from plan 08-01 with real test bodies for both files. Extend the two existing models surgically (add fillable + cast lines only — preserve existing structure). Re-run the previously-green Phase 4 / Phase 6 MatchResult and MatchObserver tests to confirm no regression. Verify EXCLUDE behaviour explicitly: in test 3 (back-to-back), the half-open `[)` interval means an endpoint shared between A.end and C.start does NOT overlap — this is intentional and documented in RESEARCH `match_server_bookings` notes.</action>
  <verify>
    <automated>make pest ARGS="--filter='MatchServerCredentialEncryptionTest|MatchServerBookingOverlapTest|MatchResultModel|MatchEventSyncTest'" 2>&amp;1 | grep -qE 'PASS|passed' &amp;&amp; make phpstan &amp;&amp; make pint ARGS="--test"</automated>
  </verify>
  <done>Both Phase 8 model tests GREEN; Phase 4/6 MatchResult/Match tests still GREEN under amended fillable/casts; PHPStan L8 + Pint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application→DB (cast layer) | Laravel encrypter is the gate between MatchServer model API and the ciphertext column. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Information disclosure | credentials_encrypted plaintext leak via raw query | mitigate | `encrypted:array` cast on the model; test 2 of MatchServerCredentialEncryptionTest asserts the raw column is ciphertext envelope, not plaintext JSON. |
| T-08-03-02 | Tampering | EXCLUDE constraint bypass via mass-assignment | mitigate | `$fillable` strictly limits writeable attributes; status enum bounded by CHECK constraint at DB tier; UNIQUE on (match_id, server_id, range) impossible to bypass at app tier. |
| T-08-03-03 | Repudiation | MatchServer changes (API token rotation, IP host change) untraceable | mitigate | LogsActivity trait (D-012) captures every change with causer; Filament admin actions land in activity_log. |
</threat_model>

<verification>
- `make pest ARGS="--filter='MatchServerCredentialEncryptionTest|MatchServerBookingOverlapTest'"` GREEN.
- `make pest --exclude=Phase8` still GREEN (no regression).
- `make phpstan` L8 clean.
- `make pint ARGS="--test"` clean.
</verification>

<success_criteria>
- MatchServer + MatchServerBooking models persist round-trip through factories.
- credentials_encrypted decrypts via cast; raw column is ciphertext.
- EXCLUDE constraint behaviour exhaustively tested (overlap rejected, back-to-back ok, cancelled doesn't block, different-server ok).
- MatchResult + GameMatch amended for source / manual_entry_required without regressing prior tests.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rcon-automation/08-03-SUMMARY.md`
</output>
