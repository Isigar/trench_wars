---
phase: 08-rcon-automation
plan: 04
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - apps/web/app/Models/MatchPlayerStat.php
  - apps/web/app/Models/MatchEvent.php
  - apps/web/database/factories/MatchPlayerStatFactory.php
  - apps/web/database/factories/MatchEventFactory.php
  - apps/web/tests/Feature/Phase8/MatchEventIdempotencyTest.php
  - apps/web/tests/Unit/Phase8/MatchEventModelTest.php
  - apps/web/tests/Unit/Phase8/MatchPlayerStatModelTest.php
autonomous: true
requirements:
  - REQ-goal-rcon-history

must_haves:
  truths:
    - "MatchEvent model wraps the raw RCON stream — payload is jsonb, event_type bound by 10-value CHECK enum, idempotency UNIQUE on (match_id, crcon_stream_id) verified by INSERT collision."
    - "MatchPlayerStat model — UNIQUE on (match_id, player_id) verified by updateOrCreate idempotency."
    - "Both models use HasUuids + LogsActivity (D-012)."
    - "Factories produce realistic CRCON wire shapes for tests (KILL event payload mirrors normalised contract from RESEARCH event-shape table)."
  artifacts:
    - path: "apps/web/app/Models/MatchEvent.php"
      provides: "Raw RCON stream row + match() + scopes (ofType, since)"
      exports: ["MatchEvent"]
    - path: "apps/web/app/Models/MatchPlayerStat.php"
      provides: "Aggregated per-player stats + match() + player()"
      exports: ["MatchPlayerStat"]
    - path: "apps/web/database/factories/MatchEventFactory.php"
      provides: "Factory with state methods kill(), connect(), matchEnd() etc."
      exports: ["MatchEventFactory"]
    - path: "apps/web/database/factories/MatchPlayerStatFactory.php"
      provides: "Factory with valid non-negative scalar fields"
      exports: ["MatchPlayerStatFactory"]
  key_links:
    - from: "MatchEvent::scopeOfType"
      to: "MatchPlayerStatAggregator (plan 08-08)"
      via: "->ofType('player_kill')->get()"
      pattern: "scopeOfType"
    - from: "MatchEvent unique (match_id, crcon_stream_id)"
      to: "Worker reconnect idempotency"
      via: "UNIQUE violation = no-op (plan 08-07 ingest service)"
      pattern: "crcon_stream_id"
---

<objective>
Author MatchEvent + MatchPlayerStat models, their factories with realistic CRCON-shape state methods, and the idempotency test for `match_events.(match_id, crcon_stream_id)` UNIQUE.

Purpose: These models are the read-side of the RCON pipeline. The aggregator (plan 08-08) reads MatchEvent; the bot embed (plan 08-12) reads MatchPlayerStat. Plan 08-07 ingest service writes MatchEvent and relies on the UNIQUE to be idempotent on worker reconnect.

Output:
- Two new models with relationships, scopes, casts.
- Two factories with state methods matching the normalised event shape table from RESEARCH.
- Unit tests covering model invariants + idempotency feature test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/app/Models/MatchResult.php
@apps/web/app/Models/Player.php
@apps/web/app/Models/GameMatch.php

<interfaces>
<!-- Canonical event-shape table from RESEARCH lines 521-534 -->

Canonical match_event payload shapes (every state method on MatchEventFactory yields one of these):
```
game_start          → { map: string, mode: string }
round_start         → { round_number: int }
player_kill         → { killer: { steam_id_64, name }, victim: { steam_id_64, name }, weapon: string }
player_team_kill    → { killer: {...}, victim: {...}, weapon: string }
player_connect      → { steam_id_64, name }
player_disconnect   → { steam_id_64, name }
team_switch         → { steam_id_64, name, from_team, to_team }
round_end           → { winning_team: 'allies'|'axis', allies_score: int, axis_score: int }
match_end           → { winning_team, allies_score, axis_score, ended_at }
manual_error        → { kind: 'unreachable'|'auth_failed'|'permission_denied', detail: string }
```

CRCON stream_id format (verified — RESEARCH "Stream ID format"):
```
"{unix_timestamp_seconds}-{increment}"
e.g., "1711657986-0", "1711657986-1"
```
Factory state should generate strictly-increasing fake stream IDs to mirror CRCON's actual contract.

Direct `use App\Models\GameMatch;` import idiom (D-04-04-C / D-04-07-C); no Match alias. BelongsTo<GameMatch, $this> with explicit 'match_id' FK arg (D-04-03-B).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MatchEvent + MatchPlayerStat models + factories</name>
  <files>apps/web/app/Models/MatchEvent.php, apps/web/app/Models/MatchPlayerStat.php, apps/web/database/factories/MatchEventFactory.php, apps/web/database/factories/MatchPlayerStatFactory.php</files>
  <behavior>
    - `MatchEvent`: HasUuids + LogsActivity (D-012 — events are append-only, never edited, but creation is audited). `$fillable = ['match_id','event_type','crcon_action','crcon_stream_id','payload','occurred_at']`. Casts: `payload => array`, `occurred_at => datetime`, `ingested_at => datetime`. `match()` BelongsTo GameMatch with explicit 'match_id' FK. Scopes: `ofType(string $type)`, `since(Carbon $when)`. NO setter for `ingested_at` — Postgres DEFAULT now() handles it (cast is read-only).
    - `MatchPlayerStat`: HasUuids + LogsActivity. `$fillable = ['match_id','player_id','kills','deaths','team_kills','score','role_played','weapons_used']`. Casts: `kills/deaths/team_kills/score => integer`, `weapons_used => array`. `match()` + `player()` BelongsTo. Accessor `kdr()` returns `deaths ? round(kills/deaths, 2) : kills` (used by MVP picker in plan 08-12).
    - MatchEventFactory definition() defaults to a `player_kill` shape with two random Steam IDs + a Karabiner 98K weapon. State methods: `gameStart()`, `roundStart(int $n)`, `kill(string $killerSteam, string $victimSteam, string $weapon='KARABINER 98K')`, `teamKill(...)`, `connect(string $steam, string $name)`, `disconnect(...)`, `teamSwitch(...)`, `roundEnd('allies', int $alliesScore, int $axisScore)`, `matchEnd('allies', int $alliesScore, int $axisScore)`, `manualError(string $kind='unreachable')`. Stream ID auto-incremented per factory call (`static $counter`).
    - MatchPlayerStatFactory definition() generates `kills=faker->numberBetween(0,30)`, `deaths=faker->numberBetween(0,15)`, `team_kills=0`, `score=kills*100`, `role_played=null`, `weapons_used=null`. State `forPlayer(Player $p)` + `forMatch(GameMatch $m)`.
  </behavior>
  <action>Author both models + factories per the behaviour list. Use the Phase 4 / Phase 6 idiom verbatim (direct GameMatch import, HasUuids, LogsActivity). Wire BelongsTo with explicit FK args (D-04-03-B). Add PHPDoc `@use HasFactory<XFactory>` so PHPStan L8 doesn't flag. Replace the plan 08-01 stub bodies; remove `@phpstan-ignore-next-line` from both factory files.</action>
  <verify>
    <automated>make phpstan &amp;&amp; make pint ARGS="--test" &amp;&amp; docker compose exec -T web php artisan tinker --execute="echo App\Models\MatchEvent::factory()->kill('111','222')->raw()['event_type'];" 2>/dev/null | grep -q player_kill</automated>
  </verify>
  <done>Both models load, factories produce valid Faker rows, PHPStan L8 + Pint clean, the `kill()` state method yields event_type='player_kill'.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN MatchEventIdempotencyTest + model unit tests</name>
  <files>apps/web/tests/Feature/Phase8/MatchEventIdempotencyTest.php, apps/web/tests/Unit/Phase8/MatchEventModelTest.php, apps/web/tests/Unit/Phase8/MatchPlayerStatModelTest.php</files>
  <behavior>
    - `MatchEventIdempotencyTest` cases:
      1. Insert MatchEvent A with `crcon_stream_id='1711657986-0'`, match $m → succeeds.
      2. Insert MatchEvent B with same crcon_stream_id + same match → expect `Illuminate\Database\UniqueConstraintViolationException` (Postgres SQLSTATE 23505).
      3. Insert MatchEvent C with same crcon_stream_id but DIFFERENT match → succeeds (UNIQUE is composite on (match_id, crcon_stream_id)).
      4. Insert MatchEvent D with `crcon_stream_id=null` AND another null → SUCCEEDS (NULL ≠ NULL in Postgres UNIQUE, allowing pre-CRCON synthetic events like manual_error not tied to a stream).
    - `MatchEventModelTest` (unit) cases:
      1. event_type CHECK rejects 'foo' — `DB::insert(...)` with `event_type='foo'` raises QueryException.
      2. `scopeOfType('player_kill')` filters correctly across 3-event fixture.
      3. `scopeSince(now()->subHour())` filters correctly.
      4. payload jsonb roundtrip — `$event->payload = ['weapon'=>'K98']; $event->save(); $event->refresh();` retains key.
    - `MatchPlayerStatModelTest` (unit) cases:
      1. UNIQUE (match_id, player_id) — second insert same pair raises QueryException; `updateOrCreate(['match_id'=>$m->id,'player_id'=>$p->id], $stats)` is idempotent.
      2. CHECK rejects negative kills (`DB::insert(...)` with `kills=-1` raises QueryException).
      3. `kdr()` accessor returns 2.0 for kills=20 deaths=10; returns 5 for kills=5 deaths=0 (no division-by-zero).
  </behavior>
  <action>Replace plan 08-01 RED stub bodies for the idempotency feature test; add the two unit test files. Use canonical Pest expect()/exception assertion idiom from Phase 4. For test 2 of MatchEventIdempotencyTest, catch the exception explicitly via `try { ... } catch (UniqueConstraintViolationException $e) { ... }` rather than `expectException` so the assertion can verify message content.</action>
  <verify>
    <automated>make pest ARGS="--filter='MatchEventIdempotencyTest|MatchEventModelTest|MatchPlayerStatModelTest'" 2>&amp;1 | grep -qE 'PASS|passed' &amp;&amp; make phpstan</automated>
  </verify>
  <done>All three test files GREEN; the idempotency invariant is verified at the DB level (UNIQUE constraint, not application code); both unit test files cover CHECK + scope behaviour.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker→DB (via web service) | UNIQUE constraint absorbs replay attempts as no-ops. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | Replayed CRCON event from buggy reconnect duplicates the kill | mitigate | (match_id, crcon_stream_id) UNIQUE — second insert rejected at DB tier; plan 08-07 catches UniqueConstraintViolationException and treats as no-op. |
| T-08-04-02 | Tampering | event_type forged outside the canonical enum | mitigate | CHECK constraint on event_type column (10 allowed values); MatchEventFactory only generates valid values. |
| T-08-04-03 | Integrity | MatchPlayerStat negative values from buggy aggregator | mitigate | CHECK kills>=0 AND deaths>=0 AND team_kills>=0 AND score>=0; unit test 2 verifies. |
</threat_model>

<verification>
- `make pest ARGS="--filter='Phase8/MatchEvent|Phase8/MatchPlayerStat'"` GREEN.
- `make phpstan` L8 clean.
- `make pint ARGS="--test"` clean.
</verification>

<success_criteria>
- MatchEvent + MatchPlayerStat models persistable via factories with realistic CRCON-shape state methods.
- (match_id, crcon_stream_id) UNIQUE makes worker reconnect replay harmless at DB tier.
- (match_id, player_id) UNIQUE makes aggregator updateOrCreate idempotent.
- CHECK enums + non-neg constraints assert at the DB tier.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rcon-automation/08-04-SUMMARY.md`
</output>
