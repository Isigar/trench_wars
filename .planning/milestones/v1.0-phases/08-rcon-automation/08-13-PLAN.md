---
phase: 08-rcon-automation
plan: 13
type: execute
wave: 9
depends_on: ["08-12"]
files_modified:
  - .planning/phases/08-rcon-automation/08-PHASE-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REQ-goal-rcon-history
  - REQ-constraint-league-owns-servers
  - REQ-success-end-to-end-scrim

must_haves:
  truths:
    - "All 7 quality gates run and pass: make pest, make phpstan, make pint --test, vue-tsc, shared-types typecheck, rcon-worker (test + typecheck + lint + build), web migration freshness (migrate:fresh --seed --pretend dry run)."
    - "08-PHASE-VERIFICATION.md authored mapping SC-1..SC-5 to specific tests + canonical D-08-* decisions + 12 Pitfalls + 5 Open Questions LOCKED inline."
    - "ROADMAP.md updates: Phase 8 marked [x] Complete; Plans count finalised; per-plan checkboxes [x]; completion date set."
    - "REQUIREMENTS.md updates: REQ-goal-rcon-history, REQ-constraint-league-owns-servers, REQ-success-end-to-end-scrim flipped to Complete; traceability table updated; footer Last-updated entry appended."
    - "STATE.md updates: stopped_at='Phase 8 COMPLETE (PENDING_MANUAL_SMOKE) — Phase 9 ready'; status reflects Phase 8 close."
    - "PENDING_MANUAL_SMOKE checklist enumerated for operator (live CRCON probe, two-clan SC-5 happy path on real server, mid-match log gap, HMAC key rotation flow)."
  artifacts:
    - path: ".planning/phases/08-rcon-automation/08-PHASE-VERIFICATION.md"
      provides: "Single-document attestation for Phase 8 complete"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 8 marked complete; Phase 9 next"
    - path: ".planning/REQUIREMENTS.md"
      provides: "3 REQs flipped to Complete; traceability table updated"
  key_links:
    - from: "ROADMAP.md → Phase 8 → Plans list"
      to: "All 13 plans marked [x]"
      via: "checkbox flips during this BLOCKING plan"
      pattern: "- \\[x\\] 08-"
---

<objective>
[BLOCKING] Phase 8 verification: run all quality gates, author the phase verification document, update ROADMAP/REQUIREMENTS/STATE. This is the canonical Phase 8 closer mirroring Phase 7 plan 07-13 (and the consistent pattern from Phases 3/4/5/6).

Purpose: Single document that proves Phase 8 satisfies SC-1..SC-5 + REQ-goal-rcon-history + REQ-constraint-league-owns-servers + REQ-success-end-to-end-scrim with specific test → success criterion → requirement traceability. Captures any plan-level decisions made during execution as canonical D-08-* entries.

Output:
- 08-PHASE-VERIFICATION.md (the deliverable).
- 3 metadata-file updates (ROADMAP, REQUIREMENTS, STATE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cms/07-PHASE-VERIFICATION.md
@.planning/phases/06-tournaments-brackets/06-PHASE-VERIFICATION.md
@.planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md
@.planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

<interfaces>
<!-- Use Phase 7 plan 07-13 as the canonical template for verification doc shape + ROADMAP/REQUIREMENTS update pattern. -->

08-PHASE-VERIFICATION.md structure:
```
# Phase 8 — RCON automation — Verification

**Phase:** 08-rcon-automation
**Closed:** YYYY-MM-DD
**Status:** Complete (PENDING_MANUAL_SMOKE)

## Quality Gates

| Gate | Command | Result | Notes |
|------|---------|--------|-------|
| Pest (web)      | `make pest`                                     | NNN tests / MMM assertions / 0 failures | Phase 8 added X tests across N files |
| PHPStan L8      | `make phpstan`                                  | No errors | Baseline preserved |
| Pint            | `make pint ARGS="--test"`                       | Clean | |
| vue-tsc         | `make pnpm ARGS="-F @trenchwars/web typecheck"` | Clean | |
| shared-types    | `pnpm --filter @trenchwars/shared-types typecheck` | Clean | Phase 8 aliases added |
| rcon-worker     | `pnpm --filter @trenchwars/rcon-worker test && typecheck && lint && build` | NN cases pass | 38 vitest cases |
| Migrations      | `make artisan ARGS="migrate:fresh --seed"`      | Clean | |

## Success Criteria Traceability

| SC | Description | Proving Test(s) | Plan |
|----|-------------|-----------------|------|
| SC-1 | MatchServer register + Test Connection | MatchServerResourcePresentTest + TestMatchServerConnectionJobTest | 08-09 |
| SC-2 | Booking EXCLUDE prevents overlap | MatchServerBookingOverlapTest | 08-03 |
| SC-3 | rcon-worker streams events HMAC-signed → MatchResult + stats auto-populate | VerifyRconSignatureTest + InternalApiRoutesPresentTest + MatchEventIngestServiceTest + MatchPlayerStatAggregatorTest + RconMatchResultIngestionTest + worker vitest suite | 08-05..08-08 + 08-10..08-11 |
| SC-4 | Graceful CRCON failure degrades to manual_entry_required | RconUnreachableFlagsManualTest + ManualOverrideWinsTest | 08-08 + 08-09 |
| SC-5 | Full E2E scrim happy path | ScrimE2EHappyPathTest | 08-12 |

## Requirements Traceability

| REQ | Status | Proving |
|-----|--------|---------|
| REQ-goal-rcon-history       | Complete | SC-3 + SC-5 traceability above |
| REQ-constraint-league-owns-servers | Complete | SC-1 + SC-2 + RESEARCH "League IT onboarding" docs |
| REQ-success-end-to-end-scrim | Complete | SC-5 capstone test |

## Canonical D-08-* decisions
| ID | Decision | Source |
|----|----------|--------|
| D-08-XX | (capture plan-level decisions accumulated during execution) | |

## Pitfalls — verified mitigated
| Pitfall | Plan | Verification |
|---------|------|--------------|
| 1 (HMAC raw body) | 08-05 | VerifyRconSignatureTest case 7 |
| 2 (clock skew) | 08-05 | VerifyRconSignatureTest case 5 |
| 3 (CRCON last_seen_id) | 08-10 | CrconClient.integration.test case 1 |
| 4 (aggregator N²) | 08-08 | Once-per-match_end design verified by code review |
| 5 (Steam ID orphan) | 08-08 | MatchPlayerStatAggregatorTest case 4 |
| 6 (Test Connection timeout) | 08-09 | Async Horizon job (TestMatchServerConnectionJobTest) |
| 7 (btree_gist) | 08-02 | Migration runs CREATE EXTENSION inline; verified via pg_extension query |
| 8 (APP_KEY rotation) | RESEARCH | Documented runbook; out of round-1 scope |
| 9 (Pino PII redact) | 08-01 + 08-10 | logger.ts redact list verified |
| 10 (CRCON connection limit) | RESEARCH | Documented operational limit; cap on round-1 simultaneous bookings |
| 11 (worker scaleout race) | 08-11 | Documented "single replica" + RESEARCH Pitfall 11 |
| 12 (manual lock no signal) | 08-08 | activity_log row written; ManualOverrideWinsTest case 2 |

## Open Questions — RESOLVED
1. CRCON version standardisation → RESOLVED: pin v10.0.0+ via config('rcon.crcon_version_pin').
2. Steam ID linkage flow → RESOLVED: orphan events silently skipped (Pitfall 5); player self-register Steam ID via public profile editor (deferred to a v2 enhancement; round-1 acceptance assumes players have populated steam_id_64 before scrim).
3. MatchServer Test Connection path → RESOLVED: web→CRCON direct (not async-via-worker); Pitfall 6 async via Horizon job from web.
4. CRCON chat capture → RESOLVED: out of scope (CONTEXT.md deferred list re-affirmed).
5. Ringer/cross-clan Steam ID → RESOLVED: MatchPlayerStat is per-player_id not per-clan; stats record regardless of clan affiliation.

## PENDING_MANUAL_SMOKE
Operator walkthrough (4 items):
- [ ] **A**: Register a real MatchServer in Filament (live CRCON instance). Click Test Connection. Verify last_test_status badge flips to "ok".
- [ ] **B**: Schedule a scrim, book the server, play it on real CRCON, verify match_results + match_player_stats populated without manual entry.
- [ ] **C**: Disconnect the CRCON network mid-match for 2 minutes, reconnect. Verify last_seen_id resume captures the missed window.
- [ ] **D**: Rotate WEB_HMAC_SECRET in Railway, restart both `web` and `rcon-worker` services. Verify the next booking ingests cleanly.
```

ROADMAP.md updates (surgical edits):
- Phase 8 line in TOC: `[ ]` → `[x]` + add `_Completed YYYY-MM-DD_`.
- Phase 8 Plans list: all 13 entries `[ ]` → `[x]`; rewrite the placeholder Phase 2 entries that are currently shown as Phase 8 plan names (verified earlier — ROADMAP currently shows Phase 2 plans names duplicated; replace with the actual 13 plan names from this phase).
- Progress table: Phase 8 row → `13/13 | Complete | YYYY-MM-DD`.

REQUIREMENTS.md updates:
- Three REQ checkboxes flipped to `[x]`.
- Traceability table rows for REQ-goal-rcon-history / REQ-constraint-league-owns-servers / REQ-success-end-to-end-scrim → status='Complete'.
- Footer line appended: `*Last updated: YYYY-MM-DD — Phase 8 close: REQ-goal-rcon-history + REQ-constraint-league-owns-servers + REQ-success-end-to-end-scrim flipped to Complete (08-PHASE-VERIFICATION.md maps SC-1..SC-5 + 12 Pitfalls + 5 Open Questions LOCKED)*`.

STATE.md updates:
- `status` → 'Phase 8 COMPLETE (PENDING_MANUAL_SMOKE) — 08-13-PLAN.md executed; 08-PHASE-VERIFICATION.md authored; ROADMAP/REQUIREMENTS/STATE updated'
- `stopped_at` → 'Phase 8 COMPLETE (PENDING_MANUAL_SMOKE) — Phase 9 ready'
- `progress.completed_phases` 7 → 8
- `progress.total_plans` += 13 (95 → 108)
- `progress.completed_plans` += 13 (95 → 108)
- `progress.percent` recompute (8/9 = 89%)
- Recent decisions section: append Phase 8 D-08-* entries.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run full quality gate sweep + author 08-PHASE-VERIFICATION.md</name>
  <files>.planning/phases/08-rcon-automation/08-PHASE-VERIFICATION.md</files>
  <behavior>
    - Execute the 7 quality gates verbatim (web Pest, PHPStan L8, Pint, vue-tsc, shared-types typecheck, rcon-worker chain, migration freshness). Capture exact counts (NNN tests / MMM assertions).
    - Verify ZERO failures across all gates before proceeding.
    - If a gate fails, STOP and report — do not falsely claim Phase 8 complete.
    - Author 08-PHASE-VERIFICATION.md per the interfaces template. Fill the Quality Gates table with real counts. Map every SC → real test name (not vague "various"). Capture any plan-level decisions made during execution as D-08-XX entries in the canonical decisions table — these come from each plan's SUMMARY.md (read them as you author this doc).
  </behavior>
  <action>Run gates in this exact order: (1) `make pest`, (2) `make phpstan`, (3) `make pint ARGS="--test"`, (4) `make pnpm ARGS="-F @trenchwars/web typecheck"` (vue-tsc), (5) `docker compose exec rcon-worker pnpm typecheck && pnpm lint && pnpm test && pnpm build`, (6) `pnpm --filter @trenchwars/shared-types typecheck` (from repo root), (7) `make artisan ARGS="migrate:fresh --seed"` (asserts schema chain stable). If any fail → STOP. Capture all output. Then author the verification document with real numbers. Read each 08-*-SUMMARY.md to extract D-08-* decisions for the canonical decisions table.</action>
  <verify>
    <automated>test -f .planning/phases/08-rcon-automation/08-PHASE-VERIFICATION.md &amp;&amp; grep -q "SC-5" .planning/phases/08-rcon-automation/08-PHASE-VERIFICATION.md &amp;&amp; grep -q "Quality Gates" .planning/phases/08-rcon-automation/08-PHASE-VERIFICATION.md</automated>
  </verify>
  <done>All 7 gates GREEN; 08-PHASE-VERIFICATION.md exists with SC-1..SC-5 → test → REQ traceability; D-08-* decisions captured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update ROADMAP.md + REQUIREMENTS.md + STATE.md</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <behavior>
    - ROADMAP.md: Phase 8 TOC line flipped `[x]` + completion date; rewrite the Phase 8 Plans list (currently contains a misplaced Phase 2 plans listing per earlier inspection); all 13 plans `[x]`; Progress table row updated to `13/13 Complete YYYY-MM-DD`.
    - REQUIREMENTS.md: 3 REQ checkboxes flipped; traceability table updated; footer line appended (mirrors Phase 7's footer format).
    - STATE.md: `status`, `stopped_at`, `progress.*` fields updated per the interfaces; append Phase 8 D-08-* decisions to the Recent decisions list.
    - Use surgical edits — preserve every other line of every file.
  </behavior>
  <action>Read each file, make the surgical edits per the interfaces, save. CRITICAL: the ROADMAP currently shows "Phase 8" with a "Plans" list that mirrors Phase 2's plan names (visible in earlier inspection) — this is roadmap drift that must be corrected. Replace with the actual 13 Phase 8 plans by name (08-01-PLAN.md ... 08-13-PLAN.md with the canonical objectives). Verify each markdown file still parses (run a markdown lint or just visual inspection that headings + tables remain intact). For STATE.md `progress.completed_phases` = 8, `progress.total_plans` = 108, `progress.completed_plans` = 108, `progress.percent` = 89.</action>
  <verify>
    <automated>grep -q "Phase 8: RCON automation" .planning/ROADMAP.md &amp;&amp; grep -q "\\[x\\] \\*\\*Phase 8" .planning/ROADMAP.md &amp;&amp; grep -q "REQ-goal-rcon-history.*Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -q "Phase 8 COMPLETE" .planning/STATE.md &amp;&amp; grep -q "08-01-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP shows Phase 8 [x] with the 13 real plan names; REQUIREMENTS shows 3 REQs Complete in both list + traceability table; STATE shows Phase 8 COMPLETE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase verification docs are the canonical attestation; downstream phases trust them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-13-01 | Repudiation | Phase verification falsely claims completion | mitigate | Quality gates are RUN, not stated. Verification doc records exact test counts; CI runs the same suite. |
| T-08-13-02 | Integrity | ROADMAP drift introduces inconsistencies | mitigate | Surgical edits only; verification command greps for both the [x] checkbox AND the 13 real plan names. |
| T-08-13-03 | Repudiation | Manual smoke list deferred indefinitely | accept | PENDING_MANUAL_SMOKE pattern is canonical to this project (every phase since 1 ships with it); operator owns the walkthrough. |
</threat_model>

<verification>
- All 7 quality gates run and GREEN.
- 08-PHASE-VERIFICATION.md exists with concrete SC→test→REQ mapping.
- ROADMAP + REQUIREMENTS + STATE updated.
- `git status` shows changes only in the 4 expected files.
</verification>

<success_criteria>
- Phase 8 closed: 13/13 plans complete, 3 REQs flipped, SC-1..SC-5 mechanically proven.
- D-08-* decisions captured for traceability.
- PENDING_MANUAL_SMOKE handed off to operator with 4-item walkthrough.
- Phase 9 (Polish) ready to plan.
</success_criteria>

<output>
After completion, create `.planning/phases/08-rcon-automation/08-13-SUMMARY.md`
</output>
