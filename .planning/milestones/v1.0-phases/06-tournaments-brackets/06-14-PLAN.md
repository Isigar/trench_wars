---
phase: 06-tournaments-brackets
plan: 14
type: execute
wave: 9
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12, 13]
files_modified:
  - .planning/phases/06-tournaments-brackets/06-PHASE-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REQ-success-tournament-end-to-end
must_haves:
  truths:
    - "06-PHASE-VERIFICATION.md authored as the canonical phase-close artifact (Phase 1-5 idiom: M1..M7 traceability + SC-1..SC-5 mapping + Pitfall coverage + Open Question RESOLUTIONS)"
    - "All 5 quality gates GREEN: Pest, Pint, PHPStan L8, vue-tsc, shared-types typecheck (Phase 1 plan 01-16 CI gate set)"
    - "Bot quality gates GREEN: pnpm --filter @trenchwars/bot tsc, pnpm --filter @trenchwars/bot test (Vitest)"
    - "Pest total: Phase 5's 618 + Phase 6 net new (~50-80 new tests across 28 RED stubs flipped to GREEN + the 8 unit tests + 6 admin tests + 4 public tests + 2 integration tests) — concrete numbers in the verification report"
    - "Activity log integration verified end-to-end via TournamentAuditLogTest (plan 06-13)"
    - "ROADMAP.md updated: Phase 6 marked Complete with date 2026-05-13 (or actual exec date); Plans line updated to 14/14"
    - "REQUIREMENTS.md updated: REQ-success-tournament-end-to-end status flipped from Pending to Complete"
    - "STATE.md updated: completed_phases=6; progress percent recalculated (6/9 = 67%); last_activity updated; canonical bindings + Open Question RESOLUTIONS documented for Phase 7+"
    - "Status: PENDING_MANUAL_SMOKE — operator walkthrough covering 4 manual-only items from 06-VALIDATION.md (full single-elim run, swiss 6-round, bracket SVG visual fidelity at 4/7/8/16, bot announce live Discord smoke)"
  artifacts:
    - path: ".planning/phases/06-tournaments-brackets/06-PHASE-VERIFICATION.md"
      provides: "Canonical phase-close artifact; SC-1..SC-5 traceability + Open Question RESOLUTIONS + Pitfall coverage matrix"
      contains: "PHASE-VERIFICATION"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 row flipped to Complete + plans 14/14 + Completed column = exec date"
      contains: "14/14"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-success-tournament-end-to-end → Complete"
      contains: "REQ-success-tournament-end-to-end"
    - path: ".planning/STATE.md"
      provides: "completed_phases=6 + recent decisions section + canonical D-06-* bindings"
      contains: "completed_phases: 6"
  key_links:
    - from: "06-PHASE-VERIFICATION.md"
      to: "06-RESEARCH.md (SC-1..SC-5 + Open Questions + Pitfalls)"
      via: "traceability matrix per Phase 5 D-05-13 idiom"
      pattern: "SC-1"
    - from: "ROADMAP.md Phase 6 row"
      to: "Plans 06-01..06-14 file list"
      via: "all checkboxes [x] + Plans 14/14"
      pattern: "06-14-PLAN"
---

<objective>
[BLOCKING] Phase 6 verification. Run all quality gates, author 06-PHASE-VERIFICATION.md, update ROADMAP / REQUIREMENTS / STATE.

This is the canonical phase-close ceremony per Phase 1-5 idiom (plans 01-18, 02-14, 03-10, 04-13, 05-13 are direct references). The verification report maps SC-1..SC-5 → test files + RESEARCH Pitfalls 1-12 → mitigations + Open Questions A1..A11 → resolutions, then locks status as PENDING_MANUAL_SMOKE pending operator walkthrough of the 4 manual items from 06-VALIDATION.md.

Purpose: SC-1..SC-5 are now verifiable via Pest + Vitest. The Phase 6 deliverable (Tournaments & brackets) is shippable subject to operator visual confirmation of bracket SVG fidelity + a live Discord bot smoke.

Output: 1 PHASE-VERIFICATION.md + 3 planning-doc amendments (ROADMAP, REQUIREMENTS, STATE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-tournaments-brackets/06-RESEARCH.md
@.planning/phases/06-tournaments-brackets/06-CONTEXT.md
@.planning/phases/06-tournaments-brackets/06-VALIDATION.md
@.planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md
@.planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
Canonical phase-verification report structure (Phase 4 + Phase 5 verified idiom):

```markdown
---
phase: 6
slug: tournaments-brackets
status: PENDING_MANUAL_SMOKE
completed: <ISO date of plan 06-14 execution>
plans_total: 14
plans_complete: 14
test_total: <full Pest test count>
test_assertions: <total assertions>
test_passing: <count>
test_failing: 0
test_incomplete: 0
quality_gates:
  pest: GREEN
  pint: GREEN
  phpstan_l8: GREEN
  vue_tsc: GREEN
  shared_types_typecheck: GREEN
  bot_tsc: GREEN
  bot_vitest: GREEN
---

# Phase 6 — Tournaments & brackets — Verification

## Status

PENDING_MANUAL_SMOKE — 4 operator walkthrough items remaining (see Manual Smoke section).

## Success Criteria Traceability

| SC | Description | Test File(s) | Status |
|----|-------------|--------------|--------|
| SC-1 | Admin creates tournament + registers 8 clans + seeds + single-elim bracket end-to-end | TournamentEndToEndTest.php + TournamentResourceTest.php + TournamentSeedActionTest.php + BracketGeneratorSingleEliminationTest.php + BracketMatchMaterialiserServiceTest.php | GREEN |
| SC-2 | Same workflow available for round-robin, double-elim, swiss formats | BracketGeneratorRoundRobinTest.php + BracketGeneratorDoubleEliminationTest.php + BracketGeneratorSwissTest.php | GREEN |
| SC-3 | Public /tournaments/{slug} with 5 tabs + 30s polling | TournamentShowPageTest.php + TournamentIndexPageTest.php + TournamentPublicJsonControllerTest.php + Vue manual smoke | PARTIAL — automated GREEN; visual fidelity pending operator smoke |
| SC-4 | Winner_participant_id propagation + standings recompute | BracketAdvancementServiceTest.php + MatchResultObserverTest.php + StandingsCalculatorServiceTest.php + TournamentEndToEndTest.php | GREEN |
| SC-5 | Admin reseed/forfeit/withdraw/recalc actions, all audited | TournamentReseedActionTest.php + TournamentForfeitActionTest.php + TournamentWithdrawActionTest.php + TournamentRecalculateStandingsTest.php + TournamentAuditLogTest.php | GREEN |

## REQ-success-tournament-end-to-end

Maps to all 5 SCs above. Status: COMPLETE.

## Open Questions RESOLVED Inline During Planning

| Q | Topic | Resolution | Where LOCKED |
|---|-------|-----------|--------------|
| A4 | Reseed eligibility threshold | "No MatchResult rows exist for any bracket-linked match" (strictest interpretation) | Plan 06-05 — Tournament::canReseed() |
| A5 | Forfeit vs withdraw semantics | IDENTICAL forward semantics (both stop future matches; past results retained); different status strings (disqualified vs withdrawn) for audit clarity | Plans 06-09 + 06-11 |
| A6 | Swiss next-round trigger | Admin click via Filament Action `generate_next_swiss_round` | Plans 06-07 + 06-11 |
| A8 | Tournament organiser role | Admin-only via spatie permission `tournament.manage`; separate organiser tier deferred to v2 | Plan 06-11 |
| 5 (RESEARCH § Open Questions) | Bot integration kind enums | 3 distinct kinds: tournament_announce, tournament_announce_update, bracket_result_announce | Plans 06-08 + 06-10 + 06-13 |

## Pitfall Coverage Matrix

| # | Pitfall | Mitigation | Where |
|---|---------|-----------|-------|
| 1 | PHP 8 `match` reserved keyword | App\Models\GameMatch (D-04-03-A continuation); direct import; `'match_id'` explicit FK on TournamentBracket::match() | Plans 06-03 + 06-06 |
| 2 | Bracket round_number off-by-one | `(int) ceil($p / 2)` for advances_to position | Plan 06-06 SingleEliminationGenerator |
| 3 | Bracket generator non-idempotency | BracketsAlreadyGeneratedException + Tournament.stages.exists() guard | Plans 06-04 + 06-06 |
| 4 | Bracket → GameMatch materialisation race | DB::transaction + TournamentBracket::lockForUpdate + partial UNIQUE INDEX on match_id WHERE NOT NULL | Plans 06-02 + 06-06 |
| 5 | Swiss never-paired-before infinite loop | SwissTooFewParticipantsException when participants_count < 2^rounds | Plan 06-07 |
| 6 | Standings recalculate trampling | DB::transaction + Tournament::lockForUpdate in StandingsCalculatorService + BracketAdvancementService | Plans 06-08 + 06-09 |
| 7 | TournamentObserver double-fire | wasChanged('status') gate in updated() body | Plan 06-10 |
| 8 | Polling endpoint cache staleness | Etag computed per-request from live tournament.updated_at + brackets.max(updated_at); no application-level cache | Plans 06-10 + 06-12 |
| 9 | SVG renderer x/y math for double-elim L-bracket | Stage-type group + separate <g transform="translate(...)"> with cumulative stageYOffset | Plan 06-12 BracketCanvas.vue |
| 10 | i18n key explosion for 4 formats × surfaces | TournamentI18nKeyCoverageTest in CI; 90+ keys verified | Plans 06-01 + 06-13 |
| 11 | tournament_brackets self-FK cycle | DB CHECK no_self_advance + service-level single-hop walk in BracketAdvancementService | Plans 06-02 + 06-08 |
| 12 | Forfeit/withdraw timing leaks | A5 LOCKED — identical forward semantics; past matches retain results | Plans 06-09 + 06-11 |

## RESEARCH Assumptions Status

| # | Assumption | Status |
|---|-----------|--------|
| A1 | Inner_outer ordering for size=8 → [1,8,4,5,2,7,3,6] | VERIFIED via BracketGeneratorSingleEliminationTest |
| A2 | Swiss minimum N for ceil(log2(N)) rounds is 2^rounds | VERIFIED via Pitfall 5 mitigation in plan 06-07 |
| A3 | Plain Buchholz tiebreak (no median variant) | LOCKED for v1; median variant = Phase 9 polish |
| A4 | Reseed eligibility = no MatchResult exists | LOCKED in plan 06-05 |
| A5 | Forfeit + withdraw identical forward semantics | LOCKED in plans 06-09 + 06-11 |
| A6 | Swiss next-round admin click | LOCKED in plans 06-07 + 06-11 |
| A7 | Double-elim L-bracket spread loser pairs across L-bracket halves | LOCKED for N=8 layout; cross-checked vs brackets-manager.js Context7 |
| A8 | Tournament organiser admin-only | LOCKED in plan 06-11 |
| A9 | Single default_game_match_type_id per tournament (no per-bracket override) | LOCKED for v1; stage-level override = Phase 9 polish |
| A10 | Bracket GameMatch.host_clan_id = NULL | LOCKED in plan 06-06 BracketMatchMaterialiserService |
| A11 | by_rank seeding v1 = created_at desc | LOCKED in plan 06-05; ELO upgrade = Phase 9 polish |

## Canonical Phase 6 Bindings (D-06-* — for Phase 7+ continuation)

| ID | Decision |
|----|----------|
| D-06-01-A | App\Models\GameMatch (D-04-03-A continuation) — direct import everywhere; `'match_id'` explicit FK on TournamentBracket::match() per D-04-03-B |
| D-06-02-A | 5 tables: tournaments + tournament_participants + tournament_stages + tournament_brackets + tournament_standings — all uuid PKs + timestamptz + LogsActivity |
| D-06-04-A | TournamentStatusService::transition mirrors MatchStatusService verbatim (D-04-04-A pattern); seeded → registering is the canonical reseed back-transition |
| D-06-05-A | Reseed eligibility: NO MatchResult rows for any bracket-linked match (Tournament::canReseed) |
| D-06-06-A | Single-elim: inner_outer seeding hardcoded for sizes 4/8/16/32; size 64+ via recursive computeInnerOuter() |
| D-06-06-B | BracketMatchMaterialiserService row-locked via TournamentBracket::lockForUpdate (Pitfall 4) |
| D-06-06-C | Bracket-spawned GameMatch.host_clan_id = NULL (A10 LOCKED) |
| D-06-07-A | Double-elim: SingleEliminationGenerator::layoutInStage extracted helper reused for W-bracket; L-bracket Burton mapping hardcoded for N=8 |
| D-06-07-B | Swiss: round 1 generated by generate(); round 2+ via SwissGenerator::generateNextRound (admin click — A6 LOCKED) |
| D-06-07-C | SwissTooFewParticipantsException when participants_count < 2^ceil(log2(N)) (Pitfall 5) |
| D-06-08-A | MatchResultObserver dispatches BracketAdvancementService::advance on every MatchResult save with winner_clan_id set (Pattern 7 Option A) |
| D-06-08-B | BracketAdvancementService routes through Tournament::lockForUpdate inside DB::transaction (Pitfall 6) |
| D-06-08-C | resolveSlot rule: from.position % 2 === 1 ? 'a' : 'b' (Pattern 7) |
| D-06-09-A | Round-robin points: FIFA 3/1/0 default; admin overrides via tournament.settings.roundrobin_points_per_{win,draw,loss} |
| D-06-09-B | Swiss tiebreak: plain Buchholz (sum of opponents' final scores) — RESEARCH Pattern 5 + A3 LOCKED |
| D-06-10-A | PublicTournamentData composes nodes[]+edges[] from tournament_brackets; etag = sha1(tournament.updated_at + brackets[id:updated_at] sorted) |
| D-06-10-B | TournamentObserver updates Discord outbound only on wasChanged('status') AND is_public (Pitfall 7) |
| D-06-11-A | spatie permission `tournament.manage` is admin-only (A8 LOCKED); web guard |
| D-06-11-B | Forfeit + withdraw row actions write activity_log with properties.reason in ('forfeit','withdraw') (A5 LOCKED) |
| D-06-12-A | /tournaments/{slug}.json under throttle:60,1; If-None-Match → 304 short-circuit (Pattern 9) |
| D-06-12-B | BracketCanvas.vue groups nodes by stage_type with separate <g> + cumulative stageYOffset (Pitfall 9) |
| D-06-13-A | Bot kinds: 3 distinct enums (tournament_announce, tournament_announce_update, bracket_result_announce) — Open Question 5 LOCKED |

## Test Inventory

(Concrete numbers filled in at exec time)

| Category | Phase 6 Test Files | Total Phase 6 Assertions |
|----------|-------------------|--------------------------|
| Models | 5 | ~25 |
| Services | 9 | ~50 |
| Observers | 2 | ~10 |
| Admin (Filament) | 7 | ~25 |
| Public (Inertia + JSON) | 4 | ~20 |
| I18n | 1 | ~10 |
| Unit/Data | 4 | ~15 |
| Bot Vitest | 1 (tournament.test.ts) | ~3 |
| **Total** | **33** | **~158** |

## Manual Smoke Items (PENDING_MANUAL_SMOKE blockers)

| # | Item | Owner | Verification |
|---|------|-------|--------------|
| A | Full single-elim 8-clan run through Filament + public viewing | Operator | Admin creates → registers → seeds → starts → walks 7 brackets to completion → public /tournaments/{slug} renders bracket SVG correctly + standings show rank 1..8 |
| B | Swiss 6-round dry run with Buchholz tiebreaks visible | Operator | Admin creates swiss tournament with 32 clans → ceil(log2(32))=5 rounds (note: VALIDATION says 6 rounds — for 64 clans → 6 rounds); after each round, the Generate Next Swiss Round button is visible + the resulting pairings respect Buchholz |
| C | Bracket SVG rendering at 4, 7, 8, 16 participants | Operator | Create 4 tournaments with these counts; visit /tournaments/{slug} bracket tab; verify SVG layout is readable + bye-rounds are visible |
| D | Bot announce on bracket creation (live Discord smoke) | Operator | Configure Discord bot with valid token; create + start a tournament; verify the announce channel receives the tournament_announce embed; finish a bracket match; verify bracket_result_announce embed posts to host clan's channel |

## Notes

- Phase 6 closes Open Questions A4, A5, A6, A8, and RESEARCH § Open Q5 LOCKED inline (see Open Questions table above).
- A1, A2, A3, A7 LOCKED as researcher-recommended defaults; no operator decision required.
- A9, A11 carried forward as Phase 9 polish items.
- Bot kinds extension (D-06-13-A) brings Phase 5 DiscordOutboundMessage outbox total to 6 message_type values: match_announce, match_announce_update, role_sync (Phase 5) + tournament_announce, tournament_announce_update, bracket_result_announce (Phase 6).
```

ROADMAP.md amendment shape (Phase 4-5 idiom):

```diff
- [ ] **Phase 6: Tournaments & brackets** — Run an end-to-end tournament with a bracket UI and standings.
+ [x] **Phase 6: Tournaments & brackets** — Run an end-to-end tournament with a bracket UI and standings. _Completed 2026-05-13_

# in the Phase 6 detail block:
- **Plans**: 14 plans
+ **Plans**: 14 plans
- [x] 06-01-PLAN.md — Wave 0 scaffolding (5 factories + 32 RED stubs + tournaments.php i18n + admin.php extension)
- [x] 06-02-PLAN.md — 5 migrations (tournaments + participants + stages + brackets + standings)
- [x] 06-03-PLAN.md — 5 models + factories + observer stub + 5 GREEN model tests
- [x] 06-04-PLAN.md — TournamentStatusService + 2 exceptions + GREEN test
- [x] 06-05-PLAN.md — TournamentSeedingService + canReseed() + A4 LOCKED + GREEN test
- [x] 06-06-PLAN.md — BracketGeneratorService + SingleEliminationGenerator + BracketMatchMaterialiserService + 2 GREEN tests
- [x] 06-07-PLAN.md — Double-elim + Round-robin + Swiss generators + Open Questions A6 + Pitfall 5 LOCKED + 3 GREEN tests
- [x] 06-08-PLAN.md — BracketAdvancementService + MatchResultObserver + 2 GREEN tests
- [x] 06-09-PLAN.md — StandingsCalculatorService (real impl) + 4 strategies + GREEN test
- [x] 06-10-PLAN.md — 8 DTOs + ParticipantSummary + TS regen + TournamentObserver real impl + 5 GREEN tests
- [x] 06-11-PLAN.md — TournamentResource Filament + 8 HeaderActions + 4 RelationManagers + 6 GREEN admin tests
- [x] 06-12-PLAN.md — Public controllers + Vue 5-tab Show + BracketCanvas SVG + useTournamentPolling + SC-1 capstone test
- [x] 06-13-PLAN.md — i18n + audit + bot embed extensions + 2+1 GREEN tests
- [x] 06-14-PLAN.md — [BLOCKING] phase verification + ROADMAP/REQUIREMENTS/STATE updates

# Progress table:
- | 6. Tournaments & brackets | 0/14 | Not started | - |
+ | 6. Tournaments & brackets | 14/14 | Complete | 2026-05-13 |
```

REQUIREMENTS.md amendment:

```diff
- [ ] **REQ-success-tournament-end-to-end**: An 8-clan single-elim tournament can be created, seeded, brackets generated, matches materialised, played, results captured, advancements computed — without admin patching.
+ [x] **REQ-success-tournament-end-to-end**: An 8-clan single-elim tournament can be created, seeded, brackets generated, matches materialised, played, results captured, advancements computed — without admin patching.

# traceability table:
- | REQ-success-tournament-end-to-end | Phase 6 | Pending |
+ | REQ-success-tournament-end-to-end | Phase 6 | Complete |

# footer:
+ *Last updated: 2026-05-13 — Phase 6 close: REQ-success-tournament-end-to-end flipped to Complete*
```

STATE.md amendment (Phase 5 D-05-13 idiom):

```diff
# header:
- stopped_at: "Phase 5 COMPLETE — ..."
+ stopped_at: "Phase 6 COMPLETE — 06-14 plan executed, 06-PHASE-VERIFICATION.md written, ROADMAP marked 14/14 Complete; Open Questions A4/A5/A6/A8/Q5 LOCKED inline; canonical D-06-* bindings recorded for Phase 7+."

# progress:
- completed_phases: 5
- completed_plans: 68
- percent: 56
+ completed_phases: 6
+ completed_plans: 82  # 68 + 14
+ percent: 67  # 6/9 * 100

# Current Position:
- Phase: 05 (Discord bot v1) — COMPLETE ...
+ Phase: 06 (Tournaments & brackets) — COMPLETE (PENDING_MANUAL_SMOKE — 4-item operator walkthrough A-D per 06-PHASE-VERIFICATION.md)

# Performance Metrics:
# (Append Phase 6 plan rows to the velocity table — actual exec durations filled in at runtime)

# Accumulated Decisions section:
# Append D-06-* canonical bindings (per <interfaces> verification report)
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase close traceability | The verification report is the SC-1..SC-5 audit; every claim is paired with a test file reference; gates must be GREEN before marking complete |
| ROADMAP/REQUIREMENTS/STATE atomicity | Three files updated in a single commit (or the SUMMARY.md serves as the rollback transcript) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-14-01 | Repudiation | Phase marked complete with failing tests | mitigate | Full Pest + Vitest + Pint + PHPStan + vue-tsc + shared-types-typecheck run in Task 1 BEFORE doc updates in Task 2; any failure blocks Task 2 |
| T-06-14-02 | Tampering | ROADMAP marked complete but Phase 7+ planner doesn't see the D-06-* bindings | mitigate | STATE.md `## Accumulated Decisions` section appended with all D-06-* IDs + their summary descriptions; Phase 7 plan-phase will read this section |
| T-06-14-03 | Repudiation | Manual smoke items A-D never get verified — phase ships with broken visual fidelity | accept | Status: PENDING_MANUAL_SMOKE — explicit flag that operator must walk through 4 items before declaring shippable; same idiom as Phase 1-5 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Full quality gate run — Pest + Vitest + Pint + PHPStan + vue-tsc + shared-types typecheck + bot tsc</name>
  <files>
  </files>
  <read_first>
    - .planning/phases/05-discord-bot-v1/05-13-PLAN.md (Phase 5 plan 05-13 reference — same idiom; quality gate sequence)
    - .planning/phases/04-matches-manual/04-13-PLAN.md (Phase 4 plan 04-13 — verbatim closing ceremony)
  </read_first>
  <acceptance_criteria>
    - `make pest` runs full suite → 0 failing tests; record total count + assertions count for the verification report
    - `make pint --test` exits 0
    - `make phpstan` exits 0 with no new errors against the existing baseline
    - `make pnpm ARGS="-F web vue-tsc"` exits 0
    - `pnpm --filter @trenchwars/shared-types typecheck` exits 0
    - `pnpm --filter @trenchwars/bot tsc --noEmit` exits 0
    - `pnpm --filter @trenchwars/bot test` (Vitest) exits 0
    - Capture the test counts into a temporary file for use in Task 2 (e.g., `.gates.txt`)
    - If ANY gate fails, DO NOT proceed to Task 2 — fix the regression first (Rule 1)
  </acceptance_criteria>
  <action>
    Run all 7 quality gates in sequence inside the container. Capture stdout for each gate; if any fails, surface the diff + stop. The verification report in Task 2 depends on these results.

    Commands:
    ```bash
    docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | tee /tmp/06-gates-pest.txt | tail -20
    docker compose exec web ./vendor/bin/pint --test 2>&1 | tee /tmp/06-gates-pint.txt | tail -5
    docker compose exec web ./vendor/bin/phpstan analyse --no-progress 2>&1 | tee /tmp/06-gates-phpstan.txt | tail -5
    cd /home/rtx/projects/trench-wars && pnpm --filter web exec vue-tsc --noEmit 2>&1 | tee /tmp/06-gates-vue-tsc.txt | tail -5
    cd /home/rtx/projects/trench-wars && pnpm --filter @trenchwars/shared-types typecheck 2>&1 | tee /tmp/06-gates-shared.txt | tail -5
    cd /home/rtx/projects/trench-wars && pnpm --filter @trenchwars/bot tsc --noEmit 2>&1 | tee /tmp/06-gates-bot-tsc.txt | tail -5
    cd /home/rtx/projects/trench-wars && pnpm --filter @trenchwars/bot test 2>&1 | tee /tmp/06-gates-bot-vitest.txt | tail -10
    ```

    Extract the test counts from the Pest output (the "Tests: N passed" line near the end). Record these for the verification report.

    If any gate exits non-zero, this task FAILS — debug the regression before authoring the report. Rule 1 deviations land here, not silenced.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | grep -E '(Tests:|FAIL)' | tail -3 && docker compose exec web ./vendor/bin/pint --test 2>&1 | tail -3 && docker compose exec web ./vendor/bin/phpstan analyse --no-progress 2>&1 | tail -3 && cd /home/rtx/projects/trench-wars && pnpm --filter web exec vue-tsc --noEmit 2>&1 | tail -5 && pnpm --filter @trenchwars/shared-types typecheck 2>&1 | tail -3 && pnpm --filter @trenchwars/bot tsc --noEmit 2>&1 | tail -3 && pnpm --filter @trenchwars/bot test 2>&1 | grep -E '(passed|failed)' | tail -3</automated>
  </verify>
  <done>All 7 quality gates GREEN; test counts captured for Task 2; any Rule 1 regressions fixed before proceeding.</done>
</task>

<task type="auto">
  <name>Task 2: Author 06-PHASE-VERIFICATION.md + amend ROADMAP.md + REQUIREMENTS.md + STATE.md</name>
  <files>
    .planning/phases/06-tournaments-brackets/06-PHASE-VERIFICATION.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md,
    .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md (Phase 4 close — canonical report shape)
    - .planning/phases/05-discord-bot-v1/05-PHASE-VERIFICATION.md (Phase 5 close — most recent canonical shape; canonical-bindings section format)
    - .planning/ROADMAP.md (current state — find Phase 6 row + plans list to update)
    - .planning/REQUIREMENTS.md (current state — find REQ-success-tournament-end-to-end + traceability table)
    - .planning/STATE.md (current state — find header progress block + Current Position + Accumulated Decisions)
  </read_first>
  <acceptance_criteria>
    - 06-PHASE-VERIFICATION.md authored using the verbatim Phase 5 idiom from <interfaces>:
      - YAML frontmatter (phase, slug, status=PENDING_MANUAL_SMOKE, completed=ISO date, plans_total=14, test counts, all 7 quality_gates=GREEN)
      - # heading + ## Status section
      - ## Success Criteria Traceability table (5 rows, one per SC)
      - ## Open Questions RESOLVED Inline During Planning table (5 rows: A4, A5, A6, A8, RESEARCH § Q5)
      - ## Pitfall Coverage Matrix table (12 rows, one per Pitfall 1-12)
      - ## RESEARCH Assumptions Status table (11 rows, A1..A11)
      - ## Canonical Phase 6 Bindings table (D-06-* IDs)
      - ## Test Inventory table (8 categories + total ~158 assertions)
      - ## Manual Smoke Items table (4 items A-D)
      - ## Notes section
    - ROADMAP.md amendment: Phase 6 row flipped from `[ ]` to `[x]`; "Completed YYYY-MM-DD" appended; Plans list checkboxes all set to `[x]`; "Plans: 14 plans" preserved; progress table row updated to "14/14 Complete <date>"
    - REQUIREMENTS.md amendment: REQ-success-tournament-end-to-end checkbox flipped; traceability table row updated to "Complete"; footer "Last updated" line added
    - STATE.md amendment: progress block (completed_phases=6, completed_plans=82, percent=67); stopped_at + Current Position updated; Accumulated Decisions section appended with D-06-* canonical bindings (~22 entries)
    - All 4 files preserve their existing structure + style (Markdown formatting, YAML indentation, Pint-style PHP arrays where applicable)
    - `git diff --stat .planning/` shows the 4 files modified + 1 new (PHASE-VERIFICATION)
  </acceptance_criteria>
  <action>
    Author 06-PHASE-VERIFICATION.md from the verbatim <interfaces> scaffold. Fill in:
    - The 5 SC-x test file references (resolved from plans 06-01..06-13 test fixtures)
    - The 11 Pitfall mitigations (resolved from each plan's threat_model section)
    - The 22+ D-06-* canonical bindings (resolved from each plan's must_haves + action sections)
    - The 4 manual smoke items (verbatim from 06-VALIDATION.md ## Manual-Only Verifications table)
    - The 7 quality gates GREEN status (from Task 1 captured output)
    - Test inventory with concrete numbers (from /tmp/06-gates-pest.txt count line)

    Amend ROADMAP.md in-place via Edit (NOT Write):
    - Find `[ ] **Phase 6: Tournaments & brackets**` → flip to `[x]` and append `_Completed YYYY-MM-DD_`
    - Find the Phase 6 detail block + Plans list → set ALL 14 plan checkboxes to `[x]` (correct any stray Phase 2 placeholder plan rows that are currently shown — RESEARCH already noted the ROADMAP Phase 6 entry was placeholder-populated; the 14 plan filenames + brief objective lines come from plans 06-01..06-14 just authored)
    - Find the progress table at the bottom → update Phase 6 row to "14/14 | Complete | YYYY-MM-DD"

    Amend REQUIREMENTS.md in-place:
    - Find `[ ] **REQ-success-tournament-end-to-end**` → flip to `[x]`
    - Find the traceability table `REQ-success-tournament-end-to-end | Phase 6 | Pending` row → flip to `Complete`
    - Update the footer "Last updated" line with the close date + Phase 6 reference

    Amend STATE.md in-place:
    - Update the YAML header: completed_phases=6, completed_plans=82 (was 68 + 14 = 82), percent=67 (6/9)
    - Update stopped_at string with the Phase 6 close summary
    - Update Current Position section (Phase: 06; Status: COMPLETE PENDING_MANUAL_SMOKE; Last activity: exec date)
    - Append performance metrics rows for plans 06-01..06-14 (placeholder durations; actual values filled at exec time)
    - Append Accumulated Decisions section with all 22 D-06-* canonical bindings (each as a one-line dash-bullet); use the same format as the existing D-04-* and D-05-* entries

    All amendments are surgical Edits (not Writes) — preserve existing content.

    Commit message convention (Phase 5 D-05-13 idiom): `docs(06): phase close — 14/14 complete; PHASE-VERIFICATION + ROADMAP + REQUIREMENTS + STATE`
  </action>
  <verify>
    <automated>ls -la .planning/phases/06-tournaments-brackets/06-PHASE-VERIFICATION.md && grep -c '\[x\] 06-' .planning/ROADMAP.md | awk '{ if ($1 < 14) { print "FAIL: not all 14 plans marked complete in ROADMAP ("$1")"; exit 1 } else { print "PASS: 14 plans marked complete" } }' && grep -c 'REQ-success-tournament-end-to-end | Phase 6 | Complete' .planning/REQUIREMENTS.md | awk '{ if ($1 != 1) { print "FAIL: REQ traceability not updated"; exit 1 } else { print "PASS" } }' && grep -c 'completed_phases: 6' .planning/STATE.md | awk '{ if ($1 != 1) { print "FAIL: STATE.md not updated"; exit 1 } else { print "PASS" } }' && grep -c 'D-06-' .planning/STATE.md | awk '{ if ($1 < 15) { print "FAIL: fewer than 15 D-06-* bindings in STATE.md ("$1")"; exit 1 } else { print "PASS D-06 count: "$1 } }'</automated>
  </verify>
  <done>06-PHASE-VERIFICATION.md authored; ROADMAP/REQUIREMENTS/STATE amended; 14 plans marked complete; REQ-success-tournament-end-to-end flipped to Complete; STATE.md has 15+ D-06-* canonical bindings recorded for Phase 7+ continuation.</done>
</task>

</tasks>

<verification>
- All 7 quality gates GREEN: Pest, Pint, PHPStan L8, vue-tsc, shared-types typecheck, bot tsc, bot vitest
- 06-PHASE-VERIFICATION.md present at .planning/phases/06-tournaments-brackets/06-PHASE-VERIFICATION.md
- ROADMAP.md Phase 6 row flipped to Complete; all 14 plan checkboxes set
- REQUIREMENTS.md REQ-success-tournament-end-to-end flipped to Complete
- STATE.md updated with completed_phases=6 + completed_plans=82 + percent=67 + Phase 6 D-06-* bindings appended
- Status: PENDING_MANUAL_SMOKE — 4 operator walkthrough items A-D documented
</verification>

<success_criteria>
Phase 6 (Tournaments & brackets) is closed. SC-1..SC-5 are mapped to GREEN tests. REQ-success-tournament-end-to-end is COMPLETE. All RESEARCH Pitfalls 1-12 have documented mitigations. Open Questions A4/A5/A6/A8/Q5 are LOCKED inline with rationale. The 22 D-06-* canonical bindings are in STATE.md ready for Phase 7+ planner consumption. The Phase 6 deliverable is shippable subject to operator visual + Discord live smoke (4 items A-D in PHASE-VERIFICATION.md). The next /gsd-plan-phase 7 invocation starts cleanly from STATE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tournaments-brackets/06-14-SUMMARY.md` documenting:
- Final Pest test count + assertion count
- Quality gate results (all 7 GREEN)
- 06-PHASE-VERIFICATION.md sections authored
- ROADMAP/REQUIREMENTS/STATE diffs applied
- 22+ D-06-* canonical bindings recorded
- 4 manual smoke items A-D documented
- Phase 6 sign-off: shippable PENDING_MANUAL_SMOKE
</output>
</content>
</invoke>
