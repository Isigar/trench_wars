---
phase: 05-discord-bot-v1
plan: 09
type: execute
wave: 7
depends_on: [08]
files_modified:
  - apps/bot/src/commands/index.ts
  - apps/bot/src/commands/clan.ts
  - apps/bot/src/commands/match.ts
  - apps/bot/src/commands/profile.ts
  - apps/bot/src/commands/me.ts
  - apps/bot/src/events/interactionCreate.ts
  - apps/bot/src/events/ready.ts
  - apps/bot/src/services/registerCommands.ts
  - apps/bot/src/index.ts
  - apps/bot/tests/commands/match.test.ts
  - apps/bot/tests/commands/clan.test.ts
  - apps/bot/tests/commands/profile.test.ts
autonomous: true
requirements:
  - REQ-goal-discord-ux
must_haves:
  truths:
    - "4 slash command modules under apps/bot/src/commands/: clan (info|list|apply subcommands), match (list|info|signup|leave subcommands), profile (look-up another user by Discord mention; PlayerPrivacyGate applied), me (current Discord user own profile; own-profile bypass)"
    - "Every slash command handler calls interaction.deferReply({flags: MessageFlags.Ephemeral}) as the FIRST awaited statement (Pitfall 1 — 3s window mitigation)"
    - "match signup subcommand invokes interaction.showModal(...) — does NOT defer first (Pitfall 1 corollary: modals must be initial response); plan 05-10 ships the modal builder + submit handler"
    - "commands/index.ts exports a Map keyed by command name -> {data, execute} module — canonical discord.js Guide pattern"
    - "registerCommands.ts uses REST + Routes.applicationGuildCommands(applicationId, guildId) to register all commands at boot (Pattern 3); idempotent — re-running on every boot re-syncs the registration"
    - "events/ready.ts is the canonical ready hook: calls registerCommands(client) + emits log; plan 05-11 will inject startOutboundWorker into this same hook"
    - "events/interactionCreate.ts is the central dispatcher: routes chat-input interactions to commands.get(name).execute(interaction); routes button + modal interactions to plan 05-10's component dispatcher (placeholder no-op until 05-10 ships)"
    - "apps/bot/src/index.ts is amended to wire ready + interactionCreate event handlers + call registerCommands inside ready"
    - "All command handlers fetch data via api.get with actsAsDiscordId set to interaction.user.id (SC-5 attribution)"
    - "3 Vitest stubs flip GREEN: match.test.ts (subcommand routing + deferReply assertion), clan.test.ts (info/list/apply routing), profile.test.ts (PlayerPrivacyGate respected — privacy-tier filtered fields)"
  artifacts:
    - path: "apps/bot/src/commands/match.ts"
      provides: "Slash command /match with list/info/signup/leave subcommands"
      contains: "deferReply"
    - path: "apps/bot/src/events/interactionCreate.ts"
      provides: "Central interaction dispatcher (chat input + buttons + modals)"
      contains: "ChatInputCommandInteraction"
    - path: "apps/bot/src/services/registerCommands.ts"
      provides: "Guild-scoped slash command registration on boot (Pattern 3)"
      contains: "applicationGuildCommands"
  key_links:
    - from: "apps/bot/src/events/interactionCreate.ts"
      to: "commands/{name}.ts execute(interaction)"
      via: "commands.get(name)?.execute(interaction) routing"
      pattern: "commands.get"
    - from: "apps/bot/src/commands/match.ts execute"
      to: "/api/bot/matches (plan 05-04)"
      via: "api.get + actsAsDiscordId header"
      pattern: "api.get"
---

<objective>
Wave 7: ship the slash command surface — /clan, /match, /profile, /me — that delivers SC-1 ("Discord user can invoke /clan info|list|apply, /match list|info|signup|leave, /profile, /me with correct, privacy-aware responses inside 3s interaction window via deferReply for slow paths").

Every command handler follows the same shape (RESEARCH Pattern 2 verbatim):
1. `await interaction.deferReply({ flags: MessageFlags.Ephemeral })` as the FIRST awaited statement (Pitfall 1)
2. Parse options via interaction.options.get*()
3. Call `api.get/post` from plan 05-08, passing `actsAsDiscordId: interaction.user.id`
4. Build response via plan 05-10's embed builders (deferred — this plan returns plain-text responses until 05-10 ships; SC-1 verifiable as plain text in 05-09 and gets the embed upgrade in 05-10)
5. `await interaction.editReply({...})` with the formatted response

The exception is `/match signup`: the slash handler invokes `interaction.showModal(modalBuilder)` — modals MUST be the initial response per discord.js docs (Pitfall 1 corollary). The modal builder + submit handler land in plan 05-10.

registerCommands runs once per boot inside the `ready` event. It uses `Routes.applicationGuildCommands(env.DISCORD_APPLICATION_ID, env.DISCORD_GUILD_ID)` (guild-scoped — Pattern 3; one league guild per D-003, instant propagation vs the 1h global delay).

interactionCreate is the single event handler. It branches on `interaction.isChatInputCommand()`, `interaction.isButton()`, `interaction.isModalSubmit()` — and routes each to the appropriate dispatcher. Buttons + modals get a placeholder console.warn until plan 05-10 ships their handlers.

Purpose: SC-1 (slash commands work inside 3s window with privacy-aware responses).

Output: 4 command modules + 2 event handlers + registerCommands service + index.ts wiring + 3 GREEN Vitest test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-discord-bot-v1/05-RESEARCH.md
@CLAUDE.md
@apps/bot/src/index.ts
@apps/bot/src/services/api.ts
@apps/bot/src/lib/customIds.ts
@apps/bot/src/lib/colors.ts
@apps/bot/src/env.ts
@apps/bot/src/types/apiContracts.ts
@packages/shared-types/src/index.ts

<interfaces>
Per-command module shape (apps/bot/src/commands/match.ts as the canonical example):

  import {
      SlashCommandBuilder,
      ChatInputCommandInteraction,
      MessageFlags,
      ModalBuilder,
      TextInputBuilder,
      TextInputStyle,
      ActionRowBuilder,
  } from 'discord.js';
  import { api } from '../services/api';
  import type { PublicMatchData } from '../types/apiContracts';

  export const data = new SlashCommandBuilder()
      .setName('match')
      .setDescription('Manage matches')
      .addSubcommand((sc) => sc.setName('list').setDescription('List open matches'))
      .addSubcommand((sc) => sc.setName('info').setDescription('Show match details')
          .addStringOption((o) => o.setName('id').setDescription('Match UUID').setRequired(true)))
      .addSubcommand((sc) => sc.setName('signup').setDescription('Sign up to a match (opens modal)')
          .addStringOption((o) => o.setName('id').setDescription('Match UUID').setRequired(true)))
      .addSubcommand((sc) => sc.setName('leave').setDescription('Leave a match slot')
          .addStringOption((o) => o.setName('id').setDescription('Match UUID').setRequired(true))
          .addStringOption((o) => o.setName('role').setDescription('Game role UUID').setRequired(true)));

  export async function execute(interaction: ChatInputCommandInteraction): Promise<void> {
      const sub = interaction.options.getSubcommand(true);

      // signup is the ONE subcommand that does NOT defer first — it opens a modal
      if (sub === 'signup') {
          const matchId = interaction.options.getString('id', true);
          const modal = new ModalBuilder()
              .setCustomId(`m:o:${matchId}`)
              .setTitle('Sign up to match')
              .addComponents(
                  new ActionRowBuilder<TextInputBuilder>().addComponents(
                      new TextInputBuilder()
                          .setCustomId('role')
                          .setLabel('Role UUID (from /match info)')
                          .setStyle(TextInputStyle.Short)
                          .setRequired(true),
                  ),
              );
          await interaction.showModal(modal);
          return;
      }

      // All other subcommands defer first
      await interaction.deferReply({ flags: MessageFlags.Ephemeral });

      if (sub === 'list') {
          const matches = await api.get<{ data: PublicMatchData[] }>(
              '/matches',
              { actsAsDiscordId: interaction.user.id },
          );
          // Plan 05-10 will replace this plain-text reply with an embed builder
          await interaction.editReply(formatMatchList(matches.data));
          return;
      }

      if (sub === 'info') {
          const matchId = interaction.options.getString('id', true);
          const match = await api.get<PublicMatchData>(
              `/matches/${matchId}`,
              { actsAsDiscordId: interaction.user.id },
          );
          await interaction.editReply(formatMatchInfo(match));
          return;
      }

      if (sub === 'leave') {
          const matchId = interaction.options.getString('id', true);
          const roleId = interaction.options.getString('role', true);
          try {
              await api.delete(`/matches/${matchId}/signups/${roleId}`, {
                  actsAsDiscordId: interaction.user.id,
              });
              await interaction.editReply('You have left the match.');
          } catch (err) {
              await interaction.editReply(`Failed to leave: ${(err as Error).message.slice(0, 200)}`);
          }
          return;
      }
  }

  // Plain-text formatters (replaced by embed builders in plan 05-10)
  function formatMatchList(matches: PublicMatchData[]): string {
      if (matches.length === 0) return 'No open matches.';
      return matches.map((m) => `- ${m.id} | ${m.status} | ${m.scheduled_at}`).join('\n');
  }

  function formatMatchInfo(m: PublicMatchData): string {
      return `Match ${m.id}\nStatus: ${m.status}\nScheduled: ${m.scheduled_at}\nHost clan id: ${m.host_clan_id ?? '—'}`;
  }

apps/bot/src/commands/clan.ts shape:

  Subcommands: info <slug>, list, apply <slug>

  - info: api.get(`/clans/${slug}`) — formats clan card (plain text v1)
  - list: api.get('/clans') — paginated list
  - apply: api.post(`/clans/${slug}/applications`, {actsAsDiscordId}) — RESEARCH notes /api/bot/clans/{id}/applications is "future v1+" so apply may return 501 or be commented out for v1; recommendation: ship the slash command stub but have it editReply with "Apply via the website at trenchwars.example.com/clans/{slug}" message until Phase 6+ ships the actual endpoint. KEEPS SC-1 stub in place; the apply subcommand existence satisfies "command exists" per ROADMAP SC-1; the redirect-to-web message is the v1 implementation per RESEARCH Open Question Q2 reasoning (Discord users who haven't logged in to web are blocked anyway)

apps/bot/src/commands/profile.ts shape:

  No subcommands; single options block

  /profile <@user> — required user mention

  Handler:
  1. deferReply ephemeral
  2. const targetUser = interaction.options.getUser('user', true)
  3. const profile = await api.get<{user: UserData, player: PublicPlayerData}>(`/users/by-discord/${targetUser.id}`)
  4. await interaction.editReply(formatProfile(profile))

  NOTE: /users/by-discord/{id} is NOT in plan 05-04's route list — it would need to be added. v1 alternative: use /api/bot/users/me with X-Bot-Acts-As-User pointing at targetUser.id; the privacy gate applies because subject != viewer (the viewing user is interaction.user.id, the subject is targetUser.id). However, ResolveBotActsAsUser middleware rebinds auth to targetUser; the privacy gate then sees subject=targetUser, viewer=targetUser (own-profile bypass) — WRONG.

  RESOLUTION for v1: ship /profile with a documented limitation: it shows the SAME data as /me because the bot-side acts-as semantics route through the target user, not the viewer. This satisfies "/profile command exists" but the privacy gate is degenerate.
  
  FUTURE: plan 05-12 + Phase 9 polish should add a /users/by-discord/{id} read endpoint that respects the VIEWER's identity (an extra header X-Bot-Viewer-Discord-User-Id) — out of scope v1.

  For v1 + this plan: ship /profile with X-Bot-Acts-As-User = interaction.user.id and a query string ?subject_discord_id=<targetUser.id>. Plan 05-12 task 1 ships the web-side /users/by-discord/{id} endpoint to make this clean. ALTERNATIVELY: ship /profile as a "coming soon" stub that editReply's with a "Use the website at trenchwars.example.com/players/<slug>" — v1 simplest path; SC-1 still satisfies "command exists with privacy-aware response (the redirect is privacy-aware by deferring to the website's gate)".

  RECOMMENDATION for execution: ship the redirect-stub v1 path; mark TODO in plan 05-12 to wire the proper viewer-aware endpoint.

apps/bot/src/commands/me.ts shape:

  No subcommands; no options

  Handler:
  1. deferReply ephemeral
  2. const me = await api.get<{user: UserData, player: PublicPlayerData}>('/users/me', {actsAsDiscordId: interaction.user.id})
  3. editReply(formatMe(me)) — own-profile bypass returns full data (Phase 2 plan 02-05 + plan 05-04 BotApiUserController::me confirmed)

apps/bot/src/commands/index.ts shape:

  import * as match from './match';
  import * as clan from './clan';
  import * as profile from './profile';
  import * as me from './me';

  type CommandModule = {
      data: { name: string; toJSON: () => unknown };
      execute: (interaction: ChatInputCommandInteraction) => Promise<void>;
  };

  export const commands = new Map<string, CommandModule>();
  commands.set(match.data.name, match);
  commands.set(clan.data.name, clan);
  commands.set(profile.data.name, profile);
  commands.set(me.data.name, me);

apps/bot/src/services/registerCommands.ts shape (RESEARCH Pattern 3):

  import { REST, Routes } from 'discord.js';
  import { commands } from '../commands';
  import { env } from '../env';

  export async function registerCommands(): Promise<void> {
      const rest = new REST({ version: '10' }).setToken(env.DISCORD_BOT_TOKEN);
      const body = Array.from(commands.values()).map((c) => c.data.toJSON());
      await rest.put(
          Routes.applicationGuildCommands(env.DISCORD_APPLICATION_ID, env.DISCORD_GUILD_ID),
          { body },
      );
      console.log(`[bot] Registered ${body.length} slash commands to guild ${env.DISCORD_GUILD_ID}`);
  }

apps/bot/src/events/ready.ts shape:

  import { Events, Client } from 'discord.js';
  import { registerCommands } from '../services/registerCommands';

  export function registerReadyHandler(client: Client): void {
      client.once(Events.ClientReady, async (c) => {
          console.log(`[bot] Logged in as ${c.user.tag}`);
          try {
              await registerCommands();
          } catch (err) {
              console.error('[bot] Failed to register commands:', err);
          }
          // plan 05-11 will inject startOutboundWorker() here
      });
  }

apps/bot/src/events/interactionCreate.ts shape:

  import { Events, Client, MessageFlags } from 'discord.js';
  import { commands } from '../commands';

  export function registerInteractionHandler(client: Client): void {
      client.on(Events.InteractionCreate, async (interaction) => {
          try {
              if (interaction.isChatInputCommand()) {
                  const cmd = commands.get(interaction.commandName);
                  if (cmd === undefined) {
                      await interaction.reply({ content: 'Unknown command.', flags: MessageFlags.Ephemeral });
                      return;
                  }
                  await cmd.execute(interaction);
                  return;
              }

              // Pitfall 11: modal submit's 3s window restarts
              if (interaction.isModalSubmit()) {
                  await interaction.deferReply({ flags: MessageFlags.Ephemeral });
                  // plan 05-10 will dispatch to signupModal.handleSubmit(interaction)
                  await interaction.editReply('Modal submit handlers ship in plan 05-10.');
                  return;
              }

              // Button + select-menu interactions
              if (interaction.isButton() || interaction.isStringSelectMenu()) {
                  await interaction.deferReply({ flags: MessageFlags.Ephemeral });
                  // plan 05-10 will dispatch via decodeButtonId routing
                  await interaction.editReply('Component handlers ship in plan 05-10.');
                  return;
              }
          } catch (err) {
              console.error('[bot/interactionCreate]', err);
              // Best-effort error reply; ignore if interaction was already deferred and 3s passed
              if (interaction.isRepliable() && !interaction.replied && !interaction.deferred) {
                  await interaction.reply({ content: 'An error occurred.', flags: MessageFlags.Ephemeral }).catch(() => {});
              } else if (interaction.deferred) {
                  await interaction.editReply('An error occurred.').catch(() => {});
              }
          }
      });
  }

apps/bot/src/index.ts amendment:

  Add imports for registerReadyHandler + registerInteractionHandler.
  Replace the existing Events.ClientReady inline handler with registerReadyHandler(client).
  Add registerInteractionHandler(client) after that.

  Final shape:
      const client = createClient();
      registerReadyHandler(client);
      registerInteractionHandler(client);
      process.on('uncaughtException', ...);
      process.on('unhandledRejection', ...);
      await client.login(env.DISCORD_BOT_TOKEN);

Test enumeration (3 Vitest files):

match.test.ts:
- it('exports data with name=match and 4 subcommands (list/info/signup/leave)')
- it('execute calls deferReply with MessageFlags.Ephemeral for list subcommand')
- it('execute calls api.get(/matches) for list subcommand')
- it('execute calls api.get(/matches/{id}) for info subcommand')
- it('execute calls api.delete(/matches/{id}/signups/{role}) for leave subcommand')
- it('execute calls interaction.showModal for signup subcommand (no defer)')
- it('actsAsDiscordId is set to interaction.user.id on all api calls')

clan.test.ts:
- it('exports data with name=clan and 3 subcommands (info/list/apply)')
- it('info subcommand calls api.get(/clans/{slug})')
- it('list subcommand calls api.get(/clans)')
- it('apply subcommand emits the redirect-to-web message — v1 implementation')

profile.test.ts:
- it('exports data with name=profile and user option required')
- it('execute calls deferReply with MessageFlags.Ephemeral')
- it('execute emits redirect-to-web message OR calls api.get(/users/by-discord/{id}) — implementation choice documented')

Vitest test setup uses mocked discord.js types — the interaction object is a vi.fn()-shaped mock with .deferReply / .editReply / .showModal / .options.getString. The api object from services/api.ts is module-level — use vi.mock('../../src/services/api') to replace it with a mocked module exposing get/post/delete spies.

Mock setup example:
  import { vi } from 'vitest';
  vi.mock('../../src/services/api', () => ({
      api: {
          get: vi.fn().mockResolvedValue({data: []}),
          post: vi.fn().mockResolvedValue({}),
          delete: vi.fn().mockResolvedValue({}),
      },
  }));
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Discord gateway interaction -> command handler | 3s window for first response (defer); 15min for editReply (Pitfall 1) |
| Slash command options -> api.get/post body | All options validated by Discord SDK shape (required/optional, type); options.getString returns string-shaped values |
| interaction.user.id -> X-Bot-Acts-As-User header | Discord-supplied user id; assumed trustworthy because Discord gateway authenticates the user |
| Modal submit -> 3s window | Pitfall 11 — defer modal submit at the dispatcher level; restarts the 3s clock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | DoS | Slash command spam from one user | mitigate | Discord per-user rate limits (5/sec); plan 05-04's throttle:bot,60,1 on web API |
| T-05-09-02 | Tampering | Command handler forgets to deferReply -> 3s window expires, user sees no response | mitigate | Test asserts deferReply is called as first awaited statement; eslint rule could also enforce |
| T-05-09-03 | Tampering | Modal flow loses signup data because submit handler in plan 05-10 not yet shipped | mitigate | interactionCreate.ts ships placeholder editReply explaining the deferred wiring; SC-1 partial — modal SHOWS but signup logic completes in plan 05-10 |
| T-05-09-04 | Information Disclosure | /profile leaks private fields by routing acts-as to target user | mitigate | v1 ships redirect-to-web message OR uses /users/me with own-profile bypass behaviour disclosed — the "limitation" is the privacy gate's CURRENT correctness boundary |
| T-05-09-05 | Spoofing | Bot registers commands to wrong guild | mitigate | env.DISCORD_GUILD_ID is fail-fast at boot; D-003 single guild |
| T-05-09-06 | DoS | registerCommands fails -> bot keeps running but commands don't show | mitigate | try/catch + console.error inside ready handler; operator sees the failure in logs and re-issues |
| T-05-09-07 | Information Disclosure | err.message leaks token in editReply error path | mitigate | err.message is already scrubbed by plan 05-08 api.ts (Pitfall 3); editReply only relays the scrubbed message |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 4 command modules + commands/index.ts registry + registerCommands service + ready event handler</name>
  <files>
    apps/bot/src/commands/index.ts,
    apps/bot/src/commands/clan.ts,
    apps/bot/src/commands/match.ts,
    apps/bot/src/commands/profile.ts,
    apps/bot/src/commands/me.ts,
    apps/bot/src/services/registerCommands.ts,
    apps/bot/src/events/ready.ts
  </files>
  <read_first>
    - apps/bot/src/services/api.ts (plan 05-08 — verify request/get/post/delete signatures)
    - apps/bot/src/env.ts (plan 05-08 — verify DISCORD_APPLICATION_ID + DISCORD_GUILD_ID available)
    - apps/bot/src/types/apiContracts.ts (plan 05-08 — verify which DTOs available for typing)
    - apps/bot/src/lib/customIds.ts (plan 05-08 — verify encodeButtonId variants for the signup modal customId)
    - .planning/phases/05-discord-bot-v1/05-RESEARCH.md (Pattern 2 verbatim slash command shape; Pattern 3 registerCommands; Pitfall 1 + Pitfall 11)
    - Context7 for discord.js SlashCommandBuilder + ModalBuilder + ChatInputCommandInteraction IF UNCERTAIN: mcp__context7__query-docs library /websites/discord_js_packages_discord_js_14_26_2 query "SlashCommandBuilder addSubcommand showModal MessageFlags"
  </read_first>
  <acceptance_criteria>
    - apps/bot/src/commands/match.ts:
      - exports `data` (SlashCommandBuilder configured for 4 subcommands list/info/signup/leave with required string options)
      - exports `execute(interaction): Promise<void>`
      - signup branch: NO deferReply; calls interaction.showModal with customId `m:o:{matchId}` per `<interfaces>`
      - other branches: deferReply ephemeral FIRST; then api call; then editReply
      - actsAsDiscordId set to interaction.user.id on all api calls
      - plain-text formatMatchList + formatMatchInfo helpers (deferred to plan 05-10 for embed upgrade)
    - apps/bot/src/commands/clan.ts:
      - exports data with 3 subcommands info/list/apply
      - info uses string option 'slug'
      - apply emits redirect-to-web message (v1 implementation per Q2 reasoning)
    - apps/bot/src/commands/profile.ts:
      - exports data with required user option 'user'
      - execute defers + emits redirect-to-web message (v1 stub) OR documented alternative (planner picks one)
    - apps/bot/src/commands/me.ts:
      - exports data with no options
      - execute defers + calls api.get('/users/me', {actsAsDiscordId: interaction.user.id})
    - apps/bot/src/commands/index.ts:
      - exports `commands: Map<string, CommandModule>` per `<interfaces>`
      - 4 commands registered
    - apps/bot/src/services/registerCommands.ts per `<interfaces>` — uses REST + Routes.applicationGuildCommands; emits log on success
    - apps/bot/src/events/ready.ts per `<interfaces>` — exports registerReadyHandler(client)
    - tsc --noEmit clean
    - eslint clean
  </acceptance_criteria>
  <action>
    Build each command module from the verbatim shape in `<interfaces>`. The match.ts is the most complex (4 subcommands + modal flow); the others (clan, profile, me) are simpler.

    Critical implementation rules:
    1. Pitfall 1: EVERY non-modal handler MUST call interaction.deferReply() as the first awaited statement
    2. Pitfall 1 corollary: the signup modal flow MUST NOT defer before showModal — modals must be the first response
    3. actsAsDiscordId is always interaction.user.id (Discord-supplied; trust the gateway auth)
    4. error handling: catch in interactionCreate.ts (task 2); commands let exceptions propagate

    Build commands/index.ts as a discord.js Map keyed by command name. The CommandModule type alias is local — does NOT need export from a shared types module.

    Build registerCommands.ts as a pure function. Called once at boot from ready.ts; idempotent.

    Build ready.ts as a single function registerReadyHandler(client) that wires Events.ClientReady. The handler awaits registerCommands; on failure logs but doesn't crash (try/catch).

    Run docker compose exec bot pnpm run typecheck + pnpm run lint.
  </action>
  <verify>
    <automated>docker compose exec bot pnpm run typecheck 2>&amp;1 | tail -5 &amp;&amp; docker compose exec bot pnpm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <done>4 command modules + commands/index.ts + registerCommands + ready handler committed; tsc + eslint clean; deferReply pattern verified by visual code review.</done>
</task>

<task type="auto">
  <name>Task 2: events/interactionCreate.ts (central dispatcher) + apps/bot/src/index.ts amendment to wire ready + interactionCreate</name>
  <files>
    apps/bot/src/events/interactionCreate.ts,
    apps/bot/src/index.ts
  </files>
  <read_first>
    - apps/bot/src/index.ts (plan 05-08 — current boot script; preserve fail-fast env + login)
    - apps/bot/src/commands/index.ts (just-shipped from task 1)
    - apps/bot/src/events/ready.ts (just-shipped from task 1)
    - .planning/phases/05-discord-bot-v1/05-RESEARCH.md (Pitfall 11 — modal submit's 3s window restarts)
    - Context7 for discord.js Interaction type guards IF UNCERTAIN: mcp__context7__query-docs library /websites/discord_js_packages_discord_js_14_26_2 query "Interaction isChatInputCommand isButton isModalSubmit isStringSelectMenu"
  </read_first>
  <acceptance_criteria>
    - apps/bot/src/events/interactionCreate.ts:
      - exports registerInteractionHandler(client: Client): void per `<interfaces>`
      - Routes chat-input via commands.get(name).execute
      - Routes modal submit + buttons + string select menus with deferReply + placeholder editReply
      - Top-level try/catch with best-effort error reply
      - Unknown command -> ephemeral reply with "Unknown command."
    - apps/bot/src/index.ts amended:
      - imports registerReadyHandler + registerInteractionHandler
      - calls both before client.login
      - preserves existing process.on uncaughtException + unhandledRejection handlers
      - preserves top-level await client.login(env.DISCORD_BOT_TOKEN)
      - removes the placeholder ClientReady handler from plan 05-08 (now provided by ready.ts)
    - tsc --noEmit clean
    - eslint clean
  </acceptance_criteria>
  <action>
    Build interactionCreate.ts verbatim from `<interfaces>`. Critical:
    - isChatInputCommand branch: lookup command; if undefined, ephemeral reply "Unknown command"; else execute
    - isModalSubmit branch: deferReply ephemeral FIRST (Pitfall 11), then placeholder editReply (plan 05-10 wires real dispatch)
    - isButton OR isStringSelectMenu: same pattern
    - Top-level try/catch: best-effort error reply with appropriate state check (already-replied? already-deferred?)

    Amend index.ts: REPLACE the inline Events.ClientReady handler from plan 05-08 with the new register*Handler calls. Final order: createClient -> registerReadyHandler -> registerInteractionHandler -> process.on handlers -> client.login.

    Run docker compose exec bot pnpm run typecheck + pnpm run lint.

    Smoke run: docker compose restart bot -> docker compose logs --tail=20 bot — should show "Logged in as ..." + "Registered N slash commands" if real env credentials are present. If env credentials are empty (test fixture), expect a known failure mode (bot can't login); acceptable for unit-level smoke.
  </action>
  <verify>
    <automated>docker compose exec bot pnpm run typecheck 2>&amp;1 | tail -5 &amp;&amp; docker compose exec bot pnpm run lint 2>&amp;1 | tail -5</automated>
  </verify>
  <done>interactionCreate.ts + index.ts wiring committed; tsc + eslint clean; modal submit defer pattern present (Pitfall 11); error handler has state-aware best-effort reply.</done>
</task>

<task type="auto">
  <name>Task 3: 3 Vitest tests GREEN — match.test.ts + clan.test.ts + profile.test.ts (command dispatch + deferReply assertion + api call routing)</name>
  <files>
    apps/bot/tests/commands/match.test.ts,
    apps/bot/tests/commands/clan.test.ts,
    apps/bot/tests/commands/profile.test.ts
  </files>
  <read_first>
    - apps/bot/src/commands/match.ts (just-shipped from task 1)
    - apps/bot/src/commands/clan.ts (just-shipped from task 1)
    - apps/bot/src/commands/profile.ts (just-shipped from task 1)
    - apps/bot/src/services/api.ts (plan 05-08 — for vi.mock target)
    - apps/bot/vitest.config.ts (Phase 1 — verify configuration)
    - .planning/phases/05-discord-bot-v1/05-RESEARCH.md (Vitest stub idiom; test enumeration per command)
  </read_first>
  <acceptance_criteria>
    - match.test.ts REPLACES Wave 0 stub:
      - 7+ it() blocks per `<interfaces>` enumeration
      - vi.mock for services/api with get/post/delete spies
      - Mock interaction object with: user.id, options.getSubcommand, options.getString, deferReply (spy), editReply (spy), showModal (spy)
      - Assertions verify: deferReply called with MessageFlags.Ephemeral; api.get called with correct path + actsAsDiscordId; showModal called for signup branch
    - clan.test.ts REPLACES Wave 0 stub:
      - 4+ it() blocks per `<interfaces>` enumeration
      - apply branch test verifies the redirect-to-web message text (or whatever v1 implementation was picked)
    - profile.test.ts REPLACES Wave 0 stub:
      - 3+ it() blocks per `<interfaces>` enumeration
    - All it.todo removed; replaced with real it() blocks
    - docker compose exec bot pnpm test passes for these 3 files; ZERO failing test; ZERO todo entries for the 3 files
  </acceptance_criteria>
  <action>
    Build each test file using Vitest + vi.mock pattern. Mock interaction shape:

      const mockInteraction = {
          user: { id: '100000000000000001' },
          options: {
              getSubcommand: vi.fn().mockReturnValue('list'),
              getString: vi.fn().mockReturnValue('match-uuid-here'),
              getUser: vi.fn().mockReturnValue({id: '999...'}),
          },
          deferReply: vi.fn().mockResolvedValue(undefined),
          editReply: vi.fn().mockResolvedValue(undefined),
          showModal: vi.fn().mockResolvedValue(undefined),
          reply: vi.fn().mockResolvedValue(undefined),
          isRepliable: () => true,
          replied: false,
          deferred: false,
      } as unknown as ChatInputCommandInteraction;

    For each it() block:
    1. Setup the mock to return the desired subcommand value
    2. Call execute(mockInteraction)
    3. Assert spies were called with expected args

    Key assertions:
    - expect(mockInteraction.deferReply).toHaveBeenCalledWith({flags: MessageFlags.Ephemeral})
    - expect(apiGetSpy).toHaveBeenCalledWith('/matches', {actsAsDiscordId: '100000000000000001'})
    - expect(mockInteraction.showModal).toHaveBeenCalled() // for signup branch

    Run docker compose exec bot pnpm test.
  </action>
  <verify>
    <automated>docker compose exec bot pnpm test 2>&amp;1 | grep -E '(match\.test|clan\.test|profile\.test|Tests|todo)' | tail -10</automated>
  </verify>
  <done>3 GREEN Vitest files; 14+ assertions; deferReply pattern verified at test level; api call routing verified; Vitest baseline drops by 3 todos.</done>
</task>

</tasks>

<verification>
- docker compose exec bot pnpm run typecheck -> clean
- docker compose exec bot pnpm run lint -> clean
- docker compose exec bot pnpm test 2>&1 | grep -E '(match|clan|profile).*pass' | wc -l -> at least 3
- grep -c 'deferReply' apps/bot/src/commands/*.ts -> at least 4 (one per non-modal command branch)
- grep -c 'showModal' apps/bot/src/commands/match.ts -> 1 (signup branch)
- Wave 0 baseline drops by 3 (match.test.ts + clan.test.ts + profile.test.ts now GREEN)
</verification>

<success_criteria>
SC-1 verifiable at unit-test level: every slash command defers within 3s (Pitfall 1); api calls route through plan 05-08's typed client with X-Bot-Acts-As-User header (SC-5); /match signup invokes the modal flow (plan 05-10 completes the modal submit handler). Live Discord smoke (VALIDATION.md manual) confirms end-to-end.
</success_criteria>

<output>
After completion, create .planning/phases/05-discord-bot-v1/05-09-SUMMARY.md documenting:
- 4 command module subcommand table (clan/info-list-apply, match/list-info-signup-leave, profile/<user>, me)
- deferReply pattern verification (every non-modal handler defers first)
- signup modal flow: showModal in command -> dispatched to plan 05-10 submit handler
- registerCommands.ts: guild-scoped registration (Pattern 3) — applicationGuildCommands not applicationCommands
- interactionCreate.ts dispatch table (chat-input -> commands map; button/modal -> 05-10 placeholder)
- Open question Q5 resolution: /me uses own-profile bypass; /profile redirect-to-web v1 stub (limitations documented)
- Open question Q2 resolution: /clan apply redirect-to-web v1 stub
- Vitest baseline impact (3 todos -> green)
</output>
</content>
