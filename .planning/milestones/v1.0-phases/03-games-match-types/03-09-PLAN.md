---
phase: 03-games-match-types
plan: 09
type: execute
wave: 6
depends_on: [01, 03, 06, 07, 08]
files_modified:
  - apps/web/lang/en/admin.php
  - apps/web/tests/Feature/Admin/GameAuditLogTest.php
autonomous: true
requirements:
  - REQ-platform-vision
must_haves:
  truths:
    - "Every t()/__() reference in Phase 3 Filament resources (GameResource, GameMatchTypeResource, all 3 RelationManagers, all Pages) has a matching key in lang/en/admin.php (NoHardcodedStringsTest GREEN + manual key-presence audit)"
    - "GameAuditLogTest GREEN: Game/GameRole/GameMatchType/GameMatchTypeRoleLimit Filament creates write activity_log rows with correct subject_type (D-012)"
    - "audit.subject mapping in admin.php includes all 4 Phase 3 model FQNs so the audit Page renders human-readable labels"
    - "Any keys missing from plan 03-01's placeholder set are added with sensible English copy"
  artifacts:
    - path: "apps/web/lang/en/admin.php"
      provides: "Phase 3 i18n key coverage — final audit"
      contains: "App\\\\Models\\\\Game"
    - path: "apps/web/tests/Feature/Admin/GameAuditLogTest.php"
      provides: "D-012 activity log integration coverage for 4 Phase 3 models"
      contains: "subject_type"
  key_links:
    - from: "Phase 3 Filament resources"
      to: "lang/en/admin.php key coverage"
      via: "grep audit + manual fill"
      pattern: "__\\('admin\\.game"
    - from: "Phase 3 model creates"
      to: "activity_log table"
      via: "LogsActivity trait via Spatie\\Activitylog\\Models\\Activity"
      pattern: "Activity::query"
---

<objective>
Finalize the Phase 3 i18n key audit and verify D-012 activity log integration on all 4 Phase 3 models from within the admin panel surface. The i18n audit catches any `__()` call in Phase 3 plans 03-06/03-07 that references a key absent from plan 03-01's placeholder set; the activity log test proves that admin actions on Game/GameRole/GameMatchType/GameMatchTypeRoleLimit produce audit rows via the LogsActivity trait wired in plan 03-03.

Purpose: D-013 requires every UI string flow through `__()`/`t()`. The NoHardcodedStringsTest (Phase 1 plan 01-08) already enforces this at the test layer for new code, but does NOT catch a `__()` call referencing a missing key — Laravel silently returns the key itself when a translation is absent, which looks like "admin.game.fields.invalid_key" in the UI. This audit explicitly checks every Phase 3 __() reference has a corresponding entry in admin.php.

The activity log test is the explicit D-012 contract proof — Filament admin creates produce audit log rows. Phase 1 plan 01-14 wired the activity log infrastructure; plan 03-03 added LogsActivity to all 4 Phase 3 models; this test exercises the end-to-end pipe.

Output: lang/en/admin.php finalized (any missing keys filled); GameAuditLogTest.php GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-games-match-types/03-RESEARCH.md
@CLAUDE.md
@apps/web/lang/en/admin.php
@apps/web/app/Filament/Resources/GameResource.php
@apps/web/app/Filament/Resources/GameMatchTypeResource.php
@apps/web/app/Filament/Resources/GameResource/RelationManagers/RolesRelationManager.php
@apps/web/app/Filament/Resources/GameResource/RelationManagers/MatchTypesRelationManager.php
@apps/web/app/Filament/Resources/GameMatchTypeResource/RelationManagers/RoleLimitsRelationManager.php

<interfaces>
i18n key audit approach (Phase 2 plan 02-14 task 2 pattern):
- Extract all `__('admin.game...')` calls via: `grep -rohE "__\('admin\.[a-z._]+'" apps/web/app/Filament/Resources/GameResource.php apps/web/app/Filament/Resources/GameResource apps/web/app/Filament/Resources/GameMatchTypeResource.php apps/web/app/Filament/Resources/GameMatchTypeResource | sort -u`
- For each key, verify it resolves via a `require` of `apps/web/lang/en/admin.php` + nested array walk
- Any missing keys: add with sensible English copy

Activity log test pattern (Phase 1 plan 01-14 + Phase 2 ClanModelTest verified):
- `use Spatie\Activitylog\Models\Activity;`
- After creating a model: `Activity::query()->where('subject_type', \App\Models\Game::class)->where('event', 'created')->exists() === true`
- The causer is the acting user (auth()->user()) which is set via actingAs() in the test setup

Approach: this test simulates an authenticated admin (matching plan 03-08's beforeEach) then directly creates a Game/GameRole/GameMatchType/GameMatchTypeRoleLimit via Eloquent and asserts the activity_log row exists with the expected subject_type + event + causer_id. This is integration-layer proof of D-012 for Phase 3 models — it does NOT exercise the Filament UI (that's plan 03-08's reachability scope), it exercises the trait → DB pipe.

NB: the cross-game saving() listener in GameMatchTypeRoleLimit (plan 03-03) requires same-game setup; the activity log test constructs same-game fixtures explicitly per the same pattern as plan 03-03 task 3 cross-game test (just inverted: success case here, failure case there).
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 3 source code __() references to lang/en/admin.php | Missing key shows as raw key string in UI; Laravel silently fails — only an explicit audit catches it |
| Filament admin write to activity_log | LogsActivity trait writes; audit Page (Phase 1 plan 01-14) reads — test exercises the write half |
| Audit row subject_type field | LogsActivity uses morphMap or full FQN; test asserts FQN match per spatie/activitylog default |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09-01 | Information Disclosure | Untranslated i18n key shows as raw string ("admin.game.fields.xyz") in admin UI | mitigate | Explicit grep audit + array walk in task 1; any missing keys added with sensible EN copy |
| T-03-09-02 | Repudiation | Filament admin action unaudited (LogsActivity not firing) | mitigate | Task 2 test asserts activity_log row exists with correct subject_type + event + causer_id for all 4 Phase 3 models |
| T-03-09-03 | Information Disclosure | activity_log readable by non-admin | accept | Phase 1 plan 01-14 audit Page is admin-only; not exposed to public; the activity_log row itself is just a DB record |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: i18n key coverage audit on Phase 3 Filament source + fill any missing keys in admin.php</name>
  <files>apps/web/lang/en/admin.php</files>
  <read_first>
    - apps/web/lang/en/admin.php (current state from plan 03-01's placeholder extension)
    - apps/web/app/Filament/Resources/GameResource.php (audit __() calls)
    - apps/web/app/Filament/Resources/GameMatchTypeResource.php (audit __() calls)
    - apps/web/app/Filament/Resources/GameResource/RelationManagers/RolesRelationManager.php
    - apps/web/app/Filament/Resources/GameResource/RelationManagers/MatchTypesRelationManager.php
    - apps/web/app/Filament/Resources/GameMatchTypeResource/RelationManagers/RoleLimitsRelationManager.php
    - apps/web/app/Filament/Resources/GameResource/Pages/* + GameMatchTypeResource/Pages/* (any __() in mutators or page-level overrides)
    - .planning/phases/02-clans-tags/02-14-PLAN.md task 2 (Phase 2 i18n audit pattern)
  </read_first>
  <acceptance_criteria>
    - Run grep extraction: `grep -rohE "__\('admin\.[a-z._]+'" apps/web/app/Filament/Resources/GameResource.php apps/web/app/Filament/Resources/GameResource apps/web/app/Filament/Resources/GameMatchTypeResource.php apps/web/app/Filament/Resources/GameMatchTypeResource | sort -u` — produces the canonical list of admin.* keys referenced by Phase 3 sources
    - For each extracted key: verify it resolves via `php -r "require 'apps/web/lang/en/admin.php'; $keys = require ...; foreach ($parts as $p) { $v = $v[$p] ?? null; ... }"` — same shape as plan 03-01 task 2 verify command, but iterating ALL extracted keys
    - Any missing keys added to apps/web/lang/en/admin.php with sensible English copy. Plausible candidates (verify against actual grep output):
      - `admin.game.section.audit` (if audit tab Placeholder uses a section header)
      - `admin.game_match_type.fields.game` (if not already added by plan 03-01)
      - `admin.tab.profile` / `admin.tab.audit` (cross-cutting tab labels — likely already present from Phase 2 plan 02-12)
      - `admin.game.fields.is_active` / `admin.game_role.fields.is_active` / `admin.game_match_type.fields.is_active` (already in plan 03-01 placeholder set)
    - audit.subject map verified: `App\Models\Game`, `App\Models\GameRole`, `App\Models\GameMatchType`, `App\Models\GameMatchTypeRoleLimit` all present (added by plan 03-01 task 2 acceptance — verify)
    - `make pest ARGS="--filter=I18n"` GREEN (existing NoHardcodedStringsTest still passes; no new key references break)
    - `make phpstan` clean (admin.php is pure array; no L8 surface change)
  </acceptance_criteria>
  <action>
    Run the grep extraction inside the web container or on the host — same regex. Save the output to memory; the list typically has 30-60 keys.

    For each unique key in the grep output: write a quick PHP one-liner that walks the admin.php nested array and reports MISSING for any key with no resolution. The Phase 2 plan 02-14 task 2 used a similar audit approach (grep + manual walk).

    For any MISSING key: open admin.php, locate the appropriate group (e.g. admin.game, admin.game_match_type), add the missing sub-key with English copy that matches the surrounding field's tone (e.g. if `admin.game.help.key_format` is missing, add it with 'Lowercase letters, digits, and underscores only.' matching plan 03-01's placeholder copy).

    For the audit.subject map: open admin.php, locate the `audit.subject` array (Phase 1 plan 01-14 wired this; Phase 2 plan 02-13 extended for clan resources). Confirm Phase 3 model FQNs (`App\Models\Game`, etc.) are present per plan 03-01 task 2 acceptance. If missing, add with EN copy: `App\Models\Game => 'Game'`, etc.

    Run `make pest ARGS="--filter=I18n"` and `make pint`. Run `make phpstan`.
  </action>
  <verify>
    <automated>docker compose exec web bash -c "grep -rohE \"__\('admin\.[a-z._]+'\" app/Filament/Resources/GameResource.php app/Filament/Resources/GameResource app/Filament/Resources/GameMatchTypeResource.php app/Filament/Resources/GameMatchTypeResource 2>/dev/null | sed -E \"s/__\('(.*)'/\\1/\" | sort -u | while read k; do php -r \"\\\$a = require 'lang/en/admin.php'; \\\$p = explode('.', '\$k'); array_shift(\\\$p); \\\$v = \\\$a; foreach (\\\$p as \\\$s) { \\\$v = \\\$v[\\\$s] ?? null; if (\\\$v === null) { echo \\\"MISSING: \$k\n\\\"; exit; } } \"; done" 2>&1 | grep MISSING | head -10</automated>
  </verify>
  <done>All __('admin.*') keys referenced by Phase 3 sources resolve in lang/en/admin.php; audit.subject map includes 4 Phase 3 model FQNs.</done>
</task>

<task type="auto">
  <name>Task 2: GameAuditLogTest — assert activity_log writes on Game/GameRole/GameMatchType/GameMatchTypeRoleLimit creates (D-012)</name>
  <files>apps/web/tests/Feature/Admin/GameAuditLogTest.php</files>
  <read_first>
    - apps/web/tests/Feature/Models/ClanModelTest.php (Phase 2 analog — activity log assertion pattern using Spatie\Activitylog\Models\Activity::query()->where(...))
    - apps/web/tests/Feature/Admin/ClanResourcesPresentTest.php (beforeEach pattern with PermissionSeeder + admin actingAs)
    - apps/web/app/Models/Game.php (plan 03-03 — confirms LogsActivity is in the trait stack)
    - apps/web/app/Models/GameRole.php + GameMatchType.php + GameMatchTypeRoleLimit.php (same)
  </read_first>
  <acceptance_criteria>
    - `GameAuditLogTest.php` is a NEW file (not a Wave 0 stub replacement):
      - `<?php\ndeclare(strict_types=1);`
      - Source comment block: "Verifies D-012 (Filament + spatie/activitylog) end-to-end for Phase 3 models per plan 03-09."
      - Imports: `use App\Models\Game; use App\Models\GameRole; use App\Models\GameMatchType; use App\Models\GameMatchTypeRoleLimit; use App\Models\User; use Database\Seeders\PermissionSeeder; use Spatie\Activitylog\Models\Activity;`
      - `beforeEach(function (): void { $this->seed(PermissionSeeder::class); $this->admin = User::factory()->create(); $this->admin->givePermissionTo('admin-access'); $this->actingAs($this->admin); });`
      - 4+ `it()` blocks:
        - `it('writes activity_log row on Game::create with subject_type=App\\Models\\Game and event=created')` — `Game::factory()->create()` then assert Activity::query()->where('subject_type', Game::class)->where('event', 'created')->exists()
        - `it('writes activity_log row on GameRole::create')` — same pattern for GameRole
        - `it('writes activity_log row on GameMatchType::create')` — same pattern
        - `it('writes activity_log row on GameMatchTypeRoleLimit::create (same-game scenario)')` — explicit same-game setup: create one Game, then GameMatchType + GameRole both ->for($game), then GameMatchTypeRoleLimit::factory()->create([...explicit IDs...]); assert activity_log row
        - `it('logs Game updates as event=updated, not event=created')` — create Game, then $game->update(['is_active' => false]); assert Activity::query()->where('subject_type', Game::class)->where('event', 'updated')->exists()
        - `it('captures causer_id as the acting admin user on Game create')` — $game = Game::factory()->create(); assert the activity row for this subject has causer_id === $this->admin->id (LogsActivity default behavior verified Phase 1 plan 01-14)
    - `make pest ARGS="--filter=GameAuditLog"` GREEN
    - `make phpstan` reports no new errors
  </acceptance_criteria>
  <action>
    Build the test file using ClanModelTest's activity log assertion pattern as the canonical reference. The Spatie\Activitylog\Models\Activity model is the query surface — it has a polymorphic subject + causer relation.

    For each `it()` block: minimal fixture, single Eloquent create/update, single Activity::query() assertion. The test does NOT exercise Filament UI directly (that's plan 03-08) — it exercises the LogsActivity trait → DB write pipe, which is the underlying mechanism the Filament UI relies on.

    For the same-game scenario (GameMatchTypeRoleLimit): construct same-game fixtures explicitly per the plan 03-03 pattern — otherwise the cross-game saving() listener throws DomainException and the test never reaches the activity log assertion.

    For the causer_id test: $this->actingAs($this->admin) in beforeEach makes Auth::user() return $this->admin throughout the test; LogsActivity's default causer resolution uses auth()->user() — the audit row's causer_id should match $this->admin->id.

    Run `make pest ARGS="--filter=GameAuditLog"`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Admin/GameAuditLogTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:)' | head -3</automated>
  </verify>
  <done>GameAuditLogTest GREEN; D-012 contract proven for all 4 Phase 3 models including causer_id capture.</done>
</task>

</tasks>

<verification>
- `make pest ARGS="--filter=I18n"` -> GREEN (no NoHardcodedStrings regression)
- `make pest ARGS="--filter=GameAuditLog"` -> GREEN
- `make phpstan` -> no new errors
- `make pint ARGS="--test"` -> green
- All __('admin.game*') keys in Phase 3 source resolve in lang/en/admin.php (manual verification via the grep+php audit)
</verification>

<success_criteria>
i18n surface is complete for Phase 3 admin resources; activity log integration end-to-end proven for D-012. Phase 3 has full audit coverage matching the Phase 1/2 precedent.
</success_criteria>

<output>
After completion, create `.planning/phases/03-games-match-types/03-09-SUMMARY.md` documenting:
- Number of admin.* keys referenced by Phase 3 sources
- Any keys added during the audit (delta from plan 03-01's placeholder set)
- audit.subject map state
- GameAuditLogTest scenarios covered
</output>
