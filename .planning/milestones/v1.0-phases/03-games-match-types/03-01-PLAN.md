---
phase: 03-games-match-types
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/database/factories/GameFactory.php
  - apps/web/database/factories/GameRoleFactory.php
  - apps/web/database/factories/GameMatchTypeFactory.php
  - apps/web/database/factories/GameMatchTypeRoleLimitFactory.php
  - apps/web/tests/Feature/Models/GameModelTest.php
  - apps/web/tests/Feature/Models/GameRoleModelTest.php
  - apps/web/tests/Feature/Models/GameMatchTypeModelTest.php
  - apps/web/tests/Feature/Models/GameMatchTypeRoleLimitModelTest.php
  - apps/web/tests/Feature/Database/GameSeederTest.php
  - apps/web/tests/Feature/Admin/GameResourcesPresentTest.php
  - apps/web/tests/Unit/Data/GameDataTest.php
  - apps/web/lang/en/admin.php
autonomous: true
requirements:
  - REQ-platform-vision
must_haves:
  truths:
    - "All 4 factory stubs exist at the Wave-0 stage and throw RuntimeException so consumers fail fast until plan 03-03 lands real definitions"
    - "All 7 test files exist with at least one RED `it()` block that fails for a structural reason (model class missing) — proving the harness runs Phase 3 tests"
    - "`make pest ARGS=\"--filter=Game\"` runs without 'no tests found' — the test surface is wired"
    - "`apps/web/lang/en/admin.php` contains placeholder key groups `admin.game.*`, `admin.game_role.*`, `admin.game_match_type.*`, `admin.game_match_type_role_limit.*`, `admin.nav.platform` so Filament resources in plan 03-06/03-07 can `__()` them without raising MissingTranslationException"
  artifacts:
    - path: "apps/web/database/factories/GameFactory.php"
      provides: "Wave 0 stub for Game factory; plan 03-03 replaces with real definition"
      contains: "throw new RuntimeException"
    - path: "apps/web/tests/Feature/Models/GameModelTest.php"
      provides: "Wave 0 RED stub asserting GameModel does not yet exist"
      contains: "placeholder"
    - path: "apps/web/lang/en/admin.php"
      provides: "Extended with admin.game.*, admin.game_role.*, admin.game_match_type.*, admin.game_match_type_role_limit.*, admin.nav.platform key groups"
      contains: "'game' =>"
  key_links:
    - from: "tests/Feature/Models/Game*ModelTest.php"
      to: "Phase 1 PlayerPrivacyModelTest.php analog"
      via: "Pest it() block structure"
      pattern: "it\\('"
    - from: "database/factories/Game*Factory.php"
      to: "Wave 0 stub pattern (Phase 2 plan 02-01)"
      via: "RuntimeException-throwing definition() body"
      pattern: "RuntimeException"
---

<objective>
Land the Wave 0 scaffolding for Phase 3: 4 factory stubs (throw-on-use so consumers fail loudly), 7 RED Pest test stubs (one per planned test file in 03-VALIDATION.md's "Wave 0 Gaps" list), and the placeholder i18n key extensions on `lang/en/admin.php` so plans 03-02..03-09 can land implementation without "missing translation" or "no factory" cascade failures.

Purpose: Per CLAUDE.md §4 ("Wave 0 test scaffolding for every phase precedes implementation"), the Phase 3 test surface MUST exist before Wave 1 implementation. This is the same pattern Phase 2 plan 02-01 used. Each RED stub becomes a GREEN replacement in a later plan — the mapping is recorded in `must_haves.truths` of each downstream plan.

Output: 4 factory stubs, 7 Pest RED stubs, `lang/en/admin.php` extended with all Phase 3 key groups (placeholder English copy, plan 03-09 finalizes the audit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-games-match-types/03-CONTEXT.md
@.planning/phases/03-games-match-types/03-RESEARCH.md
@.planning/phases/03-games-match-types/03-VALIDATION.md
@CLAUDE.md
@apps/web/database/factories/ClanFactory.php
@apps/web/tests/Feature/Models/ClanModelTest.php
@apps/web/lang/en/admin.php

<interfaces>
Phase 2 Wave 0 stub pattern (factory):
- File starts with `<?php\ndeclare(strict_types=1);\nnamespace Database\Factories;`
- Class extends `Factory<\App\Models\X>` (PHPDoc generics) with `protected $model = 'App\\Models\\X';` (string FQN — model class does NOT yet exist)
- `definition()` body: `throw new RuntimeException('GameFactory definition not yet implemented (Wave 0 stub — replaced by plan 03-03).');`

Phase 2 Wave 0 test stub pattern (Pest):
- `<?php\ndeclare(strict_types=1);`
- One `it('placeholder — Wave 0 RED stub replaced by plan 03-XX')` block whose body asserts something that will only become true once plan 03-XX runs (e.g., `expect(class_exists(\App\Models\Game::class))->toBeTrue()` — currently false → RED).

Existing `lang/en/admin.php` structure (verified codebase 2026-05-13):
- Already contains 'clan', 'clan_tag', 'clan_membership', 'clan_invite', 'clan_application' groups
- Add: 'game', 'game_role', 'game_match_type', 'game_match_type_role_limit' groups
- Add 'nav' => ['platform' => 'Platform'] for future navigationGroup if used (Pitfall 8); start with flat — keys still present so __() doesn't fail if a future deviation adds the group

i18n key shape per group (matching the existing clan_* convention in admin.php):
- `label` / `plural_label` (sidebar + breadcrumb)
- `section.{name}` (form section headers)
- `fields.{name}` + `fields.{name}_locale` + `fields.{name}_text` for translatable JSONB KeyValue rows
- `help.{name}` for form helper text
- `tab.{name}` for relation manager tab labels
- `actions.{name}` for custom action labels (if any)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0 scaffolding to downstream plans | RED stubs flip to GREEN during downstream plans; no production code path exists yet |
| `lang/en/admin.php` extension to Filament resources | Placeholder English copy is human-readable but not yet finalised; plan 03-09 audits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Repudiation | Wave 0 stubs masquerade as GREEN coverage | mitigate | Each `it()` block contains the literal string `placeholder` so a grep audit at phase close detects un-replaced stubs |
| T-03-01-02 | Tampering | Placeholder lang keys leak to production | accept | Plan 03-09 runs the i18n key audit (existing NoHardcodedStringsTest + manual visual review) before phase close |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 4 factory stubs + 7 Pest RED stubs under Phase 3 test directories</name>
  <files>
    apps/web/database/factories/GameFactory.php,
    apps/web/database/factories/GameRoleFactory.php,
    apps/web/database/factories/GameMatchTypeFactory.php,
    apps/web/database/factories/GameMatchTypeRoleLimitFactory.php,
    apps/web/tests/Feature/Models/GameModelTest.php,
    apps/web/tests/Feature/Models/GameRoleModelTest.php,
    apps/web/tests/Feature/Models/GameMatchTypeModelTest.php,
    apps/web/tests/Feature/Models/GameMatchTypeRoleLimitModelTest.php,
    apps/web/tests/Feature/Database/GameSeederTest.php,
    apps/web/tests/Feature/Admin/GameResourcesPresentTest.php,
    apps/web/tests/Unit/Data/GameDataTest.php
  </files>
  <read_first>
    - apps/web/database/factories/ClanFactory.php (current real form — invert structure back to stub via RuntimeException body and string-FQN model reference)
    - apps/web/tests/Feature/Models/ClanModelTest.php (Phase 2 GREEN model test analog — shape for the eventual replacement; Wave 0 stub is the minimum viable RED skeleton)
    - apps/web/tests/Feature/Admin/ClanResourcesPresentTest.php (Phase 2 admin presence pattern)
    - apps/web/tests/Unit/Data/PlayerProfileDataTest.php (Phase 2 unit DTO test pattern — `Spatie\LaravelData\Optional` usage etc.)
    - .planning/phases/02-clans-tags/02-01-PLAN.md (Phase 2 Wave 0 plan — analog for this entire plan's structure)
  </read_first>
  <acceptance_criteria>
    - Each factory file:
      - `declare(strict_types=1);` at top
      - `namespace Database\Factories;`
      - Class signature: `class GameFactory extends Factory` (no real model import yet — use string FQN `protected $model = 'App\\Models\\Game';`)
      - PHPDoc `/** @extends Factory<\App\Models\Game> */` above class (PHPStan L8 requirement; the model class does not yet exist but the FQN string is parseable as a docblock)
      - `public function definition(): array { throw new RuntimeException('GameFactory definition not yet implemented (Wave 0 stub — replaced by plan 03-03).'); }`
      - `use RuntimeException;` import
    - Each of the 7 test files:
      - `<?php\ndeclare(strict_types=1);` header
      - Source comment block citing this plan (`.planning/phases/03-games-match-types/03-01-PLAN.md task 1`) and the downstream plan that replaces it
      - At least one `it('placeholder — Wave 0 RED stub replaced by plan 03-XX', function (): void { expect(class_exists('App\\Models\\Game'))->toBeTrue(); });`  (or analogous assertion that fails until the model/seeder/data class is created in the named downstream plan)
      - Plan mapping per file (citation in the source comment):
        | File | Replaced by |
        | GameModelTest.php | plan 03-03 |
        | GameRoleModelTest.php | plan 03-03 |
        | GameMatchTypeModelTest.php | plan 03-03 |
        | GameMatchTypeRoleLimitModelTest.php | plan 03-03 |
        | GameSeederTest.php | plan 03-05 |
        | GameResourcesPresentTest.php | plan 03-08 |
        | GameDataTest.php | plan 03-04 |
    - `make pest ARGS="--filter=Game"` runs without "no tests found" (i.e. the test files load); RED count > 0 is expected (these are scaffold stubs by design)
    - `make phpstan` reports no errors in factories or tests (the `protected $model = 'App\\Models\\Game'` is parseable; phpstan should not flag a non-existent class behind a string)
    - `make pint ARGS="--test database/factories tests/Feature/Models/Game* tests/Feature/Admin/GameResourcesPresentTest.php tests/Feature/Database/GameSeederTest.php tests/Unit/Data/GameDataTest.php"` is clean
  </acceptance_criteria>
  <action>
    Build the 4 factory stubs mirroring Phase 2 plan 02-01's pattern: PHPDoc `@extends Factory<\App\Models\X>` above the class, `protected $model = 'App\\Models\\X';` (string FQN — the model class is created by plan 03-03), `definition()` body throws RuntimeException with a message citing the replacement plan ID.

    Build the 7 test stubs. Each file contains:
    - Source comment block at top: a multi-line PHP comment naming this plan + the replacing plan
    - One `it('placeholder — Wave 0 RED stub replaced by plan 03-XX', ...)` block whose body asserts `class_exists('App\\Models\\Game')`, `class_exists('App\\Data\\GameData')`, `class_exists('Database\\Seeders\\GameSeeder')`, or analogous proof-of-existence; these are RED today and become GREEN once the named downstream plan lands.

    For the GameResourcesPresentTest stub: also include `beforeEach(function () { $this->seed(\Database\Seeders\PermissionSeeder::class); ... })` setup mirroring the Phase 2 analog so plan 03-08's full replacement is structural, not green-field.

    NOTE: do not include `use App\Models\Game;` etc. — the import would be a hard error since the class is missing. Use string FQNs in `class_exists()` calls instead.

    Run `make pint` to format the new files, then `make phpstan` to confirm no L8 issues. Run `make pest ARGS="--filter=Game"` and confirm the runner LOADS the files (RED is expected, "no tests found" is not).
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter='Game' --no-coverage 2>&1 | grep -E '(Tests:|FAIL|PASS|No tests found)' | head -3</automated>
  </verify>
  <done>4 factory stubs + 7 test stubs committed; pest filter finds them; phpstan + pint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Extend lang/en/admin.php with Phase 3 key groups (placeholder English copy)</name>
  <files>apps/web/lang/en/admin.php</files>
  <read_first>
    - apps/web/lang/en/admin.php (current state — find the `clan_application` group at the bottom and append AFTER it; preserve indentation and trailing comma convention)
    - .planning/phases/03-games-match-types/03-RESEARCH.md (Pattern 4 — KeyValue field labels; Pattern 1 — admin-side strings the resources will reference)
    - .planning/phases/03-games-match-types/03-CONTEXT.md (Phase boundary — establishes the user-facing terms "Game", "Game Role", "Game Match Type", "Role Limit")
  </read_first>
  <acceptance_criteria>
    - `apps/web/lang/en/admin.php` extended with these key groups (placement: after the existing `clan_application` group; comment header `// Phase 3 game domain resources (03-CONTEXT.md § Phase Boundary)`):
      - `'game' => [ 'label' => 'Game', 'plural_label' => 'Games', 'section' => ['profile' => 'Profile', 'roles' => 'Roles', 'match_types' => 'Match types'], 'fields' => ['key' => 'Key', 'name' => 'Name', 'name_locale' => 'Locale', 'name_text' => 'Display name', 'is_active' => 'Active'], 'help' => ['key_format' => 'Lowercase letters, digits, and underscores only.'], 'tab' => ['profile' => 'Profile', 'roles' => 'Roles', 'match_types' => 'Match types', 'audit' => 'Audit'] ]`
      - `'game_role' => [ 'label' => 'Game role', 'plural_label' => 'Roles', 'fields' => ['key' => 'Key', 'display_name' => 'Display name', 'display_name_locale' => 'Locale', 'display_name_text' => 'Translation', 'sort_order' => 'Sort order', 'is_active' => 'Active'], 'help' => ['key_format' => 'Lowercase letters, digits, and underscores only.'] ]`
      - `'game_match_type' => [ 'label' => 'Match type', 'plural_label' => 'Match types', 'section' => ['profile' => 'Profile', 'role_limits' => 'Role capacity'], 'fields' => ['key' => 'Key', 'name' => 'Name', 'name_locale' => 'Locale', 'name_text' => 'Translation', 'description' => 'Description', 'description_locale' => 'Locale', 'description_text' => 'Translation', 'is_active' => 'Active', 'game' => 'Game'], 'help' => ['key_format' => 'Lowercase letters, digits, and underscores only.'], 'tab' => ['profile' => 'Profile', 'role_limits' => 'Role capacity', 'audit' => 'Audit'] ]`
      - `'game_match_type_role_limit' => [ 'label' => 'Role capacity', 'plural_label' => 'Role capacities', 'fields' => ['role' => 'Role', 'capacity' => 'Capacity', 'sort_order' => 'Sort order'], 'help' => ['capacity_min_zero' => 'Capacity must be zero or greater.', 'role_scope' => 'Roles are scoped to the parent game.'] ]`
      - Extend `audit.subject` map (existing) to register the 4 new model FQNs against translatable display labels — append entries for `App\Models\Game`, `App\Models\GameRole`, `App\Models\GameMatchType`, `App\Models\GameMatchTypeRoleLimit` (mirror how `App\Models\Clan` and `App\Models\ClanTag` were added in Phase 2)
      - Add `'nav' => ['platform' => 'Platform']` IF the file does not already define `admin.nav` (verify via grep first); the planner expects the existing file to already host an `admin.nav.audit` key for the audit page — extend with `platform` for future grouping use (Pitfall 8); harmless if unused
    - `make phpstan` clean (admin.php is a pure array, no L8 surface change, but rerun for safety)
    - `make pest ARGS="--filter=I18n"` GREEN (existing tests — no new key references yet so this should pass without changes)
    - `make pint ARGS="--test lang"` clean
  </acceptance_criteria>
  <action>
    Read the current `apps/web/lang/en/admin.php` end-to-end. Identify the position immediately after the `clan_application` group's closing bracket. Insert the 4 new groups with the exact key shapes named in acceptance criteria, preserving the existing 4-space indentation + trailing-comma style.

    For the `audit.subject` extension: locate the existing entries for `App\Models\Clan`, `App\Models\ClanTag` etc. Append `App\Models\Game => 'Game'`, `App\Models\GameRole => 'Game role'`, `App\Models\GameMatchType => 'Match type'`, `App\Models\GameMatchTypeRoleLimit => 'Role capacity'`. If the existing `audit.subject` map keys by class FQN, mirror that exact format; if it keys by short name, mirror that.

    For the `admin.nav` key: search for an existing `'nav' =>` block; if present, append `'platform' => 'Platform'`; if absent, create the block. Phase 1 plan 01-13 audit lang keys for `nav` — verify before edit.

    Run `make pint` then `make pest ARGS="--filter=I18n"` and `make phpstan` to confirm no regressions.
  </action>
  <verify>
    <automated>docker compose exec web php -r "require 'apps/web/vendor/autoload.php'; \$keys = require 'apps/web/lang/en/admin.php'; foreach (['game.label','game.plural_label','game_role.label','game_match_type.label','game_match_type_role_limit.label','game.fields.name','game_role.fields.display_name','game_match_type.fields.description','game_match_type_role_limit.fields.capacity'] as \$k) { \$parts = explode('.', \$k); \$v = \$keys; foreach (\$parts as \$p) { \$v = \$v[\$p] ?? null; if (\$v === null) { echo \"MISSING: \$k\n\"; exit(1); } } echo \"OK: \$k\n\"; }" 2>&1 | tail -15</automated>
  </verify>
  <done>All 4 Phase 3 key groups present; audit.subject extended; placeholder English copy ready for Filament resources in plan 03-06/03-07.</done>
</task>

</tasks>

<verification>
- `make pest ARGS="--filter=Game"` -> tests load; RED count > 0 (RED is expected for Wave 0 stubs)
- `make pest ARGS="--filter=I18n"` -> GREEN (no regression on Phase 1/2 i18n surface)
- `make phpstan` -> no new errors
- `make pint ARGS="--test"` -> clean
- `apps/web/lang/en/admin.php` contains `admin.game.*`, `admin.game_role.*`, `admin.game_match_type.*`, `admin.game_match_type_role_limit.*` key groups
</verification>

<success_criteria>
Phase 3 test surface is wired (7 RED stubs visible to pest); 4 factory stubs fail loudly if invoked prematurely; placeholder admin.* lang keys available for Filament resources downstream. The scaffolding is the contract every later plan replaces in part.
</success_criteria>

<output>
After completion, create `.planning/phases/03-games-match-types/03-01-SUMMARY.md` documenting:
- 4 factory stub paths + the downstream plan that replaces each
- 7 test stub paths + the downstream plan that replaces each
- admin.php key-group count delta and example keys added
- Any deviation from Phase 2 plan 02-01's analog
</output>
