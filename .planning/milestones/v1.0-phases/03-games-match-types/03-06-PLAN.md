---
phase: 03-games-match-types
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 03, 05]
files_modified:
  - apps/web/app/Filament/Resources/GameResource.php
  - apps/web/app/Filament/Resources/GameResource/Pages/ListGames.php
  - apps/web/app/Filament/Resources/GameResource/Pages/CreateGame.php
  - apps/web/app/Filament/Resources/GameResource/Pages/EditGame.php
  - apps/web/app/Filament/Resources/GameResource/Pages/ViewGame.php
  - apps/web/app/Filament/Resources/GameResource/RelationManagers/RolesRelationManager.php
  - apps/web/app/Filament/Resources/GameResource/RelationManagers/MatchTypesRelationManager.php
autonomous: true
requirements:
  - REQ-platform-vision
must_haves:
  truths:
    - "GameResource registered at /admin/games with List, Create, View, Edit pages"
    - "GameResource form uses Tabs (Profile + Audit) with KeyValue for name (D-013); RolesRelationManager + MatchTypesRelationManager attach as nested tabs via getRelations()"
    - "CreateGame + EditGame override mutateFormDataBeforeCreate/mutateFormDataBeforeSave to coerce null name to ['en' => ''] (Pitfall 2)"
    - "RolesRelationManager allows admin to inline-CRUD GameRole rows on the Game edit page (Pattern 1)"
    - "MatchTypesRelationManager allows admin to inline-CRUD GameMatchType rows on the Game edit page (modal-based EditAction in this plan; URL-override navigation to GameMatchTypeResource is a Rule-2 amendment delivered by plan 03-07 task 3, which MUST be executed after plan 03-07 task 1 ships GameMatchTypeResource — without that amendment this truth's 'clicking a row navigates to GameMatchTypeResource' clause is intentionally FALSE between plan 03-06 completion and plan 03-07 task 3 completion)"
    - "All admin actions write activity_log entries (LogsActivity on the 3 models)"
    - "Filament Resource $navigationSort = 10 keeps Games AFTER existing Phase 1+2 resources in the sidebar (Pitfall 8 partial fix; flat layout, no nav group)"
  artifacts:
    - path: "apps/web/app/Filament/Resources/GameResource.php"
      provides: "Admin CRUD for Games"
      contains: "class GameResource extends Resource"
    - path: "apps/web/app/Filament/Resources/GameResource/RelationManagers/RolesRelationManager.php"
      provides: "Inline GameRole CRUD on Game edit page"
      contains: "class RolesRelationManager extends RelationManager"
    - path: "apps/web/app/Filament/Resources/GameResource/RelationManagers/MatchTypesRelationManager.php"
      provides: "Inline GameMatchType CRUD on Game edit page"
      contains: "class MatchTypesRelationManager extends RelationManager"
  key_links:
    - from: "GameResource form name field"
      to: "KeyValue::make('name')->default(['en' => ''])"
      via: "Filament v3 form component for translatable JSONB (Pattern 4)"
      pattern: "KeyValue::make\\('name'\\)"
    - from: "GameResource::getRelations()"
      to: "RolesRelationManager + MatchTypesRelationManager"
      via: "static array literal"
      pattern: "RelationManagers\\\\(Roles|MatchTypes)RelationManager"
    - from: "Game model"
      to: "Filament Resource $model = Game::class"
      via: "Filament v3 Resource convention"
      pattern: "\\\\$model = Game::class"
---

<objective>
Land the GameResource Filament admin surface per D-012 (Filament covers every domain entity) + CONTEXT.md SC-1 (admin can create or edit Game and GameRoles in Filament with `(game_id, key)` uniqueness enforced). This plan ships: GameResource (single-game CRUD with Tabs: Profile + Audit), 4 Pages (List/Create/View/Edit), RolesRelationManager (inline CRUD for GameRole), MatchTypesRelationManager (inline CRUD for GameMatchType — clicking a row navigates to GameMatchTypeResource where RoleLimits are managed). Plan 03-07 ships GameMatchTypeResource + RoleLimitsRelationManager (the second tier of Filament v3's two-resource pattern per RESEARCH Pattern 2).

Purpose: D-012 requires every domain entity has a Filament resource. GameResource is the entry point for admin game-model management. Splitting RoleLimits into its own resource (plan 03-07) is per Pattern 2 — Filament v3 does NOT support nested RelationManagers.

Output: GameResource.php + 4 Pages + 2 RelationManagers; admin can visit /admin/games, see seeded HLL row, edit name + roles + match types from a single tabbed UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-games-match-types/03-RESEARCH.md
@.planning/phases/03-games-match-types/03-CONTEXT.md
@CLAUDE.md
@apps/web/app/Filament/Resources/ClanResource.php
@apps/web/app/Filament/Resources/ClanResource/Pages/EditClan.php
@apps/web/app/Filament/Resources/ClanResource/Pages/CreateClan.php
@apps/web/app/Filament/Resources/ClanResource/Pages/ListClans.php
@apps/web/app/Filament/Resources/ClanResource/Pages/ViewClan.php
@apps/web/app/Filament/Resources/ClanResource/RelationManagers/MembersRelationManager.php
@apps/web/app/Filament/Resources/ClanTagResource.php
@apps/web/lang/en/admin.php

<interfaces>
Canonical Phase 2 Filament Resource pattern (verified `apps/web/app/Filament/Resources/ClanResource.php`):
- `<?php\ndeclare(strict_types=1); namespace App\Filament\Resources;`
- `use App\Filament\Resources\GameResource\Pages;` and `use App\Filament\Resources\GameResource\RelationManagers;`
- `class GameResource extends Resource { protected static ?string $model = Game::class; protected static ?string $navigationIcon = '<heroicon>'; protected static ?int $navigationSort = N; }`
- `public static function getModelLabel(): string { return __('admin.game.label'); }` + `getPluralModelLabel`
- `public static function form(Form $form): Form` returning Tabs structure
- `public static function table(Table $table): Table` returning columns + actions
- `public static function getPages(): array` returning route map: index, create, view, edit
- `public static function getRelations(): array { return [RelationManagers\X::class, ...]; }`

KeyValue null-coercion mutator (verified `apps/web/app/Filament/Resources/ClanResource/Pages/EditClan.php` + Pitfall 2):
- Inside CreateGame::mutateFormDataBeforeCreate and EditGame::mutateFormDataBeforeSave:
- `$data['name'] = $data['name'] ?: ['en' => ''];`

RelationManager pattern (verified `apps/web/app/Filament/Resources/ClanResource/RelationManagers/MembersRelationManager.php`):
- `<?php\ndeclare(strict_types=1); namespace App\Filament\Resources\GameResource\RelationManagers;`
- `class RolesRelationManager extends RelationManager { protected static string $relationship = 'roles'; }`
- `protected static string $relationship` MUST match the HasMany method name on the Game model EXACTLY (Pitfall 3 — silent failure if mismatched)
- form(Form): Forms\Components fields scoped to the child model
- table(Table): columns + headerActions[CreateAction] + actions[EditAction, DeleteAction]

From RESEARCH.md Pattern 1 (RolesRelationManager spec):
- relationship='roles' (matches Game::roles() HasMany)
- form fields: key (TextInput, regex `^[a-z0-9_]+$`, required), display_name (KeyValue default ['en'=>''] required reorderable false), sort_order (TextInput numeric default 0), is_active (Toggle default true)
- table columns: sort_order (sortable), key (mono, searchable), display_name (extracts ['en'] for display via getStateUsing), is_active (IconColumn boolean)
- defaultSort: sort_order
- headerActions: CreateAction; row actions: EditAction, DeleteAction

MatchTypesRelationManager spec (similar to RolesRelationManager but for GameMatchType):
- relationship='matchTypes' (matches Game::matchTypes() HasMany)
- form fields: key (TextInput regex), name (KeyValue), description (KeyValue nullable), is_active (Toggle)
- The form is intentionally LEAN — RoleLimits are NOT edited here (Pattern 2 second-tier resource lives in plan 03-07); clicking a table row navigates to GameMatchTypeResource's edit page
- table columns: key (mono), name (en accessor), is_active (boolean); EditAction routes via Filament's `->url(fn ($record) => GameMatchTypeResource::getUrl('edit', ['record' => $record]))` to the standalone resource — this is the canonical Pattern 2 navigation idiom

Filament v3 navigation sort (Pitfall 8 — keep new resources after existing):
- Existing Phase 1/2 navigationSort values (verified via grep on Phase 1/2 resources): User=1, Player=2, Clan=3, ClanTag=4, others 5-9
- New: Game=10, GameMatchType=11 (plan 03-07)
- No navigationGroup — flat layout (Open Question Q3 deferral)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin user to Filament resource | admin-access permission gate (Phase 1 plan 01-12) inherited; no new gate needed |
| Filament form to model save | Filament respects $fillable; Pitfall 2 mutator coerces null translatable JSONB to ['en'=>''] |
| RelationManager actions | Admin can directly create/edit/delete GameRole + GameMatchType rows; audited via LogsActivity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Elevation of Privilege | Non-admin reaches /admin/games | mitigate | Phase 1 AdminPanelProvider gates entire /admin under `admin-access` permission via User::canAccessPanel — verified by FilamentPanelAccessTest |
| T-03-06-02 | Tampering | KeyValue submits null for name | mitigate | mutateFormDataBeforeCreate/Save coerces null to ['en'=>''] (Pitfall 2); without this $game->name returns null in templates |
| T-03-06-03 | Tampering | Admin edits Game.key (breaks seeder idempotency) | accept | Admin's job; firstOrCreate matches on (game_id, key) so post-edit re-seed creates a duplicate row — operator must understand this is a destructive edit. Document in admin help text. |
| T-03-06-04 | Repudiation | Filament inline-CRUD on GameRole / GameMatchType unaudited | mitigate | LogsActivity on all 3 models (plan 03-03); per-resource Audit tab inherits from Phase 1 plan 01-14 |
| T-03-06-05 | Tampering | RelationManager typo causes silent blank table (Pitfall 3) | mitigate | Plan 03-08 admin presence test asserts the relation tabs render with content; this plan's verification step does a quick smoke via Pest |
| T-03-06-06 | Information Disclosure | Translatable JSONB key=value display leaks raw structure | accept | KeyValue is admin-only UI; raw locale-keyed display is intentional for the EN-only launch (D-013); LocaleSwitcher plugin deferred to Phase 7 |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: GameResource.php + 4 Pages (List/Create/View/Edit) with mutateFormDataBefore* JSONB coercion</name>
  <files>
    apps/web/app/Filament/Resources/GameResource.php,
    apps/web/app/Filament/Resources/GameResource/Pages/ListGames.php,
    apps/web/app/Filament/Resources/GameResource/Pages/CreateGame.php,
    apps/web/app/Filament/Resources/GameResource/Pages/EditGame.php,
    apps/web/app/Filament/Resources/GameResource/Pages/ViewGame.php
  </files>
  <read_first>
    - apps/web/app/Filament/Resources/ClanResource.php (canonical analog — Tabs + Audit + KeyValue + mutateFormDataBeforeCreate pattern)
    - apps/web/app/Filament/Resources/ClanResource/Pages/EditClan.php (mutateFormDataBeforeSave for null JSONB coercion — Pitfall 2)
    - apps/web/app/Filament/Resources/ClanResource/Pages/CreateClan.php (mutateFormDataBeforeCreate analog)
    - apps/web/app/Filament/Resources/ClanResource/Pages/ListClans.php + ViewClan.php (List + View page skeleton)
    - apps/web/app/Filament/Resources/ClanTagResource.php (simpler analog — single translatable field; no Tabs needed if minimal)
    - .planning/phases/03-games-match-types/03-RESEARCH.md (Pattern 4 — KeyValue translatable; Pattern 2 — two-tier resource; Pitfall 2, 6, 8)
    - apps/web/lang/en/admin.php (existing admin.game.* keys after plan 03-01; verify `admin.game.label`, `admin.game.fields.name`, `admin.game.section.profile`, `admin.game.tab.audit`, `admin.tab.profile` exist)
  </read_first>
  <acceptance_criteria>
    - `GameResource.php`:
      - `<?php\ndeclare(strict_types=1); namespace App\Filament\Resources;`
      - `protected static ?string $model = \App\Models\Game::class;`
      - `protected static ?string $navigationIcon = 'heroicon-o-puzzle-piece';` (or another appropriate Heroicon — the choice is Claude's discretion per CONTEXT)
      - `protected static ?int $navigationSort = 10;` (Pitfall 8 — keep after Phase 1/2 resources)
      - `getModelLabel()` returns `__('admin.game.label')`; `getPluralModelLabel()` returns `__('admin.game.plural_label')` (D-013)
      - `form(Form $form): Form` — Tabs with 2 tabs at the page level:
        - Tab 1 (`admin.tab.profile` or `admin.game.tab.profile`): Section `admin.game.section.profile` containing fields:
          - `Forms\Components\TextInput::make('key')->label(__('admin.game.fields.key'))->required()->maxLength(64)->regex('/^[a-z0-9_]+$/')->helperText(__('admin.game.help.key_format'))->disabledOn('edit')` (key is immutable post-create — admin edits would break seeder idempotency per T-03-06-03; disable on edit page)
          - `Forms\Components\KeyValue::make('name')->label(__('admin.game.fields.name'))->keyLabel(__('admin.game.fields.name_locale'))->valueLabel(__('admin.game.fields.name_text'))->reorderable(false)->default(['en' => ''])->required()`
          - `Forms\Components\Toggle::make('is_active')->label(__('admin.game.fields.is_active'))->default(true)`
        - Tab 2 (`admin.game.tab.audit` or reuse Phase 1 audit-tab placeholder pattern): Placeholder with audit-history partial (mirror ClanResource.php audit tab implementation)
      - `table(Table $table): Table` — columns: key (mono, searchable), name (en accessor via getStateUsing), is_active (IconColumn boolean), created_at (sortable). Actions: ViewAction, EditAction. No DeleteAction at the table level — deleting a Game cascades to roles + match-types + role_limits (destructive); only expose hard-delete via the Edit page if at all (Open Question Q4 recommendation: defer hard-delete; use is_active toggle instead). For Phase 3 ship: no DeleteAction, but admin can toggle is_active=false to hide a game.
      - `getPages(): array` returning index, create, view, edit (4 entries following the Phase 2 ClanResource analog)
      - `getRelations(): array { return [RelationManagers\RolesRelationManager::class, RelationManagers\MatchTypesRelationManager::class]; }` (task 2 ships these)
    - 4 Pages files:
      - Each declares `declare(strict_types=1);`, extends the appropriate Filament base class (`ListRecords` / `CreateRecord` / `EditRecord` / `ViewRecord`), sets `protected static string $resource = GameResource::class;`
      - `CreateGame::mutateFormDataBeforeCreate(array $data): array { $data['name'] = $data['name'] ?: ['en' => '']; return $data; }`
      - `EditGame::mutateFormDataBeforeSave(array $data): array { $data['name'] = $data['name'] ?: ['en' => '']; return $data; }`
    - `make phpstan` clean on these 5 new files
    - `make pint --test app/Filament/Resources/GameResource.php app/Filament/Resources/GameResource/Pages` clean
  </acceptance_criteria>
  <action>
    Compose GameResource.php from ClanResource.php structure, substituting names + fields. Critical points:
    1. The `key` field MUST be `->disabledOn('edit')` (Filament v3 idiom — the field renders but cannot be edited on the edit page); rationale documented in T-03-06-03 — admin editing the key would break seeder idempotency.
    2. The `name` field uses KeyValue with `->default(['en' => ''])` per Pattern 4 (the Phase 2 ClanResource.php pattern). The Pitfall 2 null-coercion mutator on the Pages files prevents `name = null` writes.
    3. No DeleteAction at the table level — admin uses is_active toggle instead. If a future plan needs hard-delete, surface it via a custom Action::make('hard_delete') on the Edit page with a strong confirmation modal.

    For the audit tab implementation: mirror the Phase 2 ClanResource pattern exactly. The audit tab uses a Placeholder + the existing Phase 1 audit-tab partial. The partial file is verified codebase (Phase 1 plan 01-14 wired it).

    For the 4 Pages: each is a thin subclass of the Filament base. The mutators on Create + Edit are 3-line methods that coerce $data['name'] to `['en' => '']` if it falsy. This is the Pitfall 2 mitigation at the form layer.

    Run `make phpstan analyse app/Filament/Resources/GameResource.php app/Filament/Resources/GameResource/Pages`. Run `make pint`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/phpstan analyse app/Filament/Resources/GameResource.php app/Filament/Resources/GameResource/Pages --no-progress --error-format=raw 2>&1 | tail -5</automated>
  </verify>
  <done>GameResource + 4 Pages committed; phpstan clean; pint clean.</done>
</task>

<task type="auto">
  <name>Task 2: RolesRelationManager + MatchTypesRelationManager (Pattern 1 inline CRUD; Pattern 2 click-through navigation for nested MatchType→RoleLimit editing)</name>
  <files>
    apps/web/app/Filament/Resources/GameResource/RelationManagers/RolesRelationManager.php,
    apps/web/app/Filament/Resources/GameResource/RelationManagers/MatchTypesRelationManager.php
  </files>
  <read_first>
    - apps/web/app/Filament/Resources/ClanResource/RelationManagers/MembersRelationManager.php (canonical Phase 2 analog — form + table + headerActions + row actions structure)
    - .planning/phases/03-games-match-types/03-RESEARCH.md (Pattern 1 verbatim RolesRelationManager code; Pattern 2 — two-tier navigation for MatchType → RoleLimit)
    - apps/web/lang/en/admin.php (existing admin.game_role.*, admin.game_match_type.* keys after plan 03-01)
  </read_first>
  <acceptance_criteria>
    - `RolesRelationManager.php`:
      - `<?php\ndeclare(strict_types=1); namespace App\Filament\Resources\GameResource\RelationManagers;`
      - `class RolesRelationManager extends RelationManager`
      - `protected static string $relationship = 'roles';` (matches Game::roles() HasMany — Pitfall 3 typo guard)
      - `protected static ?string $title = '';` (empty — title comes from the tab label which Filament auto-derives from the model + i18n keys via `getTitle()` if needed; alternatively explicit `getTitle(): string { return __('admin.game.tab.roles'); }`)
      - `form(Form $form): Form` with fields:
        - `TextInput::make('key')` required, maxLength 64, regex `/^[a-z0-9_]+$/`, helperText `__('admin.game_role.help.key_format')`
        - `KeyValue::make('display_name')` keyLabel + valueLabel from i18n, reorderable false, default `['en' => '']`, required
        - `TextInput::make('sort_order')` numeric, default 0
        - `Toggle::make('is_active')` default true
      - `table(Table $table): Table` with columns: sort_order (sortable), key (mono, searchable), display_name (getStateUsing -> ['en']), is_active (IconColumn boolean). defaultSort: sort_order
      - headerActions: `Tables\Actions\CreateAction::make()`
      - row actions: `Tables\Actions\EditAction::make()`, `Tables\Actions\DeleteAction::make()`
    - `MatchTypesRelationManager.php`:
      - `protected static string $relationship = 'matchTypes';` (matches Game::matchTypes())
      - `form(Form $form): Form` with fields: key (TextInput regex), name (KeyValue with default + required), description (KeyValue with default + reorderable false but NOT required — matches the nullable JSONB), is_active (Toggle)
      - `table(Table $table): Table` with columns: key (mono), name (en accessor), is_active (IconColumn). defaultSort: name (en sub-key not directly sortable in v3; use key or created_at as fallback — Claude's choice)
      - headerActions: `Tables\Actions\CreateAction::make()` — admin can create a new GameMatchType inline; the new record's RoleLimits are NOT editable here (Pattern 2 second-tier); after create, admin clicks the row to navigate to GameMatchTypeResource::edit
      - row actions:
        - `Tables\Actions\EditAction::make()->url(fn (\App\Models\GameMatchType $record) => \App\Filament\Resources\GameMatchTypeResource::getUrl('edit', ['record' => $record]))` — overrides the modal-based edit with a navigate-to-resource edit; this is the canonical Pattern 2 navigation idiom and where the admin manages RoleLimits (plan 03-07 ships GameMatchTypeResource)
        - `Tables\Actions\DeleteAction::make()` — deleting a MatchType cascades to its RoleLimits (Pitfall 7)
    - `make phpstan` clean
    - `make pint --test` clean

    NB on the EditAction URL override: GameMatchTypeResource ships in plan 03-07; the class FQN reference will be a forward-declaration in this plan's source code that PHPStan resolves once 03-07 lands. To prevent PHPStan failure DURING this plan's execution (when 03-07 has not yet shipped), the executor MUST stub the EditAction URL to a placeholder string and revisit in plan 03-07 — alternative: ship the URL override here AND ship plan 03-07 in the same wave (forbidden — 03-07 is wave 5, this is wave 4). Resolution: skip the URL override in this plan, leave the default modal-based EditAction. Plan 03-07's task list will include a Rule 2 amendment to MatchTypesRelationManager.php adding the URL override AFTER GameMatchTypeResource is shipped.
  </acceptance_criteria>
  <action>
    Build RolesRelationManager verbatim from RESEARCH.md Pattern 1. The structure mirrors Phase 2 MembersRelationManager exactly: form()/table() methods, static $relationship property matches the HasMany method name on the parent model.

    For the KeyValue field: the default `['en' => '']` plus the parent page's mutateFormDataBeforeCreate/Save (task 1) handles the Pitfall 2 null coercion. RelationManager forms ALSO submit through the parent Resource's form pipeline, but Filament v3 docs recommend a mutator on the RelationManager itself for nested forms — verify via Context7 if uncertain. Pattern 1 in RESEARCH.md leaves this to the parent page; we follow that.

    For MatchTypesRelationManager: same skeleton. The CRITICAL DIVERGENCE from Pattern 1: the EditAction's destination. Per Pattern 2, admin clicks a MatchType row → navigates to GameMatchTypeResource's edit page (which has RoleLimitsRelationManager). However, GameMatchTypeResource doesn't exist yet (plan 03-07 ships it). Resolution per the acceptance criteria: SHIP this plan with the DEFAULT EditAction (Filament modal-based edit). Plan 03-07 task 3 amends this file to override the URL to point at GameMatchTypeResource::getUrl('edit', ['record' => $record]).

    Run `make phpstan` and `make pint`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/phpstan analyse app/Filament/Resources/GameResource/RelationManagers --no-progress --error-format=raw 2>&1 | tail -5</automated>
  </verify>
  <done>2 RelationManagers committed; phpstan clean; admin can inline-CRUD GameRole + GameMatchType from Game edit page (RoleLimits live in plan 03-07's standalone resource).</done>
</task>

</tasks>

<verification>
- `make phpstan` -> no new errors
- `make pint ARGS="--test"` -> green
- Quick reachability smoke via Pest: `make pest ARGS="--filter=GameResourcesPresent"` runs (will RED until plan 03-08 lands full presence test; the Wave 0 stub from plan 03-01 still exists)
- Manual smoke (deferred to plan 03-08): `/admin/games` returns 200 for admin; HLL row visible from plan 03-05 seed; clicking edit shows Tabs (Profile + Audit) and RelationManager tabs (Roles + MatchTypes)
</verification>

<success_criteria>
GameResource ships per D-012. Admin can CRUD Games + nested GameRoles + nested GameMatchTypes inline; the MatchType→RoleLimit drill-down is one click away (plan 03-07 lands the destination). SC-1 ("admin can create or edit a Game and its GameRoles in Filament") is satisfied. Plan 03-07 completes SC-2 by adding the RoleLimits second tier.
</success_criteria>

<output>
After completion, create `.planning/phases/03-games-match-types/03-06-SUMMARY.md` documenting:
- GameResource Tabs structure
- RolesRelationManager + MatchTypesRelationManager field maps
- The Pattern 2 click-through deferred to plan 03-07 (EditAction URL override is a Rule 2 amendment in 03-07 task 3)
- Any deviation from Phase 2 ClanResource analog
</output>
