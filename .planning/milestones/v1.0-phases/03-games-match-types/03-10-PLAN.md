---
phase: 03-games-match-types
plan: 10
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09]
files_modified:
  - .planning/phases/03-games-match-types/03-PHASE-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - REQ-platform-vision
must_haves:
  truths:
    - "Full Pest suite GREEN (Phase 1 + Phase 2 + Phase 3 tests, no skipped placeholders, no `it('placeholder ...)` literals remaining anywhere in tests/)"
    - "Pint --test clean; PHPStan L8 clean; vue-tsc clean; shared-types pnpm check clean"
    - "ROADMAP.md updated: Phase 3 row marked progress 10/10; Plans list complete; status Complete"
    - "03-PHASE-VERIFICATION.md committed mapping every SC (1-4) + REQ (REQ-platform-vision) to a passing test"
    - "REQUIREMENTS.md traceability table updated: REQ-platform-vision Phase 3 status flipped from `Pending` to `Complete`"
    - "Manual smoke checklist documented for operator (Filament UI walk-through; matches Phase 1/2 deferral pattern)"
  artifacts:
    - path: ".planning/phases/03-games-match-types/03-PHASE-VERIFICATION.md"
      provides: "Phase close artefact with SC + REQ traceability"
      contains: "SC-1"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 marked complete with full plan list"
      contains: "3. Games & match types"
  key_links:
    - from: "PHASE-VERIFICATION.md SC-1"
      to: "tests/Feature/Admin/GameResourcesPresentTest + tests/Feature/Models/GameRoleModelTest"
      via: "Test file references"
      pattern: "GameResourcesPresent|GameRoleModel"
    - from: "ROADMAP.md Phase 3 entry"
      to: ".planning/phases/03-games-match-types/03-{NN}-PLAN.md list"
      via: "ROADMAP plan checklist"
      pattern: "03-01-PLAN.md"
---

<objective>
[BLOCKING] Phase close: run the full quality gate sweep, write 03-PHASE-VERIFICATION.md mapping every ROADMAP success criterion + every REQ-* to a passing test, update ROADMAP.md to reflect Phase 3 completion with the 10-plan checklist, and update REQUIREMENTS.md traceability table to flip REQ-platform-vision from Pending to Complete.

Purpose: Mirror Phase 1's `01-18-PLAN.md` and Phase 2's `02-14-PLAN.md` closure pattern. This is the human-readable proof that Phase 3 shipped. Manual UI smokes (admin walk-through) are deferred to operator per Phase 1/2 autonomous-mode handoff convention.

Output: 03-PHASE-VERIFICATION.md, ROADMAP.md update, REQUIREMENTS.md update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-games-match-types/03-RESEARCH.md
@.planning/phases/03-games-match-types/03-CONTEXT.md
@.planning/phases/03-games-match-types/03-VALIDATION.md
@.planning/phases/02-clans-tags/02-14-PLAN.md
@.planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md
@CLAUDE.md

<interfaces>
Phase 2 close artifact (verified `.planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md`):
- Phase metadata header (number, name, completed date, plans count)
- ROADMAP Success Criteria mapping table (SC-1..SC-5 → test files + Pest filter strings)
- REQ-* mapping table (4 rows for Phase 2)
- Pest results summary (counts pass/total)
- Pint + PHPStan + vue-tsc + shared-types statuses
- Manual smokes deferred to operator (listed)
- Sign-off line

ROADMAP.md update procedure (verified Phase 2 plan 02-14):
- Find `### Phase 3: Games & match types`
- The current entry has `**Plans**: 14 plans` with a placeholder 14-plan list that is INCORRECT (it copies Phase 2's plan list). REPLACE this with the actual 10-plan Phase 3 list per the planner-step update_roadmap convention.
- Update the progress table at the bottom: row "3. Games & match types | 10/10 | Complete | 2026-05-13"
- Phase 3 entry status: change "Not started" to "Complete"

REQUIREMENTS.md update (verified `.planning/REQUIREMENTS.md` line 116):
- Current row: `| REQ-platform-vision | Phase 3 | Pending |`
- Update to: `| REQ-platform-vision | Phase 3 | Complete |`
- Also tick the v1 Requirements checkbox at line 14: `- [ ] **REQ-platform-vision**: ...` → `- [x] **REQ-platform-vision**: ...`

03-PHASE-VERIFICATION.md SC mapping (per CONTEXT.md success criteria):
- SC-1: admin can create or edit a Game and its GameRoles in Filament with `(game_id, key)` uniqueness enforced
  - Tests: GameModelTest, GameRoleModelTest (UNIQUE assertion), GameResourcesPresentTest (admin UI reachability + RolesRelationManager render)
  - Filters: `--filter='GameModel|GameRoleModel|GameResourcesPresent'`
- SC-2: admin can create a GameMatchType and set GameMatchTypeRoleLimit capacities per role through Filament Relation Managers
  - Tests: GameMatchTypeModelTest, GameMatchTypeRoleLimitModelTest (capacity CHECK + cross-game invariant), GameResourcesPresentTest (RoleLimits RelationManager render)
  - Filters: `--filter='GameMatchType|GameResourcesPresent'`
- SC-3: seeded HLL data exists (15 roles + 5 match types) and is fully editable post-seed
  - Tests: GameSeederTest (count + idempotency + admin-edit preservation)
  - Filter: `--filter='GameSeederTest'`
- SC-4: adding a new game requires zero code changes
  - Tests: covered structurally by SC-1 + SC-2 admin tests (admin UI creates a 2nd Game via Filament — the act of doing so in plan 03-08 admin presence tests proves zero-code-change for a new game)
  - Filter: `--filter='GameResourcesPresent'`
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase verification report to ROADMAP source-of-truth | Verification claims must be backed by passing tests; no false claims |
| Manual smoke deferral to operator | Documented in PHASE-VERIFICATION; operator runs before declaring Phase 3 truly shipped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | Repudiation | PHASE-VERIFICATION claims unsupported | mitigate | Each SC + REQ row in the verification table cites the exact test file + Pest filter; reviewer can rerun |
| T-03-10-02 | Tampering | ROADMAP.md update conflicts with another phase | accept | Single editor (this plan) modifies the Phase 3 section + progress row; merge conflicts surface during git commit |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run full quality gate sweep + write 03-PHASE-VERIFICATION.md with SC + REQ traceability</name>
  <files>.planning/phases/03-games-match-types/03-PHASE-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md (Phase 2 template — full SC/REQ mapping table structure)
    - .planning/phases/03-games-match-types/03-CONTEXT.md (the 4 success criteria verbatim)
    - .planning/REQUIREMENTS.md (REQ-platform-vision description)
    - All Phase 3 test files (verify they exist before citing in the SC table)
  </read_first>
  <acceptance_criteria>
    - Full quality gate sweep run inside the container:
      - `make pest` -> all GREEN (Phase 1 + Phase 2 + Phase 3 tests, count > 250)
      - `make pint ARGS="--test"` -> GREEN
      - `make phpstan` -> no errors
      - `make pnpm ARGS="-F web run typecheck"` -> clean
      - `make pnpm ARGS="-F shared-types check"` -> clean
      - Manual grep: `grep -rE "placeholder.*Wave 0" apps/web/tests/` returns ZERO matches (proves all Wave 0 stubs were replaced)
    - `03-PHASE-VERIFICATION.md` written with sections:
      - Phase metadata header: phase number (3), name (Games & match types), completed date, plans count (10)
      - ROADMAP Success Criteria mapping table — 4 rows:
        | SC | Description (verbatim from CONTEXT.md) | Test file(s) | Pest filter |
        | SC-1 | admin can create or edit a Game and its GameRoles in Filament with `(game_id, key)` uniqueness enforced | tests/Feature/Models/GameModelTest.php, GameRoleModelTest.php, tests/Feature/Admin/GameResourcesPresentTest.php | `--filter='GameModel|GameRoleModel|GameResourcesPresent'` |
        | SC-2 | admin can create a GameMatchType and set GameMatchTypeRoleLimit capacities per role through Filament Relation Managers | tests/Feature/Models/GameMatchTypeModelTest.php, GameMatchTypeRoleLimitModelTest.php, tests/Feature/Admin/GameResourcesPresentTest.php | `--filter='GameMatchType|GameResourcesPresent'` |
        | SC-3 | seeded HLL data (15 roles + 5 match types) editable post-seed | tests/Feature/Database/GameSeederTest.php | `--filter='GameSeederTest'` |
        | SC-4 | adding a new game requires zero code changes | tests/Feature/Admin/GameResourcesPresentTest.php (admin can Create a new Game via UI — structural proof) + GameSeederTest (firstOrCreate idempotency preserves the D-007 contract) | `--filter='GameResourcesPresent|GameSeederTest'` |
      - REQ-* mapping table — 1 row: `| REQ-platform-vision | Phase 3 | Complete | All 4 SCs above |`
      - Pest results summary: total test count, pass/fail breakdown, Phase 3 contribution count
      - Quality gate status: Pint pass, PHPStan L8 pass, vue-tsc pass, shared-types check pass
      - Manual smokes deferred to operator (numbered list):
        1. Log in via Discord → /admin → click "Games" in sidebar → expect HLL row (seeded by plan 03-05) → click Edit → expect Profile + Audit tabs + Roles + MatchTypes relation manager tabs → expect 15 roles + 5 match types visible
        2. From /admin/games/{hll}/edit → click "Match types" tab → click "Scrim 50v50" row → expect navigate to /admin/game-match-types/{id}/edit (Pattern 2 click-through) → expect Profile + Audit + RoleLimits relation manager tab → expect 15 capacity rows
        3. From /admin/game-match-types/{scrim}/edit → click "Create" on RoleLimits → expect Select shows only HLL roles (Pattern 3 scoping) → set capacity, save → expect row appears
        4. Create a 2nd Game via /admin/games/create with key='cs2', name=['en'=>'Counter-Strike 2'] → expect ListGames page shows both HLL and CS2 → confirms D-007 (zero code changes for new game)
        5. Edit HLL.name['en']='Hell Let Loose v2' → save → re-run `make artisan ARGS="db:seed --class=GameSeeder"` → re-open Filament → expect name stayed 'Hell Let Loose v2' (firstOrCreate preserves admin edits per plan 03-05)
        6. /admin/audit → expect Phase 3 model entries appear (Game/GameRole/GameMatchType/GameMatchTypeRoleLimit) with subject_type FQNs
      - Sign-off line: "Phase 3 verified complete pending operator manual smokes; ROADMAP.md + REQUIREMENTS.md updated; ready for Phase 4."
  </acceptance_criteria>
  <action>
    Run the full quality gate sweep. Each command must return clean exit code. If anything is red, surface the failure and route back to the responsible plan for fix-up — do NOT write a falsely-green PHASE-VERIFICATION.

    Write 03-PHASE-VERIFICATION.md using `.planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md` as the structural template. Each SC row's "Test file" and "Pest filter" columns MUST point at extant files (verify via `ls apps/web/tests/Feature/Models/Game*.php apps/web/tests/Feature/Database/GameSeederTest.php apps/web/tests/Feature/Admin/GameResourcesPresentTest.php apps/web/tests/Feature/Admin/GameAuditLogTest.php apps/web/tests/Unit/Data/GameDataTest.php` before writing the citations).

    The Pest results summary should include the actual numbers from `make pest` output (total tests, passed count, time taken).

    The manual smokes list should be detailed enough that an operator who is not the implementer can run them step-by-step. Each smoke maps to one or more success criteria.

    Sign-off line should explicitly state Phase 3 is verified pending manual smokes, and that ROADMAP + REQUIREMENTS are updated.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | grep -E 'Tests:.*passed' | tail -1 && docker compose exec web ./vendor/bin/pint --test 2>&1 | grep -E '(PASS|FAIL)' | tail -3 && docker compose exec web ./vendor/bin/phpstan analyse --no-progress 2>&1 | tail -3 && grep -c '^| SC-' .planning/phases/03-games-match-types/03-PHASE-VERIFICATION.md</automated>
  </verify>
  <done>All quality gates GREEN; 03-PHASE-VERIFICATION.md written with 4 SC rows + 1 REQ row + 6 manual smoke steps; sign-off line present.</done>
</task>

<task type="auto">
  <name>Task 2: Update ROADMAP.md (Phase 3 plan list + status + progress row) + REQUIREMENTS.md (REQ-platform-vision Complete)</name>
  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/ROADMAP.md (current state — Phase 3 entry has the placeholder 14-plan list from a template copy; replace with actual 10-plan Phase 3 list)
    - .planning/REQUIREMENTS.md (current state — line 14 v1 Requirements checkbox + line 116 traceability table row for REQ-platform-vision)
    - .planning/phases/02-clans-tags/02-14-PLAN.md (Phase 2 ROADMAP update procedure — exact step-by-step)
  </read_first>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` updated:
      - Phase 3 top-line: `- [ ] **Phase 3: Games & match types** — ...` → `- [x] **Phase 3: Games & match types** — ...`
      - Phase 3 entry `**Plans**: 14 plans` → `**Plans**: 10 plans`
      - The 14-item placeholder plan checklist (which incorrectly copies Phase 2's plan list) REPLACED with the actual 10-item Phase 3 list:
        ```
        - [x] 03-01-PLAN.md — Wave 0 scaffolding (4 factory stubs + 7 test stubs + admin.php i18n key extension)
        - [x] 03-02-PLAN.md — Migrations (4 tables: games, game_roles, game_match_types, game_match_type_role_limits; UNIQUE + CHECK + cascade FKs)
        - [x] 03-03-PLAN.md — Models (4 new) + factories + 4 model tests (UNIQUE + CHECK + cross-game saving guard + audit)
        - [x] 03-04-PLAN.md — Spatie laravel-data DTOs (4) + TS regen + shared-types sync + GameDataTest
        - [x] 03-05-PLAN.md — GameSeeder (HLL preset: 1 Game + 15 Roles + 5 MatchTypes + capacity matrix) + idempotency tests
        - [x] 03-06-PLAN.md — GameResource (Filament) + Roles + MatchTypes RelationManagers
        - [x] 03-07-PLAN.md — GameMatchTypeResource (Filament) + RoleLimits RelationManager (Pattern 3 scoped Select) + MatchTypesRelationManager URL-override amendment
        - [x] 03-08-PLAN.md — GameResourcesPresentTest (admin reachability + RelationManager render checks + Pitfall 3 typo guard)
        - [x] 03-09-PLAN.md — i18n key coverage audit + GameAuditLogTest (D-012)
        - [x] 03-10-PLAN.md — [BLOCKING] phase verification + ROADMAP update + final quality gates
        ```
      - Progress table at the bottom: row `3. Games & match types | 0/TBD | Not started | -` → `3. Games & match types | 10/10 | Complete | 2026-05-13`
    - `.planning/REQUIREMENTS.md` updated:
      - Line 14 (v1 Requirements bullet): `- [ ] **REQ-platform-vision**: ...` → `- [x] **REQ-platform-vision**: ...`
      - Line 116 (Traceability table row): `| REQ-platform-vision | Phase 3 | Pending |` → `| REQ-platform-vision | Phase 3 | Complete |`
      - Coverage summary line at the bottom: bump completed-mappable count (5 → 6 if Phase 2 already counted; verify against current state)
  </acceptance_criteria>
  <action>
    Read .planning/ROADMAP.md end-to-end. Locate the `### Phase 3: Games & match types` section. Replace the placeholder 14-plan list (which is a structural copy of Phase 2's list) with the actual 10 plan entries above. Update `**Plans**: 14 plans` to `**Plans**: 10 plans`. Flip the top-line checkbox from `[ ]` to `[x]`. Update the bottom progress table row.

    Read .planning/REQUIREMENTS.md. Locate line 14's v1 Requirements `REQ-platform-vision` bullet — flip the checkbox to `[x]`. Locate line 116's traceability row — change `Pending` to `Complete`. Update the coverage summary numerics if any of them include "completed" counts (Phase 2's plan 02-14 set the precedent for what to update).

    Save both files. Run `make pint --test` and `make phpstan` for safety (markdown files don't affect these but worth a sanity check).
  </action>
  <verify>
    <automated>grep -E '\*\*Plans\*\*: 10 plans' .planning/ROADMAP.md && grep -E '^- \[x\] \*\*Phase 3' .planning/ROADMAP.md && grep -E '3\. Games & match types \| 10/10 \| Complete' .planning/ROADMAP.md && grep -E '^- \[x\] \*\*REQ-platform-vision\*\*' .planning/REQUIREMENTS.md && grep -E '\| REQ-platform-vision \| Phase 3 \| Complete \|' .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>ROADMAP.md and REQUIREMENTS.md updated with Phase 3 completion; greps verify all 5 expected lines.</done>
</task>

</tasks>

<verification>
- `make pest` -> ALL GREEN (Phase 1 + Phase 2 + Phase 3 — count > 250 tests)
- `make pint ARGS="--test"` -> GREEN
- `make phpstan` -> no errors
- `make pnpm ARGS="-F web run typecheck"` -> clean
- `make pnpm ARGS="-F shared-types check"` -> clean
- `grep -rE "placeholder.*Wave 0" apps/web/tests/` -> ZERO matches (all 7 Wave 0 stubs were replaced)
- ROADMAP.md Phase 3 entry shows 10 plans, status Complete
- REQUIREMENTS.md REQ-platform-vision shows Complete
- 03-PHASE-VERIFICATION.md exists; greps return SC-1, SC-2, SC-3, SC-4 rows
</verification>

<success_criteria>
Phase 3 closed cleanly. REQ-platform-vision satisfied. The next phase (Phase 4 — Matches manual) has a clean slate to build on (Game/GameRole/GameMatchType/GameMatchTypeRoleLimit infrastructure is ready for match-slot templating per D-010). Operator runs the 6 manual smokes documented in 03-PHASE-VERIFICATION.md before declaring Phase 3 truly shipped.
</success_criteria>

<output>
After completion, create `.planning/phases/03-games-match-types/03-10-SUMMARY.md` documenting:
- Final test count (Phase 1 + Phase 2 + Phase 3 combined)
- Quality gate snapshot (numbers from each tool)
- Manual smoke list (6 items)
- ROADMAP.md + REQUIREMENTS.md diff summary
- Notes for Phase 4 hand-off (Game/MatchType/RoleLimit ready for match-slot templating)
</output>
