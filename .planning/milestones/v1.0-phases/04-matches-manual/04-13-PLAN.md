---
phase: 04-matches-manual
plan: 13
type: execute
wave: 8
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09, 10, 11, 12]
files_modified:
  - apps/web/resources/js/types/api.d.ts
  - packages/shared-types/src/index.ts
  - .planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - REQ-goal-match-workflows
must_haves:
  truths:
    - "Full Phase 4 suite GREEN: make pest reports all Phase 4 tests as PASS (no incomplete remaining); make pint --test green; make phpstan L8 green; make pnpm ARGS=\"-F web run typecheck\" green; pnpm --filter @trenchwars/shared-types typecheck green (host-side per Phase 1 plan 01-16)"
    - "TypeScript types are regenerated (final consolidation) — api.d.ts contains all 8 Phase 4 DTOs; packages/shared-types/src/index.ts has 8 corresponding export type aliases"
    - "04-PHASE-VERIFICATION.md is committed mapping each ROADMAP SC (SC-1..SC-5) and REQ-goal-match-workflows to the specific test file + it() block that proves it; pattern follows Phase 2 plan 02-14 and Phase 3 plan 03-10 verbatim"
    - "ROADMAP.md Phase 4 entry is updated: status checked, plan checklist 13/13 complete, completion date filled in; bottom progress table updated to '4. Matches (manual) | 13/13 | Complete | YYYY-MM-DD'"
    - "REQUIREMENTS.md REQ-goal-match-workflows status flips to Complete; traceability table updated"
    - "Grep gates green: DB::transaction count > 0 in MatchSignupService.php; lockForUpdate count > 0 in MatchSignupService.php; static::observe(MatchObserver count > 0 in Match.php; events_one_per_owner constraint visible in psql"
  artifacts:
    - path: ".planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md"
      provides: "Phase 4 close artefact with SC-1..SC-5 + REQ-goal-match-workflows traceability"
      contains: "SC-1"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 marked Complete in roadmap"
      contains: "4. Matches (manual)"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-goal-match-workflows marked Complete"
      contains: "REQ-goal-match-workflows"
  key_links:
    - from: "PHASE-VERIFICATION SC-1"
      to: "tests/Feature/Admin/MatchResourceCreateWizardTest + tests/Feature/Services/MatchSlotMaterialiserServiceTest"
      via: "test file references for SC-1 'wizard creates Match + slots materialised'"
      pattern: "MatchResourceCreateWizardTest"
    - from: "PHASE-VERIFICATION SC-2"
      to: "tests/Feature/Services/MatchSignupServiceTest + tests/Feature/Services/MatchSignupConcurrencyTest"
      via: "test file references for SC-2 'capacity enforced under concurrency'"
      pattern: "MatchSignupConcurrencyTest"
    - from: "ROADMAP.md Phase 4 plans list"
      to: "13 plan files in 04-matches-manual/"
      via: "checklist update"
      pattern: "04-01-PLAN.md"
---

<objective>
Phase-close work: regenerate TypeScript types (final consolidation in case any DTO changed during Wave 4-7 work), run the full quality gate sweep (pest + pint + phpstan + pnpm typecheck), write the 04-PHASE-VERIFICATION.md mapping every ROADMAP SC and REQ to the specific test file that proves it, update ROADMAP.md to mark Phase 4 Complete, and update REQUIREMENTS.md to flip REQ-goal-match-workflows from Pending to Complete.

Purpose: Mirror Phase 2 plan 02-14 + Phase 3 plan 03-10 closure pattern. This is the human-readable proof that Phase 4 shipped — every SC has a passing test; every REQ has a satisfied traceability row.

Output: 1 regenerated api.d.ts + shared-types index; 1 PHASE-VERIFICATION.md; ROADMAP.md update; REQUIREMENTS.md update; final quality gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-matches-manual/04-RESEARCH.md
@.planning/phases/04-matches-manual/04-VALIDATION.md
@.planning/phases/02-clans-tags/02-14-PLAN.md
@.planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md
@.planning/phases/03-games-match-types/03-10-PLAN.md
@.planning/phases/03-games-match-types/03-PHASE-VERIFICATION.md
@CLAUDE.md

<interfaces>
Phase 2 + Phase 3 PHASE-VERIFICATION.md format (verified `.planning/phases/03-games-match-types/03-PHASE-VERIFICATION.md`):
- Header: phase metadata (number, slug, completion date, plans shipped count)
- Section "Roadmap Success Criteria Mapping": one row per SC; columns: SC ID, description (verbatim from ROADMAP), test file + it() block, status (PASS/SKIPPED with reason)
- Section "Phase Requirement Mapping": REQ-goal-match-workflows -> SC-1..SC-5 -> test references
- Section "Locked Decisions Honored": D-010, D-013, D-018 etc. — explicit confirmation each is satisfied
- Section "Quality Gates": Pest count, Pint, PHPStan L8, vue-tsc, shared-types typecheck — all GREEN with timestamps
- Section "Plan-Level Deviations": any Rule 1/2/3 deviations encountered during execution (e.g., HasManyThrough vs getEloquentQuery for MvpsRelationManager)
- Section "Manual Smoke" (deferred per Phase 1/2/3 idiom): operator walkthrough checklist (4-6 items: 1) admin creates Match via wizard, 2) public visitor opens /matches calendar, 3) auth user signs up to a slot, 4) admin enters result via Filament, 5) cancelled match disappears from calendar)
- Section "Out-of-Scope Items Deferred": Phase 5 (Discord bot signups), Phase 6 (Tournament bracket integration), Phase 7 (CMS Event aggregation), Phase 8 (RCON auto-result)

ROADMAP.md update procedure (per Phase 3 plan 03-10 idiom):
- Find "### Phase 4: Matches (manual)" section
- Update "**Goal**:" line if currently a placeholder — set to the verbatim phase goal
- Verify "**Requirements**: REQ-goal-match-workflows" line is correct
- Update the "**Plans**:" count to "13 plans" (if not already)
- Update the 13-plan checklist:
  - Replace any incorrect/legacy entries (the current ROADMAP shows duplicated Phase 2 entries for Phase 4 — these MUST be corrected)
  - 04-01-PLAN.md — Wave 0 scaffolding (factory stubs + Pest RED stubs + i18n key files)
  - 04-02-PLAN.md — Migrations (6 tables: matches, match_slots, match_access_rules, match_results, match_mvps, events)
  - 04-03-PLAN.md — Models + factories + User accessor amendment + 6 model tests
  - 04-04-PLAN.md — MatchStatusService + MatchNotOpenException + state machine test
  - 04-05-PLAN.md — MatchSlotMaterialiserService + snapshot semantics tests
  - 04-06-PLAN.md — MatchSignupService (D-010 row-lock) + 3 exception classes + 3 test files
  - 04-07-PLAN.md — 8 DTOs + TS regen + shared-types sync + 3 unit tests
  - 04-08-PLAN.md — MatchObserver + Match::booted() amendment + observer test
  - 04-09-PLAN.md — MatchResource + 4 RelationManagers + EventResource + MatchResultService + 3 admin tests
  - 04-10-PLAN.md — 3 controllers + signup FormRequest + routes + 3 feature tests
  - 04-11-PLAN.md — 2 Vue pages + 6 components + PublicLayout amendment
  - 04-12-PLAN.md — i18n key audit + upgraded presence test + audit log integration test
  - 04-13-PLAN.md — [BLOCKING] phase verification + ROADMAP + REQUIREMENTS update + final quality gates
- Update bottom progress table: "4. Matches (manual) | 13/13 | Complete | YYYY-MM-DD"

REQUIREMENTS.md update procedure:
- Find REQ-goal-match-workflows row in the Traceability table (currently "Pending")
- Update status to "Complete"
- Coverage line update: increment completed count (from 7 to 8 of 15 mappable)
- Bottom "Last updated" date

Final quality gates (Phase 1/2/3 idiom):
```
make pest                                              # full suite GREEN
make pint ARGS="--test"                                # CS gate
make phpstan                                            # L8 gate
make pnpm ARGS="-F web run typecheck"                  # Vue tsc
pnpm --filter @trenchwars/shared-types typecheck        # host-side per Phase 1 plan 01-16
docker compose exec web php artisan migrate:fresh --seed  # full migrate cycle
```

NOTE on duplicated Phase 4 plan entries in current ROADMAP.md: the ROADMAP.md file under "### Phase 4: Matches (manual)" currently lists Phase 2 plan entries (02-01..02-14) — this is a legacy from when the ROADMAP was templated. Plan 04-13 fixes this by replacing the duplicated/incorrect entries with the actual Phase 4 plan list.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase verification report to future readers | The report is the human-readable proof that Phase 4 shipped; integrity is preserved by versioning the .md file in git |
| ROADMAP update to STATE.md | ROADMAP is the canonical phase tracker; STATE.md mirrors it; this plan touches both |
| Final quality gates to deploy readiness | All gates green = Phase 4 is deployable; failing gates block merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13-01 | Repudiation | Phase verification claims tests pass when they don't | mitigate | Final `make pest` run must report all GREEN before the report is committed; the report itself cites test file paths the reader can re-run |
| T-04-13-02 | Tampering | ROADMAP.md plan list out of sync with actual plan files | mitigate | Plan checklist is enumerated from `ls .planning/phases/04-matches-manual/04-*-PLAN.md`; mismatch surfaces as a manual audit |
| T-04-13-03 | Information Disclosure | Manual smoke checklist exposes operator credentials | accept | Smoke walkthrough uses local dev environment with developer-controlled credentials; no production secrets in the report |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Final TypeScript regen + 04-PHASE-VERIFICATION.md + ROADMAP.md update + REQUIREMENTS.md update + final quality gates</name>
  <files>
    apps/web/resources/js/types/api.d.ts,
    packages/shared-types/src/index.ts,
    .planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md,
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/phases/03-games-match-types/03-PHASE-VERIFICATION.md (Phase 3 close artefact — verbatim template for SC-1..SC-N traceability + quality gates + manual smoke)
    - .planning/phases/02-clans-tags/02-PHASE-VERIFICATION.md (Phase 2 close artefact — analog with 5 SCs)
    - .planning/phases/01-foundations/01-PHASE-VERIFICATION.md if exists (Phase 1 close artefact — earliest template)
    - .planning/ROADMAP.md (verify current Phase 4 section state — note the legacy Phase 2 entries that need replacement)
    - .planning/REQUIREMENTS.md (find REQ-goal-match-workflows row in Traceability table)
    - .planning/phases/04-matches-manual/04-VALIDATION.md (Per-Task Verification Map — source of test-to-plan mapping)
  </read_first>
  <acceptance_criteria>
    - Step 1 — TS regen: `make artisan ARGS="trenchwars:typescript-generate"` runs successfully; api.d.ts contains 8 Phase 4 DTOs (MatchData, MatchSlotData, MatchAccessRuleData, MatchResultData, MatchMvpData, EventData, PublicMatchOccupantData, PublicMatchData); packages/shared-types/src/index.ts has 8 corresponding export type aliases (idempotent — already added in plan 04-07 task 2; this task verifies they survived any later-wave changes)
    - Step 2 — PHASE-VERIFICATION.md committed at `.planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md`:
      - Header: `# Phase 4: Matches (manual) — Verification Report`
      - Metadata block: phase, slug (matches-manual), completed date, plans shipped (13)
      - Section "Roadmap Success Criteria Mapping" with 5 rows (SC-1..SC-5), each citing 1-3 test files
      - Section "Phase Requirement Mapping" — REQ-goal-match-workflows -> SC-1..SC-5 chain
      - Section "Locked Decisions Honored" — D-010, D-013, D-018, plus the Phase-4 architectural choices (Match class name per A4, snapshot-at-create per A1, allowlist semantics per A2, polymorphic events per A3)
      - Section "Quality Gates" with the result of each final-run command (Pest count, Pint, PHPStan, vue-tsc, shared-types typecheck)
      - Section "Plan-Level Deviations" — any Rule 1/2/3 deviations from plans 04-01..04-12 (e.g., HasManyThrough vs getEloquentQuery for MvpsRelationManager; pcntl availability for concurrency test; any factory cross-game default issue)
      - Section "Manual Smoke Walkthrough" — 5-item operator checklist
      - Section "Out-of-Scope Items Deferred to Future Phases" — Discord signups (Phase 5), Tournament bracket (Phase 6), CMS Event aggregation (Phase 7), RCON auto-result (Phase 8)
    - Step 3 — ROADMAP.md updated:
      - Phase 4 entry: status `[x]` (was `[ ]`)
      - Goal line: verbatim from "Make scheduling and recording matches the primary platform workflow..." (verify present; if placeholder, set)
      - Plans list: 13 entries per `<interfaces>` enumeration (REPLACES the legacy Phase 2 entries currently shown under Phase 4 section)
      - Bottom progress table: "4. Matches (manual) | 13/13 | Complete | YYYY-MM-DD"
    - Step 4 — REQUIREMENTS.md updated:
      - REQ-goal-match-workflows row in Traceability table: status "Complete" (was "Pending")
      - Active list: confirm the "[ ] Structured match/tournament workflows replacing ad-hoc Discord scheduling (REQ-goal-match-workflows)" line in PROJECT.md Active section is moved to Validated section (NB: PROJECT.md and REQUIREMENTS.md are separate; REQUIREMENTS.md is the canonical traceability)
      - Coverage line at bottom of REQUIREMENTS.md: updated counts (was 7/15, now 8/15 complete)
      - "Last updated" date at bottom: today's date
    - Step 5 — Final quality gate sweep (must ALL be green):
      - `make pest` returns 0 failures, 0 incomplete (or only the pcntl-skipped concurrency test if pcntl absent — documented in the report)
      - `make pint ARGS="--test"` green
      - `make phpstan` green (PHPStan L8 — no new errors)
      - `make pnpm ARGS="-F web run typecheck"` green
      - `pnpm --filter @trenchwars/shared-types typecheck` (host-side) green
      - `make artisan ARGS="migrate:fresh --seed"` succeeds end-to-end
    - Step 6 — Grep gate verification:
      - `grep -c 'DB::transaction' apps/web/app/Services/MatchSignupService.php` returns at least 1
      - `grep -c 'lockForUpdate' apps/web/app/Services/MatchSignupService.php` returns at least 1
      - `grep -c 'static::observe(MatchObserver' apps/web/app/Models/Match.php` returns at least 1
      - psql `\d events` shows events_one_per_owner index
    - Step 7 — STATE.md update (per Phase 3 plan 03-10 idiom):
      - Update progress: completed_phases: 4 (was 3); total_plans: 55 (42 + 13); completed_plans: 55; percent: ~44% (4 of 9)
      - Update stopped_at: "Phase 4 COMPLETE — 04-13 plan executed, 04-PHASE-VERIFICATION.md written, ROADMAP marked 13/13 Complete YYYY-MM-DD; ready for Phase 5 (Discord bot v1)"
      - Accumulated Context: append Phase 4 decision log entries (per-plan deviations, key idioms learned)
  </acceptance_criteria>
  <action>
    Execute the close work in order:

    1. Run `make artisan ARGS="trenchwars:typescript-generate"` to refresh api.d.ts. Verify 8 Phase 4 DTOs present via grep. Verify packages/shared-types/src/index.ts has 8 corresponding aliases (added in plan 04-07; verify survived).

    2. Write `04-PHASE-VERIFICATION.md` using Phase 3 03-PHASE-VERIFICATION.md as the template. Substitute Phase 4 specifics:
       - SC-1 -> tests/Feature/Admin/MatchResourceCreateWizardTest::it_admin_can_create_match_via_wizard + tests/Feature/Services/MatchSlotMaterialiserServiceTest::it_produces_50_slots_for_scrim_50v50
       - SC-2 -> tests/Feature/Services/MatchSignupServiceTest::it_throws_capacity_exceeded + tests/Feature/Services/MatchSignupConcurrencyTest (note pcntl status)
       - SC-3 -> tests/Feature/Matches/MatchCalendarPageTest + tests/Feature/Matches/MatchShowPageTest (privacy strip path)
       - SC-4 -> tests/Feature/Services/MatchResultServiceTest::it_flips_status_to_played + tests/Feature/Admin/MatchAuditLogTest
       - SC-5 -> tests/Feature/Services/MatchSignupTagRestrictedTest + tests/Feature/Observers/MatchEventSyncTest

    3. Update ROADMAP.md:
       - Read current file; locate "### Phase 4: Matches (manual)"
       - Update the Plans count + checklist (currently shows legacy 02-* entries — replace with 04-01..04-13)
       - Flip the top-level "- [ ]" to "- [x]" for Phase 4
       - Update bottom progress table

    4. Update REQUIREMENTS.md:
       - Find REQ-goal-match-workflows row in Traceability table; change Status from "Pending" to "Complete"
       - Update coverage count + "Last updated" date

    5. Run final quality gates in order. If any gate fails, fix BEFORE writing the report (the report MUST claim gates green truthfully).

    6. Run grep gates per acceptance criteria; record results in the verification report.

    7. Update STATE.md per Phase 3 03-10 idiom — progress counters + stopped_at + Accumulated Context.

    Commit everything in this task via execute-plan's git commit; the commit message follows the Phase 1/2/3 idiom: `docs(04-13): complete Phase 4 plan — 04-PHASE-VERIFICATION.md + STATE.md update + ROADMAP marked Complete`
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | tail -5 && ls .planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md && grep -c '\[x\] \*\*Phase 4' .planning/ROADMAP.md && grep -A 1 'REQ-goal-match-workflows' .planning/REQUIREMENTS.md | grep -c 'Complete' && grep -c 'DB::transaction' apps/web/app/Services/MatchSignupService.php && grep -c 'lockForUpdate' apps/web/app/Services/MatchSignupService.php && grep -c 'static::observe(MatchObserver' apps/web/app/Models/Match.php</automated>
  </verify>
  <done>04-PHASE-VERIFICATION.md committed; ROADMAP.md + REQUIREMENTS.md + STATE.md updated; full quality gate sweep green; all grep gates pass; api.d.ts contains 8 Phase 4 DTOs; Phase 4 marked Complete in roadmap.</done>
</task>

</tasks>

<verification>
- `make pest` -> 0 failures, all Phase 4 tests GREEN (pcntl-skipped concurrency test acceptable per Pitfall 4 fallback)
- `make pint ARGS="--test"` -> green
- `make phpstan` -> green
- `make pnpm ARGS="-F web run typecheck"` -> green
- `pnpm --filter @trenchwars/shared-types typecheck` -> green
- `make artisan ARGS="migrate:fresh --seed"` -> succeeds end-to-end
- `ls .planning/phases/04-matches-manual/04-PHASE-VERIFICATION.md` -> file exists
- `grep '\[x\] \*\*Phase 4' .planning/ROADMAP.md` -> matches the line
- `grep 'REQ-goal-match-workflows' .planning/REQUIREMENTS.md` shows "Complete" not "Pending"
- All grep gates (DB::transaction in MatchSignupService.php > 0; lockForUpdate > 0; MatchObserver registration in Match.php > 0) return positive
</verification>

<success_criteria>
Phase 4 is officially complete. All 5 SCs are mapped to passing tests. REQ-goal-match-workflows is satisfied. The platform now supports the SC-1 admin path (Filament wizard creates Match + materialised slots + signups auto-open), SC-2 capacity enforcement (D-010 row-lock proven under concurrent contention), SC-3 public surface (/matches calendar + /matches/{id} detail with privacy-stripped slots), SC-4 admin result entry (atomic status flip via MatchResultService), and SC-5 tag-restricted access + observer-synced events. Phase 5 (Discord bot v1) can now build on the MatchSignupService + MatchResultService + MatchData/PublicMatchData primitives.
</success_criteria>

<output>
After completion, create `.planning/phases/04-matches-manual/04-13-SUMMARY.md` documenting:
- Final test count (e.g., "Phase 1+2+3+4: 340+ tests, X assertions; Phase 4-specific: ~75+ tests")
- Final quality gates result (each command + green/red status)
- 04-PHASE-VERIFICATION.md table of SC -> test references
- ROADMAP.md state delta (Phase 4 marked Complete)
- REQUIREMENTS.md REQ-goal-match-workflows -> Complete transition
- Grep gate results (DB::transaction + lockForUpdate + MatchObserver registration)
- Any Phase 4 plan-level deviations encountered during execution + their resolution
- pcntl availability status + concurrency test outcome (PASS or SKIPPED)
- The 5-item manual smoke walkthrough handed off to the operator
</output>
</content>
</invoke>
