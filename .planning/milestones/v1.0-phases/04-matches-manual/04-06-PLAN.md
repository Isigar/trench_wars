---
phase: 04-matches-manual
plan: 06
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - apps/web/app/Services/MatchSignupService.php
  - apps/web/app/Exceptions/CapacityExceededException.php
  - apps/web/app/Exceptions/TagRestrictedException.php
  - apps/web/app/Exceptions/AlreadySignedUpException.php
  - apps/web/tests/Feature/Services/MatchSignupServiceTest.php
  - apps/web/tests/Feature/Services/MatchSignupConcurrencyTest.php
  - apps/web/tests/Feature/Services/MatchSignupTagRestrictedTest.php
autonomous: true
requirements:
  - REQ-goal-match-workflows
must_haves:
  truths:
    - "MatchSignupService::signup(Match $match, User $user, GameRole $gameRole): MatchSlot is the SINGLE production write path to match_slots.occupant_user_id"
    - "Service wraps the entire flow in DB::transaction(function () { ... }) (Pitfall 1 — outside-the-transaction lockForUpdate is a no-op)"
    - "Service acquires Match-row exclusive lock via Match::lockForUpdate()->findOrFail($match->id) INSIDE the transaction (Pattern 2 — lock parent, not slot, to serialise concurrent signups for the same match)"
    - "Service order of guards: 1) status === 'open' (else MatchNotOpenException); 2) tag access allowlist (else TagRestrictedException); 3) idempotency / one-slot-per-user-per-match (else AlreadySignedUpException); 4) capacity check (else CapacityExceededException); 5) claim lowest-index empty slot (firstOrFail)"
    - "Tag access check (Pattern 5): empty match_access_rules = open to all; non-empty = user's activeClanMembership->clan->tags must intersect match.accessRules.clan_tag_id (no active clan -> blocked when rules exist)"
    - "MatchSignupServiceTest covers each exception class (4 negative cases) + happy path (lowest-index claim) + role-full triggers CapacityExceededException + idempotency (second signup same user same match -> AlreadySignedUpException)"
    - "MatchSignupConcurrencyTest uses pcntl_fork to spawn 2 child processes each attempting signup to the LAST available slot; parent asserts exactly one child succeeded and one threw CapacityExceededException; pcntl extension absence is guarded via test()->markTestSkipped helper (Pitfall 4 + Assumption A8 fallback)"
    - "MatchSignupTagRestrictedTest covers SC-5: signup allowed when user clan has allowed tag, blocked when tags do not intersect, allowed when match has zero rules (open semantics)"
  artifacts:
    - path: "apps/web/app/Services/MatchSignupService.php"
      provides: "D-010 row-locked signup service — single write path to match_slots occupancy"
      contains: "lockForUpdate"
    - path: "apps/web/app/Exceptions/CapacityExceededException.php"
      provides: "Typed exception for SC-2 422 response"
      contains: "DomainException"
    - path: "apps/web/tests/Feature/Services/MatchSignupConcurrencyTest.php"
      provides: "Parallel-process race-condition assertion (Pitfall 4)"
      contains: "pcntl_fork"
  key_links:
    - from: "MatchSignupService::signup"
      to: "Match::lockForUpdate()->findOrFail"
      via: "Pattern 2 row-lock pattern inside DB::transaction"
      pattern: "lockForUpdate"
    - from: "MatchSignupService::tagAccessAllowed"
      to: "user->activeClanMembership->clan->tags intersect match->accessRules"
      via: "Eloquent collection intersect"
      pattern: "intersect"
    - from: "MatchSignupConcurrencyTest"
      to: "pcntl_fork or DB::connection alias fallback"
      via: "two separate processes/connections holding open transactions"
      pattern: "pcntl_fork"
---

<objective>
Land D-010's heart: the row-locked transactional signup service that enforces SC-2 ("live count of confirmed signups can never exceed slot capacity"). The service is THE single production write path to match_slots.occupant_user_id. It applies five guards in order (status, tag access, idempotency, capacity, claim) inside a single Postgres row-lock transaction so two simultaneous players clicking "Sign up" on the last available slot resolve to exactly one success and one CapacityExceededException — never two overflowing signups.

The plan also lands the 3 typed exception classes consumed by plan 04-10's MatchSignupController (which catches them and converts to 422 with localized message), and the concurrency test using pcntl_fork that PROVES the lock works under real parallel contention (Pitfall 4 — naive single-threaded Pest cannot exercise the race). The pcntl extension may be absent in the web container; the test guards via markTestSkipped() and documents the fallback to a dual-connection DB alias approach inline.

Purpose: SC-2 is the single Phase 4 success criterion that cannot be met by any other means. Bot signups (Phase 5) reuse THIS service via the bot:* Sanctum token. The row-lock is the only correctness guarantee against the race.

Output: 1 service, 3 exception classes, 3 GREEN test files. The Wave 0 RED stubs for MatchSignupServiceTest, MatchSignupConcurrencyTest, MatchSignupTagRestrictedTest are flipped GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-matches-manual/04-RESEARCH.md
@CLAUDE.md
@apps/web/app/Services/ClanInviteService.php
@apps/web/app/Services/MatchStatusService.php
@apps/web/app/Exceptions/MatchNotOpenException.php
@apps/web/app/Exceptions/ReservedSlugException.php
@apps/web/app/Models/Match.php
@apps/web/app/Models/MatchSlot.php
@apps/web/app/Models/MatchAccessRule.php
@apps/web/app/Models/User.php
@apps/web/app/Models/Clan.php

<interfaces>
RESEARCH Pattern 2 (verbatim — full method body for signup):

```
public function signup(MatchModel $match, User $user, GameRole $gameRole): MatchSlot
{
    return DB::transaction(function () use ($match, $user, $gameRole): MatchSlot {
        // 1. Acquire row-level exclusive lock on the parent Match row.
        $locked = MatchModel::lockForUpdate()->findOrFail($match->id);

        // 2. Status guard.
        if ($locked->status !== 'open') {
            throw new MatchNotOpenException(__('matches.signup.error.not_open'));
        }

        // 3. Tag access allowlist (Pattern 5; SC-5).
        if (! $this->tagAccessAllowed($user, $locked)) {
            throw new TagRestrictedException(__('matches.signup.error.tag_restricted'));
        }

        // 4. Idempotency / one slot per user per match.
        $existing = MatchSlot::where('match_id', $locked->id)
            ->where('occupant_user_id', $user->id)
            ->first();
        if ($existing !== null) {
            throw new AlreadySignedUpException(__('matches.signup.error.already_signed_up'));
        }

        // 5. Capacity check.
        $occupiedCount = MatchSlot::where('match_id', $locked->id)
            ->where('game_role_id', $gameRole->id)
            ->whereNotNull('occupant_user_id')
            ->count();
        $totalCapacity = MatchSlot::where('match_id', $locked->id)
            ->where('game_role_id', $gameRole->id)
            ->count();
        if ($occupiedCount >= $totalCapacity) {
            throw new CapacityExceededException(__('matches.signup.error.capacity_full'));
        }

        // 6. Find the lowest-index empty slot and claim it.
        $emptySlot = MatchSlot::where('match_id', $locked->id)
            ->where('game_role_id', $gameRole->id)
            ->whereNull('occupant_user_id')
            ->orderBy('slot_index')
            ->firstOrFail();
        $emptySlot->update([
            'occupant_user_id' => $user->id,
            'confirmed_at' => now(),
        ]);
        return $emptySlot;
    });
}

private function tagAccessAllowed(User $user, MatchModel $match): bool
{
    if ($match->accessRules()->count() === 0) {
        return true;
    }
    $userClan = $user->activeClanMembership?->clan;
    if ($userClan === null) {
        return false;
    }
    $userTagIds = $userClan->tags()->pluck('clan_tags.id');
    $allowedTagIds = $match->accessRules()->pluck('clan_tag_id');
    return $userTagIds->intersect($allowedTagIds)->isNotEmpty();
}
```

Exception classes (3 — match the Phase 2 ReservedSlugException one-liner shape, extending \DomainException per Pattern 2 alignment with MatchNotOpenException from plan 04-04):
- `final class CapacityExceededException extends \DomainException {}`
- `final class TagRestrictedException extends \DomainException {}`
- `final class AlreadySignedUpException extends \DomainException {}`

Each in `apps/web/app/Exceptions/` with `<?php\ndeclare(strict_types=1); namespace App\Exceptions;` + header docblock citing plan + threat ref.

Concurrency test pattern (Pitfall 4 + Assumption A8):
- The test file MUST include the pcntl pre-flight comment block (already in plan 04-01's stub header)
- First `it()` body: `if (!extension_loaded('pcntl')) { test()->markTestSkipped('pcntl extension not available — fallback documented in file header.'); }` — guard BEFORE the fork attempt
- After the guard, parent forks 2 children; each child opens its own DB connection (`DB::reconnect()` after fork — Postgres connections cannot be shared across forked processes); each child attempts signup() on the SAME match's LAST available slot; parent waits via `pcntl_waitpid`; parent reads exit statuses and DB state; asserts (a) exactly 1 success exit code 0, exactly 1 failure non-zero, (b) match_slots count of occupied slots for that role equals total capacity (no overflow)

Alternative (dual-connection) for pcntl-absent fallback (per RESEARCH Pitfall 4 option 2):
- Configure a second DB connection in config/database.php named 'testing_parallel' (same DSN as default).
- Use `DB::connection('testing_parallel')->transaction(...)` in a second closure inside the test; this exercises the row-lock semantics in a single-process simulation. Documented in the test file's header comment.

pcntl_fork idiom (Pest 4 — paste into the test body verbatim):
```
$childA = pcntl_fork();
if ($childA === 0) {
    DB::reconnect();
    try { app(MatchSignupService::class)->signup($match, $userA, $role); exit(0); }
    catch (\Throwable $e) { exit(1); }
}
$childB = pcntl_fork();
if ($childB === 0) {
    DB::reconnect();
    try { app(MatchSignupService::class)->signup($match, $userB, $role); exit(0); }
    catch (\Throwable $e) { exit(1); }
}
pcntl_waitpid($childA, $statusA);
pcntl_waitpid($childB, $statusB);
$successes = (pcntl_wexitstatus($statusA) === 0 ? 1 : 0) + (pcntl_wexitstatus($statusB) === 0 ? 1 : 0);
expect($successes)->toBe(1);
```

i18n keys consumed (from plan 04-01 matches.php):
- matches.signup.error.not_open
- matches.signup.error.tag_restricted
- matches.signup.error.already_signed_up
- matches.signup.error.capacity_full
- matches.signup.error.no_active_clan (optional)

User accessor `activeClanMembership` was added in plan 04-03 task 2. Verify presence before this plan ships (grep for the method name on User.php).

NOTE on Pitfall 5: Service file imports `use App\Models\Match as MatchModel;` and the method signature reads `MatchModel $match`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller (controller/bot) to signup() | Service is the SOLE production write path to match_slots.occupant_user_id; controller passes typed User + GameRole, not request body |
| Service to DB | DB::transaction + lockForUpdate is the canonical Postgres serialisation primitive; READ COMMITTED isolation + parent-row exclusive lock makes the COUNT-then-UPDATE atomic |
| Service to activity_log | MatchSlot LogsActivity (plan 04-03) writes the update event automatically |
| Tag access to match_access_rules | Eloquent pluck of clan_tag_id is parameter-bound; intersect happens in PHP, never via raw SQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Tampering (CRITICAL — SC-2) | Capacity bypass via concurrent signups | mitigate | D-010 row-locked transaction (Pattern 2): Match::lockForUpdate()->findOrFail() INSIDE DB::transaction serialises ALL signups for the same match; Postgres translates this to SELECT ... FOR UPDATE which blocks parallel writers. Proven by MatchSignupConcurrencyTest (Pitfall 4 pcntl_fork) |
| T-04-06-02 | Elevation of Privilege | Tag-access bypass | mitigate | tagAccessAllowed() server-side check; UI never trusted; signup controller never accepts request body fields other than game_role_id |
| T-04-06-03 | Spoofing | IDOR — signing up another user | mitigate | Service signature requires User $user — controller passes auth()->user(), never accepts user_id from request |
| T-04-06-04 | Tampering | Mass-assignment on MatchSlot.occupant_user_id | mitigate | $fillable on MatchSlot includes occupant_user_id (plan 04-03 deliberately fillable so service can write); MatchSignupService is the only production write path; LogsActivity audits every write |
| T-04-06-05 | Tampering | Status race (admin manually flips to "played" while signup is in flight) | mitigate | The status guard reads $locked->status INSIDE the transaction AFTER the row-lock; admin flips via MatchStatusService (plan 04-04) which also wraps in DB::transaction — both transactions cannot hold the Match row lock simultaneously |
| T-04-06-06 | DoS | Slow-lock attack (open transaction held indefinitely) | accept | Phase 4 has no enforcement of transaction timeout; signup transactions are sub-100ms in normal operation; future hardening via statement_timeout if needed |
| T-04-06-07 | Information Disclosure | Tag access leak (probing match.accessRules existence) | accept | match_access_rules table is admin-readable via Filament; public visitors see "Tag-restricted" badge on /matches/{id} (plan 04-11); no IDs disclosed |
| T-04-06-08 | Tampering | lockForUpdate outside transaction (Pitfall 1) | mitigate | Service is the SINGLE call site; grep gate in plan 04-13 verifies `DB::transaction` count in MatchSignupService.php is greater than zero |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 3 exception classes + MatchSignupService + tag access helper + MatchSignupServiceTest (happy + 4 negative paths)</name>
  <files>
    apps/web/app/Exceptions/CapacityExceededException.php,
    apps/web/app/Exceptions/TagRestrictedException.php,
    apps/web/app/Exceptions/AlreadySignedUpException.php,
    apps/web/app/Services/MatchSignupService.php,
    apps/web/tests/Feature/Services/MatchSignupServiceTest.php
  </files>
  <read_first>
    - apps/web/app/Services/ClanInviteService.php (Phase 2 DB::transaction analog; activity-log integration)
    - apps/web/app/Services/MatchStatusService.php (plan 04-04 — Phase 4 service pattern; activity audit write idiom)
    - apps/web/app/Exceptions/MatchNotOpenException.php (plan 04-04 — exception class one-liner pattern, extends DomainException)
    - apps/web/app/Models/User.php (plan 04-03 — verify activeClanMembership exists via grep)
    - apps/web/app/Models/MatchSlot.php (plan 04-03 — occupant_user_id fillable)
    - apps/web/app/Models/Match.php (plan 04-03 — accessRules HasMany)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pattern 2 — full method body; Pitfall 1, 2, 4)
    - Context7 query (only if uncertain about Eloquent lockForUpdate semantics): mcp__context7__query-docs library /websites/laravel query "queries pessimistic-locking transaction"
  </read_first>
  <acceptance_criteria>
    - 3 exception class files in apps/web/app/Exceptions/:
      - Each declares `<?php\ndeclare(strict_types=1); namespace App\Exceptions;`
      - Each has a header docblock citing 04-06-PLAN Task 1 + threat ref + the SC each maps to (T-04-06-01 / SC-2 for CapacityExceededException; T-04-06-02 / SC-5 for TagRestrictedException; T-04-06-04 for AlreadySignedUpException)
      - Each: `final class XException extends \DomainException {}` (one-liner body)
    - `MatchSignupService.php`:
      - `<?php\ndeclare(strict_types=1); namespace App\Services;`
      - Imports: AlreadySignedUpException, CapacityExceededException, MatchNotOpenException, TagRestrictedException from App\Exceptions; GameRole, MatchSlot, User from App\Models; App\Models\Match as MatchModel (Pitfall 5 alias); Illuminate\Support\Facades\DB
      - `final class MatchSignupService`
      - `public function signup(MatchModel $match, User $user, GameRole $gameRole): MatchSlot` — body per `<interfaces>` verbatim (DB::transaction; lockForUpdate->findOrFail; 5 guards in order; claim lowest-index slot)
      - `private function tagAccessAllowed(User $user, MatchModel $match): bool` — body per `<interfaces>` verbatim
      - Method docblock on signup() explicitly cites T-04-06-01 (SC-2 D-010) and the 5-guard order
      - Method docblock on tagAccessAllowed() cites Pattern 5 (allowlist semantics; empty = open)
    - `MatchSignupServiceTest.php` REPLACES Wave 0 stub:
      - it('signs up a user to the lowest-index empty slot') — happy path; assert returned $slot->slot_index === 0, $slot->occupant_user_id === $user->id, $slot->confirmed_at !== null
      - it('throws MatchNotOpenException when match.status is not open') — set status='draft' or 'locked'; expect to throw
      - it('throws AlreadySignedUpException on second signup for same user same match') — first signup succeeds; second signup (different game_role even) throws because one-per-user-per-match
      - it('throws CapacityExceededException when role is full') — fill the role's capacity, attempt one more; expect to throw
      - it('writes confirmed_at to NOW on success')
      - it('claims the lowest-numbered empty slot_index, deterministic') — pre-fill slots 0,1,2 then attempt signup; assert returned slot_index === 3
      - it('updates match_slots.occupant_user_id atomically with confirmed_at') — verify with $slot->fresh()
      - it('writes an activity log entry on slot update') — MatchSlot LogsActivity (plan 04-03)
      - Use App\Models\Match imported aliased as MatchModel (Pitfall 5)
      - Remove the Wave 0 'placeholder' literal (grep returns no hits)
    - `make pest ARGS="--filter=MatchSignupServiceTest"` GREEN (8+ tests)
    - `make phpstan` reports no new errors
    - `make pint ARGS="--test app/Services/MatchSignupService.php app/Exceptions tests/Feature/Services/MatchSignupServiceTest.php"` clean
  </acceptance_criteria>
  <action>
    Build the 3 exception classes first (each ~10 lines). Then build MatchSignupService verbatim from RESEARCH Pattern 2. Key technical points:

    1. The 5-guard order is FIXED: status, tag access, idempotency, capacity, claim. Cheap-first is also the right order for friendly UX (status check is one column read; capacity is 2 DB COUNT queries).

    2. The lockForUpdate IS the critical line. Without `MatchModel::lockForUpdate()->findOrFail($match->id)` INSIDE the DB::transaction, the whole concurrency guarantee evaporates (Pitfall 1). The lock acquires when findOrFail runs and HOLDS until the closure returns or throws.

    3. The Match $match parameter is loaded OUTSIDE the transaction (route model binding). The `$locked = MatchModel::lockForUpdate()->findOrFail($match->id)` RE-READS the row INSIDE the transaction WITH the lock. The state observed via $locked is what we act on — $match->refresh() would NOT acquire a lock.

    4. The status check uses $locked->status (the freshly-read locked version), NOT $match->status (the stale version passed in). Critical because admin could have flipped status to cancelled between controller dispatch and lock acquisition.

    5. The capacity check uses 2 queries: occupiedCount + totalCapacity. totalCapacity is the COUNT of all match_slots for (match_id, game_role_id) — equivalent to original RoleLimit.capacity (Pattern 3 snapshot). The compare is `>=` not `==` to defend against any hypothetical overflow.

    6. The claim uses `firstOrFail()` on the empty-slot query. Because we already checked occupiedCount < totalCapacity inside the locked transaction, an empty slot MUST exist; firstOrFail's exception is unreachable in practice but defensive.

    7. For the test file: use Pest 4 beforeEach if helpful for seeding the match+role+slots. Each test creates a fresh setup. The SC-5 tag-restricted cases live in MatchSignupTagRestrictedTest (task 3 below), not here — keep this file focused on the 4 service-level guards + happy path.

    8. NOTE on activity log writes: MatchSlot has LogsActivity (plan 04-03). Updating occupant_user_id triggers an 'updated' activity entry automatically. Assert via Activity::query()->where('subject_type', MatchSlot::class)->where('event', 'updated')->exists().

    Run `make pest --filter=MatchSignupServiceTest`. Run pint + phpstan.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Services/MatchSignupServiceTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:|placeholder)' | tail -10</automated>
  </verify>
  <done>3 exception classes + MatchSignupService + GREEN test committed; 8+ assertions GREEN; phpstan + pint clean; 'placeholder' literal removed.</done>
</task>

<task type="auto">
  <name>Task 2: MatchSignupConcurrencyTest (pcntl_fork parallel-process race) + DB::transaction grep gate verification</name>
  <files>
    apps/web/tests/Feature/Services/MatchSignupConcurrencyTest.php
  </files>
  <read_first>
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pitfall 4 — pcntl_fork test methodology + Assumption A8 fallback)
    - apps/web/app/Services/MatchSignupService.php (just-shipped from task 1)
    - The existing Wave 0 stub at apps/web/tests/Feature/Services/MatchSignupConcurrencyTest.php (preserve the pcntl pre-flight comment from plan 04-01)
  </read_first>
  <acceptance_criteria>
    - `MatchSignupConcurrencyTest.php` REPLACES Wave 0 stub but PRESERVES the pcntl pre-flight comment header from plan 04-01
    - First line of `it()` body checks `if (!extension_loaded('pcntl')) { test()->markTestSkipped(...); }` — skip-when-absent guard
    - After the guard, the test:
      1. Creates a Match with a SINGLE-slot role: GameMatchType with one RoleLimit capacity=1; materialise to get 1 slot
      2. Creates 2 distinct Users (each with an empty privacy / no clan setup — Pattern 5 path: zero rules => open)
      3. Forks 2 child processes per the pcntl_fork idiom in `<interfaces>`
      4. Each child: DB::reconnect(); call signup(); exit code 0 on success, 1 on any exception
      5. Parent: pcntl_waitpid both children; sum success exit codes
      6. Assert exactly 1 success (one child succeeded; one child got CapacityExceededException)
      7. Assert MatchSlot occupied count for the role === 1 (capacity); no overflow
      8. Add a SECOND `it()` block running the SAME race with 5 children attempting signup to a 3-capacity role; assert exactly 3 successes; 2 fail; final occupied count === 3
    - File contains a `pcntl` literal AND a `markTestSkipped` literal (defensive guard verified)
    - The Wave 0 'placeholder' literal is removed
    - `make pest ARGS="--filter=MatchSignupConcurrencyTest"` GREEN or SKIPPED (depending on pcntl availability; verify via plan 04-01 summary recorded pcntl status)
    - `make phpstan` reports no new errors
  </acceptance_criteria>
  <action>
    The concurrency test is the SC-2 proof. If pcntl is available (likely — plan 04-01 task 1 records the verification result), the test runs; if not, it skips with the documented fallback path noted inline.

    Implementation:
    1. Preserve the pcntl pre-flight comment block from the Wave 0 stub (plan 04-01 added it).
    2. Inside `it()`, the FIRST statement is the skip guard.
    3. Build the match + role + 1-capacity slot setup. The match status must be 'open'. No access rules (open to all).
    4. Build 2 users. They need NO active clan (tag access path: zero rules => allowed; no clan check skipped because rules count === 0).
    5. Fork twice. Critical: BEFORE the fork, close the parent's DB connection (or reconnect after fork) so each child has its own Postgres connection. `DB::reconnect()` in the child is the canonical pattern.
    6. Each child wraps signup() in try/catch and exits with 0 (success) or 1 (any throw).
    7. Parent: pcntl_waitpid for both; assert exactly 1 succeeded; assert DB final state matches capacity.

    For the SECOND `it()` block (5-children-vs-3-capacity): same pattern, looped fork. This is a stronger assertion that the lock serialises N parallel writers correctly.

    NOTE: If plan 04-01 task 1 confirmed pcntl is NOT available in the web container, the test is allowed to land in SKIPPED state with the fallback documentation; CI runs it on Linux runners where pcntl IS available. The marker text is `pcntl extension not available — fallback to dual-connection DB approach documented in Pitfall 4`.

    NOTE on RefreshDatabase + fork: the SQLite/Postgres RefreshDatabase trait creates a transaction per test that gets rolled back at teardown. pcntl_fork BREAKS this — child transactions cannot see parent's RefreshDatabase setup. WORKAROUND: do NOT use RefreshDatabase on this test; use DatabaseTransactions or migrate:fresh OR use a one-time DB setup that survives the fork. Phase 3 tests use the trait globally; this test must override via `uses(DatabaseTransactions::class)` removal OR explicit cleanup of created rows in afterEach().

    Run `make pest --filter=MatchSignupConcurrencyTest`. Verify output reports either 2 PASS or 2 SKIPPED.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Services/MatchSignupConcurrencyTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|SKIPPED|Tests:|placeholder)' | tail -8</automated>
  </verify>
  <done>Concurrency test committed; 2 it() blocks GREEN or SKIPPED with documented reason; pcntl guard present; 'placeholder' literal removed.</done>
</task>

<task type="auto">
  <name>Task 3: MatchSignupTagRestrictedTest (Pattern 5 allowlist coverage — SC-5 first half)</name>
  <files>
    apps/web/tests/Feature/Services/MatchSignupTagRestrictedTest.php
  </files>
  <read_first>
    - apps/web/app/Services/MatchSignupService.php (just-shipped from task 1 — tagAccessAllowed private helper invoked from signup)
    - apps/web/app/Models/MatchAccessRule.php (plan 04-03 — clanTag BelongsTo)
    - apps/web/app/Models/ClanTag.php (Phase 2 — slug + label)
    - apps/web/app/Models/Clan.php (Phase 2 — tags() BelongsToMany)
    - apps/web/app/Models/ClanMembership.php (Phase 2 — clan + user belongsTo)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pattern 5 — allowlist semantics; tagAccessAllowed test surface enumeration)
  </read_first>
  <acceptance_criteria>
    - `MatchSignupTagRestrictedTest.php` REPLACES Wave 0 stub
    - 4+ it() blocks covering Pattern 5 enumeration:
      - it('allows signup when match has zero access rules — empty equals open semantics') — match with no rows in match_access_rules; user with no active clan; signup succeeds (returns MatchSlot)
      - it('allows signup when user clan has an allowed tag') — match with 1 access rule for tag 'eu'; user is in clan tagged 'eu'; signup succeeds
      - it('blocks signup when user clan has no allowed tag') — match with rules for tag 'eu'; user is in clan tagged 'na' only; expect TagRestrictedException
      - it('blocks signup when user has no active clan and rules exist') — match has tag rules; user has zero ClanMemberships; expect TagRestrictedException
      - it('allows signup when user clan has at least one allowed tag among many') — match with rules for ['eu', 'tier-1']; user clan has tags ['na', 'eu']; intersection is non-empty; signup succeeds
    - All tests setup: GameMatchType + materialised match_slots (at least 1 slot in 1 role) + Match status='open'
    - Use App\Models\Match imported aliased
    - The Wave 0 'placeholder' literal is removed
    - `make pest ARGS="--filter=MatchSignupTagRestrictedTest"` GREEN
    - `make phpstan` reports no new errors
  </acceptance_criteria>
  <action>
    Build each it() block with explicit setup. The factory chain for "user in clan tagged X":

    1. $clan = Clan::factory()->create(['status' => 'active']);
    2. $tag = ClanTag::factory()->create(['slug' => 'eu']);
    3. $clan->tags()->attach($tag);
    4. $user = User::factory()->create();
    5. ClanMembership::factory()->create(['user_id' => $user->id, 'clan_id' => $clan->id, 'left_at' => null]); // active

    For "match has rules for tag X":
    1. MatchAccessRule::factory()->create(['match_id' => $match->id, 'clan_tag_id' => $tag->id]);

    For the negative case "user has no active clan": create the user but skip the ClanMembership step (or set left_at to past).

    For the negative case "user clan has no allowed tag": set up user in clan tagged 'na' only; match rules require 'eu'; intersection is empty; signup throws.

    NOTE on activeClanMembership returning null: when user has no membership where left_at IS NULL, the relation returns null. tagAccessAllowed uses `?->clan` null-safe operator; null becomes false at the intersect check.

    NOTE: tests do NOT need to exercise the concurrency path (that's task 2's scope) — keep these tests sequential and focused on Pattern 5's allowlist semantics.

    Run `make pest --filter=MatchSignupTagRestrictedTest`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Services/MatchSignupTagRestrictedTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:|placeholder)' | tail -8</automated>
  </verify>
  <done>4+ tests GREEN; Pattern 5 allowlist enumeration verified at the service layer; 'placeholder' removed.</done>
</task>

</tasks>

<verification>
- `make pest ARGS="--filter='Match(Signup|Concurrency|TagRestricted)'"` -> GREEN or 2 SKIPPED (pcntl-dependent)
- `make phpstan` -> no new errors
- `make pint ARGS="--test"` -> green
- `grep -c 'DB::transaction' apps/web/app/Services/MatchSignupService.php` -> at least 1 (Pitfall 1 grep gate)
- `grep -c 'lockForUpdate' apps/web/app/Services/MatchSignupService.php` -> at least 1
- Wave 0 baseline incomplete count drops by 3 (Match Signup, Concurrency, TagRestricted now GREEN/SKIPPED)
</verification>

<success_criteria>
SC-2 (capacity row-locked) and SC-5 first half (tag access allowlist) are verifiable via Pest. D-010 is mechanically enforced. Plan 04-10's controller calls into this service via `app(MatchSignupService::class)->signup(...)` and catches each of the 4 typed exceptions to convert into a 422 response with the localized message.
</success_criteria>

<output>
After completion, create `.planning/phases/04-matches-manual/04-06-SUMMARY.md` documenting:
- The 5-guard order (status, tag, idempotency, capacity, claim) — verbatim
- The 3 exception class FQNs and which SC each maps to
- Whether pcntl was available (pulled from plan 04-01 summary); if not, document the fallback dual-connection approach as a follow-up
- Tag-access test enumeration (4+ it() blocks)
- Confirmation that grep gates DB::transaction + lockForUpdate both return > 0 in MatchSignupService.php
</output>
</content>
</invoke>