---
phase: 04-matches-manual
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/database/factories/MatchFactory.php
  - apps/web/database/factories/MatchSlotFactory.php
  - apps/web/database/factories/MatchAccessRuleFactory.php
  - apps/web/database/factories/MatchResultFactory.php
  - apps/web/database/factories/MatchMvpFactory.php
  - apps/web/database/factories/EventFactory.php
  - apps/web/tests/Feature/Models/MatchModelTest.php
  - apps/web/tests/Feature/Models/MatchSlotModelTest.php
  - apps/web/tests/Feature/Models/MatchAccessRuleModelTest.php
  - apps/web/tests/Feature/Models/MatchResultModelTest.php
  - apps/web/tests/Feature/Models/MatchMvpModelTest.php
  - apps/web/tests/Feature/Models/EventModelTest.php
  - apps/web/tests/Feature/Services/MatchSlotMaterialiserServiceTest.php
  - apps/web/tests/Feature/Services/MatchSignupServiceTest.php
  - apps/web/tests/Feature/Services/MatchSignupConcurrencyTest.php
  - apps/web/tests/Feature/Services/MatchStatusServiceTest.php
  - apps/web/tests/Feature/Services/MatchResultServiceTest.php
  - apps/web/tests/Feature/Matches/MatchCalendarPageTest.php
  - apps/web/tests/Feature/Matches/MatchShowPageTest.php
  - apps/web/tests/Feature/Matches/MatchSignupControllerTest.php
  - apps/web/tests/Feature/Matches/MatchSignupTagRestrictedTest.php
  - apps/web/tests/Feature/Admin/MatchResourcePresentTest.php
  - apps/web/tests/Feature/Admin/MatchResourceCreateWizardTest.php
  - apps/web/tests/Feature/Admin/MatchAuditLogTest.php
  - apps/web/tests/Feature/Observers/MatchEventSyncTest.php
  - apps/web/tests/Unit/Data/MatchDataTest.php
  - apps/web/tests/Unit/Data/PublicMatchDataTest.php
  - apps/web/tests/Unit/Data/EventDataTest.php
  - apps/web/lang/en/matches.php
  - apps/web/lang/en/admin.php
autonomous: true
requirements:
  - REQ-goal-match-workflows
must_haves:
  truths:
    - "6 factory stub files exist at the canonical paths and throw RuntimeException('placeholder — replace in 04-03') from definition()"
    - "21 Pest test stub files exist with a single failing `it('placeholder — replace in plan 04-NN')->markTestIncomplete(...)` block per file (RED state)"
    - "apps/web/lang/en/matches.php exists with at least the 14 i18n keys consumed by services + controllers (capacity_full, tag_restricted, already_signed_up, not_open, invalid_transition, etc.)"
    - "apps/web/lang/en/admin.php is appended with admin.match.*, admin.match_slot.*, admin.match_access_rule.*, admin.match_result.*, admin.match_mvp.*, admin.event.* key groups"
    - "PHP `pcntl` extension availability is recorded as a comment in MatchSignupConcurrencyTest.php (Pitfall 4 / Assumption A8)"
    - "`make pest` reports 21 incomplete tests (RED) — establishes Wave 0 baseline; all later waves flip them GREEN"
  artifacts:
    - path: "apps/web/database/factories/MatchFactory.php"
      provides: "MatchFactory stub"
      contains: "RuntimeException"
    - path: "apps/web/tests/Feature/Services/MatchSignupConcurrencyTest.php"
      provides: "Concurrency test stub with pcntl guard"
      contains: "pcntl"
    - path: "apps/web/lang/en/matches.php"
      provides: "Phase 4 i18n keys for public-facing strings + service error messages"
      contains: "signup"
  key_links:
    - from: "apps/web/lang/en/matches.php signup.error keys"
      to: "MatchSignupService exceptions (plan 04-06)"
      via: "__('matches.signup.error.capacity_full') etc."
      pattern: "matches\\.signup\\.error"
    - from: "apps/web/lang/en/admin.php admin.match.* group"
      to: "MatchResource Filament labels (plan 04-09)"
      via: "__('admin.match.label') etc."
      pattern: "admin\\.match"
---

<objective>
Land the Wave 0 scaffolding for Phase 4: 6 factory stubs (one per new model), 21 Pest RED test stubs (one per Wave 0 Gap row in 04-RESEARCH.md § Validation Architecture), and the i18n key files extensions consumed by later waves. The factory stubs throw `RuntimeException('placeholder')` from `definition()` so any later-wave test accidentally invoking them blows up loudly; real factory bodies arrive in plan 04-03.

Purpose: Nyquist-compliant test infrastructure. Every later plan's `<verify><automated>` block invokes Pest filters that target these RED stubs. Wave 0 establishes the baseline; later waves flip them GREEN one cluster at a time. The i18n key files must exist BEFORE service/controller code references them — `__()` against a missing key returns the key string, not an error, so missing keys would slip through unit tests and surface only in NoHardcodedStringsTest or manual smoke.

Output: 6 factory stubs + 21 test stubs + matches.php (new) + admin.php (appended) committed; `make pest` shows ~21 incomplete tests; `make phpstan` + `make pint --test` green; `make pnpm ARGS="-F web run typecheck"` regressionless.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-matches-manual/04-RESEARCH.md
@.planning/phases/04-matches-manual/04-CONTEXT.md
@.planning/phases/04-matches-manual/04-VALIDATION.md
@CLAUDE.md
@apps/web/database/factories/GameFactory.php
@apps/web/database/factories/GameMatchTypeRoleLimitFactory.php
@apps/web/tests/Feature/Models/GameModelTest.php
@apps/web/lang/en/matches.php
@apps/web/lang/en/admin.php

<interfaces>
Canonical Wave 0 factory stub pattern (verified from Phase 3 plan 03-01 output — the factory file structure that Phase 3 used):

A factory stub:
- `<?php\ndeclare(strict_types=1); namespace Database\Factories;`
- `use Illuminate\Database\Eloquent\Factories\Factory;`
- `class MatchFactory extends Factory { protected $model = 'App\\Models\\Match'; public function definition(): array { throw new \RuntimeException('placeholder — replace in plan 04-03'); } }`

NOTE on `Match` model FQN: per RESEARCH Pitfall 5 + Assumption A4, the model class is `App\Models\Match` (legal PHP 8 — `Match` class vs lowercase `match` keyword). In factory stubs the model attribute MUST be a string literal `'App\\Models\\Match'` (not `Match::class`) because the model class file does NOT yet exist in Wave 0 (it lands in plan 04-03). Use string FQN; later plans switch to `::class` once the model file exists.

Pest test stub pattern (verified Phase 2 + Phase 3 Wave 0 outputs):
- `<?php\ndeclare(strict_types=1);`
- `it('placeholder — replace in plan 04-NN', function () { $this->markTestIncomplete('Wave 0 RED stub — implementation in plan 04-NN'); });`
- One `it()` block per file; the `NN` matches the plan that flips it GREEN per 04-VALIDATION.md Per-Task Verification Map

i18n key surface for matches.php (consumed by services in plan 04-06, controllers in plan 04-10, Vue pages in plan 04-11):
- matches.signup.error.capacity_full
- matches.signup.error.tag_restricted
- matches.signup.error.already_signed_up
- matches.signup.error.not_open
- matches.signup.error.no_active_clan
- matches.signup.success
- matches.status.error.invalid_transition (with :from and :to interpolation)
- matches.calendar.title
- matches.calendar.empty
- matches.show.title_fallback
- matches.show.signup_button
- matches.show.cancel_signup_button
- matches.show.slot_open
- matches.show.slot_taken_anonymous

i18n key surface for admin.php Phase 4 extensions (consumed by MatchResource + 4 RelationManagers + EventResource in plan 04-09 + plan 04-12 audit):
- admin.match.label / plural_label
- admin.match.fields.{game_match_type, organiser, host_clan, server_address, scheduled_at, status, is_public, title, description}
- admin.match.wizard.{step_type, step_type_desc, step_schedule, step_schedule_desc, step_review, step_review_desc}
- admin.match.section.{profile, audit}
- admin.match.actions.{open_signups, lock_signups, cancel}
- admin.match_slot.label / plural_label / fields.{game_role, slot_index, occupant_user, confirmed_at}
- admin.match_access_rule.label / plural_label / fields.{clan_tag}
- admin.match_result.label / plural_label / fields.{winner_clan, allies_score, axis_score, notes, recorded_by, recorded_at}
- admin.match_mvp.label / plural_label / fields.{player, category, value}
- admin.event.label / plural_label / fields.{eventable, starts_at, ends_at, title, is_public}

Existing apps/web/lang/en/matches.php may not yet exist — verify with `ls`. If absent, create from scratch.
Existing apps/web/lang/en/admin.php is the Phase 1/2/3 file with admin.game.*, admin.clan.* groups already populated — append to it, don't replace.

Test file path naming convention (verified Phase 3): `tests/Feature/{Domain}/{TestName}Test.php`. Phase 4 introduces `tests/Feature/Matches/`, `tests/Feature/Observers/` subdirs — make the directories.

pcntl extension check (Pitfall 4 / Assumption A8): the concurrency test stub must include a header comment:
```
// Source: 04-RESEARCH.md Pitfall 4 + Assumption A8 — pcntl extension required.
// Run `docker compose exec web php -m | grep pcntl` to verify availability before
// implementing this test in plan 04-06; if absent, fall back to dual-connection
// DB::connection alias approach per Pitfall 4 option 2.
```
The stub body remains `$this->markTestIncomplete(...)`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test scaffolding to test runner | RuntimeException stubs prevent accidental factory calls in later-wave tests before real factories ship |
| Wave 0 i18n keys to Wave 3+ services | Keys must exist before code references them, else __() returns the key string and the bug ships invisible |
| Concurrency test stub to PHP runtime | pcntl extension may be absent in the web container; Pitfall 4 fallback documented inline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Factory stub silently returns empty array | mitigate | RuntimeException('placeholder') in definition() — any accidental factory call in a later-wave test surfaces immediately as a fatal |
| T-04-01-02 | Information Disclosure | Missing i18n key returns the literal key string in production | mitigate | All 14 service-layer + 6 admin-resource i18n key groups defined in Wave 0; CI gate NoHardcodedStringsTest (Phase 1) catches missed template strings |
| T-04-01-03 | Repudiation | Wave 0 RED state mistaken for GREEN state | mitigate | Each stub uses markTestIncomplete(...) which Pest reports as "incomplete" (not "passed"); execute-plan visible in test output |
| T-04-01-04 | Tampering | pcntl absence breaks the SC-2 concurrency test silently | mitigate | Pitfall 4 fallback procedure documented in the test stub header; plan 04-06 enforces the verification step before flipping the test GREEN |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 6 factory stubs + lang/en/matches.php (new) + lang/en/admin.php appendix</name>
  <files>
    apps/web/database/factories/MatchFactory.php,
    apps/web/database/factories/MatchSlotFactory.php,
    apps/web/database/factories/MatchAccessRuleFactory.php,
    apps/web/database/factories/MatchResultFactory.php,
    apps/web/database/factories/MatchMvpFactory.php,
    apps/web/database/factories/EventFactory.php,
    apps/web/lang/en/matches.php,
    apps/web/lang/en/admin.php
  </files>
  <read_first>
    - apps/web/database/factories/GameFactory.php (Phase 3 analog — `<?php declare(strict_types=1); namespace Database\Factories;` + Factory subclass + definition())
    - apps/web/database/factories/GameMatchTypeRoleLimitFactory.php (analog for a child model factory with two FK references — Phase 3 plan 03-03 output)
    - apps/web/lang/en/admin.php (existing — currently contains admin.game.*, admin.clan.*, admin.user.*, etc.; the new admin.match.*/admin.event.* groups append to the same returned array)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pattern 1 — six tables, the model names and field names that the i18n keys must mirror)
  </read_first>
  <acceptance_criteria>
    - 6 factory files in apps/web/database/factories/:
      - Each declares `<?php\ndeclare(strict_types=1); namespace Database\Factories;`
      - Each uses `use Illuminate\Database\Eloquent\Factories\Factory;`
      - Each defines `class <Name>Factory extends Factory { protected $model = 'App\\Models\\<Name>'; public function definition(): array { throw new \RuntimeException('placeholder — replace in plan 04-03'); } }`
      - The model FQN strings: 'App\\Models\\Match', 'App\\Models\\MatchSlot', 'App\\Models\\MatchAccessRule', 'App\\Models\\MatchResult', 'App\\Models\\MatchMvp', 'App\\Models\\Event'
    - `apps/web/lang/en/matches.php` (NEW file):
      - Returns a nested PHP array with at minimum the 14 keys listed in `<interfaces>` (signup.error.*, signup.success, status.error.invalid_transition, calendar.*, show.*)
      - All values are English strings; parameter interpolation uses `:from`/`:to`/`:limit` style
      - Header comment `<?php\n/* Source: 04-01-PLAN.md Task 1 + 04-RESEARCH.md Validation Architecture */`
    - `apps/web/lang/en/admin.php` (MODIFY — append):
      - Read existing file (contains admin.game.*, admin.clan.*, admin.user.*, admin.player.*, admin.tab.*, admin.audit.*)
      - Append 6 new top-level groups inside the existing returned array: admin.match.*, admin.match_slot.*, admin.match_access_rule.*, admin.match_result.*, admin.match_mvp.*, admin.event.* (per the i18n key surface in `<interfaces>`)
      - Preserve existing groups verbatim
    - `make pint ARGS="--test database/factories lang/en"` clean
    - `make phpstan` reports no new errors
  </acceptance_criteria>
  <action>
    Mirror the Phase 3 plan 03-01 Wave 0 output structure for each factory stub. The body is literally one line: `throw new \RuntimeException('placeholder — replace in plan 04-03');` inside `definition(): array`. The `protected $model` value is a STRING (per `<interfaces>` note) because the actual model classes ship in plan 04-03 — referring to them via `::class` here would FAIL at PHP parse time when the class file is absent.

    For `apps/web/lang/en/matches.php`: this is a NEW file. Use the canonical Laravel locale-file shape: `<?php\n\nreturn [\n  'signup' => [\n    'error' => [\n      'capacity_full' => 'Sorry — that role is full.',\n      ...\n    ],\n    'success' => 'Signed up to :role.',\n  ],\n  'status' => [...],\n  'calendar' => [...],\n  'show' => [...],\n];`. Write English copy that is grammatical; parameter interpolation in matches.signup.success uses `:role`; in matches.status.error.invalid_transition uses `:from` and `:to`.

    For `apps/web/lang/en/admin.php`: this file already exists. READ it first to understand the current structure (it returns a single array literal). Append the 6 new groups INSIDE the existing array — do NOT replace. The 6 groups follow the structure used by admin.game.* (label, plural_label, section, tab, fields, actions sub-keys). Mirror the depth of admin.game.fields.* verbatim for admin.match.fields.* etc.

    NOTE on Pitfall 5 / Assumption A4: `App\Models\Match` is the class FQN — the singular form. The factory file is `MatchFactory.php`. Do NOT rename to `MatchEvent` or `GameMatch` — the planner adopts RESEARCH Recommendation B (keep `Match`, header docblock warns reviewers).

    Run `make pint ARGS="--test"` on the touched paths.
  </action>
  <verify>
    <automated>ls apps/web/database/factories/Match*.php apps/web/database/factories/EventFactory.php apps/web/lang/en/matches.php 2>&1 | wc -l | grep -E '^7$' && docker compose exec web php -r "require 'apps/web/lang/en/matches.php';" 2>&1 | head -3 && docker compose exec web ./vendor/bin/pint --test database/factories/MatchFactory.php database/factories/MatchSlotFactory.php database/factories/MatchAccessRuleFactory.php database/factories/MatchResultFactory.php database/factories/MatchMvpFactory.php database/factories/EventFactory.php lang/en/matches.php 2>&1 | tail -3</automated>
  </verify>
  <done>6 factory stubs + 2 lang files committed; pint --test clean; require'matches.php' parses without warning.</done>
</task>

<task type="auto">
  <name>Task 2: 21 Pest RED test stubs across 5 subdirectories (Models, Services, Matches, Admin, Observers, Unit/Data)</name>
  <files>
    apps/web/tests/Feature/Models/MatchModelTest.php,
    apps/web/tests/Feature/Models/MatchSlotModelTest.php,
    apps/web/tests/Feature/Models/MatchAccessRuleModelTest.php,
    apps/web/tests/Feature/Models/MatchResultModelTest.php,
    apps/web/tests/Feature/Models/MatchMvpModelTest.php,
    apps/web/tests/Feature/Models/EventModelTest.php,
    apps/web/tests/Feature/Services/MatchSlotMaterialiserServiceTest.php,
    apps/web/tests/Feature/Services/MatchSignupServiceTest.php,
    apps/web/tests/Feature/Services/MatchSignupConcurrencyTest.php,
    apps/web/tests/Feature/Services/MatchStatusServiceTest.php,
    apps/web/tests/Feature/Services/MatchResultServiceTest.php,
    apps/web/tests/Feature/Matches/MatchCalendarPageTest.php,
    apps/web/tests/Feature/Matches/MatchShowPageTest.php,
    apps/web/tests/Feature/Matches/MatchSignupControllerTest.php,
    apps/web/tests/Feature/Matches/MatchSignupTagRestrictedTest.php,
    apps/web/tests/Feature/Admin/MatchResourcePresentTest.php,
    apps/web/tests/Feature/Admin/MatchResourceCreateWizardTest.php,
    apps/web/tests/Feature/Admin/MatchAuditLogTest.php,
    apps/web/tests/Feature/Observers/MatchEventSyncTest.php,
    apps/web/tests/Unit/Data/MatchDataTest.php,
    apps/web/tests/Unit/Data/PublicMatchDataTest.php,
    apps/web/tests/Unit/Data/EventDataTest.php
  </files>
  <read_first>
    - apps/web/tests/Feature/Models/GameModelTest.php (Phase 3 model test — Pest 4 it() + uses() + expect() syntax)
    - apps/web/tests/Feature/Clans/PublicClanRoutesTest.php (Phase 2 feature test analog for controller/route stubs)
    - .planning/phases/04-matches-manual/04-VALIDATION.md (Per-Task Verification Map — the plan number per stub: 04-03 for models, 04-04 for status, 04-05 for materialiser, 04-06 for signup, 04-07 for DTOs, 04-08 for observer, 04-09 for admin, 04-10 for controllers, 04-11 for pages, 04-12 for audit/presence)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pitfall 4 — concurrency test pcntl prerequisite + fallback)
  </read_first>
  <acceptance_criteria>
    - 22 test files exist at the canonical paths (apps/web/tests/Feature/Models/*.php × 6, apps/web/tests/Feature/Services/*.php × 5, apps/web/tests/Feature/Matches/*.php × 4, apps/web/tests/Feature/Admin/*.php × 3, apps/web/tests/Feature/Observers/*.php × 1, apps/web/tests/Unit/Data/*.php × 3)
    - Each test file:
      - Declares `<?php\ndeclare(strict_types=1);`
      - Contains exactly ONE `it('placeholder — replace in plan 04-NN', function () { $this->markTestIncomplete('Wave 0 RED stub — implementation in plan 04-NN'); });` block
      - The `NN` matches the plan that flips it GREEN per 04-VALIDATION.md (e.g., MatchModelTest -> 04-03; MatchSignupServiceTest -> 04-06; MatchCalendarPageTest -> 04-10; MatchDataTest -> 04-07)
    - MatchSignupConcurrencyTest.php additionally includes the pcntl pre-flight comment block from `<interfaces>` (Pitfall 4 + A8 reference) BEFORE the `it()` line
    - `tests/Feature/Matches/` and `tests/Feature/Observers/` directories exist (Laravel creates them automatically when the first file is written, but verify with ls)
    - `make pest` reports approximately 22 incomplete tests in scope (alongside the existing 278 Phase 1+2+3 GREEN tests)
    - `make phpstan` reports no new errors
    - `make pint ARGS="--test tests/Feature/Models tests/Feature/Services tests/Feature/Matches tests/Feature/Admin tests/Feature/Observers tests/Unit/Data"` clean
  </acceptance_criteria>
  <action>
    Write each stub file using the Phase 3 Wave 0 idiom: a single it() block calling markTestIncomplete(). Per-file plan-number mapping comes from 04-VALIDATION.md's Per-Task Verification Map (read this file first to assign correct NN values).

    Mapping (source: 04-VALIDATION.md):
    - Model tests (6): plan 04-03 (Wave 2 models + factories + tests)
    - MatchStatusServiceTest: plan 04-04 (Wave 2 state machine)
    - MatchSlotMaterialiserServiceTest: plan 04-05 (Wave 3 materialiser)
    - MatchSignupServiceTest + MatchSignupConcurrencyTest + MatchSignupTagRestrictedTest: plan 04-06 (Wave 3 row-locked signup)
    - DTO unit tests (3): plan 04-07 (Wave 4 DTOs)
    - MatchEventSyncTest: plan 04-08 (Wave 4 observer)
    - Admin tests (3): plan 04-09 (Wave 5 Filament) — MatchResourcePresentTest and MatchAuditLogTest also revisited in plan 04-12
    - MatchCalendarPageTest + MatchShowPageTest + MatchSignupControllerTest: plan 04-10 (Wave 6 controllers + routes)
    - MatchResultServiceTest: plan 04-09 (Wave 5 — alongside Filament ResultRelationManager because MatchResultService is the service the RelationManager calls)

    For MatchSignupConcurrencyTest.php specifically: paste the pcntl pre-flight comment block from `<interfaces>` immediately AFTER `declare(strict_types=1);` and BEFORE the it() block. This documents Assumption A8 / Pitfall 4 at the source-code layer.

    Make sure the subdirectory `tests/Feature/Matches/`, `tests/Feature/Observers/` exist (Laravel does not auto-create them — verify with the file system; if the write step fails on missing parent, create via the first file write).

    Run `make pest --filter='Match|Event|Phase04'` to observe 22 incomplete tests (or run `make pest` and verify the incomplete count grows by 22). Run pint and phpstan.
  </action>
  <verify>
    <automated>ls apps/web/tests/Feature/Models/Match*.php apps/web/tests/Feature/Models/EventModelTest.php apps/web/tests/Feature/Services/Match*.php apps/web/tests/Feature/Matches/*.php apps/web/tests/Feature/Admin/Match*.php apps/web/tests/Feature/Observers/MatchEventSyncTest.php apps/web/tests/Unit/Data/MatchDataTest.php apps/web/tests/Unit/Data/PublicMatchDataTest.php apps/web/tests/Unit/Data/EventDataTest.php 2>&1 | wc -l | grep -E '^22$' && grep -l 'pcntl' apps/web/tests/Feature/Services/MatchSignupConcurrencyTest.php 2>&1 | head -1 && docker compose exec web ./vendor/bin/pest --filter='Match|EventModel|EventData' --no-coverage 2>&1 | grep -E '(Tests:|Incomplete:|placeholder)' | tail -5</automated>
  </verify>
  <done>22 RED stub files committed (21 distinct stubs + the EventModelTest counted with Models); pcntl comment present on concurrency stub; pest reports incomplete count; pint + phpstan clean.</done>
</task>

</tasks>

<verification>
- `make pest` -> 22 new incomplete tests in scope; no new failures
- `make phpstan` -> no new errors
- `make pint ARGS="--test"` -> green
- `docker compose exec web php -r "var_dump(require 'apps/web/lang/en/matches.php');"` -> outputs the nested array structure
- `grep -c 'admin.match' apps/web/lang/en/admin.php` -> > 0 (new keys present)
</verification>

<success_criteria>
Wave 0 baseline established. Plans 04-02 through 04-13 can target each RED stub via Pest filter, flip GREEN, and the test count moves from "incomplete" to "passed" cluster by cluster. The Phase 4 Wave 0 invariant from 04-VALIDATION.md (`wave_0_complete: true`) becomes true after this plan ships.
</success_criteria>

<output>
After completion, create `.planning/phases/04-matches-manual/04-01-SUMMARY.md` documenting:
- The 6 factory stub paths and the `App\Models\<X>` FQN strings each one references
- The 22 test stub paths with the plan number each is flipped GREEN by
- The matches.php i18n key tree (14 keys minimum)
- The admin.php appended groups (6 new groups)
- Verified pcntl extension status via `docker compose exec web php -m | grep pcntl` (record yes/no — Assumption A8 input for plan 04-06)
</output>
</content>
</invoke>