---
phase: 04-matches-manual
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - apps/web/app/Models/Match.php
  - apps/web/app/Models/MatchSlot.php
  - apps/web/app/Models/MatchAccessRule.php
  - apps/web/app/Models/MatchResult.php
  - apps/web/app/Models/MatchMvp.php
  - apps/web/app/Models/Event.php
  - apps/web/app/Models/User.php
  - apps/web/database/factories/MatchFactory.php
  - apps/web/database/factories/MatchSlotFactory.php
  - apps/web/database/factories/MatchAccessRuleFactory.php
  - apps/web/database/factories/MatchResultFactory.php
  - apps/web/database/factories/MatchMvpFactory.php
  - apps/web/database/factories/EventFactory.php
  - apps/web/tests/Feature/Models/MatchModelTest.php
  - apps/web/tests/Feature/Models/MatchSlotModelTest.php
  - apps/web/tests/Feature/Models/MatchAccessRuleModelTest.php
  - apps/web/tests/Feature/Models/MatchResultModelTest.php
  - apps/web/tests/Feature/Models/MatchMvpModelTest.php
  - apps/web/tests/Feature/Models/EventModelTest.php
autonomous: true
requirements:
  - REQ-goal-match-workflows
must_haves:
  truths:
    - "6 Phase 4 models exist with HasUuidPrimaryKey + LogsActivity + HasFactory<XFactory>; Match additionally uses HasTranslations[title, description]; Event additionally uses HasTranslations[title]"
    - "Match model has BelongsTo gameMatchType + BelongsTo organiser (User) + BelongsTo hostClan (Clan); HasMany slots (ordered by sort_order, slot_index); HasMany accessRules; HasOne result; MorphOne event"
    - "Event model has MorphTo eventable() (polymorphic) plus standard casts on starts_at/ends_at"
    - "User model gains `activeClanMembership(): HasOne` relation returning the user's single active ClanMembership where left_at IS NULL (D-009 helper — consumed by MatchSignupService Pattern 5 tag access check)"
    - "6 real factories replace the Wave 0 stubs; factories cover sensible defaults for happy-path tests"
    - "6 GREEN model tests cover: UNIQUE/CHECK invariants, partial UNIQUE (one slot per user per match), cascade behavior, HasTranslations round-trip on Match.title/description and Event.title, polymorphic morphOne/morphTo round-trip, activity log writes"
    - "Header comment on Match.php documents Pitfall 5 (class name `Match` vs PHP `match` keyword) and recommends `use App\\Models\\Match as MatchModel;` at use sites that contain `match($x)` expressions"
  artifacts:
    - path: "apps/web/app/Models/Match.php"
      provides: "Match eloquent model with HasTranslations[title, description] + LogsActivity + 6 relations + MorphOne event"
      contains: "use HasTranslations"
    - path: "apps/web/app/Models/Event.php"
      provides: "Polymorphic event model"
      contains: "morphTo"
    - path: "apps/web/app/Models/User.php"
      provides: "User model with activeClanMembership() accessor"
      contains: "activeClanMembership"
    - path: "apps/web/tests/Feature/Models/MatchSlotModelTest.php"
      provides: "Partial UNIQUE + cascade + activity log assertions"
      contains: "match_slots_one_occupancy_per_user"
  key_links:
    - from: "apps/web/app/Models/Match.php event()"
      to: "Event MorphOne (polymorphic)"
      via: "$this->morphOne(Event::class, 'eventable')"
      pattern: "morphOne\\(Event::class"
    - from: "apps/web/app/Models/User.php activeClanMembership()"
      to: "ClanMembership where left_at IS NULL"
      via: "HasOne with whereNull('left_at')"
      pattern: "activeClanMembership.*HasOne"
    - from: "apps/web/app/Models/Match.php slots()"
      to: "MatchSlot HasMany"
      via: "hasMany ordered by sort_order then slot_index"
      pattern: "hasMany\\(MatchSlot"
---

<objective>
Replace the 6 Wave 0 factory stubs (from plan 04-01) with real Eloquent models + real factories, and replace the 6 Wave 0 model test stubs with GREEN assertions covering UNIQUE composites + CHECK constraints + partial UNIQUE (one slot per user per match) + HasTranslations round-trip on Match.title/description and Event.title + cascade chain + polymorphic MorphOne/MorphTo round-trip + activity log writes (D-012).

Also add the `activeClanMembership(): HasOne` accessor to the existing User model — RESEARCH Pattern 5's `tagAccessAllowed()` reads `$user->activeClanMembership?->clan` and Phase 2 did not ship this accessor (Clan has `activeMembers()` and `memberships()` but User has neither). Add it as a Rule 2 amendment here.

Purpose: Models are the contract every later wave compiles against. Without Match + MatchSlot + the new User accessor, the materialiser (plan 04-05), signup service (plan 04-06), DTOs (plan 04-07), observer (plan 04-08), Filament resources (plan 04-09), and controllers (plan 04-10) cannot reference real types.

Output: 6 model classes; 6 real factory files overwriting Wave 0 stubs; 6 GREEN model tests replacing the Wave 0 RED stubs; User model amended with activeClanMembership().
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-matches-manual/04-RESEARCH.md
@.planning/phases/04-matches-manual/04-CONTEXT.md
@CLAUDE.md
@apps/web/app/Models/Game.php
@apps/web/app/Models/GameMatchType.php
@apps/web/app/Models/GameMatchTypeRoleLimit.php
@apps/web/app/Models/Clan.php
@apps/web/app/Models/ClanMembership.php
@apps/web/app/Models/User.php
@apps/web/app/Concerns/HasUuidPrimaryKey.php
@apps/web/database/factories/GameMatchTypeFactory.php
@apps/web/database/factories/GameMatchTypeRoleLimitFactory.php
@apps/web/tests/Feature/Models/GameMatchTypeRoleLimitModelTest.php
@apps/web/tests/Feature/Models/ClanMembershipModelTest.php

<interfaces>
Canonical Phase 3 model pattern (verified `apps/web/app/Models/GameMatchType.php`):
- `<?php\ndeclare(strict_types=1); namespace App\Models;`
- Use stack: `HasFactory<XFactory>` (PHPDoc generics), `HasUuidPrimaryKey`, `LogsActivity`, conditionally `HasTranslations`
- `/** @var list<string> */ public array $translatable = [...];` (PHPStan L8 requirement)
- `/** @var list<string> */ protected $fillable = [...];`
- `protected function casts(): array` returning typed casts
- `getActivitylogOptions(): LogOptions` returning defaults + logFillable + logOnlyDirty + setDescriptionForEvent
- Relations with PHPDoc generics: `/** @return HasMany<Child, $this> */`

From RESEARCH.md Code Examples § Model: Match (verified pattern — verbatim):
- `$translatable = ['title', 'description']`
- `$fillable = ['game_match_type_id', 'title', 'description', 'scheduled_at', 'organiser_user_id', 'host_clan_id', 'server_address', 'status', 'is_public']`
- casts: ['scheduled_at' => 'datetime', 'is_public' => 'boolean']
- Header docblock notes the `Match` class name pitfall (Pitfall 5)
- Relations: gameMatchType (BelongsTo), organiser (BelongsTo User, foreign 'organiser_user_id'), hostClan (BelongsTo Clan, foreign 'host_clan_id'), slots (HasMany ordered sort_order then slot_index), accessRules (HasMany), result (HasOne), event (MorphOne)

MatchSlot model (per RESEARCH Pattern 1 + 2):
- `$fillable = ['match_id', 'game_role_id', 'slot_index', 'occupant_user_id', 'confirmed_at', 'sort_order']`
- IMPORTANT: occupant_user_id IS fillable for materialiser writes; but MatchSignupService is the only PRODUCTION write path (defense-in-depth via service-layer policy + LogsActivity audit)
- casts: ['confirmed_at' => 'datetime', 'slot_index' => 'integer', 'sort_order' => 'integer']
- Relations: match() BelongsTo, role() BelongsTo (GameRole, foreign 'game_role_id'), occupantUser() BelongsTo (User, foreign 'occupant_user_id')

MatchAccessRule model:
- `$fillable = ['match_id', 'clan_tag_id']`
- Relations: match() BelongsTo, clanTag() BelongsTo

MatchResult model:
- `$fillable = ['match_id', 'winner_clan_id', 'allies_score', 'axis_score', 'notes', 'recorded_by_user_id', 'recorded_at']`
- casts: ['recorded_at' => 'datetime', 'allies_score' => 'integer', 'axis_score' => 'integer']
- Relations: match() BelongsTo, winnerClan() BelongsTo (Clan, foreign 'winner_clan_id'), recordedBy() BelongsTo (User, foreign 'recorded_by_user_id'), mvps() HasMany

MatchMvp model:
- `$fillable = ['match_result_id', 'player_id', 'category', 'value']`
- casts: ['value' => 'integer']
- Relations: result() BelongsTo (MatchResult, foreign 'match_result_id'), player() BelongsTo

Event model (the polymorphic one):
- `$translatable = ['title']`
- `$fillable = ['eventable_type', 'eventable_id', 'starts_at', 'ends_at', 'title', 'is_public']`
- casts: ['starts_at' => 'datetime', 'ends_at' => 'datetime', 'is_public' => 'boolean']
- Relations: eventable() MorphTo — `/** @return MorphTo<\Illuminate\Database\Eloquent\Model, $this> */`

User model amendment (Rule 2 — required by Pattern 5 in plan 04-06):
- Add `public function activeClanMembership(): HasOne` returning `$this->hasOne(ClanMembership::class)->whereNull('left_at')`
- This is a Rule 2 amendment — Phase 2 ships Clan->activeMembers() (HasMany) but the reverse User->activeClanMembership() (HasOne, exactly one or null per D-009) was not added
- PHPDoc generics: `/** @return HasOne<ClanMembership, $this> */`
- Add `public function memberships(): HasMany` returning `$this->hasMany(ClanMembership::class)` as the all-history variant (also missing in Phase 2; symmetric to Clan::memberships)

Factory definition shapes:
- MatchFactory: scheduled_at = fake()->dateTimeBetween('+1 day', '+30 days'); status = 'open'; is_public = true; title = ['en' => fake()->sentence(3)]; description = null; game_match_type_id = GameMatchType::factory(); organiser_user_id = User::factory(); host_clan_id = null (optional Clan::factory())
- MatchSlotFactory: match_id = Match::factory(); game_role_id = GameRole::factory(); slot_index = fake()->numberBetween(0, 49); occupant_user_id = null (override in tests); confirmed_at = null; sort_order = 0
  - Note: factory default produces a CROSS-GAME pair (Match's GameMatchType game vs GameRole game may differ). Tests needing same-game slots must construct explicitly via `Match::factory()->for($matchType)` + `GameRole::factory()->for($game)`.
- MatchAccessRuleFactory: match_id = Match::factory(); clan_tag_id = ClanTag::factory()
- MatchResultFactory: match_id = Match::factory(); winner_clan_id = Clan::factory(); allies_score = 4; axis_score = 1; notes = null; recorded_by_user_id = User::factory(); recorded_at = now()
- MatchMvpFactory: match_result_id = MatchResult::factory(); player_id = Player::factory(); category = 'kills'; value = fake()->numberBetween(1, 100)
- EventFactory: eventable_type = 'App\\Models\\Match'; eventable_id = Match::factory(); starts_at = now()->addDay(); ends_at = null; title = ['en' => fake()->sentence(2)]; is_public = true
  - Note: factory default creates a STANDALONE Event (Match is created implicitly). Real production writes go through MatchObserver, not Event::factory.

Pitfall 5 header for Match.php (must be present per RESEARCH Code Examples § Model: Match):
```
/**
 * NOTE: Class name conflicts visually (NOT syntactically) with PHP 8's `match` keyword.
 * The class is legal and Eloquent/Filament/Inertia infer the table name `matches` correctly.
 * At call sites where `match($x) { ... }` expressions appear nearby, alias on import:
 *   use App\Models\Match as MatchModel;
 *
 * D-010: capacity enforced via MatchSignupService row lock — never write to MatchSlot.occupant_user_id
 * outside that service.
 */
```
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller / API to model fill()/update() | $fillable list is the ONLY arrays-from-request gate; mass-assignment attack surface |
| Filament edit form to model save | Filament respects $fillable; bypasses are by-design (admin) but audited via LogsActivity |
| App code to MatchSlot.occupant_user_id | $fillable INCLUDES occupant_user_id but the production write path is MatchSignupService (Pattern 2) — defense-in-depth via service + partial UNIQUE + LogsActivity |
| App code to LogsActivity | Every CRUD on Match/MatchSlot/MatchAccessRule/MatchResult/MatchMvp/Event logs causer/subject (D-012) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Mass-assignment on $fillable | mitigate | Strict $fillable list on all 6 new models; audit log captures every change (D-012) |
| T-04-03-02 | Tampering | Composite UNIQUE bypass on match_slots | mitigate | DB-layer UNIQUE INDEX (plan 04-02) is the primary gate; Pest test in this plan asserts QueryException on duplicate (match_id, game_role_id, slot_index) insert |
| T-04-03-03 | Tampering | Partial UNIQUE bypass — second slot for same user | mitigate | DB-layer partial UNIQUE INDEX (plan 04-02); Pest test asserts QueryException on second update to same user_id within the same match |
| T-04-03-04 | Tampering | Invalid status enum write via raw factory | mitigate | matches_status_check CHECK constraint (plan 04-02); plan 04-04 MatchStatusService is the canonical transition path |
| T-04-03-05 | Tampering | Polymorphic eventable_type drift | mitigate | morphTo()/morphOne() Eloquent relation handles the FQN serialisation; Pest test asserts round-trip Match::find($id)->event->eventable_type === Match::class |
| T-04-03-06 | Repudiation | All model mutations | mitigate | LogsActivity on every Phase 4 model; logFillable + logOnlyDirty; admin causer auto-captured via Phase 1 plan 01-14 wiring |
| T-04-03-07 | Tampering | Cascade delete orphans | mitigate | Migration-layer FK cascade (plan 04-02) handles this; Pest test asserts that deleting a Match cascades to slots + access rules + result (and result cascades to mvps) |
| T-04-03-08 | Information Disclosure | Activity log captures match.title in JSONB form | accept | Match.title is intentionally public (renders on /matches calendar); D-013 + D-012 — translatable user content is part of the audit trail |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Match + Event models (the two HasTranslations + polymorphic anchors) + real factories</name>
  <files>
    apps/web/app/Models/Match.php,
    apps/web/app/Models/Event.php,
    apps/web/database/factories/MatchFactory.php,
    apps/web/database/factories/EventFactory.php
  </files>
  <read_first>
    - apps/web/app/Models/GameMatchType.php (Phase 3 analog — HasTranslations on 2 fields name + description; same shape as Match.title + description)
    - apps/web/app/Models/Clan.php (Phase 2 analog — HasTranslations on description JSONB)
    - apps/web/app/Concerns/HasUuidPrimaryKey.php (UUID PK trait)
    - apps/web/database/factories/GameMatchTypeFactory.php (analog for a translatable + multi-FK factory)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Code Examples § Model: Match — verbatim header docblock + class body)
    - Pitfall 5 + Assumption A4 (class name `Match` rationale)
  </read_first>
  <acceptance_criteria>
    - `Match.php`:
      - `<?php\ndeclare(strict_types=1); namespace App\Models;`
      - Header docblock from `<interfaces>` (Pitfall 5 note — verbatim)
      - Use stack: `HasFactory<MatchFactory>` PHPDoc generics; `HasTranslations`; `HasUuidPrimaryKey`; `LogsActivity`
      - `/** @var list<string> */ public array $translatable = ['title', 'description'];`
      - `/** @var list<string> */ protected $fillable = ['game_match_type_id', 'title', 'description', 'scheduled_at', 'organiser_user_id', 'host_clan_id', 'server_address', 'status', 'is_public'];`
      - `protected function casts(): array { return ['scheduled_at' => 'datetime', 'is_public' => 'boolean']; }`
      - `getActivitylogOptions(): LogOptions` returning defaults + logFillable + logOnlyDirty + setDescriptionForEvent("Match {event}")
      - 6 relations with PHPDoc generics: gameMatchType (BelongsTo<GameMatchType, $this>), organiser (BelongsTo<User, $this> with explicit foreign 'organiser_user_id'), hostClan (BelongsTo<Clan, $this> with foreign 'host_clan_id'), slots (HasMany<MatchSlot, $this> with orderBy('sort_order')->orderBy('slot_index')), accessRules (HasMany<MatchAccessRule, $this>), result (HasOne<MatchResult, $this>), event (MorphOne<Event, $this> via morphOne(Event::class, 'eventable'))
      - NO `static::observe(MatchObserver::class)` yet — that lands in plan 04-08 task 1 as a Rule-2 amendment to this file; this plan ships the model without the observer wiring
    - `Event.php`:
      - Use stack: HasFactory<EventFactory>, HasTranslations, HasUuidPrimaryKey, LogsActivity
      - `$translatable = ['title']`
      - `$fillable = ['eventable_type', 'eventable_id', 'starts_at', 'ends_at', 'title', 'is_public']`
      - casts: ['starts_at' => 'datetime', 'ends_at' => 'datetime', 'is_public' => 'boolean']
      - getActivitylogOptions: setDescriptionForEvent("Event {event}")
      - `/** @return MorphTo<\Illuminate\Database\Eloquent\Model, $this> */ public function eventable(): MorphTo { return $this->morphTo(); }`
    - 2 real factories replace Wave 0 stubs (matchFactory + EventFactory):
      - MatchFactory definition() per `<interfaces>` spec; protected $model = Match::class (now resolvable)
      - EventFactory definition() per `<interfaces>` spec; eventable_type defaults to `'App\\Models\\Match'`; eventable_id defaults to Match::factory()
    - `make phpstan` reports no new errors on these 4 files
    - `make pint ARGS="--test app/Models/Match.php app/Models/Event.php database/factories/MatchFactory.php database/factories/EventFactory.php"` clean
  </acceptance_criteria>
  <action>
    Compose Match.php from GameMatchType.php verbatim, substituting names. Critical points:

    1. The header docblock from `<interfaces>` (Pitfall 5 / A4 note) is REQUIRED — it documents the class-name choice for future readers. Paste it verbatim.

    2. For the 6 relations: every BelongsTo with a non-conventional foreign key (organiser uses `organiser_user_id`, hostClan uses `host_clan_id`) must pass the foreign key as the second arg: `$this->belongsTo(User::class, 'organiser_user_id')`. PHPStan L8 generics: `/** @return BelongsTo<User, $this> */`.

    3. The MorphOne relation uses `morphOne(Event::class, 'eventable')` — the 'eventable' string matches `events.eventable_type` + `events.eventable_id` column names from plan 04-02.

    4. The MatchObserver registration via `static::observe()` is INTENTIONALLY OMITTED in this plan — it ships in plan 04-08 task 1 as a Rule-2 amendment (the observer class doesn't exist yet; adding it here would cause class-not-found at boot).

    5. For Event.php: simpler — no scheduled_at, no organiser, no host clan. Pure polymorphic. The morphTo() relation requires NO foreign key argument (Laravel infers from method name 'eventable' to (eventable_type, eventable_id)).

    6. For factories: the model FQN is now resolvable — switch from string FQN to `::class`: `protected $model = Match::class;` (NOT `'App\\Models\\Match'`).

    Run `make phpstan` and `make pint --test`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/phpstan analyse app/Models/Match.php app/Models/Event.php database/factories/MatchFactory.php database/factories/EventFactory.php --no-progress --error-format=raw 2>&1 | tail -5</automated>
  </verify>
  <done>Match + Event models + 2 real factories committed; phpstan clean; pint clean; Pitfall 5 docblock present on Match.php.</done>
</task>

<task type="auto">
  <name>Task 2: MatchSlot + MatchAccessRule + MatchResult + MatchMvp models + factories + User accessor (activeClanMembership + memberships) amendment</name>
  <files>
    apps/web/app/Models/MatchSlot.php,
    apps/web/app/Models/MatchAccessRule.php,
    apps/web/app/Models/MatchResult.php,
    apps/web/app/Models/MatchMvp.php,
    apps/web/app/Models/User.php,
    apps/web/database/factories/MatchSlotFactory.php,
    apps/web/database/factories/MatchAccessRuleFactory.php,
    apps/web/database/factories/MatchResultFactory.php,
    apps/web/database/factories/MatchMvpFactory.php
  </files>
  <read_first>
    - apps/web/app/Models/GameMatchTypeRoleLimit.php (Phase 3 analog for child-with-two-FK model)
    - apps/web/app/Models/ClanMembership.php (Phase 2 analog; relevant for activeClanMembership accessor pattern)
    - apps/web/app/Models/User.php (current — must read fully before amending; preserve all existing methods/relations)
    - apps/web/database/factories/GameMatchTypeRoleLimitFactory.php (two-FK factory analog)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pattern 1 — column lists for the 4 models; Pattern 5 — user->activeClanMembership consumed by signup service)
  </read_first>
  <acceptance_criteria>
    - `MatchSlot.php`:
      - Use stack: HasFactory<MatchSlotFactory>, HasUuidPrimaryKey, LogsActivity (NO HasTranslations)
      - `$fillable = ['match_id', 'game_role_id', 'slot_index', 'occupant_user_id', 'confirmed_at', 'sort_order']`
      - casts: ['confirmed_at' => 'datetime', 'slot_index' => 'integer', 'sort_order' => 'integer']
      - getActivitylogOptions: setDescriptionForEvent("MatchSlot {event}")
      - 3 relations: match() BelongsTo<Match, $this> (foreign 'match_id' — explicit because of the unusual model name); role() BelongsTo<GameRole, $this> (foreign 'game_role_id'); occupantUser() BelongsTo<User, $this> (foreign 'occupant_user_id')
      - Header comment notes that occupant_user_id IS in $fillable but production writes go through MatchSignupService (D-010 defense-in-depth note)
    - `MatchAccessRule.php`:
      - Use stack: HasFactory, HasUuidPrimaryKey, LogsActivity
      - `$fillable = ['match_id', 'clan_tag_id']`
      - 2 relations: match() BelongsTo; clanTag() BelongsTo<ClanTag, $this> (foreign 'clan_tag_id')
      - getActivitylogOptions: setDescriptionForEvent("MatchAccessRule {event}")
    - `MatchResult.php`:
      - Use stack: HasFactory, HasUuidPrimaryKey, LogsActivity
      - `$fillable = ['match_id', 'winner_clan_id', 'allies_score', 'axis_score', 'notes', 'recorded_by_user_id', 'recorded_at']`
      - casts: ['recorded_at' => 'datetime', 'allies_score' => 'integer', 'axis_score' => 'integer']
      - 4 relations: match() BelongsTo; winnerClan() BelongsTo<Clan, $this> (foreign 'winner_clan_id'); recordedBy() BelongsTo<User, $this> (foreign 'recorded_by_user_id'); mvps() HasMany<MatchMvp, $this>
      - getActivitylogOptions: setDescriptionForEvent("MatchResult {event}")
    - `MatchMvp.php`:
      - Use stack: HasFactory, HasUuidPrimaryKey, LogsActivity
      - `$fillable = ['match_result_id', 'player_id', 'category', 'value']`
      - casts: ['value' => 'integer']
      - 2 relations: result() BelongsTo<MatchResult, $this> (foreign 'match_result_id'); player() BelongsTo<Player, $this>
      - getActivitylogOptions: setDescriptionForEvent("MatchMvp {event}")
    - `User.php` (MODIFY — Rule 2 amendment):
      - ADD two relations near the existing player() HasOne method:
        - `/** @return HasOne<ClanMembership, $this> */ public function activeClanMembership(): HasOne { return $this->hasOne(ClanMembership::class)->whereNull('left_at'); }`
        - `/** @return HasMany<ClanMembership, $this> */ public function memberships(): HasMany { return $this->hasMany(ClanMembership::class); }`
      - ADD `use Illuminate\Database\Eloquent\Relations\HasMany;` import if not already present (it's not — verify with grep)
      - Add `use App\Models\ClanMembership;` — wait, ClanMembership is in the same namespace, no import needed
      - PRESERVE ALL EXISTING content (player() relation, canAccessPanel, getFilamentName, $fillable, $hidden, $guard_name, getActivitylogOptions, etc.)
      - Header comment: add a note before the new relations referencing 04-03-PLAN task 2 + D-009 (one active membership per player)
    - 4 real factories replace Wave 0 stubs per `<interfaces>` spec; switch `protected $model = '...'` strings to `::class`
    - `make phpstan` reports no new errors
    - `make pint ARGS="--test app/Models database/factories"` clean
  </acceptance_criteria>
  <action>
    Build each model from the Phase 3 analogs. Critical points:

    1. MatchSlot — the model class name is `MatchSlot` (no spaces, no underscores; standard CamelCase). The match() BelongsTo relation needs explicit foreign key 'match_id' AND explicit owner Match::class because Laravel's relation-method-name inference would look for `matches_id` (plural) by default. Use: `$this->belongsTo(Match::class, 'match_id')`.

    2. MatchResult.match() and MatchAccessRule.match() — same explicit foreign-key + Match::class pattern.

    3. Polymorphic model name on Event.eventable_type: when reading back via `$event->eventable_type` you get the string 'App\Models\Match' (with backslash). Models that need to verify polymorphic type comparisons should compare against `Match::class` (Laravel auto-converts via morphMap if configured; here no morphMap is set, so the FQN string is direct).

    4. User.php amendment is the CRITICAL Rule 2 piece. Read the entire current User.php first. The activeClanMembership() relation is a HasOne with `whereNull('left_at')` — D-009 guarantees at most one row matches, but Eloquent's HasOne returns the FIRST match (deterministic with the partial UNIQUE in place). Add the HasMany import. The memberships() relation is needed by RESEARCH Pattern 5 (`$user->activeClanMembership?->clan->tags()...`) and also by future Phase 5 Discord bot work.

    5. For factories: each definition() returns the array shape from `<interfaces>`. Document any cross-game/multi-FK caveat in a docblock above definition() per the Phase 3 GameMatchTypeRoleLimitFactory precedent.

    Run `make phpstan` and `make pint --test`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/phpstan analyse app/Models/MatchSlot.php app/Models/MatchAccessRule.php app/Models/MatchResult.php app/Models/MatchMvp.php app/Models/User.php database/factories/MatchSlotFactory.php database/factories/MatchAccessRuleFactory.php database/factories/MatchResultFactory.php database/factories/MatchMvpFactory.php --no-progress --error-format=raw 2>&1 | tail -5 && grep -c 'activeClanMembership' apps/web/app/Models/User.php</automated>
  </verify>
  <done>4 child models + 4 real factories committed; User.php amended with activeClanMembership() + memberships(); phpstan clean.</done>
</task>

<task type="auto">
  <name>Task 3: Replace 6 Wave 0 model test stubs with GREEN assertions (UNIQUE, partial UNIQUE, CHECK, cascade, HasTranslations, polymorphic, audit log)</name>
  <files>
    apps/web/tests/Feature/Models/MatchModelTest.php,
    apps/web/tests/Feature/Models/MatchSlotModelTest.php,
    apps/web/tests/Feature/Models/MatchAccessRuleModelTest.php,
    apps/web/tests/Feature/Models/MatchResultModelTest.php,
    apps/web/tests/Feature/Models/MatchMvpModelTest.php,
    apps/web/tests/Feature/Models/EventModelTest.php
  </files>
  <read_first>
    - apps/web/tests/Feature/Models/GameMatchTypeRoleLimitModelTest.php (Phase 3 analog — UNIQUE/CHECK/cascade/audit assertion pattern via QueryException + DomainException)
    - apps/web/tests/Feature/Models/ClanMembershipModelTest.php (Phase 2 analog — partial-unique assertion via QueryException; uses Match analog directly)
    - apps/web/tests/Feature/Models/GameMatchTypeModelTest.php (Phase 3 analog — HasTranslations round-trip on TWO fields name + description; same shape as Match.title + Match.description)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Validation Architecture § Phase Requirements → Test Map — explicit assertion list per test file)
  </read_first>
  <acceptance_criteria>
    - `MatchModelTest.php` (5+ it() blocks):
      - Match factory creates a valid record (status='open', scheduled_at in future, JSONB title)
      - HasTranslations round-trip on title AND description independently (setTranslation/getTranslations both fields)
      - matches_status_check fires: Match::factory()->create(['status' => 'banana']) throws QueryException
      - 6 relations exist and round-trip: gameMatchType, organiser (User), hostClan (Clan), slots (HasMany), accessRules, result (HasOne), event (MorphOne)
      - Activity log writes a row with subject_type=App\Models\Match on creation
      - Cascade-on-delete (verified via attached slot): deleting a Match deletes its match_slots rows
    - `MatchSlotModelTest.php` (5+ it() blocks):
      - Composite UNIQUE: duplicate (match_id, game_role_id, slot_index) throws QueryException
      - Partial UNIQUE: user can occupy at most one slot per match — explicit setup: create 2 slots in the same match, assign user X to slot A, then attempt to update slot B with user X — QueryException (partial UNIQUE fires)
      - SAME user CAN occupy slots in DIFFERENT matches (different match_id; partial UNIQUE scope is per-match)
      - 3 relations round-trip: match, role (GameRole), occupantUser (User)
      - Cascade-on-delete: deleting a Match deletes its slots
      - Activity log writes a row on slot creation
    - `MatchAccessRuleModelTest.php` (3+ it() blocks):
      - Composite UNIQUE: (match_id, clan_tag_id) duplicate throws QueryException
      - Cascade-on-delete: deleting a Match deletes access rules
      - clanTag relation eager-loads correctly
    - `MatchResultModelTest.php` (4+ it() blocks):
      - 1:1 UNIQUE: two MatchResult rows with the same match_id throws QueryException
      - Score CHECK: allies_score = -1 throws QueryException (match_results_scores_nonneg_check)
      - 4 relations round-trip: match, winnerClan, recordedBy, mvps
      - Cascade-on-delete: deleting Match deletes result + result's MVPs (via the mvps cascade chain)
    - `MatchMvpModelTest.php` (4+ it() blocks):
      - Composite UNIQUE: duplicate (match_result_id, category, player_id) throws QueryException
      - Same player CAN appear in DIFFERENT categories of the same result (assert no throw)
      - Category CHECK: category='banana' throws QueryException
      - Cascade-on-delete: deleting MatchResult deletes mvps
    - `EventModelTest.php` (5+ it() blocks):
      - HasTranslations on title round-trips
      - One-per-owner UNIQUE: creating 2 events for the same (eventable_type, eventable_id) throws QueryException
      - MorphTo eventable() round-trip: create Match + Event with eventable_type='App\Models\Match' + eventable_id=$match->id; assert `$event->eventable->is($match)` returns true (the morph relation resolves to the actual Match instance)
      - Polymorphic eager-load via Match: `$match->load('event')`; `$match->event->id === $event->id`
      - Activity log writes a row on Event creation
    - `make pest ARGS="--filter='Models/(Match|EventModel)'"` GREEN (all 6 files; replaces Wave 0 RED stubs)
    - `make phpstan` reports no new errors
    - The literal string 'placeholder' MUST NOT appear in any of these test files after replacement
  </acceptance_criteria>
  <action>
    Replace each file completely. Use Phase 3 GameMatchTypeRoleLimitModelTest.php + Phase 2 ClanMembershipModelTest.php as analogs. Each Pest file declares `<?php\ndeclare(strict_types=1);` then `use` imports for the model classes, factories, Spatie\Activitylog\Models\Activity, Illuminate\Database\QueryException.

    For the PARTIAL UNIQUE assertion in MatchSlotModelTest (the most security-critical Phase 4 assertion):
    ```
    it('blocks a user occupying two slots in the same match', function () {
        $match = Match::factory()->create();
        $role = GameRole::factory()->create();
        $user = User::factory()->create();
        $slotA = MatchSlot::factory()->create([
            'match_id' => $match->id, 'game_role_id' => $role->id,
            'slot_index' => 0, 'occupant_user_id' => $user->id, 'confirmed_at' => now(),
        ]);
        $slotB = MatchSlot::factory()->create([
            'match_id' => $match->id, 'game_role_id' => $role->id,
            'slot_index' => 1, 'occupant_user_id' => null,
        ]);
        expect(fn () => $slotB->update(['occupant_user_id' => $user->id, 'confirmed_at' => now()]))
            ->toThrow(QueryException::class);
    });
    ```

    For SAME user across DIFFERENT matches:
    ```
    it('allows a user to occupy slots in different matches', function () {
        $matchA = Match::factory()->create();
        $matchB = Match::factory()->create();
        $role = GameRole::factory()->create();
        $user = User::factory()->create();
        MatchSlot::factory()->create([
            'match_id' => $matchA->id, 'game_role_id' => $role->id,
            'slot_index' => 0, 'occupant_user_id' => $user->id,
        ]);
        $slotInB = MatchSlot::factory()->create([
            'match_id' => $matchB->id, 'game_role_id' => $role->id,
            'slot_index' => 0, 'occupant_user_id' => $user->id,
        ]);
        expect($slotInB->fresh()->occupant_user_id)->toBe($user->id);
    });
    ```

    For polymorphic MorphTo round-trip in EventModelTest:
    ```
    it('resolves morphTo eventable to a Match', function () {
        $match = Match::factory()->create();
        $event = Event::factory()->create([
            'eventable_type' => Match::class, 'eventable_id' => $match->id,
        ]);
        expect($event->eventable)->toBeInstanceOf(Match::class)
            ->and($event->eventable->is($match))->toBeTrue();
    });
    ```

    For activity log assertions: import `Spatie\Activitylog\Models\Activity;`. After creating a model, assert `Activity::query()->where('subject_type', \App\Models\Match::class)->where('event', 'created')->exists()`.

    Remove ALL Wave 0 stub `it()` blocks from each file. The literal string `placeholder` must NOT appear in any of these files after replacement.

    NOTE on `Match` class name in test files — these files MUST import `use App\Models\Match;` at the top. PHP's `match($x) { ... }` keyword shares the lowercase spelling; tests that use `match($x)` expressions alongside `Match::factory()` need the alias `use App\Models\Match as MatchModel;` per Pitfall 5. Audit each test file before commit; Phase 4 tests are unlikely to use `match($x)` expressions but the safety pattern is to alias on import in services and controllers consistently (plan 04-04+).

    Run `make pest ARGS="--filter='Models/Match|EventModel'"`. Run `make phpstan`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Feature/Models/MatchModelTest.php tests/Feature/Models/MatchSlotModelTest.php tests/Feature/Models/MatchAccessRuleModelTest.php tests/Feature/Models/MatchResultModelTest.php tests/Feature/Models/MatchMvpModelTest.php tests/Feature/Models/EventModelTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:|placeholder)' | tail -10 && grep -l 'placeholder' apps/web/tests/Feature/Models/Match*.php apps/web/tests/Feature/Models/EventModelTest.php 2>&1 | head -3</automated>
  </verify>
  <done>6 model tests GREEN; Wave 0 stubs removed (grep returns no 'placeholder' hits); partial UNIQUE (match_slots_one_occupancy_per_user) proven; polymorphic MorphTo round-trip proven.</done>
</task>

</tasks>

<verification>
- `make pest ARGS="--filter='Models/(Match|EventModel)'"` -> all GREEN
- `make phpstan` -> no new errors in app/Models, database/factories, tests/Feature/Models, app/Models/User.php
- `make pint ARGS="--test app/Models database/factories tests/Feature/Models"` -> green
- `make artisan ARGS="migrate:fresh --seed"` still succeeds (no seeder dependency change — Phase 2+3 seeders run)
</verification>

<success_criteria>
6 new models + 6 real factories committed; 6 model tests GREEN proving UNIQUE composites + partial UNIQUE (one slot per user per match) + CHECK constraints + cascade chain + HasTranslations round-trip + polymorphic MorphTo round-trip + activity log writes. User.php amended with activeClanMembership() + memberships() accessors (Rule 2 amendment for plan 04-06 consumption). The model layer is the foundation for DTOs (plan 04-07), services (plans 04-04..04-06), observer (plan 04-08), Filament resources (plan 04-09), and controllers (plan 04-10).
</success_criteria>

<output>
After completion, create `.planning/phases/04-matches-manual/04-03-SUMMARY.md` documenting:
- The 6 new model files (with LogsActivity setDescription strings)
- The Pitfall 5 docblock content on Match.php
- The 4 child-model BelongsTo<Match, $this> explicit-foreign-key declarations (because `match_id` doesn't auto-resolve from `match()` due to the non-conventional model name)
- The User.php Rule 2 amendments (activeClanMembership + memberships) with reference to D-009
- 6 GREEN test counts (per-file scenario count)
- The partial UNIQUE assertion approach
- The polymorphic morphTo round-trip assertion
</output>
</content>
</invoke>