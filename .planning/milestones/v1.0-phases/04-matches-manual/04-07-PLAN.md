---
phase: 04-matches-manual
plan: 07
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - apps/web/app/Data/MatchData.php
  - apps/web/app/Data/MatchSlotData.php
  - apps/web/app/Data/MatchAccessRuleData.php
  - apps/web/app/Data/MatchResultData.php
  - apps/web/app/Data/MatchMvpData.php
  - apps/web/app/Data/EventData.php
  - apps/web/app/Data/PublicMatchOccupantData.php
  - apps/web/app/Data/PublicMatchData.php
  - apps/web/resources/js/types/api.d.ts
  - packages/shared-types/src/index.ts
  - apps/web/tests/Unit/Data/MatchDataTest.php
  - apps/web/tests/Unit/Data/PublicMatchDataTest.php
  - apps/web/tests/Unit/Data/EventDataTest.php
autonomous: true
requirements:
  - REQ-goal-match-workflows
must_haves:
  truths:
    - "8 spatie/laravel-data DTO classes exist with #[TypeScript] attribute, mirroring Phase 3 GameMatchTypeData verbatim pattern (Pitfall 4 — fromModel factory uses getTranslations for translatable JSONB)"
    - "MatchData carries translatable title/description (array<string,string>|null), scheduled_at ISO-8601 string, status, is_public, game_match_type_id, organiser_user_id, host_clan_id"
    - "MatchSlotData carries id, match_id, game_role_id, slot_index, occupant_user_id (nullable), confirmed_at (nullable ISO-8601), sort_order"
    - "EventData carries id, eventable_type, eventable_id, starts_at, ends_at (nullable), title (array<string,string>|null), is_public"
    - "PublicMatchOccupantData is the privacy-shaped slot DTO for /matches/{id}: displayName/playerSlug nullable when PlayerPrivacyGate denies; clanTag always shown (D-008 — clan tags public); isViewer bool"
    - "PublicMatchData strips admin-only fields (organiser_user_id, status if cancelled) — used by /matches/{id} controller"
    - "make artisan ARGS=\"trenchwars:typescript-generate\" regenerates apps/web/resources/js/types/api.d.ts with 8 new DTO types under the App.Data namespace"
    - "packages/shared-types/src/index.ts is updated with 8 new export type aliases (MatchData, MatchSlotData, etc.)"
    - "3 GREEN DTO unit tests cover: translatable JSONB round-trip on MatchData and EventData; PublicMatchOccupantData privacy strip (displayName=null path); EventData polymorphic eventable_type/id; pnpm typecheck on shared-types passes"
  artifacts:
    - path: "apps/web/app/Data/MatchData.php"
      provides: "Match DTO with translatable JSONB casts + fromModel factory"
      contains: "#[TypeScript]"
    - path: "apps/web/app/Data/PublicMatchOccupantData.php"
      provides: "Privacy-shaped slot occupant DTO consumed by Matches/Show.vue"
      contains: "displayName"
    - path: "apps/web/resources/js/types/api.d.ts"
      provides: "Regenerated TypeScript types including Phase 4 DTOs"
      contains: "MatchData"
    - path: "packages/shared-types/src/index.ts"
      provides: "Cross-package aliases for Phase 4 DTOs"
      contains: "MatchData"
  key_links:
    - from: "MatchData::fromModel"
      to: "Match->getTranslations('title')"
      via: "translatable JSONB extraction"
      pattern: "getTranslations"
    - from: "PublicMatchOccupantData::fromMatchSlot"
      to: "PlayerPrivacyGate::passesTier + allowsSection"
      via: "privacy gate applied server-side; UI never re-derives"
      pattern: "PlayerPrivacyGate"
    - from: "EventData"
      to: "MorphTo eventable resolution"
      via: "eventable_type FQN string + eventable_id UUID"
      pattern: "eventable_type"
---

<objective>
Land the 8 spatie/laravel-data DTOs for Phase 4 + TypeScript regen + shared-types sync + 3 GREEN unit tests. The DTOs follow Phase 3 GameMatchTypeData verbatim pattern (Pitfall 4 from RESEARCH — translatable JSONB extracted via getTranslations() inside fromModel; PHPStan L8 typed array<string,string>|null for translatable columns).

The two privacy-shaped DTOs (PublicMatchOccupantData + PublicMatchData) are the security-critical pieces — Matches/Show.vue (plan 04-11) renders these DTOs verbatim, never re-deriving privacy from raw fields. The PublicMatchOccupantData applies PlayerPrivacyGate per occupant per match (D-018 + RESEARCH Pattern 7); the controller in plan 04-10 builds the DTO collection server-side.

Purpose: SC-1, SC-3, SC-4, SC-5 all require Vue pages and Filament panels to receive typed data. Without DTOs, Inertia props are unstable shapes and api.d.ts cannot be regenerated. Plan 04-10 (public controllers) and plan 04-11 (Vue pages) consume these DTOs. Plan 04-08 (observer) uses EventData via the public render path.

Output: 8 DTO classes; api.d.ts regenerated; shared-types/src/index.ts amended; 3 GREEN unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-matches-manual/04-RESEARCH.md
@CLAUDE.md
@apps/web/app/Data/GameData.php
@apps/web/app/Data/GameMatchTypeData.php
@apps/web/app/Data/GameMatchTypeRoleLimitData.php
@apps/web/app/Data/PublicPlayerData.php
@apps/web/app/Services/PlayerPrivacyGate.php
@apps/web/app/Models/Match.php
@apps/web/app/Models/MatchSlot.php
@apps/web/app/Models/Event.php
@apps/web/resources/js/types/api.d.ts
@packages/shared-types/src/index.ts

<interfaces>
Canonical Phase 3 DTO pattern (verified `apps/web/app/Data/GameMatchTypeData.php`):
- `<?php\ndeclare(strict_types=1); namespace App\Data;`
- Imports: `use Spatie\LaravelData\Data; use Spatie\TypeScriptTransformer\Attributes\TypeScript;`
- Class attribute: `#[TypeScript]` immediately above `final class XData extends Data`
- PHPDoc `/** @param array<string, string>|null $name */` on the constructor for translatable JSONB params (PHPStan L8 requirement)
- Constructor with public typed properties (snake_case to match the Vue/Inertia transport convention — Phase 3 idiom)
- Static `fromModel(GameMatchType $matchType): self` factory using $matchType->getTranslations('name') for translatable JSONB; null-coalesce empty to null

From RESEARCH Pattern 9 (verbatim — full MatchData snippet):
```
#[TypeScript]
final class MatchData extends Data
{
    /**
     * @param array<string, string>|null $title
     * @param array<string, string>|null $description
     */
    public function __construct(
        public string $id,
        public string $game_match_type_id,
        public ?array $title,
        public ?array $description,
        public string $scheduled_at,
        public string $status,
        public bool $is_public,
        public string $organiser_user_id,
        public ?string $host_clan_id,
    ) {}

    public static function fromModel(Match $match): self
    {
        return new self(
            id: $match->id,
            game_match_type_id: $match->game_match_type_id,
            title: $match->getTranslations('title') ?: null,
            description: $match->getTranslations('description') ?: null,
            scheduled_at: $match->scheduled_at->toIso8601String(),
            status: $match->status,
            is_public: $match->is_public,
            organiser_user_id: $match->organiser_user_id,
            host_clan_id: $match->host_clan_id,
        );
    }
}
```

Other DTOs (column lists, mostly derived from migrations + RESEARCH Pattern 9):

MatchSlotData:
- public string $id, string $match_id, string $game_role_id, int $slot_index, ?string $occupant_user_id, ?string $confirmed_at, int $sort_order
- fromModel(MatchSlot $slot): self

MatchAccessRuleData:
- public string $id, string $match_id, string $clan_tag_id, ?ClanTagData $clan_tag (eager-load-aware via relationLoaded check)
- fromModel(MatchAccessRule $rule): self

MatchResultData:
- public string $id, string $match_id, ?string $winner_clan_id, ?int $allies_score, ?int $axis_score, ?string $notes, string $recorded_by_user_id, string $recorded_at
- fromModel(MatchResult $result): self

MatchMvpData:
- public string $id, string $match_result_id, string $player_id, string $category, ?int $value
- fromModel(MatchMvp $mvp): self

EventData:
- public string $id, string $eventable_type, string $eventable_id, string $starts_at, ?string $ends_at, ?array $title, bool $is_public
- fromModel(Event $event): self (with getTranslations('title') for the title)
- PHPDoc `/** @param array<string, string>|null $title */`

PublicMatchOccupantData (RESEARCH Pattern 7 verbatim):
```
#[TypeScript]
final class PublicMatchOccupantData extends Data
{
    public function __construct(
        public string $slotId,
        public string $gameRoleId,
        public int $slotIndex,
        public ?string $displayName,
        public ?string $playerSlug,
        public ?string $clanTag,
        public ?string $clanSlug,
        public bool $isViewer,
    ) {}

    public static function empty(MatchSlot $slot): self
    {
        return new self(
            slotId: $slot->id,
            gameRoleId: $slot->game_role_id,
            slotIndex: $slot->slot_index,
            displayName: null, playerSlug: null,
            clanTag: null, clanSlug: null,
            isViewer: false,
        );
    }
}
```
- camelCase property names (UI-facing — different from MatchData snake_case; matches the Phase 2 PublicPlayerData camelCase convention)

PublicMatchData (privacy-shaped Match for /matches/{id}):
- Same as MatchData minus organiser_user_id (admin-only field) plus computed slug placeholder for future SEO
- Public read: id, game_match_type_id, title, description, scheduled_at, status, is_public, host_clan_id
- Excludes: organiser_user_id (admin-internal), server_address (only shown to confirmed signups in a future polish phase)
- fromModel(Match $match): self

TypeScript regen command (Phase 3 plan 03-04 / Phase 1 plan 01-15 idiom):
- `make artisan ARGS="trenchwars:typescript-generate"`
- This runs the custom `trenchwars:typescript-generate` artisan command (Phase 1 plan 01-15) which uses spatie/laravel-typescript-transformer to emit `apps/web/resources/js/types/api.d.ts` with `declare namespace App { export namespace Data { ... } }`

shared-types sync (Phase 1 plan 01-15 + Phase 3 idiom):
- After regen, the file `packages/shared-types/src/index.ts` needs new `export type X = App.Data.X;` lines for the 8 new DTOs (mirroring the Phase 3 update done in plan 03-10 / phase verification)
- The sync command from Phase 1 plan 01-15: either an artisan-side sync OR a manual edit to packages/shared-types/src/index.ts depending on the wiring set up in Phase 1

Pest test patterns (Phase 3 GameDataTest analog, located at `apps/web/tests/Feature/Data/GameDataTest.php` — note: Phase 3 used tests/Feature/Data; Phase 4 uses tests/Unit/Data per VALIDATION.md):
- it('hydrates MatchData from a Match model with translatable title') — assert ->title === ['en' => '...']
- it('returns null title when match.title JSONB is empty array') — assert ->title === null
- it('emits #[TypeScript] attribute resolved by the transformer') — assert via reflection `(new ReflectionClass(MatchData::class))->getAttributes(TypeScript::class)` is not empty
- For PublicMatchOccupantData test: setup with privacy=denied case → assert displayName === null && clanTag !== null

PHPStan L8 requirements:
- `/** @param array<string, string>|null $title */` on every translatable JSONB parameter
- Constructor property promotion with explicit types
- Static fromModel returns `self`

NOTE on the `App\Models\Match` import in DTO files: Phase 4 DTO classes reference `App\Models\Match` — use alias on import per Pitfall 5: `use App\Models\Match as MatchModel;` then signature reads `fromModel(MatchModel $match): self`.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DTO shape to Vue render | Vue templates receive DTO arrays; absent vs null distinguishes "withheld" vs "no value" (Pattern 7 / Phase 2 Pitfall 2) |
| PlayerPrivacyGate to PublicMatchOccupantData | Privacy gate runs SERVER-SIDE during DTO construction; Vue receives pre-filtered data; UI never re-derives |
| TypeScript regen to packaged types | shared-types/src/index.ts aliases the App.Data.X namespace; package consumers (bot, rcon-worker) import these types |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Information Disclosure | Privacy bypass via raw DTO field exposure | mitigate | PublicMatchOccupantData has NO raw User/Player fields — only the privacy-stripped output. The controller (plan 04-10) builds the DTO collection via PlayerPrivacyGate per occupant; Vue receives `displayName=null` instead of "show_match_history=false" |
| T-04-07-02 | Tampering | Translatable JSONB null vs empty drift | mitigate | Phase 3 Pitfall 4 mitigation copied: fromModel uses `getTranslations('field') ?: null` — null-coalesce empty array to null so TypeScript receives null (Vue v-if then absent-vs-null contract works correctly) |
| T-04-07-03 | Information Disclosure | Admin-only fields leak via PublicMatchData | mitigate | PublicMatchData omits organiser_user_id, server_address; admin fields live in MatchData (used by Filament + my-clan management surfaces) |
| T-04-07-04 | Tampering | Polymorphic eventable_type as untyped string | accept | EventData carries eventable_type as plain string; Phase 4 only writes Match::class; Phase 6 Tournament reuses; consumers compare by FQN string |
| T-04-07-05 | Information Disclosure | DTOs of cancelled matches surface on public calendar | mitigate | The controller (plan 04-10) filters at the query layer (status NOT IN ('draft','cancelled')) BEFORE DTO hydration; DTO itself is a shape, not a filter |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: 6 internal DTOs (Match, MatchSlot, MatchAccessRule, MatchResult, MatchMvp, Event)</name>
  <files>
    apps/web/app/Data/MatchData.php,
    apps/web/app/Data/MatchSlotData.php,
    apps/web/app/Data/MatchAccessRuleData.php,
    apps/web/app/Data/MatchResultData.php,
    apps/web/app/Data/MatchMvpData.php,
    apps/web/app/Data/EventData.php
  </files>
  <read_first>
    - apps/web/app/Data/GameMatchTypeData.php (Phase 3 — two translatable JSONB fields name + description; same shape as MatchData title + description)
    - apps/web/app/Data/GameMatchTypeRoleLimitData.php (Phase 3 — child DTO with two FK references)
    - apps/web/app/Data/GameData.php (Phase 3 — single-translatable analog for EventData's title)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pattern 9 — verbatim MatchData snippet)
    - apps/web/app/Models/Match.php (plan 04-03 — confirms getTranslations API)
    - apps/web/app/Models/Event.php (plan 04-03 — confirms HasTranslations on title)
  </read_first>
  <acceptance_criteria>
    - 6 DTO files in apps/web/app/Data/:
      - Each declares `<?php\ndeclare(strict_types=1); namespace App\Data;`
      - Each has `#[TypeScript]` attribute above `final class XData extends Data`
      - Each has constructor with public typed properties matching the column list in `<interfaces>` (snake_case for internal DTOs to match Phase 3 idiom)
      - Each has `public static function fromModel(<Model> $model): self` factory
      - Translatable fields: PHPDoc `/** @param array<string, string>|null $title */` + null-coalesce `?: null` pattern in fromModel
      - DateTime fields: emitted as ISO-8601 strings via `->toIso8601String()` (per Phase 3 idiom + Pitfall 8/9)
      - `use App\Models\Match as MatchModel;` import alias in MatchData (Pitfall 5)
    - `make phpstan` reports no new errors on these 6 files
    - `make pint ARGS="--test app/Data"` clean
  </acceptance_criteria>
  <action>
    Mirror Phase 3 GameMatchTypeData verbatim, substituting names + fields. Key technical points:

    1. The constructor uses snake_case for internal DTOs (game_match_type_id, scheduled_at, is_public) to match Phase 3 — the TS namespace App.Data.MatchData will have snake_case keys; Vue components access via match.game_match_type_id etc.

    2. The `#[TypeScript]` attribute is the trigger for typescript-transformer to emit the type. Without it, the DTO is invisible to the TS generator.

    3. Translatable fields use `$match->getTranslations('title') ?: null` — the `?: null` is critical (Phase 3 Pitfall 4). An empty JSONB column returns `[]` from getTranslations; `[] ?: null` evaluates to null because empty arrays are falsy in PHP. This makes TypeScript receive null, which Vue's `v-if="match.title !== undefined"` handles correctly (rather than rendering `{en: ''}` empty string).

    4. For datetime: use `->toIso8601String()` (Phase 3 idiom) — emits e.g. "2026-05-13T12:00:00+00:00". Vue's dayjs/luxon parsers consume this.

    5. For nullable FK columns (host_clan_id, occupant_user_id, winner_clan_id, value, ends_at): use `?string` or `?int` types; pass through from model directly.

    6. EventData.eventable_type: emitted as plain string. Vue components compare by FQN: `event.eventable_type === 'App\\\\Models\\\\Match'`. (Backslash escaping for JS string.)

    Run `make phpstan` and `make pint --test`.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/phpstan analyse app/Data/MatchData.php app/Data/MatchSlotData.php app/Data/MatchAccessRuleData.php app/Data/MatchResultData.php app/Data/MatchMvpData.php app/Data/EventData.php --no-progress --error-format=raw 2>&1 | tail -5</automated>
  </verify>
  <done>6 internal DTOs committed; phpstan + pint clean; translatable JSONB extraction pattern preserved from Phase 3.</done>
</task>

<task type="auto">
  <name>Task 2: 2 privacy-shaped DTOs (PublicMatchOccupantData + PublicMatchData) + TypeScript regen + shared-types sync</name>
  <files>
    apps/web/app/Data/PublicMatchOccupantData.php,
    apps/web/app/Data/PublicMatchData.php,
    apps/web/resources/js/types/api.d.ts,
    packages/shared-types/src/index.ts
  </files>
  <read_first>
    - apps/web/app/Data/PublicPlayerData.php (Phase 2 — privacy-shaped DTO analog; camelCase property names; PlayerPrivacyGate integration)
    - apps/web/app/Services/PlayerPrivacyGate.php (Phase 2 — passesTier + allowsSection signatures)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pattern 7 verbatim PublicMatchOccupantData snippet + Controller pseudocode for the DTO construction site)
    - apps/web/resources/js/types/api.d.ts (current — verify the App.Data namespace structure before regen)
    - packages/shared-types/src/index.ts (current — verify the Phase 2/3 export type alias structure)
  </read_first>
  <acceptance_criteria>
    - `PublicMatchOccupantData.php`:
      - `<?php\ndeclare(strict_types=1); namespace App\Data;`
      - `#[TypeScript] final class PublicMatchOccupantData extends Data`
      - Constructor per RESEARCH Pattern 7 verbatim (8 properties: slotId, gameRoleId, slotIndex, displayName?, playerSlug?, clanTag?, clanSlug?, isViewer)
      - camelCase property names (matches Phase 2 PublicPlayerData convention)
      - `public static function empty(MatchSlot $slot): self` factory for unfilled slots — returns instance with all nullable fields as null, isViewer=false
      - `public static function fromMatchSlot(MatchSlot $slot, ?User $viewer, PlayerPrivacyGate $gate): self` factory — applies privacy gate per Pattern 7 controller pseudocode: if !occupant → empty(); else canSee = gate->passesTier($player, $viewer) && gate->allowsSection($player, $viewer, 'show_match_history'); set displayName/playerSlug to null if !canSee; clanTag always present (D-008 — clan tags public)
    - `PublicMatchData.php`:
      - `#[TypeScript] final class PublicMatchData extends Data`
      - Constructor with subset of MatchData fields excluding organiser_user_id + server_address (admin-only)
      - `public static function fromModel(MatchModel $match): self`
      - camelCase OR snake_case — match the Phase 2 PublicPlayerData convention (Phase 2 uses camelCase for "Public*" prefix DTOs; mirror)
    - `make artisan ARGS="trenchwars:typescript-generate"` runs successfully and produces an updated `apps/web/resources/js/types/api.d.ts` containing:
      - `interface MatchData` (or `type MatchData`) under `App.Data` namespace
      - All 8 new DTO types from this plan
    - `packages/shared-types/src/index.ts` is updated with 8 new export type aliases:
      - `export type MatchData = App.Data.MatchData;`
      - `export type MatchSlotData = App.Data.MatchSlotData;`
      - `export type MatchAccessRuleData = App.Data.MatchAccessRuleData;`
      - `export type MatchResultData = App.Data.MatchResultData;`
      - `export type MatchMvpData = App.Data.MatchMvpData;`
      - `export type EventData = App.Data.EventData;`
      - `export type PublicMatchOccupantData = App.Data.PublicMatchOccupantData;`
      - `export type PublicMatchData = App.Data.PublicMatchData;`
      - Append after the Phase 2/3 export blocks; preserve existing aliases
    - `make pnpm ARGS="-F web run typecheck"` clean
    - `pnpm --filter @trenchwars/shared-types typecheck` (run host-side via corepack per Phase 1 plan 01-16 idiom) clean
    - `make phpstan` reports no new errors
  </acceptance_criteria>
  <action>
    Build PublicMatchOccupantData verbatim from RESEARCH Pattern 7. Two factories (empty + fromMatchSlot). The fromMatchSlot factory is THE Pattern 7 implementation — Vue receives data shaped by THIS code, not by the raw model.

    For PublicMatchData: a thin shape that excludes admin-only fields. Same fromModel signature as MatchData but with fewer properties.

    For typescript-generate: run `make artisan ARGS="trenchwars:typescript-generate"` (Phase 1 plan 01-15 wired this artisan command). The transformer emits api.d.ts with all #[TypeScript] DTOs in the codebase. After running, `grep -c 'MatchData\|EventData\|PublicMatchOccupantData' apps/web/resources/js/types/api.d.ts` should return 3+.

    For shared-types/src/index.ts: read the existing file. The Phase 2 + Phase 3 aliases are already there. Append 8 new lines for Phase 4. The container-side sync runs through the existing bind mount (Phase 1 plan 01-15 wiring); the api.d.ts is generated INSIDE the web container at apps/web/resources/js/types/api.d.ts, then the bind mount on packages/shared-types means edits to packages/shared-types/src/index.ts are visible to BOTH host and container immediately.

    NOTE on the privacy gate dependency: PublicMatchOccupantData::fromMatchSlot takes PlayerPrivacyGate as a dependency-injected parameter (not auto-resolved). The controller in plan 04-10 resolves the gate from the container and passes it to the DTO factory: `app(PlayerPrivacyGate::class)` or via constructor injection in MatchShowController.

    Run `make pnpm ARGS="-F web run typecheck"`. Run host-side `pnpm --filter @trenchwars/shared-types typecheck` (canonical from Phase 1 plan 01-16). Run pint + phpstan.
  </action>
  <verify>
    <automated>docker compose exec web php artisan trenchwars:typescript-generate 2>&1 | tail -5 && grep -E 'MatchData|EventData|PublicMatchOccupantData' apps/web/resources/js/types/api.d.ts | head -5 && grep -E 'MatchData|EventData|PublicMatchOccupantData' packages/shared-types/src/index.ts | head -5 && docker compose exec web ./vendor/bin/phpstan analyse app/Data/PublicMatchOccupantData.php app/Data/PublicMatchData.php --no-progress --error-format=raw 2>&1 | tail -5</automated>
  </verify>
  <done>2 privacy DTOs committed; api.d.ts regenerated; shared-types/src/index.ts amended; phpstan + pint clean; typescript-generate succeeded.</done>
</task>

<task type="auto">
  <name>Task 3: Replace 3 Wave 0 DTO unit test stubs with GREEN assertions</name>
  <files>
    apps/web/tests/Unit/Data/MatchDataTest.php,
    apps/web/tests/Unit/Data/PublicMatchDataTest.php,
    apps/web/tests/Unit/Data/EventDataTest.php
  </files>
  <read_first>
    - apps/web/tests/Feature/Data/GameDataTest.php (Phase 3 — DTO test analog; fromModel + translatable JSONB assertions)
    - apps/web/tests/Unit (existing structure — Phase 2 PublicPlayerDataTest pattern)
    - .planning/phases/04-matches-manual/04-RESEARCH.md (Pattern 7 + Pattern 9 — assertion enumeration for the 3 unit tests)
    - apps/web/app/Services/PlayerPrivacyGate.php (privacy gate signatures for the PublicMatchOccupantData test setup)
  </read_first>
  <acceptance_criteria>
    - `MatchDataTest.php` (4+ it() blocks):
      - it('hydrates MatchData from a Match model with translatable title')
      - it('returns null title when match.title JSONB is empty array')
      - it('emits scheduled_at as ISO-8601 string')
      - it('preserves host_clan_id null when match has no host clan')
      - it('emits #[TypeScript] attribute resolved by transformer reflection')
    - `PublicMatchDataTest.php` (4+ it() blocks):
      - it('strips organiser_user_id from PublicMatchData')
      - it('strips server_address from PublicMatchData')
      - it('PublicMatchOccupantData::fromMatchSlot returns displayName=null when privacy.show_match_history=false')
      - it('PublicMatchOccupantData::fromMatchSlot returns clanTag !== null even when displayName is null (D-008: clan tags always public)')
      - it('PublicMatchOccupantData::empty returns null for displayName/playerSlug/clanTag and isViewer=false')
      - it('PublicMatchOccupantData::fromMatchSlot sets isViewer=true when viewer === occupant')
    - `EventDataTest.php` (3+ it() blocks):
      - it('hydrates EventData from polymorphic match-owned Event')
      - it('preserves eventable_type and eventable_id verbatim from model')
      - it('returns null title when event.title JSONB is empty array')
      - it('emits starts_at and ends_at as ISO-8601 strings; ends_at nullable')
    - All tests use App\Models\Match imported aliased as MatchModel (Pitfall 5)
    - Wave 0 'placeholder' literal removed from all 3 files
    - `make pest ARGS="--filter='Data/(MatchData|PublicMatchData|EventData)'"` GREEN
    - `make phpstan` reports no new errors
  </acceptance_criteria>
  <action>
    Build each test using the Phase 3 GameDataTest analog. The PublicMatchDataTest is the most security-critical — exercise the PlayerPrivacyGate integration directly.

    For PublicMatchOccupantData test setup (privacy-strip path):
    ```
    $player = Player::factory()->create();
    PlayerPrivacy::factory()->create([
        'player_id' => $player->id,
        'show_to' => 'public',
        'show_match_history' => false,
    ]);
    $slot = MatchSlot::factory()->create(['occupant_user_id' => $player->user->id]);
    $viewer = User::factory()->create();
    $dto = PublicMatchOccupantData::fromMatchSlot($slot, $viewer, app(PlayerPrivacyGate::class));
    expect($dto->displayName)->toBeNull()->and($dto->playerSlug)->toBeNull();
    ```

    For the clan-tag-always-public assertion: set up the same scenario but additionally attach a clan tag to the user's active clan; assert dto->clanTag === 'eu' (or whatever tag.slug is) even though displayName is null. This proves D-008 fidelity.

    For the EventData polymorphic test: assert dto->eventable_type === 'App\\Models\\Match' (the literal class FQN string).

    Run pest --filter and pint + phpstan.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest tests/Unit/Data/MatchDataTest.php tests/Unit/Data/PublicMatchDataTest.php tests/Unit/Data/EventDataTest.php --no-coverage 2>&1 | grep -E '(PASS|FAIL|Tests:|placeholder)' | tail -8</automated>
  </verify>
  <done>3 DTO unit tests GREEN; privacy strip path proven; 'placeholder' literal removed.</done>
</task>

</tasks>

<verification>
- `make pest ARGS="--filter='Data/(MatchData|PublicMatchData|EventData)'"` -> GREEN (11+ tests)
- `make phpstan` -> no new errors
- `make pint --test` -> green
- `make pnpm ARGS="-F web run typecheck"` -> clean
- `pnpm --filter @trenchwars/shared-types typecheck` -> clean
- `grep -c 'MatchData\|EventData\|PublicMatchOccupantData' apps/web/resources/js/types/api.d.ts` -> 3 or more
- `grep -c 'MatchData\|EventData\|PublicMatchOccupantData' packages/shared-types/src/index.ts` -> 3 or more
- Wave 0 baseline incomplete count drops by 3 (3 Data tests GREEN)
</verification>

<success_criteria>
8 DTOs exist; api.d.ts regenerated; shared-types/src/index.ts amended. Plan 04-10's MatchShowController calls `PublicMatchOccupantData::fromMatchSlot($slot, $viewer, $gate)` per slot to build the per-role-grouped occupant grid. Plan 04-11's Matches/Show.vue receives the privacy-stripped occupant DTOs and renders them verbatim — no client-side privacy logic.
</success_criteria>

<output>
After completion, create `.planning/phases/04-matches-manual/04-07-SUMMARY.md` documenting:
- The 8 DTO classes (snake_case vs camelCase per Phase 2/3 convention split)
- Translatable JSONB null-coalesce pattern reuse (Phase 3 Pitfall 4)
- The PublicMatchOccupantData::fromMatchSlot signature including the PlayerPrivacyGate dependency
- api.d.ts regen confirmation
- packages/shared-types/src/index.ts new aliases (8 lines added)
- Test count + per-it() coverage; privacy strip path proven
</output>
