---
phase: 07-cms
plan: 11
type: execute
wave: 6
depends_on: ["07-10"]
files_modified:
  - docker-compose.yml
  - apps/web/config/inertia.php
  - apps/web/resources/js/ssr.ts
  - apps/web/resources/views/app.blade.php
  - apps/web/.env.example
  - apps/web/.env.testing
  - apps/web/tests/Feature/Ssr/SsrBundleExistsTest.php
  - apps/web/tests/Feature/Ssr/SsrLocaleHonouredTest.php
autonomous: true
requirements:
  - REQ-success-public-browse
must_haves:
  truths:
    - "docker-compose.yml extended with a 6th `ssr` service (Open Question 7 LOCKED inline RESOLVED — Per RESEARCH Pattern 5 Assumption A5 + Open Question 7 Recommendation: split SSR into its own service for prod-parity, kept off in dev by default — opt-in via INERTIA_SSR_ENABLED=true env override): build context same as web Dockerfile; command runs `php artisan inertia:start-ssr`; healthcheck via curl -f http://127.0.0.1:13714/health; depends_on postgres+redis; env INERTIA_SSR_ENABLED=true (service-level — does NOT affect web service default)"
    - "apps/web/config/inertia.php — ssr.enabled = env('INERTIA_SSR_ENABLED', false) — already scaffolded in Phase 1 plan 01-06 (no change needed, just verify). ssr.url = env('INERTIA_SSR_URL', 'http://ssr:13714') — UPDATE the url default from 'http://127.0.0.1:13714' to 'http://ssr:13714' so the web service in docker-compose finds the ssr service via service name (D-021)"
    - "apps/web/resources/js/ssr.ts — verify present (Phase 1 plan 01-06 left it scaffolded but disabled). If present + functional, leave as-is. If broken / outdated, refresh to use the current @inertiajs/vue3/server entry shape with createInertiaApp + ZiggyVue + Vue's createSSRApp"
    - "apps/web/resources/views/app.blade.php — verify <html lang=\"{{ app()->getLocale() }}\"> already present (Phase 1 plan 01-06 + plan 01-08 LocaleMiddleware established). If not, add it. Pitfall 8 mitigation — locale resolver must run BEFORE Inertia middleware (verify in apps/web/bootstrap/app.php middleware chain)"
    - ".env.example documents INERTIA_SSR_ENABLED=false + INERTIA_SSR_URL=http://ssr:13714 with comments referencing this plan + Pattern 5"
    - ".env.testing — explicit INERTIA_SSR_ENABLED=false override so test runs (without ssr service running) don't try to reach the ssr port"
    - "SsrBundleExistsTest GREEN (replaces 07-01 RED stub) — 4+ it() blocks: bootstrap/ssr/ssr.mjs file exists post pnpm build (smoke); config('inertia.ssr.enabled') honours INERTIA_SSR_ENABLED env var; config('inertia.ssr.url') defaults to http://ssr:13714; ssr service registered in docker-compose.yml (parse the yaml file via Symfony/Yaml — Phase 5 plan 05-01 idiom)"
    - "SsrLocaleHonouredTest GREEN (NEW) — 3+ it() blocks: Accept-Language: cs request returns <html lang=\"cs\"> in HTML head (Pitfall 8 — LocaleMiddleware → app()->getLocale() → Blade resolution chain); default unspecified Accept-Language returns en; setLocale via cookie path tested too"
    - "Manual smoke not required at this plan — automated tests prove SSR mechanically; operator smoke deferred to plan 07-13 phase verification (verify first-paint via Railway prod env)"
    - "make pest filter GREEN; phpstan + pint clean; pnpm build produces ssr bundle"
  artifacts:
    - path: "docker-compose.yml"
      provides: "6th `ssr` service for prod-parity SSR (Open Question 7 LOCKED inline RESOLVED — split service)"
      contains: "ssr:"
    - path: "apps/web/config/inertia.php"
      provides: "Verified ssr.enabled env-driven + url default updated to http://ssr:13714"
      contains: "ssr.url"
    - path: "apps/web/.env.example"
      provides: "Documented INERTIA_SSR_ENABLED + INERTIA_SSR_URL shape"
      contains: "INERTIA_SSR_ENABLED"
    - path: "apps/web/tests/Feature/Ssr/SsrBundleExistsTest.php"
      provides: "4+ GREEN it() blocks asserting SSR config + bundle + service registration"
      contains: "bootstrap/ssr/ssr.mjs"
    - path: "apps/web/tests/Feature/Ssr/SsrLocaleHonouredTest.php"
      provides: "3+ GREEN it() blocks proving locale resolution chain (Pitfall 8 mitigation)"
      contains: "Accept-Language"
  key_links:
    - from: "docker-compose ssr service"
      to: "Railway prod env (D-014)"
      via: "Railway scales the ssr service as its own component; ENV var INERTIA_SSR_ENABLED=true flips it on per-env"
      pattern: "ssr:"
    - from: "config/inertia.php ssr.url"
      to: "Phase 5 worker service docker-compose"
      via: "Web service nginx-php-fpm → ssr service over docker network port 13714; no DB access from ssr Node process (T-07-11-04 mitigation)"
      pattern: "http://ssr"
    - from: "app.blade.php <html lang>"
      to: "LocaleMiddleware → app()->getLocale() (Phase 1)"
      via: "Locale resolver runs before Inertia middleware so SSR Node process sees correct locale via Inertia shared props (Pitfall 8)"
      pattern: "lang="
---

<objective>
Wave 6 (parallel with 07-10): Enable Inertia v2 SSR for production. Split into a 6th `ssr` service in docker-compose (Open Question 7 LOCKED inline RESOLVED: split service for prod parity, off in dev by default per env). Verify the Phase 1 SSR scaffolding (ssr.ts, app.blade.php lang attribute, config/inertia.php) is intact and tune the ssr.url default for docker service-name resolution.

Purpose: SC-3 explicitly requires "SSR enabled in production for first paint on public pages." This plan wires the production-parity SSR path. Dev defaults stay false (Phase 1 plan 01-06 D-021 — keeps dev loop fast); CI tests use .env.testing override to keep test runs predictable. Pitfall 8 (locale drift between Blade SSR HTML and Inertia-resolved client locale) is locked down via test.

Output: docker-compose ssr service + 4 config / view / env edits + 2 GREEN test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cms/07-CONTEXT.md
@.planning/phases/07-cms/07-RESEARCH.md
@.planning/phases/07-cms/07-10-SUMMARY.md
@docker-compose.yml
@apps/web/config/inertia.php
@apps/web/resources/js/ssr.ts
@apps/web/resources/views/app.blade.php
@apps/web/.env.example

<interfaces>
<!-- docker-compose ssr service addition (verbatim from RESEARCH Pattern 5) -->

```yaml
# docker-compose.yml — APPEND under services: (preserve existing 5 services)
  ssr:
    build:
      context: .
      dockerfile: docker/web/Dockerfile
    container_name: trenchwars-ssr
    command: ["php", "artisan", "inertia:start-ssr"]
    environment:
      INERTIA_SSR_ENABLED: "true"
      INERTIA_SSR_URL: "http://0.0.0.0:13714"
      APP_ENV: "${APP_ENV:-production}"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://127.0.0.1:13714/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - .:/repo  # consistent with other services per D-021
    ports: []   # NOT exposed to host — internal docker network only (T-07-11-04)
```

<!-- Open Question 7 LOCKED inline -->

Per RESEARCH Open Question 7 Recommendation: "Global toggle on in production. Filament panel is already SSR-friendly (Livewire SSR by design). Phase 1 plan 01-06 left SSR disabled in dev (Pitfall 8 mitigation); production stays enabled."

Decision: SSR enabled globally in production (single env var INERTIA_SSR_ENABLED=true at Railway env group level — D-014); off in dev by default. Per-route opt-in deferred to v2.

<!-- Pitfall 8 mitigation — locale resolution chain verification -->

```php
// apps/web/resources/views/app.blade.php should already have this from Phase 1 plan 01-06
<html lang="{{ app()->getLocale() }}" data-theme="dark">
```

Verify via test that Accept-Language: cs header → app()->getLocale() === 'cs' → <html lang="cs"> in response body. If not, the LocaleMiddleware (Phase 1 plan 01-08) is mis-ordered in bootstrap/app.php — it must run BEFORE HandleInertiaRequests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docker-compose.yml with ssr service + verify/tune apps/web/config/inertia.php + apps/web/.env.example + .env.testing</name>
  <files>docker-compose.yml, apps/web/config/inertia.php, apps/web/.env.example, apps/web/.env.testing</files>
  <action>
    1. Edit docker-compose.yml in-place — PRESERVE all 5 existing services verbatim (web, worker, bot, postgres, redis from D-021 + Phase 5 worker addition). APPEND the ssr service per <interfaces>. Critical:
       - command runs php artisan inertia:start-ssr (no shell expansion needed; array form works).
       - ports: [] explicitly — SSR is internal-only; never exposed to host (T-07-11-04).
       - healthcheck on /health endpoint (inertia:start-ssr exposes one automatically; verify via curl after first up).
       - depends_on postgres + redis (even though SSR doesn't touch DB directly — keep consistent with web service).
       - APP_ENV defaults to production via env interpolation to ensure SSR runs as if in prod (when developer manually runs `docker compose up ssr` in dev).

    2. Edit apps/web/config/inertia.php in-place. Verify 'ssr.enabled' line is `'enabled' => (bool) env('INERTIA_SSR_ENABLED', false)` (Phase 1 plan 01-06 set this; verify). Update 'ssr.url' default from 'http://127.0.0.1:13714' to 'http://ssr:13714' so the web service finds the ssr service via docker service-name DNS:
       ```php
       'ssr' => [
           'enabled' => (bool) env('INERTIA_SSR_ENABLED', false),
           'url' => env('INERTIA_SSR_URL', 'http://ssr:13714'),
           'ensure_bundle_exists' => false,  // Phase 1 plan 01-06 set this to false; preserve
       ],
       ```

    3. Append to apps/web/.env.example:
       ```
       # Inertia v2 SSR (Phase 7 plan 07-11 — Open Question 7 LOCKED)
       INERTIA_SSR_ENABLED=false
       INERTIA_SSR_URL=http://ssr:13714
       ```

    4. Edit apps/web/.env.testing in-place — APPEND `INERTIA_SSR_ENABLED=false` so the test environment never tries to reach the ssr port (which may not be running during make pest). Phase 1 plan 01-05 established .env.testing as committed (test keys are NOT secrets).

    5. Verify the ssr.ts entry file exists at apps/web/resources/js/ssr.ts (Phase 1 plan 01-06). If it does, no changes. If broken / outdated, refresh per Inertia v2 SSR docs (createInertiaApp + Vue.createSSRApp + ZiggyVue). Surface in SUMMARY if refresh was needed.

    Run `docker compose config --quiet` to validate docker-compose.yml syntax.
  </action>
  <verify>
    <automated>docker compose config --quiet 2>&1 | head -5 && docker compose config 2>&1 | grep -E "^\s+ssr:" && docker compose exec web grep -c "INERTIA_SSR_URL" /app/.env.example && docker compose exec web php -r "echo config('inertia.ssr.url');" 2>&1 | grep "ssr:13714"</automated>
  </verify>
  <done>docker-compose.yml has 6 services; ssr service config valid; .env.example documents both env vars; .env.testing overrides to false; inertia.php url default updated.</done>
</task>

<task type="auto">
  <name>Task 2: Replace 07-01 SsrBundleExistsTest RED stub with GREEN + author new SsrLocaleHonouredTest</name>
  <files>apps/web/tests/Feature/Ssr/SsrBundleExistsTest.php, apps/web/tests/Feature/Ssr/SsrLocaleHonouredTest.php</files>
  <action>
    1. Replace apps/web/tests/Feature/Ssr/SsrBundleExistsTest.php (was 07-01 RED stub) with bare functional Pest. 4+ it() blocks:
       - `it('SSR config honours INERTIA_SSR_ENABLED env var', ...)` — config(['inertia.ssr.enabled' => true]); expect(config('inertia.ssr.enabled'))->toBe(true). Then config(['inertia.ssr.enabled' => false]); expect false.
       - `it('SSR url defaults to http://ssr:13714 (docker service name)', ...)` — config(['inertia.ssr.url' => null]); reset via app('config'); expect config('inertia.ssr.url') === 'http://ssr:13714' OR a configurable URL pulled from env (test inspects the default closure behaviour).
       - `it('SSR bundle file exists at bootstrap/ssr/ssr.mjs after pnpm build', ...)` — File::exists(base_path('bootstrap/ssr/ssr.mjs')) — fileMayNotBeBuilt in test env so use ->skip() with explicit reason "Test passes when pnpm build has run; CI gate runs build before pest"; OR run `pnpm build --ssr` inline during the test setUp (heavier but deterministic).
       - `it('ssr service registered in docker-compose.yml', ...)` — parse docker-compose.yml via Symfony\Component\Yaml\Yaml::parseFile(base_path('../docker-compose.yml')); expect data['services']['ssr'] not null; expect command contains 'inertia:start-ssr'.

    2. Author apps/web/tests/Feature/Ssr/SsrLocaleHonouredTest.php — 3+ it() blocks (Pitfall 8 mitigation):
       - `it('renders <html lang="en"> for default Accept-Language', ...)` — get('/'); response body contains 'lang="en"'.
       - `it('renders <html lang="cs"> when Accept-Language: cs is set', ...)` — withHeaders(['Accept-Language' => 'cs'])->get('/'); response body contains 'lang="cs"'. NOTE: requires LocaleMiddleware to actually resolve 'cs' as available locale per Phase 1 plan 01-08 config('i18n.available_locales'); if 'cs' is not in available_locales (D-013 — EN only at launch), this test should assert that an unavailable locale falls back to en, OR temporarily extend config('i18n.available_locales') in the test via Config::set.
       - `it('renders <html lang> matching cookie-set locale', ...)` — withCookie('locale', 'en')->get('/'); response body contains 'lang="en"'.

    Run filtered pest. Assert all GREEN.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter='SsrBundleExistsTest|SsrLocaleHonouredTest' --no-coverage 2>&1 | tail -10 | grep -E "(passed|PASS)"</automated>
  </verify>
  <done>2 GREEN test files; SsrBundleExistsTest replaces 07-01 RED with 4+ blocks; SsrLocaleHonouredTest proves Pitfall 8 mitigation with 3+ blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| web service (PHP) → ssr service (Node) | Internal docker network; Inertia sends serialised page data over HTTP to port 13714; no DB access from Node process (defence-in-depth) |
| ssr service → public internet | Explicitly forbidden — `ports: []` in docker-compose; no exposure to host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-01 | Tampering | SSR bundle out of sync with client bundle (Pitfall 5) | mitigate | Deploy pipeline calls `php artisan inertia:stop-ssr` BEFORE swapping bundles; both `vite build` + `vite build --ssr` run in deploy (apps/web/vite.config.ts ssr: 'resources/js/ssr.ts' already configured — Phase 1 plan 01-06); docker healthcheck restarts ssr service if SSR throws "page not found" |
| T-07-11-02 | Information Disclosure | SSR Node process leaking server-side secrets through prop serialization | mitigate | Inertia shared props (HandleInertiaRequests) explicitly enumerate exposed keys (translations, auth, locale, theme); no global state exposure; T-07-11-04 ensures Node process has no DB credentials |
| T-07-11-03 | Tampering | Locale drift — SSR HTML lang != active app locale (Pitfall 8) | mitigate | LocaleMiddleware runs BEFORE Inertia middleware (Phase 1 plan 01-08 verified); app.blade.php uses `{{ app()->getLocale() }}` so the SSR-rendered HTML carries the resolved locale; SsrLocaleHonouredTest exercises Accept-Language + cookie paths |
| T-07-11-04 | Information Disclosure | SSR Node process accessing Postgres directly | mitigate | Per security_constraints in <planning_context>: "SSR Node sidecar security (no DB access, only HTTP to Laravel)" — ssr service in docker-compose has no DATABASE_URL env var; Node process can only render Inertia-supplied props, never query DB; ports: [] keeps it internal |
| T-07-11-05 | Denial of Service | SSR Node process crash under load | mitigate | docker-compose healthcheck on /health endpoint; Railway restart policy applies in prod; Inertia falls back to client-only render if SSR is unreachable (graceful degradation) |
| T-07-11-06 | Tampering | INERTIA_SSR_ENABLED env override flipped on in dev unintentionally | accept | .env.testing explicitly sets INERTIA_SSR_ENABLED=false; dev .env file (not committed) is operator-controlled |
| T-07-11-07 | Information Disclosure | ssr service hostname leaking via http://ssr:13714 in error messages | accept | Service name `ssr` is internal-only; not exposed in user-facing error pages; only visible in Laravel logs which are operator-controlled per D-014 |
</threat_model>

<verification>
make pest --filter='SsrBundleExistsTest|SsrLocaleHonouredTest' GREEN.
`docker compose config` shows 6 services (web, worker, bot, postgres, redis, ssr).
`config('inertia.ssr.url')` resolves to 'http://ssr:13714' in container.
`.env.example` documents both new env vars.
SsrLocaleHonouredTest proves <html lang> reflects the active locale across Accept-Language + cookie paths.
</verification>

<success_criteria>
SSR service registered in docker-compose with proper isolation (ports: [], internal-only). Open Question 7 LOCKED inline (split service, env-driven enable). 2 GREEN test files. Pitfall 8 (locale drift) mitigation proven via test.
</success_criteria>

<output>
Create `.planning/phases/07-cms/07-11-SUMMARY.md` documenting:
- Whether config/inertia.php needed updates or Phase 1 scaffolding was sufficient
- Whether ssr.ts needed refresh
- LocaleMiddleware order verified in bootstrap/app.php (Pitfall 8 chain)
- Decision: split ssr service vs co-host on worker (RESEARCH Assumption A5 — split chosen for prod parity)
- Open Question 7 LOCKED inline reference
</output>
</content>
</invoke>