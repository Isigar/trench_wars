---
phase: 07-cms
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/composer.json
  - apps/web/composer.lock
  - apps/web/package.json
  - pnpm-lock.yaml
  - apps/web/config/media-library.php
  - apps/web/config/filament-tiptap-editor.php
  - apps/web/.env.example
  - apps/web/database/factories/ArticleFactory.php
  - apps/web/database/factories/CategoryFactory.php
  - apps/web/lang/en/cms.php
  - apps/web/lang/en/events.php
  - apps/web/lang/en/search.php
  - apps/web/lang/en/admin.php
  - apps/web/tests/Feature/Articles/ArticleResourcePresentTest.php
  - apps/web/tests/Feature/Articles/ArticlePublishWorkflowTest.php
  - apps/web/tests/Feature/Articles/ArticleIndexPageTest.php
  - apps/web/tests/Feature/Articles/ArticleShowPageTest.php
  - apps/web/tests/Feature/Articles/ArticleHeadMetaTest.php
  - apps/web/tests/Feature/Articles/FtsBackfillTest.php
  - apps/web/tests/Feature/Events/EventsCalendarPageTest.php
  - apps/web/tests/Feature/Events/EventsFeedJsonControllerTest.php
  - apps/web/tests/Feature/Search/SearchServiceTest.php
  - apps/web/tests/Feature/Search/SearchControllerTest.php
  - apps/web/tests/Feature/Sitemap/SitemapGenerateCommandTest.php
  - apps/web/tests/Feature/I18n/CmsI18nKeyCoverageTest.php
  - apps/web/tests/Feature/Observers/ArticleObserverTest.php
  - apps/web/tests/Feature/Outbound/ArticleAnnounceOutboundTest.php
  - apps/web/tests/Feature/Ssr/SsrBundleExistsTest.php
  - apps/web/tests/Feature/Admin/ArticleAuditLogTest.php
  - apps/web/tests/Unit/Data/PublicArticleDataTest.php
autonomous: true
requirements:
  - REQ-goal-cms
  - REQ-success-public-browse
must_haves:
  truths:
    - "4 new composer deps installed in apps/web: spatie/laravel-medialibrary:^11.22, awcodes/filament-tiptap-editor:^3.5, ueberdosis/tiptap-php:^2.1, spatie/laravel-sitemap:^8.1 (via container composer per D-021)"
    - "5 new npm deps under apps/web/package.json devDependencies: @fullcalendar/core@^6.1.20, @fullcalendar/vue3@^6.1.20, @fullcalendar/daygrid@^6.1.20, @fullcalendar/timegrid@^6.1.20, @fullcalendar/interaction@^6.1.20 (pnpm-lock.yaml at repo root regenerated)"
    - "config/media-library.php published with default disk='public', max_file_size set; config/filament-tiptap-editor.php published (or stub created) with the 'default' profile pinned to a safe-node allowlist excluding iframe/script/oembed/youtube (Pitfall 10 mitigation)"
    - "apps/web/lang/en/cms.php pre-shipped with full Phase 7 namespace: status.{draft,scheduled,published}.{label,badge_class}; actions.{publish_now,schedule,unpublish}.{label,modal_heading,success}; fields.{title,slug,excerpt,body,hero,category_id,scheduled_at,published_at,allow_discord_announce}.{label,help}; errors.{slug_taken,invalid_status_transition,scheduled_at_in_past,tiptap_unsafe_node}; empty.{articles,categories,search,events}.label"
    - "apps/web/lang/en/events.php pre-shipped with calendar UI: header.{title,today,month,week,day}; types.{match,tournament,article}.label; empty.label"
    - "apps/web/lang/en/search.php pre-shipped: placeholder.label; results.{none,heading,total_count}; sections.{articles,clans,players}.label"
    - "apps/web/lang/en/admin.php extended with admin.article.{label,plural_label,fields.*,publication}; admin.category.{label,plural_label,fields.*} (Phase 4 admin.php extension idiom)"
    - "2 factory stubs created: ArticleFactory + CategoryFactory using string FQN $model + per-line @phpstan-ignore-next-line on missingType.generics + property.defaultValue (Phase 6 D-06-01-A idiom verbatim — implementation lands in plan 07-03)"
    - "15 Pest RED test stub files created using bare functional Pest convention (no namespace, no per-file uses() — Phase 6 D-06-01-B idiom verbatim); every stub holds at least one it('placeholder') block that asserts true → false to fail"
    - ".env.example documents INERTIA_SSR_ENABLED=false (defaults safe; flipped to true in production only)"
    - "make pint --test exits 0 across all newly authored files"
    - "make phpstan reports zero NEW errors against the 2 factory stubs"
    - "Pre-check: php -m | grep -iE 'gd|imagick' inside the web container returns at least one of the two (medialibrary dependency — Environment Availability check from RESEARCH.md). storage:link verified via 'ls -la apps/web/public/storage' — symlink target apps/web/storage/app/public exists"
  artifacts:
    - path: "apps/web/composer.json"
      provides: "4 new Phase 7 composer deps pinned (medialibrary ^11.22, filament-tiptap-editor ^3.5, tiptap-php ^2.1, laravel-sitemap ^8.1)"
      contains: "spatie/laravel-medialibrary"
    - path: "apps/web/package.json"
      provides: "5 new @fullcalendar/* npm deps under devDependencies at ^6.1.20"
      contains: "@fullcalendar/vue3"
    - path: "apps/web/config/filament-tiptap-editor.php"
      provides: "Pinned safe-node profile (Pitfall 10 mitigation) — iframe/script/oembed/youtube explicitly excluded"
      contains: "default"
    - path: "apps/web/database/factories/ArticleFactory.php"
      provides: "Factory stub — string FQN \\App\\Models\\Article + PHPStan ignore comments; implementation in plan 07-03"
      contains: "ArticleFactory"
    - path: "apps/web/lang/en/cms.php"
      provides: "Phase 7 CMS i18n namespace (status / actions / fields / errors / empty)"
      contains: "status"
    - path: "apps/web/tests/Feature/I18n/CmsI18nKeyCoverageTest.php"
      provides: "Pitfall 10 (i18n key coverage) Phase 7 mitigation — RED stub mirroring Phase 6 TournamentI18nKeyCoverageTest"
      contains: "placeholder"
    - path: "apps/web/tests/Feature/Ssr/SsrBundleExistsTest.php"
      provides: "RED stub asserting bootstrap/ssr/ssr.mjs exists + INERTIA_SSR_ENABLED config honoured"
      contains: "placeholder"
  key_links:
    - from: "Wave 0 factory stubs"
      to: "Plan 07-03 (Article + Category models)"
      via: "Factories filled with real definition() bodies when models exist"
      pattern: "ArticleFactory"
    - from: "Wave 0 Pest RED stubs"
      to: "Plans 07-02..07-12"
      via: "Each implementation plan replaces the corresponding RED stub with GREEN assertions"
      pattern: "placeholder"
    - from: "apps/web/lang/en/cms.php"
      to: "NoHardcodedStringsTest + CmsI18nKeyCoverageTest"
      via: "All Phase 7 t()/__() calls in Vue + PHP MUST resolve to keys defined here"
      pattern: "cms\\."
    - from: "config/filament-tiptap-editor.php profile pinning"
      to: "Plan 07-05 ArticleResource form schema"
      via: "TiptapEditor field references the pinned 'default' profile from this config"
      pattern: "TiptapEditor"
---

<objective>
Wave 0 scaffolding for Phase 7 (CMS): install 4 composer + 5 npm dependencies, pin the Tiptap profile to safe nodes (Pitfall 10 mitigation), ship 2 factory stubs + 15 Pest RED stubs + 4 i18n namespaces (cms, events, search) + admin.php extension. Verify medialibrary OS prerequisites (gd or imagick, storage:link).

Purpose: Plans 07-02..07-12 each cite "replace Wave 0 RED stub" in their acceptance criteria — without this scaffolding wave, those plans have nowhere to land GREEN tests. The i18n namespaces are pre-shipped so every Phase 7 UI string (status labels, action buttons, field labels, calendar headers, search placeholders, sitemap copy) wraps in `__()` / `t()` from day one without CI failures from `NoHardcodedStringsTest` (D-013 + Phase 6 D-06-01-C precedent). Tiptap profile pinning is the day-zero defence against Pitfall 10 (stored XSS via author-inserted iframe/script).

Output: 4 composer + 5 npm deps locked, 2 factory stubs, 15 RED Pest stubs, 3 new lang/en/*.php files, admin.php amendment, .env.example INERTIA_SSR_ENABLED documented, environment availability verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-cms/07-CONTEXT.md
@.planning/phases/07-cms/07-RESEARCH.md
@.planning/phases/07-cms/07-VALIDATION.md
@.planning/phases/06-tournaments-brackets/06-PHASE-VERIFICATION.md
@CLAUDE.md
@apps/web/database/seeders/PermissionSeeder.php

<interfaces>
<!-- Phase 6 D-06-01-A factory stub idiom (verbatim — DO NOT improvise) -->

```php
// apps/web/database/factories/ArticleFactory.php
<?php
declare(strict_types=1);
namespace Database\Factories;
use Illuminate\Database\Eloquent\Factories\Factory;

/**
 * @phpstan-ignore-next-line missingType.generics
 */
class ArticleFactory extends Factory
{
    /** @var string */
    protected $model = 'App\\Models\\Article'; // @phpstan-ignore-line property.defaultValue
    public function definition(): array { return []; }
}
```

Same shape for CategoryFactory ($model = 'App\\Models\\Category').

<!-- Phase 6 D-06-01-B Pest stub idiom (verbatim — bare functional, no namespace, no uses()) -->

```php
// apps/web/tests/Feature/Articles/ArticleResourcePresentTest.php
<?php
declare(strict_types=1);

it('placeholder — landing in plan 07-05', function () {
    expect(true)->toBe(false); // RED until plan 07-05 lands ArticleResource
});
```

<!-- Tiptap profile config pinning (Pitfall 10) — explicit allowlist -->

Reference config keys (from awcodes/filament-tiptap-editor 3.x docs):
- `profiles.default.toolbar`: ['heading','bold','italic','bullet-list','ordered-list','link','code','blockquote','media','horizontal-rule','table']
- `profiles.default.tools`: SAME list — explicitly OMIT 'oembed', 'youtube', 'video' (iframe-bearing nodes)
- `extensions`: include 'Image' (media upload), exclude 'Iframe' (Phase 7 Open Question 8 → out of scope v1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify container prereqs, install composer + npm deps, publish configs, pin Tiptap safe-node profile</name>
  <files>apps/web/composer.json, apps/web/composer.lock, apps/web/package.json, pnpm-lock.yaml, apps/web/config/media-library.php, apps/web/config/filament-tiptap-editor.php, apps/web/.env.example</files>
  <action>
    1. Verify OS prereqs in the web container (RESEARCH Environment Availability):
       - `docker compose exec web php -m | grep -iE "gd|imagick"` — at least one must be present. If neither, STOP and surface to operator (medialibrary cannot install). Note in SUMMARY which is present.
       - `docker compose exec web ls -la /app/public/storage` — verify symlink exists. If absent, run `docker compose exec web php artisan storage:link` and re-verify.
    2. Install 4 composer packages via container composer (D-021): `docker compose exec web composer require spatie/laravel-medialibrary:^11.22 awcodes/filament-tiptap-editor:^3.5 ueberdosis/tiptap-php:^2.1 spatie/laravel-sitemap:^8.1`. Verify packagist returns the pinned major.minor (HIGH-confidence versions per RESEARCH).
    3. Publish required vendor configs INSIDE the container:
       - `docker compose exec web php artisan vendor:publish --provider="Spatie\\MediaLibrary\\MediaLibraryServiceProvider" --tag="medialibrary-migrations"` — produces the medialibrary migration. DO NOT migrate yet (plan 07-02 owns migrations).
       - `docker compose exec web php artisan vendor:publish --provider="Spatie\\MediaLibrary\\MediaLibraryServiceProvider" --tag="medialibrary-config"` — produces config/media-library.php. Verify default disk='public', max_file_size set.
       - `docker compose exec web php artisan vendor:publish --tag="filament-tiptap-editor-config"` — produces config/filament-tiptap-editor.php. If the package does not publish a config file (verify after publish), create one at config/filament-tiptap-editor.php containing the safe-node profile keys shown in <interfaces>.
    4. **Pin the Tiptap 'default' profile (Pitfall 10 — Open Question 8 RESOLVED: markdown v2 out of scope):** Edit config/filament-tiptap-editor.php so the 'default' profile's tools array explicitly EXCLUDES `oembed`, `youtube`, `video` (anything iframe-bearing). Allowlist: heading, bold, italic, bullet-list, ordered-list, link, code, blockquote, media (image upload), horizontal-rule, table. Comment block at top of file MUST reference Pitfall 10 + Open Question 8 LOCKED.
    5. Install 5 npm packages at repo root (so pnpm workspace lockfile updates correctly): `docker compose exec web pnpm add -D '@fullcalendar/core@^6.1.20' '@fullcalendar/vue3@^6.1.20' '@fullcalendar/daygrid@^6.1.20' '@fullcalendar/timegrid@^6.1.20' '@fullcalendar/interaction@^6.1.20'`. NOTE: markdown-it is INTENTIONALLY NOT INSTALLED in v1 — Open Question 8 LOCKED inline: out of scope v1; tiptap_converter()->asHTML is the sole render path.
    6. Append `INERTIA_SSR_ENABLED=false` to apps/web/.env.example with a comment: "# Flipped to true in production (Railway env group); plans 07-09..07-11 wire the prod ssr service".
  </action>
  <verify>
    <automated>docker compose exec web composer show spatie/laravel-medialibrary awcodes/filament-tiptap-editor ueberdosis/tiptap-php spatie/laravel-sitemap | grep -E "versions" && docker compose exec web grep -E "@fullcalendar/(core|vue3|daygrid|timegrid|interaction)" /app/package.json | wc -l | grep -q 5 && docker compose exec web grep -c "oembed\|youtube" /app/config/filament-tiptap-editor.php | grep -E "^[0-9]+$"</automated>
  </verify>
  <done>4 composer + 5 npm deps installed at pinned versions; config/media-library.php + config/filament-tiptap-editor.php published with Tiptap default profile pinned to safe-node allowlist (no iframe/script/oembed/youtube); INERTIA_SSR_ENABLED documented in .env.example.</done>
</task>

<task type="auto">
  <name>Task 2: Ship 2 factory stubs + 15 Pest RED stubs + 3 i18n namespaces + admin.php extension</name>
  <files>apps/web/database/factories/ArticleFactory.php, apps/web/database/factories/CategoryFactory.php, apps/web/lang/en/cms.php, apps/web/lang/en/events.php, apps/web/lang/en/search.php, apps/web/lang/en/admin.php, apps/web/tests/Feature/Articles/ArticleResourcePresentTest.php, apps/web/tests/Feature/Articles/ArticlePublishWorkflowTest.php, apps/web/tests/Feature/Articles/ArticleIndexPageTest.php, apps/web/tests/Feature/Articles/ArticleShowPageTest.php, apps/web/tests/Feature/Articles/ArticleHeadMetaTest.php, apps/web/tests/Feature/Articles/FtsBackfillTest.php, apps/web/tests/Feature/Events/EventsCalendarPageTest.php, apps/web/tests/Feature/Events/EventsFeedJsonControllerTest.php, apps/web/tests/Feature/Search/SearchServiceTest.php, apps/web/tests/Feature/Search/SearchControllerTest.php, apps/web/tests/Feature/Sitemap/SitemapGenerateCommandTest.php, apps/web/tests/Feature/I18n/CmsI18nKeyCoverageTest.php, apps/web/tests/Feature/Observers/ArticleObserverTest.php, apps/web/tests/Feature/Outbound/ArticleAnnounceOutboundTest.php, apps/web/tests/Feature/Ssr/SsrBundleExistsTest.php, apps/web/tests/Feature/Admin/ArticleAuditLogTest.php, apps/web/tests/Unit/Data/PublicArticleDataTest.php</files>
  <action>
    1. Write 2 factory stubs verbatim per <interfaces> idiom (Phase 6 D-06-01-A): ArticleFactory + CategoryFactory. Each file: declare(strict_types=1), namespace Database\Factories, extends Factory, string FQN `$model = 'App\\Models\\Article'` / `'App\\Models\\Category'`, two per-line `@phpstan-ignore-next-line` comments (missingType.generics on class doc + property.defaultValue on $model), definition() returns [].
    2. Write apps/web/lang/en/cms.php with the full Phase 7 namespace (~50 leaf keys covering status × 3 × 2 = 6, actions × 3 × 3 = 9, fields × 9 × 2 = 18, errors × 4, empty × 4). PHP array<string,mixed> return; declare(strict_types=1) at top.
    3. Write apps/web/lang/en/events.php (~15 keys: header.{title,today,month,week,day}, types.{match,tournament,article}.label, empty.label, page.{title,description}).
    4. Write apps/web/lang/en/search.php (~10 keys: placeholder.label, results.{none,heading,total_count}, sections.{articles,clans,players}.label, page.title).
    5. Extend apps/web/lang/en/admin.php in-place (additive — preserve existing keys verbatim; this is the Phase 4 idiom): append `'article' => [...]` block with label, plural_label, fields, publication, status sub-keys; append `'category' => [...]` block.
    6. Write 15 Pest RED stub files verbatim per <interfaces> bare functional idiom (Phase 6 D-06-01-B): declare(strict_types=1) at top, NO namespace, NO uses() call (Pest.php autowires TestCase + RefreshDatabase via uses(...)->in('Feature')). Each file has at least one it('placeholder — landing in plan 07-XX') asserting `expect(true)->toBe(false)`. The 15 files map to:
       - tests/Feature/Articles/ArticleResourcePresentTest.php (plan 07-05)
       - tests/Feature/Articles/ArticlePublishWorkflowTest.php (plan 07-07)
       - tests/Feature/Articles/ArticleIndexPageTest.php (plan 07-09)
       - tests/Feature/Articles/ArticleShowPageTest.php (plan 07-10)
       - tests/Feature/Articles/ArticleHeadMetaTest.php (plan 07-12)
       - tests/Feature/Articles/FtsBackfillTest.php (plan 07-02)
       - tests/Feature/Events/EventsCalendarPageTest.php (plan 07-10)
       - tests/Feature/Events/EventsFeedJsonControllerTest.php (plan 07-09)
       - tests/Feature/Search/SearchServiceTest.php (plan 07-08)
       - tests/Feature/Search/SearchControllerTest.php (plan 07-09)
       - tests/Feature/Sitemap/SitemapGenerateCommandTest.php (plan 07-12)
       - tests/Feature/I18n/CmsI18nKeyCoverageTest.php (plan 07-12)
       - tests/Feature/Observers/ArticleObserverTest.php (plan 07-06)
       - tests/Feature/Outbound/ArticleAnnounceOutboundTest.php (plan 07-06)
       - tests/Feature/Ssr/SsrBundleExistsTest.php (plan 07-11)
       - tests/Feature/Admin/ArticleAuditLogTest.php (plan 07-12)
       - tests/Unit/Data/PublicArticleDataTest.php (plan 07-03/07-05)
       (Total = 17 stubs — the count of 15 in CONTEXT was a draft; this plan ships every RED stub referenced in 07-VALIDATION.md and RESEARCH.md Wave 0 Gaps list)
    7. Run `make pint --test` to confirm style compliance on every new file. Run `make phpstan` to confirm zero new errors on factories.
  </action>
  <verify>
    <automated>docker compose exec web ls apps/web/database/factories/ArticleFactory.php apps/web/database/factories/CategoryFactory.php apps/web/lang/en/cms.php apps/web/lang/en/events.php apps/web/lang/en/search.php && find apps/web/tests -name "*Test.php" -newer apps/web/composer.json | wc -l | grep -E "^[0-9]+$" && docker compose exec web ./vendor/bin/pint --test apps/web/database/factories/ apps/web/lang/en/ apps/web/tests/ && docker compose exec web ./vendor/bin/phpstan analyse apps/web/database/factories/ArticleFactory.php apps/web/database/factories/CategoryFactory.php --no-progress --memory-limit=2G 2>&1 | grep -E "\[OK\] No errors"</automated>
  </verify>
  <done>2 factory stubs + 17 Pest RED stubs + 3 new lang/en/*.php files + admin.php extension all present; pint --test exits 0; phpstan reports 0 new errors on factories; every RED stub explicitly references its target plan in the placeholder description.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → web container | composer/pnpm/php commands cross from host shell into container (D-021); secret leakage if .env values cross |
| package registry → web container | composer + pnpm pull from packagist + npm registry; supply-chain risk in newly added deps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | filament-tiptap-editor profile (Pitfall 10 — stored XSS via author iframe/script) | mitigate | Pin 'default' profile tools allowlist explicitly in config/filament-tiptap-editor.php; exclude oembed/youtube/video iframe-bearing nodes; profile config committed and audited |
| T-07-01-02 | Tampering | Supply chain — 4 new composer + 5 new npm packages | accept | All 4 PHP packages are first-party Spatie / Filament-community (HIGH source reputation per RESEARCH); npm @fullcalendar/* is MIT and widely vetted; pin to exact ^major.minor (^11.22, ^3.5, ^2.1, ^8.1, ^6.1.20); composer.lock + pnpm-lock.yaml capture the hash chain |
| T-07-01-03 | Information Disclosure | .env.example introducing new INERTIA_SSR_ENABLED variable | accept | INERTIA_SSR_ENABLED is a boolean (false default in dev, true in prod); not a secret; .env.example commits empty/default shape per CLAUDE.md §6 |
| T-07-01-04 | Tampering | medialibrary default disk configuration | mitigate | Default disk='public' (Laravel public disk) — already gitignored at apps/web/storage/app/public; storage:link verified in task 1 ensures symlink to public/storage is in place; no remote-disk credentials introduced |
| T-07-01-05 | Denial of Service | Image bomb via medialibrary upload | mitigate | config/media-library.php max_file_size defaults; Filament form layer adds ->maxSize(5120) + ->acceptedFileTypes(['image/jpeg','image/png','image/webp']) (plan 07-05 wires this) |
</threat_model>

<verification>
All four composer packages resolvable: `docker compose exec web composer show <package>`.
All five npm packages installed: `grep '@fullcalendar' apps/web/package.json | wc -l == 5`.
Tiptap profile pins safe nodes: `grep -E "(oembed|youtube|video)" config/filament-tiptap-editor.php` returns no permitted entries (only commented-out or excluded references).
RED stubs fail predictably: `docker compose exec web ./vendor/bin/pest --filter="placeholder" --no-coverage` reports 17 failures (or skipped/RED state per Pest convention) — the suite must not crash.
make pint --test exits 0. make phpstan reports zero new errors on the 2 factories.
</verification>

<success_criteria>
All must_haves.truths verified. Composer + npm deps locked at HIGH-confidence versions from RESEARCH.md. Tiptap profile pinned (Pitfall 10 mitigation in place from day 0). 17 RED stubs + 3 new i18n namespaces + admin.php extension shipped. Environment prereqs (gd/imagick + storage:link) confirmed in SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cms/07-01-SUMMARY.md` recording:
- Verified PHP extension (gd vs imagick) and storage:link state
- Final composer.lock + pnpm-lock.yaml line counts (delta tracking)
- Tiptap profile config exact toolbar/tools array (Pitfall 10 audit trail)
- The 17 RED stub file paths and their target plan numbers
- Any deviations from <interfaces> idioms (Rule 1/2/3 deviations per Phase 5/6 SUMMARY conventions)
</output>
</content>
</invoke>