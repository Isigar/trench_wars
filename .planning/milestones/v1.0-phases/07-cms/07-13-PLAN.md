---
phase: 07-cms
plan: 13
type: execute
wave: 8
depends_on: ["07-02", "07-03", "07-04", "07-05", "07-06", "07-07", "07-08", "07-09", "07-10", "07-11", "07-12"]
files_modified:
  - .planning/phases/07-cms/07-PHASE-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements:
  - REQ-goal-cms
  - REQ-success-public-browse
must_haves:
  truths:
    - "All 7 quality gates GREEN: web Pest full suite (≥ 866 + ~100 Phase 7 = ~960+ tests passing); bot Vitest (139 passing — no Phase 7 bot work); Pint (clean); PHPStan L8 [OK]; vue-tsc 0 errors; bot tsc strict clean; shared-types tsc clean — Phase 6 precedent for 7-gate verification"
    - "07-PHASE-VERIFICATION.md authored at .planning/phases/07-cms/07-PHASE-VERIFICATION.md mapping SC-1..SC-5 + REQ-goal-cms + REQ-success-public-browse to concrete test files + source artifacts (Phase 6 06-PHASE-VERIFICATION.md format verbatim); includes 12 Pitfall coverage matrix, 8 Open Question resolution log (1-8 — all LOCKED inline), 10 Assumption status table, D-07-XX-NN canonical bindings table (~25-40 decisions captured across plans 07-01..07-12)"
    - "ROADMAP.md updated: Phase 7 entry flips from 0/TBD → 13/13 Complete; Completed date 2026-05-15; all 13 plan rows flipped from [ ] to [x] with their final 1-line objectives (replacing the Phase 2 placeholder list from the orchestrator template); ROADMAP table at bottom updates Phase 7 status to Complete"
    - "REQUIREMENTS.md: REQ-goal-cms Pending → Complete; REQ-success-public-browse Pending → Complete; traceability table updates the 2 Phase 7 rows"
    - "STATE.md: completed_phases 6 → 7; completed_plans 82 → 95 (13 new plans); percent 67 → 78; status_line updated to Phase 7 COMPLETE PENDING_MANUAL_SMOKE; Accumulated Decisions section appended with all D-07-* canonical bindings"
    - "Status flag PENDING_MANUAL_SMOKE for the 4-item operator walkthrough below (full Filament editor flow, calendar UX month/week/day toggles, search ranking matches expectations, sitemap.xml accessible + valid + Discord announce on publish live smoke)"
  artifacts:
    - path: ".planning/phases/07-cms/07-PHASE-VERIFICATION.md"
      provides: "Authoritative Phase 7 close artifact (Phase 6 06-PHASE-VERIFICATION.md format)"
      contains: "REQ-goal-cms"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 flipped 13/13 Complete + dated"
      contains: "Phase 7: CMS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "REQ-goal-cms + REQ-success-public-browse → Complete in traceability table"
      contains: "Phase 7 | Complete"
    - path: ".planning/STATE.md"
      provides: "completed_phases 7; percent 78; Accumulated Decisions appended"
      contains: "Phase 07 — CMS"
  key_links:
    - from: "07-PHASE-VERIFICATION.md"
      to: "Phase 8 (RCON automation) — hand-off note"
      via: "RCON Phase 8 will surface MatchResult events that potentially carry through the same Phase 7 calendar feed (eventable_type=GameMatch already covered); article_announce CHECK constraint precedent reusable for rcon_result_announce in Phase 8"
      pattern: "Phase 8"
    - from: "manual_smoke_required (4 items)"
      to: "Operator out-of-band verification"
      via: "Editor flow + calendar UX + search ranking + sitemap/Discord live smoke against Railway prod"
      pattern: "PENDING_MANUAL_SMOKE"
---

<objective>
Wave 8 [BLOCKING]: Phase 7 close — run all 7 quality gates; author 07-PHASE-VERIFICATION.md mapping SC-1..SC-5 + 2 requirements + 12 pitfalls + 8 open questions + assumptions + D-07-* canonical bindings; update ROADMAP + REQUIREMENTS + STATE atomically; flag 4 operator manual smoke items.

Purpose: Phase 6 plan 06-14 established the canonical close pattern. This plan ships the verification artifact + tri-file update so the orchestrator can advance to Phase 8 cleanly. PENDING_MANUAL_SMOKE flag indicates 4 operator items remain (editor flow / calendar UX / search ranking / live Discord smoke) that the automated gates intentionally don't cover.

Output: 1 verification report + 3 surgical edits (ROADMAP / REQUIREMENTS / STATE).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-tournaments-brackets/06-PHASE-VERIFICATION.md
@.planning/phases/07-cms/07-CONTEXT.md
@.planning/phases/07-cms/07-RESEARCH.md
@.planning/phases/07-cms/07-VALIDATION.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-cms/07-01-SUMMARY.md
@.planning/phases/07-cms/07-02-SUMMARY.md
@.planning/phases/07-cms/07-03-SUMMARY.md
@.planning/phases/07-cms/07-04-SUMMARY.md
@.planning/phases/07-cms/07-05-SUMMARY.md
@.planning/phases/07-cms/07-06-SUMMARY.md
@.planning/phases/07-cms/07-07-SUMMARY.md
@.planning/phases/07-cms/07-08-SUMMARY.md
@.planning/phases/07-cms/07-09-SUMMARY.md
@.planning/phases/07-cms/07-10-SUMMARY.md
@.planning/phases/07-cms/07-11-SUMMARY.md
@.planning/phases/07-cms/07-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all 7 quality gates and capture counts</name>
  <files></files>
  <action>
    Run all 7 gates inside the container per CLAUDE.md §1 + D-021. Capture exact output (counts, durations, [OK] markers) for the verification artifact:

    1. `docker compose exec web ./vendor/bin/pest --no-coverage` — Pest web full suite; expect ≥ 866 + ~100 Phase 7 = ~960+ passing.
    2. `docker compose run --rm --no-deps -v $PWD:/repo bot sh -c "cd /repo/apps/bot && pnpm test"` — bot Vitest; expect 139 passing (no Phase 7 bot work; should be regressionless from Phase 6 close).
    3. `docker compose exec web ./vendor/bin/pint --test` — expect "PASS — N files clean" (N likely ≥ 450).
    4. `docker compose exec web ./vendor/bin/phpstan analyse --no-progress --memory-limit=2G` — expect "[OK] No errors".
    5. `docker compose run --rm --no-deps -v $PWD:/repo bot sh -c "cd /repo/apps/bot && pnpm run typecheck"` — expect "PASS".
    6. `corepack pnpm --filter @trenchwars/shared-types run typecheck` (host) — expect "PASS".
    7. `docker compose exec web /app/node_modules/.bin/vue-tsc --noEmit` — expect 0 errors.

    If ANY gate fails, STOP and surface the specific failure to the operator for plan-bounce. Do NOT proceed to verification artifact authoring with red gates.

    Record counts + durations in a temporary scratch buffer for task 2.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --no-coverage 2>&1 | tail -3 | grep -E "passed.*assertions" && docker compose exec web ./vendor/bin/pint --test 2>&1 | tail -3 | grep -E "PASS" && docker compose exec web ./vendor/bin/phpstan analyse --no-progress --memory-limit=2G 2>&1 | grep "\[OK\] No errors" && docker compose run --rm --no-deps -v $PWD:/repo bot sh -c "cd /repo/apps/bot && pnpm test" 2>&1 | tail -3 | grep -E "passed"</automated>
  </verify>
  <done>All 7 gates GREEN with captured counts; if any RED, plan halts.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 2: Author 07-PHASE-VERIFICATION.md + update ROADMAP.md + REQUIREMENTS.md + STATE.md</name>
  <files>.planning/phases/07-cms/07-PHASE-VERIFICATION.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    1. Author .planning/phases/07-cms/07-PHASE-VERIFICATION.md following the Phase 6 06-PHASE-VERIFICATION.md template VERBATIM. Sections:
       - **Frontmatter**: phase=07-cms, slug=cms, status=PENDING_MANUAL_SMOKE, completed date, plans 13/13, test_count + test_assertions + test_passing + test_failing + test_incomplete + bot_test_count + 7 quality_gates ALL=GREEN, requirements=[REQ-goal-cms, REQ-success-public-browse], manual_smoke_required (4 items), canonical_model_binding (re-affirm App\Models\GameMatch).
       - **Overview**: 4-paragraph summary of Phase 7 deliverables (medialibrary + Tiptap + Sitemap + FTS + SSR + 4 public Vue pages + 2 Filament resources + cms-editor role + 17 RED→GREEN test files + 6 new packages).
       - **[BLOCKING] Quality gates table**: per-gate result + command + numerical output (counts).
       - **Test growth across phases table**: Phase 1..7, with Phase 7 contribution delta (e.g., 866 → ~960 web Pest, +~94 / +~280 assertions).
       - **ROADMAP Success Criteria mapping table**: SC-1..SC-5 each with verbatim description + concrete test file + plan source + PASS|PARTIAL status.
       - **Requirements traceability table**: REQ-goal-cms + REQ-success-public-browse → tests covered → PASS.
       - **Open Questions RESOLVED Inline table**: 8 entries (1: Discord channel → global config; 2: editorial team → trenchwars:make-cms-editor; 3: 4 starter categories; 4: slug collision → unique-rule validation; 5: single non-translatable slug v1; 6: per-event-type colors; 7: SSR global production toggle; 8: markdown v2 out of scope).
       - **Pitfall Coverage Matrix**: 12 pitfalls from RESEARCH mapped to mitigation test + plan source + status.
       - **RESEARCH Assumptions Status table**: 10 assumptions (A1..A10) each verified or LOCKED inline.
       - **Canonical Phase 7 Bindings (D-07-*) table**: capture cross-cutting decisions made during 07-01..07-12 (e.g., D-07-01-A Tiptap profile pinning, D-07-02-A FTS trigger pattern, D-07-03-A media uuidMorphs amendment, D-07-04-A cms-editor permission grant list, D-07-05-A unique slug validation, D-07-06-A two-hook observer pattern continuation, D-07-07-A onOneServer dual-guard, D-07-08-A PlayerPrivacyGate canShowInSearch addition, D-07-09-A throttle:60,1 precedent reuse, D-07-10-A v-html bodyHtml safe-by-construction, D-07-11-A split ssr service, D-07-12-A head-key on every meta tag — plus any per-plan SUMMARY-recorded deviations as additional D-07-XX-NN rows).
       - **Locked Decisions Honored table**: D-001..D-021 + D-04-03-A + Phase 4-6 carry-forwards.
       - **Pest full suite snapshot + Vitest full suite snapshot**: actual command output.
       - **Test Inventory by Category table**: Models, Services, Observers, DTOs/Unit Data, Admin (Filament), Public, I18n, Sitemap, Search, SSR — Phase 7 test files + counts.
       - **Static analysis snapshot table**: 7 gates each with command + result.
       - **Grep gate verification table**: invariants from 07-01..07-12 (Tiptap profile excludes iframe, head-key on every meta tag, throttle middleware present, etc.).
       - **Must-have traceability table**: M1..M8 each mapped to plan acceptance criteria.
       - **Manual Smoke Checklist (PENDING_MANUAL_SMOKE)**: 4 items A-D verbatim from 07-VALIDATION.md manual_smokes:
         - A. Filament editor flow: write article, schedule, publish
         - B. Calendar UX month/week/day toggles + FullCalendar event click navigation
         - C. Search ranking matches expectations across articles + clans + players
         - D. Sitemap.xml accessible + valid XML + Discord announce on publish + SSR first paint
       - **Performance Metrics (Phase 7 plan timings) table**: 13 plans.
       - **Open Items Carrying Forward to Phase 8+ table** + **Out-of-Scope Items Deferred to Future Phases table** (Markdown V2 path, multi-author workflows, comment threads, newsletter subscriptions, translation memory infra, sitemap index split, ELO-based search ranking).
       - **Files Created / Modified Summary**: cross-cutting notes (medialibrary uuidMorphs amendment, dual-CHECK migration idiom, etc.).
       - **Sign-off + Phase 8 hand-off note**: RCON automation hand-off — Article events on /events calendar precedent reusable for match-result events; throttle precedent; observer chain.

    2. Edit .planning/ROADMAP.md in-place — find the Phase 7 entry (line ~187 per current ROADMAP). Replace the entire "Plans:" block (currently lists Phase 2 placeholder plans 02-01..02-14) with the actual 13 Phase 7 plan entries flipped to [x]:
       - [x] 07-01-PLAN.md — Wave 0 scaffolding (composer/npm deps + 17 RED Pest stubs + Tiptap safe-node profile + 3 i18n namespaces)
       - [x] 07-02-PLAN.md — Migrations (categories + articles + media uuidMorphs amendment + FTS triggers + discord_outbound CHECK extension)
       - [x] 07-03-PLAN.md — Article + Category models + factories + CategorySeeder (4 starter categories — Open Question 3 LOCKED)
       - [x] 07-04-PLAN.md — cms-editor role permissions + ArticlePolicy + CategoryPolicy + trenchwars:make-cms-editor artisan
       - [x] 07-05-PLAN.md — Filament ArticleResource + CategoryResource + 6 page classes + PublicArticleData tiptap_converter wiring (Pitfall 10 mitigation)
       - [x] 07-06-PLAN.md — ArticleObserver + ArticleStatusService + DiscordOutboundPayloadBuilder extension + config('discord.league_announce_channel_id')
       - [x] 07-07-PLAN.md — ArticlesPublishScheduledCommand + ArticlePublishService + scheduler entry (Pitfall 12 dual-guard)
       - [x] 07-08-PLAN.md — SearchService (Postgres FTS + PlayerPrivacyGate filter + Pitfall 2 plainto_tsquery) + SearchResultData DTOs
       - [x] 07-09-PLAN.md — 5 public controllers + 2 FormRequests + CalendarFeedService + 5 routes + throttle:60,1
       - [x] 07-10-PLAN.md — 4 Vue pages + 4 Cms/* components + PublicLayout SearchBar + FullCalendar mount
       - [x] 07-11-PLAN.md — Inertia v2 SSR enable + 6th ssr docker-compose service (Open Question 7 LOCKED — split service)
       - [x] 07-12-PLAN.md — sitemap:generate + Sitemapable on 3 models + Inertia <Head> meta tags + CmsI18nKeyCoverageTest + ArticleAuditLogTest
       - [x] 07-13-PLAN.md — [BLOCKING] phase verification + ROADMAP/REQUIREMENTS/STATE updates + 7 quality gates
       Also update: "**Plans**: 13 plans" (replace TBD/14). Goal line stays as is (already outcome-shaped). Add "**Completed**: 2026-05-15" (today's date — system prompt confirmed). At bottom progress table flip Phase 7 row: Plans Complete 13/13, Status Complete, Completed 2026-05-15.

    3. Edit .planning/REQUIREMENTS.md in-place — find traceability table; flip:
       - `| REQ-goal-cms | Phase 7 | Pending |` → `| REQ-goal-cms | Phase 7 | Complete |`
       - `| REQ-success-public-browse | Phase 7 | Pending |` → `| REQ-success-public-browse | Phase 7 | Complete |`
       Also flip the v1 Requirements Active list checkboxes: `- [ ] **REQ-goal-cms**` → `- [x]` and `- [ ] **REQ-success-public-browse**` → `- [x]`.
       Append a footer line: `*Last updated: 2026-05-15 — Phase 7 close: REQ-goal-cms + REQ-success-public-browse flipped to Complete (07-PHASE-VERIFICATION.md authored)*`.

    4. Edit .planning/STATE.md in-place:
       - frontmatter: completed_phases 6 → 7; completed_plans 82 → 95; percent 67 → 78; last_updated → 2026-05-15T... (system clock); last_activity → 2026-05-15; status field flipped to "Phase 7 COMPLETE (PENDING_MANUAL_SMOKE) — 07-13-PLAN.md executed; 07-PHASE-VERIFICATION.md authored; ROADMAP/REQUIREMENTS/STATE updated".
       - "Current Position" block: Phase 7 → COMPLETE PENDING_MANUAL_SMOKE; Plan: 13 of 13 complete; Status: Ready to plan Phase 8 (RCON automation); progress bar 78%.
       - Performance Metrics: append Phase 07 P01..P13 rows with timings (collect from each SUMMARY.md or leave placeholder per-plan from orchestrator if unknown).
       - Accumulated Context → Decisions: APPEND every D-07-XX-NN binding sourced from per-plan SUMMARYs (~25-40 entries) plus a final D-04-03-A continuation note ("App\Models\GameMatch direct import everywhere in Phase 7 — articles morphMany Event polymorphic; canonical binding for Phase 8 RCON automation").
       - Session Continuity: last session 2026-05-15...; stopped at "Phase 7 COMPLETE (PENDING_MANUAL_SMOKE) — Phase 8 ready"; resume file None.

    Run no automated verify — verification is the very document this task produces. Operator review is the only gate beyond the 7 automated quality gates from task 1.
  </action>
  <verify>
    <automated>ls .planning/phases/07-cms/07-PHASE-VERIFICATION.md && grep -c "Phase 7: CMS" .planning/ROADMAP.md | grep -E "^[1-9]" && grep -c "REQ-goal-cms.*Complete\|REQ-success-public-browse.*Complete" .planning/REQUIREMENTS.md | grep -E "^[2-9]" && grep "completed_phases: 7" .planning/STATE.md</automated>
  </verify>
  <done>07-PHASE-VERIFICATION.md authored (target ~600 lines matching Phase 6 structure); ROADMAP updated to 13/13 Complete; REQUIREMENTS flips 2 requirements to Complete; STATE bumps completed_phases to 7 + percent to 78 + adds D-07-* bindings; commit pattern preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification artifact → operator | 07-PHASE-VERIFICATION.md is the canonical "Phase 7 done" claim; operator manually verifies via the 4 PENDING_MANUAL_SMOKE items |
| ROADMAP / REQUIREMENTS / STATE atomicity | Tri-file update must commit as a single git operation to keep planning consistent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-13-01 | Repudiation | Manual smoke items A-D never get verified | accept | PENDING_MANUAL_SMOKE flag in frontmatter; ROADMAP shows "Complete (PENDING_MANUAL_SMOKE)" — operator-driven; same pattern as Phase 6 plan 06-14 |
| T-07-13-02 | Tampering | Test counts in verification artifact differ from actual gate output | mitigate | Task 1 captures exact counts; task 2 transcribes verbatim — no synthesized numbers |
| T-07-13-03 | Tampering | ROADMAP plan list still shows Phase 2 placeholder entries after close | mitigate | Task 2 explicitly replaces the entire Plans block with Phase 7 entries — surgical edit verified via grep |
| T-07-13-04 | Information Disclosure | Verification artifact leaking internal architecture decisions to public | accept | .planning/ is project-internal; not exposed to public surface; D-007/D-012/etc. are already public via PROJECT.md anyway |
</threat_model>

<verification>
All 7 quality gates GREEN (task 1 capture).
07-PHASE-VERIFICATION.md exists at .planning/phases/07-cms/07-PHASE-VERIFICATION.md with frontmatter + all canonical sections per Phase 6 template.
ROADMAP Phase 7 row flipped 13/13 Complete + dated 2026-05-15; plan list reflects actual Phase 7 plans (not Phase 2 placeholders).
REQUIREMENTS traceability table flips 2 Phase 7 rows + v1 list checkboxes.
STATE.md frontmatter + body + Accumulated Decisions all updated.
Optional final commit reference: "feat(07-13): Phase 7 COMPLETE — CMS shipped (PENDING_MANUAL_SMOKE 4-item operator walkthrough A-D)".
</verification>

<success_criteria>
Phase 7 closed cleanly with all 7 quality gates GREEN. 17 RED Pest stubs from 07-01 all replaced with GREEN tests across 07-02..07-12. 8 Open Questions RESOLVED Inline. 12 Pitfalls mitigated with concrete tests. PENDING_MANUAL_SMOKE flag set for the 4-item operator walkthrough. Phase 8 (RCON automation) is unblocked.
</success_criteria>

<output>
Create `.planning/phases/07-cms/07-13-SUMMARY.md` documenting:
- Final 7-gate counts (Pest passing N + assertions M, etc.)
- D-07-* binding count (target ~30-40 entries)
- Operator outcome line placeholders for the 4 manual smoke items
- Any Rule 1/2/3 deviations encountered during the close (likely zero — close plans are mostly transcription)
</output>
</content>
</invoke>