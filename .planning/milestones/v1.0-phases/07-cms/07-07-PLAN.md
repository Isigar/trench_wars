---
phase: 07-cms
plan: 07
type: execute
wave: 4
depends_on: ["07-05", "07-06"]
files_modified:
  - apps/web/app/Console/Commands/ArticlesPublishScheduledCommand.php
  - apps/web/app/Services/ArticlePublishService.php
  - apps/web/routes/console.php
  - apps/web/tests/Feature/Articles/ArticlePublishWorkflowTest.php
  - apps/web/tests/Feature/Console/ArticlesPublishScheduledCommandTest.php
autonomous: true
requirements:
  - REQ-goal-cms
must_haves:
  truths:
    - "ArticlePublishService at apps/web/app/Services/ArticlePublishService.php — publishDue(?Carbon $now=null): int method; queries articles where status='scheduled' AND scheduled_at <= now(); chunkById(100) for memory safety; delegates each row's status flip to ArticleStatusService::transition($a, 'published') so the observer chain fires uniformly (vs raw $article->update which would still fire observer but bypass the state machine guard); returns count of articles flipped"
    - "ArticlesPublishScheduledCommand at apps/web/app/Console/Commands/ArticlesPublishScheduledCommand.php — signature 'articles:publish-scheduled'; handle() injects ArticlePublishService + calls publishDue(now()); $this->info echoes count; returns Command::SUCCESS"
    - "Scheduler hook in apps/web/routes/console.php — `Schedule::command('articles:publish-scheduled')->everyMinute()->withoutOverlapping()->onOneServer()` (Pitfall 12 mitigation BOTH guards required for Railway multi-replica per D-014 + RESEARCH Pattern 4); preserve existing scheduler entries from Phase 1-6 verbatim (Phase 1 plan 01-14 may already have entries)"
    - "ArticlesPublishScheduledCommandTest GREEN — 4+ it() blocks: command registered + callable via Artisan::call; 0 scheduled articles → count=0 + SUCCESS; 1 scheduled article with scheduled_at in past → flips to published + count=1; 1 scheduled article with scheduled_at in future → NOT flipped (count=0); ArticleObserver fires during flip (article_announce outbound row landed — defence-in-depth chain proof)"
    - "ArticlePublishWorkflowTest GREEN (replaces 07-01 RED stub) — 6+ it() blocks: full happy path (cms-editor creates draft → schedules → Schedule::command runs → status=published + Event row + outbound row + activity_log row); withoutOverlapping prevents duplicate run when invoked twice in quick succession (mock cache lock); idempotent on no-op runs; chunkById processes 250 scheduled rows correctly (boundary test)"
    - "make pest filter GREEN; phpstan + pint clean"
  artifacts:
    - path: "apps/web/app/Services/ArticlePublishService.php"
      provides: "Stateless publish-due service; delegates state flip to ArticleStatusService to keep observer chain firing uniformly"
      contains: "publishDue"
    - path: "apps/web/app/Console/Commands/ArticlesPublishScheduledCommand.php"
      provides: "Artisan command target for the every-minute scheduler"
      contains: "articles:publish-scheduled"
    - path: "apps/web/routes/console.php"
      provides: "Schedule::command entry with withoutOverlapping + onOneServer (Pitfall 12)"
      contains: "articles:publish-scheduled"
    - path: "apps/web/tests/Feature/Articles/ArticlePublishWorkflowTest.php"
      provides: "End-to-end workflow test: draft → scheduled → scheduler runs → published + audit"
      contains: "ArticlePublishService"
  key_links:
    - from: "ArticlePublishService"
      to: "ArticleStatusService (07-06)"
      via: "Service delegates each status flip to ArticleStatusService::transition so the observer chain fires uniformly across cron + manual admin paths"
      pattern: "ArticleStatusService"
    - from: "Schedule::command()->everyMinute"
      to: "Railway cron (D-014) + Phase 5 worker container"
      via: "Existing Laravel scheduler cron tick (Phase 1 plan 01-14 established) picks up the new entry; withoutOverlapping uses Redis lock from Phase 5 Horizon stack"
      pattern: "everyMinute"
    - from: "Pitfall 12 multi-replica guards"
      to: "Phase verification manual smoke"
      via: "Operator scales worker to 2 replicas in Railway and verifies no duplicate Discord announces"
      pattern: "onOneServer"
---

<objective>
Wave 4: Wire the Laravel Scheduler everyMinute auto-publish path. ArticlePublishService walks the scheduled-due rows in chunked batches; ArticlesPublishScheduledCommand is the artisan target; routes/console.php registers the Schedule::command with withoutOverlapping + onOneServer (Pitfall 12 dual-guard for Railway multi-replica).

Purpose: SC-1 requires "publishing flowing Draft → Scheduled → Published via Laravel Scheduler." This plan ships the scheduler half; plan 07-05 already shipped the cms-editor authoring half. Delegating to ArticleStatusService::transition (instead of raw model update) keeps the observer chain firing identically whether the publish was admin-driven or scheduler-driven — a critical correctness property for the audit log + Discord announce.

Output: 1 service + 1 command + 1 scheduler entry + 2 GREEN test files (1 replacing 07-01 RED stub).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cms/07-CONTEXT.md
@.planning/phases/07-cms/07-RESEARCH.md
@.planning/phases/07-cms/07-06-SUMMARY.md
@apps/web/routes/console.php
@apps/web/app/Console/Commands/MakeAdminCommand.php

<interfaces>
<!-- ArticlePublishService - delegates to ArticleStatusService for uniform observer chain -->

```php
namespace App\Services;
use App\Models\Article;
use Carbon\Carbon;

final class ArticlePublishService
{
    public function __construct(private ArticleStatusService $statusService) {}

    public function publishDue(?Carbon $now = null): int
    {
        $now ??= now();
        $count = 0;
        Article::query()
            ->where('status', 'scheduled')
            ->where('scheduled_at', '<=', $now)
            ->orderBy('scheduled_at')   // FIFO publish order on tie
            ->chunkById(100, function ($articles) use (&$count) {
                foreach ($articles as $a) {
                    // Delegate to state service — observer chain fires identically to admin path
                    $this->statusService->transition($a, 'published');
                    $count++;
                }
            });
        return $count;
    }
}
```

<!-- Pitfall 12 mitigation - canonical Laravel idiom (verbatim from RESEARCH Pattern 4) -->

```php
// apps/web/routes/console.php
use Illuminate\Support\Facades\Schedule;

Schedule::command('articles:publish-scheduled')
    ->everyMinute()
    ->withoutOverlapping()    // single-host single-execution
    ->onOneServer();          // multi-host single-execution via cache lock — Railway multi-replica critical
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author ArticlePublishService + ArticlesPublishScheduledCommand + register scheduler entry</name>
  <files>apps/web/app/Services/ArticlePublishService.php, apps/web/app/Console/Commands/ArticlesPublishScheduledCommand.php, apps/web/routes/console.php</files>
  <action>
    1. Author apps/web/app/Services/ArticlePublishService.php per <interfaces> verbatim. Constructor injects ArticleStatusService. Final class.

    2. Author apps/web/app/Console/Commands/ArticlesPublishScheduledCommand.php:
       - $signature = 'articles:publish-scheduled'
       - $description = 'Promote Article status=scheduled → published when scheduled_at has passed'
       - handle(ArticlePublishService $service): int — calls $count = $service->publishDue(); $this->info(sprintf('Published %d article(s).', $count)); returns self::SUCCESS.

    3. Edit apps/web/routes/console.php in-place — APPEND the Schedule::command per <interfaces>. PRESERVE all existing scheduler entries verbatim (Phase 1-6 may have entries for activity log cleanup, queue maintenance, etc.). Add `use Illuminate\Support\Facades\Schedule;` if not already imported.

    Run `make artisan ARGS="schedule:list"` to verify the new entry appears with `every minute` cadence + onOneServer markers.
  </action>
  <verify>
    <automated>docker compose exec web php artisan schedule:list 2>&1 | grep "articles:publish-scheduled" | grep -i "minute" && docker compose exec web php artisan articles:publish-scheduled --help 2>&1 | grep -i "scheduled" && docker compose exec web ./vendor/bin/phpstan analyse apps/web/app/Services/ArticlePublishService.php apps/web/app/Console/Commands/ArticlesPublishScheduledCommand.php apps/web/routes/console.php --no-progress --memory-limit=2G 2>&1 | grep "\[OK\]"</automated>
  </verify>
  <done>Service + command + scheduler entry all present; `schedule:list` shows the new everyMinute task; PHPStan L8 clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace 07-01 RED stubs with GREEN ArticlePublishWorkflowTest + author new ArticlesPublishScheduledCommandTest</name>
  <files>apps/web/tests/Feature/Articles/ArticlePublishWorkflowTest.php, apps/web/tests/Feature/Console/ArticlesPublishScheduledCommandTest.php</files>
  <action>
    1. Replace apps/web/tests/Feature/Articles/ArticlePublishWorkflowTest.php (was 07-01 RED stub) with end-to-end Pest GREEN tests. Bare functional convention. 6+ it() blocks:
       - `it('walks an article from draft to scheduled to published via the scheduler command', ...)` — create draft article; ArticleStatusService::transition to scheduled with scheduled_at = now()->subMinute(); Artisan::call('articles:publish-scheduled'); assert $article->fresh()->status === 'published' AND published_at !== null AND scheduled_at === null AND Event MorphOne row exists AND DiscordOutboundMessage with message_type=article_announce exists AND activity_log row with log_name=article logs the transition.
       - `it('does not publish a scheduled article with scheduled_at in the future', ...)` — scheduled_at = now()->addHour; run command; assert status still 'scheduled'.
       - `it('processes 250 scheduled articles correctly via chunkById', ...)` — factory 250 scheduled articles past due; run command; assert 250 flipped (boundary test — exercises chunkById(100) batching).
       - `it('emits SUCCESS exit code with zero matches', ...)` — empty scheduled set; Artisan exit code === 0.
       - `it('logs the published count via $this->info', ...)` — capture output via Artisan::output(); assert contains "Published 1 article(s)".
       - `it('fails gracefully when ArticleStatusService throws', ...)` — directly mock or inject articles where transition would fail (e.g., concurrent admin already flipped to published mid-run — Pitfall 12 horizon); assert command does NOT crash, surfaces the error.

    2. Author apps/web/tests/Feature/Console/ArticlesPublishScheduledCommandTest.php — narrower test, 4+ it() blocks:
       - `it('registers the articles:publish-scheduled command in the artisan registry', ...)` — Artisan::call returns 0; output not empty.
       - `it('returns exit code 0 with zero articles', ...)`
       - `it('returns exit code 0 with one published article', ...)`
       - `it('uses ArticlePublishService via container resolution', ...)` — bind a stub ArticlePublishService instance in the container, assert Artisan::call invokes the stub's publishDue.

    Run filtered pest. Assert all GREEN.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter='ArticlePublishWorkflowTest|ArticlesPublishScheduledCommandTest' --no-coverage 2>&1 | tail -10 | grep -E "(passed|PASS)"</automated>
  </verify>
  <done>Both test files GREEN with 6+ and 4+ it() blocks; full workflow proven from draft → scheduled → scheduler tick → published + audit + outbound.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cron tick (Railway scheduler) → artisan command | System-driven; auth()->id() is null inside the command; activity_log captures the system-level action; ArticleObserver causer_user_id stays null (T-07-06-04 accept) |
| Multi-replica worker scaling | Pitfall 12: Railway scales worker to 2 replicas → both run schedule:run → without dual guards, duplicate publishes happen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-01 | Repudiation | Duplicate publish across multi-replica worker (Pitfall 12) | mitigate | Schedule::command()->withoutOverlapping() acquires single-host lock; ->onOneServer() acquires distributed Redis lock (cache driver) — BOTH guards required; ArticlePublishWorkflowTest exercises rapid-fire double-invocation |
| T-07-07-02 | Tampering | Race between cms-editor manual publish + scheduler tick | mitigate | ArticleStatusService::transition throws InvalidArticleStatusTransitionException on published→published (since 'published' has no permitted target 'published'); ArticlePublishService catches OR surfaces; test asserts no crash on race |
| T-07-07-03 | Information Disclosure | Scheduler-driven publish lacks causer_user_id in activity log | accept | T-07-06-04 accept continuation; documented in <interfaces> + audit log explicitly shows "(system)" attribution for null causer (Phase 5 D-05-12-E precedent) |
| T-07-07-04 | Denial of Service | Massive scheduled-article backlog blowing chunkById memory | mitigate | chunkById(100) caps per-batch memory; FIFO orderBy(scheduled_at) ensures oldest-first processing; 250-article boundary test proves no OOM at 2.5x default batch |
| T-07-07-05 | Tampering | Cron impostor invoking artisan articles:publish-scheduled directly | accept | Container-only docker compose exec is the dev path (D-021); production Railway runs the cron under the worker service's own permissions; non-root user inside container has no SQL injection surface beyond what Eloquent already protects |
| T-07-07-06 | Denial of Service | onOneServer cache key collision exhausting Redis | accept | Laravel's onOneServer lock uses a short-lived (~60s) cache key; per-job; not a DOS surface; Phase 5 Horizon already monitors Redis health |
</threat_model>

<verification>
make pest --filter='ArticlePublishWorkflowTest|ArticlesPublishScheduledCommandTest' GREEN.
`php artisan schedule:list` shows the new everyMinute task with withoutOverlapping + onOneServer flags.
Full suite stays regression-free.
PHPStan L8 + Pint clean.
</verification>

<success_criteria>
Auto-publish state machine functional end-to-end: cms-editor schedules an article via Filament → cron tick → ArticleStatusService transition → observer fires → Event row + Discord outbound row + activity_log row all present. Pitfall 12 dual-guard wired (withoutOverlapping + onOneServer). SC-1 Draft → Scheduled → Published flow complete.
</success_criteria>

<output>
Create `.planning/phases/07-cms/07-07-SUMMARY.md` documenting:
- Existing routes/console.php entries (Phase 1-6 carryforward preserved)
- Whether Schedule facade was already imported or this plan added it
- Pitfall 12 dual-guard evidence (test output showing single fire on double invocation)
- ArticlePublishService chunkById batch size justification (100 — Laravel default for chunkById, balanced for round-1 corpus)
</output>
</content>
</invoke>