---
phase: 07-cms
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - apps/web/app/Models/Article.php
  - apps/web/app/Models/Category.php
  - apps/web/database/factories/ArticleFactory.php
  - apps/web/database/factories/CategoryFactory.php
  - apps/web/database/seeders/CategorySeeder.php
  - apps/web/database/seeders/DatabaseSeeder.php
  - apps/web/tests/Feature/Models/ArticleModelTest.php
  - apps/web/tests/Feature/Models/CategoryModelTest.php
  - apps/web/tests/Unit/Data/PublicArticleDataTest.php
autonomous: true
requirements:
  - REQ-goal-cms
  - REQ-success-public-browse
must_haves:
  truths:
    - "Article model class at apps/web/app/Models/Article.php — extends Model, implements HasMedia + Sitemapable (deferred Sitemapable impl to plan 07-12 — interface declared here, toSitemapTag() lands later), uses HasTranslations + HasUuidPrimaryKey + InteractsWithMedia + LogsActivity + SoftDeletes; translatable=['title','excerpt','body']; casts() returns body+excerpt+title as 'array' (JSONB), scheduled_at + published_at as datetime, allow_discord_announce as boolean"
    - "Article registers 3 media conversions via registerMediaConversions(?Media): 'thumb' (Fit::Cover 600x400 + responsiveImages), 'hero' (Fit::Cover 1600x900 + responsiveImages), 'og-image' (Fit::Cover 1200x630 ->nonQueued — SEO immediacy per RESEARCH Pitfall 6)"
    - "Article relationships: category() BelongsTo Category; author() BelongsTo User on author_user_id; events() MorphMany App\\Models\\Event (Phase 4 polymorphic events table reuse — Pattern 7 precedent)"
    - "Article LogsActivity options via getActivitylogOptions(): logsAll() + logOnlyDirty() + dontLogIfAttributesChangedOnly(['updated_at']) + useLogName('article') (Phase 6 D-06-03-B + D-06-03-C verbatim continuation)"
    - "Category model class at apps/web/app/Models/Category.php — extends Model, uses HasTranslations + HasUuidPrimaryKey + LogsActivity + SoftDeletes; translatable=['name']; articles() HasMany Article"
    - "ArticleFactory + CategoryFactory definition() methods filled with realistic data; Article factory uses Category::factory() for category_id, generates JSONB title via faker->sentence then ['en' => $sentence], default status='draft', allow_discord_announce=true"
    - "CategorySeeder seeds 4 starter categories per Open Question 3 LOCKED inline: News, Match Reports, Tournament Updates, Community — uses firstOrCreate(['slug' => Str::slug($name)], ['name' => ['en' => $name]]) idiom (Phase 2 plan 02-04 precedent); seeder registered in DatabaseSeeder::run() AFTER Phase 1-6 seeders + ClanTag seeder"
    - "ArticleModelTest GREEN — 6+ it() blocks covering: HasTranslations smoke (title->>'en' round-trip), HasMedia smoke (addMedia from path attaches to 'hero' collection), category() relationship, author() relationship, events() MorphMany shape, LogsActivity smoke (Article::create writes activity_log row with log_name='article')"
    - "CategoryModelTest GREEN — 3+ it() blocks: HasTranslations on name, articles() HasMany relationship, slug UNIQUE enforcement (DB-level)"
    - "PublicArticleDataTest GREEN partial — 1-2 it() blocks asserting PublicArticleData DTO shape; full DTO + tiptap_converter integration lands in plan 07-05 (this plan ships the DTO class skeleton with public typed constructor properties to unblock plan 07-05 form schema — implementation can defer to plan 07-05)"
    - "ArticleObserver placeholder NOT registered yet (plan 07-06 ships real observer + registration via Article::observed); Article::booted() empty in this plan"
    - "make pest --filter='ArticleModelTest|CategoryModelTest' GREEN; make phpstan zero new errors; make pint --test clean"
  artifacts:
    - path: "apps/web/app/Models/Article.php"
      provides: "Article Eloquent model with HasTranslations + HasMedia + LogsActivity + media conversions"
      contains: "registerMediaConversions"
    - path: "apps/web/app/Models/Category.php"
      provides: "Category Eloquent model"
      contains: "translatable"
    - path: "apps/web/database/factories/ArticleFactory.php"
      provides: "Real factory definition replacing 07-01 stub"
      contains: "definition"
    - path: "apps/web/database/seeders/CategorySeeder.php"
      provides: "4 starter categories per Open Question 3 LOCKED"
      contains: "Match Reports"
    - path: "apps/web/tests/Feature/Models/ArticleModelTest.php"
      provides: "6+ GREEN it() blocks"
      contains: "HasMedia"
    - path: "apps/web/tests/Unit/Data/PublicArticleDataTest.php"
      provides: "Partial GREEN — DTO shape smoke; full impl in plan 07-05"
      contains: "PublicArticleData"
  key_links:
    - from: "Article::events()"
      to: "App\\Models\\Event (Phase 4 polymorphic events table)"
      via: "MorphMany — Article becomes the 3rd eventable_type alongside GameMatch + Tournament (RESEARCH Pattern 7)"
      pattern: "morphMany.*Event"
    - from: "Article media conversions"
      to: "Plan 07-05 ArticleResource SpatieMediaLibraryFileUpload form field"
      via: "Form field uses ->collection('hero') matching the registerMediaConversions performOnCollections('hero')"
      pattern: "collection.*hero"
    - from: "Category seeded 4 starter rows"
      to: "Plan 07-05 ArticleResource Select::make('category_id')->relationship('category', 'name->en')"
      via: "Category rows must exist before any Article can be created in Filament"
      pattern: "category_id"
---

<objective>
Wave 2: Land Article + Category Eloquent models + factories + CategorySeeder. Ship Article with HasMedia (medialibrary integration) + 3 media conversions + LogsActivity + HasTranslations + polymorphic events() relation (Pattern 7 — Article becomes the 3rd eventable_type). Seed the 4 starter categories Open Question 3 LOCKED inline.

Purpose: Plans 07-05 (Filament ArticleResource) + 07-06 (ArticleObserver) + 07-09 (public controllers) + 07-12 (sitemap Sitemapable impl) all reference the Article model. This plan ships the canonical shape so downstream plans wire against a stable contract. Media conversions defined here mean plan 07-05 SpatieMediaLibraryFileUpload field needs zero re-configuration of disk / collection names.

Output: 2 model classes, 2 real factory bodies (replacing 07-01 stubs), 1 seeder, 3 model tests, 1 partial DTO test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-cms/07-CONTEXT.md
@.planning/phases/07-cms/07-RESEARCH.md
@.planning/phases/07-cms/07-02-SUMMARY.md
@apps/web/app/Models/Tournament.php
@apps/web/app/Models/Clan.php
@apps/web/app/Models/Event.php

<interfaces>
<!-- Article model FQN + key trait imports (from RESEARCH Pattern 1 + 2) -->

```php
namespace App\Models;

use App\Concerns\HasUuidPrimaryKey;            // Phase 1 existing trait
use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Database\Eloquent\Model;
use Illuminate\Database\Eloquent\Relations\BelongsTo;
use Illuminate\Database\Eloquent\Relations\MorphMany;
use Illuminate\Database\Eloquent\SoftDeletes;
use Spatie\Activitylog\LogOptions;
use Spatie\Activitylog\Traits\LogsActivity;     // canonical v5 path per D-06-03-B
use Spatie\Image\Enums\Fit;
use Spatie\MediaLibrary\HasMedia;               // interface
use Spatie\MediaLibrary\InteractsWithMedia;     // trait
use Spatie\MediaLibrary\MediaCollections\Models\Media;
use Spatie\Sitemap\Contracts\Sitemapable;       // interface (toSitemapTag deferred to plan 07-12)
use Spatie\Sitemap\Tags\Url;
use Spatie\Translatable\HasTranslations;
```

<!-- Phase 6 D-06-03-B + D-06-03-C LogsActivity idiom (verbatim) -->

```php
public function getActivitylogOptions(): LogOptions
{
    return LogOptions::defaults()
        ->logFillable()
        ->logOnlyDirty()
        ->dontLogIfAttributesChangedOnly(['updated_at'])
        ->useLogName('article');
}
```

<!-- Phase 4 events MorphMany precedent (Tournament + GameMatch — 2 existing eventable_type values; Article is the 3rd) -->

```php
public function events(): MorphMany
{
    return $this->morphMany(Event::class, 'eventable');
}
```

<!-- Open Question 3 LOCKED categories (RESEARCH Open Question 3 Recommendation) -->

Seed exactly these 4 (in this order): News, Match Reports, Tournament Updates, Community. Slug = Str::slug($name) = ['news', 'match-reports', 'tournament-updates', 'community']. Name JSONB = ['en' => $name].

<!-- PublicArticleData DTO partial — shape only -->

Plan 07-03 ships the class with these public typed properties (constructor body filled by plan 07-05):
- public string $id, $slug, $title, $excerpt, $bodyHtml, $categoryName, $authorName
- public ?string $heroThumbUrl, $heroOgImageUrl
- public Carbon $publishedAt
- public bool $allowDiscordAnnounce
- public string $url (computed from slug for OG meta — plan 07-12 sitemap reuses)

This plan can ship a minimal fromModel(Article $a) factory that DOES NOT call tiptap_converter (defers $bodyHtml = '' to plan 07-05 which adds the converter dependency to the controller side).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Article + Category models with full HasMedia + HasTranslations + LogsActivity + polymorphic events() relation</name>
  <files>apps/web/app/Models/Article.php, apps/web/app/Models/Category.php, apps/web/database/factories/ArticleFactory.php, apps/web/database/factories/CategoryFactory.php</files>
  <action>
    1. Author apps/web/app/Models/Article.php per <interfaces> imports. Class signature: `class Article extends Model implements HasMedia, Sitemapable`. Use traits: HasFactory + HasUuidPrimaryKey + HasTranslations + InteractsWithMedia + LogsActivity + SoftDeletes.
       - `public array $translatable = ['title', 'excerpt', 'body'];`
       - `protected $fillable` = ['slug','category_id','title','excerpt','body','status','scheduled_at','published_at','author_user_id','allow_discord_announce']
       - `protected function casts()` returns scheduled_at + published_at as 'datetime'; allow_discord_announce as 'boolean'. Do NOT cast title/excerpt/body — HasTranslations handles JSONB.
       - `public function registerMediaConversions(?Media $media = null)` defines 3 conversions per <interfaces> + RESEARCH Pattern 2: thumb (600x400 responsiveImages, performOnCollections('hero')), hero (1600x900 responsiveImages, performOnCollections('hero')), og-image (1200x630 ->nonQueued, performOnCollections('hero')). The nonQueued conversion is critical for SEO first-paint (Pitfall 6 mitigation — og:image meta tag must be available before queue worker processes thumb/hero).
       - Relations: category() BelongsTo Category, author() BelongsTo User on 'author_user_id', events() MorphMany Event using morphMany(Event::class, 'eventable').
       - LogsActivity options per <interfaces>.
       - `public function toSitemapTag(): Url { throw new \LogicException('Sitemapable implementation lands in plan 07-12'); }` — declares the interface so PHPStan accepts the class, deferred body.

    2. Author apps/web/app/Models/Category.php — extends Model, implements (nothing yet — Sitemapable comes in 07-12), uses HasFactory + HasUuidPrimaryKey + HasTranslations + LogsActivity + SoftDeletes.
       - `public array $translatable = ['name'];`
       - `protected $fillable = ['slug', 'name'];`
       - `articles()` HasMany Article.
       - LogsActivity options: useLogName('category').

    3. Replace apps/web/database/factories/ArticleFactory.php with real definition() body: Title via fake()->sentence then ['en' => $sentence]; excerpt similar; body as Tiptap-JSON-shaped array (e.g., ['type' => 'doc', 'content' => [['type' => 'paragraph', 'content' => [['type' => 'text', 'text' => fake()->paragraph]]]]]); slug via Str::slug(fake()->slug); category_id via Category::factory(); author_user_id null by default (can be overridden in tests); status='draft'; allow_discord_announce=true. Remove the per-line @phpstan-ignore comments now that the model class exists (typed generic resolution kicks in via `extends Factory<Article>` annotation).

    4. Replace apps/web/database/factories/CategoryFactory.php with real definition(): slug via Str::slug(fake()->word).'-'.Str::random(4); name = ['en' => fake()->word]; remove phpstan ignores; add `extends Factory<Category>` generic.

    Run `make phpstan` and `make pint --test` — confirm clean.
  </action>
  <verify>
    <automated>docker compose exec web ls apps/web/app/Models/Article.php apps/web/app/Models/Category.php && docker compose exec web ./vendor/bin/phpstan analyse apps/web/app/Models/Article.php apps/web/app/Models/Category.php apps/web/database/factories/ArticleFactory.php apps/web/database/factories/CategoryFactory.php --no-progress --memory-limit=2G 2>&1 | grep -E "\[OK\] No errors" && docker compose exec web ./vendor/bin/pint --test apps/web/app/Models/Article.php apps/web/app/Models/Category.php</automated>
  </verify>
  <done>Article + Category models authored with all traits + relations + media conversions + LogsActivity options; factories produce realistic data; PHPStan L8 + Pint both clean on the new files.</done>
</task>

<task type="auto">
  <name>Task 2: Author CategorySeeder + register in DatabaseSeeder + author 3 model/data tests (replacing 07-01 RED stubs)</name>
  <files>apps/web/database/seeders/CategorySeeder.php, apps/web/database/seeders/DatabaseSeeder.php, apps/web/tests/Feature/Models/ArticleModelTest.php, apps/web/tests/Feature/Models/CategoryModelTest.php, apps/web/tests/Unit/Data/PublicArticleDataTest.php</files>
  <action>
    1. Author apps/web/database/seeders/CategorySeeder.php — Open Question 3 LOCKED inline: News / Match Reports / Tournament Updates / Community. Use Category::firstOrCreate(['slug' => Str::slug($name)], ['name' => ['en' => $name]]) idiom (Phase 2 plan 02-04 precedent). Class extends Seeder; class doc comment cites Open Question 3 LOCKED + RESEARCH recommendation source.

    2. Register CategorySeeder in apps/web/database/seeders/DatabaseSeeder.php::run() AFTER the existing ClanTagSeeder + any Phase 1-6 seeders. Surgical edit — preserve all existing $this->call(...) entries verbatim.

    3. Replace apps/web/tests/Feature/Models/ArticleModelTest.php (was 07-01 RED stub) with bare functional Pest GREEN tests:
       - `it('round-trips translatable title via HasTranslations', ...)` — create article, set title via setTranslation('title', 'en', 'foo'); assert getTranslation('title', 'en') === 'foo' AND DB::table('articles')->where('id', $a->id)->value('title') JSON-decodes to ['en' => 'foo'].
       - `it('attaches an image to the hero media collection', ...)` — use UploadedFile::fake()->image('hero.jpg', 1600, 900); $article->addMediaFromString($file->getContent())->usingFileName('hero.jpg')->toMediaCollection('hero'); assert $article->getFirstMedia('hero') is not null.
       - `it('relates to a category via BelongsTo', ...)` — category() loaded; assert instance of Category.
       - `it('relates to an author via BelongsTo nullable', ...)` — Article without author_user_id has null author(); Article with author_user_id resolves User.
       - `it('exposes events() MorphMany pointing at the polymorphic events table', ...)` — verify $article->events() returns MorphMany instance with morphClass='App\\Models\\Article' (or whatever the morph map resolves to).
       - `it('writes an activity_log row on Article::create', ...)` — create article, assert activity_log table has a row with log_name='article' and subject_id matching.

    4. Replace apps/web/tests/Feature/Models/CategoryModelTest.php with 3+ GREEN tests:
       - HasTranslations round-trip on name.
       - articles() HasMany shape.
       - DB-level UNIQUE on slug: insert duplicate slug raises QueryException.

    5. Replace apps/web/tests/Unit/Data/PublicArticleDataTest.php (was 07-01 RED stub) with a PARTIAL GREEN test:
       - Create a PublicArticleData via fromModel($article) (the factory exists but may emit empty $bodyHtml since plan 07-05 wires tiptap_converter).
       - Assert basic field shape — id, slug, title (extracted via getTranslation('title', 'en')), publishedAt nullable, url computed.
       - Leave at least one it()->skip('Plan 07-05 wires tiptap_converter()->asHTML for the bodyHtml field') as a marker for plan 07-05.

    Also create apps/web/app/Data/PublicArticleData.php as a minimal class with public typed constructor properties per <interfaces> shape; fromModel() factory returns a new self with empty bodyHtml (TODO marker for plan 07-05).

    Run filtered pest and assert GREEN.
  </action>
  <verify>
    <automated>docker compose exec web ./vendor/bin/pest --filter='ArticleModelTest|CategoryModelTest|PublicArticleDataTest' --no-coverage 2>&1 | tail -10 | grep -E "(passed|PASS)"</automated>
  </verify>
  <done>CategorySeeder seeds 4 starter categories idempotently; 3 GREEN test files replace 07-01 RED stubs (ArticleModelTest with 6+ blocks, CategoryModelTest with 3+ blocks, PublicArticleDataTest with at least 1 GREEN it() + 1 skip marker); make pest full suite stays regression-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Eloquent → Postgres | Article + Category model writes flow through Eloquent; raw inserts bypass observers but trigger DB triggers (FTS) — defence-in-depth |
| InteractsWithMedia → public disk | Article uploaded images land on the public disk via medialibrary; filename normalisation guards against path traversal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Tampering | Article.body Tiptap-JSON stored as 'array' cast vs HasTranslations JSONB | mitigate | body is in $translatable array (HasTranslations handles JSONB write/read); NOT in casts() (would conflict). Round-trip verified in ArticleModelTest |
| T-07-03-02 | Information Disclosure | Author user soft-delete cascading to Article authorship | accept | author_user_id FK uses nullOnDelete (Phase 2 idiom); article remains with null author; activity_log row preserves causer identity |
| T-07-03-03 | Tampering | Polymorphic events() relation could be targeted with crafted eventable_type | mitigate | Phase 4 events table morph constraint enforced by App service provider morph map (Phase 4 plan 04-02); Article registers as 'article' morph alias |
| T-07-03-04 | Repudiation | Direct DB::table()->update bypasses LogsActivity | accept | Activity log is for application-driven mutations; raw SQL writes are by design ungovernable; documented in CLAUDE.md §6 |
| T-07-03-05 | Denial of Service | Image upload conversions consuming queue worker capacity | mitigate | og-image conversion is ->nonQueued (immediate); thumb + hero are queued and rate-limited by Horizon worker config from Phase 5 |
| T-07-03-06 | Information Disclosure | CategorySeeder running in production exposing 'cms-editor' editorial categories | accept | Categories are public content; seeded names are operator-confirmed at phase verification time |
</threat_model>

<verification>
make pest --filter='Article|Category|PublicArticleData' GREEN.
psql `\d articles` shows author_user_id FK with ON DELETE SET NULL.
After migrate:fresh + seed: `SELECT slug FROM categories ORDER BY created_at;` returns the 4 seeded slugs (news, match-reports, tournament-updates, community).
PHPStan L8 reports zero new errors against Article.php + Category.php + factories + DTOs.
Pint --test exits 0.
</verification>

<success_criteria>
Article + Category models functional with HasMedia / HasTranslations / LogsActivity. Factories produce realistic data. CategorySeeder seeds 4 starter categories idempotently (Open Question 3 LOCKED inline). 3 GREEN test files replace 07-01 RED stubs.
</success_criteria>

<output>
After completion, create `.planning/phases/07-cms/07-03-SUMMARY.md` recording:
- The 4 seeded category slugs verifiable in psql
- Any media conversion config tuning surfaced during Pest execution
- Whether the ::booted() empty hook in Article was necessary for the test suite to pass (plan 07-06 ships the real observer)
- Generic factory shape: `extends Factory<Article>` annotation present (replaces 07-01 PHPStan ignore comments)
</output>
</content>
</invoke>