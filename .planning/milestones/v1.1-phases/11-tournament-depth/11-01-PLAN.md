---
phase: 11-tournament-depth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/database/migrations/2026_06_05_100000_add_elo_rating_to_clans.php
  - apps/web/database/migrations/2026_06_05_100010_add_rated_at_to_tournament_brackets.php
  - apps/web/database/migrations/2026_06_05_100020_add_median_buchholz_to_tournament_standings.php
  - apps/web/database/migrations/2026_06_05_100030_add_game_match_type_id_to_tournament_stages.php
  - apps/web/app/Models/Clan.php
  - apps/web/app/Models/TournamentBracket.php
  - apps/web/app/Models/TournamentStanding.php
  - apps/web/app/Models/TournamentStage.php
  - apps/web/app/Data/TournamentStandingData.php
  - apps/web/tests/Feature/Services/EloRatingServiceTest.php
  - apps/web/tests/Feature/Services/SwissMedianBuchholzTest.php
  - apps/web/tests/Feature/Services/SwissAutoAdvanceTest.php
  - apps/web/tests/Feature/Services/StageMatchTypeOverrideTest.php
autonomous: true
requirements: [TOUR-01, TOUR-02, TOUR-03, TOUR-04]

must_haves:
  truths:
    - "Running migrate:fresh --seed succeeds with the four new columns present"
    - "clans.elo_rating defaults to 1500 and clans.elo_matches_count defaults to 0 for every row"
    - "tournament_brackets.rated_at is a nullable timestamp; tournament_standings.median_buchholz is decimal(8,2) default 0; tournament_stages.game_match_type_id is a nullable FK to game_match_types with nullOnDelete"
    - "All four Phase 11 RED test files exist and fail (services/columns not yet wired)"
  artifacts:
    - path: "apps/web/database/migrations/2026_06_05_100000_add_elo_rating_to_clans.php"
      provides: "clans.elo_rating + elo_matches_count columns"
      contains: "elo_rating"
    - path: "apps/web/database/migrations/2026_06_05_100010_add_rated_at_to_tournament_brackets.php"
      provides: "tournament_brackets.rated_at idempotency marker"
      contains: "rated_at"
    - path: "apps/web/database/migrations/2026_06_05_100020_add_median_buchholz_to_tournament_standings.php"
      provides: "tournament_standings.median_buchholz column"
      contains: "median_buchholz"
    - path: "apps/web/database/migrations/2026_06_05_100030_add_game_match_type_id_to_tournament_stages.php"
      provides: "tournament_stages.game_match_type_id nullable FK"
      contains: "game_match_type_id"
    - path: "apps/web/app/Data/TournamentStandingData.php"
      provides: "median_buchholz field on the standings DTO"
      contains: "median_buchholz"
  key_links:
    - from: "apps/web/app/Models/TournamentStage.php"
      to: "game_match_types"
      via: "gameMatchType() BelongsTo on game_match_type_id"
      pattern: "function gameMatchType"
    - from: "apps/web/app/Models/Clan.php"
      to: "clans.elo_rating"
      via: "fillable + casts"
      pattern: "elo_rating"
---

<objective>
Wave 0/1 foundation for Phase 11. Adds the four schema columns that every later
plan reads or writes, wires their model fillable/casts/relations, extends the
standings DTO, and lays down the four RED test scaffolds the pure-logic plans
turn GREEN.

This plan is deliberately additive and regression-free: no behaviour changes to
any Phase-6 service. The columns carry safe defaults (elo_rating=1500,
elo_matches_count=0, median_buchholz=0, rated_at=null, game_match_type_id=null)
so existing Phase-6 tests keep passing unchanged.

Purpose: every Phase 11 requirement (TOUR-01..04) needs at least one of these
columns; isolating them in Wave 1 lets Waves 2-4 run against a stable schema.
Output: 4 migrations, 4 model edits, 1 DTO edit, 4 failing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/11-tournament-depth/11-CONTEXT.md
@.planning/phases/11-tournament-depth/11-PATTERNS.md

# Migration analog (Phase 10 column-add idiom)
@apps/web/database/migrations/2026_06_04_100000_add_accepts_applications_to_clans.php

# Models being extended
@apps/web/app/Models/Clan.php
@apps/web/app/Models/TournamentBracket.php
@apps/web/app/Models/TournamentStanding.php
@apps/web/app/Models/TournamentStage.php
@apps/web/app/Data/TournamentStandingData.php

<interfaces>
<!-- Contracts later plans implement against. Executor: use these directly. -->

Stage type vs Tournament format (CRITICAL distinction, do not conflate):
  - Tournament.format ∈ {single_elimination, double_elimination, round_robin, swiss}
  - TournamentStage.type for Swiss rounds == 'swiss-round' (NOT 'swiss')
  - Swiss round number == TournamentStage.ordinal (integer, 1-based)

TournamentStage already exposes:
  public function tournament(): BelongsTo<Tournament>
  public function brackets(): HasMany<TournamentBracket>   // ordered (round_number, position)
  protected $fillable = ['tournament_id','type','ordinal','name','settings'];

Game::matchTypes(): HasMany<GameMatchType>   // used by plan 11-04 scoped Select
Tournament::game(): BelongsTo<Game>; Tournament::defaultGameMatchType(): BelongsTo<GameMatchType>

TournamentStanding columns today: wins, losses, draws, points(decimal:2),
  tiebreak_score(decimal:2 — stores plain Buchholz for Swiss), rank(nullable int).

TournamentStandingData today (constructor order matters — Spatie\LaravelData):
  id, tournament_id, tournament_stage_id, participant_id, wins, losses, draws,
  points(float), tiebreak_score(float), rank(?int).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four additive migrations + model fillable/casts/relations</name>
  <read_first>
    - apps/web/database/migrations/2026_06_04_100000_add_accepts_applications_to_clans.php (Schema::table up/down idiom, reverse-order dropColumn)
    - apps/web/app/Models/Clan.php (casts() method at lines 74-79; fillable at 52-63)
    - apps/web/app/Models/TournamentBracket.php (fillable 44-54; casts 57-63)
    - apps/web/app/Models/TournamentStanding.php (fillable 37-47; casts 50-60)
    - apps/web/app/Models/TournamentStage.php (fillable 36-42; tournament()/brackets() relations 62-74)
    - 11-PATTERNS.md "Schema Migration Patterns" section (exact column specs)
  </read_first>
  <files>
    apps/web/database/migrations/2026_06_05_100000_add_elo_rating_to_clans.php,
    apps/web/database/migrations/2026_06_05_100010_add_rated_at_to_tournament_brackets.php,
    apps/web/database/migrations/2026_06_05_100020_add_median_buchholz_to_tournament_standings.php,
    apps/web/database/migrations/2026_06_05_100030_add_game_match_type_id_to_tournament_stages.php,
    apps/web/app/Models/Clan.php,
    apps/web/app/Models/TournamentBracket.php,
    apps/web/app/Models/TournamentStanding.php,
    apps/web/app/Models/TournamentStage.php
  </files>
  <action>
    Create four anonymous-class migrations following the 2026_06_04_100000 idiom (Schema::table up(), reverse dropColumn down()):

    1. add_elo_rating_to_clans (TOUR-02): on clans add `integer('elo_rating')->default(1500)->after('status')` and `integer('elo_matches_count')->default(0)->after('elo_rating')`. down() drops both in reverse order (['elo_matches_count','elo_rating']).
    2. add_rated_at_to_tournament_brackets (TOUR-01/TOUR-02 idempotency marker): on tournament_brackets add `timestampTz('rated_at')->nullable()->after('winner_participant_id')`. No index. down() drops 'rated_at'.
    3. add_median_buchholz_to_tournament_standings (TOUR-03): on tournament_standings add `decimal('median_buchholz', 8, 2)->default(0)->after('tiebreak_score')` (matches existing points/tiebreak_score precision). down() drops 'median_buchholz'.
    4. add_game_match_type_id_to_tournament_stages (TOUR-04): on tournament_stages add `uuid('game_match_type_id')->nullable()->after('settings')` then `foreign('game_match_type_id')->references('id')->on('game_match_types')->nullOnDelete()`. down() drops the foreign key then the column.

    Then wire the models (additive only — preserve all existing entries and the existing casts() method bodies):
    - Clan: add 'elo_rating' and 'elo_matches_count' to $fillable; add `'elo_rating' => 'integer'` and `'elo_matches_count' => 'integer'` to the existing casts() return array (the method already exists for accepts_applications — extend it, do not replace it).
    - TournamentBracket: add 'rated_at' to $fillable; add `'rated_at' => 'datetime'` to casts().
    - TournamentStanding: add 'median_buchholz' to $fillable; add `'median_buchholz' => 'decimal:2'` to casts().
    - TournamentStage: add 'game_match_type_id' to $fillable; add a `gameMatchType(): BelongsTo<GameMatchType>` relation `return $this->belongsTo(GameMatchType::class, 'game_match_type_id');` with the matching `/** @return BelongsTo<GameMatchType, $this> */` PHPDoc (PHPStan L8 generic annotation, mirror the existing tournament() relation). Add `use App\Models\GameMatchType;`-equivalent import (GameMatchType is in the App\Models namespace; no import needed since both are App\Models — reference it bare, matching how tournament() references Tournament).

    Do NOT touch any service, the seeder, or any non-swiss standings calculator. All four columns carry defaults so migrate:fresh --seed and existing Phase-6 standings inserts (which omit median_buchholz) succeed unchanged.
  </action>
  <verify>
    <automated>cd apps/web && docker compose -f ../../docker-compose.yml exec -T web php artisan migrate:fresh --seed 2>&1 | tail -5 && docker compose -f ../../docker-compose.yml exec -T web php -r "require 'vendor/autoload.php'; \$app=require 'bootstrap/app.php'; \$app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); foreach(['clans'=>['elo_rating','elo_matches_count'],'tournament_brackets'=>['rated_at'],'tournament_standings'=>['median_buchholz'],'tournament_stages'=>['game_match_type_id']] as \$t=>\$cols){foreach(\$cols as \$c){echo \$t.'.'.\$c.': '.(Illuminate\Support\Facades\Schema::hasColumn(\$t,\$c)?'OK':'MISSING').PHP_EOL;}}"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] migrate:fresh --seed exits 0 (no SQL errors, seeder completes)
    - [ ] Schema::hasColumn reports OK for all six (clans.elo_rating, clans.elo_matches_count, tournament_brackets.rated_at, tournament_standings.median_buchholz, tournament_stages.game_match_type_id)
    - [ ] clans.elo_rating column default is 1500; elo_matches_count default 0
    - [ ] tournament_stages.game_match_type_id FK uses nullOnDelete (dropping a game_match_type sets stage column null, does not delete the stage)
    - [ ] make phpstan passes (model PHPDoc generics correct on gameMatchType())
  </acceptance_criteria>
  <done>migrate:fresh --seed green; six columns present with correct types/defaults; TournamentStage::gameMatchType() relation resolves; PHPStan L8 green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend TournamentStandingData DTO + author four RED test scaffolds</name>
  <read_first>
    - apps/web/app/Data/TournamentStandingData.php (constructor + fromModel; field order is the Spatie Data contract)
    - apps/web/tests/Feature/Services/StandingsCalculatorServiceTest.php (makeStandingsTournament / makeStandingsParticipants fixture helpers)
    - apps/web/tests/Feature/Services/BracketAdvancementServiceTest.php lines 45-90 (makeAdvancementTournament / makeAdvancementParticipants helpers)
    - apps/web/tests/Feature/Services/TournamentSeedingServiceTest.php lines 32-50 (by_rank fixture style)
    - 11-PATTERNS.md "Pest Test Patterns" section (exact it() examples)
  </read_first>
  <behavior>
    - EloRatingServiceTest: equal-rated winner gains, loser loses (K=32 → 1516/1484); draw between equals = no change; idempotency contract via rated_at is asserted in plan 11-03, here assert pure math + draw + lopsided (1800 vs 1200 winner gains < 16).
    - SwissMedianBuchholzTest: a 4+-opponent fixture where median_buchholz drops highest+lowest opponent score; a <3-opponent fixture where median_buchholz == plain buchholz (tiebreak_score).
    - SwissAutoAdvanceTest: recording the final result of a Swiss round generates the next swiss-round stage automatically (stage count increments by 1); a second advance does NOT generate a third stage (idempotency).
    - StageMatchTypeOverrideTest: a stage with game_match_type_id set materialises a GameMatch with that type; a stage with null override falls back to tournament.default_game_match_type_id.
  </behavior>
  <files>
    apps/web/app/Data/TournamentStandingData.php,
    apps/web/tests/Feature/Services/EloRatingServiceTest.php,
    apps/web/tests/Feature/Services/SwissMedianBuchholzTest.php,
    apps/web/tests/Feature/Services/SwissAutoAdvanceTest.php,
    apps/web/tests/Feature/Services/StageMatchTypeOverrideTest.php
  </files>
  <action>
    DTO: add `public float $median_buchholz,` to TournamentStandingData's constructor immediately AFTER `public float $tiebreak_score,` and BEFORE `public ?int $rank,` (Spatie\LaravelData maps by constructor position — keep this order). In fromModel(), add `median_buchholz: (float) $standing->median_buchholz,` between the tiebreak_score and rank lines. The #[TypeScript] attribute auto-regenerates the TS surface (shared-types regen happens in plan 11-05).

    Author four Pest RED scaffolds (Feature tests, RefreshDatabase via Pest.php). Each must be a real failing test that exercises the not-yet-built behaviour so the GREEN plans (11-02/11-03/11-04) have a target. Reuse the existing fixture helpers' STYLE (copy makeAdvancementTournament/makeAdvancementParticipants and makeStandings* helper shapes into the new files or factor minimal local helpers). Per the Nyquist rule these are the automated gates for Waves 2-3.

    - EloRatingServiceTest.php: `use App\Services\EloRatingService;` + `use App\Models\Clan;`. Tests:
        it('winner gains and loser loses with K=32 for equal ratings') → create two Clans elo_rating=1500; app(EloRatingService::class)->applyResult($winner,$loser); expect winner->fresh()->elo_rating === 1516 and loser === 1484.
        it('draw between equal ratings leaves both unchanged') → applyResult($a,$b, draw:true); both stay 1500.
        it('upset winner gains fewer than 16 points') → winner 1200 vs loser 1800 default (non-draw, winner is the lower-rated) gains > 16; favourite-wins gains < 16 (pick one clear assertion).
        it('elo_matches_count increments for both clans') → after applyResult both fresh()->elo_matches_count === 1.
      This file FAILS now because App\Services\EloRatingService does not exist.
    - SwissMedianBuchholzTest.php: fixture a swiss tournament + standings recompute, assert tournament_standings.median_buchholz; and a small-N case asserting median_buchholz == tiebreak_score. FAILS now because SwissStandingsCalculator does not yet write median_buchholz (column defaults 0 → assertion mismatch).
    - SwissAutoAdvanceTest.php: build a 4-participant Swiss tournament (format 'swiss'), generate round 1, record results for every round-1 bracket via the MatchResult path that drives BracketAdvancementService::advance, assert `stages()->where('type','swiss-round')->count()` went 1→2; second pass does not reach 3. FAILS now because auto-advance is not wired.
    - StageMatchTypeOverrideTest.php: build a tournament with default match type A and a swiss/elim stage; set stage.game_match_type_id = B; materialise → assert spawned GameMatch.game_match_type_id === B; null-override stage → assert === default A. FAILS now because the materialiser ignores the stage override.

    Mark these RED scaffolds clearly with a header comment `Source: 11-01-PLAN.md Task 2 — RED scaffold; turned GREEN by 11-0{2,3,4}.`
  </action>
  <verify>
    <automated>cd apps/web && docker compose -f ../../docker-compose.yml exec -T web ./vendor/bin/pest tests/Feature/Services/EloRatingServiceTest.php tests/Feature/Services/SwissMedianBuchholzTest.php tests/Feature/Services/SwissAutoAdvanceTest.php tests/Feature/Services/StageMatchTypeOverrideTest.php 2>&1 | tail -25; echo "EXPECT: all four files present and FAILING (RED)"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] All four test files exist and are discovered by Pest (no "No tests found" / parse errors)
    - [ ] Every test in the four files currently FAILS or errors for the right reason (missing service / unwired behaviour) — none pass yet
    - [ ] TournamentStandingData constructor has median_buchholz between tiebreak_score and rank; fromModel populates it
    - [ ] make phpstan passes on the DTO change (median_buchholz typed float)
    - [ ] EloRatingServiceTest asserts the exact K=32 values 1516/1484 for the equal-rating win
  </acceptance_criteria>
  <done>DTO carries median_buchholz; four RED scaffolds exist and fail for the correct (unwired) reasons; PHPStan green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → existing data | New columns must not corrupt or re-default existing Phase-6 rows |
| stage → game_match_type FK | Deleting a game_match_type must not cascade-delete tournament stages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | tournament_stages.game_match_type_id FK | mitigate | nullOnDelete (not cascade) — dropping a match type nulls the override, stage survives; asserted in acceptance criteria |
| T-11-01-02 | Denial of Service | migrate:fresh on existing standings inserts | mitigate | median_buchholz default 0 so Phase-6 calculators that omit the column still insert; verified by full migrate:fresh --seed |
| T-11-01-03 | Integrity | elo_rating seed default | mitigate | NOT NULL default 1500 — every existing + new clan starts at the canonical base; no null-rating leak into seeding (plan 11-03) |
| T-11-01-SC | Tampering | package installs | accept | no npm/composer installs in this plan — pure migrations + model edits; no Package Legitimacy Gate needed |
</threat_model>

<verification>
- `make artisan ARGS="migrate:fresh --seed"` — full schema + seed green.
- `make phpstan` — model + DTO generics clean at level 8.
- Four RED test files discovered and failing.
- No Phase-6 regression: existing tournament service tests are NOT run here (they would still pass — only additive columns added) but the full suite gate runs in plan 11-05.
</verification>

<success_criteria>
- Four columns exist with correct types/defaults; models wired; DTO extended.
- TournamentStage::gameMatchType() relation present and PHPStan-clean.
- Four RED scaffolds in place as the GREEN targets for Waves 2-3.
- migrate:fresh --seed and PHPStan both green.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-depth/11-01-SUMMARY.md` when done.
</output>
