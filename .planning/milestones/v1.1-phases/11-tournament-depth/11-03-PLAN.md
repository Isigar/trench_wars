---
phase: 11-tournament-depth
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - apps/web/app/Services/BracketAdvancementService.php
  - apps/web/app/Services/TournamentSeedingService.php
  - apps/web/tests/Feature/Services/SwissAutoAdvanceTest.php
  - apps/web/tests/Feature/Services/EloAdvancementHookTest.php
  - apps/web/tests/Feature/Services/TournamentSeedingServiceTest.php
autonomous: true
requirements: [TOUR-01, TOUR-02]

must_haves:
  truths:
    - "Recording a bracket-match result applies Elo to both clans exactly once per bracket, guarded by the rated_at marker (a re-fired advance does not double-apply)"
    - "When every bracket in a Swiss round has a winner, the next swiss-round stage generates automatically with no admin action, and only once (re-fired advance does not create a duplicate round)"
    - "Auto-advance never fires past the final Swiss round (round-exhaustion guard) and only for stages of type 'swiss-round'"
    - "by_rank seeding orders participants by clan elo_rating DESC; with all clans at 1500 the resulting seed order is identical to the pre-Phase-11 created_at behaviour (no Phase-6 regression)"
  artifacts:
    - path: "apps/web/app/Services/BracketAdvancementService.php"
      provides: "Elo hook (rated_at-guarded) + Swiss auto-advance hook inside advance()"
      contains: "rated_at"
    - path: "apps/web/app/Services/TournamentSeedingService.php"
      provides: "by_rank ordered by clan.elo_rating"
      contains: "elo_rating"
  key_links:
    - from: "apps/web/app/Services/BracketAdvancementService.php"
      to: "apps/web/app/Services/EloRatingService.php"
      via: "app(EloRatingService::class)->applyResult inside the advance transaction, guarded by bracket.rated_at"
      pattern: "EloRatingService"
    - from: "apps/web/app/Services/BracketAdvancementService.php"
      to: "apps/web/app/Services/Brackets/SwissGenerator.php"
      via: "app(SwissGenerator::class)->generateNextRound on round completion"
      pattern: "SwissGenerator"
---

<objective>
Wire the two event-driven hooks into the existing Phase-6 BracketAdvancementService
and repoint by_rank seeding at real Elo — without rewriting any of it. Both hooks
slot into the SAME DB::transaction that already wraps advance(), between the
standings recalc (step 5) and the Discord announce enqueue (step 6).

1. TOUR-02 Elo hook: after the winner is written, resolve both participants' clans
   and call EloRatingService::applyResult — but only if bracket.rated_at is null,
   then stamp rated_at = now() inside the transaction. This guard is the integrity
   control: each bracket result moves Elo exactly once.
2. TOUR-01 Swiss auto-advance: if the resolved stage is type 'swiss-round' and every
   bracket in it now has a winner, call SwissGenerator::generateNextRound — guarded by
   an existence check (no next swiss-round stage with a higher ordinal yet) and the
   round-exhaustion check, so each round generates exactly once. The existing
   TournamentObserver outbound (tournament_announce_update) fires off the new stage so
   the public page reflects it. The admin HeaderAction (plan 06-11) stays as a manual
   fallback.
3. TOUR-02 seeding: TournamentSeedingService::orderByRank orders by clan elo_rating DESC.

Purpose: these are the two requirements whose SC is "happens automatically, no admin
action". Idempotency is the load-bearing risk and is tested explicitly.
Output: 2 modified services, 3 GREEN/regression test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-tournament-depth/11-CONTEXT.md
@.planning/phases/11-tournament-depth/11-PATTERNS.md

# The service being extended — Elo hook + auto-advance go between line 187 and line 189
@apps/web/app/Services/BracketAdvancementService.php

# generateNextRound (the auto-advance target) + the round-exhaustion guard at lines 151-159
@apps/web/app/Services/Brackets/SwissGenerator.php

# Seeding service — orderByRank at lines 177-180
@apps/web/app/Services/TournamentSeedingService.php

# The Elo engine this plan calls (shipped GREEN by 11-02)
@apps/web/app/Services/EloRatingService.php

@apps/web/app/Models/TournamentBracket.php
@apps/web/app/Models/TournamentParticipant.php

<interfaces>
<!-- The exact insertion points + idioms. Executor: use these, do not re-explore. -->

In BracketAdvancementService::advance(), inside the DB::transaction closure
(lines 120-206), the winner participant is already resolved as $winnerParticipant
and the loser (when both slots filled) as $loserParticipant. Step 5 is
`app(StandingsCalculatorService::class)->recalculate($tournament);` at line 187.
Insert BOTH new hooks between line 187 (step 5) and line 189 (step 6 Discord enqueue),
so they run inside the existing transaction + tournament lock.

CRITICAL: $loserParticipant may be null (bye / single-slot bracket). Elo needs BOTH
clans — skip the Elo apply when $loserParticipant is null. A bye is not a played
match; it must not move ratings.

Stage type guard: use $stage->type === 'swiss-round' (NOT 'swiss'; 'swiss' is the
Tournament.format). $stage is already in scope (line 154 re-reads $bracket->stage).

rated_at is on $bracket (added by 11-01, cast 'datetime', in fillable). The bracket
was just updated with winner_participant_id at line 125; re-read its rated_at via the
in-memory model ($bracket->rated_at) — it is null on first advance.

SwissGenerator::generateNextRound(Tournament): void throws LogicException when rounds
are exhausted — the caller MUST avoid calling it past the last round. Replicate the
exhaustion guard from SwissGenerator lines 151-159 (currentRound = max ordinal of
swiss-round stages; totalRounds = max(1, ceil(log2(activeCount)))) OR wrap the call in
try/catch(\LogicException). Prefer the inline guard (no exception-as-control-flow).

Circular-DI: resolve EloRatingService and SwissGenerator via app(...) lookup, NOT
constructor injection — same documented pattern as the StandingsCalculatorService call
at line 187 (comment at lines 41-44 / 51-53).

orderByRank today: `return $participants->sortByDesc('created_at')->values();`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Elo hook (rated_at-guarded) + Swiss auto-advance hook in advance()</name>
  <read_first>
    - apps/web/app/Services/BracketAdvancementService.php (the whole advance() transaction, lines 74-207; insertion is between 187 and 189)
    - apps/web/app/Services/Brackets/SwissGenerator.php lines 149-160 (exhaustion guard) + 225-231 (next-stage create)
    - apps/web/app/Services/EloRatingService.php (applyResult signature from 11-02)
    - apps/web/tests/Feature/Services/BracketAdvancementServiceTest.php lines 45-90 (makeAdvancement* helpers — copy for the new test)
    - apps/web/tests/Feature/Services/SwissAutoAdvanceTest.php (11-01 RED scaffold to turn GREEN)
  </read_first>
  <files>
    apps/web/app/Services/BracketAdvancementService.php,
    apps/web/tests/Feature/Services/SwissAutoAdvanceTest.php,
    apps/web/tests/Feature/Services/EloAdvancementHookTest.php
  </files>
  <behavior>
    - First result on a bracket: both clans' elo_rating change; bracket.rated_at set to a timestamp.
    - Re-running advance() for the same already-rated bracket: ratings unchanged (rated_at guard short-circuits) — the double-fire case.
    - Bye / single-participant bracket ($loserParticipant null): no Elo applied, no rated_at stamp (no opponent).
    - Swiss round fully decided → exactly one new swiss-round stage appears (ordinal+1).
    - advance() fired again after the round is already advanced → no duplicate stage (existence check).
    - Final Swiss round decided → no stage created (exhaustion guard; no LogicException surfaces to the caller).
    - Non-swiss formats (single/double elim, round robin): auto-advance block is skipped (stage.type !== 'swiss-round') — existing BracketAdvancementServiceTest stays green.
  </behavior>
  <action>
    Edit BracketAdvancementService::advance(), inserting after the standings recalc call (line 187) and before the DiscordOutboundMessage::create (line 189), INSIDE the existing transaction closure. Do not change the method signature, the existing steps, or the transaction/lock structure.

    Elo hook (TOUR-02):
      - Guard: only proceed when $loserParticipant !== null AND $bracket->rated_at === null.
      - Resolve clans: $winnerClan = Clan::find($winnerParticipant->clan_id); $loserClan = Clan::find($loserParticipant->clan_id). (TournamentParticipant has clan_id; both are non-null for a played bracket.) Guard against null clan (PHPStan + integrity): skip if either is null.
      - Determine draw: this hook only runs when there is a winner_participant_id (advance() already returned early on null winner_clan_id at line 76), so pass draw:false. (Swiss draws never reach advance()'s winner path — a drawn result has winner_clan_id null and is a no-op for advancement; Elo for swiss draws is out of scope per the host_clan-only data model and CONTEXT — accept.)
      - Call app(\App\Services\EloRatingService::class)->applyResult($winnerClan, $loserClan).
      - Stamp the guard: $bracket->update(['rated_at' => now()]); (inside the transaction).
      - Add the circular-DI doc comment in the BracketAdvancementService lines 41-53 style for the EloRatingService app() resolution.

    Swiss auto-advance hook (TOUR-01):
      - Only when $stage->type === 'swiss-round'.
      - roundComplete = ! TournamentBracket::query()->where('tournament_stage_id', $stage->id)->whereNull('winner_participant_id')->exists();
      - Exhaustion + existence guard (replicate SwissGenerator lines 151-159 inline): compute currentRound = max ordinal of this tournament's swiss-round stages; activeCount = participants where status='active'; totalRounds = max(1, (int) ceil(log(max(activeCount,2),2))). nextRoundExists = $tournament->stages()->where('type','swiss-round')->where('ordinal','>',$stage->ordinal)->exists().
      - If roundComplete && currentRound !== null && currentRound < totalRounds && ! nextRoundExists: app(\App\Services\Brackets\SwissGenerator::class)->generateNextRound($tournament); Add the circular-DI doc comment.
      - generateNextRound creates the next stage; the existing standings recalc already ran for this round, so the new round pairs off correct standings. Do NOT call recalculate again here.

    Note on the announce outbound: the existing step 6 (DiscordOutboundMessage bracket_result_announce) still fires for the recorded result. The new swiss-round stage triggers the TournamentObserver / tournament_announce_update through its own create() (no extra wiring needed here — the observer is already registered on TournamentStage? Verify: TournamentStage has LogsActivity but the announce is on Tournament's observer). If the public-page refresh relies on the Tournament observer rather than a stage create, touch the tournament (e.g. $tournament->touch()) so updated_at bumps and the 30s public poll picks up the new round. Choose the lightest correct trigger and document it in the SUMMARY.

    Turn the two RED scaffolds GREEN:
      - SwissAutoAdvanceTest.php (from 11-01): assert stage count 1→2 on round completion + no third on re-fire + no advance past final round.
      - EloAdvancementHookTest.php (NEW): assert first-result Elo delta + rated_at set; re-fire leaves ratings unchanged (double-fire idempotency); bye does not move Elo. Copy makeAdvancement* helpers from BracketAdvancementServiceTest.
  </action>
  <verify>
    <automated>cd apps/web && docker compose -f ../../docker-compose.yml exec -T web ./vendor/bin/pest tests/Feature/Services/SwissAutoAdvanceTest.php tests/Feature/Services/EloAdvancementHookTest.php tests/Feature/Services/BracketAdvancementServiceTest.php 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - [ ] SwissAutoAdvanceTest GREEN: round 1→2 auto-generates; re-fire creates no duplicate; final round does not over-generate
    - [ ] EloAdvancementHookTest GREEN: first result moves both clans' elo_rating + sets rated_at; second advance on the same bracket leaves ratings unchanged (double-fire guard); bye bracket moves no rating
    - [ ] Existing BracketAdvancementServiceTest STILL GREEN (single/double-elim advancement, grand-final reset, completion, Discord outbound all unaffected)
    - [ ] No EloRatingService / SwissGenerator constructor injection added — both resolved via app() (circular-DI break preserved)
    - [ ] make phpstan + make pint --test green
  </acceptance_criteria>
  <done>Both hooks live inside the existing advance() transaction; Elo applies once per bracket (rated_at guard); Swiss rounds auto-advance once; no Phase-6 advancement regression.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint by_rank seeding at clan elo_rating (no all-1500 regression)</name>
  <read_first>
    - apps/web/app/Services/TournamentSeedingService.php lines 170-180 (orderByRank — current created_at DESC proxy)
    - apps/web/app/Models/TournamentParticipant.php (clan() BelongsTo + clan_id)
    - apps/web/tests/Feature/Services/TournamentSeedingServiceTest.php lines 32-50 (existing by_rank test asserts created_at DESC order — this is the regression contract)
    - 11-CONTEXT.md decision #4 + specifics line 60 (all-equal Elo MUST match current order)
  </read_first>
  <files>
    apps/web/app/Services/TournamentSeedingService.php,
    apps/web/tests/Feature/Services/TournamentSeedingServiceTest.php
  </files>
  <action>
    Replace orderByRank's body so it orders participants by clan elo_rating DESC.

    BINDING DECISION (D-11-03-A) — resolve a CONTEXT/code conflict: CONTEXT.md decision #4
    says "tiebreak created_at ASC". The EXISTING Phase-6 test asserts the current proxy is
    `created_at DESC`, and CONTEXT specifics line 60 + the SC require "all-equal Elo produces
    the SAME order as the current proxy (no regression)". These conflict. The no-regression
    requirement wins: use elo_rating DESC as the primary key and `created_at DESC` as the
    equal-rating tiebreak, so when every clan is 1500 the output is byte-identical to today's
    sortByDesc('created_at'). Document D-11-03-A in the SUMMARY.

    Implementation: $participants->loadMissing('clan'); then sort by a composite comparator —
    primary (clan?->elo_rating ?? 1500) DESC, secondary created_at DESC. Use a single
    usort/sort closure (Collection::sort with a comparator) so the two keys compose
    deterministically; do NOT chain two sortByDesc calls (chaining is not stable across keys).
    Return ->values(). Keep the PHPDoc @param/@return generics. Update the method docblock to
    note Phase 11 (elo_rating) supersedes the A11 created_at proxy.

    Tests:
      - Add `it('by_rank orders participants by clan elo_rating desc')`: high-elo clan (1800) → seed 1, low-elo (1200) → seed 2 (per 11-PATTERNS.md example).
      - Add `it('by_rank with all clans at 1500 matches created_at desc order (no regression)')`: four participants with distinct created_at, all clans elo 1500 → seeds match created_at DESC (oldest = last seed), proving D-11-03-A.
      - The EXISTING `by_rank ... created_at desc order` test: if its clans default to 1500 (factory default from 11-01), it must STILL PASS unchanged. If the factory sets random elo, adjust the existing test's clans to elo_rating=1500 so it remains a created_at-tiebreak assertion (Rule 2: keep the regression contract meaningful).
  </action>
  <verify>
    <automated>cd apps/web && docker compose -f ../../docker-compose.yml exec -T web ./vendor/bin/pest tests/Feature/Services/TournamentSeedingServiceTest.php 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - [ ] by_rank orders by clan elo_rating DESC (1800 → seed 1)
    - [ ] all-1500 case produces created_at DESC order (byte-identical to pre-Phase-11 behaviour) — D-11-03-A regression test green
    - [ ] the pre-existing by_rank/reseed/canReseed tests all stay GREEN (no D-06-05 semantic change)
    - [ ] make phpstan + make pint --test green
  </acceptance_criteria>
  <done>by_rank seeds by Elo; the all-1500 path is provably regression-free; reseed/canReseed untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MatchResult save → advance() | a recorded result triggers rating + round-generation side effects |
| concurrent advances → tournament | parallel referee submissions could double-apply Elo or double-generate a round |
| admin → seeding | only panel-admins can seed; ratings used are server-computed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Integrity | Elo applied more than once per bracket | mitigate | rated_at null-guard before applyResult + rated_at stamp inside the existing DB::transaction + Tournament lockForUpdate; double-fire test asserts no second apply |
| T-11-03-02 | Integrity | Swiss round generated twice | mitigate | roundComplete + nextRoundExists existence check + exhaustion guard, all inside the locked transaction; re-fire test asserts no duplicate stage |
| T-11-03-03 | Denial of Service | auto-advance past final round throws LogicException up the observer chain | mitigate | inline currentRound < totalRounds guard prevents the call entirely (no exception-as-control-flow) |
| T-11-03-04 | Tampering | client-supplied rating | mitigate | applyResult receives only Clan models resolved from participant.clan_id inside the trusted advance path; no request input reaches the rating math |
| T-11-03-05 | Elevation of Privilege | non-admin triggers seeding/advance | mitigate | seeding stays behind the existing Phase-1 canAccessPanel admin gate (D-06-11-A, no new permission); advance() fires only from the MatchResult observer on results an admin/referee recorded |
| T-11-03-SC | Tampering | package installs | accept | no installs |
</threat_model>

<verification>
- `make pest ARGS="tests/Feature/Services/SwissAutoAdvanceTest.php tests/Feature/Services/EloAdvancementHookTest.php tests/Feature/Services/TournamentSeedingServiceTest.php tests/Feature/Services/BracketAdvancementServiceTest.php"` — all GREEN.
- `make phpstan` + `make pint ARGS="--test"` — green.
- Idempotency double-fire cases (Elo + round) explicitly asserted.
- No-regression: existing BracketAdvancementServiceTest + the existing by_rank seeding test stay green.
</verification>

<success_criteria>
- Elo applies exactly once per bracket result (rated_at guard); Swiss rounds self-advance exactly once; both inside the existing locked transaction.
- by_rank seeds by Elo; all-1500 is regression-free (D-11-03-A).
- All Phase-11 advancement/seeding tests GREEN; Phase-6 advancement tests stay GREEN.
</success_criteria>

<output>
Create `.planning/phases/11-tournament-depth/11-03-SUMMARY.md` when done.
</output>
