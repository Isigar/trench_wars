---
phase: 12-notifications-bot-polish
plan: 03
type: tdd
wave: 2
depends_on: [12-02]
files_modified:
  - apps/bot/src/commands/match.ts
  - apps/bot/src/commands/clan.ts
  - apps/bot/tests/commands/match.test.ts
  - apps/bot/tests/commands/clan.test.ts
autonomous: true
requirements: [BOT-01]
must_haves:
  truths:
    - "/match list fetches /matches?page=N (default 1) reading both data AND meta from the {data, meta} envelope"
    - "/clan list fetches /clans?page=N (default 1) reading data + meta"
    - "Both commands render a Prev/Next ActionRow ONLY when meta.last_page > 1, and a 'Page X of Y' indicator (i18n)"
    - "Empty list and single-page (last_page === 1) cases render NO pagination buttons (existing behavior preserved)"
  artifacts:
    - path: "apps/bot/src/commands/match.ts"
      provides: "/match list paginated via API meta, with Prev/Next row + page indicator"
      contains: "paginationButtons"
    - path: "apps/bot/src/commands/clan.ts"
      provides: "/clan list paginated via API meta, with Prev/Next row + page indicator"
      contains: "paginationButtons"
  key_links:
    - from: "apps/bot/src/commands/match.ts"
      to: "/matches?page=N"
      via: "api.get<{ data; meta }>"
      pattern: "meta"
    - from: "apps/bot/src/commands/match.ts"
      to: "apps/bot/src/lib/buttons.ts"
      via: "paginationButtons(listType, page, lastPage)"
      pattern: "paginationButtons"
---

<objective>
Wire BOT-01 pagination into the `/match list` and `/clan list` slash commands: fetch a specific page via the API meta envelope, render that page's items, and append a Prev/Next row plus a "Page X of Y" indicator when more than one page exists. Replace `/match list`'s hardcoded top-5 slice with real API pagination (per_page from meta). TDD on the render decision.

Purpose: Produces the in-message paginated list (the visible half of BOT-01 SC). The button handler that re-fetches on click is plan 12-04.
Output: Updated match.ts + clan.ts list branches, page-indicator i18n, tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/12-notifications-bot-polish/12-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Contracts from plan 12-02 (this plan's dependency) + existing code. -->

apps/bot/src/lib/buttons.ts (from 12-02):
  export function paginationButtons(listType: 'match' | 'clan', page: number, lastPage: number): ActionRowBuilder<ButtonBuilder>

apps/bot/src/services/api.ts:
  api.get<T>(path: string, opts?): Promise<T>   // query params are part of the path string, e.g. api.get('/matches?page=2')
  // Web API returns { data, meta } for list endpoints (Laravel paginate). api.get does NOT unwrap.

Web index meta shape (BotApiMatchController / BotApiClanController index, verified):
  meta: { current_page: number; per_page: number; total: number; last_page: number }
  // Laravel ->paginate() reads ?page=N automatically; out-of-range page returns empty data with correct meta.
  // /matches clamps ?limit to <=50 (default 25); /clans clamps ?limit to <=100 (default 25).

apps/bot/src/commands/match.ts (current list branch, to REPLACE):
  if (sub === 'list') {
    const matches = await api.get<{ data: PublicMatchData[] }>('/matches', { actsAsDiscordId });
    // ... empty check, then top = matches.data.slice(0, 5); embeds = top.flatMap(m => matchCard(m).embeds);
  }
  // matchCard(m) from ../lib/embeds.js returns { embeds: [...] }.

apps/bot/src/commands/clan.ts (current list branch):
  if (sub === 'list') {
    const clans = await api.get<{ data: ClanData[] }>('/clans', { actsAsDiscordId });
    await interaction.editReply(formatClanList(clans.data));
  }
</interfaces>
</context>

<feature>
  <name>/match list + /clan list in-message pagination</name>
  <files>apps/bot/src/commands/match.ts, apps/bot/src/commands/clan.ts, apps/bot/tests/commands/match.test.ts, apps/bot/tests/commands/clan.test.ts</files>
  <behavior>
    /match list (tests/commands/match.test.ts):
    - calls api.get with path '/matches?page=1' (default page) and actsAsDiscordId set
    - when meta.last_page > 1: editReply payload includes a components array containing a Prev/Next ActionRow AND the reply content/embed shows "Page 1 of <last_page>" (i18n string, not hardcoded English literal duplicated inline)
    - when meta.last_page === 1: editReply payload has NO pagination components
    - when data is empty: existing empty-state reply, NO components
    - items rendered are the page's matches (matchCard each) — no client-side slice(0,5)

    /clan list (tests/commands/clan.test.ts):
    - calls api.get with path '/clans?page=1'
    - when meta.last_page > 1: components include the Prev/Next row + "Page 1 of Y" indicator
    - when last_page === 1 or empty: no components
  </behavior>
  <implementation>
    RED first: extend tests/commands/match.test.ts and tests/commands/clan.test.ts to mock api.get returning { data, meta } and assert on the editReply argument (components present iff last_page>1; page indicator text present; api.get called with the ?page=1 path). Mirror the existing test setup in those files (mock interaction, mock api). Run vitest, confirm failing. Commit `test(12-03): pagination render expectations for /match + /clan list`.

    GREEN:
    - match.ts list branch: read the current page from a default of 1 (helper accepts a page param so 12-04 can reuse). Call `api.get<{ data: PublicMatchData[]; meta: ListMeta }>(\`/matches?page=\${page}\`, { actsAsDiscordId })`. Destructure { data, meta } — read BOTH (the {data} envelope gotcha; bare-reading data only would drop meta). Empty data -> existing plain-string empty reply, no components. Otherwise build embeds = data.flatMap(m => matchCard(m).embeds) (NO slice). If meta.last_page > 1, set components = [paginationButtons('match', meta.current_page, meta.last_page)] and include a "Page X of Y" indicator via t()/i18n (content string or an embed field). editReply with { embeds, components } (components omitted/empty when last_page === 1).
    - clan.ts list branch: same pattern with `/clans?page=${page}`, formatClanList(data) for the body, paginationButtons('clan', meta.current_page, meta.last_page) + page indicator when last_page > 1.
    - Define a shared `ListMeta` type ({ current_page; per_page; total; last_page }) in types/apiContracts.ts (or wherever PublicMatchData/ClanData live) and import it in both commands.
    - Page-indicator i18n: add the bot's locale string for "Page :page of :last" (follow the bot's existing i18n mechanism — if the bot has no i18n layer, use a single shared helper/constant with interpolation rather than scattering the literal; surface this choice in the SUMMARY). Per D-013, do not hardcode the same English literal in two files.
    Refactor the page-render into a small shared function per command (e.g. renderMatchListPage(page)) so 12-04's handler can call the identical builder. Run vitest green, then tsc --noEmit + eslint. Commit `feat(12-03): in-message pagination for /match list + /clan list`.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bot -> web GET /matches?page=N, /clans?page=N | bot forwards a page integer to the web API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-D | DoS | page query param | mitigate | page originates from a default (1) here; web index clamps ?limit and Laravel paginate bounds page to existing pages (out-of-range -> empty data, correct meta). Buttons rendered only within [1, last_page]. |
| T-12-03-I | Info disclosure | api.get { data, meta } | accept | List endpoints already return only public list data (PublicMatchData/ClanData) under the existing bot Sanctum token; no new fields exposed. |
</threat_model>

<verification>
- cd apps/bot && node_modules/.bin/vitest run (match + clan command specs green)
- cd apps/bot && node_modules/.bin/tsc --noEmit
- cd apps/bot && node_modules/.bin/eslint .
</verification>

<success_criteria>
- /match list and /clan list fetch ?page=N (default 1), read data + meta, render the page's items
- Prev/Next row + "Page X of Y" appear only when last_page > 1; empty/single-page render no buttons
- /match list no longer slices client-side; full bot suite green
</success_criteria>

<output>
Create `.planning/phases/12-notifications-bot-polish/12-03-SUMMARY.md` when done
</output>
