# Milestone v1.1: Completion

**Status:** ✅ SHIPPED 2026-06-04
**Phases:** 10-12
**Total Plans:** 17

## Overview

v1.1 Completion of Trenchwars — finishing the partially-built and deferred round-1-adjacent features so the platform's core workflows are fully fleshed out. Delivered as three sequential phases extending the v1.0 foundation: clan application submission flow (web + Discord), Swiss tournament auto-advance + ELO seeding + median Buchholz tiebreaker + stage-level GameMatchType override, and full notification-preferences UX + Discord bot list pagination.

Delivered across 17 plans (2026-06-03 → 2026-06-04) using GSD-driven autonomous planning + execution. 1370 Pest web tests + 238 bot Vitest tests passing at milestone close. All 10 v1.1 requirements (CLAN-01..04, TOUR-01..04, NOTF-01, BOT-01) flipped to Complete.

## Phases

### Phase 10: Clan applications

**Goal**: Users can apply to join a clan from the web and from Discord, and clan leaders control whether their clan accepts applications
**Depends on**: Phase 2 (ClanApplication model/observer already present), Phase 5 (bot `/clan apply` stub exists)
**Requirements**: CLAN-01, CLAN-02, CLAN-03, CLAN-04
**Plans**: 7 plans

Plans:

- [x] 10-01-PLAN.md — Schema (accepts_applications column + pending-per-clan partial unique index), Clan model cast, 3 typed exceptions
- [x] 10-02-PLAN.md — ClanApplicationService::apply() with the 3 eligibility guards + service test + web/bot i18n keys
- [x] 10-03-PLAN.md — Bot + web submit controllers, routes, and PHP tests (bot 201/422 + abilities, web feature)
- [x] 10-04-PLAN.md — CLAN-04 recruiting toggle surfaces (ClanData DTO, MyClan settings UI, Filament admin)
- [x] 10-05-PLAN.md — Bot wiring: /clan apply + clan_apply button → api.post, translateError extension, bot tests
- [x] 10-06-PLAN.md — Web "Apply to join" form on the clan show page + eligibility props + feature test
- [x] 10-07-PLAN.md — Phase close: full web+bot gate suite, SC traceability, REQUIREMENTS/ROADMAP updates

**Details:**
`accepts_applications` boolean column on clans with partial unique index preventing duplicate pending applications per clan. `ClanApplicationService::apply()` enforces three eligibility guards (clan open, not already active member, no pending duplicate). `POST /api/bot/clans/{slug}/applications` bot API endpoint (201/422) + `POST /clans/{slug}/apply` web route. Bot `/clan apply <slug>` command wired live via api.post (replaces stub redirect). `ClanShowController` adds three eligibility props (acceptsApplications / viewerIsActiveMember / viewerHasPendingApplication); `Clans/Show.vue` adds a `showApplyBlock`-gated Apply-to-join form. MyClan settings UI + Filament ClanResource for the recruiting toggle. Tests at phase close.

### Phase 11: Tournament depth

**Goal**: Swiss tournaments self-advance, standings use the correct tiebreaker, seeding uses actual player ratings, and stage configuration is more flexible
**Depends on**: Phase 6 (SwissGenerator, StandingsCalculatorService, BracketGenerator, TournamentStage all present)
**Requirements**: TOUR-01, TOUR-02, TOUR-03, TOUR-04
**Plans**: 5 plans

Plans:

- [x] 11-01-PLAN.md — Schema markers (clans.elo_rating/elo_matches_count, brackets.rated_at, standings.median_buchholz, stages.game_match_type_id) + model wiring + DTO field + 4 RED test scaffolds
- [x] 11-02-PLAN.md — Pure logic (TDD): EloRatingService (K=32, base 1500) + median Buchholz (Cut 1) in SwissStandingsCalculator
- [x] 11-03-PLAN.md — BracketAdvancementService hooks: Elo apply (rated_at-guarded, once per bracket) + Swiss auto-advance (idempotent) + by_rank seeding by elo_rating
- [x] 11-04-PLAN.md — TOUR-04 stage override: materialiser stage.game_match_type_id fallback + cross-game-scoped Filament Select + i18n
- [x] 11-05-PLAN.md — Public median Buchholz column + shared-types regen + phase-close full web/bot gate suite + TOUR traceability

**Details:**
Four additive schema columns: `clans.elo_rating` (base 1500) + `clans.elo_matches_count`, `brackets.rated_at` (idempotency guard), `tournament_standings.median_buchholz`, `tournament_stages.game_match_type_id`. `EloRatingService` (K=32 standard Elo, DB transaction, activity audit) updates both clan ratings after each bracket result. `SwissStandingsCalculator` adds Buchholz Cut-1 as the third tiebreaker (removes the lowest opponent score). `BracketAdvancementService` gains: Elo hook (rated_at-guarded to fire exactly once per bracket), Swiss auto-advance (generates next round when all current-round matches have results — idempotent, premature-completion guard). `by_rank` tournament seeding repointerd to `clan elo_rating DESC`. Stage-level `game_match_type_id` override in `BracketMatchMaterialiserService` using `stage override ?? tournament default`. Cross-game-scoped Filament Select on `StagesRelationManager`. Median Buchholz column visible on public Swiss standings tab. 1365 Pest web + 238 bot Vitest at phase close.

### Phase 12: Notifications & bot polish

**Goal**: Users have full control over which notifications they receive and how, and the Discord bot's list commands support pagination
**Depends on**: Phase 9 (UserNotificationPreference model + NotificationDispatcher present), Phase 5 (bot `/match list` + `/clan list` commands present)
**Requirements**: NOTF-01, BOT-01
**Plans**: 5 plans

Plans:

- [x] 12-01-PLAN.md — NOTF-01: notification-preferences nav link (discoverability) + end-to-end honor test
- [x] 12-02-PLAN.md — BOT-01: list_page customId + Prev/Next pagination button factory (TDD)
- [x] 12-03-PLAN.md — BOT-01: in-message pagination render in /match list + /clan list (page + "Page X of Y")
- [x] 12-04-PLAN.md — BOT-01: Prev/Next handler re-fetches page and interaction.update()s the same message
- [x] 12-05-PLAN.md — Phase close: full web + bot gate suites green; trace NOTF-01 + BOT-01 to Met

**Details:**
NOTF-01: Notification-preferences account-settings page surfaces all event-type × channel preference combinations (in-app / Discord DM / both / none). Nav link added for discoverability. `NotificationDispatcher` updated to honor per-user preferences from `user_notification_preferences`. End-to-end test confirms dispatcher skips channels blocked by user preference. BOT-01: `list_page` customId token added to `/match list` and `/clan list` interactions. `PaginationButtonFactory` produces Prev/Next component buttons (TDD). Pagination render in command responses shows page + "Page X of Y". Prev/Next button handler re-fetches the requested page via web API and calls `interaction.update()` to replace the same message in-place. 1370 Pest web + 238 bot Vitest at phase close.

---

## Milestone Summary

**Key accomplishments:**
- Clan-apply submission flow (web + Discord): full eligibility enforcement, three typed exceptions, bot `/clan apply` wired live
- Swiss auto-advance: next round generated automatically when all current-round results are recorded
- ELO seeding: `by_rank` tournament seeding uses actual clan ELO ratings (K=32, base 1500) from match results
- Median Buchholz tiebreaker: Cut-1 third tiebreaker added to Swiss standings, visible on public bracket view
- Stage-level `GameMatchType` override: admins can override match type per tournament stage
- Full notification-preferences UX: per event-type × channel control in account settings; dispatcher honors choices
- Discord bot list pagination: `/match list` + `/clan list` support Prev/Next navigation for multi-page results

**Test coverage at close:**
- 1370 Pest web tests
- 238 bot Vitest tests

**Requirements delivered:** CLAN-01, CLAN-02, CLAN-03, CLAN-04, TOUR-01, TOUR-02, TOUR-03, TOUR-04, NOTF-01, BOT-01 — all Complete

---

_For current project status, see .planning/ROADMAP.md_
